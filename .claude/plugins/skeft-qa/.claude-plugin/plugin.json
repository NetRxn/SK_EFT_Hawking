{
  "name": "skeft-qa",
  "description": "QA review agents + the autonomous-development harness for the SK_EFT_Hawking physics project. Stage-13 review agents: adversarial paper audit, sentence-level claims verification with chain-of-backing, figure review, and prose review (the fourth reviewer: reads a draft start to finish as a referee at its named venue and returns a restructuring instruction). Authoring skill paper-authoring shares its references/prohibited-patterns.md with prose-reviewer so a rule cannot drift between writing and review. Loop-durability dev-harness (keeps native /goal loops on-track across compactions): SessionStart re-inject + PreToolUse(AskUserQuestion) guard + SessionEnd marker-cleanup hooks; skills goal-prompt (launch+posture core), goal-dev (in-loop dev), sync, wave-close, harvest, debrief; commands orient, goal-guard, goal-end, reset-slot. v4.2 web-egress: research-scout web-recon agent (read-only sandbox) + PreToolUse(WebSearch|WebFetch) egress guard (denylist + scholarly-domain whitelist, fail-closed) + three-tier research ladder.",
  "author": {
    "name": "SK-EFT Hawking project"
  }
}
