{
  "description": "skeft-qa loop-durability hooks (v4.0): SessionStart re-inject + PreToolUse(AskUserQuestion) guard. Default-inert + fail-open.",
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "uv run --no-sync python \"${CLAUDE_PLUGIN_ROOT}/scripts/harness_reinject.py\" 2>/dev/null || true",
            "timeout": 15
          }
        ]
      }
    ],
    "PreToolUse": [
      {
        "matcher": "AskUserQuestion",
        "hooks": [
          {
            "type": "command",
            "command": "uv run --no-sync python \"${CLAUDE_PLUGIN_ROOT}/scripts/harness_question_guard.py\" 2>/dev/null || true",
            "timeout": 15
          }
        ]
      }
    ]
  }
}
