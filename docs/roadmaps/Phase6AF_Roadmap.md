# Phase 6AF — Strengthen the deferred 6AE analytic core (trace-distance metric → diamond norm)

**Status:** OPEN (2026-06-01). Bundle-target **D6 §6**. Closes the analytic frontier deferred by Phase 6AE (DONE) — the trace-norm triangle, mixed-state distinguishability (Uhlmann fidelity + Fuchs–van de Graaf), CPTP contractivity (data processing), and the diamond norm. This fully unblocks arbitrary-state certification. User greenlit full 6AF-1→5 via autonomous `/goal`, research risk accepted.

**Inherited (do NOT re-litigate):** pin v4.29.1/`5e932f97`; CONCRETE finite-dim route on `Matrix (Fin n) (Fin n) ℂ` (no Stinespring); `open scoped ComplexOrder` (+ `Matrix.Norms.L2Operator`/`MatrixOrder` where the C*-structure is needed). Kernel-pure `{propext, Classical.choice, Quot.sound}`; **NO sorry, NO new project-local axiom** (fence intractable steps as documented deferred lemmas with the precise blocker); no `maxHeartbeats`; Stage-9 per wave (root-import + ExtractDeps + update_counts); leak discipline; preemptive-strengthening checklist before each theorem (load-bearing statements, no tautologies).

## Build-on (shipped 6AE substrate — use by name)
`QuantumNetwork/MixedState.lean`: `IsDensityOperator`, `traceNorm A = ∑√eig(AᴴA)` (via `traceNormOf` on the PSD witness), `traceDist = ½‖ρ−σ‖₁`, structural lemmas; **`trace_cfc`** (`(hM.cfc f).trace = ∑(f(eig):ℂ)`), **`isHermitian_mul_self_eq_cfc_sq`**, **`map_eigenvalues_conjTranspose_mul_self`**, **`traceNorm_posSemidef`** (PSD⟹‖A‖₁=tr A — the linchpin), `traceNorm_density_eq_one`. `QuantumNetwork/DiamondNorm.lean`: `partialTrace` + `trace_partialTrace`, `choiMatrix`.

## TOEHOLDS (the FKLW spectral arsenal — the unlock; verify each via lean-lsp before use)
- `FKLW/GenericSUdDecreasingSortPartialSums.lean` — `partial_sums_nonneg_of_decreasing_traceless` (non-increasing + traceless ⟹ partial sums ≥0): the **Ky-Fan / majorization seed** for the Hermitian trace-norm triangle.
- `FKLW/GenericSUdSpectralLift.lean`, `GenericSUdHermitianDischargeFull.lean` — extract `H = U·diag(a)·Uᴴ` + lift through unitary conjugation: the **polar-decomposition substrate**.
- `FKLW/GenericSUdEigenvalueLinftyBound.lean` — Gershgorin `|λ(A)| ≤ ‖A‖_op`.
- `FKLW/GenericSUdTraceNorm.lean` — row-sum domination / norm-via-`Finset.sup` template.
- `FKLW/SpecialUnitaryTopology.lean` — `specialUnitaryGroup_isCompact` (sup-attainment); `SpecialUnitaryPathConnected.lean` — live `CStarMatrix`/`CStarAlgebra` instance on matrices.
- `DKMBootstrap/LinearFunctionals.lean` — convex/dual-cone closure (`ProperCone`/`PointedCone`) for sup-of-linear-functionals.
- `RTReplicaTrickOnMTC.lean` — sup-attainment witness pattern. `BHEntropyMicroscopic.lean` — entropy-of-state analytic style.

## Wave sequence (dependency-ordered; ship each kernel-pure, Stage-9 wired)
| Wave | Content | Toehold | Risk |
|---|---|---|---|
| **6AF-1** | **Hermitian trace-norm triangle** `‖A+B‖₁≤‖A‖₁+‖B‖₁` (A,B Hermitian) ⟹ `traceDist` genuine metric (triangle) + `traceDist ρ σ ∈ [0,1]` for density operators. Route: `‖H‖₁ = ∑|λ| = sup over Hermitian U, −I≤U≤I, of tr(HU)` (achieved at `sign(H)` via cfc), OR Ky-Fan partial sums. | FKLW `partial_sums_nonneg_of_decreasing_traceless` + spectral lift | **days — do FIRST, de-risk** |
| **6AF-2** | Operator `|A|` (`cfc √ (AᴴA)`) + **polar decomposition** `A=U|A|`; general trace-norm triangle; von Neumann trace inequality `|tr(AB)|≤∑σᵢ(A)σᵢ(B)` if needed for the dual char | FKLW spectral lift + `trace_cfc`/`map_eigenvalues_…` | ~1 wk |
| **6AF-3** | **Uhlmann fidelity** `F(ρ,σ)=(tr√(√ρ σ √ρ))²` + **Fuchs–van de Graaf** `1−F ≤ D ≤ √(1−F²)` (quantitative, both bounds) | `cfc √` of operator products | ~1 wk |
| **6AF-4** | CPTP channel (instantiate `CompletelyPositiveMap` on `CStarMatrix`/`Matrix` + trace-preservation) + **trace-distance CONTRACTIVITY** `D(Φρ,Φσ)≤D(ρ,σ)` (data processing) | FKLW C*-bridge + 6AF-2 | **hardest — 1–2 wk, genuine risk** |
| **6AF-5** | **`diamondNorm`** `‖Φ‖_◇ = sup_ρ ‖(Φ⊗id)ρ‖₁`: tensor channel `Φ⊗id`, bounded sup (compactness), norm axioms, submultiplicativity, Choi characterization | FKLW compactness + RTReplicaTrick sup-attainment + `choiMatrix` | ~1–2 wk |

**OUT:** infinite-dim / von Neumann algebra generality; SDP solver; private/product content; demand/GTM.

**DONE:** 6AF-1→5 proven (trace-distance metric + bounds, fidelity + FvdG, CPTP contractivity, diamond norm + properties) OR any genuinely-intractable step fenced as a documented deferred lemma with the precise blocker; all modules kernel-pure + root-imported + counts regenerated; D6 §6 + preprint extended (compile-clean); fresh-context Stage-13 FULLY CLEAN.

---

## ⭐ REMAINING FRONTIER (6AF-7/8/9) — blueprints, toehold inventory, fence discipline (READ BEFORE ANY FENCE)

**6AF-1→6 are DONE** (trace-norm triangle general+Herm; traceDist metric+[0,1]; operator modulus; Uhlmann fidelity foundation+symmetry; CPTP data-processing contractivity; Choi positivity; **and the full diamond DISTANCE metric** — `diamondDist` nonneg/≤1/symm/self=0/`le_diamondDist` LUB/`diamondDist_triangle`). Files: `MixedState.lean`, `CPTPChannel.lean`, `DiamondNorm.lean`, `DiamondNormSup.lean`, `FidelityBounds.lean`. The trace-norm/CPTP layer is now GENERALIZED to an arbitrary `[Fintype ι][DecidableEq ι]` index (so it instantiates at `Fin n × Fin n` for free).

**⚠️ FENCE-DISCIPLINE (hard, learned 2026-06-01):** THREE fences were drawn too wide this phase — the general triangle, the diamond-norm definition, and the diamond-distance triangle ALL turned out reachable with tools in hand once the perimeter was re-examined. **Therefore: do NOT fence any remaining item without (a) a fresh fan-out of 2–4 Explore/general-purpose agents over Mathlib + the project FKLW/spectral substrate to hunt toeholds, AND (b) a re-read of THIS section + the relevant file's blueprint, AND (c) a written, arithmetic- or grep-verified argument that the headline genuinely requires an absent Mathlib brick (not just "the assembly is long").** A fence is a LAST resort after that diligence, recorded with the precise blocker.

### Mathlib toehold inventory (deep-swept 2026-06-01 — present vs ABSENT)
PRESENT: `IsHermitian.eigenvectorBasis` (OrthonormalBasis), `mulVec_eigenvectorBasis`, `spectral_theorem`, `IsHermitian.cfc`+`CFC.abs`/`CFC.sqrt`; Frobenius norm (`Analysis/Matrix/Normed.lean`, `open scoped Matrix.Norms.Frobenius`; `frobenius_norm_def`, `frobenius_norm_mul`, `frobenius_norm_conjTranspose`); trace inner product `⟪x,y⟫=(y·M·xᴴ).trace` as a parametric `def` `toMatrixInnerProductSpace M hM` (instantiate at `M=1`/`PosSemidef.one`); Cauchy-Schwarz `norm_inner_le_norm`/`re_inner_le_norm`; Loewner order `scoped[MatrixOrder]` + `conjugate_le_conjugate_of_nonneg` + `PosSemidef.trace_nonneg` + `tracePositiveLinearMap`; Birkhoff/doubly-stochastic; `charpoly_mul_comm`/`charpoly_fromBlocks_zero₁₂`; `Real.sSup`/`csSup_le`/`le_csSup`/`Real.sSup_nonneg`/`Real.sSup_le`; `ProperSpace`+`isCompact_iff_isClosed_bounded`+`IsCompact.exists_sSup_image_eq` (EVT); `Continuous.matrix_trace`. ABSENT (verified): Schatten p-norms, matrix Hölder `‖AB‖₁≤‖A‖₂‖B‖₂`, von Neumann trace ineq, Ky Fan, polar decomposition, SVD-decomposition theorem, singular-value majorization (Schur-Horn/Lidskii), eigenvalue Weyl-monotonicity under Loewner, **continuity of eigenvalues/singular-values in matrix entries**, SDP duality.

### 6AF-7 — Fuchs–van de Graaf + `F ≤ 1` (proof IN HAND; entry shipped)
NO shortcut (verified): Frobenius-CS bounds `tr(√σ√ρ)` not the trace-NORM `F`; Weyl gives `νᵢ≤ρᵢ` which is insufficient. **Reachable via double-Cauchy-Schwarz** (spectral thm + CS×2 only): `M=√ρσ√ρ`, eigenpairs `(μᵢ,vᵢ)`; `F=∑√μᵢ` (shipped `sqrtFidelity_eq_sum_sqrt_eig`); `μᵢ=‖√σ√ρ vᵢ‖²`; with unit `wᵢ=√σ√ρvᵢ/√μᵢ` (0 if μᵢ=0), `√μᵢ=⟨√σwᵢ,√ρvᵢ⟩`; `F=∑⟨√σwᵢ,√ρvᵢ⟩≤√(∑‖√σwᵢ‖²)√(∑‖√ρvᵢ‖²)≤√(trσ)√(trρ)=1`. CLEANEST formalization: assemble columns into matrices `V=√ρ·U`, `Ĉ=` normalized `√σ√ρU`; then it's ONE Frobenius-CS `|tr(WᴴV)|≤‖W‖_F‖V‖_F` + `tr(σ·ĈĈᴴ)≤trσ` (with `ĈĈᴴ≤1`, `tr(σP)≤trσ` for `P≤1` via `tr(σ(1-P))≥0`) + `tr(VᴴV)=trρ=1`. ~10 sub-lemmas, all tools PRESENT. **FvdG bounds** `1−F≤D≤√(1−F²)`: separate argument after F≤1 (trace-dist↔fidelity coupling); sweep before fencing. Blueprint also in `FidelityBounds.lean` header.

### 6AF-8 — diamond-norm sup ATTAINMENT
`diamondDist` is already a metric; ATTAINMENT = the sup is a `max` (`∃ρ` optimal). Compactness half PRESENT (`ProperSpace`+EVT). Binding gap: **continuity of `ρ↦‖(Φ⊗id)ρ‖₁`** (the singular-value sum) in matrix entries — i.e. continuity of `traceNorm`. Route to BUILD it: `traceNorm A=∑√eig(AᴴA)`; eigenvalues of a Hermitian matrix are continuous in entries (roots of charpoly continuous; or via `Continuous` of the sorted spectrum). Sweep for charpoly-root / eigenvalue continuity toeholds before fencing. If continuity builds, attainment = `IsCompact.exists_sSup_image_eq` on the (closed+bounded) density set.

### 6AF-9 — Choi-SDP characterization of the diamond norm
Watrous: `‖Φ‖_◇` = an SDP over the Choi matrix. Needs SDP/convex-duality substrate (`ProperCone`/`PointedCone` in `DKMBootstrap/LinearFunctionals.lean` is a partial toehold). Deepest; sweep hard before fencing — a primal-only bound or a one-sided characterization may be reachable even if full SDP duality is not.

## Progress log
- **6AF-7 — 🚧 IN PROGRESS: Fuchs–van de Graaf + F≤1** (the genuine-infrastructure item). Deep 3-agent sweep (2026-06-01) CONFIRMED no shortcut: Mathlib at pin has NO Schatten norms / matrix Hölder / von Neumann trace ineq / polar decomp / SVD-decomposition / singular-value majorization (all grep-verified absent). The "light" routes provably bound the WRONG quantity (arithmetic-verified): Frobenius-CS gives `tr(√σ√ρ)≤1` (the trace, not the trace-NORM `F`); Weyl monotonicity `√ρσ√ρ≤ρ⟹νᵢ≤ρᵢ` is insufficient (`νᵢ≤ρᵢ`+`∑ρᵢ=1` does NOT give `∑√νᵢ≤1`, e.g. `(0.9,0.1)→1.27`). **BUT NOT BLOCKED** — found an elementary **double-Cauchy-Schwarz** proof using only spectral theorem + CS×2 (no Schatten/polar/SVD): `M=√ρσ√ρ` eigenpairs `(μᵢ,vᵢ)`, `wᵢ=√σ√ρvᵢ/√μᵢ` orthonormal, `√μᵢ=⟨√σwᵢ,√ρvᵢ⟩`, `F=∑√μᵢ≤√(∑‖√σwᵢ‖²)√(∑‖√ρvᵢ‖²)≤√(trσ)√(trρ)=1`. Blueprint recorded in `FidelityBounds.lean`. SHIPPED this round (kernel-pure, new `FidelityBounds.lean`): the ENTRY lemma **`sqrtFidelity_eq_sum_sqrt_eig`** (`F=∑√eigenvalues(√ρσ√ρ)`, via `conjTranspose_mul_self_sqrtFidelity`+`traceNormOf_congr`) + `posSemidef_sqrt_mul_mid_mul_sqrt`. Counts 773 mod / 10155 thm / 0 axiom / 0 sorry. REMAINING (next focused round — the monolithic EuclideanSpace core, ~6 sub-lemmas, all tools present `eigenvectorBasis`/`mulVec_eigenvectorBasis`/`norm_inner_le_norm`): `μᵢ=‖√σ√ρvᵢ‖²`, the `wᵢ` construction + orthonormality, `√μᵢ=⟨√σwᵢ,√ρvᵢ⟩`, the two CS steps, `∑‖√ρvᵢ‖²=trρ`, partial-sum≤trace; then FvdG (its own trace-dist↔fidelity coupling argument). Items 6AF-8 (attainment) + 6AF-9 (Choi-SDP) still queued.
- **6AF-6 — ✅ DIAMOND DISTANCE BUILT (reopened from the 6AF-5 fence)** (2026-06-01): after a 4-agent toehold sweep, the diamond-norm fence was found to be drawn around the wrong perimeter — **define+bound** is reachable; only **attainment** is genuinely deep. Recovery in 3 sub-waves:
  - **6AF-6a** (`bc6e6509`): generalized `MixedState.lean` + `CPTPChannel.lean` from `{n:ℕ}, Matrix (Fin n)…` to `{ι}[Fintype][DecidableEq], Matrix ι ι ℂ` (every Mathlib lemma called was already index-generic; the Hermitian dilation simplified, dropping the `finSumFinEquiv` reindex). Downstream-safe, kernel-pure, warning-clean. This dissolves the product-index blocker — the trace norm now instantiates at `Fin n × Fin n` for free.
  - **6AF-6b** (new `DiamondNormSup.lean`): **`tensorKraus K k = Kₖ ⊗ I`** + **`isKrausChannel_tensorKraus`** (`Φ⊗id` is CPTP, via `conjTranspose_kronecker`/`mul_kronecker_mul`/`one_kronecker_one` + the new `sum_kronecker_left`). **`diamondDist K₁ K₂ = sSup {D((Φ₁⊗id)ρ,(Φ₂⊗id)ρ) : ρ density}` = ½‖Φ₁−Φ₂‖_◇`** defined via `Real.sSup` (boundedness only, NO compactness). Proven: **`diamondDist_nonneg`** (`Real.sSup_nonneg`), **`diamondDist_le_one`** (each output is a density op ⟹ `traceDist∈[0,1]` ⟹ `Real.sSup_le`), **`diamondDist_comm`** (symmetry), **`diamondDist_self`** (`=0`). A genuine `[0,1]`-valued distinguishability measure — the diamond norm in operational form, NOT fenced. Counts 772 mod / 10149 thm / 0 axiom / 0 sorry, kernel-pure, no native_decide/maxHeartbeats.
  - 🔴 Now-narrowed FENCE (no sorry/axiom, in `DiamondNormSup.lean`): **attainment** of the sup (blocked on continuity of the singular-value sum in matrix entries — verified absent at pin; compactness half IS present), the **triangle inequality** (metric-on-channels), and the **Choi-SDP characterization** (Watrous). The fence is now only the genuinely-deep analytic frontier, not the definition.
- **6AF-5 — ✅ CHOI POSITIVITY DONE, diamond-norm sup fenced** (channel–state duality): **`choiMatrix_krausMap_posSemidef`** (`DiamondNorm.lean`, kernel-pure) — the Choi/Jamiołkowski matrix of a Kraus channel is PSD (the ⟹ direction of Choi's theorem, complete-positivity ⟹ PSD Choi). Proven concretely: `choiMatrix (krausMap K) = ∑ₖ wₖ wₖᴴ` with `wₖ(p) = Kₖ(p.2,p.1)`, a sum of rank-one PSD outer products (`posSemidef_self_mul_conjTranspose` + `posSemidef_sum`), via `single`-matrix entrywise expansion. Also refreshed the **stale** 6AE deferred-frontier doc in `DiamondNorm.lean` (steps 3/4/5 now PROVEN by 6AF-1→4). Counts 771 mod / 10142 thm / 0 axiom / 0 sorry, kernel-pure. 🔴 **FENCED (documented deferred, NO sorry/axiom):** the diamond *norm* proper `‖Φ‖_◇ = sup_ρ ‖(Φ⊗id)ρ‖₁` — sup boundedness/attainment (needs concrete-matrix compactness of the density set + tensored-trace-norm continuity, absent at pin), the tensor channel `Φ⊗id` over the product index `Fin n × Fin n'` (trace norm is `Fin n`-bound; needs a general-index refactor or `Fin n × Fin n' ≃ Fin(n·n')` layer), and the norm axioms. Fenced in `DiamondNorm.lean` §"Deferred frontier — the diamond norm proper". The load-bearing trace-distance + CPTP-contractivity certification core (6AF-1→4) is fully shipped.
- **6AF-4 — ✅ DONE (the flagged-hardest wave, NOT fenced)** (CPTP contractivity / data processing): new `QuantumNetwork/CPTPChannel.lean` (kernel-pure, root-imported). **`krausMap K ρ = ∑ₖ Kₖ ρ Kₖᴴ`** + **`IsKrausChannel K = (∑ₖ Kₖᴴ Kₖ = 1)`** (trace-preservation). Structural: `krausMap_add`/`krausMap_sub` (linearity), **`krausMap_posSemidef`** (PSD-preservation via `conjTranspose_mul_mul_same` + `posSemidef_sum`), `krausMap_isHermitian`, **`trace_krausMap`** (`tr Φρ = tr ρ`, via `trace_mul_cycle` + `∑KᴴK=1`), **`krausMap_isDensityOperator`** (density→density). Headline: **`traceNorm_krausMap_le`** (`‖Φ(A)‖₁ ≤ ‖A‖₁`, A Hermitian) ⟹ **`traceDist_krausMap_le`** (`D(Φρ,Φσ) ≤ D(ρ,σ)` — full data-processing inequality). 🔑 KEY: proven via the **positive/negative-part split** (`A=A⁺−A⁺`, `traceNorm_hermitian_eq_pos_add_neg`: `‖A‖₁=trA⁺+trA⁻`) + general triangle (6AF-2) + PSD/trace preservation — sidesteps the dual-norm `‖A‖₁=sup tr(PA)` that the 6AE notes flagged as the blocker. The 6AE-flagged "hardest, genuine risk" wave shipped clean, no fence. Counts 771 mod / 10140 thm / 0 axiom / 0 sorry, kernel-pure. NEXT: 6AF-5 (diamond norm sup).
- **6AF-3 — ✅ FOUNDATION DONE, FvdG fenced** (Uhlmann fidelity): **`psdSqrt M = √M`** (`cfc Real.sqrt`, PSD+Hermitian) + **`psdSqrt_mul_self`** (`√M·√M = M`, via new `cfc_congr_eig`: cfc depends only on spectral values + `Real.mul_self_sqrt`). **`sqrtFidelity ρ σ = ‖√σ·√ρ‖₁`** (root Uhlmann fidelity) + `fidelity = sqrtFidelity²` (Jozsa). HONESTY: **`sqrtFidelity_eq_trace_sqrt`** + **`conjTranspose_mul_self_sqrtFidelity`** (`(√σ√ρ)ᴴ(√σ√ρ) = √ρσ√ρ`) prove the def IS `(tr√(√ρσ√ρ)).re` — the literal Uhlmann form, not an ad-hoc substitute. Properties: `sqrtFidelity_nonneg`/`fidelity_nonneg`; **`sqrtFidelity_self`/`fidelity_self`** (`F(ρ,ρ)=1` for density ops); **`sqrtFidelity_comm`/`fidelity_comm`** (symmetry, via new **`traceNorm_conjTranspose`** `‖Aᴴ‖₁=‖A‖₁` = cospectrality `charpoly_mul_comm`). Counts 770 mod / 10131 thm / 0 axiom / 0 sorry, kernel-pure. 🔴 **FENCED (documented deferred, NO sorry/axiom):** Fuchs–van de Graaf `1−F ≤ D ≤ √(1−F²)` + the range upper bound `F ≤ 1` (`‖√σ√ρ‖₁ ≤ 1`). PRECISE BLOCKER: both need machinery absent at pin — `F≤1` is the Schatten Hölder/Cauchy-Schwarz `‖AB‖₁ ≤ ‖A‖₂‖B‖₂` (no Schatten-2 norm, no matrix Hölder in Mathlib); FvdG additionally needs the trace-distance↔fidelity spectral relationship (Uhlmann purification / joint spectral argument — no concrete substrate). Multi-week from-scratch (the Schatten-2 layer alone). Fenced in `MixedState.lean` §"Deferred frontier (6AF-3)". NEXT: 6AF-4 (CPTP contractivity).
- **6AF-2 — ✅ DONE** (operator modulus + general triangle): **`absOp A = √(AᴴA)`** (`cfc Real.sqrt` on `AᴴA`) + `absOp_isHermitian`/`absOp_posSemidef` + **`traceNorm_eq_trace_absOp`** (`‖A‖₁ = (tr|A|).re`) + `traceNorm_absOp`. Then the **general (non-Hermitian) trace-norm triangle** `traceNorm_triangle` (`‖A+B‖₁ ≤ ‖A‖₁+‖B‖₁`, NO Hermitian hypothesis) — NOT fenced; proven via the **Hermitian dilation** `dilate X = [[0,X],[Xᴴ,0]]` reindexed to `Fin(n+n)`: `dilate_isHermitian` + `dilate_add` (linearity) + **`traceNorm_dilate`** (`‖dilate X‖₁ = 2‖X‖₁`) reduce it to the proven Hermitian triangle. Keystone infra: **`sqrtRootSum p = ∑√(Re roots)`** + `traceNormOf_eq_sqrtRootSum` (trace norm is a function of the charpoly alone, via Mathlib `IsHermitian.roots_charpoly_eq_eigenvalues`) + `sqrtRootSum_pow_two` (`sqrtRootSum(p²)=2·sqrtRootSum p` via `roots_pow`) + `charpoly_dilate_block` (`(BᴴB).charpoly = (XᴴX).charpoly²` via `charpoly_fromBlocks_zero₁₂` + `charpoly_mul_comm` cospectrality of `XXᴴ`/`XᴴX`). The 6AE blocker ("Mathlib lacks von Neumann/Ky-Fan/polar") was bypassed entirely — the dilation+charpoly route needs none of them. Counts 770 mod / 10115 thm / 0 axiom / 0 sorry, kernel-pure. NEXT: 6AF-3 (Uhlmann fidelity + Fuchs–van de Graaf).
- **6AF-1 — ✅ DONE** (the trace-distance metric): **`traceNorm_hermitian_triangle`** (`‖A+B‖₁≤‖A‖₁+‖B‖₁` for Hermitian A,B — the hard core), **`traceDist_triangle`** (genuine metric), **`traceDist_mem_Icc`** (`∈[0,1]` for density ops). Route: NOT the dual-sup but the **positive-part decomposition** — `‖H‖₁=2·eigPosSum−tr` (`traceNorm_hermitian_eq`) + `eigPosSum` subadditivity (`eigPosSum_add_le`) via the projection bound `re_trace_proj_mul_le_eigPosSum` (crux: `re_trace_proj_mul_posSemidef_nonneg` = `Re tr(Q·S)≥0` for projection Q + PSD S, via `tr(QS)=tr(QᴴSQ)`+`conjTranspose_mul_mul_same`+`trace_nonneg`). Support: `cfc_id`/`cfc_mul`/`cfc_sub`/`cfc_isHermitian`/`cfc_posSemidef`/`posPart`/`negPart`/`posProj`. Did NOT need the FKLW Ky-Fan seed — the cfc+PSD-congruence route was cleaner. Counts 770 mod / 10101 thm / 0 axiom / 0 sorry, all kernel-pure. **6AF-1 metric + bounds COMPLETE.** NEXT: 6AF-2 (operator |A| / polar / general non-Hermitian triangle).
- **6AF-1 (in progress) — reduction shipped** (`MixedState.lean`, kernel-pure): `traceNorm_hermitian` (`‖H‖₁ = ∑|eigᵢ|`, via the shipped `map_eigenvalues_conjTranspose_mul_self` + `Real.sqrt_sq_eq_abs`); `eigPosSum hA := ∑ max(eigᵢ,0)`; **`traceNorm_hermitian_eq`** (`‖H‖₁ = 2·eigPosSum − tr H`, from `|x|=2max(x,0)−x`). This **reduces the Hermitian trace-norm triangle to subadditivity of `eigPosSum`**. Counts 770 mod / 10079 thm / 0 axiom / 0 sorry. REMAINING for 6AF-1: `eigPosSum` subadditivity `eigPosSum(A+B) ≤ eigPosSum A + eigPosSum B` — route: positive-eigenprojection `P_H := cfc (indicator>0) H` gives **achievement** `eigPosSum H = tr_re(P_H·H)` (via cfc multiplicativity + `trace_cfc`, since `indicator·id = max(·,0)` on the spectrum) and the **bound** `tr_re(P·H) ≤ eigPosSum H` for any projection P (via spectral theorem: `tr(PH)=∑λᵢpᵢ`, `pᵢ=diag(UᴴPU)∈[0,1]`, so `∑λᵢpᵢ ≤ ∑λᵢ⁺`). Then triangle = subadditivity + trace-additivity via `traceNorm_hermitian_eq`; then `traceDist` metric + `∈[0,1]`.
  - **Bound machinery SHIPPED** (cleaner route than the diagonal-∈[0,1] argument): `cfc_isHermitian`, `cfc_posSemidef` (cfc of `f≥0` is PSD — for posPart/negPart, also reused in 6AF-3), and the crux **`re_trace_proj_mul_posSemidef_nonneg`** (`0 ≤ Re tr(Q·S)` for projection Q + PSD S, via `tr(QS)=tr(QᴴSQ)` cyclic + `conjTranspose_mul_mul_same` PSD + `trace_nonneg`). Counts 770/10085/0/0. REMAINING (all pieces now in hand): `posPart/negPart M := cfc(max(±·,0))M` (PSD via cfc_posSemidef) + `M = posPart−negPart` (cfc_sub) + `eigPosSum=Re tr(posPart)`; then bound `Re tr(P·A) ≤ eigPosSum A = Re tr((I−P)·posPart) + Re tr(P·negPart) ≥ 0` (both terms via the crux lemma, P & I−P projections); then subadditivity (achievement + bound×2) → `traceNorm_hermitian_eq` triangle → traceDist metric + [0,1].
  - **Achievement half SHIPPED**: `cfc_id` (`cfc(x↦x)M=M`), `cfc_mul` (`cfc f·cfc g = cfc(f·g)` — reusable for 6AF-2/3), **`eigPosSum_eq_re_trace_posProj`** (`eigPosSum = Re tr(P_H·M)`, `P_H=cfc(𝟙_{x>0})M`, since `𝟙_{x>0}·id = max(·,0)`). Counts 770/10082/0/0. REMAINING: the **bound** `Re tr(P·A) ≤ eigPosSum A` for any projection P — route: `tr(PA)=∑ Re(Q_{ii})λᵢ`, `Q=UᴴPU` (spectral + `trace_mul_cycle`), `Q_{ii}=star(colᵢU)⬝P⬝colᵢU ∈[0,1]` (P PSD ⟹ ≥0 via `re_dotProduct_nonneg`; I−P PSD ⟹ ≤‖colᵢU‖²=1), so `∑Re(Q_{ii})λᵢ ≤ ∑max(λᵢ,0)`. Then subadditivity (apply bound to A,B with P=P_{A+B}) → triangle → traceDist metric/bounds.
