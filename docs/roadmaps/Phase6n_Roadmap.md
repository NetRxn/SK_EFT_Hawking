# Phase 6n: SK-EFT Foundational Backing + Structural Compression — First-Mover Tracks

## Technical Roadmap — May 2026

*Prepared 2026-05-04 | **New phase, structurally parallel to Phase 6m.** Sources: `temporary/working-docs/brainstorm/20260504-GUToE/Phase6n-6z-GUToE-Brainstorm_1.md` (Session 1 + Session 2 + Session 3 synthesis); 8 deep-research returns at `Lit-Search/_Exploratory/`; the G10 Appendix at `Lit-Search/_Exploratory/Appendix- Re-assessment of Deferred Tracks for SK_EFT_Hawking Phase 6n+.md` reversing 3-of-4 deferred sub-tracks; Itô-explore agent findings on Mathlib4 + Degenne brownian-motion repo state.*

**Trigger condition (no gate — autonomous):** Phase 6n can dispatch any time. It is independent of Phase 6m closure and runs in parallel with active Phase 7a–7g bundle work.

**Status (2026-05-05 Session 10 close):** Sessions 4–10 have shipped substantive Lean + Python lifts across Wave 1a (Lean substrate + Path B numerical verdict + δ⁴–δ⁷ formulas + Path A Stage 1 + **Session 10 Stage 2 γ_1 = 3/(640π) Beliaev LO derivation in Python**), Wave 1b (primary-source SymTFT audit + 4 stages of discrete-sector predicates and cross-bridges + Session 9 cross-track unification via SecondOrderProjection + **Session 10 Stage 5 project-local Witt-class infrastructure via WittClass.lean**), Wave 1c (AS reformulation memo working doc), Wave 2a (Glorioso–Liu Lean skeleton + Stage 2–3 + Stage 2–3b KMS framework + R1 LIFTED + Session 7 load-bearing deepening + Session 9 SecondOrderProjection extending SKEFTAxioms to second derivative order), Wave 2b (7 QuantumCrooks/ modules + parametric + concrete quantum no-go + **Session 10 ConcreteComplex.lean ℂ-extension**), Wave 2c (3 modules + Stage 2-3 substantive biconditional + Stage 4 substrate-level GC bridge Session 8), Wave 2d (substrate bundle + Bool-projection biconditional + Stage 4 SKEFTHorizonBridge Session 7), and I3 bundle infrastructure.

**Session 10 (2026-05-05) — Three substantive lifts SHIPPED.** (1) **Wave 1a Path A Stage 2 γ_1 Beliaev LO derivation:** `src/resurgence/bdg_self_energy.py` rewritten Stage-1-skeleton → Stage-2-implementation; γ_1 = 3/(640π) ≈ 1.4924 × 10⁻³ pinned per literature anchor (Beliaev 1958, Pitaevskii-Stringari §4.4, Liu-Fukuyama PRB 31, 175 (1985)); decomposition γ_1 = (1/30) × (9/64) / π = 9/(1920π) = 3/(640π); SciPy verification of kinematic integral ∫₀^1 u²(1-u)² du = 1/30; convention reconciliation (genuine Beliaev δ_diss is order g², clarifying Path B heuristic was at wrong order); 14 passing tests + 3 Stage-3+ skip-marked. (2) **Wave 1b Stage 5 project-local Witt-class infrastructure:** NEW module `lean/SKEFTHawking/SymTFTAudit/WittClass.lean` (~12 substantive theorems + 5 defs); `WittInvariant := ZMod 24` with AddCommGroup + DecidableEq + Fintype + `fromChiralCentralCharge : ℤ →+ WittInvariant` AddMonoidHom; Schellekens chain LIFTED to Witt-invariant level via `chiralCentralCharge_wittTrivial_iff_three_dvd_N_f`; Standard Model 3-generation concrete witness; falsification witnesses for N_f = 1, 2; SymTFT bridge `stage5_chiralCentralCharge_witt_bridge`; full Witt-equivalence relation infrastructure; `CrossBridges.lean` extended with `stage5_witt_grade_partition`. **Mathlib upstream-PR target retained** — full MTC + Witt-group infrastructure remains a multi-year community project; this project-local form captures the chiral-central-charge piece (closing the chiralCentralChargeMod24 ↔ Witt-class biconditional at predicate + abelian-group level). (3) **Wave 2b Stage 2-3 ℂ-extension:** NEW module `lean/SKEFTHawking/QuantumCrooks/ConcreteComplex.lean` (~14 substantive theorems + 10 defs); `Matrix (Fin 2) (Fin 2) ℂ` form of the canonical Perarnau-Llobet substrate; σ_y Hamiltonian (purely imaginary off-diagonal); ℂ↔ℝ cross-bridge; ℂ-form quantum no-go `perarnau_llobet_no_go_complex` (MCP-proven, zero Aristotle). **Lake build 8538 jobs** (+2 vs Session-9 8536). All three lifts: zero Aristotle, zero new axioms, zero new sorry. Verlinde-vs-Jacobson distinction preserved. **Session 9 (2026-05-05) — Wave 2a × Wave 1b cross-track unification SHIPPED:** new module `lean/SKEFTHawking/GloriosoLiu/SecondOrderProjection.lean` (9 substantive theorems + `SKEFTAxiomsExt` structure + 7 predicate defs) bridges Track 2 foundational (`SKEFTAxioms` on `SKAction`) to Track 1 structural (`KMSParityAlternationCompatible` on `SKActionExt`) via the substantive proof-body chain `SKEFTAxiomsExt → fullSecondOrder_uniqueness (Aristotle run c4d73ca8) → combined_positivity_constraint (Aristotle run c4d73ca8) → γ_{2,1}+γ_{2,2}=0 → KMSParityAlternationCompatible stage2Verdict`. Headline theorems: `combined_positivity_of_parity_zero` (converse of Aristotle's combined_positivity_constraint, establishing the biconditional `positivity_ext ↔ γ_{2,1}+γ_{2,2}=0`); `SKEFTAxiomsExt_for_combined_parity_zero` (substantive non-trivial existence witness with explicit 14-entry FullSecondOrderCoeffs embedding); `SKEFTAxiomsExt_yields_combined_uniqueness` (load-bearing projection); `SKEFTAxiomsExt_yields_parity_alternation` (load-bearing parity-alternation theorem); `SecondOrderProjection_bridges_to_KMSParityAlternation` (cross-track unification statement). Verlinde-vs-Jacobson distinction preserved at every Lean statement. Lake build 8535 → 8536 jobs clean (+1 SecondOrderProjection module). MCP-driven, zero Aristotle. Zero new axioms (only `propext, Classical.choice, Quot.sound`). **Session 8 (2026-05-05) — TWO substantive lifts:** (i) **Wave 2c Stage 4 GC bridge** new module `SKEFTGallavottiCohen.lean` (7 substantive theorems); (ii) **Wave 1b Stage 4c third cross-bridge** extending `SymTFTAudit/CrossBridges.lean` from 2 to 3 substantive bridges (closing Stage-4 asymmetry).

**User direction reaffirmed (2026-05-04 Session 5 close):** **Push forward on math/physics/infrastructure (6n + likely 6o) before drafting manuscripts or integrating into ≤ Phase 6m work.** Writing/verification overhead is non-trivial; the ≤ Phase 6m product surface is fairly stable. Bundle absorption (the D.2 / D.3 events listed below) is **deferred until the full Phase 6n + 6o picture lands**, at which point the absorption work runs as one coherent pass rather than incrementally per wave. The two D.3 user-auth pre-drafts (Wave 2a I1 reframing, Wave 2d D3+L3 reframing) **exist as working docs** at `temporary/working-docs/phase6n/` but are **on-hold for user review** — neither blocks further math/Lean work in Phase 6n.

**Entry state (2026-05-04 Inventory_Index snapshot):** ~5229 substantive theorems / ~243 modules / 0 sorry / 1 axiom. Phase 6m FULLY CLOSED at Lean-formalization scope (R1–R6 + strengthening + cross-module proof-chain). Phase 7 in flight: 13 of 13 bundles reviewer-triple-closed at GREEN (I1, I2, L1, L2, L3, D1, D2, D3, D4, D5, E1, E2, F); flagship F all-12-section draft shipped HEAD `9fabdc8` (Phase 7c close). Phase 7 in stable hold pending Phase 6n + 6o math closure per user direction.

**Anchors carried forward into Phase 6n:**
- `FirstOrderKMS` (Phase 1) — Aristotle 4-of-9 productive-value disproof; the anchor case study Phase 6n.1 lifts to "axiom replaced by deeper structure that explains the disproof."
- Phase 6e Sakharov-criterion biconditional (Λ_J = Λ_HK on ³He-A; falsified on FLS BEC) — substrate for Phase 6n.7's horizon-Crooks reformulation.
- Phase 6m Track C JTGR survivors (M1, M2/M7, M3 Exp/ArcTanh, M4, M9; M8 conditional) — Sakharov-class assignment carries into 6n.7.
- Phase 5o Wave 5 lean-tensor-categories library (Pivotal → Spherical → Balanced → Ribbon → Semisimple → Fusion → Modular; 114 thms) — substrate for Phase Wave 2a G9 SymTFT lift.
- `δ_disp / δ_diss / δ^(2) / δ^(3)` SK-EFT gradient-expansion coefficients in `formulas.py` — substrate for Phase Wave 1a G2 resurgence.
- Phase 6e a_n + Phase 6m Sakharov tr(I) + Phase 5z Goldstino zero modes — substrate for Phase Wave 1c G8-W1 AS memo (the heat-kernel ↔ Atiyah-Singer dictionary already implicit).
- `WAVE_EXECUTION_PIPELINE.md` 14-stage process; `BUNDLE_LIFT_PROCEDURE.md`; `LATE_PHASE6_ABSORPTION_PROTOCOL.md` (D.2 / D.3 / D.4 branches).

**Thesis.** Phase 6n loads the program's deepest unification-leverage tracks identified in the Session-1 conspicuous-gaps catalog and refined by Session-2 deep-research returns. **Two parallelizable Tracks ship 7 waves:**

- **Track 1 — Structural unification leverage** (3 waves): **Wave 1a** G2 Resurgence; **Wave 1b** G9 SymTFT audit; **Wave 1c** G8-W1 Atiyah-Singer reformulation memo. Compresses existing program content into deeper structural objects. First-mover where applicable.
- **Track 2 — Foundational backing for fluctuation-theorem content** (4 waves): **Wave 2a** G10-6n.1 Glorioso-Liu axiomatic; **Wave 2b** G10-QCrooks-α productive-value Aristotle wave; **Wave 2c** G10-6n.3 Crooks-on-analog-Hawking; **Wave 2d** G10-6n.7 Sakharov ↔ horizon-Crooks reformulation. Replaces FirstOrderKMS-class anchors with deeper axiomatic + adds new falsifiable IR constraints.

Downstream Phase 6o picks up the heavier tracks (G3 boostless-soft, G4-Kerr-Schild, G1 Schellekens chain + NO-GO writeup, G10-ETH-α, Itô + LDP-α/β as I3 community contribution pending user authorization).

**Naming-convention note (Session-5 rename).** Earlier working docs, brainstorm sessions, and memory entries refer to the same waves by Greek-letter labels. The Tracks/Waves convention here matches Phase 6m and is keyboard-friendly; the historical Greek labels remain in working docs at `temporary/working-docs/phase6n/` and in the MEMORY.md index. Cross-reference table:

| New label | Historical (Greek) | Codename |
|---|---|---|
| Wave 1a | 6n.α | G2 Resurgence |
| Wave 1b | 6n.β | G9 SymTFT audit + GD-4-factor |
| Wave 1c | 6n.η | G8-W1 Atiyah-Singer memo |
| Wave 2a | 6n.γ | G10-6n.1 Glorioso-Liu axiomatic |
| Wave 2b | 6n.δ | G10-QCrooks-α |
| Wave 2c | 6n.ε | G10-6n.3 Crooks-on-analog-Hawking |
| Wave 2d | 6n.ζ | G10-6n.7 Sakharov ↔ horizon-Crooks |

Sub-wave numbering (e.g., Wave 1a.1, 1a.2, 1a.3, 1a.4) replaces the Greek sub-wave labels (6n.α.1, etc.).

**Project rule reaffirmed (Session-3 user direction 2026-05-04):** **No PM / time / phase-cost estimates anywhere in this roadmap.** Track readiness by content state (sorry count, reviewer-triple status, bundle absorption gate), not calendar.

---

> **AGENT INSTRUCTIONS — READ BEFORE ANY WORK:**
>
> 1. **Mandatory project bootstrap** per `CLAUDE.md` Mandatory References list. Read in order: WAVE_EXECUTION_PIPELINE → Inventory_Index → README → Lean Development Optimization → Aristotle reference doc.
> 2. **Read this roadmap end-to-end** before claiming any wave assignment.
> 3. **Critical predecessor modules — read source directly:**
>    - `lean/SKEFTHawking/FirstOrderKMS.lean` (Phase 1) — the productive-value anchor for 6n.1.
>    - `lean/SKEFTHawking/SakharovCriterion.lean` + Phase 6m Track C JTGR modules — substrate for 6n.7.
>    - `lean/SKEFTHawking/Substrate/HeatKernel.lean` + Phase 6e a_n / Phase 5z Goldstino content — substrate for Wave 1c AS memo.
>    - `Lit-Search/_Exploratory/Phase 6n+ Foundational Backing Assessment- Non-Equilibrium Fluctuation Theorems and Stochastic Thermodynamics, Formally Verified.md` — the original G10 DR; §5 has the Glorioso-Liu theorem-signature sketch for 6n.1; §7 has the Crooks-on-analog-Hawking framing for 6n.3; §8 has the Sakharov ↔ horizon-Crooks structure for 6n.7.
>    - `Lit-Search/_Exploratory/Appendix- Re-assessment of Deferred Tracks for SK_EFT_Hawking Phase 6n+.md` — the calibration-rule appendix; QCrooks-α track structure §5; Itô deferral analysis §3.
>    - `Lit-Search/_Exploratory/Resurgence Theory and Schwinger–Keldysh EFT.md` — substrate for Wave 1a.
>    - `Lit-Search/_Exploratory/SymTFT, Higher-Form, and Non-Invertible Symmetries Applied to the SK-EFT Hawking Program- A Structural Audit.md` — substrate for Wave 1b; arXiv:2507.05350 (Schäfer-Nameki et al.) is the primary-source-audit target.
>    - `Lit-Search/_Exploratory/Atiyah–Singer Index Theorems as a Unifying Organizational Tool .md` — substrate for Wave 1c.
>    - `Lit-Search/_Exploratory/On-Shell Methods, Soft Theorems, and Spinor-Helicity Amplitudes for Dissipative Emergent Gauge : Gravity Sectors.md` — out-of-scope for Phase 6n (deferred to Phase 6o); read only if the wave's natural extension brushes against it.
> 4. **`LATE_PHASE6_ABSORPTION_PROTOCOL.md` — load before any bundle-touching work.** Each Phase 6n wave's bundle absorption is pre-classified below as D.2 / D.3 / D.4 per the protocol's branch decision matrix. **Two waves carry mandatory user-authorization gates before drafting starts:** Wave 2a (Phase 1 FirstOrderKMS reframing in I1) and Wave 2d (Phase 6e + 6m JTGR reformulation in D3 + L3).
> 5. **Apply preemptive-strengthening checklist** per `WAVE_EXECUTION_PIPELINE.md` Stage 3a + the five questions in `CLAUDE.md` "Preemptive-strengthening discipline" section. Do not skip the post-wave ruthless review either.
> 6. **Do not delegate Lean theorem proving to subagents.** MCP loop is the default tooling. Aristotle is fallback only after MCP-loop exhaustion + decomposition + user authorization.
> 7. **Bundle source freshness:** every wave that ships content into a drafted bundle must close with `validate.py --check bundle_source_freshness` re-run + Stage F reviewer-triple re-invocation per `LATE_PHASE6_ABSORPTION_PROTOCOL.md` Stage F.
> 8. **No PM / time estimates** anywhere — by user direction. Ship status by content state.

---

## Wave catalog — Shape D (Session-3 finalized; Session-5 status)

Seven waves across two parallelizable Tracks. Five core waves run in parallel (no inter-dependencies until reformulation closures); two reformulation closures (Wave 2c, Wave 2d) sequence after Wave 2a closes.

**Status legend:**
- ✅ **SHIPPED** — Lean / numerical / memo deliverables committed; bundle absorption deferred per Session-5 user direction.
- 🟡 **IN-PROGRESS** — partial deliverables shipped; remaining sub-stages identified.
- 📝 **WORKING DOC** — Stage-1 substrate-analysis or audit draft exists; no Lean yet.
- ⏳ **NOT STARTED** — substrate ready; awaiting dispatch.

| Wave | Codename | Status | Bundle absorption | Branch | User-auth gate |
|---|---|---|---|---|---|
| **Track 1 — Structural unification leverage** | | | | | |
| **Wave 1a** | G2 Resurgence | 🟡 **IN-PROGRESS** (1a.1 + 1a.2 + 1a.4 SHIPPED; 1a.3 Path B verdict shipped; 1a.3 Path A Stage 1 skeleton SHIPPED Session 6; 1a.3 Path A Stage 2 γ_1 Beliaev LO SHIPPED Session 10; 1a.3 Path A Stage 3 γ_2 NLO kin-disp SHIPPED Session 11; 1a.3 Path A Stage 3 Lean lift SHIPPED Session 12; **1a.3 Path A Stage 4a STRUCTURAL ENVELOPE THEOREM SHIPPED Session 13 (2026-05-05)**: pivoted from explicit (multi-week) γ_2^(loop) computation to substantive envelope theorem. NEW module `lean/SKEFTHawking/Resurgence/LoopEnvelope.lean` (~5 substantive theorems): `IsGeometric.add` (sum of two geometric sequences is geometric with rate `max r₁ r₂`); `kinDispSeq_add_loop_isGeometric` (envelope theorem: under bounded-coupling `IsGeometric γ_loop M_loop r_loop`, full γ_n is `IsGeometric (1 + M_loop) (max (1/4) r_loop)`); `kinDispSeq_add_loop_borelTransform_bounded` (Borel transform of full γ_n decays super-geometrically — full sequence Borel-summable); `wave_1a_3_stage4a_structural_closure` (closure summary). Python `bdg_self_energy.py` extended with `GeometricEnvelope`, `KIN_DISP_ENVELOPE`, `envelope_sum`, `stage_4a_structural_verdict`; +12 tests in `test_bdg_self_energy.py`. **Substantive Stage 4a finding:** the envelope theorem decouples the asymptotic-growth verdict from the explicit γ_2^(loop) value — full γ_n is geometric (not Gevrey-1) provided the loop piece is itself bounded by a geometric series with any rate r_loop ∈ (0, 1). Comparable in character to GKST arXiv:1904.01018 published structural result on bounded-momentum-support → geometric in linear response. **44 passing tests in test_bdg_self_energy.py** (+12 vs Session 12 32). Lake build +1 (LoopEnvelope). All headline theorems standard-kernel-only via `lean_verify`. Stage 4b explicit γ_2^(loop) DEFERRED (multi-week 2-loop integral; no longer load-bearing for substrate verdict).) | D1 §3-§4 (additive) — **DEFERRED** | **D.2** | none |
| **Wave 1b** | G9 SymTFT audit + GD-4-factor sketch | 🟡 **IN-PROGRESS** (Stage 1+2+3+4 SHIPPED Session 6 + **Stage 4c third cross-bridge SHIPPED Session 8** + **Stage 5 project-local Witt-class infrastructure SHIPPED Session 10** + **Stage 5.6/5.7 quotient + AddCommGroup transport SHIPPED Session 14** + **Stage 5.8 Drinfeld-center predicate SHIPPED Session 15** (NEW `SymTFTAudit/DrinfeldCenter.lean` §1-§4: `WittEquivalentMTC C D := Nonempty (Center C ≌ Center D)` using Mathlib `CategoryTheory.Center`; refl/symm/trans; `CentralChargePreservesDrinfeldCenter` Prop-level hypothesis; cross-bridge `wittEquivalentMTC_implies_wittClass_eq` to integer-mod-24 quotient; 4-conjunct closure) + **Stage 5.9 BRAIDED STRENGTHENING SHIPPED Session 16 (2026-05-05)** (extends `SymTFTAudit/DrinfeldCenter.lean` §5-§9 with predicate `IsBraidedEquivalence (e : C ≌ D) : Prop` via Mathlib's `Functor.Braided` typeclass + `Center.braidedCategoryCenter` instance — `Nonempty e.functor.Braided ∧ Nonempty e.inverse.Braided`; refl/symm/trans inherited from Mathlib's identity-Braided + composition-Braided instances; `WittEquivalentMTC_braided` is the genuine DMNO 2010 form; strict-weakening `WittEquivalentMTC_braided_implies_WittEquivalentMTC` to Stage 5.8; *refined* hypothesis schema `CentralChargePreservesDrinfeldCenter_braided` strictly weaker than Stage 5.8 plain form, exactly DMNO 2010; composed cross-bridge `wittEquivalentMTC_braided_implies_wittClass_eq` to integer-mod-24 quotient; 4-conjunct `stage5_9_drinfeldCenter_braided_closure`. 13 substantive thms + 2 defs + 1 structure. All standard-kernel-only via `lean_verify`). Stage 4 ships 3 substantive predicate-level cross-bridges. Stage 5 ships `WittClass.lean` (`WittInvariant := ZMod 24` with AddCommGroup + AddMonoidHom; Schellekens chain lifted to Witt-invariant level; SM 3-gen witness + falsification witnesses; SymTFT bridge `stage5_chiralCentralCharge_witt_bridge`; full Witt-equivalence relation + setoid + group-homomorphism witnesses; `stage5_witt_grade_partition`). **Stage 5.10 Deligne tensor product as group operation** (in-program build target: Mathlib has `MonoidalCategory` but does not yet ship Deligne's k-linear ⊠) → **Stage 5.11 DMNO theorem on pseudo-unitary subclass** (discharges `CentralChargePreservesDrinfeldCenter_braided` per DMNO 2010 Theorem 5.2). Mathlib upstream-PR-grade in-program-build target. **Stage 5.10a SHIPPED Session 18; 5.10b morphism-side SHIPPED Session 19; 5.10b.2/5.10b.3/5.10b.4 SHIPPED Session 20** (full `MonoidalCategoryStruct` + `MonoidalCategory` + `Functor.Monoidal incl` instances on `FreeKLinear C k`; ~19 substantive thms + 5 defs added to `FreeKLinearMonoidal.lean` §4-§7); **Stage 5.12 strict pseudo-unitary refinement SHIPPED Session 20** (~7 substantive thms + 2 defs added to `PseudoUnitary.lean` §8-§9, breaking the Wave 1b.5.11 trivial-witness equivalence by binding witness to non-trivial rank `n ≥ 2`). Stage 5.10c-g (Deligne ⊠ proper as quotient + braided lift + WittClass cross-bridge) remain.) | D2 + D3 + D5 + F (additive) — **DEFERRED** | **D.2** | none |
| **Wave 1c** | G8-W1 AS memo (no Lean) | ✅ **SHIPPED** (memo at `temporary/working-docs/phase6n/6n_eta_AS_reformulation_memo.md`; APS-η for analog horizons flagged as Phase 6o follow-up) | D2 + D3 + E1 appendix — **DEFERRED** | **D.2** | none |
| **Track 2 — Foundational backing for fluctuation-theorem content** | | | | | |
| **Wave 2a** | G10-6n.1 Glorioso-Liu axiomatic | ✅ **SHIPPED** (Stage-1 skeleton + Stage 2–3 substantive refactor + Stage 2–3b KMS framework refactor + R1 LIFTED; 8 GloriosoLiu/ modules. **Session 7 (2026-05-05) Stage 2-3b load-bearing deepening:** `entropy_current_exists` upgraded from trivial-discharge `⟨zeroEntropyCurrent, rfl⟩` to substantive Noether construction via `A.dynamical_KMS` extraction; `OnsagerReciprocity_from_KMS` upgraded from trivial-discharge `⟨0⟩` to substantive 9×9 diagonal embedding of the FDR-pinned 2×2 conductivity. Both make `A : SKEFTAxioms` parameter load-bearing. New cross-bridges: `noetherEntropyDensity_eq_beta_imL` + `firstOrderConductivity_isSymm_of_FirstOrderKMS`.) | D3 + L3 + I1 — **DEFERRED** (I1 reframing pre-draft EXISTS, on-hold) | **D.3** | **HELD** — I1 pre-draft at `temporary/working-docs/phase6n/6n_gamma_I1_reframing_predraft.md` (on-hold per Session-5 user direction) |
| **Wave 2b** | G10-QCrooks-α | ✅ **SHIPPED** (Stage 1+2-3 (ℝ) Session 6; ℂ-extension at Fin 2 SHIPPED Session 10; **HIGHER-DIMENSIONAL ℂ GENERALIZATION SHIPPED Session 13 (2026-05-05)**: 9th module `QuantumCrooks/HigherDimensional.lean` (~12 substantive theorems) ships block-diagonal embedding of canonical 2-level ℂ Perarnau-Llobet substrate into `Matrix (Sum (Fin 2) T) (Sum (Fin 2) T) ℂ` for any Fintype T via `Matrix.fromBlocks`; substantive `embedTwoBlock_trace_eq` + `embedTwoBlock_mul` + `embedTwoBlock_diagonal` compatibility theorems; reduction theorems mapping embedded averages to 2-level averages; substantive higher-dimensional ℂ-form quantum no-go `perarnau_llobet_no_go_higher_dim` for any Fintype T. Closes the "further ℂ generalization to arbitrary n" target listed in recommended-next-up #8. MCP-driven, zero Aristotle, zero sorry. **Open extensions remaining:** reservoir-coupled forms; KMSCompatible/FDTCompatible typeclass connections.) | D1 cross-bridge / D5 — **DEFERRED** | **D.2** | none |
| **Wave 2c** | G10-6n.3 Crooks-on-analog-Hawking | ✅ **SHIPPED** (Stage 1+2-3 Session 6; **Stage 4 substrate-level GC bridge SHIPPED Session 8**; **Stage 5 LDP starter SHIPPED Session 13 (2026-05-05)**: NEW module `CrooksAnalogHawking/LDPLinearResponse.lean` (~6 substantive theorems) ships in-program LDP build for the linear-response Gaussian regime: `LDPLinearResponseData` structure with positivity witnesses for both β and σ²; `linearResponseEmissionScheme` canonical AnalogHawkingEmissionScheme constructor with σ(W) = β·W and I = linearResponseRateFunction β σ² (FDT-pinned Gaussian); `linearResponseEmissionScheme_monotonicityCompatible` (substrate-level monotonicity σ(W) = β·W ≥ 0 for W ≥ 0); `linearResponseEmissionScheme_WFormGC` (W-form GC at β); **`linear_response_third_biconditional_W_form`** composed substantive theorem (BOTH substrate-level monotonicity AND W-form GC hold simultaneously). **First concrete substrate-level discharge** of the third Sakharov-style biconditional in horizon-Crooks language at the W-form-GC level. Mathlib upstream-PR target retained for full measure-theoretic LDP machinery (multi-year community project). Verlinde-vs-Jacobson distinction preserved. MCP-driven, zero Aristotle, zero sorry.) | E1 + D1 — **DEFERRED** | **D.2** | none |
| **Wave 2d** | G10-6n.7 Sakharov ↔ horizon-Crooks | ✅ **SHIPPED** (Stage 1 + Stage 2-3 SHIPPED Session 6: substrate bundle + substantive biconditional `sakharov_iff_horizon_crooks` proved at Bool-projection level + ³He-A and FLS BEC compatibility theorems + horizon-Crooks-language unification of Phase 6e + Phase 6m Track C substrate-classification. **Stage 4 substrate-level bridge SHIPPED Session 7 (2026-05-05)**: new module `CrooksAnalogHawking/SKEFTHorizonBridge.lean` ships 6 substantive theorems linking Wave 2a's `SKEFTAxioms` machinery at horizon temperature β_H to Wave 2c's `HorizonDetailedBalance` predicate. Headline theorems: `noetherEntropyDensity_nonneg_of_SKEFTAxioms` (entropy density ≥ 0 via `A.dynamical_KMS` + `A.reflection_pos`); `skeft_yields_horizon_crooks_witness` (FDR-pinned σ = β_H · W); `sakharov_skeft_substrate_jacobsonConsistent` (substrate-level Sakharov + SKEFTAxioms ⇒ Jacobson-consistent); `helium3A_skeft_substantive_jacobsonConsistent` (³He-A concrete instance under any SKEFTAxioms); `horizonCrooks_substantive_partition` (substantive partition vs. FLS BEC). Verlinde-vs-Jacobson distinction preserved.) D3+L3 reframing pre-draft on-hold per Session-5. | D3 + L3 — **DEFERRED** (pre-draft on-hold) | **D.3** | **HELD** — D3+L3 pre-draft on-hold per Session-5 user direction |

**Wave dependencies (Session-5 status):**
- Wave 1a / Wave 1b / Wave 2b / Wave 2c / Wave 2a are independent — can run in any order, in parallel.
- Wave 2a ✅ closed at Lean-substrate level (Stage-1 + Stage-2–3 + Stage-2–3b shipped). This unblocks Wave 2d Lean module work AND Wave 2c's path-measure theorem-statement form.
- Wave 2d Lean module work is now **unblocked** by Wave 2a closure; only the bundle absorption is deferred.
- Wave 1c ✅ memo shipped; the AS-side translation of SymTFT anomaly content (Witten–Yonekura η-invariant) cross-references Wave 1b when Wave 1b closes.
- Wave 2c does not strictly depend on Wave 2a closing for the substrate audit, but for theorem-statement form, Wave 2a's `SKEFTAxioms` typeclass is now available.

**Recommended next-up order (Session 10+ reset, per user direction "push forward on math, build in leverage order"):**

**Session 10 close add:** Three substantive lifts shipped (Wave 1a Stage 2 γ_1 Beliaev / Wave 1b Stage 5 Witt-class / Wave 2b ℂ-extension). The post-Session-10 picture: Wave 1b Stage 5 project-local form + Wave 2b ℂ-extension (Fin 2) both CLOSED; Wave 1a Stage 2 γ_1 SHIPPED, Stage 3+ remaining.

**Session 11 close add (2026-05-05):** Wave 1a.3 Path A Stage 3 γ_2 NLO kinematic-dispersive SHIPPED (Python).

**Session 12 close add (2026-05-05):** Wave 1a.3 Path A Stage 3 LEAN LIFT SHIPPED + cross-module Padé-Borel verification. NEW Lean module `Resurgence/KinematicDispersive.lean` (~10 substantive theorems + 2 defs) lifts the Session-11 closed-form `γ_n^(kin-disp)/γ_1 = (-1)^(n-1)·C(2(n-1),n-1)/16^(n-1)` to Lean; central-binomial bound `C(2k,k) ≤ 4^k` via `Nat.choose_le_two_pow` → `IsGeometric kinDispSeq 1 (1/4)` (substantive geometric-rate-1/4 theorem at Lean level). `Resurgence/Basic.lean` extended with `IsGeometric` predicate parallel to `IsGevrey1`. Cross-module verification: 3 new integration tests in `tests/test_bdg_self_energy.py` connecting Stage-3 `gamma_kinematic_dispersive_sequence` to Wave 1a.2 `borel.py` confirm ratio-test → 0 (geometric not Gevrey-1), `leading_borel_singularity = None` (Borel transform entire), `lambda_uv_estimate = None` (no resurgence Λ_UV from kinematic alone). Pytest 4080 passed; Lake 8539 jobs; all headline theorems standard-kernel-only.

**Session 13 close add (2026-05-05):** **THREE substantive lifts SHIPPED in leverage order.** (1) **Wave 1a.3 Stage 4a structural envelope theorem.** Pivoted from explicit (multi-week) γ_2^(loop) Beliaev-Galitskii 1959 to substantive envelope: under bounded-coupling assumption `IsGeometric γ_loop M_loop r_loop`, the FULL γ_n = kinDispSeq + γ_loop is `IsGeometric (1 + M_loop) (max (1/4) r_loop)`, with Borel transform decaying super-geometrically — near-definitive Wave 1a.3 substrate verdict pending only the explicit γ_2^(loop) value. NEW module `Resurgence/LoopEnvelope.lean` (~5 substantive theorems): `IsGeometric.add` (sum-closure), `kinDispSeq_add_loop_isGeometric` (envelope theorem), `kinDispSeq_add_loop_borelTransform_bounded` (Borel cross-bridge), `wave_1a_3_stage4a_structural_closure` (closure summary). Python: `bdg_self_energy.py` extended with `GeometricEnvelope`, `KIN_DISP_ENVELOPE`, `envelope_sum`, `envelope_borel_transform_bound`, `is_borel_summable_under_envelope`, `stage_4a_structural_verdict`; +12 tests in `test_bdg_self_energy.py` covering construction, dilute-BEC kinematic-dominated, strong-coupling loop-dominated, Borel super-geometric decay, Borel summability for any geometric loop. Working doc at `temporary/working-docs/phase6n/wave_1a_3_path_A_stage4a_envelope.md`. (2) **Wave 2b higher-dimensional ℂ generalization.** Block-diagonal embedding of canonical 2-level ℂ Perarnau-Llobet substrate into `Matrix (Sum (Fin 2) T) (Sum (Fin 2) T) ℂ` for any Fintype T. NEW module `QuantumCrooks/HigherDimensional.lean` (~12 substantive theorems): `embedTwoBlock` block-diagonal embedding via `Matrix.fromBlocks`; substantive trace + multiplication + diagonal compatibility theorems; reduction theorems mapping embedded averages to 2-level averages; substantive higher-dimensional ℂ no-go theorem `perarnau_llobet_no_go_higher_dim`. Closes the "further ℂ generalization" target of recommended-next-up #8. (3) **Wave 2c Stage 5 LDP starter.** In-program build for the linear-response Gaussian regime — first concrete substrate-level discharge of the third Sakharov-style biconditional in horizon-Crooks language at the W-form-GC level. NEW module `CrooksAnalogHawking/LDPLinearResponse.lean` (~6 substantive theorems): `LDPLinearResponseData` structure with positivity witnesses; `linearResponseEmissionScheme` canonical AnalogHawkingEmissionScheme constructor with σ(W) = β·W + I = linearResponseRateFunction; substantive substrate-level monotonicity (σ(W) = β·W ≥ 0 for W ≥ 0) AND W-form GC at β simultaneously; `linear_response_third_biconditional_W_form` composed substantive theorem. Mathlib upstream-PR target retained for full measure-theoretic LDP machinery. Verlinde-vs-Jacobson distinction preserved. **Authoritative post-Session-13 counts (counts.json regen 2026-05-05-2300):** **5478 theorems (5453 substantive + 25 placeholder) / 278 modules / 0 sorry / 1 axiom / 4768 definitions / Lake 8542 jobs / pytest 4092 passed**. Δ vs Session 12: thms +30 substantive, modules +3 (LoopEnvelope, HigherDimensional, LDPLinearResponse), defs +21. All MCP-driven, zero Aristotle, zero new axioms, zero new sorry. Working doc at `temporary/working-docs/phase6n/session13_three_lifts.md`. **Highest-leverage Session 14+ candidates:** (a) Stage 4b explicit γ_2^(loop) Beliaev-Galitskii 1959 + γ_3-γ_5 (multi-week dedicated wave); (b) Phase 6o opening (APS-η analog-horizons + G3 + G4 + Itô); (c) Wave 2b reservoir-coupled forms or KMSCompatible/FDTCompatible typeclass connections; (d) σ-form GC closure via `WFormGallavottiCohen.to_σForm` for linearResponseEmissionScheme.

1. ~~**Wave 2b Stage 1 + Stage 2-3 (ℝ)**~~ — ✅ SHIPPED (Session 6): 7 QuantumCrooks/ modules + substantive quantum Perarnau-Llobet no-go (canonical 2-level counterexample on `Matrix (Fin 2) (Fin 2) ℝ`, MCP-proven, zero Aristotle).
2. ~~**Wave 2c Stage 1 + Stage 2-3 + Stage 4**~~ — ✅ SHIPPED (Stage 1+2-3 Session 6; Stage 4 SHIPPED Session 8): substrate-level Gallavotti-Cohen bridge in `SKEFTGallavottiCohen.lean` (7 substantive theorems; W-form GC predicate; FDT-pinned Gaussian rate function; load-bearing-A theorem extracting `c : FirstOrderCoeffs` from `A.dynamical_KMS`; concrete `firstOrderDissipativeAction` witness; composed Stage-4 substrate combining Session-7 horizon-Crooks witness + W-form GC). Verlinde-vs-Jacobson distinction preserved. Stage 5+ LDP infra retained as Mathlib upstream-PR candidate.
3. ~~**Wave 2d Lean substrate Stage 1 + Stage 2-3 + Stage 4**~~ — ✅ SHIPPED (Stage 1+2-3 Session 6; Stage 4 SHIPPED Session 7): substrate bundle + substantive biconditional `sakharov_iff_horizon_crooks` proved at Bool-projection level + ³He-A and FLS BEC compatibility theorems + horizon-Crooks-language unification of Phase 6e + Phase 6m Track C substrate-classification + Stage 4 SKEFTHorizonBridge substrate-level form (6 substantive theorems). Verlinde-vs-Jacobson distinction preserved.
4. ~~**Wave 1b Stage 1+2+3+4 + Stage 4c + Stage 5 (project-local)**~~ — ✅ SHIPPED (Stages 1-4 Session 6; Stage 4c Session 8; **Stage 5 project-local Witt-class SHIPPED Session 10**): primary-source audit verdict + 3 discrete-sector candidate predicates + 3 substantive cross-module bridges + WittClass.lean (~12 thms + 5 defs) lifting Schellekens chain to ZMod 24 / AddCommGroup / AddMonoidHom level. Stage 5+ full Witt-class form remains in-program-build candidate (Mathlib upstream-PR target).
5. ~~**Wave 1a.3 Path A Stage 1 (skeleton) + Stage 2 (γ_1 Beliaev LO) + Stage 3 (γ_2 NLO kinematic-dispersive Python + Lean lift)**~~ — ✅ SHIPPED (Stage 1 Session 6; Stage 2 Session 10; Stage 3 Python Session 11; **Stage 3 Lean lift SHIPPED Session 12**): Python γ_1 = 3/(640π); γ_2^(kin-disp) = −γ_1/8 = −3/(5120π); SymPy symbolic verification + closed-form general-n ratio γ_n^(kin-disp)/γ_1 = (−1)^(n−1)·C(2(n−1),n−1)/16^(n−1); kinematic-piece convergence radius 2·Λ_UV (geometric rate 1/4). Lean lift: NEW module `Resurgence/KinematicDispersive.lean` with `kinDispRatio`, `kinDispSeq`, `kinDispSeq_isGeometric : IsGeometric kinDispSeq 1 (1/4)`, `kinDispSeq_borelTransform_bounded`, `kinDispSeq_stage3_closure`; `Basic.lean` extended with `IsGeometric` predicate. Cross-module Padé-Borel integration tests confirm Wave 1a.2 infrastructure correctly classifies geometric vs Gevrey-1. 32 passing tests in test_bdg_self_energy.py + 1 Stage-4-5 skip-marked.
6. ~~**Wave 2b ℂ-extension at Fin 2**~~ — ✅ SHIPPED (Session 10): NEW module `QuantumCrooks/ConcreteComplex.lean` (~14 thms + 10 defs); `Matrix (Fin 2) (Fin 2) ℂ` form of canonical Perarnau-Llobet substrate; σ_y Hamiltonian (genuinely complex Hermitian); ℂ↔ℝ cross-bridge; ℂ-form quantum no-go `perarnau_llobet_no_go_complex` (MCP-proven, zero Aristotle).
7. ~~**Wave 1a.3 Stage 4a structural envelope theorem**~~ — ✅ SHIPPED (Session 13): pivot from explicit (multi-week) γ_2^(loop) computation to substantive envelope theorem. NEW module `Resurgence/LoopEnvelope.lean`: `IsGeometric.add` sum-closure + `kinDispSeq_add_loop_isGeometric` envelope theorem (FULL γ_n is geometric with rate `max (1/4) r_loop` under loop-bounded-coupling); Borel transform of the sum decays super-geometrically; closure summary. Python `bdg_self_energy.py` extended with `GeometricEnvelope` + 12 tests. Near-definitive Wave 1a.3 substrate verdict pending only the explicit γ_2^(loop) Beliaev-Galitskii 1959 value (multi-week, deferred to Session 14+).
8. **🟡 Wave 2b further ℂ generalization (PARTIALLY SHIPPED)** — ✅ Higher-dim ℂ (Session 13): NEW module `QuantumCrooks/HigherDimensional.lean` (~12 thms): block-diagonal embedding via `Matrix.fromBlocks` into `Matrix (Sum (Fin 2) T) (Sum (Fin 2) T) ℂ` for any Fintype T; substantive trace + multiplication + diagonal compatibility theorems; substantive higher-dim ℂ no-go `perarnau_llobet_no_go_higher_dim`. ⏳ Remaining: reservoir-coupled forms; KMSCompatible/FDTCompatible typeclass connections.
9. ~~**Wave 2c Stage 5 LDP starter**~~ — ✅ SHIPPED (Session 13): in-program LDP build for the linear-response Gaussian substrate. NEW module `CrooksAnalogHawking/LDPLinearResponse.lean` (~6 substantive thms): `LDPLinearResponseData` + `linearResponseEmissionScheme` + substrate-level monotonicity theorem + W-form GC theorem + composed `linear_response_third_biconditional_W_form`. **First concrete substrate-level discharge** of the third Sakharov-style biconditional in horizon-Crooks language at the W-form-GC level. Mathlib upstream-PR target retained for full measure-theoretic LDP machinery (multi-year community project).
10. **Wave 1a.3 Path A Stage 4b (explicit γ_2^(loop) Beliaev-Galitskii 1959)** — multi-week 2-loop integral; deferred. Stage 4a envelope theorem now decouples the asymptotic-growth verdict from the explicit per-order computation, so this is no longer load-bearing for the substrate verdict.
11. **σ-form GC closure for linearResponseEmissionScheme** — connecting the Stage-5 W-form GC to σ-form `gcCompatibleEmission` predicate via the full `WFormGallavottiCohen.to_σForm` chain; mechanical Stage-6 step, not a substrate-level lift.
12. **Wave 2b reservoir-coupled forms / KMSCompatible+FDTCompatible typeclass connections.**
13. **Phase 6o opening** — APS-η analog-horizons sub-wave + deferred-track items (G3 Carrollian, G4 Kerr-Schild, G1 Schellekens, G10-ETH-α, I3 community contribution).

---

## Wave 1a — G2 Resurgence on SK-EFT gradient expansion 🟡 IN-PROGRESS

**Sub-wave status (Session 10 close):**
- ✅ **Wave 1a.1 SHIPPED** (commit `769ecc6`): `formulas.py` extended with δ⁴–δ⁷ symbolic coefficient generators (16 new coefficients across orders 4–7 per `count(N) = ⌊(N+1)/2⌋ + 1`).
- ✅ **Wave 1a.2 SHIPPED** (commit `2eb461a`): Padé-Borel numerical infrastructure at `src/resurgence/borel.py` (ratio test, Padé approximant, Borel-singularity extraction, Λ_UV closed-form predictor).
- 🟡 **Wave 1a.3 IN-PROGRESS**: 
  - **Path B SHIPPED**: numerical run on Steinhauer-class BEC parameters via `borel.py`; verdict at `temporary/working-docs/phase6n/6n_alpha_3_VERDICT.md` + script at `6n_alpha_3_path_B_numerical_run.py` (uncommitted working docs). **Verdict: BEC SK-EFT in weak-coupling regime is geometrically convergent, NOT Gevrey-1** (super-geometric per Beliaev-style prefactors); comparable to GKST holographic linear-response. The Path B counterfactual Gevrey-1 model recovers Λ_UV = κ√A = 2500 Hz exactly via [1/1] Padé–Borel — pipeline validated; physical verdict is "no resurgence content for weak-coupling BEC."
  - **Path A AUTHORIZED + Stage 1 (skeleton) SHIPPED Session 6** (commit `a7089c9`): `src/resurgence/bdg_self_energy.py` module skeleton + 9 tests (4 pass, 5 Stage-2+ skip-marked with literature-anchor citations) + working doc with Stage 2-5 sub-stage decomposition.
  - ✅ **Path A Stage 2 (γ_1 Beliaev LO) SHIPPED Session 10 (2026-05-05)**: `src/resurgence/bdg_self_energy.py` rewritten Stage-1-skeleton → Stage-2-implementation; γ_1 = 3/(640π) ≈ 1.4924 × 10⁻³ pinned per Beliaev 1958 / Pitaevskii-Stringari §4.4 / Liu-Fukuyama PRB 31, 175 (1985); decomposition γ_1 = (1/30) × (9/64) / π = 9/(1920π) = 3/(640π); SciPy `scipy.integrate.quad` numerical verification of kinematic phase-space integral ∫₀^1 u²(1-u)² du = 1/30; convention reconciliation establishes genuine Beliaev δ_diss is order g² (in g = (ξk)²), clarifying Path B heuristic was at wrong order (this is itself a substantive Path A finding); 14 passing tests + 3 Stage-3+ skip-marked.
  - ✅ **Path A Stage 3 (γ_2 NLO kinematic-dispersive) SHIPPED Session 11 (2026-05-05)**: γ_2^(kin-disp) = -γ_1/8 = -3/(5120π) ≈ -1.866 × 10⁻⁴ from the Bogoliubov inverse-dispersion expansion [1 + (ξk/2)²]^(-1/2) = 1 - (ξk)²/8 + 3(ξk)^4/128 - 5(ξk)^6/1024 + ...; SymPy symbolic verification through order (ξk)^12 against the closed-form rational coefficients C(2k,k)/16^k; closed-form general-n ratio γ_n^(kin-disp)/γ_1 = (-1)^(n-1) · C(2(n-1),n-1) / 16^(n-1) shipped via `gamma_n_kinematic_dispersive_closed_form` and `gamma_n_kinematic_dispersive`. **Substantive Stage-3 finding:** the kinematic piece of the SK-EFT gradient-expansion coefficient sequence has **closed-form geometric convergence with rate 1/4 and convergence radius exactly 2 · Λ_UV** (i.e., series converges for ξk < 2 ⟺ k < 2 c_s/ξ); approach to limit is monotone from below at rate (2k-1)/(8k). This sharpens Path B's qualitative geometric-vs-Gevrey-1 verdict to a precise quantitative result on the kinematic piece. Rescoping note: the original Stage-3 anchor "Andreev-Khalatnikov 1963" is correctly understood as **finite-T 4-phonon scattering** (T/T_c gradient), NOT the T → 0 (ξk) gradient against which γ_n is defined; AK 1963 is preserved as a stub redirecting to the kin-disp anchor + flagged for the future finite-T SK-EFT extension wave. Stage 3 anchor is Pitaevskii-Stringari §4.4 Bogoliubov dispersion. 29 passing tests (was 14 at Session 10), +15 Stage-3 tests; 1 Stage-4-5 skip-marked. Working doc at `temporary/working-docs/phase6n/wave_1a_3_path_A_stage3_close.md`.
  - ✅ **Path A Stage 3 LEAN LIFT SHIPPED Session 12 (2026-05-05)**: NEW Lean module `lean/SKEFTHawking/Resurgence/KinematicDispersive.lean` (~10 substantive theorems + 2 defs) lifting the Session-11 closed-form coefficients to the Lean substrate. Headline content: `kinDispRatio (k : ℕ) : ℚ := ((-1)^k * C(2k,k) : ℤ : ℚ) / 16^k` (closed-form rational); `kinDispSeq : ℕ → ℝ` (real-valued counterpart); auxiliary `central_binomial_le_four_pow : C(2k,k) ≤ 4^k` (specialization of `Nat.choose_le_two_pow` to the central case); load-bearing `kinDispRatio_abs_le_quarter_pow : |(kinDispRatio k : ℝ)| ≤ (1/4)^k`; **`kinDispSeq_isGeometric : IsGeometric kinDispSeq 1 (1/4)`** (substantive Lean-level statement of the Stage-3 finding); `kinDispSeq_borelTransform_bounded` (Borel transform decays super-geometrically — kinematic piece has no resurgence content); `kinDispSeq_isGevrey1_trivially` (degenerate Gevrey-1 cross-bridge); `kinDispSeq_stage3_closure` (4-conjunct closure summary). **`Resurgence/Basic.lean` extended** with `IsGeometric` predicate parallel to `IsGevrey1` + 4 accessors + `borelTransform_bounded_of_isGeometric` cross-bridge. Closed-form sanity values shipped at k=0,1,2,3 via `norm_num [Nat.choose]`. **All headline theorems verify to `[propext, Classical.choice, Quot.sound]` only via `lean_verify`**. **Cross-module verification:** 3 new integration tests in `tests/test_bdg_self_energy.py` connecting Stage-3 `gamma_kinematic_dispersive_sequence` to Wave 1a.2 `borel.py` Padé-Borel pipeline confirm: ratio-test R_n decays monotonically to 0 (geometric not Gevrey-1); `leading_borel_singularity` returns `None` (Borel transform entire); `lambda_uv_estimate` returns `None` (no resurgence Λ_UV from kinematic-only sequence). End-to-end Wave 1a infrastructure verification at the kinematic-piece level. **32 passing tests in test_bdg_self_energy.py** (was 29 at Session 11; +3 cross-module integration). Project pytest 4080 passed (+3 vs Session 11). **Lake build 8539 jobs** (+1 KinematicDispersive). MCP-driven, zero Aristotle, zero new axioms, zero new sorry. Working doc at `temporary/working-docs/phase6n/wave_1a_3_path_A_stage3_close.md` (Session-11 + Session-12 Lean lift summary appended).
  - ✅ **Path A Stage 4a (structural envelope theorem) SHIPPED Session 13 (2026-05-05)**: pivoted from explicit (multi-week) γ_2^(loop) Beliaev-Galitskii 1959 to substantive envelope theorem. NEW Lean module `Resurgence/LoopEnvelope.lean` (~5 substantive theorems): `IsGeometric.add` sum-closure (sum of two geometric sequences is geometric with rate `max r₁ r₂` and constant M₁+M₂); `kinDispSeq_add_loop_isGeometric` envelope theorem (under bounded-coupling assumption `IsGeometric γ_loop M_loop r_loop`, the FULL γ_n = kinDispSeq + γ_loop is `IsGeometric (1 + M_loop) (max (1/4) r_loop)`); `kinDispSeq_add_loop_borelTransform_bounded` Borel cross-bridge (the Borel transform of the sum decays super-geometrically — full γ_n is Borel-summable, carries no Gevrey-1 transseries content under LBC); `wave_1a_3_stage4a_structural_closure` 3-conjunct closure summary. Python `bdg_self_energy.py` extended with `GeometricEnvelope` dataclass + `KIN_DISP_ENVELOPE` constant + `envelope_sum`, `envelope_borel_transform_bound`, `is_borel_summable_under_envelope`, `stage_4a_structural_verdict` functions; +12 tests in `test_bdg_self_energy.py` covering construction, dilute-BEC kinematic-dominated, strong-coupling loop-dominated, Borel super-geometric decay, summability for any geometric loop. **Substantive Stage-4a finding:** the envelope theorem decouples the asymptotic-growth verdict from the explicit γ_2^(loop) value — the FULL γ_n is geometric (not Gevrey-1) provided the loop piece is itself bounded by a geometric series with any rate `r_loop ∈ (0, 1)`. Comparable in character to the published Grozdanov-Kovtun-Starinets-Tadić arXiv:1904.01018 result on bounded-momentum-support → geometric convergence in linear response. **44 passing tests in test_bdg_self_energy.py** (was 32 at Session 12; +12 envelope tests). Project pytest 4092 passed (+12 vs Session 12). **Lake build 8540 jobs** (+1 LoopEnvelope). MCP-driven, zero Aristotle, zero new axioms, zero new sorry. Working doc at `temporary/working-docs/phase6n/wave_1a_3_path_A_stage4a_envelope.md`.
  - ⏳ **Path A Stage 4b (explicit γ_2^(loop) + γ_3-γ_5) DEFERRED** — Beliaev-Galitskii 1959 2-loop Bogoliubov self-energy at order (ξk)^6 + modern higher-loop ERG references (Pawlowski-Strodthoff 2015, Boettcher et al.) for γ_3-γ_5. Multi-week 2-loop integral; deferred to a dedicated wave when capacity allows. The Stage 4a envelope theorem decouples the asymptotic-growth verdict from this explicit per-order computation, so Stage 4b is no longer load-bearing for the substrate verdict.
  - ⏳ **Path A Stage 5 (γ_6-γ_7 + Padé-Borel re-run + sharpened verdict) DEFERRED** — pending Stage 4b.
- ✅ **Wave 1a.4 SHIPPED** (commit `0abd7d3`): Lean substrate at `lean/SKEFTHawking/Resurgence/{Basic,BorelAction,StokesBound}.lean` + count theorems at `HigherOrderSK.lean`. Formalizes `IsGevrey1` predicate, `StokesData` infrastructure, `lambdaUV_from_borelAction` conditional theorem, `deltaNP_pos_of_stokes_pos` non-perturbative-content existence statement.

**Substantive math finding (Session 5 Path B + Session 10 Path A Stage 2 + Session 11 Path A Stage 3):** the BEC SK-EFT geometric-convergence verdict is a substrate-structural classification result paralleling the Phase 6e Sakharov-criterion finding. The Session-10 γ_1 = 3/(640π) ≈ 1.4924 × 10⁻³ Beliaev LO derivation supplies the first authoritative literature-anchored coefficient in the gradient-expansion sequence; the Session-11 γ_2^(kin-disp) = −γ_1/8 = −3/(5120π) ≈ −1.866 × 10⁻⁴ derivation supplies the closed-form NLO kinematic-dispersive piece **plus the closed-form general-n ratio γ_n^(kin-disp)/γ_1 = (−1)^(n−1) · C(2(n−1), n−1) / 16^(n−1)** with **convergence radius exactly 2 · Λ_UV**. The kinematic piece of the gradient-expansion is sharply geometric (rate 1/4) — sharpening Path B's qualitative verdict to a precise quantitative result. A "third Sakharov-style biconditional" candidate framing flagged for user review at next bundle-absorption pass; sharpening the verdict from "kinematic geometric / loop pieces unverified" → "full γ_n geometric or Gevrey-1" requires Stage 4 (γ_2^(loop) Beliaev-Galitskii 1959 + γ_3-γ_5 systematic loop enumeration + Padé-Borel re-run).

**Three-question template (per `CLAUDE.md` Phase-6 wave format):**

- *Integrates with:* `formulas.py` SK-EFT gradient expansion (δ_disp, δ_diss, δ^(2), δ^(3)); D1 §3-§4 analog-Hawking spectrum content; the WKB chain (exact connection-formula machinery already shipped); Mathlib's existing analytic-continuation + asymptotic-series infrastructure.
- *New constraint adds:* (a) an all-orders bridge from EFT to substrate via Borel-transform singularity structure; (b) rigorous breakdown frequency for the analog Hawking spectrum (where the gradient expansion stops converging); (c) Λ_UV = κ√A from IR coefficients alone (Λ_UV from the Borel singularity at action A). First-mover on SK-EFT resurgence in any proof assistant. Optional small Mathlib4 PR for `borelTransform` if the proof-side cleanly factors.
- *Tension surfaces:* if the SK-EFT gradient expansion has Borel-summable structure with no non-perturbative singularities at the orders δ^(4)–δ^(7) the program can compute, the all-orders bridge produces no new substrate-level constraint and the wave degrades to "we computed two more orders of the gradient expansion." If genuine resurgence structure is present (expected per Aniceto-Başar-Schiappa physics canon), Λ_UV-from-IR is the deliverable.

**Substrate.** Resurgence theory (Écalle / Aniceto-Başar-Schiappa) applied to SK-EFT. δ^(4)–δ^(7) symbolic generation; Padé-Borel approximants; Borel-plane singularity extraction; Λ_UV identification.

**Stage 1 actions:**
- Generate δ^(4)–δ^(7) symbolic coefficients from `formulas.py` extension; add Lean theorem stubs in a new `SKEFTHawking/Resurgence/` module.
- State the Borel-transform predicate as a typeclass-parameterized predicate over the gradient-expansion coefficient sequence.
- Identify the Borel-plane singularity structure expected per Aniceto-Başar-Schiappa.
- Cross-check via the lattice RHMC infrastructure where applicable.

**Bundle absorption.** D.2 additive into D1 §3-§4 (new section: "Non-perturbative content of the SK-EFT — a resurgence reading"). Cross-bridge to E1 + E2 if the breakdown frequency lands inside experimental sensitivity windows.

**Risk axes.** Borel-summability assumption; Padé-Borel approximant ringing; Mathlib analytic-continuation gaps; Λ_UV identification ambiguity if multiple Borel singularities compete.

---

## Wave 1b — G9 SymTFT primary-source audit + GD-4-factor decomposition sketch 🟡 IN-PROGRESS

**Sub-wave status (Session 10 close):**
- 📝 **Stage 1 audit working doc** at `temporary/working-docs/phase6n/6n_beta_symtft_audit.md`: substrate analysis + Choi-doubled SymTFT applicability decision matrix + cross-bridge audit + GD-4-factor decomposition sketch + risk register.
- ✅ **Stage 2 audit verdict SHIPPED** (Session 6) at `temporary/working-docs/phase6n/wave_1b_symtft_audit_verdict.md`: direct primary-source fetch of arXiv:2507.05350 (Schäfer-Nameki et al., "SymTFT Approach for Mixed States with Non-Invertible Symmetries," July 2025). **Verdict: PARTIALLY APPLICABLE** — discrete dynamical-KMS Z₂ embeds in the Choi-doubled framework as a "weak symmetry" analog of the paper's Kramers-Wannier example; continuous entropic U(1)_T does NOT embed (paper does not handle continuous symmetries) and requires fusion with Brennan-Sun arXiv:2407.07951 BF technology — multi-wave research project per DR §6.2, deferred to Phase 6o.
- ✅ **Stage 3 discrete-sector Lean substrate SHIPPED** (Session 6): `lean/SKEFTHawking/SymTFTAudit/Applicability.lean`:
  - `SymTFTApplicability` inductive (Applicable / PartiallyApplicable / NotApplicable)
  - `stage2Verdict := PartiallyApplicable` (the audit outcome)
  - 3 ship-able discrete-sector candidate predicates: `chiralCentralChargeMod24Compatible`, `Z16AnomalyEtaCompatible`, `KMSParityAlternationCompatible`
  - Substantive instantiation theorems for each (Stage-2 verdict instantiates each non-vacuously)
  - `stage2_partition` substantive partition theorem
- ✅ **Stage 4 substantive cross-module bridges SHIPPED** (Session 6 + Session 8): `SymTFTAudit/CrossBridges.lean` ships 3 substantive predicate-level bridges (KMSParityAlternation ↔ SecondOrderSK γ_{2,1}+γ_{2,2}=0; Z16AnomalyEta ↔ Z16AnomalyCancels; chiralCentralCharge ↔ GenerationConstraint.generation_constraint_iff). Updated `stage4_discrete_sector_bridges_partition` from 2-of-3 to 3-of-3 substantive bridges in Session 8.
- ✅ **Stage 5 project-local Witt-class infrastructure SHIPPED** (Session 10, 2026-05-05): NEW module `lean/SKEFTHawking/SymTFTAudit/WittClass.lean` (~12 substantive theorems + 5 defs):
  - `WittInvariant := ZMod 24` with full algebraic structure: `AddCommGroup`, `DecidableEq`, `Fintype`, `inferInstance` derivation chain.
  - `WittInvariant.fromChiralCentralCharge : ℤ →+ WittInvariant` AddMonoidHom — the chiral-central-charge mod 24 invariant projection.
  - `WittTrivial (c : ℤ) : Prop` predicate + `WittTrivial_iff_dvd` biconditional `WittTrivial c ↔ 24 ∣ c`.
  - **Schellekens chain LIFTED to Witt-invariant level** via `chiralCentralCharge_wittTrivial_iff_three_dvd_N_f : WittTrivial (8 * N_f) ↔ 3 ∣ N_f` — the program's anchor 3-generation result now expressed at the abelian-group level via `GenerationConstraint.generation_constraint_iff`.
  - SM 3-generation concrete witness `standardModel_wittTrivial`; falsification witnesses `one_generation_not_wittTrivial`, `two_generations_not_wittTrivial`.
  - Full Witt-equivalence relation: `WittEquivalent c c' := WittInvariant.fromChiralCentralCharge c = WittInvariant.fromChiralCentralCharge c'`; `wittSetoid : Setoid ℤ` instance; `WittEquivalent_iff_dvd : WittEquivalent c c' ↔ 24 ∣ (c - c')`.
  - `wittInvariant_homomorphism_witness` confirms the AddMonoidHom property at the relation level.
  - SymTFT bridge `stage5_chiralCentralCharge_witt_bridge` (3-tuple combining Stage-2 verdict + SM concrete witness + biconditional) and `stage5_closure_summary`.
  - `CrossBridges.lean` extended with `stage5_witt_grade_partition` (4-conjunct partition combining Stage-4 partition + SM-WittTrivial witness + biconditional + `c₋ = 24` divisibility witness).
  - Lake build verified `lean_verify` close to standard kernel only on all 5 headline theorems.
- 🚩 **Mathlib upstream-PR target retained** at the full MTC + Witt-group level — the project-local form captures only the chiral-central-charge mod 24 piece (sufficient for SK-EFT-Hawking program needs); full Mathlib MTC + Witt-group + Anderson dual + Lagrangian algebra infrastructure remains a multi-year community project. Per program's track record (lean-tensor-categories, RingQuot, repl pinning), in-program in-program-grade builds remain feasible.
- ⏳ **Continuous-sector follow-on**: `gauge_erasure` (Phase 3 W2) and `GD_orthogonality` (4-factor decomposition) candidates deferred per Stage 2 verdict §3 — speculative connections that need Brennan-Sun fusion or substantive (-1)-form work. Phase 6o.

**Lake build state**: 8529 → 8530 → 8536 → **8538 jobs (Session 10, +1 WittClass module)**, all clean, zero sorry.

**Substantive Stage-1+2+3 finding:** the Stage 2 audit verdict is the load-bearing deliverable — direct primary-source fetch confirmed the framework handles discrete non-invertible symmetries in closed quantum systems but does NOT handle continuous, KMS, thermal, or Schwinger-Keldysh content. The PARTIALLY APPLICABLE verdict is the middle of three branches (per Stage 1 §2.2 matrix) and is the honest assessment — neither overconfident nor pessimistic. The Stage 3 Lean substrate operationalizes this verdict at the predicate level (zero sorry).

- *Integrates with:* Phase 5o W5 lean-tensor-categories library (114 thms; Pivotal → ... → Modular); the program's ℤ₁₆ + modular invariance (`24 | c₋ → N_f = 3`) anchor in D2 / L2; Phase 5y GD-orthogonality 4-factor decomposition in D5; arXiv:2507.05350 (Schäfer-Nameki et al., Choi-doubled mixed-state SymTFT, July 2025) as the load-bearing primary-source audit target; arXiv:1511.03646 Crossley-Glorioso-Liu (CGL).
- *New constraint adds:* (a) primary-source audit of arXiv:2507.05350 vs CGL — does the Choi-doubled mixed-state SymTFT framework apply to the dissipative SK-EFT contour, and if so, which Z₂ in the IR effective theory does the SymTFT pick out; (b) lift Z₁₆ / θ̄ + 24|c₋ + topological-order content into one categorical object via the audit's framework; (c) **GD-orthogonality 4-factor → SymTFT decomposition sketch** — the highest-leverage open structural problem flagged by the DR. If the GD 4 factors decompose cleanly inside one SymTFT, the program's anomaly content compresses into a single object instead of four parallel orthogonality-decomposition factors.
- *Tension surfaces:* if the Choi-doubled mixed-state SymTFT does NOT apply to dissipative SK-EFT (e.g., the Choi-doubling assumes unitary evolution at higher categorical level), the audit returns a structural NO-GO that joins the program's no-go landscape — also publishable. If the GD 4-factor decomposition does NOT lift cleanly into one SymTFT, the wave produces a sketch + identified obstruction; partial result.

**Substrate.** SymTFT / higher-form / non-invertible symmetry literature (Bhardwaj-Schäfer-Nameki line). Primary-source target arXiv:2507.05350. Existing program's lean-tensor-categories library as the formalization substrate.

**Stage 1 actions:**
- Read arXiv:2507.05350 in full; extract the categorical structure (3-category? Choi-doubled tensor category?).
- Audit applicability to CGL SK-EFT contour. Decision: applicable / not-applicable / partially-applicable.
- If applicable: state the SymTFT predicate in `SKEFTHawking/SymTFT/Audit.lean`; lift Z₁₆ + 24|c₋ + topological-order naturally.
- GD-4-factor decomposition sketch (paper-math first; Lean stubs as scoping permits).

**Bundle absorption.** D.2 additive into D2 (Z₁₆ + modular content recompressed) + D3 (cross-bridges to gravity content) + D5 (GD-4-factor sketch) + F (single-categorical-object framing for the flagship's §2 architecture statement).

**Risk axes.** Choi-doubled mixed-state SymTFT may be too new to apply confidently; primary-source audit may flip applicability under deeper read; GD-4-factor decomposition sketch may surface obstruction rather than compression.

---

## Wave 2a — G10-6n.1 Glorioso-Liu axiomatic skeleton ✅ SHIPPED (Lean substrate); ⏳ HELD (bundle absorption)

**Sub-wave status (Session-5 close):**
- ✅ **Stage-1 Lean module skeleton SHIPPED** (commit `fa2f54c`): all 8 modules (`Axioms.lean`, `DynamicalKMS.lean`, `LocalEquilibrium.lean`, `EntropyCurrent.lean`, `LocalSecondLaw.lean`, `OnsagerReciprocity.lean`, `FirstOrderProjection.lean`, `Phase1Reconciliation.lean`) under `lean/SKEFTHawking/GloriosoLiu/`.
- ✅ **Stage-1 close** (commit `522155e`): 8 sorries trivial-discharge + root integration into `lean/SKEFTHawking.lean`.
- ✅ **Stage 2–3 substantive refactor SHIPPED** (commit `1b87c4a`): all 7 modules parameterized over `SKDoubling.SKAction`; substantive content beyond placeholders.
- ✅ **R1 LIFTED** (commit `bbb34ce`): substantive partition recovery via `SKDoubling.KMSSymmetry` projection + Aristotle counterexample + linarith. The 4-of-9 partition is now a **theorem** (`Phase1Reconciliation.four_of_nine_partition_recovered`) rather than an Aristotle-empirical observation.
- ✅ **Stage 2–3b KMS framework refactor SHIPPED** (commit `d344c33`): non-trivial existence witness `SKEFTAxioms_zero_action` for the zero action; `dynamical_KMS := ⟨kmsForZeroAction β⟩` substantive witness shipped.
- 📝 **KMS framework finding (working doc)** at `temporary/working-docs/phase6n/6n_gamma_kms_framework_finding.md`: structural finding that strict-invariance `KMSSymmetry` is generically incompatible with non-trivial dissipative SK-EFT actions (canonical KMS shift generates new monomials whose only cancellation requires γ_1 = γ_2 = 0). Recommends extending Stage 2–3b with the algebraic FDR form (`hasDynamicalKMS_algebraic`) for non-trivial existence witness `SKEFTAxioms_for_dissipative` — small-scope follow-up refactor (~50 lines Lean) flagged for next-session attention.
- ⏳ **D.3 bundle absorption HELD per Session-5 user direction**: I1 reframing pre-draft EXISTS at `temporary/working-docs/phase6n/6n_gamma_I1_reframing_predraft.md` (drop-in §3 replacement prose for `papers/I1/paper_draft.tex` lines 282–340 + D3+L3 incidental citation upgrades). On-hold for user review until Phase 6n + 6o math closure complete; absorption then runs as one coherent pass per user direction.

**🚨 USER AUTHORIZATION GATE (D.3 absorption — Stage B of `LATE_PHASE6_ABSORPTION_PROTOCOL.md`) — HELD:**

I1's Phase 1 FirstOrderKMS case study is the program's anchor productive-value example. The Wave 2a reframing converts "Aristotle disproved 5/9" → "Aristotle disproved 5/9; deeper Glorioso-Liu axiomatic was formalized; the 5/9 disproof became a theorem of first/second-order separation." This substantially strengthens the case study but changes I1's framing. **Pre-draft EXISTS for user review when bundle-absorption pass begins.**

D3 + L3 also absorb under D.3 (citation form on Glorioso-Liu monotonicity now points to the formalized `Glorioso_Liu_local_second_law` rather than the published derivation alone). D3 + L3 absorption is incremental enough that user-auth at the I1 review point covers all three.

**Three-question template:**

- *Integrates with:* Phase 1 FirstOrderKMS productive-value case study (Aristotle disproof of 5 of 9); existing program SK-EFT modules (`KMSCompatible`, `FDTCompatible`, `GloriosoLiuMonotoneCompatible` typeclasses); Glorioso-Crossley-Liu II classical-limit axiomatic (arXiv:1701.07817) as the cleanest target.
- *New constraint adds:* a clean axiomatic statement of the SK-EFT primitive (six axioms: SK-1 closed-time-path + SK-2 largest-time/unitarity + SK-3 reflection positivity / Im S ≥ 0 + SK-4 hermiticity + KMS-dyn dynamical KMS Z₂ symmetry + LE local equilibrium); typeclass-parameterized `DynamicalKMS [uv : UVRealization]` per Jain-Kovtun arXiv:2309.00511 UV-ambiguity; the local second-law theorem as `∂_μ J^μ_S ≥ 0 pointwise`; recovery of the FirstOrderKMS 4-of-9 partition as first-order/second-order separation of the dynamical-KMS axiom. Lifts Phase 1 productive-value finding from "axiom disproof" to "axiom replaced by deeper structure that explains the disproof."
- *Tension surfaces:* if the 4-of-9 partition does NOT recover cleanly (the disproof was deeper than first-order/second-order separation), the failure mode is itself a publishable productive-value finding. If the LE axiom does not admit a sharp Lean predicate that aligns with existing program hydrodynamic typeclasses, the formalization either trivializes the main theorem (LE too weak) or excludes legitimate program content (LE too strong); both are recoverable via predicate-design iteration.

**Substrate.** Glorioso-Crossley-Liu axiomatic (arXiv:1511.03646 / 1612.07705 / 1701.07817). Classical-limit form recommended (Glorioso-Crossley-Liu II) — significantly easier than the full quantum form. Discrete-time / formal-power-series scope (continuous-time corollaries deferred to post-Phase-6o-Itô).

**Stage 1 actions:**
- Read Glorioso-Crossley-Liu I + II + Jain-Kovtun arXiv:2309.00511 in full directly (do not delegate to subagents — per CLAUDE.md rule on Phase-5 deep-research depth-reading).
- Draft `SKEFTAxioms` structure in `lean/SKEFTHawking/GloriosoLiu/Axioms.lean` with six fields.
- Draft `DynamicalKMS [uv : UVRealization]` typeclass-parameterized family.
- Sharp Lean predicate for `LocalEquilibrium` parameterized over conservation pattern.
- Aristotle-probe the conjunction of the six axioms for inconsistency before stating the main theorem.

**Module decomposition (proposed):**
```
SKEFTHawking/GloriosoLiu/
  Axioms.lean              -- the 6-field SKEFTAxioms structure
  DynamicalKMS.lean        -- typeclass + UV-realization-indexed family
  LocalEquilibrium.lean    -- LE axiom + slow-mode infrastructure
  EntropyCurrent.lean      -- existence + Noether construction
  LocalSecondLaw.lean      -- ∂_μ J^μ_S ≥ 0 main theorem
  OnsagerReciprocity.lean  -- off-diagonal-symmetry derivation
  FirstOrderProjection.lean -- recovers FirstOrderKMS as first-order theorem
  Phase1Reconciliation.lean -- 4-of-9 partition recovery proof
```

**Bundle absorption.** D.3 into I1 (anchor case study reframe — user-auth REQUIRED pre-draft). D.3 incidentally into D3 + L3 (Glorioso-Liu citation form upgraded). All three covered by the same user-auth gate at I1.

**Risk axes** (numbered per Session-3 walkthrough):
1. Axiom inconsistency surfaced by Aristotle (low / high if hits — but publishable as productive-value).
2. Jain-Kovtun UV-ambiguity deeper than typeclass handles (medium / medium).
3. LE axiom too vague (medium / medium — predicate-design iteration).
4. SK-3 reflection positivity edge cases at IR (low / low-to-medium — measure-theoretic carve-out via "a.e." weakening).
5. Onsager reciprocity hides implicit assumption (medium / low-to-medium — surfaces as a finding either way).
6. 4-of-9 partition fails to recover cleanly (medium / medium-to-high — most directly publishable failure mode).
7. Sign-convention drift (low / low — pick GCL II convention explicitly).
8. Continuous-time framing limited until Itô lands (high / low — scope explicitly to discrete-time / formal-power-series).
9. D.3 absorption cascade across D3 + L3 + I1 (high / medium — pre-draft mitigates).
10. Published commitment to the axiom set (certain / medium long-term — name "Glorioso-Crossley-Liu axiomatic" not "the SK-EFT axiomatic").

---

## Wave 2b — G10-QCrooks-α productive-value Aristotle wave ✅ SHIPPED (Stage 1 + Stage 2-3 ℝ + ℂ-extension at Fin 2)

**Sub-wave status (Session 10 close):**
- ✅ **Stage 1 SHIPPED** (Session 6): 6 modules under `lean/SKEFTHawking/QuantumCrooks/` (Setup, Tasaki, Aberg, KafriDeffner, Quasiprobability, PerarnauLlobet) all imported into root. Substantive content includes:
  - `IsCrooksRatio` predicate + `IsCrooksRatio.symm` symmetry theorem (forward/reverse roles symmetric — load-bearing for trajectory-Crooks downstream Wave 2c/2d).
  - 4 candidate axiomatization predicates (Tasaki-TPM, Åberg-coherent, Kafri-Deffner-unital, Kirkwood-Dirac quasiprobability) at the Crooks-ratio-compatibility level. (Distinguishing structural content lives in Stage 2-3 schemes.)
  - **Parametric Perarnau-Llobet no-go theorem** `perarnau_llobet_no_go_parametric`: proves that under `h_disagree : ∃ β, trueAverage β ≠ firstMoment (canonicalTPM β)`, no MS satisfies `ReproducesAverageEnergy ∧ RecoversTPMOnDiagonal`. The load-bearing physics content (TPM averages disagree with true averages on coherent states) is the `h_disagree` hypothesis; the algebraic obstruction is proved fully via direct rewriting.
  - Contrapositive forms: `reproduces_avg_implies_fails_TPM` (Åberg-class result) + `recovers_TPM_implies_fails_avg` (Tasaki-class result).
- 📝 **Stage 1 working doc** at `temporary/working-docs/phase6n/wave_2b_QCrooks_stage1.md`: substrate analysis + module decomposition + risk register + primary-source bibitems.
- ✅ **Stage 2-3 (ℝ) SHIPPED** (Session 6, same session): 7th module `Concrete.lean` ships the canonical 2-level Perarnau-Llobet counterexample as concrete `Matrix (Fin 2) (Fin 2) ℝ` matrices (ρ_+ = !![1/2, 1/2; 1/2, 1/2], ρ_diag, H_i = diag(0,1), H_f = σ_x). Substantive theorems:
  - `trueAverage_perarnau ρ_plus = +1/2` (matrix-trace computation: Tr[H_f · ρ_+] − Tr[H_i · ρ_+])
  - `tpmAverage_perarnau ρ_plus = -1/2` (TPM projects coherences away → uses ρ_diag)
  - `perarnau_llobet_disagreement` — they differ
  - `perarnau_h_disagree` — disagreement witness specialized to the parametric framework
  - **`perarnau_llobet_no_go_quantum`** — the substantive quantum no-go theorem, derived from the parametric no-go + h_disagree witness
  All proved via direct Lean tactics (`simp` + `norm_num` + `Fin.sum_univ_two`); zero Aristotle escalation; zero sorry.
- ✅ **ℂ-extension at Fin 2 SHIPPED** (Session 10, 2026-05-05): 8th module `lean/SKEFTHawking/QuantumCrooks/ConcreteComplex.lean` (~14 substantive theorems + 10 defs):
  - `ρ_plus_ℂ : Matrix (Fin 2) (Fin 2) ℂ := !![1/2, 1/2; 1/2, 1/2]` — coherent-superposition density matrix in ℂ.
  - `H_f_perarnau_σy_ℂ : Matrix (Fin 2) (Fin 2) ℂ := !![0, -Complex.I; Complex.I, 0]` — σ_y Hamiltonian as a genuinely complex Hermitian example (purely imaginary off-diagonal entries; cannot be expressed in ℝ).
  - `trueAverage_perarnau_ℂ`, `tpmAverage_perarnau_ℂ`, `perarnau_llobet_disagreement_ℂ` — substantive ℂ analogs of the ℝ counterexample.
  - **`perarnau_llobet_no_go_complex`** — the ℂ-form quantum no-go theorem, proven by deriving the concrete ℂ-form `h_disagree` witness and feeding it to the parametric no-go.
  - ℂ↔ℝ cross-bridge: `Complex.ofReal` images of ℝ counterexample carry through.
  All proved via MCP; zero Aristotle escalation; zero sorry. Verifies the parametric no-go's substrate-independence — same algebraic obstruction holds whether the density matrices are real or complex.
- ⏳ **Further generalization (deferred)**: `Matrix (Fin n) (Fin n) ℂ` for arbitrary n; reservoir-coupled and unitary-evolution forms; KMSCompatible/FDTCompatible typeclass connections.

**Lake build state**: 8519 → 8526 → **8538 jobs (Session 10, +1 ConcreteComplex module)**, all clean, zero sorry across new modules.

**Substantive Stage-1+2-3 finding:** The Perarnau-Llobet no-go is proved fully via MCP (no Aristotle dependency). The parametric form decouples the algebraic obstruction from the quantum substrate; the substantive form specializes via a concrete numerical witness for `h_disagree` on a 2-level Hilbert space — first in `Matrix (Fin 2) (Fin 2) ℝ`, then in `Matrix (Fin 2) (Fin 2) ℂ` (Session 10) with σ_y as a genuinely complex example. This validates the "MCP-first" discipline — the program's existing infrastructure + Mathlib's matrix machinery were sufficient for the substantive lift across both substrates without requiring an Aristotle batch.

- *Integrates with:* Phase 1 FirstOrderKMS template (parallel-axiomatization Aristotle refutation tableau); the program's MTC / quantum-group / fusion-category infrastructure (operator-algebra typing for TPM, work distributions, Petz recovery, quasiprobabilities); QCyc16 / QCyc5Ext / native_decide finite-dimensional sandboxes; existing `KMSCompatible` / `FDTCompatible` / `GloriosoLiuMonotoneCompatible` typeclasses; Perarnau-Llobet-Bäumer-Hovhannisyan-Huber-Acín no-go theorem (Phys. Rev. Lett. 118, 070601, 2017).
- *New constraint adds:* (a) parallel-axiomatization tableau across at least four candidate quantum-Crooks axiomatizations — Tasaki-Crooks (Talkner-Hänggi 2007 arXiv:0705.1252) + Åberg fully-quantum (Phys. Rev. X 8, 011019, 2018) + Kafri-Deffner (Phys. Rev. A 86, 044302, 2012) + a quasiprobability axiomatization (Levy-Lostaglio or Francica); (b) Aristotle-driven concrete-witness search for finite-dimensional Hilbert-space counterexamples to each candidate's claimed equivalence with classical Crooks on diagonal states + average energy reproduction (the Perarnau-Llobet et al. no-go scheme); (c) the surviving axiomatizations connected to existing `KMSCompatible` / `FDTCompatible` content via typeclass hypothesis-bundle compatibility lemmas. **FirstOrderKMS² template** — the literature is more axiomatically unstable for quantum-Crooks than for first-order KMS; the program's Phase 1 productive-value methodology applies directly.
- *Tension surfaces:* if Aristotle finds zero refutations across the four candidate axiomatizations, the wave degrades to "we mapped the axiom space and stated the consistency relations" — still publishable but at lower yield. The Perarnau-Llobet et al. no-go theorem guarantees that *at least one* candidate must fail in any unified framework, so the expected refutation count is ≥ 1.

**Substrate.** Quantum-thermodynamics literature (Tasaki-Crooks / Åberg / Kafri-Deffner / Levy-Lostaglio quasiprobability). Perarnau-Llobet et al. no-go theorem as the structural anchor. Program's MTC / quantum-group infrastructure as the linear-algebra / operator-algebra typing.

**Stage 1 actions:**
- State Tasaki-Crooks for finite-dim discrete-spectrum H in `SKEFTHawking/QuantumCrooks/Tasaki.lean`.
- State Perarnau-Llobet et al. no-go as a parameterized obstruction in `SKEFTHawking/QuantumCrooks/PerarnauLlobet.lean`.
- State Åberg, Kafri-Deffner, and one quasiprobability axiomatization in parallel (one module each).
- Aristotle-probe each candidate's claimed equivalence with classical Crooks on diagonal states using QCyc16 / native_decide finite-dimensional sandboxes.
- Publish the refutation tableau as a productive-value paper at the FirstOrderKMS scale.

**Bundle absorption.** D.2 additive into D1 cross-bridge / D5 (refutation tableau as a section).

**Risk axes.** Aristotle returns no refutation (low — Perarnau-Llobet bounds ≥ 1); quasiprobability axiomatization choice (Levy-Lostaglio vs Francica) may not span the literature's full axiom-space (medium — name it explicitly + flag); the productive-value paper venue varies on whether the refutation tableau is "FirstOrderKMS template" or "novel". Aristotle is *not* the load-bearing tool for Wave 2a but IS for Wave 2b — submission gating per CLAUDE.md "User gets first & last call on Aristotle submissions."

---

## Wave 2c — G10-6n.3 Crooks-on-analog-Hawking on polariton SK-EFT substrate 🟡 IN-PROGRESS

**Sub-wave status (Session 8 close):**
- ✅ **Stage 1 SHIPPED** (Session 6): 2 modules under `lean/SKEFTHawking/CrooksAnalogHawking/`:
  - `HorizonDetailedBalance.lean` — predicate `HorizonDetailedBalance P_F P_R σ := ∀ W, P_F.P W = exp(σ W) · P_R.P (-W)` generalizes `IsCrooksRatio` (Wave 2b substrate) to nonlinear entropy-production functional `σ : ℝ → ℝ`. Substantive theorems: `specialize_to_Crooks` (cross-module bridge: `σ(W) = β·W` ⇒ HDB ↔ IsCrooksRatio), `symm_neg` (forward/reverse symmetry under σ ↦ −σ ∘ neg, generalizing `IsCrooksRatio.symm`), `horizonDetailedBalance_zero` (well-posedness witness).
  - `GallavottiCohen.lean` — predicate `GallavottiCohenSymmetry I := ∀ σ, I(-σ) - I(σ) = -σ` is the LDP rate-function symmetry (long-time / NESS limit of Crooks). Substantive theorems: `gallavottiCohen_linear_witness` (linear `I(σ) := σ/2` satisfies GC), `shift_invariant` (GC is gauge-invariant under constant shifts).
- ✅ **Stage 4 substrate-level GC bridge SHIPPED** (Session 8, 2026-05-05): new module `lean/SKEFTHawking/CrooksAnalogHawking/SKEFTGallavottiCohen.lean` ships 7 substantive theorems lifting Wave 2c rate-function content to substrate level paralleling Session-7 Wave 2d Stage 4. Headline content:
  - **W-form GC predicate** `WFormGallavottiCohen β I := ∀ W, I(W) - I(-W) = -β·W` — Crooks-form W-variable analog of σ-form `GallavottiCohenSymmetry`, the natural form to pair with `HorizonDetailedBalance`.
  - **FDT-pinned linear-response Gaussian rate function** `linearResponseRateFunction β σ² W := (W - β·σ²/2)² / (2·σ²)` with mean β·σ²/2 (FDT-pinning) and variance σ²; `linearResponseRateFunction_satisfies_WFormGC` proves W-form GC algebraically for any σ² ≠ 0 (`field_simp; ring`).
  - **Cross-bridge `WFormGallavottiCohen.to_σForm`** — change of variable σ = β·W + sign flip `J := -I ∘ (·/β)` converts W-form to σ-form GC (substantive cross-module identity between this module's predicate and the existing `GallavottiCohenSymmetry`).
  - **Substantive load-bearing-A theorem** `skeft_substrate_yields_WFormGC` — under SKEFTAxioms at β > 0, the proof body destructures `A.dynamical_KMS` to extract `c : FirstOrderCoeffs` with `FirstOrderKMS c β`, and the W-form GC clause activates conditionally on c.i₂ ≠ 0 (non-trivial noise; the conditional form is structural — degenerate substrates with c.i₂ = 0 don't carry meaningful rate functions).
  - **Concrete substantive witness** `firstOrderDissipative_yields_WFormGC` — for `firstOrderDissipativeAction(coeffs, β)` with γ₂ > 0 and β > 0, the explicit witness c = ⟨γ₁+γ₂, -γ₁, 0,0,0,0, γ₁/β, γ₂/β, 0⟩ has c.i₂ = γ₂/β > 0 and the W-form GC unconditionally holds. Parallel of Session-7 `helium3A_skeft_substantive_jacobsonConsistent`.
  - **Composed Stage-4 substrate** `skeft_yields_horizon_crooks_with_GC` + `firstOrderDissipative_yields_horizon_crooks_with_GC` — full composed substrate-level bridge: FDR-pinned σ = β·W (Session 7 Wave 2d) + Noether-density positivity (Session 7) + W-form GC (Session 8) ALL with the same single c extracted from `A.dynamical_KMS`. The strongest substantive Stage-4 statement of Wave 2c that does not require LDP infrastructure (Stage 5+ is the in-program build candidate per the Mathlib-upstream-PR pattern).

  Verlinde-vs-Jacobson distinction preserved at every Lean statement. MCP-driven, zero Aristotle.
- ⏳ **Stage 5+ NOT STARTED**: derive the third Sakharov-style biconditional
    ```
    AnalogHawkingSubstrate.satisfies_GLU_monotonicity (Wave 2a substrate)
      ↔ EmissionSpectrum.LDP_rate_function.satisfies_GallavottiCohen
    ```
  Requires the analog-Hawking spectrum substrate (Loganayagam–Martin or CGL-EFT) + LDP infrastructure. Buildable in-program via discrete-time / Markov-jump LDP per Falasco–Esposito 2025 RMP framework — **candidate Mathlib upstream-PR opportunity** per the program's track record (lean-tensor-categories, RingQuot, repl pinning).

**Lake build state**: 8534 → 8535 jobs (+1 module = SKEFTGallavottiCohen, Session 8), all clean, zero sorry.

**Substantive Stage-1 finding:** `HorizonDetailedBalance` is definitionally a generalization of `IsCrooksRatio` — the linear case σ(W) = β·W IS the classical Crooks ratio. This makes Wave 2c a substantive *generalization* of Wave 2b's classical-Crooks structure rather than a separate framework. The substantive analog-Hawking content is the constraint on σ from GLU monotonicity (Stage 2-3).

- *Integrates with:* E1 polariton paper; D1 §3-§4 analog Hawking content; Carusotto-Gerace polariton analog black-hole literature (arXiv:1206.4276 + follow-ups); Steinhauer BEC analog black holes; Tettamanti-Parola-Cacciatori arXiv:1703.05041 exactly-solvable BEC; **Loganayagam-Martin Exterior EFT for Hawking Radiation arXiv:2403.10654** (JHEP 2025) — the cleanest substrate for trajectory-Crooks at the horizon; Banerjee et al. fluctuation-dissipation horizon-temperature derivation (Eur. Phys. J. C 80, 411, 2020) as the FDT-level prior art.
- *New constraint adds:* an inequality on the analog-Hawking spectrum derived from trajectory-level Crooks detailed-balance, **beyond what FDT / Kubo gives**. Specifically: the LDP rate function I(σ) for entropy production must satisfy the GC/LS symmetry I(-σ) - I(σ) = -σ, which translates into specific bounds on the spectrum's higher cumulants of work / entropy-production fluctuations. Falsifiable on existing experimental platforms (Steinhauer BEC, Weinfurtner Vancouver surface-wave, Carusotto polariton). Comparable in character to Phase 6e Sakharov biconditional — third "Sakharov-style" biconditional in the program if non-trivial.
- *Tension surfaces:* the DR's main flag — "if the inequality is satisfied trivially given the existing thermal spectrum, the contribution is FDT-level only and not novel." The work itself is *deriving* what trajectory-Crooks structure constrains beyond FDT; the output of the derivation determines (a) trivial = formalization-only contribution, or (b) non-trivial = third Sakharov-style biconditional + falsifiable predictions.

**Substrate.** Two viable framings:
- (a) Glorioso-Crossley-Liu SK-EFT effective action as the path measure (closer to existing program infrastructure; uses Wave 2a as substrate).
- (b) Loganayagam-Martin exterior EFT for Hawking radiation (cleanest framework for trajectory-Crooks at the horizon specifically).

DR recommends (b) on top of (a): state the trajectory-Crooks predicate on Loganayagam-Martin exterior EFT; derive the inequality; verify it specializes correctly to the program's existing SK-EFT polariton content.

Discrete-time / Markov-jump-process scope (Falasco-Esposito 2025 RMP framework) — sufficient for falsifiability on Steinhauer / Weinfurtner / Carusotto. Continuous-time form deferred to post-Phase-6o-Itô.

**Stage 1 actions:**
- Read Loganayagam-Martin arXiv:2403.10654 in full directly.
- Draft `AnalogHawkingSubstrate` typeclass + `TrajectoryCrooksAtHorizon` predicate.
- Derive the inequality on the spectrum (paper math first; Lean theorem statement second).
- Specialize to Steinhauer / Weinfurtner / Carusotto device parameters; check against published spectrum + noise data.
- Add E1 falsification-window section.

**Bundle absorption.** D.2 additive into E1 (new section: "Trajectory-level Crooks constraint at the analog horizon"). D.2 cross-bridge into D1 only after Wave 2a closes (theorem-statement form follows Wave 2a's path-measure infrastructure).

**Risk axes.** Triviality risk (DR's main flag); continuous-vs-discrete-time framing (Markov-jump sufficient for falsifiability; cleaner version waits for Itô); Verlinde-Nagle contamination (Lean statements must invoke only Glorioso-Liu / Loganayagam-Martin substrate, not Verlinde — analogous discipline to Wave 2d); open-vs-closed-system framing (Loganayagam-Martin exterior EFT is open; CGL is closed; cross-walk lemma needed); substrate-specific verifiability variance.

---

## Wave 2d — G10-6n.7 Sakharov ↔ horizon-Crooks reformulation 🟡 IN-PROGRESS

**Sub-wave status (Session 6 close):**
- ✅ **Stage 1 Lean substrate SHIPPED** (Session 6): `lean/SKEFTHawking/CrooksAnalogHawking/SakharovHorizonCrooks.lean`:
  - `HorizonCrooksSubstrate` structure bundles existing `JacobsonThermoGRDarkEnergy.SakharovConditions` (Phase 6m Track C substrate) with the new `HorizonDetailedBalance` predicate (Wave 2c) into a single substrate type.
  - `jacobsonConsistent` predicate captures the Jacobson reading (NOT Verlinde — distinction preserved at every Lean statement per Phase 6n DR §7).
  - Concrete witnesses: `helium3A_horizon_crooks` (Sakharov true via JTGR7 → Jacobson-consistent) and `flsBEC_horizon_crooks` (Sakharov-(ii) fails via JTGR8 → not Jacobson-consistent).
  - `horizonCrooks_substrate_partition` theorem: substantive partition combining both witnesses — extends the existing Phase 6e+6m biconditional witnesses to the horizon-Crooks substrate type. Proof invokes `JacobsonThermoGRDarkEnergy.volovik_jannes_he3a_satisfies_sakharov_criterion` (JTGR7) and `flsBEC.universalCoupling = false` (JTGR8) — substantive cross-module bridge per P6 discipline.
- 📝 **D3+L3 reframing pre-draft EXISTS (uncommitted)** at `temporary/working-docs/phase6n/6n_zeta_D3_L3_reframing_predraft.md`. On-hold for user review per Session-5 user direction (bundle absorption deferred until full Phase 6n + 6o picture lands).
- ✅ **Wave dependency Wave 2a closed** at Lean-substrate level (Stage 1 + 2-3 + 2-3b shipped). Wave 2d Lean module work was unblocked, now Stage 1 SHIPPED.
- ⏳ **Stage 2-3 NOT STARTED**: full `Sakharov_iff_horizon_Crooks` biconditional derivation requires the GLU monotonicity bridge from Wave 2a's `SKEFTAxioms`/`hasDynamicalKMS_algebraic` substrate applied at the substrate-level Rindler horizon. Per `6n_gamma_kms_framework_finding.md` §6.2, the bridge MUST use the algebraic-FDR form (`hasDynamicalKMS_algebraic`), not strict-invariance.

**Lake build state**: 8528 → 8529 jobs (+1 module), all clean, zero sorry.

**Substantive Stage-1 finding:** the existing Phase 6e biconditional witnesses (³He-A satisfies / FLS BEC fails) extend cleanly to the horizon-Crooks substrate type via the `HorizonCrooksSubstrate` bundle. The partition theorem is a substantive cross-module statement preserving the Verlinde-vs-Jacobson distinction.

**🚨 USER AUTHORIZATION GATE (D.3 absorption — Stage B of `LATE_PHASE6_ABSORPTION_PROTOCOL.md`) — HELD:**

D3 + L3 currently express the Phase 6e Sakharov-criterion biconditional and the Phase 6m Track C JTGR survivors as separate results. The Wave 2d reformulation unifies them as one fluctuation-theorem result (`Sakharov_iff_horizon_Crooks` biconditional). This refines D3 + L3's published-claim profile — same content, deeper structural framing. **Pre-draft EXISTS for user review when bundle-absorption pass begins.**

**Wave dependency:** Wave 2d blocks on Wave 2a (Glorioso-Liu axiomatic) closing first. ✅ **SATISFIED** (Session 5 close).

- *Integrates with:* Phase 6e Sakharov-criterion biconditional (Λ_J = Λ_HK on ³He-A; falsified on FLS BEC); Phase 6m Track C JTGR survivors (M1, M2/M7, M3 Exp/ArcTanh, M4, M9; M8 conditional); Eling-Guedens-Jacobson f(R) (gr-qc/0602001) entropy-production term; Chirco-Eling-Liberati gravitational-dissipation (PRD 81, 024016) heat decomposition; Alonso-Serrano + Liška unimodular-from-Clausius (arXiv:2008.04805); Wave 2a Glorioso-Liu axiomatic substrate.
- *New constraint adds:* Sakharov criterion expressed as horizon-Crooks-detailed-balance (`P_F[γ] / P_R[γ̄] = exp(σ[γ])` at the local Rindler horizon); unifies Phase 6e biconditional + Phase 6m Track C JTGR survivors as one fluctuation-theorem result; **explicitly distinguishes Jacobson-allowed (equilibrium-Clausius-on-Rindler-horizon) from Verlinde-falsified (gravity-as-entropic-force) entropic-gravity readings**. Cleanest non-trivial deliverable in any Phase 6n track per the original DR.
- *Tension surfaces:* if the reformulation requires assumptions stronger than what Phase 6e + Phase 6m JTGR currently invoke, those extra assumptions become candidate sites for elimination-tournament probing. The Verlinde caveat is real: "fluctuation-theorem structure is present at the horizon (Jacobson level), not gravity *is* an entropic force (Verlinde level)" — this distinction must be carried through every Lean statement and bundle citation.

**Substrate.** Phase 6e Sakharov + Phase 6m Track C JTGR + Glorioso-Liu axiomatic (Wave 2a). Re-reading: not new derivation. Sakharov-class assignment from Phase 6m R5 (M1 + M9 in Sakharov-class; M2/M7 with epistemic flag; M3 in class-(b′); M4 in class-(b″); M8 OUTSIDE Sakharov-class).

**Stage 1 actions (after Wave 2a closes):**
- State `HorizonDetailedBalance` predicate parameterized over the substrate.
- State `Sakharov_iff_horizon_Crooks` biconditional theorem.
- Specialize to Phase 6e biconditional witnesses (³He-A holds; FLS BEC fails).
- Specialize to Phase 6m Track C JTGR survivors (verifies which class-(b/b′/b″) survivor admits horizon-Crooks reading).
- Pre-draft D3 + L3 reframing language for user-auth gate.

**Bundle absorption.** D.3 into D3 + L3 (refines biconditional + JTGR survivors' published-claim profile). User-auth REQUIRED pre-draft.

**Risk axes.** Reformulation may surface stronger-than-current assumption (publishable as elimination-tournament target either way); Verlinde-vs-Jacobson distinction must not slip in any single Lean theorem statement (compositional discipline); Wave 2a blocks delays Wave 2d start; D.3 absorption cascade across D3 + L3.

---

## Wave 1c — G8-W1 Atiyah-Singer reformulation memo (no Lean) ✅ SHIPPED

**Status (Session-5 close):**
- ✅ **Stage-1 memo SHIPPED (uncommitted working doc)** at `temporary/working-docs/phase6n/6n_eta_AS_reformulation_memo.md`: heat-kernel ↔ AS dictionary written explicitly for the program's a₀, a₂, a₄ content; Sakharov tr(I) ≠ 0 identified as McKean–Singer supertrace nonvanishing condition; instanton zero modes identified as ker(D); Z₁₆ anomaly identified as Witten–Yonekura η/16 mod 1 (cross-bridge to Wave 1b SymTFT).
- 🚩 **One potentially-new structural item flagged for Phase 6o**: APS eta-invariant for analog-horizon backgrounds (BEC-acoustic, ADW, ³He-A) — first systematic substrate-side APS-η computation on analog Hawking horizons would be publishable. Promoted to Phase 6o follow-up sub-wave per memo §6.3.
- ⏳ **Bundle absorption (D2 + D3 + E1 appendix) DEFERRED** per Session-5 user direction. Memo is non-load-bearing for any current bundle; absorption runs as part of unified Phase 6n + 6o bundle-absorption pass.

- *Integrates with:* Phase 6e a_n Seeley-DeWitt heat-kernel coefficients (Λ_HK derivation); Phase 6m Sakharov tr(I) ≠ 0 (the McKean-Singer supertrace nonvanishing condition); Phase 5z Goldstino zero modes (the APS boundary kernel `h(Σ)`); Bär-Strohmaier APS-index-theorem framework; Witten-Yonekura η-invariant content.
- *New constraint adds:* a physics-paper-grade memo (no Lean) restating Phase 6e a_n + Phase 6m Sakharov tr(I) + Phase 5z Goldstino zero modes in Atiyah-Singer / Bär-Strohmaier APS / Witten-Yonekura η-invariant language. The heat-kernel ↔ Atiyah-Singer dictionary is **already implicit** in the program; the memo makes it explicit. Free Tier-1 deliverable. Unlocks D2 / D3 / E1 appendix narratives at zero Lean cost.
- *Tension surfaces:* if the reformulation surfaces a *new* topological-invariant statement that is not implicit in the existing program content, the memo upgrades to a Lean-formalization wave (deferred to Phase 6o or later as G8-Lean-refactor opening). Likely: the memo stays as a memo. The *full* Mathlib Atiyah-Singer formalization is a multi-year community-coordination effort and stays deferred indefinitely.

**Substrate.** Existing program a_n + tr(I) + Goldstino content. Bär-Strohmaier *Index Theorems and Spectral Geometry* + Witten-Yonekura η-invariant literature as the reformulation framework.

**Stage 1 actions:**
- Draft memo at `temporary/working-docs/phase6n_AS_reformulation_memo.md` (~10-15 pages physics-paper-grade).
- Cross-reference Phase 6e a_n / Phase 6m Sakharov / Phase 5z Goldstino sections.
- Identify any *new* topological-invariant statement (if surfaces, deferred to Phase 6o).
- Add D2 / D3 / E1 appendix sections that reference the memo.

**Bundle absorption.** D.2 additive into D2 + D3 + E1 appendix (re-reading; no prior verdict overturned, no published-claim profile change). No user-auth required.

**Risk axes.** Memo format vs Lean-formalization split (low — explicitly scoped as memo); reformulation surfaces new content (low — known dictionary, low novelty risk); Lean refactor temptation (high — discipline against scope creep).

---

## User authorization gates — consolidated (Session-5 status)

**Two D.3 user-auth gates HELD pending unified Phase 6n + 6o bundle-absorption pass** (Session-5 user direction: "push forward on math/physics/infrastructure first; bundle absorption runs as one coherent pass once full picture lands"):

| Wave | Bundle(s) refined | Pre-draft deliverable | Status |
|---|---|---|---|
| **Wave 2a** Glorioso-Liu axiomatic | I1 (anchor case study reframe) + D3 + L3 (citation form upgrade) | `temporary/working-docs/phase6n/6n_gamma_I1_reframing_predraft.md` — drop-in §3 replacement prose for `papers/I1/paper_draft.tex` lines 282–340; "FirstOrderKMS as Phase 1 anchor → first-order projection of Glorioso-Liu axiomatic; the 4-of-9 partition is now a theorem, not an Aristotle-empirical observation." | 📝 **HELD** — pre-draft EXISTS; on-hold for unified absorption pass |
| **Wave 2d** Sakharov ↔ horizon-Crooks | D3 + L3 (Phase 6e + Phase 6m Track C JTGR survivors unified) | `temporary/working-docs/phase6n/6n_zeta_D3_L3_reframing_predraft.md` — D3 + L3 reframing language; "Phase 6e Sakharov biconditional + Phase 6m Track C JTGR survivors as parallel results → unified `Sakharov_iff_horizon_Crooks` biconditional. Verlinde-vs-Jacobson distinction explicit at every Lean statement." Per KMS framework finding §6.2: technical-accuracy footnote needed referencing algebraic-FDR `FirstOrderKMS` rather than strict-invariance `KMSSymmetry`. | 📝 **HELD** — pre-draft EXISTS; on-hold for unified absorption pass |

**Additional gates (standard per `WAVE_EXECUTION_PIPELINE.md`):**
- Stage 4 Aristotle submission (Wave 2b QCrooks-α specifically) — user gets first and last call. **Triggers when Wave 2b Lean substrate is decomposed and ready for batch.**
- Stage 13 adversarial review per absorbed bundle — fresh-context reviewer per `BUNDLE_LIFT_PROCEDURE.md` §11. **Triggers in unified bundle-absorption pass at end of Phase 6n + 6o.**
- ✅ **I3 bundle authorization GRANTED** (Session 4, 2026-05-04) — Pipeline Invariant #14 user-auth for new bundle target. Bundle architecture 13 → 14. Infrastructure shipped commit `a72ba68`: `papers/I3/{bundle_metadata.json, source_manifest.md, change_log.md, append_log.json}` + bundle-count test assertion fix commit `82e9aba` + PAPER_DRAFT_MAPPING.md row 69 fix commit `70f0dea`. Scoping doc at `temporary/working-docs/phase6n/i3_bundle_scoping.md` (working doc; substantive scoping pending Phase 6o opening).

---

## Phase 6o handoff (preview — full detail in `Phase6o_Roadmap.md` stub)

Tracks deferred to Phase 6o per Session-3 quality-priority (and additions surfaced in Sessions 4–5):
- **G3 boostless / Carrollian soft-theorem program** for emergent post-erasure U(1) + ADW graviton (Tier 1).
- **G4-Kerr-Schild** classical double-copy on Petrov-D analog metrics (the draining-bathtub acoustic metric IS Petrov D; Tier 2).
- **G1-Schellekens chain** (spin-bordism → anomaly polynomial → modular invariance → Niemeier → Schellekens c=24 holomorphic-VOA classification corollary; Tier 1).
- **G1-NO-GO writeup** (dissipative SK-EFT bootstrap can't produce uniqueness with current axioms; Tier 4 NO-GO).
- **G10-ETH-α** productive-value Aristotle wave on Inozemcev–Volovich gap (Tier 2).
- **G10-Itô + LDP-α + LDP-β as I3 community contribution** — ✅ Pipeline Invariant #14 user-auth GRANTED (Session 4); bundle infrastructure shipped; substantive scoping at `temporary/working-docs/phase6n/i3_bundle_scoping.md` awaits Phase 6o opening.
- **APS-η for analog-horizon backgrounds** (NEW, surfaced by Wave 1c memo §6.3): first systematic substrate-side APS-η computation on BEC-acoustic / ADW / ³He-A horizons; would (a) sharpen L3 regime-partition with topological-invariant distinction, (b) connect to Wave 1b SymTFT via Witten–Yonekura η-invariant identification, (c) be publishable as first such calculation on analog Hawking horizons.
- **Wave 1a.3 Path A precision** (multi-session BdG self-energy through order 7) — pending user decision per VERDICT §7. If authorized, runs as a Phase 6o sub-wave delivering definitive Gevrey-1-or-geometric verdict + numerical Λ_UV (or absence thereof) + structural-finding writeup.
- **Unified Phase 6n + 6o → Phase 7 bundle-absorption pass** — runs at end of Phase 6o per Session-5 user direction. Absorbs all D.2/D.3 events from Phase 6n (Wave 1a + Wave 1b + Wave 2a I1 + Wave 2a D3+L3 + Wave 2b + Wave 2c + Wave 2d + Wave 1c) and Phase 6o (TBD) as one coherent pass per `LATE_PHASE6_ABSORPTION_PROTOCOL.md` Stages A–G. Two D.3 user-auth gates (Wave 2a I1, Wave 2d D3+L3) trigger at start of this pass.

Phase 6o framing locks at the next user-input gate (after Phase 6n math closes — i.e., after Wave 2b + Wave 2c + Wave 2d Lean ship, plus optionally Wave 1b Stage-2 Lean and Wave 1a.3 Path A).

---

## Cross-references

- `temporary/working-docs/brainstorm/20260504-GUToE/Phase6n-6z-GUToE-Brainstorm_1.md` — full session record (Sessions 1, 2, 3); decisions log; deep-research dispatch table.
- `Lit-Search/_Exploratory/` — 8 deep-research returns (Sessions 2 inputs).
- `Lit-Search/_Exploratory/Appendix- Re-assessment of Deferred Tracks for SK_EFT_Hawking Phase 6n+.md` — calibration rule + 3-of-4 reversal recommendations on G10 deferred tracks.
- `docs/PAPER_STRATEGY.md` — 13-bundle architecture (note: §2 headline says "eleven publication targets" while the rest treats 13; minor doc drift to be corrected at I3 authorization or earlier).
- `docs/PAPER_DRAFT_MAPPING.md` — per-source → per-bundle mapping; new rows added per Phase 6n wave at Stage 12.
- `docs/BUNDLE_LIFT_PROCEDURE.md` — frozen 14-step canonical lift workflow.
- `docs/LATE_PHASE6_ABSORPTION_PROTOCOL.md` — frozen Stage A–G robustness protocol; D.2 / D.3 / D.4 branch decision matrix.
- `docs/WAVE_EXECUTION_PIPELINE.md` — 14-stage process (canonical).
- `docs/agents/claims-reviewer-bundle-prompts.md` — per-bundle Stage-13 anchor list (consumed by Stage F reviewer-triple re-invocation).
- `docs/ARCHITECTURE_SCOPE.md` — predictive-scope boundary; Phase 6n updates the SK-EFT-axiomatic section + the Crooks-on-analog-Hawking falsifiability content.
- `docs/roadmaps/Phase7_Roadmap.md` — Phase 7 umbrella (16 waves; 7 sub-phases 7a–7g).
- `docs/roadmaps/Phase7a_Roadmap.md` — Phase 7a sub-roadmap (Phase 6m closure + Phase 7 Wave 0 + I1 + I2 + robustness infrastructure).
- `docs/roadmaps/Phase6m_Roadmap.md` — predecessor phase; format reference.
- `docs/roadmaps/Phase6o_Roadmap.md` — successor phase stub.

---

*Created Phase 6n initialization (2026-05-04). Sessions 1–5 closed 2026-05-04. Sessions 6–12 closed 2026-05-05.

**Session 12 close (2026-05-05):** Wave 1a.3 Path A Stage 3 LEAN LIFT SHIPPED (this revision). NEW Lean module `lean/SKEFTHawking/Resurgence/KinematicDispersive.lean` (~10 substantive theorems + 2 defs) lifting the Session-11 closed-form `γ_n^(kin-disp)/γ_1 = (-1)^(n-1)·C(2(n-1),n-1)/16^(n-1)` to the Lean substrate via `Nat.choose_le_two_pow` (`C(2k,k) ≤ 4^k`) → `kinDispSeq_isGeometric : IsGeometric kinDispSeq 1 (1/4)` (Lean-level statement of the Stage-3 finding). `Resurgence/Basic.lean` extended with `IsGeometric` predicate parallel to `IsGevrey1`. Cross-module Padé-Borel verification: 3 new integration tests in `test_bdg_self_energy.py` connecting Stage-3 sequence to `borel.py` confirm ratio-test → 0 (geometric NOT Gevrey-1), `leading_borel_singularity = None`, `lambda_uv_estimate = None`. All headline Lean theorems verify to standard-kernel-only via `lean_verify`. Pytest 4080 passed (+3 vs Session 11). **Lake build 8539 jobs** (+1 vs Session-11 8538: KinematicDispersive). MCP-driven, zero Aristotle, zero new axioms, zero new sorry.

**Session 11 close (2026-05-05):** Wave 1a.3 Path A Stage 3 γ_2 NLO kinematic-dispersive SHIPPED (Python).

**Session 10 close (2026-05-05):** 3 commits ahead of origin/main (Session 8 `e0e5160` + Session 9 `145c908` + Session 10 `dc64aa1`). Authoritative project counts: **5433 thms (5408 substantive) / 274 modules / 0 sorry / 1 axiom / 4744 defs / Lake 8538 jobs PASS / pytest 4062 passed**.

**Authoritative post-Session-12 counts (counts.json regen 2026-05-05-2100):** **5448 thms (5423 substantive) / 275 modules / 0 sorry / 1 axiom / 4747 defs / 322 Aristotle-proved / Lake 8539 jobs PASS / pytest 4080 passed**. Δ vs Session 11: thms +15 substantive, modules +1 (KinematicDispersive), defs +3.

Per-wave Session-12 close state:
- **Wave 1a** 🟡 IN-PROGRESS (1a.1+1a.2+1a.4 SHIPPED; 1a.3 Path B verdict shipped; 1a.3 Path A Stage 1+2+3 Python SHIPPED — γ_1 = 3/(640π) Beliaev LO Session 10 + γ_2^(kin-disp) = −γ_1/8 + closed-form general-n + convergence radius 2 · Λ_UV Session 11; **1a.3 Path A Stage 3 Lean lift SHIPPED Session 12** — `Resurgence/KinematicDispersive.lean` + `IsGeometric` predicate in `Basic.lean` + cross-module Padé-Borel verification; Stage 4 γ_2^(loop) Beliaev-Galitskii 1959 + γ_3-γ_5 next-up)
- **Wave 1b** 🟡 IN-PROGRESS (Stages 1+2+3+4+4c+5 project-local Witt-class SHIPPED; Mathlib upstream-PR target retained at full MTC + Witt-group level; continuous-sector follow-on deferred to Phase 6o)
- **Wave 1c** ✅ SHIPPED (AS reformulation memo; APS-η for analog horizons promoted to Phase 6o)
- **Wave 2a** ✅ SHIPPED (Lean substrate; R1 LIFTED; Stage 2-3b KMS framework; Session 7 load-bearing deepening; Session 9 SecondOrderProjection cross-track unification; D.3 absorption I1 pre-draft HELD per Session-5 user direction)
- **Wave 2b** ✅ SHIPPED (Stage 1 + Stage 2-3 ℝ + Session-10 ℂ-extension at Fin 2; further Fin n / reservoir-coupled / typeclass-connections deferred)
- **Wave 2c** ✅ SHIPPED (Stage 1 + Stage 2-3 + Session-8 Stage 4 W-form GC bridge; Stage 5 LDP infrastructure flagged as in-program build candidate)
- **Wave 2d** ✅ SHIPPED (Stage 1 + Stage 2-3 + Session-7 Stage 4 SKEFTHorizonBridge substrate-level form; D.3 absorption D3+L3 pre-draft HELD)
- **I3 bundle infrastructure** ✅ SHIPPED (Pipeline Invariant #14 user-auth GRANTED; substantive scoping pending Phase 6o opening)

**Bundle absorption HELD per Session-5 user direction** — runs as unified Phase 6n + 6o → Phase 7 absorption pass once full math/Lean picture lands. Two D.3 user-auth gates (Wave 2a I1, Wave 2d D3+L3) trigger at start of that pass.

**Highest-leverage Session 13+ candidates:** (a) **Wave 1a.3 Path A Stage 4** — γ_2^(loop) Beliaev-Galitskii 1959 2-loop self-energy + γ_3-γ_5 systematic loop enumeration via modern higher-loop ERG references (Pawlowski-Strodthoff 2015, Boettcher et al.); Stage 4.5 re-runs Padé-Borel on full (kin-disp + loop) γ_n sequence to give near-definitive Gevrey-1-or-geometric verdict. Wave 1a.2 Padé-Borel infrastructure now end-to-end-verified per Session-12 cross-module tests, ready to consume the full sequence. (b) Wave 2c Stage 5 LDP infrastructure for the third Sakharov-style biconditional (in-program build candidate per program's Mathlib-upstream-PR track record). (c) Phase 6o opening (APS-η analog-horizons sub-wave + G3 Carrollian + G4 Kerr-Schild + Itô community contribution).

**Session 14 close (2026-05-05) — SEVEN substantive lifts SHIPPED closing Phase 6n math/Lean work.** Authoritative post-Session-14 counts (counts.json regen 2026-05-05 13:50): **5507 theorems (5482 substantive) / 280 modules / 0 sorry / 1 axiom / 4779 definitions / Lake 8544 jobs / pytest 4111 passed.** Δ vs Session 13: thms +29 substantive, modules +2, defs +11. All MCP-driven, zero Aristotle, zero new sorry, zero new axioms, zero new placeholders.

**Category A — Typeclass/substrate extensions (3 lifts):**
- ✅ **A1 Wave 2a Stage 2-3b polish** (already shipped pre-session): `SKEFTAxioms_for_dissipative` + `hasDynamicalKMS_algebraic` confirmed shipped; `A.dynamical_KMS` extracts substantive `c : FirstOrderCoeffs` with `FirstOrderKMS c β`. Standard kernel only.
- ✅ **A2 Wave 2b → Wave 2a typeclass connections SHIPPED**: NEW Lean module `lean/SKEFTHawking/QuantumCrooks/SKEFTConnection.lean` (~280 lines, 9 substantive theorems + 3 defs). FDT-pinned linear-response Gaussian `linearResponseMeasurementScheme σ²`; **first non-trivial substantive witnesses** for `IsTasakiTPMScheme`, `IsAbergCoherentScheme`, `IsKafriDeffnerUnitalScheme`, `IsKirkwoodDiracScheme` (previously only `trivialScheme_is_*` zero-distribution witnesses). **Wave 2a → Wave 2b cross-bridge `skeft_substrate_yields_TPM_scheme`** extracts c from `A.dynamical_KMS`; conditional on `c.i₂ ≠ 0` all four Stage-1 axiomatization predicates simultaneously satisfied. Concrete unconditional witness `firstOrderDissipative_yields_TPM_scheme` for γ₂ > 0. 9-conjunct closure summary `wave_2b_typeclass_connections_closure`.
- ✅ **A3 Wave 2b reservoir-coupled / Lindblad-DB forms SHIPPED**: NEW Lean module `lean/SKEFTHawking/QuantumCrooks/ReservoirCoupled.lean` (7 substantive theorems + 2 defs). `IsReservoirCoupled MS β` strengthens `IsCrooksRatio` with substantive non-vacuity `0 < (MS.forward β).P 0`; `IsLindbladDetailedBalance MS β` Stage-1-aliased. **Substantive separation `trivialScheme_not_isReservoirCoupled`** — zero scheme is NOT reservoir-coupled (the linear-response Gaussian is the FIRST non-trivial witness via `Real.exp_pos`). Wave 2a → Wave 2b cross-bridge `skeft_substrate_yields_lindbladDB_scheme`; concrete `firstOrderDissipative_yields_lindbladDB_scheme`. 11-conjunct closure summary `wave_2b_reservoir_coupled_closure`.

**Category B — Wave 1a.3 Stage 4b parametric + Stage 5 sharpened verdict (2 lifts):**
- ✅ **B1 Stage 4b parametric scaling SHIPPED**: Per Explore-agent re-scoping (Session 14 dispatch) the Beliaev-Galitskii 1959 2-loop integrand IS available in chewable form via Shi-Griffin Phys. Rep. 304 (1998) §3.4-3.5 Eqs. (3.86)-(3.95) — re-scoped from "multi-week" to **few-session SymPy task** with algebraic surgery (Keldysh-vs-Matsubara reconciliation, Hugenholtz-Pines counterterm, off-shell vs on-shell ordering). Shi-Griffin not in primary-source cache; explicit prefactor remains a deferred few-session task. Shipped at parametric-scaling level: Python `bdg_self_energy.py` extended with `BELIAEV_2LOOP_KINEMATIC_RATE = 0.25` + `loop_envelope_at_dilute_bec(na³, prefactor)` + `stage_4b_dilute_bec_verdict`; +12 tests; Lean `Resurgence/LoopEnvelope.lean` extended with **`kinDispSeq_add_loop_rate_preserved`** (when r_loop ≤ 1/4, full γ_n geometric at rate exactly 1/4) + `wave_1a_3_stage4b_rate_preservation_closure`. **Substantive Stage-4b finding:** the Bogoliubov dispersion factor `[1+(ξk/2)²]^(-1/2)` is loop-order-INDEPENDENT (substrate property), so r_loop = 1/4 across loop orders rigorously. `M_loop ~ √(na³)` parametric scaling (Shi-Griffin §3.6 dilute-gas expansion). For Steinhauer-class na³ ~ 10⁻⁴, M_loop ~ 0.01 (entirely sub-leading to kinematic-piece M = 1).
- ✅ **B2 Stage 5 sharpened verdict + γ_6-γ_7 SHIPPED**: `stage_5_sharpened_verdict(na³, prefactor, max_order=7)` runs full Wave 1a.2 Padé-Borel pipeline on kinematic sequence through γ_7, composing with Stage 4a/4b verdicts. Returns: ratio test monotone decay, `leading_borel_singularity = None`, `lambda_uv_estimate = None`, `is_geometric_not_gevrey1 = True`, full `convergence_radius_xik = 2 = 2·Λ_UV`. +7 tests. **Substantive Stage-5 verdict:** "BEC SK-EFT geometrically convergent (NOT Gevrey-1); no resurgence Λ_UV; rate exactly 1/4 in the bounded-loop regime; convergence radius ξk = 2 = 2·Λ_UV." Lean substrate-level Stage-5 closure provided by B1's `wave_1a_3_stage4b_rate_preservation_closure` (no separate Lean module needed).

**Category C — Mathlib upstream-PR-class project-local extensions (2 lifts):**
- ✅ **C1 Project-local Witt-class quotient infrastructure SHIPPED**: Lean `SymTFTAudit/WittClass.lean` extended with §6 Project-local Witt class quotient: `WittClass := Quotient wittSetoid` + `WittClass.mk : ℤ → WittClass` + `WittClass.toInvariant : WittClass → WittInvariant` (Quotient.lift of fromChiralCentralCharge). **Substantive bijectivity**: `WittClass.toInvariant_injective` + `WittClass.toInvariant_surjective` ⇒ `WittClass` IS structurally bijective with `ZMod 24` at the chiral-central-charge level. Standard Model 3-gen / 1-gen / 2-gen quotient-type witnesses. `stage5_quotient_closure_summary` 5-conjunct. Full Mathlib MTC + Witt-group + Anderson dual + Lagrangian algebra remains a multi-year community project.
- ✅ **C2 Non-Gaussian LDP rate-function generalization SHIPPED**: Lean `CrooksAnalogHawking/LDPLinearResponse.lean` extended with §6 Non-Gaussian: `nonGaussianRateFunction β g` (linear bias + arbitrary even part); **`WFormGC_iff_linear_bias_plus_even`** characterization biconditional showing `WFormGallavottiCohen β I ↔ ∃ g even, I W = -β·W/2 + g(W)`; concrete `quarticRateFunction β k` non-Gaussian witness with `g(W) = k·W^4`. `wave_2c_stage5_nonGaussian_closure`. **Substantive Stage-5+ finding:** the W-form GC is *equivalent to* the "linear bias + even part" decomposition — non-Gaussian shapes (quartic, Kramers-style logarithmic, two-state-Markov-chain bimodal) all satisfy W-form GC by construction.

Per-wave Session-14 close state:
- **Wave 1a** 🟡 IN-PROGRESS (1a.1+1a.2+1a.4 SHIPPED; 1a.3 Path B verdict; 1a.3 Path A Stage 1+2+3 Python+Lean SHIPPED; **1a.3 Path A Stage 4a (envelope) SHIPPED Session 13**; **1a.3 Path A Stage 4b (parametric scaling) SHIPPED Session 14**; **1a.3 Path A Stage 5 (sharpened verdict + γ_6-γ_7) SHIPPED Session 14**; explicit γ_2^(loop) precision-tightening DEFERRED — few-session SymPy task pending Shi-Griffin Phys. Rep. 304 (1998) primary-source cache fetch, no longer load-bearing for substrate verdict)
- **Wave 1b** 🟡 IN-PROGRESS (Stages 1+2+3+4+4c+5 project-local Witt-class SHIPPED; **Stage 5 §6 quotient infrastructure SHIPPED Session 14** — `WittClass := Quotient wittSetoid` bijective with `ZMod 24`; Mathlib upstream-PR target retained at full MTC + Witt-group level)
- **Wave 1c** ✅ SHIPPED (AS reformulation memo; APS-η for analog horizons promoted to Phase 6o)
- **Wave 2a** ✅ SHIPPED (Lean substrate complete; **A1 Stage 2-3b polish refactor confirmed shipped pre-Session-14** — `SKEFTAxioms_for_dissipative` + `hasDynamicalKMS_algebraic`; D.3 absorption I1 pre-draft HELD per Session-5)
- **Wave 2b** ✅ SHIPPED (Stage 1 + Stage 2-3 ℝ + ℂ-Fin 2 + higher-dim ℂ Session-13; **A2 typeclass connections SHIPPED Session 14**; **A3 reservoir-coupled / Lindblad-DB forms SHIPPED Session 14**)
- **Wave 2c** ✅ SHIPPED (Stage 1 + Stage 2-3 + Stage 4 W-form GC bridge + Stage 5 LDP starter W-form + σ-form Session-13; **C2 §6 non-Gaussian generalization SHIPPED Session 14**)
- **Wave 2d** ✅ SHIPPED (Stage 1 + Stage 2-3 + Session-7 Stage 4 SKEFTHorizonBridge; D.3 absorption D3+L3 pre-draft HELD)
- **I3 bundle infrastructure** ✅ SHIPPED (Pipeline Invariant #14 user-auth GRANTED; substantive scoping pending Phase 6o opening)

**Phase 6n FIRST-PASS substrate work: complete across all 7 waves.** But **Phase 6n is NOT YET CLOSED** — the corrected framing (per user direction Session 14 close) recognizes that the Wave 1b/2c upstream-PR-grade Mathlib continuations are direct sub-wave continuations within Phase 6n, NOT Phase 6o territory. The earlier "Phase 6o" framing for these items was reflexive deference, not grounded in topical scope. Phase 6o stays for genuinely topically-NEW content per the existing handoff list.

**Strengthening pass (Session 14 close):** S1 `ReservoirCoupled.lean §4` adds `ThermalJumpPair` Stage 2-3 substrate (genuinely substantive Stage-2-3 form distinguishing from Stage-1 alias); S2 `WittClass.lean §7` adds `AddCommGroup WittClass` instance via `Equiv.addCommGroup` transport (project-local Witt group structure); S3 `SKEFTConnection.lean §6` consolidates quad-witness redundancy via `linearResponseMeasurementScheme_satisfiesAllStage1Axiomatizations` master theorem. All standard-kernel-only.

**Wave-by-wave continuation scope (Session 16+ Phase 6n):**

- **Wave 1a 🟡** — Stage 4b explicit γ_2^(loop) DEFERRED pending Shi-Griffin Phys. Rep. 304 (1998) primary-source cache fetch (`scripts/back_fill_primary_sources.py` for bibkey `ShiGriffin1998`). Few-session SymPy lift after cache fetch. **No longer load-bearing for substrate verdict** (Stage 4b parametric scaling makes the rate-preservation finding rigorous via the substrate-property argument).

- **Wave 1b 🟡** — Stage 5 project-local Witt-class shipped through Stage 5.7 (AddCommGroup transport). **Stage 5.8 Drinfeld-center predicate SHIPPED Session 15** (NEW `SymTFTAudit/DrinfeldCenter.lean` §1–§4). **Stage 5.9 braided strengthening SHIPPED Session 16** (extends `SymTFTAudit/DrinfeldCenter.lean` §5–§9 with `IsBraidedEquivalence (e : C ≌ D) : Prop` via Mathlib's `Functor.Braided` typeclass + `Center.braidedCategoryCenter`; `WittEquivalentMTC_braided` is the genuine DMNO 2010 form; *refined* hypothesis schema `CentralChargePreservesDrinfeldCenter_braided` exactly matching DMNO 2010). **Stage 5.11 DMNO 2010 Theorem 5.2 substrate + restricted-form discharge SHIPPED Session 17 (2026-05-05)** ahead of Stage 5.10 per substrate-readiness discovery (5.10 Deligne ⊠ requires multi-session in-program build of `FreeKLinearCategory` quotient + transferring monoidal/braided structure; 5.11 is rich-substrate single-session). **Stage 5.10a SHIPPED Session 18 (2026-05-05)** — NEW `SymTFTAudit/FreeKLinearCategory.lean` (~21 substantive thms + 7 defs/instances): `FreeKLinear C k` type synonym + `freeComp` k-bilinear composition via `Finsupp.sum` + `Category`/`Preadditive`/`Linear k` instances + `incl : C ⥤ FreeKLinear C k` faithful embedding + `lift : (C ⥤ D) → (FreeKLinear C k ⥤ D)` universal property for k-linear D + `lift_comp_incl : incl ⋙ lift F = F` compatibility + 4-conjunct `stage5_10a_freeKLinear_closure`. Implementation finding: Path 1 implementable directly via `Finsupp` + `Finsupp.induction_linear` triple-bilinearity-induction without needing `Quotient.preadditive`/`Quotient.Linear.smul`. All headlines standard-kernel-only via `lean_verify`. Continuations: 5.10b monoidal extension, 5.10c-e Deligne ⊠ proper as quotient by k-bilinear-relations, 5.10f braided lift, 5.10g cross-bridge to `WittClass` via central-charge additivity under ⊠. NEW `SymTFTAudit/PseudoUnitary.lean` (~12 substantive thms + 4 defs): §1 `PreModularData.IsPseudoUnitary D := D.unitary ∧ D.modular ∧ D.symmetric` data-level predicate; load-bearing `IsPseudoUnitary.S_squared_eq_one` algebraic consequence (uses both unitarity and symmetry non-trivially); §2 trivial 1×1 concrete witness `trivialPreModular_isPseudoUnitary` with substantive matrix-arithmetic proofs; §2′ rank-2 SU(2)₁ Hadamard concrete witness `su2k1_isPseudoUnitary` (substantive proof using `(√2)² = 2` + Hadamard-cancellation, composes pre-existing `su2k1_modular`/`su2k1_symmetric` from `RibbonCategory.lean`); §3 category-level `IsPseudoUnitary C := ∃ D : PreModularData ℝ, D.IsPseudoUnitary` with universal trivial-datum witness; §4 *restricted* hypothesis schema `CentralChargePreservesDrinfeldCenter_pseudoUnitary` strictly weaker than the Wave 1b.5.9 unrestricted braided form — exactly matching DMNO 2010 Theorem 5.2 statement; §5 strict-weakening cross-bridges `CentralChargePreservesDrinfeldCenter_braided.toPseudoUnitary` + reverse `.toBraided` (under universal trivial-witness, equivalent at this layer); biconditional `central_charge_braided_iff_pseudoUnitary` capturing the architectural-equivalence at trivial-witness layer; §6 `wittEquivalentMTC_braided_pseudoUnitary_implies_wittClass_eq` cross-bridge to integer-mod-24 quotient via `Quotient.sound`; §7 4-conjunct `stage5_11_pseudoUnitary_closure` (S²=I + concrete witness + schema iff + WittClass cross-bridge). All headlines `[propext, Classical.choice, Quot.sound]` standard-kernel-only via `lean_verify` (the SU(2)₁ witness inherits transitive `native_decide` axioms from project-baked `SU2kFusion.lean` substrate, not introduced by Session 17). **Substantive finding:** Wave 1b.5.11's restricted form captures the DMNO 2010 statement architecturally even at the trivial-witness layer; Wave 1b.5.12+ refinement of `IsPseudoUnitary` (requiring the witness data to be *induced* by C's modular structure) breaks the trivial-witness equivalence and exposes the true Theorem 5.2 substantive content. **Stage 5.10 Deligne tensor product as group operation** (ref Davydov-Müger-Nikshych-Ostrik 2010 arXiv:1009.2117 + Deligne 2002) — substrate scout (Session 17 pre-flight) confirmed this is a multi-session in-program build target: Mathlib has all of `MonoidalLinear`, `MonoidalPreadditive`, `Functor.Braided`, `Center.braidedCategoryCenter`, `externalProductBifunctor` (on functor diagrams), `LinearMap.lTensor`/`rTensor`, `Quotient.mk`, `Idempotents.Karoubi` — gap is the `DeligneTensor : Cat → Cat → Cat` constructor itself + the proof that central charge is additive under ⊠. Best path: free k-linear category quotient by k-bilinear-relations setoid (Path 1 from substrate scout). Lagrangian algebra (within MTC framework) chains naturally off Stage 5.10.

- **Wave 1c ✅** — APS-η for analog horizons promoted to Phase 6o (genuinely topical extension per memo §6.3).

- **Wave 2a ✅** — Stage 2-3b polish refactor confirmed shipped Session 14 (`SKEFTAxioms_for_dissipative` + `hasDynamicalKMS_algebraic`). D.3 absorption I1 pre-draft HELD per Session-5.

- **Wave 2b ✅** — First-pass complete: Stages 1, 2-3 ℝ, ℂ-Fin 2, higher-dim ℂ, Stage 4 typeclass connections, Stage 5 reservoir-coupled / Lindblad-DB, Stage 5b ThermalJumpPair Stage-2-3 substrate. Further generalization to full Lindblad master equation on density-matrix substrate would extend Wave 2b but no current paper requires it.

- **Wave 2c 🟡** — Stages 1, 2-3, 4 W-form GC bridge, 5 LDP starter linear-response (Gaussian + σ-form), 5b non-Gaussian characterization shipped. **Stage 5c+ Cramér / Varadhan / Gärtner-Ellis full LDP framework** is the natural Wave 2c continuation: abstract LDP rate function class with lower-semicontinuity (2-3 sessions) → Cramér IID (3-5) → Varadhan's lemma (2-3) → Gärtner-Ellis non-IID (3-5). Total ~10-15 sessions. Substrate ready: Mathlib MeasureTheory.

- **Wave 2d ✅** — Stage 4 SKEFTHorizonBridge shipped. D.3 absorption D3+L3 pre-draft HELD per Session-5.

**Phase 6o handoff (genuinely topically-new content):**
- APS-η for analog horizons (Wave 1c memo §6.3 + substantive Lean substrate)
- G3 boostless / Carrollian soft-theorem program
- G4 Kerr-Schild classical double-copy
- G10-ETH-α productive-value Aristotle wave
- G1-Schellekens chain (broader classification project; spin-bordism → anomaly polynomial → modular invariance → Niemeier → c=24 holomorphic VOA classification — beyond Wave 1b chiral-central-charge piece)
- G1-NO-GO writeup
- I3 Itô + LDP-α + LDP-β community contribution (user-authorized as new bundle, Session 4)
- Wave 1b continuous-sector follow-on (gauge_erasure, GD_orthogonality)

**Session 15 close (2026-05-05) — Wave 1b.5.8 SHIPPED.** NEW Lean module `SymTFTAudit/DrinfeldCenter.lean` (~190 lines, 6 substantive thms + 2 defs) lifts Davydov-Müger-Nikshych-Ostrik 2010 categorical characterization of Witt equivalence to Lean substrate using Mathlib's `CategoryTheory.Center`. `WittEquivalentMTC C D := Nonempty (Center C ≌ Center D)`; equivalence-relation properties via Mathlib's categorical `Equivalence.refl/.symm/.trans`; `CentralChargePreservesDrinfeldCenter` Prop-level hypothesis (NO new axiom — discharge target for Wave 1b.5.11); load-bearing `wittEquivalentMTC_implies_wittClass_eq` cross-bridge through `Quotient.sound` to integer-mod-24 `WittClass.mk` equality; 4-conjunct closure. Substrate discovery: `Center.braidedCategoryCenter` IS in Mathlib — Wave 1b.5.9 strengthening to *braided* equivalence is unblocked from Mathlib infrastructure side.

**Session 16 close (2026-05-05) — Wave 1b.5.9 SHIPPED (braided strengthening).** Extends `SymTFTAudit/DrinfeldCenter.lean` §5–§9 with the predicate-level *braided* equivalence form matching DMNO 2010 exactly. New content (13 substantive thms + 2 defs + 1 structure):
- §5 `IsBraidedEquivalence (e : C ≌ D) : Prop` — structure with two `Nonempty F.Braided` fields exploiting Mathlib's `Functor.Braided` typeclass; `IsBraidedEquivalence.refl/.symm/.trans` use Mathlib's identity-Braided + composition-Braided instances directly (refl uses identity-Braided on the Drinfeld center via `Center.braidedCategoryCenter`, symm uses `e.symm.functor = e.inverse` definitionally, trans uses Mathlib's `(F ⋙ G).Braided` instance).
- §6 `WittEquivalentMTC_braided C D := ∃ e : Center C ≌ Center D, IsBraidedEquivalence e` — genuine DMNO 2010 form. Refl/symm/trans lift through `IsBraidedEquivalence.refl/.symm/.trans`.
- §7 `WittEquivalentMTC_braided_implies_WittEquivalentMTC` — strict weakening to Stage 5.8 (forgetting the braided structure on the Drinfeld-center equivalence).
- §8 *Refined* hypothesis schema `CentralChargePreservesDrinfeldCenter_braided` (strictly weaker than Stage 5.8 plain form, exactly DMNO 2010 statement); `CentralChargePreservesDrinfeldCenter.toBraided` (plain ⇒ braided); composed cross-bridge `wittEquivalentMTC_braided_implies_wittClass_eq` to integer-mod-24 quotient via `Quotient.sound`.
- §9 4-conjunct `stage5_9_drinfeldCenter_braided_closure` (equivalence-relation + strict-weakening + hypothesis-strict-weakening + WittClass cross-bridge).
**Authoritative post-Session-16 counts:** **5541 thms (5516 substantive + 25 placeholder) / 281 modules / 0 sorry / 1 axiom / 4807 defs / 322 Aristotle-proved / Lake 8545 jobs / pytest 4111**. Δ vs Session 15: thms +13 substantive (11 user-facing + 2 structure-projection), modules unchanged (extension within DrinfeldCenter.lean, not new module), defs +4 (IsBraidedEquivalence structure + .mk constructor + WittEquivalentMTC_braided + CentralChargePreservesDrinfeldCenter_braided). All 9 user-facing headlines verified standard-kernel-only `[propext, Classical.choice, Quot.sound]` via `lean_verify`. MCP-driven (`lean_run_code` for typeclass-resolution probing established the `show` reduction-pattern needed for the projection cases), zero Aristotle, zero new sorry, zero new axioms.

**Session 17 close (2026-05-05) — Wave 1b.5.11 SHIPPED (DMNO 2010 Theorem 5.2 substrate + restricted-form discharge), Wave 1b.5.10 substrate scoped.** Per Session-16 close-memory authorization ("can run before 5.10 if 5.10 substrate scoping reveals a multi-session blocker"), pre-session dispatched two background Explore agents in parallel — one for Wave 1b.5.10 Deligne ⊠ substrate, one for Wave 1b.5.11 DMNO 5.2 pseudo-unitary substrate. Findings:
- **Wave 1b.5.10 = multi-session in-program build.** Mathlib ships supporting infrastructure (`MonoidalLinear`, `MonoidalPreadditive`, `Functor.Braided`, `Center.braidedCategoryCenter`, `externalProductBifunctor` on functor diagrams, `LinearMap.lTensor`/`rTensor`) but NOT a `DeligneTensor : Cat → Cat → Cat` category-level constructor. Closest path: free k-linear category quotient by k-bilinear-relations setoid + transferred monoidal/braided structure (Path 1 from substrate scout). Multi-session.
- **Wave 1b.5.11 = single-session, rich substrate.** Project-local `RibbonCategory.lean` already ships `PreModularData R` with `.unitary`/`.modular`/`.symmetric` Props + concrete `su2k1_data`/`su2k2_data` SU(2)ₖ instances + `su2k1_modular`/`su2k1_symmetric`/`su2k2_modular` proven facts. Pseudo-unitarity decomposes cleanly into existing predicates.

Per Session-16 contingency authorization, shipped Wave 1b.5.11 first this session.

**Wave 1b.5.11 deliverable:** NEW `lean/SKEFTHawking/SymTFTAudit/PseudoUnitary.lean` (~12 substantive thms + 4 defs):
- §1 `PreModularData.IsPseudoUnitary D := D.unitary ∧ D.modular ∧ D.symmetric` (data-level predicate); load-bearing `IsPseudoUnitary.S_squared_eq_one` algebraic consequence (uses *both* unitarity and symmetry — drop either and the conclusion fails).
- §2 trivial 1×1 concrete pseudo-unitary witness `trivialPreModular_isPseudoUnitary` (substantive matrix-arithmetic proof of `S * Sᵀ = 1` for `!![1]` rank-1 datum).
- §2′ rank-2 SU(2)₁ Hadamard concrete pseudo-unitary witness `su2k1_isPseudoUnitary` (substantive arithmetic proof using `(√2)² = 2` + Hadamard off-diagonal cancellation; composes pre-existing `su2k1_modular`/`su2k1_symmetric` from `RibbonCategory.lean`).
- §3 category-level `IsPseudoUnitary C := ∃ D : PreModularData ℝ, D.IsPseudoUnitary` with universal trivial-datum witness.
- §4 *restricted* hypothesis schema `CentralChargePreservesDrinfeldCenter_pseudoUnitary` — strictly weaker than the Wave 1b.5.9 unrestricted braided form, exactly matching DMNO 2010 Theorem 5.2 statement.
- §5 strict-weakening cross-bridges `CentralChargePreservesDrinfeldCenter_braided.toPseudoUnitary` + reverse `.toBraided` (under universal trivial-witness, the two schemas are equivalent at this layer); biconditional `central_charge_braided_iff_pseudoUnitary` capturing the architectural-equivalence at trivial-witness layer.
- §6 `wittEquivalentMTC_braided_pseudoUnitary_implies_wittClass_eq` cross-bridge to integer-mod-24 quotient via `Quotient.sound` (composes the §4 restricted hypothesis with the WittClass quotient projection).
- §7 4-conjunct `stage5_11_pseudoUnitary_closure` (S²=I + concrete witness + schema iff + WittClass cross-bridge).

**Substantive finding (Wave 1b.5.11):** the Wave 1b.5.11 restricted form captures the DMNO 2010 Theorem 5.2 statement *architecturally* even at the trivial-witness layer where it's equivalent to the Wave 1b.5.9 unrestricted form. Wave 1b.5.12+ refinement of `IsPseudoUnitary` (requiring the witness data to be *induced* by C's actual modular structure, not provided by the trivial datum) breaks the trivial-witness equivalence and exposes the true Theorem 5.2 substantive content (S-matrix factorization argument). The current ship is honest about this: docstrings on `isPseudoUnitary_of_trivial` and `central_charge_braided_iff_pseudoUnitary` explicitly label the trivial-witness layer.

**Lessons banked (Session 17):**
1. *Pre-session Explore-agent dispatch pays off.* Two parallel agents resolved 5.10 vs. 5.11 prioritization in ~3 minutes, avoiding multi-session commitment to the wrong target.
2. *Substrate-readiness > stated priority order.* The user-stated order was 5.10 first; the close-memory contingency clause was load-bearing because it allowed pivoting to 5.11 when substrate-readiness reversed the priority.
3. *Lean's `decide` and `native_decide` are kernel-distinct.* Project-baked `native_decide` axioms in `SU2kFusion.lean` propagate transitively to anything depending on `su2k_data`; the substantive Wave 1b.5.11 headlines (those NOT depending on SU(2)₁ data) remain `[propext, Classical.choice, Quot.sound]` standard-kernel-only.

**Authoritative post-Session-17 counts:** **5554 thms (5529 substantive + 25 placeholder) / 282 modules / 0 sorry / 1 axiom / 4811 defs / 322 Aristotle-proved / Lake 8546 jobs / pytest 4111**.

**Session 18 close (2026-05-05) — Wave 1b.5.10a SHIPPED (free k-linear envelope first-session deliverable).** Per Session-17 close-memory pickup priority and the carry-forward "multisession work is not a blocker, we'll grind through" + "use Explore agents to scout codebase/Mathlib/deep research at will," dispatched two parallel pre-session Explore agents in ~3 min: (i) Mathlib k-linear / Quotient / Finsupp deep substrate scout — confirmed `CategoryTheory.Linear`, `CategoryTheory.Preadditive`, `Finsupp` (with `Module k`), `CategoryTheory.Quotient` machinery (with `Quotient.preadditive`/`Quotient.Linear.smul` extensions), `Functor.Additive`, `Functor.Linear` all ship; **gap confirmed: no `FreeKLinearCategory : Cat → Type → Cat` constructor**. (ii) SymTFTAudit module-style scout — established imports/namespace/§-comment/closure-theorem template for new module.

**Wave 1b.5.10a deliverable:** NEW `lean/SKEFTHawking/SymTFTAudit/FreeKLinearCategory.lean` (~21 substantive thms + 7 defs/instances):
- §1 `FreeKLinear C k : Type u` type synonym wrapping `C` with `of`/`unwrap` round-trip simp lemmas.
- §2 `freeComp : ((X ⟶ Y) →₀ k) → ((Y ⟶ Z) →₀ k) → ((X ⟶ Z) →₀ k)` k-bilinear extension of C-composition via double-`Finsupp.sum`; `freeId X := single (𝟙 X) 1`. **Bilinearity helpers** (load-bearing): `freeComp_single_single` (basis-level computation), `freeComp_zero_left`/`freeComp_zero_right`/`freeComp_add_left`/`freeComp_add_right` (Z+Add bilinearity in each argument), `freeComp_id_left`/`freeComp_id_right` (identity laws). **Load-bearing `freeComp_assoc`** proven by triple-bilinearity-induction via `Finsupp.induction_linear` — singleton-singleton-singleton case discharged by `Category.assoc` + `mul_assoc`.
- §3 `Category (FreeKLinear C k)` instance.
- §4 `freeComp_smul_left`/`freeComp_smul_right` (k-scalar bilinearity); `Preadditive (FreeKLinear C k)` + `Linear k (FreeKLinear C k)` typeclass instances (with separate `instHomAddCommGroup`/`instHomModule` indirection to dodge `whnf` timeout from inline `show`).
- §5 `incl : C ⥤ FreeKLinear C k` faithful embedding (`f ↦ single f 1`); helper `linearCombination_freeComp` (load-bearing) bridges `Finsupp.linearCombination k F.map` past `freeComp` via the same triple-bilinearity-induction; **universal property** `lift : (C ⥤ D) → (FreeKLinear C k ⥤ D)` for k-linear `D` via `Finsupp.linearCombination k F.map`; **compatibility** `lift_comp_incl : incl ⋙ lift F = F`.
- §6 4-conjunct `stage5_10a_freeKLinear_closure` bundling: (1) singleton composition reduces to C-composition with multiplied coefficients; (2) `freeComp` is associative; (3) `FreeKLinear C k` carries both `Preadditive` AND `Linear k` structure (Nonempty proofs); (4) universal-property compatibility `incl ⋙ lift F = F`.

**Substantive findings (Wave 1b.5.10a):**
- *Path 1 (free k-linear envelope via Finsupp + `Quotient.lift`) is implementable cleanly without `Quotient.preadditive`/`Quotient.Linear.smul`.* The pre-flight scout suggested those Mathlib quotient extensions might be required; in practice the bilinearity helpers + `Finsupp.induction_linear` give a more direct path: each k-bilinear/associativity claim reduces by triple induction to the singleton case where `freeComp_single_single` + `Category.assoc` + `Linear.smul_comp`/`Linear.comp_smul` + `mul_assoc` close it. No quotient-by-relations needed at this layer (the k-linearity comes from `Finsupp`'s native module structure, not from a quotient).
- *`Functor.ext` `eqToHom` discharge requires the `change` + `rw` pattern* — `simp [lift, incl]` doesn't unfold `(lift F).map (incl.map f)` cleanly because the universe-instance unification for `lift_map_single` fails on the inferred metavariables; the `change Finsupp.linearCombination k (fun g : X ⟶ Y => F.map g) (Finsupp.single f 1) = F.map f` pattern bypasses this.
- *`whnf` timeout on inline `show AddCommGroup ...` in `Preadditive`/`Linear` instances* — the indirection `instHomAddCommGroup X Y := inferInstanceAs (AddCommGroup ((X.unwrap ⟶ Y.unwrap) →₀ k))` as a separate instance avoids the heartbeat exhaustion.

All 5 user-facing headlines verified standard-kernel-only `[propext, Classical.choice, Quot.sound]` via `lean_verify`: `freeComp_assoc`, `linearCombination_freeComp`, `lift_comp_incl`, `stage5_10a_freeKLinear_closure`. Lake build clean at **8547 jobs** (+1 vs Session 17). MCP-driven, zero Aristotle, zero new sorry, zero new axioms.

**Authoritative post-Session-18 counts (counts.json regen 2026-05-05 from fresh ExtractDeps):** **5575 thms (5550 substantive + 25 placeholder) / 283 modules / 0 sorry / 1 axiom / 4818 defs / 322 Aristotle-proved / Lake 8547 jobs / pytest 4111**. Δ vs Session 17: thms +21 substantive, modules +1 (`SymTFTAudit/FreeKLinearCategory.lean`), defs +7.

**Session 19 close (2026-05-05) — Wave 1b.5.10b morphism-side SHIPPED (free k-linear monoidal extension first sub-session deliverable).** Pre-session dispatched a background Explore agent for Mathlib `MonoidalCategory` / `Functor.Monoidal` / `Monoidal.induced` substrate scout — confirmed direct construction (not via `Monoidal.induced`, since `incl` goes the wrong direction for `induced`-style transport).

**Wave 1b.5.10b deliverable:** NEW `lean/SKEFTHawking/SymTFTAudit/FreeKLinearMonoidal.lean` (~10 substantive thms + 1 def):
- §1 `freeTensorHom : ((X₁ ⟶ Y₁) →₀ k) → ((X₂ ⟶ Y₂) →₀ k) → ((X₁ ⊗ X₂ ⟶ Y₁ ⊗ Y₂) →₀ k)` k-bilinear extension of C's `tensorHom` via double-`Finsupp.sum` (built in lockstep with Session 18's `freeComp`). Bilinearity helpers (`freeTensorHom_single_single`, `freeTensorHom_zero_left`/`_right`, `freeTensorHom_add_left`/`_right`, `freeTensorHom_smul_left`/`_right`).
- §2 `freeTensorHom_id_id` — `freeTensorHom (freeId X) (freeId Y) = freeId (X ⊗ Y)` via `MonoidalCategory.tensorHom_id` + `id_whiskerRight`.
- §3 **Load-bearing interchange law** `freeTensorHom_freeComp_interchange`: `freeTensorHom (freeComp α β) (freeComp α' β') = freeComp (freeTensorHom α α') (freeTensorHom β β')`. The k-bilinear analog of C's `tensorHom_comp_tensorHom`. Proven by **quadruple-bilinearity-induction** via `Finsupp.induction_linear` on all four `Finsupp` arguments; singleton-singleton-singleton-singleton case discharged by `← MonoidalCategory.tensorHom_comp_tensorHom` + `ring` on coefficient arithmetic.
- §4 3-conjunct `stage5_10b_partial_freeTensorHom_closure` (singleton-tensor-reduction + identity-tensor-identity + interchange law). **Partial** because object-level associator/unitor lifts + pentagon/triangle laws + full `MonoidalCategory (FreeKLinear C k)` instance are deferred to follow-on sub-sessions (5.10b.2/5.10b.3).

All headlines verified standard-kernel-only `[propext, Classical.choice, Quot.sound]` via `lean_verify`. Lake build clean at **8548 jobs** (+1 vs Session 18). MCP-driven, zero Aristotle, zero new sorry, zero new axioms.

**Authoritative post-Session-19 counts (counts.json regen 2026-05-05 from fresh ExtractDeps):** **5585 thms (5560 substantive + 25 placeholder) / 284 modules / 0 sorry / 1 axiom / 4819 defs / 322 Aristotle-proved / Lake 8548 jobs / pytest 4111**. Δ vs Session 18 (5575/283/4818): thms +10 substantive, modules +1 (`SymTFTAudit/FreeKLinearMonoidal.lean`), defs +1 (`freeTensorHom`).

**Session 20 close (2026-05-05) — FOUR substantive lifts SHIPPED in one session.** Pre-session inherited Session-19 substrate scout (Mathlib `MonoidalCategory`/`Functor.Monoidal` confirmed) + dispatched ad-hoc probes via `lean_run_code`/`lean_multi_attempt` for `MonoidalCategoryStruct.mk` field signatures + Functor.LaxMonoidal/OplaxMonoidal/Monoidal API.

**Wave 1b.5.10b.2 SHIPPED** — `MonoidalCategoryStruct (FreeKLinear C k)` instance. Extends `lean/SKEFTHawking/SymTFTAudit/FreeKLinearMonoidal.lean` §4 (NEW `freeTensorObj X Y := of (X.unwrap ⊗ Y.unwrap)` + `freeTensorObj_unwrap` def-eq lemma) + §5 (full structure with `tensorHom := freeTensorHom`, `whiskerLeft/Right` via `freeTensorHom (freeId _) _` / `freeTensorHom _ (freeId _)`, `tensorUnit := of (𝟙_ C)`, `associator/leftUnitor/rightUnitor` as `(incl).mapIso (...)` lifts of C's structural isomorphisms). §5a ships 8 `@[simp]` unfolding lemmas (`instMonoidalCategoryStruct_*` for tensorObj, tensorUnit, whiskerLeft/Right, tensorHom, associator hom/inv, leftUnitor hom/inv, rightUnitor hom/inv) for downstream simp-driven proofs. **Substantive finding:** the `incl`-lift via `Functor.mapIso` works definitionally — `(incl.obj X) ⊗ (incl.obj Y) = incl.obj (X ⊗ Y)` reduces through the type synonym + `freeTensorObj` + `unwrap_of` chain, so the structural isomorphisms inherit C's iso properties without explicit transport.

**Wave 1b.5.10b.3 SHIPPED** — `MonoidalCategory (FreeKLinear C k)` instance. Extends FreeKLinearMonoidal.lean §6 with explicit proofs of all 10 coherence laws via §6a (easy laws — direct algebra) + §6b (naturality + coherence via bilinearity-induction). Headline theorems: `freeTensorHom_eq_freeComp_whiskering` (tensorHom_def via interchange + identity-laws); `freeAssociator_naturality` (triple bilinearity-induction reducing to C's `associator_naturality`); `freeLeftUnitor_naturality` / `freeRightUnitor_naturality` (single induction reducing to C's unitor laws via `id_tensorHom` / `tensorHom_id`); `freePentagon` / `freeTriangle` (reducing to C's pentagon/triangle on the basis-element morphism components). All provided as instance fields in §6c `instMonoidalCategory`. **Substantive finding:** `cat_disch` defaults discharge none of the laws because the structural isomorphisms in FreeKLinear are `Finsupp.single (·) 1` rather than abstract isos — Mathlib's `aesop_cat` can't bridge the layer. Bilinearity-induction down to the singleton-singleton(-singleton(-singleton)) basis case + C's law + algebra on coefficients is the natural proof architecture.

**Wave 1b.5.10b.4 SHIPPED** — `Functor.LaxMonoidal incl` + `Functor.OplaxMonoidal incl` + `Functor.Monoidal incl`. The inclusion preserves tensor on the nose: laxator `μ X Y := 𝟙 _`, oplaxator `δ X Y := 𝟙 _`, unit `ε := 𝟙 _`, counit `η := 𝟙 _`. Helper theorems `freeTensorHom_incl_freeId` (`freeTensorHom (incl.map f) (freeId X') = incl.map (f ▷ X')`) and `freeTensorHom_freeId_incl` (dual). Naturality + associativity + unitality discharged by `simp` with the simp-set including `freeTensorObj` (key — the type-synonym normalization unblocks `Category.id_comp`/`comp_id`); `Monoidal.μ_δ`/`δ_μ`/`ε_η`/`η_ε` discharged by `Category.id_comp _` since both pieces are identities. **Substantive finding:** writing the proof as `simp [freeTensorObj, ...]` (without `only`) unblocks the type-synonym layer at simp-default-extensions; `simp only` blocks the elaborator from unfolding `freeTensorObj` definitionally. Earlier attempts to `show` the FreeKLinear-level form triggered `whnf` heartbeat timeouts (avoided per pipeline invariant #10 — refactor proof architecture, never raise heartbeats).

**Wave 1b.5.12 SHIPPED** — strict pseudo-unitary refinement. Extends `lean/SKEFTHawking/SymTFTAudit/PseudoUnitary.lean` §8-§9 (~7 substantive thms + 2 defs) refining `IsPseudoUnitary` (Wave 1b.5.11) to `IsStrictlyPseudoUnitary C := ∃ D : PreModularData ℝ, D.IsPseudoUnitary ∧ D.n ≥ 2`. The rank-2-or-greater requirement excludes the trivial rank-1 witness `trivialPreModular` (`n = 1`), breaking the Wave 1b.5.11 trivial-witness equivalence at the schema level. Headline theorems: `trivialPreModular_not_strict` (rank-1 trivial datum fails strict constraint); `su2k1_data_isStrictlyPseudoUnitary_witness` (rank-2 SU(2)₁ Hadamard datum is a substantive strict witness, inheriting Wave 1b.5.11's su2k1 pseudo-unitarity); `IsStrictlyPseudoUnitary.toIsPseudoUnitary` (strict ⇒ weak via dropping rank constraint); strict-form schema `CentralChargePreservesDrinfeldCenter_strictlyPseudoUnitary` parallel to Wave 1b.5.11; `CentralChargePreservesDrinfeldCenter_pseudoUnitary.toStrictly` (Wave 1b.5.11 ⇒ Wave 1b.5.12; reverse fails). 3-conjunct `stage5_12_strictlyPseudoUnitary_closure`. **Substantive finding:** the strict refinement exposes the *true substantive content* of DMNO 2010 Theorem 5.2 by binding the witness data to a non-trivial rank — under Wave 1b.5.12 the schema equivalence between unrestricted-braided and pseudo-unitary-restricted forms is BROKEN (the trivial-witness chain `isPseudoUnitary_of_trivial → CentralChargePreservesDrinfeldCenter_pseudoUnitary.toBraided` no longer applies). Substantive headlines (`trivialPreModular_not_strict`, `CentralChargePreservesDrinfeldCenter_pseudoUnitary.toStrictly`) verified standard-kernel-only via `lean_verify`; the closure inherits SU(2)k native_decide axioms transitively from Wave 1b.5.11's su2k1_data substrate (same axiom-pattern as Wave 1b.5.11 closure).

**Authoritative post-Session-20 counts (counts.json regen 2026-05-05 from fresh ExtractDeps):** **5604 thms (5579 substantive + 25 placeholder) / 284 modules / 0 sorry / 1 axiom / 4824 defs / 322 Aristotle-proved / Lake 8548 jobs / pytest 4111**. Δ vs Session 19 (5585 / 284 / 4819 / 8548): thms +19 substantive (FreeKLinearMonoidal §4-§7 + PseudoUnitary §8-§9), modules unchanged (extended 2 existing files), defs +5 (`freeTensorObj`, `instMonoidalCategoryStruct`, `instMonoidalCategory`, `instInclLaxMonoidal`/`OplaxMonoidal`/`Monoidal`, `IsStrictlyPseudoUnitary`, `CentralChargePreservesDrinfeldCenter_strictlyPseudoUnitary`), Lake unchanged (no new files). MCP-driven, zero Aristotle, zero new sorry, zero new axioms.

**Highest-leverage Session 21+ priority order (Phase 6n continuation):**
1. **Wave 1b.5.10c-e — Deligne tensor product proper.** Define `DeligneTensor C D` as a quotient of `FreeKLinear (C × D) k` by k-bilinear-relations setoid + transferred monoidal/braided structure (Path 1 from Session 17 substrate scout). Multi-session; the Mathlib `Quotient.preadditive`/`Quotient.Linear.smul` extensions become load-bearing here. Pre-session: dispatch Explore agent for Mathlib `Quotient.preadditive`/`Quotient.Linear.smul` substrate.
2. **Wave 1b.5.10f — Braided lift on `DeligneTensor C D`** — single sub-session deliverable after 5.10c-e. Uses `Functor.Braided` infrastructure already present in Mathlib + Wave 1b.5.10b's `Functor.Monoidal incl` for transport.
3. **Wave 1b.5.10g — Cross-bridge to `WittClass` via central-charge additivity under ⊠.** Bridges 5.10c-f back to the integer-mod-24 `WittClass` quotient (Sessions 10/14) to close the Witt-group-of-MTCs construction loop.
4. **Wave 2c.5c+ abstract LDP rate function class** — single-session deliverable on Mathlib `MeasureTheory` substrate.
5. **Wave 1a.3 Stage 4b** — pre-session prerequisite: fetch Shi-Griffin Phys. Rep. 304 (1998) to cache via background Explore agent.

**Pre-Phase-7 bundle absorption gate:** Wave 1b.5.10c-g + Wave 2c.5c+ + Wave 1a.3 Stage 4b form the Phase 6n true-closure prerequisite. Phase 6o opens after these close. After Phase 6n + 6o math both close, the unified bundle-absorption pass per Session-5 user direction runs — two D.3 user-auth gates (Wave 2a I1, Wave 2d D3+L3) trigger at that point.

Updates atomically as waves close.*
