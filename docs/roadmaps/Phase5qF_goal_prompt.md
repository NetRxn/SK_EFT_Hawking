# Phase 5q.F — `/goal` prompt: discharge `SmithInflow`, make the 16-convergence UNCONDITIONAL

> Paste the block below into `/goal`. It is self-contained and survives compaction. It is written to
> leave **no exit route for incomplete work** — every stop-short rationalization we used overnight is
> named and closed. Read the STOP RULE every bootstrap/compaction.

---

## THE GOAL (one sentence)

Derive the ℤ₁₆ bordism invariants from **finite / elementary** content and **discharge the `SmithInflow`
tracked hypothesis**, so that `SKEFTHawking.CommonOrigin.sixteen_convergence_common_origin` (and
`…_via_constructed_smith`) hold **UNCONDITIONALLY** — i.e. `Ω₄^{Pin⁺} ≅ ℤ₁₆` and `Ω₅^{Spin-ℤ₄} ≅ ℤ₁₆`
proved, the geometric Smith iso built, the posited `signature`/`daiFreed` carriers retired, every
declaration kernel-pure, `validate.py` all-green, and a fresh-context adversarial review confirming the
discharge is **genuine** (not a thin-substrate rename).

The tracker with full per-wave detail is **`docs/roadmaps/Phase5qF_GeometricBordism_Roadmap.md`** — read
it first, then execute the spine W2 → W3 → W4 → W5 → W6 → W7 → W8 → W9.

---

## DONE — the ONLY definition of complete (all 8 must hold; verifiable)

1. **`Ω₄^{Pin⁺} ≅ ℤ₁₆` proved from BOTH finite bounds** — the Brown/ABK lower bound (`|·| ≥ 16`, W1 ✅ +
   W3) and the finite A(1)-`Ext` upper bound (`|·| ≤ 16`, W4). The `pinPlusRP4` generator value is
   **derived** (the posited `signature = 1` retired or proved), not asserted.
2. **`Ω₅^{Spin-ℤ₄} ≅ ℤ₁₆` proved** via the geometric Smith iso (the `daiFreed : ℤ` carrier derived).
3. **The geometric Smith homomorphism built** (replaces the thin constructed `smithHom` of 5q.E W6).
4. **`SmithInflow` DISCHARGED**: `sixteen_convergence_common_origin` (+ `…_via_constructed_smith`) proved
   with **no `SmithInflow` hypothesis binder**; the `smith_inflow_z16` HYPOTHESIS_REGISTRY entry moved
   from tracked-hypothesis → discharged-by-theorem; `docs/PERMANENT_TRACKED_HYPOTHESES.md` regenerated.
5. **Kernel-pure throughout** `{propext, Classical.choice, Quot.sound}`; **ZERO new project-local axioms**
   (any new axiom requires explicit user sign-off *in-session* — DR "ship as axiom" advice is advisory
   only); **zero `sorry` / `native_decide` / `maxHeartbeats` / `synthInstance.maxHeartbeats`** in proof
   bodies.
6. **No vacuous theorems** — the preemptive 5-question strengthening checklist applied before each
   theorem statement, plus a ruthless post-wave review (CLAUDE.md "Preemptive-strengthening discipline").
7. **`lake build SKEFTHawking.ExtractDeps` clean; `validate.py` ALL green;** counts.json + lean_deps.json
   + Inventory + status doc + roadmap landed-shards synced (committed per the git protocol below, when
   your tree is the clean one — never while the parallel agent has uncommitted source).
8. **Fresh-context adversarial review PASS**, dispatched specifically to check that the discharge is
   GENUINE: `SmithInflow` is actually removed, the two isos are actually proved from finite content, and
   nothing is a disclosure-downgrade-presented-as-proof-upgrade (the 5q.E W6 over-claim failure mode).

When and only when all 8 hold, the goal is met → dispatch the closure reviewer, update memory, stop.
Until then, **the next brick always exists; ship it.**

---

## THE STOP RULE — there are exactly TWO legitimate stops, and a list of CLOSED exit routes

**The ONLY two reasons to pause/ask are:**
1. **A PROVED no-go** — a *decomposition-backed mathematical impossibility* (the bar set by
   `[[nogo-lattice-arf-not-sigma8]]`: an actual kernel-checked refutation), NOT "this is hard." If you
   hit one, surface it ONCE with the proof + impact, then **keep working everything else.**
2. **A genuine product/strategy decision only the user can make** — a real tradeoff with no clear
   pre-decision. Run FULL diligence first (zoom out → re-read the roadmap/DR directly → Explore agents →
   reason about tradeoffs). If one option is clearly best, **TAKE IT.** Ask at most ONCE, and **keep
   working everything else meanwhile.**

**Everything below is a CLOSED exit route. Taking any of them IS the overnight failure mode. Do not.**

- ❌ *"Needs the Adams spectral sequence / APS index theorem / Dirac operator, which is absent from
  Mathlib."* → The route is the **finite A(1)-`Ext`** + the **Brown invariant** (both finite/elementary;
  see "The route is concrete" below). Build them. And in general: **"absent from Mathlib" is a BUILD
  instruction, not a wall.** We routinely build upstream Mathlib infra.
- ❌ *"This is a multi-week / multi-month / multi-day / large program, so I'll stop / hand off / leave it
  for a fresh session."* → **Duration and size are ELIMINATED from the continue-vs-stop decision.** Ship
  the next brick now.
- ❌ *"I'll document the gap / defer the substrate / leave it awaiting sign-off."* → Effort-based descope.
  Forbidden. Build it. A "documented residual whose only blocker is effort" is the antipattern.
- ❌ *"Ship it as a predicate-substrate AXIOM (the DR recommends it)."* → The DR's axiom recommendation is
  **advisory only**. No new axiom without explicit user sign-off. If — *after building everything around
  it* — a single sub-lemma is a genuine proved-impossible-in-current-Mathlib landmark, carry **that one
  sub-lemma** as ONE disclosed tracked `Prop` with a discharge plan, and surface it. Never as a shortcut,
  never to skip buildable work, never the whole wave.
- ❌ *"The conditional convergence is good enough / the ℤ₁₆-level chain is already built."* → **NO.** The
  goal is the **unconditional** discharge of `SmithInflow`. Conditional ≠ done. The ℤ₁₆-level chain being
  built is 5q.E; 5q.F exists precisely to make it unconditional.
- ❌ *"Holding / awaiting direction / this needs a fresh session / how much context is left."* → Forbidden.
  **It is not your job to manage the context window.** Auto-compact is handled safely. Ship the next brick.
- ❌ *(Over-claim, the opposite failure)* *"I built the substrate, so it's discharged / unconditional."* →
  Check before claiming: is `SmithInflow` **actually proved away** (hypothesis binder gone, iso proved
  from finite content), or did you rename/rescope a thin substrate? **Scope every claim to exactly what is
  kernel-proved.** Check the project's OWN records before claiming a reversal. A thin substrate is genuine
  Lean content but *tracks* geometry — do not call it "discharged."

**Self-recognition trip-wires.** If you catch yourself about to write any of these, STOP — that IS the
antipattern — and build the next brick instead: *"no foothold" · "multi-week landmark" · "fund that
separately" · "fresh session" · "awaiting your" · "holding" · "documented gap with deferred substrate" ·
"ship as axiom" · "good enough" · "conditional is fine" · "how much context".* Full rule:
`[[feedback-no-hypothesis-based-descope]]`, `[[feedback-goal-stop-hook-multisession]]`.

---

## The route is CONCRETE and FINITE — "no foothold" is pre-refuted

`Ω₄^{Pin⁺} ≅ ℤ₁₆` splits into two **finite, machine-checkable** bounds (source read directly:
`Lit-Search/Phase-5a/Formalizing the chirality wall- a Lean 4 feasibility assessment.md`, line 57):

- **LOWER `|Ω₄^{Pin⁺}| ≥ 16`** = the **Brown/ABK invariant** (W1 ✅ `BrownInvariant.lean`, kernel-pure):
  `β : Ω₄ → ℤ₁₆`, `β([RP⁴])` of order 16. Pure finite algebra (Gaussian-integer Gauss sum).
- **UPPER `|Ω₄^{Pin⁺}| ≤ 16`** = `Ω₄^{Pin⁺} = π₄(MPin⁻)` (Pontryagin–Thom) and the `Ext_{A(1)}` in total
  degree 4 — *"finite and algorithmic … machine-checked even without general spectral sequence
  infrastructure, by encoding A(1) as an 8-dimensional 𝔽₂-algebra and computing Ext via a free
  resolution"* (assessment, verbatim). A **finite 𝔽₂-linear-algebra** computation.

Neither needs Adams SS / APS / Dirac. The Smith side (Ω₅, W6–W7) is the geometric cut along the Poincaré
dual of the ℤ₄ class — elementary. **The whole spine is finite algebra + elementary 4-manifold topology.**

---

## Mandatory reads before working (and after every compaction)

- `docs/roadmaps/Phase5qF_GeometricBordism_Roadmap.md` — the wave spine + per-wave acceptance + the
  existing-substrate table (what to build ON, not duplicate).
- `Lit-Search/Phase-5a/Formalizing the chirality wall…md` and `Lit-Search/Phase-5c/Rokhlin/*` — the
  ℤ₁₆ routes, the Brown–van der Blij formula, the Guillou–Marin char-surface story. **Read directly; do
  not delegate depth-reading to subagents.**
- Memory: `[[project-phase5qF-geometric-level4]]`, `[[project-phase5qE-sixteenconv-2026-06-14]]`,
  `[[nogo-lattice-arf-not-sigma8]]`, `[[feedback-no-hypothesis-based-descope]]`.
- The substrate Lean files you re-base: `SKEFTHawking/BrownInvariant.lean` (W1, done),
  `SymTFT/PinPlusManifold4.lean`, `SymTFT/PinPlusBordism4.lean`, `SymTFT/SpinZ4Bordism5.lean`,
  `CommonOrigin.lean`, `RokhlinArfNoGo.lean`, `ArfInvariant.lean`.
- The MCP Lean dev loop (`lean_goal` → `lean_multi_attempt` → write → `lean_diagnostic_messages`) and the
  Wave Execution Pipeline. Use semantic search (not grep) for "does Mathlib decl X exist".

---

## The wave spine (acceptance criteria — see roadmap for detail)

- **W2** — general `brown : Z4Quadratic → ZMod 8` extraction (via the Gaussian-integer norm-`2^{2d}`
  structure ⇒ `gaussSum4·(1-i)^d = 2^d·i^k` ⇒ additivity, *without* needing the full form-classification)
  + `brown(stdForm g)=g` + Arf-mod-2 bridge; the ℤ₄-quadratic enhancement on a characteristic surface.
- **W3** — LOWER bound: assemble `β = (signature defect) + 2·brown(char surface) ∈ ℤ₁₆`; `β([RP⁴])` of
  exact order 16. β's bordism-invariance via the elementary handle-calculus / Guillou–Marin route.
- **W4** — UPPER bound: the finite `Ext_{A(1)}` degree-4 computation (A(1) as an 8-dim 𝔽₂-algebra, free
  resolution) ⇒ `|Ω₄^{Pin⁺}| ≤ 16`. Independent of W3.
- **W5** — combine W3+W4 ⇒ `Ω₄^{Pin⁺} ≅ ZMod 16`; re-base `PinPlusManifold4` on derived β (retire posited
  `signature`); re-derive `Omega4PinPlusBordism ≃+ ZMod 16`.
- **W6** — the geometric Smith homomorphism `Ω₅^{Spin-ℤ₄} → Ω₄^{Pin⁺}`.
- **W7** — `Ω₅^{Spin-ℤ₄} ≅ ℤ₁₆` via the Smith iso (derive the `daiFreed` carrier).
- **W8** — discharge `SmithInflow`: rewire `CommonOrigin`, remove the hypothesis binder, registry entry
  hypothesis→theorem, reconcile all disclosure surfaces.
- **W9** — closure: lib+ExtractDeps clean, validate.py, counts/Inventory/status/roadmap sync, adversarial
  review.

Each wave: kernel-pure, Stage-13 zero-blockers, commit a green source shard before moving on.

---

## Hard rules (non-negotiable)

- **Kernel purity** `{propext, Classical.choice, Quot.sound}`; **no new axiom without explicit user
  sign-off**; **no `sorry` / `native_decide` / `maxHeartbeats`** in proof bodies (decompose into `have`
  sub-lemmas instead — a heartbeat timeout is a wrong architecture, not a compute shortage).
- **Strengthening discipline** — apply the 5 preemptive questions before each theorem statement; no
  identity-wrappers, within-±2σ tautologies, definitional-unfolding-as-physics, or bundle-redundancy.
  Ruthless post-wave review remains mandatory.
- **Anti-over-claim** — name nothing `_unconditional` / "discharged" until it genuinely is (hypothesis
  removed + proved from finite content). Reconcile EVERY disclosure surface (Lean headers, registry,
  `PERMANENT_TRACKED_HYPOTHESES.md`, roadmap, status doc) when a status changes.
- **Git protocol (parallel agent shares `main`)** — before EVERY commit run `git diff --cached
  --name-only` and `git restore --staged` anything not yours; commit with **`git commit -- <explicit
  paths>`** (a bare `git commit` commits the whole shared index); never `add -A`/`-a`/`.`; never push;
  never touch/commit their files; never `rm -rf .lake/build` (use `lake build SKEFTHawking.<Module>` and
  `lake build SKEFTHawking.ExtractDeps`). Do NOT commit whole-project autogen (counts.json, lean_deps.json,
  Inventory) while the parallel agent has uncommitted source — self-heals at a clean-tree closure.
- **Build commands** — `lake build SKEFTHawking.<Module>` for a module; `lake build
  SKEFTHawking.ExtractDeps` for the graph artifact; `rm -rf .lake/build && lake build
  SKEFTHawking.ExtractDeps` ONLY for a trusted baseline (never bare `rm -rf .lake/build` alone, and never
  while the other agent is mid-build).

---

## Closure (only after all 8 DONE criteria hold)

Dispatch a fresh-context adversarial reviewer (`feature-dev:code-reviewer` or equivalent) with the
explicit charge: *"Verify `SmithInflow` is genuinely discharged — the hypothesis binder is gone, both
ℤ₁₆ isos are proved from finite/elementary content (Brown lower + A(1)-Ext upper + geometric Smith), and
nothing is a thin-substrate rename or disclosure-downgrade-as-proof-upgrade."* Remediate findings.
Then sync counts/Inventory/status/roadmap, update memory (`[[project-phase5qF-geometric-level4]]`), and
stop. **The loop ends only when the goal is genuinely met — or the user clears/redirects it.**
