Phase 5q.F /goal — discharge SmithInflow, make the 16-convergence UNCONDITIONAL.

GOAL: derive the ℤ₁₆ bordism invariants from finite/elementary content and DISCHARGE the `SmithInflow` tracked hypothesis, so `CommonOrigin.sixteen_convergence_common_origin` (+ `…_via_constructed_smith`) hold UNCONDITIONALLY. Spine + per-wave acceptance + reads: `docs/roadmaps/Phase5qF_GeometricBordism_Roadmap.md` (read first). Execute W2→W9.

DONE — all 8 must hold: (1) `Ω₄^{Pin⁺}≅ℤ₁₆` from BOTH finite bounds: Brown/ABK lower |·|≥16 (W1 done+W3) + finite A(1)-Ext upper |·|≤16 (W4); RP⁴ generator DERIVED not posited. (2) `Ω₅^{Spin-ℤ₄}≅ℤ₁₆` via the geometric Smith iso. (3) geometric Smith hom built. (4) `SmithInflow` DISCHARGED — convergence theorems carry NO `SmithInflow` binder; `smith_inflow_z16` registry hypothesis→theorem; `PERMANENT_TRACKED_HYPOTHESES.md` regenerated. (5) kernel-pure {propext,Classical.choice,Quot.sound}; ZERO new axioms; no sorry/native_decide/maxHeartbeats. (6) no vacuous theorems (5-question strengthening + ruthless review). (7) ExtractDeps clean, validate.py green, counts/Inventory/status/roadmap synced. (8) fresh adversarial review confirms the discharge is GENUINE (binder gone, isos from finite content), NOT a thin-substrate rename. All 8 → closure reviewer, update memory, stop.

ONLY TWO LEGITIMATE STOPS: (a) a PROVED no-go — kernel-checked impossibility (the [[nogo-lattice-arf-not-sigma8]] bar, NOT "it's hard"); (b) a genuine user-only product decision (full diligence first; if one option is clearly best TAKE IT; ask ≤1×; keep working everything else). EVERYTHING ELSE IS A CLOSED EXIT ROUTE — taking any = the overnight failure:
- "absent from Mathlib / no foothold" → BUILD instruction, not a wall. The route is FINITE: Brown (done) + finite A(1)-Ext (A(1) as 8-dim 𝔽₂-algebra + free resolution; Phase-5a l.57). NO Adams-SS/APS/Dirac.
- "multi-week / large / fresh session" → duration+size ELIMINATED from the decision. Ship the next brick.
- "document the gap / defer substrate / awaiting sign-off" → effort descope, forbidden.
- "ship as axiom (DR recommends)" → advisory ONLY; NO axiom without explicit user sign-off. A genuine proved-impossible sub-lemma → ONE disclosed tracked Prop + discharge plan, ONLY after building everything around it; never a shortcut, never the whole wave.
- "conditional is good enough / ℤ₁₆-chain already built" → NO; the goal is UNCONDITIONAL (that's 5q.E).
- "holding / awaiting / how much context left" → not your job to manage context; auto-compact is safe.
- OVER-CLAIM: "built the substrate so it's discharged" → scope claims to what's kernel-proved; is the binder ACTUALLY gone? Don't present a thin-substrate rename as a discharge (5q.E W6 error). Check the project's OWN records before claiming a reversal.

TRIP-WIRES — about to write any → STOP, build the next brick: "no foothold" · "multi-week landmark" · "fund separately" · "fresh session" · "holding" · "documented gap" · "ship as axiom" · "good enough". Rule: [[feedback-no-hypothesis-based-descope]].

HARD RULES: kernel-pure; no axiom w/o sign-off; no sorry/native_decide/maxHeartbeats (decompose into `have` lemmas). Strengthening 5-questions before each theorem. Reconcile EVERY disclosure surface on any status change. Read Lit-Search/Phase-5a+5c directly.

GIT (parallel agent shares `main`): before EVERY commit `git diff --cached --name-only` + `git restore --staged` anything not yours; commit `git commit -m … -- <paths>`; never add -A/-a/.; never push; never touch their files; never `rm -rf .lake/build` (use `lake build SKEFTHawking.<Module>`/`.ExtractDeps`); don't commit whole-project autogen while they have uncommitted source.

The loop ends ONLY when all 8 DONE criteria hold (→ closure reviewer) or the user redirects.
