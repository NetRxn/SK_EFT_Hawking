GOAL: Phase 6a Center-Bridge Discharge — unattended overnight, STRICT SEQUENCE Wave 8 (consistency conditions) → Wave 7B (genuine literal-Verlinde Laplace) → Wave 9 (Frolov–Fursaev induced-gravity 1/4). PUBLIC repo. Read in order FIRST: CLAUDE.md, docs/WAVE_EXECUTION_PIPELINE.md, docs/roadmaps/Phase6a_Roadmap.md "RE-OPENED 2026-06-13" §, then the three authoritative per-wave specs Phase6a_Wave8/Wave7B/Wave9_goal_prompt.md — follow each exactly.

INVARIANTS (all waves): axioms exactly {propext,Classical.choice,Quot.sound}; NO new axiom; NO native_decide; NO sorry; NO maxHeartbeats in proof bodies (decompose to `have`; maxRecDepth OK); never weaken a statement; every new Prop ships witness AND falsifier (no True-dressed-vacuous fields); apply strengthening checklist #4 (trivial-discharge)+#5 (defining-the-conclusion) to every statement. Dev loop = lean-lsp MCP (lean_goal→lean_multi_attempt→write; lean_verify each module); lean4 semantic search not grep.

PARALLEL AGENT (5QE on `main` tonight): stage ONLY your own explicit paths — NEVER git add -A/-a/.; commit own completed-wave files at each closure gate (if index locked, wait+retry); NEVER push; NEVER touch their files; NEVER rm -rf .lake/build (use lake build SKEFTHawking.<Module> + .ExtractDeps). SCOPE: only BHEntropyMicroscopic + new LaplaceMethodAsymptotic/induced-gravity modules + their consumers + paper26 + Phase6a roadmap/registry/counts/Inventory. Don't touch Phase 5qE.

CONTROL FLOW (strict, no reorder): (1) Wave 8 to its CLOSURE GATE — validate.py green · lake build of changed modules + ExtractDeps clean · lean_verify axiom-pure each new theorem · consumers compile · Fib+Ising proven · two Trues + F4 tautology discharged · counts/Inventory/roadmap synced · commit own paths. (2) ONLY after 8 green → Wave 7B (do its decision gate first: Hardy–Ramanujan vs direct saddle; recheck Mathlib) to its gate (H_VerlindeKMLiteralSumDerivation discharged; verlindeEntropy_SU2k = literal sum, not its own saddle) · commit. (3) ONLY after 7B green → Wave 9 to its gate (frolov_fursaev_quarter_coefficient + consistency + witness + falsifier; Gate A.2 CLOSED) · commit. Never skip ahead if closure isn't clean — keep working THAT wave.

NEVER BLOCK (user unreachable): stop hook = GO signal; each firing ship the next kernel-pure increment THIS turn across auto-compacts; never "hold"/"await"/"next session"/budget reasoning. Walls→fallbacks, never stall: (a) would-need-new-axiom → ship tracked-Prop instead (witness+falsifier+register in PERMANENT_TRACKED_HYPOTHESES.md), flag MORNING-REVIEW, CONTINUE — but CIRCLE BACK after all other work and work toward genuine discharge; (b) genuine product/narrative tradeoff with no clear default → take best-supported option, document alternatives as MORNING-REVIEW flag, CONTINUE; (c) a sorry surviving the exhausted MCP loop → isolate behind a tracked-Prop (never axiom/sorry), flag, proceed. One stuck lemma never halts the chain.

COMPLETE ONLY WHEN: Wave 8 (Trues+F4 discharged, consumers compile, Fib+Ising proven) ∧ Wave 7B (H_VerlindeKMLiteralSumDerivation discharged via genuine Laplace/documented direct saddle; verlindeEntropy_SU2k redefined) ∧ Wave 9 (frolov_fursaev_quarter_coefficient + consistency + witness + falsifier; Gate A.2 closed) ∧ full validate.py green ∧ per-wave Stage-13 adversarial review remediated ∧ counts/Inventory/Inventory_Index/roadmap synced. THEN: discharge any tracked-Props/Hypotheses ADDED this session (don't leave them open), run a FINAL adversarial review, write a concise MORNING SUMMARY to Phase6a_Roadmap (per-wave landings, all flags, fallbacks, defaults) + a memory note, dispatch the closure reviewer. Only then is the goal met.

MORNING HANDOFF (write also if the chain truly can't proceed): status block — current wave + gate state, what's green, what's flagged, exact next action — resumable in one read.