# PR review — `infra/adr-009-validation-modularization`, 2026-08-05

**Read this before touching the branch.** It is the handoff record for a six-reviewer PR review
run at the operator's request before any merge to `main`. The companion
[`README.md`](README.md) is the finding tracker; this file is the review's own record and the
resume point.

> **VERDICT: DO NOT MERGE.** Five of six reviewers returned *No* or *With fixes*. The branch is
> also RED on its own suite by design (57/59), so it cannot merge green regardless.

---

## 0. State at handoff

| | |
|---|---|
| Branch | `infra/adr-009-validation-modularization`, **~80 ahead of `main`, 0 behind, NOT merged** |
| Fast suite | **5412 passed / 5 skipped / 0 failed** (~193 s) |
| `validate.py` | **57 of 59**; reds are `bundle_metadata_matches_graph` (14 of 21 bundles claim `stage13_status: green` with blockers open) and `readiness_submission_gate` (61 of 64 papers RED). Both by design, both owned by the publication workstream. |
| Characterization | `CHARACTERIZATION HELD — 49 checks identical` |
| Registered checks | 59 on `main`, 59 on branch — **none dropped** (AST-verified both sides) |
| Findings | 31 closed (QI-01…QI-30 + QI-26b); **QI-31…QI-34 OPEN and blocking** |

⚠️ **RETRACTED CLAIM.** *"All 59 checks are mutation-verified in both directions"* stood in this
tracker, `RESUME_STATE.md` and ADR-009. It is false and has been struck. What
`tests/test_d5_mutation_obligation.py` actually certifies is *"a decision was recorded for every
check, and the named test references it in code"* — which is what its own docstring says. Do not
restore the stronger reading.

---

## 1. The four open blockers (QI-31…QI-34)

All four are checks that **cannot fail in production**. Detail in [`README.md`](README.md) §W-F.

| id | check | why it cannot fail | measured |
|---|---|---|---|
| QI-31 | `paper_table` | opens `paper_draft.tex`, uses it **only for `.exists()`**; the "paper" side is a hardcoded dict | 11 of 12 cells byte-identical to `numerical`'s |
| QI-32 | `paper_provenance` | the `\texttt{}` regex cannot cross a backslash; LaTeX escapes `_` as `\_` | 480 raw matches, **0** with `_`, vs **1,963** blocks containing `\_` |
| QI-33 | `bibitem_title_primary_source` | `not STRICT_MODE or (…)` ≡ `True`; no caller passes `--strict` | live: PASS while suppressing **9 DROP-WORD flags** |
| QI-34 | `recurrence_reopens_closures` | threshold above the live maximum | 7,489 pairs, max Jaccard **0.3750**, threshold **0.40** |

**None is a branch regression** — all inherited. `paper_table` never read the draft (sampled to
2026-05-01); the broken regex dates to **2026-03-26**. That makes it worse: this audit existed to
find guards that cannot fire and did not find these.

⚠️ **`paper_provenance` is the leg QI-01 "fixed."** The `glob`→`rglob` repair was applied to a leg
with an empty population, and the "verdict movement: zero" I recorded as evidence the exposure was
latent was in fact evidence the leg had been dead for months.

**Root cause, and the process fix:** QI-30 recorded the criterion — *a mutation caught against a
patched fixture does not establish that the check can fail in production* — and it was filed as
residue instead of applied as a sweep. All four would have been caught. **Seed the defect in the
PRODUCTION artifact, not in a monkeypatched fixture.**

---

## 2. Coverage: broad for form, narrow for correctness

Directly answering *"does the infrastructure work across the board, or are we testing paper 1?"*

**Not paper-1-specific.** Exactly **1 of 59** checks is hardcoded to one paper (`paper_table`, which
is QI-31 and reads nothing anyway). **12 checks sweep all 64 drafts / all 21 bundle codes.**

**But depth is scoped to the papers where a human found the bug:**

| property | coverage |
|---|---|
| numeric claims vs the canonical evaluator | **D1 and F only** — 19 of 21 bundles have none |
| figure legibility / source drift | **D11, D12 only** — **7 of 42** roster-bundle PNGs (16%); 10 bundles ship figures unchecked |
| notebook stored-output correctness | **`D1[12]_*` only** — 2 of 91 |

Each was written to close exactly the artifact where a reviewer found a defect, and left the rest in
the state that produced it.

---

## 3. What has NO enforcement at all

- **Figure *content* correctness.** `FigureSpec.physics_checks` has **no runner anywhere in the
  repo**; 118 of 137 specs have it empty. Nothing consumes `figure_review_report.json`.
- **Recomputing a paper-quoted number from its formula.** `threshold_arithmetic` — **0 files in
  `scripts/`**, never shipped. Documented precedent: a factor-50 threshold error that survived a
  full Stage-13 review.
- **Whether a cited theorem's STATEMENT supports the prose claim.**
  `theorem_quoted_bound_matches_lean_literal` — **0 files**. Precedent: prose claiming "formally
  bounded at ≤1.8%" where the 1.8% lives in a docstring, not the theorem.
- **Citation content correspondence.** `citation_bibkey_form_matches_metadata` and
  `bibitem_registry_character_match` — **0 files each**. `citation_primary_sources_present` verifies
  a file *exists*, not that it is the right paper.
- **LLM-artifact / placeholder strings in TeX** (`as an AI`, `[CITATION]`, `TODO`) — unscanned.
- **Invariant #10 (no `maxHeartbeats` in a proof body): 22 LIVE VIOLATIONS** across
  `Uqsl2AffineHopf.lean` (10), `Uqsl3Hopf.lean` (8), `QuantumGroupCoproduct.lean` (2),
  `QuantumGroupAntipode.lean` (2). `elaboration_knob_watchlist` **deliberately excludes**
  `maxHeartbeats` on a premise stated in its own docstring — *"forbidden outright by Invariant #10"*
  — that nothing implements.

**No CI exists.** (Operator has stated CI is deferred at this stage — recorded here so the
dependency is explicit: 56 of 59 checks, all 11 readiness gates and all 3 reviewer agents run only
when someone remembers. The pre-commit hook runs 3 of 59, fail-open, `main`-only, and is installed
from the *private* repo so a fresh clone has none.)

---

## 4. Fixed during this review (committed)

- **A published paper table shipped EMPTY.** `paper_tables.sources.validation_checks()` parsed
  `validate.py` alone; Phase 2 moved the checks out, so `papers/paper15_methodology/tables/
  table2_checks.tex` rendered with rules and **no rows** for 40+ commits while the paper `\input`s
  it. `tables_fresh` compares mtimes, never content. Restored to 59 rows + a guard tying row count
  to the live registry.
- **Two more instances of the same scope class** (I had declared QI-01 swept — I had swept `*.lean`,
  and the class is about SCOPE): roster gate Leg C globbed `scripts/*.py` non-recursively (the 12
  check modules were outside its own scan); `extract_python_test_nodes` globbed `tests/*.py`
  (30 test functions under `tests/e2e/` minted no node — 4804 → 4834). `TestGraphTestNodeCoverage`
  counted with the **same** broken glob, so its equality held vacuously.
- **A vacuous test**: `test_an_illegible_figure_fails` never called the function it patched, leaving
  `if pt < FLOOR_PT:` with zero coverage. Real fixture; mutation now CAUGHT.
- **The D5 seam guard was satisfiable by a DOCSTRING** — 3 of 59 entries were; one
  (`axiom_count_prose_consistency`) never invoked its check at all. Guard now strips docstrings and
  resolves the function from the live registry.
- **QI-30 rewrite**: `theorems` was vacuous in all three legs (two unreachable behind an import-time
  assert, one a tautology). Now resolves every Aristotle registry key against Lean; 14 of 322
  resolve to nothing, ratcheted.
- Doc corrections: ADR-009's *"Phases 0–2 behaviour-preserving"* qualified (`cdb81f7e` added a
  semantic tie-breaker in a commit titled "characterization harness"); item 6's *"no defect to fix
  here"* corrected; 6 wrong §Deferred ordinals; `RESUME_STATE`'s stale claims, one sitting **290
  lines below** the banner that had already corrected it.

---

## 5. Resume point — do these in order

1. **Fix QI-31…QI-34.** `paper_table` needs a real parser (copy `d1_hierarchy_table`'s approach) and
   its duplicate dict deleted. `paper_provenance` needs `r'\\texttt\{([a-zA-Z0-9_\\]+)\}'` + strip
   `\`, or reuse `_extract_prose_lean_candidates`. `recurrence` needs recalibration against the
   measured distribution (p99 0.200 / max 0.375) **plus** a test asserting the live max stays below
   the threshold. `bibitem_title` needs an operator decision (see 3 below).
2. **Sweep with QI-30's criterion** across all 59 — seed in the production artifact, not a fixture.
3. **Operator decisions needed** (do not guess): (a) several checks are `--strict`-gated with no
   caller — accept as human-run, or wire a submission-gate runner? (b) merging turns `main`'s
   wave-close gate red at 57/59 — remediate bundles first, or accept and document? (c) three
   unrelated commits ride this branch (`a41f8573` web-egress, `cc943091` 20 bundle reviews,
   `9ccba366` ADR-010 charter) — split before merge?
4. **Generalise the incident-scoped checks** — `bundle_figure_integrity` off its `d11_/d12_` filter
   to all of `FIGURE_REGISTRY`; `notebook_stored_outputs_current` to every bundle companion
   notebook. These are scope widenings of working code, not new checks.
5. **The unshipped correspondence checks** (§3) — highest value per the adequacy review, ~10 h for
   the two numeric/theorem ones by the project's own estimate.

## 6. Standing harness rules (learned the hard way; see README §4b)

- Scope every mutation to the target function's AST span; restore by writing back saved bytes,
  **never `git checkout`**, and **delete the cached `.pyc`** — a same-length mutation restored inside
  one second survives a source restore and later runs import mutated bytecode from a clean tree.
- Never take a "before" characterization in a `git worktree` — it lacks gitignored artifacts, so
  every Lean check takes its absent branch and the diff is noise. Revert only the changed source
  files in place.
- A MISSED mutation is a finding, never a formality: it means the guard is inert, the test is
  vacuous, or the line should not be counted as verified.
