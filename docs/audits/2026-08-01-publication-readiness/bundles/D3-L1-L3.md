# Emergent-gravity family (D3, L1, L3) — Publication-Readiness Audit
**Auditor:** emergent-gravity-family (D3 / L1 / L3)   **Date:** 2026-08-01
**Artifacts examined:**
- `papers/D3/paper_draft.tex`, `papers/L1/paper_draft.tex`, `papers/L3/paper_draft.tex`
- `papers/{D3,L1,L3}/{source_manifest.md,bundle_metadata.json,figures/}`
- `docs/PAPER_STRATEGY.md` §2.2/§2.3, `docs/BUNDLE_READINESS_HEATMAP.md`
- `src/core/formulas.py`, `src/core/constants.py` (`KERNEL_NOGO_REGISTRY`), `docs/dev-loops/SETTLED_FORKS.md`
- `lean/SKEFTHawking/{GravitationalWaves,BHThermodynamicsFourLaws,EmergentGravityBounds,FractonGravity}.lean`
- Primary sources: arXiv:2312.09435v2 (= JETP Lett. **119**, 330 (2024)) read in full by the auditor; arXiv:2111.07817v4 (= J. Low Temp. Phys. **207**, 127 (2022)) read by a scout (see *What I could not check*)

**Compile command (run out-of-tree so no repo file was modified):**
```bash
cd papers/<B> && latexmk -pdf -interaction=nonstopmode -outdir=<scratch>/<B> paper_draft.tex
pdfinfo <scratch>/<B>/paper_draft.pdf | grep Pages
```
D3 additionally required `-f` to produce a PDF at all (see D3-01).

---

## Verdict summary

| Bundle | Compiled pp | Target pp | Axis1 | Axis2 | Axis3 | Axis4 | Axis5 | Axis6 | Overall | Distance to submittable |
|---|---|---|---|---|---|---|---|---|---|---|
| **D3** | 59 (only with `latexmk -f`; clean run dies at l.712) | ~30–50 (PRD long) | D | F | D | D | C | C | **D** | `restructure` + `substantial-new-writing` |
| **L1** | 3 | 4 (PRL, ≤3750 word-equiv) | B | D | **F** | D | B | B | **F** | `new-research-required` |
| **L3** | 4 | 4 (PRL) | B | D | C | B | C | B | **C** | `revision` |

**Length is not the problem in this family.** D3 at 59 pp clears its target; L1 (3 pp) and L3 (4 pp) are both inside PRL's limit. Do not read the L-papers' brevity as a defect — per the rubric they are judged on completeness, and both are complete letters. The defects here are attribution, prose register, and (for D3) architecture.

**Headline:** L1 — the program's designated first submission — attributes to a named living author a quantitative prediction that is not in the cited paper, and then falsifies it. That is a P0 and it propagates into D3 §6. Nothing in this family should be submitted until it is resolved.

---

## Per-bundle findings

### L1

**This manuscript is not publishable in its current form, and the reason is not stylistic.**

**What this manuscript currently is:** A tight, well-constructed 3-page letter that takes a formula, `c_GW = c√χ_vest`, and a susceptibility window, `χ_vest ∈ [0.1, 10]`, and shows with Lean-checked arithmetic that the window is disjoint from the GW170817 bound by ~14 orders of magnitude. The internal logic is airtight and every number checks. The problem is entirely at the interface with the literature: the formula and the window are both **project constructs**, while the title, abstract and introduction present them as **Volovik's**.

**Strongest asset:** The falsification arithmetic and its Lean backing are genuinely clean and independently reproducible. `GravitationalWaves.lean` really does carry 21 theorems, zero `sorry` (the sole `sorry` string in the file is the word inside a docstring at line 501), and every theorem name cited in the manuscript resolves. The GW170817 bound is cited to the correct primary source with the correct numbers. If a real target for the falsification can be found, the machinery is ready.

**Verification I performed on the arithmetic (all pass):**
- `√0.1 − 1 = −0.68377` ✓ ; `√10 − 1 = +2.16228` ✓ → the quoted `[−0.68, +2.16]` is right.
- `2.16228 / 3×10⁻¹⁵ = 7.21×10¹⁴` ✓ ; `0.68377 / 3×10⁻¹⁵ = 2.28×10¹⁴` ✓ → the "7 × 10¹⁴" headline is the upper endpoint and is correct.
- Window measure `(1+τ)² − (1−τ)² = 4τ = 1.2×10⁻¹⁴` ✓.
- GW170817: `−3×10⁻¹⁵ ≤ Δc/c ≤ +7×10⁻¹⁶`, Abbott *et al.* (LIGO/Virgo, Fermi-GBM, INTEGRAL), ApJL **848**, L13 (2017), DOI 10.3847/2041-8213/aa920c — correct source, correct numbers, and taking `|Δc/c| ≤ 3×10⁻¹⁵` as the two-sided envelope is genuinely the conservative choice ✓.
- Bibliography: 10 cite keys, 10 `\bibitem`, zero orphans either way ✓. `Volovik2024Vestigial` is cited as **119, 330 (2024)**, which is correct (`docs/citation_verifications.jsonl` shows the page-564 error was a Stage-13 round-1 BLOCKER and was fixed). Note that `docs/PAPER_STRATEGY.md` §2.3 still carries the **stale 564** — the manuscript is right and the planning doc is wrong.

**Findings**

| ID | Sev | Axis | Location | Finding | Required end state | Size |
|---|---|---|---|---|---|---|
| L1-01 | **P0** | 3, 4 | `paper_draft.tex:78–79`, and abstract `:33–37` | **The central attribution is false.** Line 78: *"Volovik's 2024 vestigial-fluid review~\cite{Volovik2024Vestigial} proposes this directly"* — where *"this"* is the identification of the **spin-2 graviton** with the **second-sound mode** of a vestigial fluid, carrying `c_GW = c√χ_vest`. I read arXiv:2312.09435v2 ("Fermionic quartet and vestigial gravity", = JETP Lett. 119, 330) in full, all 6 pages. It contains **no collective-mode analysis at all**: no graviton mode, no second sound, no dispersion relation, no propagation speed of any kind, and no dimensionless metric-channel susceptibility. Its vestigial-gravity content is Eq. (19), `⟨Ê^a_μ⟩ = 0, g_μν = η_ab⟨Ê^a_μ Ê^b_ν⟩`, and the transition sequence Eq. (20) `disorder → GR → ECSK`. The paper's `χ` (Eq. 24) is a **boson-pair condensate amplitude** `χ = Σ_{k≠0}⟨â_k â_{−k}⟩` in a 4-boson toy Hamiltonian, minimised at `χ = −n₀V₁/V₂` (Eq. 27) — dimensionful, complex, not a susceptibility, never connected to any speed. The observable consequence Volovik actually draws from vestigial memory (Eqs. 21–22, §V) is **Equivalence-Principle violation**, not a modified `c_GW`. | Either (a) locate a primary source in which the falsified prediction is actually made, and re-anchor the paper to it; or (b) reframe the manuscript honestly as *"a GW170817 constraint on any vestigial-metric emergent-gravity scenario in which the graviton speed inherits a metric-channel susceptibility"* — a hypothetical the paper constructs and tests, with Volovik cited only for the vestigial-phase *scenario* (Eq. 19), never for the speed formula. The author's name must leave the title under either route. | `new-work` |
| L1-02 | **P0** | 3 | `:36–37`, `:86–91`, `:129–148` | **The falsified object is the project's own construct, and the paper's own corpus says so.** `Lit-Search/Phase-6a/6a-BCH four laws…Wave 5 literature survey.md:11` records *"Wave 2 **fixed** the GW propagation speed `c_GW = c·√χ_vest`"* — i.e. the formula was set by the project, not read off a paper. `Lit-Search/Phase-6a/6a-The Microscopic Coefficient α_ADW…md:161` is more damaging still: the vestigial mode *"is the J=0 Higgs amplitude mode of Volovik 2022, **not a graviton**"*. The manuscript itself concedes the window is *"a **project-adopted** naturalness window… a modeling choice of this project, not a published derivation"* (`:136–141`). Stacking these: the paper invents a formula, invents a range for its parameter, proves the range violates a bound, and titles the result as a falsification of a named author. The abstract's *"Several emergent-gravity programs identify…"* (`:33`) generalises this to an unnamed plurality with a single citation behind it. | The χ_vest range must either be **derived** (an RPA bubble computation with the stated `O(Λ²/16π²)` normalisation actually carried out, so the ±1-decade window is a result rather than a stipulation), or the paper must stop calling the outcome a falsification of anyone's published claim. A "±1 decade about unity" prior chosen by the authors cannot falsify a third party. | `new-work` |
| L1-03 | **P0** | 2 | `:243–246` | Review-history narration in the body, verbatim: *"(The original Wave-2 P3 = ∀c>0, c_GW(χ_vest, c) > 0 was removed in the **2026-04-25 post-Wave-2 audit** as redundant given P1; P3′ replaces it with a load-bearing quantitative constraint per the project's **preemptive-strengthening discipline**.)"* This is a message to an internal reviewer, dated, inside a PRL manuscript. | Delete the parenthetical entirely. P3′ needs no genealogy; state it and move on. | `trivial` |
| L1-04 | **P0** | 2 | `:55`, `:98`, `:248–250`, `:283–285`, `:220`, `:157` | Internal process vocabulary throughout the body: *"the **Phase 5y H1** underdetermination caveat"*, *"As established by the present project's **Phase 5y H1 deep-research finding**"*, *"the **Phase 5y H1** negative-evidence result"*, *"**Phase 6e** (full nonlinear emergent action) is the natural venue"*, *"Per the **project's discipline** for non-derived identifications"*, *"The GW170817 **correctness-push**"*. A referee has no idea what Phase 5y or a correctness-push is, and reading it tells them the manuscript was written for a different audience. | Rewrite each as a forward-facing statement of the physics: "Phase 5y H1" → "the identification has not been derived as a propagating degree of freedom"; "Phase 6e" → "a full nonlinear emergent action"; "correctness-push biconditional" → "the necessary-and-sufficient condition". | `small` |
| L1-05 | **P1** | 4 | `:369–373`, cited at `:146`, `:299` | `\bibitem{Roehm2026LinearizedEFE}` is an unpublished internal draft cited by **repository path**: *"SK–EFT-Hawking project, in preparation, 2026 (`papers/paper23_linearized_efe/`)"*. It is load-bearing twice — it anchors the χ_vest range (*"which we treat as the project-internal anchor for the order-of-magnitude prior"*) and carries the entire "Comparison to the linearized-EFE wave" paragraph. A reader cannot obtain it. | Either post the companion to arXiv first and cite the preprint ID, or drop the dependency and make L1 self-contained. A path inside a private working tree is not a citation. | `small` |
| L1-06 | **P1** | 1 | `:16` | `\input{../../docs/counts.tex}` reaches two directories outside the bundle. It breaks any single-directory submission (arXiv, PRL). It is also **dead**: L1 uses no macro from that file (checked — zero counts-macro expansions in the body). Reproduced verbatim by my first compile attempt: `! LaTeX Error: File '../../docs/counts.tex' not found.` → `! Emergency stop.` → no PDF. | Delete the line. | `trivial` |
| L1-07 | **P2** | 2 | `:378–389` | Seven lines of commented-out lift bookkeeping after `\end{thebibliography}`, including *"TODO: lift content from papers/\_phase6o\_W1b\_lean\_only/paper\_draft.tex"* and *"Section heading + label commented out 2026-05-06 Session 2: empty post-bibliography stub from bundle_append.py default-insertion"*. Invisible in the PDF, but arXiv publishes the `.tex` source. | Strip before deposit. | `trivial` |
| L1-08 | **P2** | 3 | `:315–323` | The Reproducibility paragraph promises two reproducible layers and names files, but gives **no repository URL and no archival DOI** (checked: zero `github`/`zenodo`/`doi:10.5281` strings in the file). *"at the project's pinned Mathlib4 commit"* names no commit. A formally-verified result the reader cannot locate is not a result. | Add a Zenodo DOI (or public repo URL) and the literal Mathlib commit hash. | `trivial` |
| L1-09 | **P2** | — | `bundle_metadata.json` | `"stage13_status": "green"` sits alongside `"blockers_open": 2`, `"open_findings": 5`, `"readiness": "RED"`. The heatmap agrees with the pessimistic reading (`L1 … 2 blockers … 🔴 RED`). The status field is the outlier. | Reconcile; a green Stage-13 with open blockers is not a meaningful state. | `trivial` |
| L1-10 | **P3** | 5 | title | Three titles in circulation: tex = *"GW170817 falsifies the vestigial-second-sound graviton identification **at the natural-range level**"*; `PAPER_STRATEGY.md` §2.3 = *"…**by 7 × 10¹⁴**"*; `bundle_metadata.json` = *"GW170817 / vestigial-graviton"*. | Pick one. (Under L1-01 the title changes anyway.) | `trivial` |

---

### L3

**This manuscript could plausibly reach PRL after a focused revision — it is the healthiest of the three — but its abstract currently reads like an internal ticket and its load-bearing theorem is weaker than the prose implies.**

**What this manuscript currently is:** A complete, correctly-proportioned 4-page letter that proposes a mass scale `M_c` separating two black-hole evaporation regimes and formalises the sign-flip of `dT_H/dt` across it. It is unusually candid — three separate paragraphs flag which of its ingredients are project-original with no published derivation. That candour is the paper's best feature and also, read by a referee, its problem: after the disclosures, what remains proved is thinner than the abstract's framing.

**Strongest asset:** The honesty discipline. `:62–63` (*"The critical-mass functional form M_c is project-original and explicitly disclosed as such: no published derivation exists"*), `:159–169` (the exponential/ODE alias disclosure), and `:288–290` (*"We cite [Jacobson–Koike] **only as the contrast case, not as the cooling anchor**"*) are exactly the kind of scoping a referee rewards. Do not remove any of them in remediation — the fix is to strengthen the content behind them, not to soften the disclosure.

**Findings**

| ID | Sev | Axis | Location | Finding | Required end state | Size |
|---|---|---|---|---|---|---|
| L3-01 | **P0** | 2 | `:50–63` (**the abstract**) | Internal process vocabulary in the abstract itself: *"ship the classification… in **tracked-hypothesis mode (Outcome-3 per the project's discipline for non-derived structural identifications)**"*; *"(**project-internal shorthand** for the bare induced-gravity limit…)"*; *"is **project-original** and explicitly disclosed as such"*. An editor reads the abstract first, and this one announces a workflow. | Keep the *substance* of the disclosures, move them into §I/§Discussion, and phrase them in literature-facing language: "Outcome-3 / tracked-hypothesis mode" → "as an explicitly hypothesised structure with named falsifiers"; "project-internal shorthand" → "an ansatz introduced here". The abstract should state the physics result and the one caveat that changes how it is read. | `small` |
| L3-02 | **P1** | 3 | `:159–178`, `:266–268` | **The load-bearing formal claim is an alias, and a trivial one.** The manuscript's own words: *"The Lean module's analytic anchor for the strict-decreasing claim is the **exponential alias** `T_H^acoustic(t) = T_H^(0)·exp(−t/τ_cool)` … note that `backreaction.py` evolves the full coupled-ODE system **rather than assuming the exponential form**"*. The theorem billed as *"the load-bearing"* one, `T_H_acoustic_evolution_strict_decreasing`, therefore proves that a decaying exponential decays. Balbinot's actual leading-order result is the **linear-in-t** Eq. (1) with a `t ~ 1/T³` asymptote; neither is what is formalised. A referee will ask what the formalisation bought. | Formalise the Balbinot form actually cited — monotone decrease of `T_H(t) = (ħc/2π)κ[1 − Ct]` on its domain of validity, plus the `t ~ 1/T³` asymptote — or drop the claim that the BEC-acoustic regime is formalised and present it as literature input to a classification theorem. | `medium` |
| L3-03 | **P1** | 3 | `:37`, `:111–115`, `:139` | `M_c = N_f Λ_UV/(12π α_ADW)` carries the entire novelty claim, and it is a **dimensional ansatz with no derivation** (self-flagged) **and no canonical Python home**. I checked `src/core/formulas.py`: the string `M_c` occurs **zero** times, and no function computes it. Pipeline Invariant #1 says `formulas.py` is the only place physics formulas live; the project's own headline PRL result is outside it. It exists only in `BHThermodynamicsFourLaws.lean:243` and in prose. | Either derive `M_c` (the Sakharov identity `1/G_N ∝ N_f Λ²` gives `M_c = 1/(G_N Λ_UV)` — that *argument* is a paragraph, not an ansatz, and would materially strengthen the paper), or state plainly that the partition scale is parametric and the physics claim is the *existence* of a sign flip, not its location. Either way add the canonical `formulas.py` entry. | `medium` |
| L3-04 | **P1** | 2 | `:202`, `:225`, `:256`, `:279`, `:296`, `:303`, `:328`, `:344` | Process vocabulary through the body: *"(**anti-$\exists$-absorption-pattern audit clean**)"* — an internal audit taxonomy, in a PRL; *"the **Wave-9 ansatz**"*; *"a **Phase-6 cross-bridge** open question"* (×3); *"(**Phase 6m Track C**)"*, *"The **Phase 6n** substrate partition"*, *"(**Phase 6m Track B**)"*. | Delete the audit parenthetical outright. Replace every Phase/Wave/Track label with what the work *is* ("a companion analysis of Sakharov-criterion satisfaction"), or with a citation. | `small` |
| L3-05 | **P1** | 5 | `:281–290` vs `PAPER_STRATEGY.md` §2.3 vs `D3:833–835` | **Three-way disagreement on the same equation.** The strategy lede asserts *"The Jacobson-Koike form `T_H(v) = T_H(0)(1 − v²/c_⊥²)` is the **structural anchor**."* L3 says the opposite: Jacobson–Koike is a *heating* profile *"structurally similar to Schwarzschild"*, cited *"only as the contrast case, **not as the cooling anchor**"*. D3 §8 then attributes that same formula to **Balbinot** and uses it as the ADW-extremality *cooling* profile. At most one of these three can be right; L3's reading is the defensible one. | L3 is correct and needs no change here — but D3-06 and the strategy §2.3 lede must be brought into line with it. | `small` (in the siblings) |
| L3-06 | **P1** | 4 | `:424–427`, cited at `:310` | `\bibitem{Roehm2026D3}` — an unpublished sibling, *"in preparation (D3 deep companion to L3, 2026)"*, carrying the load for *"the companion deep paper carries the full subsection including the substantive biconditional lift"*. Same class as L1-05: the reader cannot check the claim that is being deferred. | Post the companion, or state the biconditional here. | `small` |
| L3-07 | **P2** | 1 | `:13`, `:50`, `:332` | `\input{../../docs/counts.tex}` (same submission-breakage as L1-06) — but unlike L1, L3 *uses* the macro: `\bhThermoTotal{}` appears three times, twice in prose that a referee will read as a theorem count. `docs/counts.tex:39` sets it to **20**; a direct count of top-level `theorem`/`lemma` in `BHThermodynamicsFourLaws.lean` gives **21**. | Inline the number and reconcile it with the module. | `trivial` |
| L3-08 | **P2** | 4 | `:212–216` vs `D3:860–867` | L3 says *"**four** falsifier theorems"*; D3 §8 says *"The **five** falsifier theorems"* and lists a fifth, `four_laws_consistent_with_acoustic_regime`, which is a consistency lemma rather than a falsifier. Both cannot be the register. | Reconcile; state what each theorem falsifies. | `trivial` |
| L3-09 | **P2** | 2 | `:432–443` | Commented-out lift block after the bibliography, including *"Section heading commented out 2026-05-06 Session-2 to keep PRL splash compile clean (post-bibliography section was rendering as empty page-4 content)"*. Ships in the `.tex`. | Strip before deposit. | `trivial` |
| L3-10 | **P2** | — | `bundle_metadata.json` / title | `"stage13_status": "green"` with `"blockers_open": 6`, `"open_findings": 24`, `"readiness": "RED"` (heatmap: *"15 advisory, 2 critical, 4 major, 3 minor"*). Title drift: tex = *"Sign-flip of dT_H/dt as the regime partition…"*; strategy = *"Bardeen-Carter-Hawking Four Laws Partitioned by Regime in an Emergent-Gravity Substrate"*; metadata = *"BCH four laws by regime"*. | Reconcile both. | `trivial` |
| L3-11 | **P3** | 4 | `:88–93` | Eq. (1)'s numerical coefficient `563/(720π)` is attributed to Balbinot–Fagnocchi–Fabbri, PRD **71**, 064019. I did not verify it against the paper (see *What I could not check*). | Spot-check against the primary source. | `trivial` |

---

### D3

**This manuscript does not compile, calls itself by its internal project codename, and has zero figures and zero tables across 59 pages. It is not submittable, and the gap is structural rather than editorial.**

**What this manuscript currently is:** An ambitious consolidation with a genuinely good thesis and a body that does not deliver it. The introduction advances a real architectural claim — one Sakharov–Adler coefficient `G_N^Sak = 12π/(N_f Λ_UV²)` propagating without a free dial through six observables — and says so with force (`:125–127`: *"This single-coefficient propagation is the synthesis-level claim that no individual source paper makes end-to-end."*). That is a paper worth writing. What follows is thirty sections at a median of 262 words each, mapped one-to-one onto thirty source drafts, most of which restate a result and name its Lean theorem without arguing anything.

**Strongest asset:** The single-coefficient thesis, and the closure-register framing that treats NO-GOs as first-class results. Both are correct instincts and both survive the restructure. The tracked-hypothesis registry (`:2201–2292`) is also a real asset — an explicit, per-Prop list of what is *not* proved, with a closure path for each. Very few papers in this genre do that, and it should be kept (as an appendix table, not a bulleted subsection).

#### Answer to the dispatch question: architecture or sedimentation?

**Sedimentation, with a spine retrofitted in the introduction.** The evidence:

- **30 numbered sections** (+1 starred disclosure). Per-section word counts: median **262**, min 114, max 1264. **22 of 30 sections are ≤ 400 words.** Three sections (Introduction 1024, Synthesis 1264, Heat-kernel 889) carry 26 % of the prose; the other 27 average ~250 words.
- `source_manifest.md` maps sources to sections **one-to-one**: `paper5→§2`, `paper3→§3`, `paper23→§5`, `paper25→§6`, `paper26→§7`, `paper27→§8`, `paper35→§9`, `paper6→§10`, `paper20→§11`, `paper21→§12`, `paper22→§13`, `paper33→§13.5`, `paper36→§14`, `paper37→§15`, `paper38→§16`, `paper39→§17`, `paper40→§18`, `paper41→§19`, `paper42→§20`, `paper42b→§21`, `paper43→§22`, `paper44→§22.5`. That is the stitched lift in its literal form.
- The connective tissue that exists is real but thin and lives almost entirely in three places: the Introduction's "Roadmap" paragraph, the `\paragraph{Forward calibration}` at `:584–590`, and the Synthesis' "One coefficient, six observables". Between §11 and §16 the spine is a single shared integer (`N_f = 16`) and nothing else; between §23 and §28 there is no spine at all — the introduction itself calls those sections "appendices" (`:218–219`) while they ship as numbered in-sequence `\section`s.
- The tex header comment (`:9–11`) says *"22 main sections + 5 substrate appendices"* = 27. Thirty ship. The architecture drifted from its own plan and nobody re-derived it.

**Sketch of the architecture a ~50 pp PRD article on this material needs** (7 sections + one appendix; the numbers are rough page budgets):

| § | Content (merging current sections) | pp | New figures/tables it forces |
|---|---|---|---|
| 1 | Introduction — the single-coefficient thesis, expanded from the existing one | 4 | — |
| 2 | **The substrate and its calibration** — §2 (gap equation, tetrad gap, bifurcation, Vergeles) merged with §17 (heat-kernel `a₂` → `G_N^Sak`). The calibration currently sits 17 sections away from the substrate it calibrates. | 8 | **Fig. 1** phase diagram in `(G, N_f, Λ)`; **Table 1** substrate parameters with provenance |
| 3 | **Why ADW and not the alternatives** — §3 (gauge erasure) + §4 (closure triad), each closure stated at its *actual* evidential status | 6 | **Table 2** the closure register: channel │ obstruction type │ evidence class │ Lean witness │ status |
| 4 | **Linearised gravity and its observational envelope** — §5 + §19 (diff-invariance) + §18 (Stelle) + §20 (PPN) | 10 | **Fig. 2** predicted PPN ratios against the generic three-dial space; **Fig. 3** Stelle `(α,β,γ)` vs pulsar-timing / BBH ceilings |
| 5 | **Black-hole thermodynamics on the substrate** — §7 (entropy) + §8 (BCH regime partition); §9 QEC reduced to a cross-reference | 10 | **Fig. 4** `T_H(t)` regime partition (L3's figure already exists — reuse it); **Table 3** four laws × two regimes |
| 6 | **Where the substrate reproduces the open problem** — §21 (Λ) + §22 (torsion) | 6 | **Fig. 5** *the thesis figure*: all six observables as (prediction, bound) pairs on one log axis — five clearances and one 10¹⁰⁰ failure. This figure is the paper's argument and it does not exist. |
| 7 | Discussion, closure register, tracked-hypothesis table, outlook | 4 | **Table 4** tracked-hypothesis registry (currently a bulleted subsection) |
| App. A | Algebraic-Lorentzian-geometry package — current §23–§28, as a genuine `\appendix` | 6 | — |

**Moves out of D3:** §6 GW170817 → cite L1 and keep one summary paragraph (this also resolves the undeclared duplicate publication, D3-08); §11–§16 the six SM/QCD "rungs" (1,557 words) → **D2**, which owns Standard-Model particle content; §10 vestigial Monte Carlo → hold until `L ≥ 8` resolves, or report the null result honestly (D3-11); §9 QEC → **D4** with a cross-reference.

#### Answer to the dispatch question: are the NO-GOs presented at the status they actually have?

Mixed, and one is materially overstated. I checked `KERNEL_NOGO_REGISTRY` in `src/core/constants.py` (**45 entries**, enumerated) and `docs/dev-loops/SETTLED_FORKS.md` (grep for wen / fracton / vestigial / graviton / gw170817: **zero hits**). **None of D3's four headline no-gos is in either register.** That is not automatically wrong — those registers exist for *provably-false* forks with a kernel refutation theorem — but it does mean none of the four carries machine-enforced status, and the manuscript should not imply otherwise. Per-no-go:

- **Wen–ADW deficit ≈ 1/6000** — *honestly stated.* `EmergentGravityBounds.wen_adw_ratio_eq`, `wen_adw_ratio_bracket`, `wen_adw_factor_6000` all exist and prove `1/6202 < α²N_f/(32π³) < 1/6000` at `α = 1/5, N_f = 4`. D3 §4.1 names all three and discloses the evaluation point. Good.
- **Fracton-hydrodynamics no-go** — *overstated; see D3-07.*
- **Non-Abelian gauge erasure** (§3, 240 words) — the thinnest section in the paper carrying a claimed selection rule (*"only U(1) survives the fluid layer"*). It names a decisive test (`N=4` SYM) in 14 lines. Under-argued for its role in the closure triad.
- **Vestigial-graviton / GW170817** — *the theorems are real, the target is not.* See D3-03.

**Findings**

| ID | Sev | Axis | Location | Finding | Required end state | Size |
|---|---|---|---|---|---|---|
| D3-01 | **P0** | 1 | `paper_draft.tex:712` | **Does not compile.** A clean `latexmk` run halts. Verbatim from the log: `! Undefined control sequence.` / `<recently read> \Imm` / `l.712 ...induced-gravity route it is derived $\Imm`. `\Imm` is defined nowhere in the file (the preamble has no `\newcommand`s at all). Because pdflatex returns 1, latexmk stops iterating and leaves **212 unresolved references and 86 unresolved citations**. Only `latexmk -f` produces a PDF (59 pp, 3 residual undefined refs, 73 overfull hboxes). | Line 711 says *"a horizon boundary-condition (Immirzi) tuning"*, so `\Imm` is plainly meant to be `\gamma` (the Immirzi parameter). Fix, then confirm a clean non-`-f` run resolves all refs. | `trivial` |
| D3-02 | **P0** | 2 | `:2325–2337`, `:2355–2359`, `:2292` | **The manuscript refers to itself by its internal bundle codename and narrates the project's release process.** *"**D3** does not solve the cosmological-constant problem"*, *"**D3** does not derive individual Yukawa couplings"*, *"**D3** is the architectural unification of twenty-two formally verified threads"* — and then, as a rendered paragraph: *"**Submission gate.** D3 ships at this **Phase 7 close-out gate** under the **Phase 7 sub-wave 7c.0 architectural skeleton**. **Phase 8 is the submission roll-out**; D3's PRD-long-format submission is recommended in the Phase 7 close-out window per the project strategy."* Immediately above, the reader is directed to *"the provenance dashboard at **`localhost:8050`**"*. | Delete the "Submission gate" paragraph. Replace every self-reference "D3" with "this paper"/"we". Replace the localhost URL with an archival DOI. | `small` |
| D3-03 | **P0** | 3, 4 | `:140–153`, `:599–624` | **§6 reproduces L1-01 verbatim**, including the misattribution: *"**Volovik's identification** of the vestigial second-sound mode in the ADW substrate as the spin-2 graviton~\cite{Volovik2024Vestigial} carries a leading-order prediction `c_GW(χ_vest) = c√χ_vest`"*. As established under L1-01 by direct reading of arXiv:2312.09435v2, that paper contains no mode analysis, no speed, and no such susceptibility. This is the paper's *third closure* — one of the three results the abstract says *force* the ADW route. | Resolve jointly with L1-01. If the falsification target cannot be re-anchored, the "three closures force ADW" architecture loses a leg and the abstract's central claim must be restated as two closures plus an open channel. | `new-work` |
| D3-04 | **P0** | 2 | `:161`, `:164`, `:166`, `:518`, `:542`, `:547`, `:1328`, `:1369`, `:1557`, `:1566`, `:1588`, `:1607–1609`, `:1723`, `:2159–2163` | **"Decision Gate E.1 / E.2 / E.3 / E.4" is used as a rendered organising device**, including in two `\section` titles (`\section{Heat-kernel calibration: … (Decision Gate E.2)}`, `\section{Order-by-order diffeomorphism invariance through $a_4$ (**Decision Gate E.3**)}`) and as a verdict vocabulary (*"**Decision Gate E.3 closes.**"*). These are internal roadmap gate IDs and they appear in the table of contents. Alongside them: *"(closing **substrate-audit #13**)"* (`:720`), *"(**Wave~7C**)"*, *"(**Wave~8B**)"*, *"(**Wave~9**)"*, *"**DISCHARGED (Wave 7B, 2026-06-14)**"* (`:2277`), *"**Phase 6a Wave 4** FLRW backbone"* (`:569`), *"**Phase 6n.ζ**"* in a subsection title (`:1390`), *"the program's **Phase 6m Track B** verdict"* (`:1483`). | Strip every gate/wave/phase/audit label. Where a gate marks a genuine necessary-and-sufficient condition, state it as such ("we show `G_N^emerg` matches the Sakharov–Adler form *iff* `α_ADW = 1`"); the label adds nothing a reader can use. | `medium` |
| D3-05 | **P1** | 1 | whole document | **Zero figures and zero tables in 59 pages.** Confirmed: `grep -c 'begin{figure'` = 0, `grep -c 'begin{table'` = 0, `grep -c tabular` = 0; `papers/D3/figures/` contains only `figure_review_report.json`. `bundle_metadata.json` records this as *"Stage 9 GREEN (0 figures; advisory…)"*. For a PRD long article whose entire thesis is that one coefficient propagates across six observables, the absence of the figure showing that propagation is a structural defect. | Minimum viable set: the five figures and four tables in the architecture sketch above. **Fig. 5** (six observables, prediction vs bound, one log axis) is non-optional — it *is* the paper's argument. | `medium` |
| D3-06 | **P1** | 4 | `:833–835` | **Citation does not support its sentence, and contradicts the sibling bundle.** D3 writes: *"in the ADW-extremality branch the BEC-acoustic profile of **Balbinot**~\cite{Balbinot2005} `T_H(v) = T_H(0)(1 − v²/c_⊥²)` holds with the Jacobson–Koike contrast"*. That functional form is the **Jacobson–Koike** moving-domain-wall result, not Balbinot's — and L3 `:281–290` states explicitly that the Jacobson–Koike profile is a **heating** profile *"structurally similar to Schwarzschild"*, cited *"only as the contrast case, not as the cooling anchor"*. D3 therefore attributes the formula to the wrong author and deploys a heating profile as its cooling branch. | Adopt L3's (correct) treatment: Balbinot's linear-in-t Eq. (1) is the cooling anchor; Jacobson–Koike is the contrast. Fix `PAPER_STRATEGY.md` §2.3's lede at the same time (L3-05). | `small` |
| D3-07 | **P1** | 3 | `:460–474` | **A no-go presented as stronger than its kernel evidence.** §4.2 asserts *"the hydrodynamic stress tensor cannot transform covariantly under arbitrary diffeomorphisms; the obstruction shows up at order `a₄`… **This is a structural no-go, not a numerical one**"* — and names **no theorem for the fracton failure**, only `NonlinearDiffInvariance.diff_invariance_a4_iff_dirac_basis_consistent`, which is about the *Dirac* bundle. The Lean content that does exist is `FractonGravity.fracton_fails_diffeo` / `fracton_route_no_diffeo`, and it is `by decide` over two hand-assigned record literals: `fracton_gauge = ⟨2,1⟩` vs `diffeo_gauge 4 = ⟨1,4⟩`, with `achieves_diffeo_invariance r := decide (routeGaugeStructure r = diffeo_gauge 4)`. That formalises a *bookkeeping comparison of two stipulated gauge-structure records* — a faithful encoding of a literature classification, not a derivation of an obstruction at order `a₄`. Neither theorem appears in `KERNEL_NOGO_REGISTRY` (45 entries checked) or `SETTLED_FORKS.md` (zero hits). | Either derive the `a₄` obstruction (and then it is genuinely a structural no-go), or restate §4.2 as *"we encode the Pretko–Radzihovsky mobility-constrained gauge algebra and verify it is not the diffeomorphism algebra; the order-`a₄` localisation of the obstruction is argued, not proved"*, and name the two theorems so a reader can check the strength for themselves. | `medium` |
| D3-08 | **P1** | 4, 5 | bibliography, vs `:487`, `:662`, `:872`, `:889`, `:2296–2306` | **Sibling manuscripts are referenced by internal codename and are absent from the bibliography.** The body says *"(matching **Bundle~L1** §6 **character-for-character**)"*, *"**Bundle~L1** ships the same content as a four-page Physical Review Letters splash"*, *"appear identically in **Bundle~L3**"*, *"**Bundle~D4** §7 will close the QEC content downstream"*, *"**Bundle~D5** §7 share the heat-kernel `a₀` coefficient character-for-character"*, *"**Bundle~I1**"*, *"**Bundle~L2**"*. The bibliography (69 entries; cite/bibitem integrity otherwise **clean** — 69 keys, 69 bibitems, zero orphans in either direction) contains **no entry for L1, L3, D4, D5 or I1**. Two consequences: a reader cannot locate any of them, and — because §6 and §8 admittedly duplicate L1 and L3 *character-for-character* — the **duplicate-publication overlap is undeclared to the editor**, which APS policy requires. | Cite each sibling as a preprint (arXiv ID) or "submitted", and add an explicit companion-paper statement demarcating what D3 §6/§8 restate versus what L1/L3 own. Under D3-03 the L1 overlap should shrink to a summary paragraph anyway. | `small` |
| D3-09 | **P1** | 4 | `\bibitem{Roehm2026Strategy}` (`:2758`), cited at `:234` and `:2359` | An **internal planning document cited as literature** and used to justify a methodological posture: *"**Following~\cite{Roehm2026Strategy}** we treat NO-GO results as first-class publishable content."* The entry reads *"SK-EFT Hawking **14-bundle** publication architecture, **project paper-strategy frame**, in preparation (2026)"* — it is not a paper, is not obtainable, and its own bundle count is stale (the strategy now carries 20 targets). | Delete both citations. The NO-GO-as-content posture needs no citation; assert it in the authors' own voice, or cite the actual literature on publishing negative results. | `small` |
| D3-10 | **P1** | 5 | `:977–1326` (§11–§16), `:1826–2133` (§23–§28) | **Off-charter content, ~2,855 words.** §11–§16 (Higgs identification, type-I seesaw + Z₁₆ singlet, electroweak phase transition, EWBG chirality wall, confinement/Svetitsky–Yaffe, chiral SSB/GMOR, CFL Z₃) are Standard-Model and QCD physics in a paper titled *"Emergent gravity through black-hole thermodynamics"*. `PAPER_STRATEGY.md` §2.2 charters exactly one such section (*"Cross-bridges to QCD strong-coupling closure … **as a section**"*) and says nothing about Higgs, seesaw, EWPT or EWBG. §23–§28 are a Lean GR-infrastructure package that the introduction itself calls *"**Five appendices**"* (`:218`) but which ship as numbered in-sequence sections — and `:2304–2306` concedes *"**I1 is the primary, D3 the supplement**"*. | §11–§16 → **D2** (which owns SM particle content), leaving at most one QCD-voucher section. §23–§28 → a real `\appendix` (or to I1, per D3's own admission). | `large` |
| D3-11 | **P1** | 6 | `:949–957`, `:969–970`, `:2331–2333` | **A rendered subsection heading promises in-flight numerics**: `\subsection{$L \ge 8$ convergence: in flight}`, followed by *"**If** RHMC converges, the quantitative bifurcation prediction … is empirically confirmed"* and *"**Conditional on RHMC convergence**, this is the substrate's first quantitative empirical anchor for emergent gravity."* The "What this paper is not" section repeats *"convergence is **in flight at the time of writing**"*. A published article cannot carry a section whose result is pending. The project's own record of the `L = 8` run is a null result. | Either report the `L = 8` result as it stands (a null result is publishable and honest), or cut §10 to the `L = 4, 6` first-order transition with no extrapolation promised. Remove "in flight" from the manuscript entirely. | `medium` |
| D3-12 | **P1** | 3 | `:809–812`, `:2237–2247` | `M_c` is quoted as a headline result with **no entry in `formulas.py`** (zero occurrences of `M_c` in that file), violating Pipeline Invariant #1. Same finding as L3-03, in the bundle that is supposed to be the canonical reference for the material. | Add the canonical `formulas.py` entry; give the dimensional-analysis derivation rather than presenting `M_c` as an ansatz. | `small` |
| D3-13 | **P2** | 6 | `:2364–2461`, `source_manifest.md` | **The manifest overstates coverage by nine sources.** It lists 31 contributing papers; nine (`_phase6n_W1a`, `_phase6n_W2a`, `_phase6n_W2c`, `_phase6n_W2d`, `_phase6o_W1a`, `_phase6o_W1b`, `_phase6o_W1c`, `_phase6o_W2a`, `_phase6o_W3b`) are commented-out stubs at the end of the file carrying only *"TODO: lift content from …"*, *"TODO: ensure all numerical claims trace via formulas.py / counts.tex"*. `bundle_metadata.json` has `"freshness_stale": true`. On the positive side, the `%% BUNDLE_APPEND_INSERT_HERE` marker and the tracked-hypothesis registry are genuine absorbability assets — future waves *can* land as additions. | Either lift the nine or remove them from the manifest. Strip the TODO blocks before deposit (they ship in the arXiv source). | `small` |
| D3-14 | **P2** | 1 | `:2019–2056`, `:2088–2133`; `:70` | Residual `-f`-run breakage: 3 undefined refs (`sec:singularity-thms` ×2, `sec:cfl-z3-matching`) and 73 overfull hboxes. Separately, `\tableofcontents` (`:70`) generates ~5 pages of front matter; PRD long articles do not carry one, and it inflates the page count. | Fix the labels; drop the TOC. | `trivial` |
| D3-15 | **P2** | 3 | `:48–54` (abstract) | The abstract hedges its cosmological-constant result with a **proof-assistant implementation detail**: *"the formal Lean lower bound discharged at the precision of Mathlib's shipped fundamental constants; the classical Weinberg estimate of ~10¹²² is recovered **when Mathlib's precision is sufficient** to evaluate to that order, and the gap is a **Mathlib-precision artefact** rather than a substrate-prediction uncertainty"*. A physics referee does not care which library's constants have how many digits; they will read this as the result being uncertain. | State the physics: "the emergent Λ exceeds the observed value by more than 100 orders of magnitude". Move the numerical-precision note to a footnote or the Methods block. | `trivial` |
| D3-16 | **P2** | — | `bundle_metadata.json`, `:9–11` | `"stage13_status": "green"` with `"blockers_open": 7`, `"open_findings": 57`, `"readiness": "RED"`, `"freshness_stale": true`; the heatmap row reads *"57 │ 7 │ 43 advisory, 1 critical, 6 major, 7 minor │ 🔴 RED"*. Separately the tex header claims *"22 main sections + 5 substrate appendices"* (27) where 30 ship. | Reconcile both. | `trivial` |

---

## Cross-bundle observations

**1. One P0 spans two bundles.** The Volovik misattribution (L1-01 / D3-03) is the single most consequential finding in this family. It is not a citation-formatting error: the manuscript's title, abstract, introduction and headline theorem all point at a claim that is not in the cited paper, and the *falsified* object — the formula `c_GW = c√χ_vest` and the window `χ_vest ∈ [0.1, 10]` — is documented inside the project's own corpus as a project construct (`Lit-Search/Phase-6a/…Wave 5 literature survey.md:11`: *"Wave 2 **fixed** the GW propagation speed"*; `…α_ADW…md:161`: the vestigial mode *"is the J=0 Higgs amplitude mode of Volovik 2022, **not a graviton**"*). Because L1 ships first and alone, this would be the program's public debut. Recommend it does not ship until re-founded.

**2. The duplication is real, self-acknowledged, and undeclared to editors.** D3 §6 ≡ L1 (D3 `:487`: *"matching Bundle~L1 §6 **character-for-character**"*); D3 §8 ≡ L3 (D3 `:874`: *"The Lean theorem name and numerical anchors are **character-for-character identical** between L3 and §8"*). Quantitatively: D3 §6 = 407 words vs L1's ~1,965; D3 §8 = 388 words vs L3's ~2,264. So D3's versions are ~20 % compressions, not verbatim copies — the prose is rewritten. **Does D3's version add the architectural context that justifies the duplication?** For §8, marginally yes: `M_c` is placed inside the single-coefficient propagation chain (`:2149–2151`), which L3 cannot do. For §6, essentially no: §6 restates L1's result and adds only a `VestigialGravity` phase-count aside. Either way, neither sibling is in D3's bibliography and no companion-paper statement exists, which is the actual submission risk (D3-08).

**3. `PAPER_STRATEGY.md` §2.2/§2.3 is stale against all three artifacts.** It carries the wrong Volovik page (564; the manuscripts correctly say 330); it asserts *"The Jacobson-Koike form … is the structural anchor"* where L3 says the opposite; it asserts the third law *"admits Kehle-Unger-style violation in BPS-violating matter sectors"* where L3 calls that *"a Phase-6 cross-bridge open question"*; it charters one QCD section where D3 ships six SM/QCD sections; and all three titles differ from the shipped `\title`. Per the rubric the artifact wins in each case — but the divergence itself is a portfolio finding, because the strategy doc is what downstream planning reads.

**4. Metadata is uniformly optimistic in the same way.** All three carry `"stage13_status": "green"` alongside open blockers (D3: 7, L1: 2, L3: 6) and `"readiness": "RED"`. This is the same inconsistency class the rubric flags for D7. The `green` field appears to record "the last Stage-13 *round* closed", not "the bundle is clean", and it is being read as the latter.

**5. Formula integrity against `src/core/formulas.py` — three of four clean, one missing.**

| Formula | Manuscript | Canonical source | Verdict |
|---|---|---|---|
| `G_c = 8π²/(N_f Λ²)` | D3 `:97` | `formulas.py:2263` `tetrad_critical_coupling_integral` | ✓ match |
| `G_N^emerg = α_ADW · 12π/(N_f Λ²)` | D3 `:530–534`, L1 `:302` | `formulas.py:7899` `G_N_emergent` / `:7896` `G_N_sakharov` | ✓ match |
| `S = A/(4G_N) − (3/2)log(A/(4G_N))` | D3 `:697–701` | `formulas.py:8585` `bh_entropy_kaul_majumdar` | ✓ match |
| `M_c = N_f Λ_UV/(12π α_ADW)` | D3 `:811`, L3 `:139` | **absent** — zero occurrences of `M_c` in `formulas.py` | ✗ Invariant #1 violation |

No numerical drift between manuscript and canonical source was found. The `M_c` gap is a provenance hole, not a drift.

**6. Remediation sequencing.** L3 is the only member of this family that a focused revision can carry to submission (`revision`, days). L1 is blocked on a physics decision, not an edit (`new-research-required`). D3 is blocked on L1 (for §6) *and* needs a restructure plus five figures and four tables that do not exist (`restructure` + `substantial-new-writing`, weeks). If the program needs an arXiv-voucher first submission and L1 cannot be re-founded quickly, **L3 is the better first-submission candidate** — it is complete, in-scope, correctly proportioned, and its outstanding findings are tractable.

---

## What I could not check

- **The 2022 Volovik spin-counting (L1 `:79–84`) I did not read myself.** I read arXiv:2312.09435v2 (the 2024 paper, on which L1-01 and D3-03 rest) in full, page by page — that finding is lead-verified. The claim that arXiv:2111.07817v4 §VIII assigns the two surviving `J_z = ±2` helicities to the **J=2 Higgs/amplitude ("real squashing") modes** rather than to leftover Goldstones — and that Volovik explicitly distinguishes his construction from graviton-as-Goldstone theories — comes from a scout's page-by-page read with verbatim quotes, not from my own. If it holds, L1 `:79–84` misdescribes the sector as well as the mode; treat it as strong corroboration, not as independently established.
- **Journal-of-record versions.** Both Volovik verifications are against the arXiv primaries (v2 dated 2023-12-27; v4 dated 2021-12-15), not the Springer typeset versions — `link.springer.com` was off the fetch whitelist. Residual risk that copy-editing added material is low but nonzero.
- **Balbinot Eq. (1)'s coefficient** `563/(720π)` (L3 `:88–93`) and the `t ~ 1/T³` asymptote were not checked against PRD **71**, 064019.
- **Semantic correctness of D3's ~200 `\texttt{}` theorem references.** I verified that fifteen load-bearing names resolve to real declarations (all fifteen do) and inspected four for strength. I did not check that each of the remaining references proves what the surrounding sentence says it proves.
- **The `N=4` SYM decisive test (D3 §3.3), the Kostelecky–Russell–Tasson torsion bound, the Stelle-coefficient observational ceilings, and the PPN one-parameter-family claim** were read but not verified against their primary sources.
- **Figure quality.** L1 and L3 each ship one PNG; I confirmed the files exist and that the captions describe plottable content, but did not render or inspect them.
- **`bundle_readiness.py` and the Stage-13 sweep documents** were read only for their status fields, not re-run. Per the rubric I treated their verdicts as evidence, not conclusions, and every finding above is grounded in the artifact.
