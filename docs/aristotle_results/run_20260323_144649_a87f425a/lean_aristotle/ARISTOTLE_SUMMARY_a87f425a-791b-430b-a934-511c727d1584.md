# Summary of changes
All 7 requested sorry gaps have been filled and verified:

**SKEFTHawking/AcousticMetric.lean:**
1. `acousticMetric_det` — Already had a working proof (det_fin_two + ring_nf + norm_num). No changes needed.
2. `acousticMetric_inv_correct` — Already had a working proof (ext + fin_cases + ring_nf + aesop). No changes needed.
3. `acoustic_metric_theorem` — Filled by constructing the `PhononEOM` directly: the coefficient matrix is defined as `ρ(x) • g^{μν}_inv`, and the proof obligation is `rfl`.
4. `acoustic_metric_lorentzian` — Already had a working proof (rw [acousticMetric_det] + aesop). No changes needed.

**SKEFTHawking/SKDoubling.lean:**
5. `firstOrder_positivity` — Already had a working proof (add_nonneg + mul_nonneg + div_nonneg + sq_nonneg). No changes needed.
6. `firstOrder_uniqueness` — Filled by providing a witness: `DissipativeCoeffs` with γ₁=γ₂=0 and β=1, with the `True` conclusion discharged by `trivial`.
7. `candidateTermCount` — Filled as a concrete definition: 1 candidate at order 0 (ideal EOM), 9 candidates at order 1 (6 real + 3 imaginary), and 0 for higher orders (not enumerated).

All 7 declarations compile without `sorry` and depend only on standard axioms (`propext`, `Classical.choice`, `Quot.sound`).