# Goal prompt — Phase 6EB, `goal_id = 20260729T135527`

**Armed:** 2026-07-29 · **role:** solo · **mode:** lean · **arm_sha:** `9c2f4f0e`
**Roadmap:** `docs/roadmaps/Phase6EB_Roadmap.md`
**Notebook INDEX:** `docs/dev-loops/Phase6EB/LAB_NOTEBOOK_INDEX.md`

This is the **durable, crash-recoverable source** of the `/goal` condition. If the marker
(`.claude/dev-harness/managed/<sid>.json`) is lost, re-arm from this file and **reuse
`goal_id = 20260729T135527`** — do not mint a new one for this goal.

## Scope resolved at arm time (plan-currency check)

`6EB full strength` was armed against a roadmap whose header read `PLANNED` with Wave 2's AC boxes
unchecked. Verified against the tree instead of the checkboxes:

| wave | state at arm time |
|---|---|
| W1 `Detection/FilterFloors.lean` | **SHIPPED** — 23 decls, merged `4378cc01` |
| W2 `Detection/NEPAlgebra.lean` | **SHIPPED** — 41 decls, merged `193437a1` |
| W3 `Detection/MatchedFilter.lean` | **NOT STARTED** ← the goal |

Both shipped modules: zero sorry, zero axiom, zero `native_decide`, zero `maxHeartbeats`, both
root-imported in `lean/SKEFTHawking.lean`. W2 landed under names differing from the AC text
(`nep_def` → `nepOfPSD`/`nepOfOutput` + `nep_def_operational_eq_spectral`; `snr_composition` →
`snrChain` + composition lemmas). The roadmap was corrected before arming — the notebook FRONTIER
was already accurate.

**So the goal is Wave 3 + the Phase Definition of Done, not the whole phase.**

## The condition

```
GOAL: Ship Phase 6EB Wave 3 — lean/SKEFTHawking/Detection/MatchedFilter.lean, matched-filter
optimality over the admissible class — and close 6EB to its roadmap Definition of Done.
Source-of-truth: docs/roadmaps/Phase6EB_Roadmap.md + docs/dev-loops/Phase6EB/LAB_NOTEBOOK_INDEX.md
(re-read BOTH after any compaction; the roadmap's per-wave AC is the declaration-level truth; open
notebook shards on demand). Waves 1 and 2 are SHIPPED and green — do not rebuild them.

SCOPE IS SETTLED — land the MAXIMUM coherent GREEN increment THIS turn (as much as you can finish
cleanly; do NOT atomize into one tiny brick — per-turn throughput matters). A stop-hook firing is a
GO signal, never a cue to stop/hold/re-scope/hand back. Never re-derive a settled-dead fork: consult
the notebook's DECISIONS block and docs/dev-loops/SETTLED_FORKS.md — the one-sided PSD convention is
LOCKED, and a convention-ambiguous theorem is a defect, not a simplification. Legitimate stops ONLY:
a kernel-checked no-go, or a genuine user-only decision (ask once, keep shipping meanwhile).

NON-NEGOTIABLE: kernel-pure {propext, Classical.choice, Quot.sound}; zero sorry; zero new project
axioms without explicit user sign-off; zero native_decide; zero set_option maxHeartbeats in proof
bodies — a heartbeat wall means wrong proof architecture, decompose into `have` sub-lemmas.
Preemptive-strengthening BEFORE writing each statement. Optimality theorems state the admissible
class explicitly and carry their convention in the STATEMENT, not only the docstring. The new module
adds its own root import to lean/SKEFTHawking.lean. Never push.

DONE when ALL of:
(1) Detection/MatchedFilter.lean is shipped and root-imported, covering the roadmap's Wave-3 AC:
    matched-filter SNR optimality with equality characterization, the optimal-z budget corollary,
    the error-floor composition with 6EA Wave 2, and a norm_num non-vacuity witness at a concrete
    budget point;
(2) `lake build SKEFTHawking.ExtractDeps` prints "Build completed successfully" in the transcript,
    with zero sorry / axiom / native_decide / maxHeartbeats in the new module;
(3) validate.py prints 50/50 (or a higher N/N) in the transcript;
(4) the roadmap's Phase Definition of Done is met — Status flipped to COMPLETE with a dated
    shipped-declarations list, Inventory + counts refreshed;
(5) the fresh-context skeft-qa:adversarial-reviewer ran with ZERO BLOCKER findings surfaced in
    the transcript.
```

## Notes for a re-arm

- Mutable state deliberately excluded: no current-sorry line, no "live route", no commit SHAs in
  the condition. Those live in `scripts/repo_state_probe.py` (FIRST_ACTION), `SETTLED_FORKS.md`,
  and the notebook FRONTIER.
- `validate.py` was 50/50 at arm time (50, not 49 — `paper_toolchain_pin_drift` was added
  2026-07-29). The condition says "50/50 or a higher N/N" so a future check addition does not
  falsify it.
- Wave 3's roadmap AC names `6EA avg_error_ge_of_z_le` as the composition brick; 6EA is COMPLETE.
