# Goal prompt — Phase 6EA (goal_id `20260728T114358`)

**Armed:** 2026-07-28 · **role:** lead · **mode:** lean · **slots:** wt1, wt2
**arm_sha:** `c7a977cc20c297f9ba3e6a2f6dea762b9e014dee`
**Roadmap:** `docs/roadmaps/Phase6EA_Roadmap.md`
**Stage-2 freeze:** `docs/dev-loops/Phase6EA/Phase6EA_Stage2_StatementFreeze.md` *(moved here from
`docs/roadmaps/` 2026-07-28 — still tracked; only `**/LAB_NOTEBOOK*.md` is gitignored)*
**Notebook INDEX:** `docs/dev-loops/Phase6EA/LAB_NOTEBOOK_INDEX.md`

This file is the **durable, crash-recoverable source** of the `/goal` condition below. If the
marker (`.claude/dev-harness/managed/<session>.json`) is lost, re-arm by re-invoking
`/skeft-qa:goal-prompt`, recovering **this same `goal_id`** (do not mint a new one) and the
condition verbatim from this file.

> **Scope note (operator, 2026-07-28).** This goal is **6EA ONLY**. The `6E*` series (6EB–6EE)
> is developed **back-to-back in separate goals**, one per phase, each with its **own** notebook
> directory `docs/dev-loops/Phase6E{B,C,D,E}/` — already scaffolded and seeded from their own
> roadmaps. Rationale: keep per-goal context small; a series-wide goal goes off the rails.

---

## The `/goal` condition (verbatim)

```
GOAL: Ship Phase 6EA — kernel-verified photodetection statistics (Poisson & Gaussian
discrimination floors) — all three waves, to the roadmap's Phase Definition of Done.

SOURCE-OF-TRUTH (re-read after any compaction; open notebook shards on demand):
- docs/roadmaps/Phase6EA_Roadmap.md — the phase roadmap (3 waves, per-wave AC, Definition of
  Done). Its "Stage-2 resolutions - LEAD SIGN-OFF 2026-07-27" block is BINDING and supersedes
  the AC bullets it touches.
- docs/dev-loops/Phase6EA/Phase6EA_Stage2_StatementFreeze.md — the FROZEN statement layer for
  Waves 1-2 (exact Lean statement text + proof recipes + the SS9 D1-D13 deviation table). Also
  BINDING. Wave 3 is scoped and recommended there, not frozen: resolve its UNKNOWN before
  freezing Wave-3 statements.
- docs/dev-loops/Phase6EA/LAB_NOTEBOOK_INDEX.md — read FRONTIER and DECISIONS & DEAD-ENDS first.

SCOPE IS SETTLED - land the MAXIMUM coherent GREEN increment THIS turn (as much as you can
finish cleanly; do NOT atomize into one tiny brick - per-turn throughput matters). A stop-hook
firing is a GO signal, never a cue to stop, hold, re-scope, or hand back. Never re-derive a
settled-dead fork (negative frontier / docs/dev-loops/SETTLED_FORKS.md); specifically CLOSED for
this phase: the interval-restricted Gaussian lower tail Q z >= 1/2 - z/sqrt(2pi) (vacuous at
z > 1.2533, i.e. exactly in the operating range) and the OptimalHypothesisRate quantum seam
(type-level impossible - Poisson on N is not a Fintype). Legitimate stops ONLY: a kernel-checked
no-go, or a genuine user-only decision (ask once, keep shipping everything else meanwhile).

NON-NEGOTIABLE LOCKS (every wave): kernel-pure axiom set {propext, Classical.choice, Quot.sound};
ZERO sorry; ZERO new project-local axioms (Invariant #15 - explicit user sign-off required, a DR
recommendation is advisory only); no native_decide; no maxHeartbeats in any proof body
(Invariant #10 - decompose into `have` sub-lemmas); the preemptive-strengthening checklist BEFORE
writing every theorem statement, plus a ruthless post-wave audit logged in the notebook; EVERY
new module adds its own root import to lean/SKEFTHawking.lean IN ITS OWN COMMIT (the lean_lib
declares no globs, so an unimported module is not built by lake build at all and is invisible to
the zero-sorry gate, ExtractDeps, and counts); never push.

Bundle target D12 (Invariant #14 - bundle-aware content from inception; on-disk bundle
scaffolding executes only at first content-lift, not during substrate waves).

DONE when ALL of:
(1) The three wave AC blocks in the roadmap are checked off, with
    lean/SKEFTHawking/Detection/{PoissonDiscrimination,GaussianThreshold,ShotNoise}.lean shipped
    and the roadmap Status updated PLANNED -> COMPLETE with its dated shipped-declarations list;
(2) `lake build` AND `lake build SKEFTHawking.ExtractDeps` complete clean with zero sorry and
    zero axioms, and `uv run python scripts/validate.py` reports ZERO FAIL - all three surfaced
    in the transcript;
(3) the fresh-context skeft-qa:adversarial-reviewer ran with ZERO BLOCKER findings surfaced in
    the transcript (a vacuity/tautology hunt over the statement set is mandatory even though this
    phase has no paper target yet);
(4) SK_EFT_Hawking_Inventory.md, the Inventory Index, and counts are refreshed for the new
    Detection/ module family.
```

---

## Wave structure (durable — from the roadmap + freeze)

| Wave | File | Content | Parallelism |
|---|---|---|---|
| 1 | `Detection/PoissonDiscrimination.lean` | Le Cam / Bhattacharyya floor family + the two-sided folklore refutation | **∥ Wave 2** |
| 2 | `Detection/GaussianThreshold.lean` | project-local `gaussianQ`, tail enclosures, threshold algebra | **∥ Wave 1** |
| 3 | `Detection/ShotNoise.lean` | thinning, shot PSD plane transfer, the classical↔quantum seam | consumes W1 — serialize last |

**Launch fan-out:** Waves 1 and 2 are independent (different files, disjoint substrate, sharing
only `avgAssignmentError`) → two slots on turn 1. Wave 3 serializes after Wave 1.

## Frozen decisions that must NOT be re-litigated

- **UNKNOWN-1** → Mathlib `poissonPMFReal` carrier; generic Le Cam chain stated over bare
  `p q : ℕ → ℝ` with `HasSum … 1`. The randomized-rule quantifier is *orthogonal* to the pmf
  carrier — a rule is `δ : ℕ → ℝ` with `δ n ∈ [0,1]`. `PMF`/`ℝ≥0∞` rejected. Use
  `Real.sqrt (Nb : ℝ)`, never `NNReal.sqrt`.
- **UNKNOWN-2** → Mathlib has **no** `erf`/`erfc`/Gaussian-CDF at pin, so Wave 2 *defines* `Q`.
  Must-ship lower tail is the global parametric window bound `c·φ(z+c) ≤ Q z`; Birnbaum
  `z/(1+z²)·φ(z)` is a stretch that substitutes without restating any consumer.
- **UNKNOWN-3** → seam routes through the `Fin 2` pushforward as a diagonal restriction of the
  project's own FvdG. Do **not** describe Wave 3 as "first project consumption of
  `HypothesisTesting`" unless that consumption actually happens.
- **D7** → attempt `gaussianTail_chernoff` at `z ≥ 0` **first**; `z ≥ 0.8` is the fallback, and
  if taken, the failure must be documented. Not a pre-authorized narrowing.
- **D3** → `poissonTV_le_of_bhattacharyya` demoted to optional (the Le Cam route never passes
  through TV).

## Lead-verified substrate (2026-07-28, this session — not inherited from the docs)

- `SKEFTHawking.QuantumNetwork.avgAssignmentError (e0 e1 : ℝ) := (e0 + e1) / 2` — exists,
  `ReadoutRelaxationBound.lean:143`. Consume it; never re-define `(e₀+e₁)/2`.
- `expNeg_enclosure` — exists, `NumericalBounds.lean:31` (roadmap's `:23` is stale).
- `classical_fvdg` — exists, `FidelityUpperBound.lean:51`. Cited as structural **sibling**, NOT
  imported (would pull the trace-norm tower into `Detection/`).
- Mathlib Poisson: `Mathlib.Probability.Distributions.Poisson.Basic`; rate is `ℝ≥0`;
  `poissonPMFRealSum` carries **no** positivity hypothesis — this is what makes the `N_b = 0`
  dark-baseline statement possible at all.
- Mathlib `erf`/`erfc`/Gaussian-CDF: **zero** declaration-level hits, confirmed.
- PhysLib path is `QuantumInfo.Finite.ResourceTheory.HypothesisTesting` (not `Physlib.…`).
- `lean/SKEFTHawking.lean` is an explicit **1,918**-import aggregator; `lean_lib SKEFTHawking`
  declares **no `globs`** — hence the per-wave root-import lock.

## Atlas context at arm time

KEYSTONE `hyp:rokhlin_sigma_mod_16` (gates 11) belongs to the 5q.H track and is **unrelated** —
6EA is a new-substrate build, not a tracked-hypothesis discharge, so there is no plan/atlas
conflict. Negative frontier: 8 ranked kernel-checked dead forks (+399 naming-only), none in
`Detection/`.
