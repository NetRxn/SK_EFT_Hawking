# Goal prompt — D10 Discharge loop (durable, crash-recoverable source)

- **goal_id:** `20260630T063621`  (re-armed 2026-06-30 — condition rewritten for crisp evaluator-moderation + full-completion scope; bump-gated DFT semiboundedness cleanly separated out)
- **role:** solo
- **mode:** lean
- **roadmap_path:** `docs/roadmaps/D10_Discharge_Roadmap.md`
- **notebook (INDEX):** `docs/dev-loops/D10_Discharge/LAB_NOTEBOOK_INDEX.md`
- **armed_ts:** 1782828742 (2026-06-30)
- **arm_sha:** `ae320a8e05556f8af50b4b95c95f62b2d62fa352`
- **Scope:** discharge hkin (W2), hrel (W3), hreal (W4) to fully unconditional. DFT semiboundedness (bddBelow / fiber_bddBelow, W5) and anything depending on the PhysLib v4.30.0 bump are OUT of this goal — a separate goal picks them up after this completes.

## The `/goal` condition (the durable, re-injected source — this file is the recovery copy)

GOAL: Make the D10 comp-chem/open-system headline theorems UNCONDITIONAL by discharging their three disclosed analytic hypotheses — hkin, hrel, hreal — to fully proven, kernel-pure theorems. Source-of-truth (re-read after any compaction; open shards / roadmap waves on demand): docs/roadmaps/D10_Discharge_Roadmap.md + docs/dev-loops/D10_Discharge/LAB_NOTEBOOK_INDEX.md; also read docs/dev-loops/PRE_DECISIONS.md + docs/dev-loops/SETTLED_FORKS.md before any "impossible / needs-a-banned-route" reasoning. FIRST_ACTION every fresh turn: run scripts/repo_state_probe.py for the REAL live sorry/build state (never trust a snapshot) + lean_diagnostic_messages on the active file. SCOPE IS SETTLED — build the next brick THIS turn; a stop-hook firing is a GO signal, never a cue to stop/hold/re-scope/hand-back; never invoke context-budget/session-depth to defer (auto-compaction is handled; the full budget for this program is authorized). Legitimate stops ONLY: a kernel-checked no-go, or a genuine user-only decision (ask once via the guard, keep shipping everything else meanwhile). The whole program ships at full strength — correctness over expediency; every theorem in its strongest correct form. DURABLE APPROACH (detail lives in the roadmap): W1's ess-s.a. criterion isSelfAdjoint_closure_of_dense_range is DONE in root; hkin via PhysLib kineticOperator -> momentumSqOperator + Mathlib laplacian_eq_fourierMultiplierCLM dense-range + that criterion; hrel via Hardy's inequality (from scratch); hreal via the matrix Lie-Trotter product of CP factors. No new project-local axiom without explicit user sign-off (#15). BAR: kernel-pure {propext, Classical.choice, Quot.sound}; zero new sorry/axiom/native_decide; no maxHeartbeats in proof bodies (decompose into have); preemptive-strengthening checklist before EACH statement + ruthless post-wave audit; never push. DONE — and not before — when ALL of the following are surfaced in the transcript: (1) hkin discharged: the molecular kinetic operator's essential self-adjointness is a PROVEN theorem with no such hypothesis, and molecularHamiltonian_essSelfAdjoint NO LONGER takes an hkin argument; (2) hrel discharged: the Coulomb relative-bound a<1 is a PROVEN theorem (Hardy), and molecularHamiltonian_essSelfAdjoint NO LONGER takes an hrel argument — so molecular-Hamiltonian essential self-adjointness is now fully unconditional; (3) hreal discharged: the CPTP/Kraus realization of e^{tL} is a PROVEN theorem, and traceDist_lindblad_monotone NO LONGER takes an hreal argument; (4) every touched module builds clean — 0 sorry, 0 new axiom, lean_verify shows exactly {propext, Classical.choice, Quot.sound} on each discharged headline; root imports added; full lake build AND lake build SKEFTHawking.ExtractDeps print green in the transcript; (5) papers/D10/paper_draft.tex updated so its theorem statements and Conclusion present hkin/hrel/hreal as discharged (removed from the "next steps"), with the supersession ledger noted; (6) validate.py prints its full ALL CHECKS PASSED (N/N) line in the transcript, and counts + Inventory_Index are refreshed; (7) a fresh-context skeft-qa:adversarial-reviewer AND skeft-qa:claims-reviewer each ran and reported ZERO BLOCKER and ZERO MAJOR findings, surfaced in the transcript — self-audit is insufficient.
