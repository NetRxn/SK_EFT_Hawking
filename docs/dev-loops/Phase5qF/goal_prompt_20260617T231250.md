# /goal — Phase 5q.F L2 close (Route B grind)

**goal_id:** `20260617T231250` · **role:** solo · **session:** `8ca6a498-7a8d-4dad-84c7-5e5739d628ac`
**Re-arm history:** armed 2026-06-17 · re-armed 2026-06-20 (post `/clear`+restart; notebook sharded to INDEX) ·
re-armed 2026-06-22 (brick-3 cup-form condition) · **re-armed 2026-06-23 (THIS — grind-on-B explicit; under
harness v4.2; broken turn-cap removed).** Prior condition bodies are superseded — recover them from git history
of this file if needed. The gitignored marker is the fast-read copy; **this tracked file is the durable,
crash-recoverable source** (spec A.5).

---

## ⚡ RE-ARM 2026-06-23 — CURRENT condition (supersedes all blocks above in git history)

**Why re-armed:** the prior arm spiralled — ~5 days of *lateral motion* (route-shopping, re-deriving the known
reduction, re-opening Route C, "should I ask?") instead of executing the approved Route-B grind. The 2026-06-23
retrospective found the engine was **grind-avoidance**, not the keystone's difficulty. This condition makes
**the grind on B the explicit centerpiece**, removes the broken `stop after N turns` turn-cap (the Haiku
evaluator can't count turns; it conflicted with the GO posture and produced the terminal stop-loop), reframes
Route C as settled-off-the-table, and adds STUCK⇒GRIND routing. Runs under harness v4.2 (stall-detector +
`skeft-qa:coach` backstop live; harvest host armed).

**Operative `/goal` condition (verbatim — pass to `/goal`; mirrored in the marker):**

```
GOAL: Finish L2 — discharge the SINGLE sorry (line 115) in SK_EFT_Hawking/lean/SKEFTHawking/SingularConnSquareCloseNC.lean (thm subHomConnecting_openDuality), KERNEL-PURE, via Route B PAIRING = cap-product MV-naturality (Hatcher 3.36). SOURCE-OF-TRUTH (re-read after ANY compaction): roadmap SK_EFT_Hawking/docs/roadmaps/Phase5qF_GeometricBordism_Roadmap.md + notebook INDEX Lit-Search/Phase-5qF/LAB_NOTEBOOK_INDEX.md (FRONTIER + DECISIONS/DEAD-ENDS; open shards on demand) + standing decisions SK_EFT_Hawking/docs/dev-loops/PRE_DECISIONS.md. THE GRIND IS THE WORK (re-read every turn): Route B is a KNOWN, APPROVED, multi-brick grind (the cap-Leibniz construction); its length was anticipated and chosen deliberately. DOING the grind, one concrete kernel-pure brick per turn, IS the task. The ONE failure mode that cost ~5 days was AVOIDING the grind via lateral motion — route-shopping, re-deriving the known reduction, re-opening Route C, decompose-for-the-Nth-time, or drafting a 'should I ask?'. Every one is the SAME anti-pattern: motion that burns the exact budget that would finish the grind. 'Generate motion' is NEVER the goal; 'advance Route B by one brick' is. If you catch yourself hunting a cleaner route or re-questioning the path, stop that and return to the next B brick. STUCK => GRIND: 'stuck-but-tractable' has ONE resolution, do the tractable work per PRE_DECISIONS.md. Async escalation (Aristotle ~1 day) is dispatch-and-CONTINUE, never block. SETTLED LOCKS (B<->C thrashed for DAYS — NON-NEGOTIABLE): (1) Route C (snake / MV-SES + SnakeInput) is SETTLED-BANNED, a multi-wave rebuild kept ONLY as last-resort fallback; NEVER re-open, re-cost, or re-litigate B-vs-C. (2) Stay at CHAIN-pairing altitude (whnf-dodged, z_seam ∃-bound); NEVER lift to the homology-CLASS square (needs the false hmem; reverted 2026-06-22). (3) kronecker_cap_chainIncl_eq_rcap_chainIncl (MatchLHS:83) is a FALSE POSITIVE — multi_attempt empty-goals != in-file close; confirm with lean_diagnostic/lake build. (4) NO subagents for the L2 close, drive SOLO; MCP-first (/skeft-qa:goal-dev, lean4). CURRENT STATE: NC green, ONE sorry @115 = the cross-realization cap-product MV-naturality match; the connecting-square descent + subdivision helper are BANKED kernel-pure (subHomConnecting_openDuality_of_crossRealization, kronecker_singularSd_iterate_cocycle, merged 1f92f0b7). Close-path engines: kronecker_cap_eq_kronecker_rcap (MatchLHS) + chainIncl_rcap_cover_agree (RcapCoverAgree) + relativeDualityK_cycle_compat_relB (OpenDualityCycle), both V-parts onto the joined grep+a' over the shared z₀. The concrete c hits the 200k-whnf wall — decompose into <=12-term have sub-lemmas, NEVER maxHeartbeats. KERNEL-PURITY: axioms EXACTLY {propext, Classical.choice, Quot.sound}; NO new axiom/sorry/native_decide/maxHeartbeats; STATEMENT of subHomConnecting_openDuality UNCHANGED. Each brick: update INDEX FRONTIER + LAB_NOTEBOOK.md; commit GREEN kernel-pure shards; NEVER push. POSTURE: scope SETTLED — build the next B brick THIS turn; a stop-hook firing is a GO signal, never a cue to stop/hold/re-scope/hand back. DONE when ALL hold, surfaced in the transcript: (1) NC zero sorry AND #print axioms SKEFTHawking.SingularConnSquareCloseNC.subHomConnecting_openDuality = EXACTLY {propext, Classical.choice, Quot.sound}; (2) lake build SKEFTHawking.SingularConnSquareCloseNC + SKEFTHawking.ExtractDeps clean AND validate.py prints N/N (>=43/43) in the transcript, INDEX/notebook/counts synced; (3) a fresh-context skeft-qa:adversarial-reviewer ran in a SEPARATE context (kernel-purity + statement-integrity; NC + downstream PoincareDual4Mid/Lo green) with ZERO BLOCKER findings. Then PARENT Phase 5q.F (L3/L4/L5) resumes. Legitimate stops ONLY: a kernel-checked no-go (carry ONE disclosed Prop + discharge plan) or a genuine user-only decision (coach / AskUserQuestion — ask once, keep shipping) — NO turn-cap; the v4.2 stall-detector + harvest/coach are the non-convergence backstop.
```

## Recommended pre-step (not blocking the arm)

The notebook is oversized (INDEX ~83 KB, `LAB_NOTEBOOK.md` ~276 KB / ~69k tokens) — exactly the bloat that
drives post-compaction re-discovery (the failure-#3 mechanism). Run `/skeft-qa:notebook shard` + trim the INDEX
FRONTIER to a few lines early in the loop so re-orientation stays cheap.
