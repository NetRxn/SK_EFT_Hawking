# /goal — Phase 5q.F FULLY-UNCONDITIONAL (the w₂-tower floor-collapse)

**goal_id:** `20260617T231250` · **armed:** 2026-06-17 · **re-armed:** 2026-06-20 (this session, after a `/clear` + computer restart; notebook sharded W1–W4 → INDEX entry point) · **session:** `6e3e8d9d-ba0f-4d02-b2fc-8dcc21ca1090` (prior `70cc8da1…` was this same conversation pre-`/clear`; its marker is cleared) · **re-armed:** 2026-06-22 (session `8ca6a498-7a8d-4dad-84c7-5e5739d628ac`; goal_id REUSED — same L2 goal; condition refreshed to the brick-3 state) · **role:** solo (the L2 close is SOLO-mandated; the prior `lead`/3-slot apparatus was for the broader L1–L4 fan-out)
**Durable, crash-recoverable source of the `/goal` condition** (the gitignored marker is the fast-read copy; this tracked file is authoritative — spec A.5).

---

## ⚡⚡ RE-ARM 2026-06-22 (session `8ca6a498-7a8d-4dad-84c7-5e5739d628ac`, goal_id REUSED) — CURRENT condition (brick 3; SUPERSEDES the cup-form block below)

The route is **still Route B (pairing)** — but the SUB-approach evolved cup-form → hmatch → (a)+(b)+cover_partition. Steps **(a)+(b) are BAKED green** in NC: `← kroneckerH_absCohomConn` (adjunction onto the absolute-cohomology class) + `obtain ⟨grep,rfl⟩ := Submodule.Quotient.mk_surjective _ g` + `erw [legW_mk]` ⟹ LHS = `kroneckerH (absCohomConn (mk a'rep)) [grep ⌢ fundCycleW]`. The 1 remaining `sorry` = the cross-space match. The cover-split recipe (`exists_mvUnion_partition` + `kroneckerH_absCohomConn_cover_partition`) is found and likely shard-free (metavar trick). **Lock #3 CLARIFIED:** `← kroneckerH_absCohomConn` THEN `cover_partition` is ALLOWED (concrete CHAIN-pairing seam, whnf-dodged); only `…THEN congr/relativeDualityK to the homology-CLASS square` is the forbidden-circular route.

**Operative `/goal` condition (verbatim — pass to `/goal`; mirrored in the marker):**

```
GOAL: Close L2 — the LAST open piece of unconditional Phase 5q.F: discharge the SINGLE `sorry` in `SK_EFT_Hawking/lean/SKEFTHawking/SingularConnSquareCloseNC.lean` (thm `subHomConnecting_openDuality`), KERNEL-PURE, via Route B PAIRING (cap-product MV-naturality, Hatcher 3.36). Re-read after ANY compaction: `Lit-Search/Phase-5qF/LAB_NOTEBOOK_INDEX.md` (FRONTIER+Dead-ends) + `LAB_NOTEBOOK.md` (bricks 3/3b/3b.2 = current) + roadmap `SK_EFT_Hawking/docs/roadmaps/Phase5qF_GeometricBordism_Roadmap.md`. CURRENT STATE (brick 3, 2026-06-22): steps (a)+(b) BAKED green (`← SingularSubHomologyMVCohomConn.kroneckerH_absCohomConn`; `obtain ⟨grep,rfl⟩ := Submodule.Quotient.mk_surjective _ g`; `erw [SingularLegWCapForm.legW_mk]`) ⟹ LHS = `kroneckerH (absCohomConn (mk a'rep)) [grep ⌢ fundCycleW]`; the 1 sorry = the cross-space match `LHS = relKroneckerH σ [chainIncl(legSplitVᶜ) w']`. FIRST ACTION (LAB_NOTEBOOK.md brick-3b.2): add imports `SKEFTHawking.SingularCoverPartitionExist`+`SingularConnSquareLHSExplicit`+`SingularAbsCohomConnGeom` to NC; then the INLINE metavar cover-split (the shard is likely UNNECESSARY): `exists_mvUnion_partition` on the preimage cover (val⁻¹U,val⁻¹V) with `_` for the cycle → `rw [hclass]` (rw SOLVES the metavar) → `kroneckerH_absCohomConn_cover_partition U V hU hV p a'rep zA zB hcyc'` → `rw [hkron]` ⟹ LHS = `kronecker a'rep (mapChain subSeam (mapChain seam zseam))`. THEN the FINAL cross-space match (the irreducible core, NO single closer): both V-parts → `⟨grep∪a', z₀⟩` over the shared z₀, via `SingularConnSquareMatchLHS.kronecker_cap_eq_kronecker_rcap` + `SingularRcapCoverAgree.chainIncl_rcap_cover_agree` + `SingularOpenDualityCycle.relativeDualityK_cycle_compat_relB`. ANTI-THRASH LOCKS (B↔C thrashed for DAYS — NON-NEGOTIABLE): (1) NEVER re-open Route B vs C; B SETTLED, C(snake) BANNED. (2) NEVER cite the hcross DR route ranking. (3) `← kroneckerH_absCohomConn` THEN `cover_partition` is ALLOWED (gives the seam as a CONCRETE CHAIN pairing, whnf-dodged, z_seam ∃-bound — the endorsed brick-2 path); FORBIDDEN-CIRCULAR is `kroneckerH_absCohomConn` THEN `congr`/`relativeDualityK` to the homology-CLASS square (returns this exact square, needs the false `hmem`; reverted 2026-06-22). Stay at CHAIN-pairing altitude; NEVER lift to the class square. (4) NO subagents for the L2 close — drive SOLO. (5) `kronecker_cap_chainIncl_eq_rcap_chainIncl` (MatchLHS:83) 1-liner is a FALSE POSITIVE (multi_attempt empty-goals ≠ in-file close — confirm with lean_diagnostic/lake build; single-space vs cross-space). Resist ~10-15 turns → decompose+bank / arc-trace, NEVER switch routes. POSTURE: scope SETTLED — build the next brick THIS turn; a stop-hook firing is a GO signal, never a cue to stop/hold/re-scope/hand back. MCP-first (goal-dev `references/lean-dev.md`). Kernel-purity: axioms EXACTLY {propext,Classical.choice,Quot.sound}; NO new axiom/sorry/native_decide/maxHeartbeats; STATEMENT of `subHomConnecting_openDuality` UNCHANGED. Each brick: update INDEX FRONTIER + LAB_NOTEBOOK.md; commit GREEN kernel-pure shards; NEVER push. Legitimate stops ONLY: a kernel-checked no-go (carry ONE scoped disclosed Prop + discharge plan), or a genuine user-only decision (ask once, keep shipping). DONE when ALL hold, surfaced in the transcript: (1) NC zero sorry; `#print axioms SKEFTHawking.SingularConnSquareCloseNC.subHomConnecting_openDuality` = EXACTLY {propext,Classical.choice,Quot.sound}; (2) `lake build SKEFTHawking.SingularConnSquareCloseNC` + `SKEFTHawking.ExtractDeps` clean and `validate.py` prints N/N (≥43/43) in the transcript; INDEX/notebook/counts synced; (3) a fresh-context `skeft-qa:adversarial-reviewer` ran in a SEPARATE context (kernel-purity + statement-integrity; NC + downstream PoincareDual4Mid/Lo green) with ZERO BLOCKER findings. Then PARENT Phase 5q.F (L4/L5/W6–W9) resumes — re-arm `goal_prompt_20260617T231250.md`. — or stop after 60 turns.
```

---

## ⚡ ACTIVE FOCUS (2026-06-20→22, cup-form — SUPERSEDED by the RE-ARM above): TARGETED L2 — cup-form PAIRING close

The user **narrowed the operative `/goal` condition to L2 only** (the single blocker) to kill the multi-day
B↔C route thrash. The background agent's `relativeDualityK`/`congr` detour was **reverted 2026-06-22** → back
to the clean cup-form match (`SingularConnSquareCloseNC.lean:91`, GREEN, one `sorry`; `subHomConnecting_openDuality`).
The PARENT Phase 5q.F goal (L4/L5/W6–W9, in the synthesis below + its condition further down) **resumes once L2
closes**. Route B (cup-form pairing) is **SETTLED**; Route C (snake) is **BANNED**; the homology-class /
`relativeDualityK` / `congr` lift is **DOCUMENTED-CIRCULAR** (returns this exact square) and is forbidden.

**Operative `/goal` condition (verbatim — pass to `/goal`; mirrored in the marker):**

```
GOAL: Close L2 — the LAST open piece of unconditional Phase 5q.F: discharge the SINGLE `sorry` in `SK_EFT_Hawking/lean/SKEFTHawking/SingularConnSquareCloseNC.lean` (thm `subHomConnecting_openDuality`, now NC:91), KERNEL-PURE, via Route B cup-form PAIRING ONLY (cap-product MV-naturality, Hatcher 3.36). Re-read after ANY compaction: INDEX `Lit-Search/Phase-5qF/LAB_NOTEBOOK_INDEX.md` (FRONTIER+Dead-ends) + `LAB_NOTEBOOK.md` + memory `project-l2-routes-b-c-compaction-prep-2026-06-22` + roadmap `SK_EFT_Hawking/docs/roadmaps/Phase5qF_GeometricBordism_Roadmap.md`. GOAL STATE (clean cup form, detour reverted 2026-06-22): `kronecker (cup (ω↾) b) fund_∪ = kronecker (cup (mvω↾) a'rep) fund_∩`; `b` reps `absCohomConn a'rep` (`hb`), `mvω` reps `relCohomMvConnecting σ` (`hmvc`), both `fund=fundCycleW(castChain z₀)` of the SAME `z₀` (∂z₀=0). LOCKED PATHWAY — stay at the cup/chain-pairing altitude; both legs → `⟨grep∪a', z₀⟩` by cap-Leibniz over the shared z₀. Committed 0-sorry toolkit: LHS `SingularConnSquareLHSCover.cup_cover_pairing_sd` (splits `⟨cup g b,fund⟩` into the 2 cover-part cap-pairings); match `SingularConnSquareRHSPairing.pair_fund_eq_pair_z0` (cocycle `c=grep∪a'` vanishing on `C(Kᶜ)` pairs equally vs `fund` & `z₀` when rel-homologous); RHS `SingularConnSquareCloseFinal.relKroneckerH_relMvDelta_pairing` (UNCONDITIONAL, no hmem). Reconciliations = THE work (not thrash): sub↔M `subspaceChainsEquiv` bridge for cup_cover_pairing_sd; opaque `b↔a'rep` via `hb`; `fund_∪↔fund_∩↔z₀`. ANTI-THRASH LOCKS (the B↔C debate thrashed for DAYS — NON-NEGOTIABLE): (1) NEVER re-open Route B vs C; B SETTLED, C (snake) BANNED. (2) NEVER cite the hcross DR route ranking — it re-seeds the thrash. (3) NEVER lift to homology classes: the `congr`/class/`relativeDualityK`/`kroneckerH_absCohomConn`-then-`congr` route is DOCUMENTED-CIRCULAR (returns this exact square; reverted 2026-06-22); the seam V-part class match needs the false `hmem` — dead. (4) NO subagents — drive SOLO. (5) NO form-juggling (cup↔relativeDualityK↔hmatch round-trips) as a substitute for closing. (6) `kronecker_cap_chainIncl_eq_rcap_chainIncl` 1-liner is a FALSE POSITIVE (lean_multi_attempt empty-goals ≠ in-file close — confirm with lake build). If a step resists: climb the PD-1 ladder (toehold-sweep substrate / decompose+bank / arc-trace) — NEVER switch routes. POSTURE: scope SETTLED — build the next brick THIS turn; a stop-hook firing is a GO signal, never a cue to stop/hold/re-scope/hand back. MCP-first (goal-dev `references/lean-dev.md`). Kernel-purity non-negotiable: axioms EXACTLY {propext, Classical.choice, Quot.sound}; NO new axiom/sorry/native_decide/maxHeartbeats; the STATEMENT of `subHomConnecting_openDuality` UNCHANGED. Each brick: update INDEX FRONTIER + append terse detail to LAB_NOTEBOOK.md. Public SK_EFT_Hawking: commit GREEN kernel-pure shards; NEVER push; never touch the private repo. Legitimate stops ONLY: a kernel-checked no-go (build around it; carry ONE scoped disclosed Prop + discharge plan), or a genuine user-only decision (ask once, keep shipping). DONE when ALL hold, surfaced in the transcript: (1) `subHomConnecting_openDuality` closes — NC zero sorry; `#print axioms` of it shows EXACTLY {propext, Classical.choice, Quot.sound}; (2) `lake build SKEFTHawking.SingularConnSquareCloseNC` + `SKEFTHawking.ExtractDeps` clean and `validate.py` prints N/N (≥43/43) in the transcript; INDEX/notebook/counts synced; (3) a fresh-context `skeft-qa:adversarial-reviewer` ran in a SEPARATE context (kernel-purity + statement-integrity audit: statement matches committed form; no new axiom/sorry/native_decide/maxHeartbeats; NC + downstream PoincareDual4Mid/Lo green) with ZERO BLOCKER findings. Then the PARENT Phase 5q.F goal (L4/L5/W6–W9) resumes — re-arm `goal_prompt_20260617T231250.md`. — or stop after 60 turns.
```

---

## Objective (literal AC#1 of `Lit-Search/Phase-5qF/goal_prompt_unconditional.md`)

Derive `Ω₄^{Pin⁺} ≅ ℤ/16` on the **FULL** genuine `DataBordismGrp ξ` carrier (real manifolds-with-boundary) with **`ker(abkGrade)=0`** — taking **NO** `PinPlusBordismLandmark` / cap-hypothesis / disclosed `Prop`. This is the fully-unconditional finish line; the honest-scope goal (the iso *with* one permitted disclosed input) is already COMPLETE + review-genuine (`3d35dc45`→`c1e3365e`).

## Synthesis — where we are (2026-06-17, 101 kernel-pure commits, GREEN)

The only thing between the done honest-scope iso and the unconditional one is the **floor-collapse**: `DataBordismGrp` over all `SingularManifold`s retains an unoriented-bordism floor `Ω^O_•` (2-torsion, invisible to the ABK grade). `ker(abkGrade)=0` ⇔ pin down *which* manifolds carry Pin⁺ ⇔ **`w₂(TM)` as a genuine manifold invariant** = the smooth-4-mfld algebraic-topology tower Mathlib lacks. That tower is what the c8.x bricks build.

### The tower (5 layers), honest status

| Layer | Gives | Status |
|---|---|---|
| **L1 — fundamental class `[M]∈Hₙ(M;ℤ/2)`** | PD substrate | ✅ **COMPLETE** (`homologyTopEquivZMod2 : Hₘ₊₂(M;ℤ/2)≅ℤ/2` + `fundamentalClass [M]`, Hatcher 3.26/3.27 from scratch — singular ℤ/2 homology, excision, MV, spheres, local homology; fresh-context review GENUINE). |
| **L2 — Poincaré duality + cap** → `PoincareDual4Mid` | `mu`/`nondeg` pairing | 🔄 **LIVE FRONTIER.** The whole compactly-supported-cohomology 5-lemma ladder is built; `nondeg` reduced (`nondeg_of_duality_injective`) to `D_M` injective = the PD6f-(c4) connecting-square. **Left = ONE residual:** the cross-realization match (cup form) at `SingularConnSquareCloseNC.lean:91` (both legs `=⟨g∪a',∂z₀⟩` via cap-Leibniz over the shared `z₀`) ⟹ unconditional `subHomConnecting_openDuality` ⟹ instantiate `PoincareDual4Mid`/`PoincareDual4Lo`. **See the ⚡ ACTIVE FOCUS section at top for the locked cup-form PAIRING route (2026-06-22).** |
| **L3 — Steenrod `Sq²` + Wu `w₂=v₂+v₁²`** | `w₂` as an invariant | ✅ **DONE** (`621b46de`). `PoincareDualityWuFormula` (`wuW2=v₂+v₁²`, `wuW2_eq_zero_iff` floor-collapse criterion, proven PARAMETRICALLY over `PoincareDual4Lo X`); both operation fields CONCRETE + kernel-pure: `cupH13`→`cup13`, `Sq¹` Bockstein (`SingularBockstein`)→`sq1₃`. All 3 L3 modules wired into the aggregator; review GENUINE. |
| **L4 — `w₂` bordism-invariance** | floor collapse → `ker(abkGrade)=0` | not started; consumes `wuW2_eq_zero_iff` (L3, have) + the L2 `PoincareDual4Mid` instance. The `Function.Injective abkGrade` feeds the proven `dataBordism_iso_zmod16_of_complete_grade hinj`. |
| **L5 — the `≤16` ABK-completeness** | upper bound | the one genuinely-hard piece (below); sequenced LAST. |

**Update 2026-06-20 (re-arm, this session):** **L1 `[M]` COMPLETE + L3 DONE** (Wu formula / cupH13 / Sq¹ Bockstein, both review-GENUINE). **L2-PD is the live frontier, reduced to the single `hcore` cross-realization match** (the documented multi-session crux at `SingularConnSquareCloseNC.lean:334`; route = the chain-map reframe, the M2 homology-class V-part vs. the rcap-subdivision LVᶜ-part, reconciled by cap-Leibniz on the shared `z₀`). The "Left:" cells are the live state; the per-brick frontier is the notebook INDEX FRONTIER + checklist (re-read after any compaction). NEXT bricks: build the `hLHS`/`hRHS` V-part realization bridges → close `hcore` → L2 done → L4 floor-collapse → L5.

### The honest crux on L5 (not papered over)

Primary-source fact (TY/HKT/DDDKLPT/GEM; ABP-1969, Kirby–Taylor-1990) — **a theorem, not an LOE estimate**: the "16" is **irreducibly a spectral-sequence fact**; it provably cannot come from a LES of elementary groups. Exactly **one** cited spectral-sequence input is mathematically required, and the goal's permitted decidable height-4 cap (`col4_height_eq_four = 4`, machine-checked, `axioms:[]`) **is** that input. The only path to `≤16` with **zero** cited input is to formalize **Kirby–Taylor's geometric generation theorem** (every Pin⁺ 4-mfld bordant to k·[RP⁴], handle/surgery) — the deepest single brick.

## Plan (locked — user agreed 2026-06-17)

1. **Build L1→L4 now via the 3-slot apparatus** — shared by every L5 endpoint, where the active bricks are, and the DAG has branched enough to fan out.
2. **L5 is sequenced LAST.** Target: geometric Kirby–Taylor generation (→ literal AC#1, zero cited input). The decidable height-4 cap is the **pre-authorized goal-permitted terminal input ONLY on a kernel-checked no-go** (STOP POLICY (a) — carry ONE scoped disclosed `Prop` + discharge plan, only after building everything around it). This is not a descope; it is the route's own ABP-mandated factoring as the final fallback.

### Fan-out decomposition (lead, persistent slots wt1/wt2/wt3)

- **wt1 (lead-coupled spine):** L1's MV connecting-map → LES → `Hₙ(M\|A)` compactness induction → `[M]`. Tightly coupled — one fast slot / solo.
- **wt2 (independent, cohomology-side):** L3 Steenrod `Sq²` deepening + the Wu functional — independent of the `[M]` homology chain.
- **wt3:** L2's algebraic PD-pairing / intersection-form packaging that does not yet need `[M]`.
- Converge for **L4** (w₂-invariance), then **L5**.

Lead flow per the goal-prompt skill `references/lean-dev.md` (§ Parallel Lean development): reset each slot to `main` (`git -C .claude/worktrees/wtN checkout -B worktree-wtN main`), dispatch `Agent(subagent_type="skeft-qa:lean-worker", prompt="SLOT N=…, use mcp__lean-lsp-wtN__*, <brick + Lit-Search/Phase-5qF DR refs + acceptance>")`, merge `worktree-wtN`, re-gate. Fan out only on genuinely-branched bricks; keep the coupled spine solo.

## Acceptance criteria (from `goal_prompt_unconditional.md`)

1. Full-carrier `Ω₄^{Pin⁺} ≅ ℤ/16` with `ker(abkGrade)=0`; **no** landmark/cap/disclosed-Prop binder.
2. Geometric Smith map manifold-valued (`PD(a)` cut), not the thin/typed grade map.
3. `Ω₅^{Spin-ℤ₄} ≅ ℤ/16` via the Smith iso; `daiFreed:ℤ` carrier discharged.
4. `CommonOrigin.sixteen_convergence_common_origin` drops `SmithInflow`; registry `smith_inflow_z16` → theorem; Rokhlin/Kitaev re-pointed.
5. Kernel-pure ({propext, Classical.choice, Quot.sound}); zero new axiom / sorry / native_decide / maxHeartbeats.
6. Fresh-context `skeft-qa:adversarial-reviewer` confirms GENUINE (not thin/renamed; honest scope).
7. Closure: roadmap W1–W9 closed; `lake build SKEFTHawking.ExtractDeps` + `validate.py` green; counts / Inventory / status / roadmap / notebook synced.
8. `pin4_abutment` / `adamsAbutment` / posited `Omega4PinPlusBordism` retired.

## Posture

Full-strength autonomous; authorized through ANY chunk size (fundamental class / PD / 4-mfld classification / Smith machinery from scratch = pre-authorized). **NO disclosure-stopping** (`feedback-no-disclosure-stopping-in-goal-mode`); a stop-hook firing = "do the next brick THIS turn." LOE / person-year estimates DISREGARDED (`feedback-ignore-pm-estimates`); Mathlib-absence = a brick list. Legitimate stops ONLY: a kernel-checked no-go, or a genuine user-only product decision. Update the lab notebook each brick. Public `SK_EFT_Hawking`; commit GREEN kernel-pure shards + regenerated autogen; **NEVER push**; never touch a parallel agent's files or the private repo.

---

## PARENT `/goal` condition (broad Phase 5q.F — resumes AFTER L2 closes; superseded for now by the ACTIVE FOCUS L2 condition at the top)

```
GOAL: Close out fully-unconditional Phase 5q.F — derive `Ω₄^{Pin⁺} ≅ ℤ/16` on the FULL genuine `DataBordismGrp ξ` carrier (real mflds-with-boundary) with `ker(abkGrade)=0`, NO `PinPlusBordismLandmark`/cap-hypothesis/disclosed Prop, by finishing the smooth-4-mfld w₂ tower. DONE already (re-read the INDEX for live state): L1 fundamental class [M] ✅ + L3 Wu w₂=v₂+v₁² / cupH13 / Sq¹ Bockstein ✅ (kernel-pure, both adversarial-review GENUINE). REMAINING: L2 — close the connecting-square `hcore` cross-realization match (the SINGLE residual at lean/SKEFTHawking/SingularConnSquareCloseNC.lean:334; both legs =⟨g∪a',∂z₀⟩ via cap-Leibniz over the shared z₀ — build the hLHS/hRHS V-part realization bridges) ⟹ unconditional subHomConnecting_openDuality ⟹ instantiate PoincareDual4Mid/PoincareDual4Lo for a closed 4-mfld; L4 — w₂ bordism-invariance → floor collapse ker(abkGrade)=⊥ (consume wuW2_eq_zero_iff; feed dataBordism_iso_zmod16_of_complete_grade hinj); L5 — the ≤16 ABK-completeness LAST (geometric Kirby–Taylor generation the target; decidable col4_height_eq_four is the pre-authorized goal-permitted terminal input ONLY on a kernel-checked no-go, STOP POLICY (a)); then W6–W9 (geometric Smith map / Ω₅≅ℤ/16 / discharge SmithInflow → convergence unconditional). Source-of-truth, re-read after ANY compaction: roadmap SK_EFT_Hawking/docs/roadmaps/Phase5qF_GeometricBordism_Roadmap.md (IN FULL) + the lab notebook via its INDEX entry point Lit-Search/Phase-5qF/LAB_NOTEBOOK_INDEX.md (FRONTIER + checklist + decisions + shard pointers W1–W4; open shards on demand). Each brick: update the INDEX FRONTIER + flip the checklist, append terse detail to the active LAB_NOTEBOOK.md — one source of truth across compacts. Lean MCP-first per the goal-dev skill references/lean-dev.md; as LEAD fan independent sub-chains to persistent slots wt1/wt2/wt3 — Agent(subagent_type="skeft-qa:lean-worker", prompt="SLOT N=…, use mcp__lean-lsp-wtN__*, <brick+DR refs+acceptance>"), reset each slot to main first, merge its worktree-wtN; keep the coupled hcore/cap-Leibniz spine solo. Read the cited Lit-Search/Phase-5qF/*.md DR directly before a proof. Public SK_EFT_Hawking: commit GREEN kernel-pure shards + regenerated autogen; NEVER push; never touch the private repo. SCOPE IS SETTLED — build the next brick THIS turn; a stop-hook firing is a GO signal, never a cue to stop/hold/re-scope/hand back/surface scale. Kernel-purity non-negotiable: axioms exactly {propext, Classical.choice, Quot.sound}; NO new axiom/sorry/native_decide/maxHeartbeats. Mathlib-absence = a brick list; LOE/person-year estimates DISREGARDED. Legitimate stops ONLY: a kernel-checked no-go (build around it; carry ONE scoped disclosed Prop + discharge plan), or a genuine user-only PRODUCT decision (ask once, keep shipping meanwhile). DONE when ALL hold, surfaced in the transcript: (1) the full-carrier Ω₄^{Pin⁺} ≅ ℤ/16 theorem with ker(abkGrade)=0 and NO landmark/cap/disclosed-Prop binder; pin4_abutment/adamsAbutment/posited Omega4PinPlusBordism retired; every flagship kernel-pure (#print axioms / lean_verify showing exactly {propext,Classical.choice,Quot.sound}); (2) lake build SKEFTHawking.ExtractDeps clean and validate.py prints N/N (≥43/43) in the transcript, counts/roadmap/notebook/Inventory synced; (3) a fresh-context skeft-qa:adversarial-reviewer ran in a SEPARATE context with ZERO BLOCKER findings surfaced in the transcript (GENUINE, not thin/renamed). — or stop after 400 turns.
```
