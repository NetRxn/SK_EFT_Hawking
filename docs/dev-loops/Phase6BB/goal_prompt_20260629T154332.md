# 6BB goal prompt — durable source (goal_id 20260629T154332)

- **goal_id:** `20260629T154332`
- **roadmap (source-of-truth):** `docs/roadmaps/Phase6BB_Roadmap.md`
- **notebook INDEX:** `docs/dev-loops/Phase6BB/LAB_NOTEBOOK_INDEX.md`
- **mode:** lean · **role:** solo
- **arm_sha:** `3c8a2f6acffe0df04bcc58ee5b25d5ce5c30dbf0` · **armed_ts:** 1782765812

This file is the crash-recoverable source of the `/goal` condition below (the marker's `goal`
field is the fast-read copy). Re-arm of THIS goal reuses `goal_id 20260629T154332`.

## /goal condition

```
GOAL: Phase 6BB — verified DFT foundations (molecular Hamiltonian → Hohenberg–Kohn → Levy–Lieb) — COMPLETE: all four waves shipped AT FULL STRENGTH, every named theorem proven in-loop (no tracked-deferral of any wave's headline). Source-of-truth: docs/roadmaps/Phase6BB_Roadmap.md + docs/dev-loops/Phase6BB/LAB_NOTEBOOK_INDEX.md — re-read BOTH after any compaction (open shards / roadmap waves on demand); also read docs/dev-loops/PRE_DECISIONS.md + SETTLED_FORKS.md. FIRST_ACTION every fresh turn: run scripts/repo_state_probe.py for the REAL current sorry/build state (never trust a snapshot).

SCOPE IS SETTLED — build the next brick THIS turn. A stop-hook firing is a GO signal, never a cue to stop/hold/re-scope/hand back, and never invoke context-budget/session-depth to defer. Legitimate stops ONLY: a kernel-checked no-go, or a genuine user-only decision (ask once via the guard; keep shipping everything else meanwhile).

FULL-STRENGTH MANDATE — the budget for the WHOLE phase is authorized in THIS loop; correctness over expediency. Each wave's named theorem ships in its strongest correct form IN-LOOP: load-bearing hypotheses DISCHARGED (not parked as conditional / hw-gated), unconditional wherever the physics permits, hypotheses substantive (not vacuous). Kato–Rellich self-adjointness, HK I uniqueness, HK II variational, and Levy–Lieb are PROVEN this loop, not modeled as tracked follow-ups. HK I is a constructive reductio, NOT an axiom. No new project-local axiom without explicit user sign-off (#15) — use a disclosed tracked-Prop. Bar: kernel-pure {propext, Classical.choice, Quot.sound}; zero new sorry/axiom/native_decide; no maxHeartbeats in proof bodies (decompose into `have`); preemptive-strengthening checklist before EACH statement; ruthless post-wave audit.

DONE when ALL hold AND are surfaced in the transcript:
(1) W1 MolecularHamiltonian.lean — N-body Coulomb Hamiltonian + molecularHamiltonian_essSelfAdjoint via Kato–Rellich (hypotheses load-bearing);
(2) W2 HohenbergKohnUniqueness.lean — hohenberg_kohn_uniqueness (potential↦density injective up to a constant) + a falsifier;
(3) W3 HohenbergKohnVariational.lean — hohenberg_kohn_variational (E_v[n]≥E₀, equality iff true density) on the Rayleigh extremal substrate;
(4) W4 LevyLiebFunctional.lean — levyLieb_functional + levyLieb_eq_HK_on_vrep;
each module builds clean (0 sorry, kernel-pure, no new axiom), imported into root SKEFTHawking.lean, full `lake build` + `lake build SKEFTHawking.ExtractDeps` green;
(5) validate.py prints its full ALL CHECKS PASSED (N/N) line in the transcript; counts + Inventory_Index refreshed; D10 §DFT row staged for first-lift;
(6) the fresh-context skeft-qa:adversarial-reviewer ran with ZERO BLOCKER ZERO MAJOR findings surfaced in the transcript (self-audit is insufficient).
```
