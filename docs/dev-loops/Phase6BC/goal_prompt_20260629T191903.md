# Phase 6BC `/goal` — durable source (goal_id 20260629T191903)

Open-System Dynamics (Lindblad / GKSL). Roadmap: `docs/roadmaps/Phase6BC_Roadmap.md`.
Notebook INDEX: `docs/dev-loops/Phase6BC/LAB_NOTEBOOK_INDEX.md`. Bundle target: **D10** (§open-systems).
Crash-recovery source of the `/goal` condition below (reuse THIS `goal_id` on re-arm; mint a new one only for a genuinely new goal).

## The `/goal` condition (verbatim)

GOAL: Phase 6BC — verified open-system dynamics (Lindblad/GKSL master equation) — COMPLETE: all four waves shipped AT FULL STRENGTH, every named theorem proven in-loop (no tracked-deferral of any wave's headline). Source-of-truth: docs/roadmaps/Phase6BC_Roadmap.md + docs/dev-loops/Phase6BC/LAB_NOTEBOOK_INDEX.md — re-read BOTH after any compaction (open shards / roadmap waves on demand); also read docs/dev-loops/PRE_DECISIONS.md + SETTLED_FORKS.md. FIRST_ACTION every fresh turn: run scripts/repo_state_probe.py for the REAL current sorry/build state (never trust a snapshot). SCOPE IS SETTLED — build the next brick THIS turn. A stop-hook firing is a GO signal, never a cue to stop/hold/re-scope/hand back, and never invoke context-budget/session-depth to defer. Legitimate stops ONLY: a kernel-checked no-go, or a genuine user-only decision (ask once via the guard; keep shipping everything else meanwhile). FULL-STRENGTH MANDATE — the budget for the WHOLE phase is authorized in THIS loop; correctness over expediency. Each wave's named theorem ships in its strongest correct form IN-LOOP: load-bearing hypotheses substantive (not vacuous), unconditional wherever the physics permits. SUBSTRATE (durable, verified 2026-06-29): reuse PhysLib QuantumInfo CPTP/Choi/Kraus (CPTPMap, choi_PSD_of_CPTP, CPTP_of_choi_PSD_Tr, choi_equiv, of_kraus) + HermitianMat CFC for Hermitian H/rho/Gibbs parts; consume the existing project IsLindbladDetailedBalance toehold (don't re-derive). The Markovian semigroup e^{tL} is the exponential of the NON-Hermitian Liouvillian -> Mathlib Matrix.exp, NOT HermitianMat.exp. Bundle target D10 (open-systems). No new project-local axiom without explicit user sign-off (#15) — use a disclosed tracked-Prop with a discharge note. Bar: kernel-pure {propext, Classical.choice, Quot.sound}; zero new sorry/axiom/native_decide; no maxHeartbeats in proof bodies (decompose into `have`); preemptive-strengthening checklist (drop-conjunct P2 · norm_num numerical content · cross-module bridge P6 · trivial-discharge P3/P4/P5 · defining-the-conclusion) before EACH statement; ruthless post-wave audit. DONE when ALL hold AND are surfaced in the transcript: (1) W1 LindbladGenerator.lean — GKSL generator L(rho)=-i[H,rho]+Sum_k(L_k rho L_k†-½{L_k†L_k,rho}) defined + lindblad_generator_CP (complete positivity via PhysLib Choi) proven; (2) W2 GKSLStructure.lean — gksl_trace_preserving (Tr L(rho)=0) + gksl_canonical_form (Hamiltonian/dissipator split) proven; (3) W3 LindbladSemigroup.lean — lindblad_semigroup (Lambda_t∘Lambda_s=Lambda_{t+s} via Mathlib Matrix.exp) + traceDist_lindblad_monotone (data-processing) proven; (4) W4 DampedTwoLevel.lean — dampedTwoLevel_decay_envelope with a norm_num-backed bound + a two-layer-honesty note in the module header; each module builds clean (0 sorry, kernel-pure, no new axiom), imported into root SKEFTHawking.lean, full `lake build` + `lake build SKEFTHawking.ExtractDeps` green; (5) validate.py prints its full ALL CHECKS PASSED (N/N) line in the transcript; counts + Inventory_Index refreshed; D10 open-systems row staged for first-lift; (6) the fresh-context skeft-qa:adversarial-reviewer ran with ZERO BLOCKER ZERO MAJOR findings surfaced in the transcript (self-audit is insufficient).
