# Goal prompt — Phase 6BA Waves 2 & 3 (goal_id 20260629T131227)

Durable, crash-recoverable source of the `/goal` condition (spec A.5). The marker's `goal`
field is the fast-read copy; this file is the source. Re-arm of THIS goal reuses
`goal_id = 20260629T131227`.

- **role:** solo
- **mode:** lean
- **roadmap_path:** docs/roadmaps/Phase6BA_Roadmap.md
- **notebook (INDEX):** docs/dev-loops/Phase6BA/LAB_NOTEBOOK_INDEX.md
- **W1 status:** COMPLETE — lean/SKEFTHawking/NEGFGreenFunction.lean (24 decls, kernel-pure,
  imported into root). Substrate decision (SETTLED): finite-dim `Matrix m m ℂ`, NOT PhysLib
  `LinearPMap.resolvent`.

## /goal condition

```
GOAL: Complete Phase 6BA Waves 2 & 3 — the Landauer–Büttiker conductance (W2) and the certified conductance-quantization bound (W3) — building on the COMPLETE W1 NEGF substrate (lean/SKEFTHawking/NEGFGreenFunction.lean: gRetarded/gAdvanced/spectralFn/broadening, A⪰0, sum rule, A=G^R Γ G^A). Source-of-truth: docs/roadmaps/Phase6BA_Roadmap.md + docs/dev-loops/Phase6BA/LAB_NOTEBOOK_INDEX.md — re-read the roadmap + notebook INDEX after any compaction (open shards on demand); consult docs/dev-loops/SETTLED_FORKS.md + PRE_DECISIONS.md. Substrate is finite-dim Matrix m m ℂ (NOT PhysLib LinearPMap.resolvent — SETTLED in W1). For in-loop dev invoke /skeft-qa:goal-dev.

SCOPE IS SETTLED — build the next brick THIS turn; a stop-hook firing is a GO signal, never a cue to stop/hold/re-scope/hand back, and never invoke context-budget/session-depth to defer. Legitimate stops ONLY: a kernel-checked no-go, or a genuine user-only decision (ask once via the guard; keep shipping everything else meanwhile). Bar: kernel-pure {propext, Classical.choice, Quot.sound}; zero new sorry/axiom/native_decide; no maxHeartbeats in proof bodies; preemptive-strengthening checklist before each theorem statement.

DONE when ALL hold, each surfaced in the transcript:
(1) W2 — lean/SKEFTHawking/LandauerConductance.lean builds clean (0 sorry, kernel-pure, no new project-local axiom): transmission T(E)=Tr[Γ_L G^R Γ_R G^A] + landauer_conductance_def (G=(2e²/h)∫T(E)(−∂f/∂E)dE) + the linear-response limit, all proven substantive;
(2) W3 — lean/SKEFTHawking/NEGFTransportCertificate.lean builds clean: conductance_quantization (G=n·G₀ for n open channels) + the norm_num-backed falsifier (G>n·G₀ ⇒ ⊥), proven;
(3) both new modules imported into root SKEFTHawking.lean; `lake build` and `lake build SKEFTHawking.ExtractDeps` both reported clean in the transcript;
(4) `uv run python scripts/validate.py` prints ALL CHECKS PASSED (N/N) in the transcript; counts.json + Inventory refreshed (update_counts.py); roadmap W2/W3 status updated;
(5) the fresh-context skeft-qa:adversarial-reviewer ran with ZERO BLOCKER findings surfaced in the transcript (drive per wave via /skeft-qa:wave-close).
```
