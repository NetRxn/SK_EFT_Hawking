# Goal prompt — Phase 5q.H, finish the 16-convergence (goal_id `20260704T145819`)

**Role:** lead (delegation/parallelization). **Mode:** lean. **Armed:** 2026-07-04.
**Entry point:** `docs/dev-loops/Phase5qH/PHASE5QH_EXECUTION_MAP.md`. **Strategic tracker:** `docs/roadmaps/Phase5qH_LiteratureGradeUnconditional_Roadmap.md`. **Master loop notebook:** `docs/dev-loops/Phase5qH/LAB_NOTEBOOK_INDEX.md`.

## /goal condition (durable source; the string passed to `/goal`)

```
GOAL: finish the 16-convergence — land the GENUINE unconditional Ω₄^{Pin⁺}≅ℤ/16: PinPlusGMWitness.omega4PinPlusGM_equiv_zmod16 : DataBordismGrp (pinPlusGMData) ≃+ ZMod 16 on the genuine GM carrier (computed invariant, injective+surjective, ZERO posits), kernel-pure. Option A / full L4, NOT the disclosed-form L3.
SOURCE OF TRUTH (read every session/compaction, in order): (1) docs/dev-loops/Phase5qH/PHASE5QH_EXECUTION_MAP.md — THE entry point: the E1–E5 DAG, the Naming/Rosetta cross-walk, the verified-source index, the worktree pointers, the parallelization plan; (2) docs/dev-loops/Phase5qH/LAB_NOTEBOOK_INDEX.md — the master loop FRONTIER (live state) → then your active effort's E<n>/LAB_NOTEBOOK_INDEX.md; (3) docs/roadmaps/Phase5qH_LiteratureGradeUnconditional_Roadmap.md — strategic tracker (Status checklist = present-state checkoffs); (4) temporary/working-docs/16Convergence_Reconciliation_Audit_2026-07-04.md (coherence source, wins on conflict). Read each effort's verified PRIMARY source DIRECTLY, never a summary. FIRST_ACTION each turn: scripts/repo_state_probe.py; consult BOTH atlas fronts (/skeft-qa:frontier) + SETTLED_FORKS.md before any impossibility reasoning.
NAMING (canonical — do not drift): track work by E1–E5 ONLY (Rosetta in the execution map). L1–L4 = result-strength diagnostic; H1–H8 / P-waves = historical. E1 Substrate-G topology (foundation) → E2 Rokhlin gm + E3 geometric Smith map/exactness (both build on E1); E4 genuine carrier pinPlusGMData + computed abkGM8 + capstone; E5 Substrate-S spectral (independent). Substrate G = E1+E2+E3, S = E5, E4 = assembly.
DELEGATION / PARALLELIZATION (LEAD loop): the lead builds E1 on main (E1's foundation includes harvesting the wt1 PD substrate — exec-map §4; NEVER /reset-slot 1 before harvesting). Fan out with skeft-qa:lean-worker on reset slots (≤2 concurrent + lead on main; feedback-parallel-slot-concurrency-enfile): E5 is INDEPENDENT → run it in parallel from day 1; E2 + E3 once E1 provides the shared primitives. Name the relevant negative-frontier no-go in each worker's brief. E4 assembly is lead-work once E2+E3 land; E4⊕E5 = the unconditional capstone.
SETTLED (do not re-litigate): both substrates are from-scratch/Mathlib-absent (verified); the ℤ/16 completeness smith_inflow_z16 is REQUIRED + non-elementary (E5); NO-GOs lattice_arf_bridge_refuted + synthetic-grade-ker-bot (free-grade ker=⊥) are kernel-checked + machine-fed (negative frontier) — NEVER re-derive. All external inputs are direct-primary-read verified; verify any NEW scout finding at the primary before folding.
KERNEL-PURITY non-negotiable: axioms exactly {propext, Classical.choice, Quot.sound}; NO new project-local axiom without explicit user sign-off; no sorry/native_decide/maxHeartbeats in proof bodies; a hard geometric input is ONE disclosed tracked Prop (KERNEL_NOGO_REGISTRY/HYPOTHESIS_REGISTRY + discharge plan), never an axiom. Land the MAXIMUM coherent GREEN increment each turn (don't atomize); commit GREEN kernel-pure increments (~5–10 bricks) + update the effort notebook each brick; NEVER push. No PM/LOE estimates. A stop-hook firing is a GO signal — do the next increment; legitimate stops ONLY = a kernel-checked no-go or a genuine user-only decision. Invoke /skeft-qa:goal-dev for the in-loop dev references.
DONE when: (1) omega4PinPlusGM_equiv_zmod16 exists on the genuine GM carrier, injective+surjective, computed invariant, ZERO posits (smith_inflow_z16 discharged by E5), kernel-pure (lean_verify → the 3 std axioms); (2) rm -rf .lake/build && lake build SKEFTHawking.ExtractDeps exits 0 AND validate.py prints N/N in the transcript; (3) a fresh-context skeft-qa:adversarial-reviewer ran with ZERO BLOCKER findings surfaced in the transcript.
```
