# /goal condition — Phase 5q.H full 16-convergence, arm 2 (goal_id 20260712T150610)

Durable, crash-recoverable source of the `/goal` condition (spec A.5). Route-AGNOSTIC by explicit user
instruction: this file never pins a carrier, wall (W1/W2), or route — those are live tactical state
(atlas FRONTIER + `LAB_NOTEBOOK_INDEX.md` FRONTIER + `SETTLED_FORKS.md`). Supersedes
`goal_prompt_20260706T163405.md` (same DONE conditions; adds the three guards learned from the
2026-07-06→12 run: worktree reconcile, notebook discipline, WIP-scaffold policy).

## The condition (what `/goal` is set with)

```
GOAL: Finish the genuine, fully UNCONDITIONAL 16-convergence — Ω₄^{Pin⁺} ≅ ℤ/16 formalized in Lean 4 on a FAITHFUL carrier (ℤ/16 genuinely COMPUTED as the invariant, injective + surjective, ZERO posits), kernel-pure {propext, Classical.choice, Quot.sound}, with EVERY remaining completeness/PD/orientation/Rokhlin input discharged — no project-local axiom, no sorry, no native_decide, no maxHeartbeats. Path- and carrier-AGNOSTIC: which faithful carrier and which route (KT-geometric vs spectral, cover-induction vs other) are LIVE tactical state — the loop picks the highest-leverage open node from the atlas FRONTIER + notebook, and this condition NEVER pins one. Source-of-truth: docs/roadmaps/Phase5qH_LiteratureGradeUnconditional_Roadmap.md + docs/dev-loops/Phase5qH/LAB_NOTEBOOK_INDEX.md (re-read the roadmap + notebook INDEX after any compaction — its FRONTIER + DECISIONS blocks are the re-grounding read; open shards + E1–E5 sub-notebooks + SETTLED_FORKS.md on demand). FIRST_ACTION each fresh context: (1) run scripts/repo_state_probe.py + /skeft-qa:frontier for the REAL current state (never trust a snapshot); (2) RECONCILE the worktree slots — `git cherry -v main worktree-wtN` for N=1,2,3 — and either harvest genuinely-unmerged slot work or record the verified verdict in exec-map §4 BEFORE building (⛔ wt1's 7-commit sourceIso chain is a VERIFIED FULL DUPLICATE of main's 2aa2f5f0 — never re-dispatch that harvest). NOTEBOOK DISCIPLINE (binding, the 07-06→12 run's decay mode): every brick gets its own `### ` entry in the effort's ACTIVE LAB_NOTEBOOK.md shard the turn it lands (dates from `git log` — NEVER future-stamp); the INDEX stays glanceable (superseded framings roll to HISTORY/detail shards the same turn via /skeft-qa:notebook); any untracked WIP .lean scaffold is LOGGED in the notebook the turn it is created, lives under lean/SKEFTHawking/ (never the lean/ root), and is deleted or promoted when superseded. SCOPE IS SETTLED — land the MAXIMUM coherent GREEN increment THIS turn (as much as you can finish cleanly; do NOT atomize into one tiny brick — per-turn throughput matters; commit every ~5–10 green kernel-pure bricks, updating the notebook each brick). A stop-hook firing is a GO signal, never a cue to stop/hold/re-scope/hand back, and NEVER invoke context-budget/session-depth to defer (auto-compaction is handled). Never re-derive a settled-dead fork (negative frontier / docs/dev-loops/SETTLED_FORKS.md; binding no-gos incl. genuine-gm-carrier-eight-torsion, nogo_lattice_arf_not_sigma8, mfd-equals-H1-dead-end, synthetic-grade-ker-bot-nogo; the mod-2 field-Kronecker/class-lift routes are FALSE over ℤ). Legitimate stops ONLY: a kernel-checked no-go (build around it; carry one scoped disclosed Prop + discharge plan), or a genuine user-only decision (ask once, keep shipping meanwhile). NEVER ship a new project-local axiom without explicit user sign-off (DR recs advisory). DONE when: (1) the unconditional Ω₄^{Pin⁺} ≅ ℤ/16 equivalence on a faithful carrier is a kernel-pure theorem with ZERO posits/axioms/sorries — evidenced by lean_verify reporting exactly {propext, Classical.choice, Quot.sound} surfaced in the transcript AND `rm -rf .lake/build && lake build SKEFTHawking.ExtractDeps` exit 0 in the transcript; (2) `uv run python scripts/validate.py` prints N/N (all checks pass) in the transcript; (3) the fresh-context skeft-qa:adversarial-reviewer ran with ZERO BLOCKER findings surfaced in the transcript.
```

## Notes
- **End state = the genuine unconditional `Ω₄^{Pin⁺} ≅ ℤ/16`** (the "full 16 convergence"), ZERO posits.
  Arm-time live state (NOT pinned): **`hcoreG` is DISCHARGED** (2026-07-12, `1bdfda73`, kernel-pure —
  both seam-matches; the bot PD–MV square anti-commutes over ℤ, the `(−1)`-twist is threaded through
  HcoreG/`hcore₀`/bot-five-lemma). The assembled σ÷16 leg (`sixteen_dvd_latticeSig_of_hcoreGInt`) now
  rests on orientation (`hcyc`/`hloc`) + finite data (`kron`/basis/spin-Wu/`htopo`=E2 Rokhlin) only.
  Atlas keystone at arm time: `smith_inflow_z16` ranks #1 by wiring, but the KT geometric route
  (`Ω₄^{Spin}≅ℤ`, re-anchored 2026-07-06) is the live keystone; both bottom out at the W1 manifold
  topology, so the σ÷16/finite-data work is on the critical path either way.
- Arm sha `1237ad3b` (main, armed 2026-07-12); mode `lean`; solo lead + ≤2 lean-worker slots (wt2/wt3; wt1 free after reset).
- New-in-arm-2 guards: FIRST_ACTION slot-reconcile; notebook discipline (per-brick ACTIVE-shard entries,
  git-log dates, same-turn INDEX rolls); WIP-scaffold logging/placement policy.
