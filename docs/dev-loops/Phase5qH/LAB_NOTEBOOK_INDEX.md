# Phase 5q.H — MASTER loop notebook INDEX (live state — THE single live execution-state doc)

> **🛑 SOUNDNESS FINDING (2026-07-13, kernel-confirmed; now fully contained + re-planned).** The T2-less
> `Bordism` relation collapses ⇒ the `DataBordismGrp` carrier is NON-FAITHFUL (no-go
> `nonhausdorff-bordism-collapse`). The strategic re-base LANDED 2026-07-13 (operator planning session) —
> see the FRONTIER below + the rewritten durable roadmap. SURVIVES: E1 integral topology, K3 lattice,
> Rokhlin/Brown/ABK algebra, Smith tower, D³/S²×D³ atlas. Detail = `LAB_NOTEBOOK.md` "★★★ SOUNDNESS FINDING".

> **Read order each session/compaction:** (1) THIS FRONTIER (live loop state — route/carrier/keystone
> selection lives HERE, never in durable docs); (2) the durable roadmap
> `docs/roadmaps/Phase5qH_LiteratureGradeUnconditional_Roadmap.md` (objective, constraints, substrate verdict,
> wave breakdown, gates — 2026-07-13 durable-only rewrite); (3) `/skeft-qa:frontier` (atlas, both fronts).
> ⛔ `PHASE5QH_EXECUTION_MAP.md` is RETIRED/FROZEN (2026-07-13) — historical only (E-naming cross-walk).

## ⛔ Posture (binding — every turn)
Option A: genuine unconditional `Ω₄^{Pin⁺}≅ℤ/16` on the FAITHFUL carrier (ZERO posits). **Land the MAXIMUM
coherent GREEN increment each turn** (don't atomize). Kernel-purity `{propext, Classical.choice, Quot.sound}`;
NO new sorry/axiom/native_decide/maxHeartbeats. **Never re-derive a settled-dead fork** (negative frontier /
SETTLED_FORKS). Legitimate stops = kernel no-go OR user-only decision. **Naming: track by W-A…W-F** (roadmap §5
waves); E1–E5 / L / H / P = historical (cross-walk in the retired exec map). **Vacuity-attack gate (roadmap §3):
every new completeness/bounding Prop must survive a zero-geometric-input discharge attempt before consumption.**

## 🎯 Objective
**Faithful full-strength discharge of the 16-convergence** — the genuine unconditional `Ω₄^{Pin⁺} ≅ ℤ/16`
on a **faithful carrier** (ℤ/16 genuinely *computed* as the invariant, injective + surjective, **ZERO posits**),
kernel-pure `{propext, Classical.choice, Quot.sound}`, with the completeness input discharged **on a
genuinely FAITHFUL carrier** (the honest T2 + smooth relation — see the top banner + PRESENT-STATE FRONTIER).
**Path- and carrier-agnostic:** which faithful carrier and which completeness formulation are LIVE tactical
state (notebook FRONTIER + atlas frontier), **never pinned here** — do NOT re-inherit `smith_inflow_z16` /
the σ-route / any `DataBordismGrp` framing as "the keystone" (that layer is the vacated non-faithful one).
- ⛔ **Do NOT target the thin `pinPlusGMData` carrier** — it is kernel-proven **8-torsion**
  (`pinPlusGMData_not_equiv_zmod16`; no-go `genuine-gm-carrier-eight-torsion`), so `… ≃+ ZMod 16` on it is
  *uninhabitable*. The faithful ℤ/16 lives on an **enriched (σ,F•F) / Smith-LES extension carrier**. Never
  re-seed the thin literal as the target (this is the decoherence bug that cost the 2026-07-05 run).

## ✅ Deliverables checklist (W-canonical — roadmap §5)
- [ ] **W-A · the faithful instance** — genuine Pin⁺/GM structure type + structure-extension `Bor` over the T2
  relation (genuine `revStr`); definition gated by adversarial review + vacuity attack BEFORE building on it;
  absorbs the vacated-brick restatement (containment)
- [ ] **W-B · smooth witnesses + surjectivity** — ℝP⁴ smooth (`k≥1`) atlas + structure; surjectivity of the
  computed invariant on the honest carrier
- [ ] **W-C · computed invariant + bordism invariance** — ABK/Brown computed from the structure; invariance
  along structured T2 bordisms; additivity
- [ ] **W-D · completeness (the summit)** — `invariant = 0 ⟹ bounds` on the faithful carrier (route: live
  selection below)
- [ ] **W-E · deep-input discharge** — Rokhlin-content (`hyp:rokhlin_sigma_mod_16`) + the `Ω₄^{Spin}≅ℤ`-class
  input of the selected route
- [ ] **W-F · capstone + integration** — the `k=∞` literature-grade statement; Ω₅/`sixteen_convergence_*`
  recast; bundle freshness pass
- [ ] **Gate** `rm -rf .lake/build && lake build SKEFTHawking.ExtractDeps` exit 0 + `validate.py` N/N + fresh
  `skeft-qa:adversarial-reviewer` 0-BLOCKER + vacuity attacks passed


## ▶ Frontier (PRESENT STATE — **goal 20260716T073016 LIVE (the CLOSE-OUT arm)**)

**═══ 2026-07-21 late — wt3 REGULARITY LIFT MERGED (lead-reviewed; RATIFIED, headline HOLDS) ═══**
- **THE ASSEMBLY OF RECORD IS NOW GENUINELY SMOOTH.** Build 10272 clean, no-go integrity green.
  The `k = 0` (C⁰) category problem — roadmap §2 leg 2, the phase's single largest soundness risk —
  is **resolved by route (b): re-declaration at `k = ⊤`**, NOT by a transport (that stays refuted,
  `k0_to_k1_transport_refuted`). `residualProvK (k)` is the provider at every regularity; the
  headline `kt_equiv_zmod16_of_residuals_smooth` concludes
  `Nonempty (T2DataBordismGrp (pinPlusCharPairData (residualProvK ⊤)) ≃+ ZMod 16)`.
- **✅ CRUX VERIFIED BY THE LEAD (not taken on report):** the `k = ⊤` carrier genuinely quantifies
  over SMOOTH manifolds. `example (p : StrMfd …(residualProvK ⊤)…) : IsManifold (𝓡 4) ⊤ p.1.M :=
  inferInstance` **typechecks**, and the control at `residualProvK 0` **fails instance synthesis** for
  the same `⊤` goal — so `⊤` is a real constraint and this is the smooth category, not a relabeled
  free one. This is the whole point of the lift, and it landed.
- **✅ NOT A SHELL — unconditional non-vacuous content at `⊤`:** `charPairBrown_surjective_smooth :
  Function.Surjective (charPairBrown (residualProvK ⊤))` — codomain confirmed `ZMod 8`, axiom-pure, NO
  hypotheses. The smooth carrier already realizes all 8 classes off the `Cω` ℝP⁴ witness, and
  `rp4_ne_zero_smooth` (also unconditional) pins it nonzero.
- **✅ NOTHING WEAKENED:** the `⊤` headline carries the SAME eight open binders as the `k = 0` one
  (H, row, RealizesSphereProducts, SphereProductBounds, RankZeroCollapsesToEmptySurf, KerPhiSubDoubles,
  SpinImageCyclic, k₀-2-torsion); the generic KRS leaf is *weaker* than the old `k=0` `KRSResidualRow`.
  Every `k = 0` statement is kept verbatim as a corollary (`residualProv = residualProvK 0`,
  `rp4CharPairK 0 = rp4CharPair := rfl` conservativity cert). 45 files → 12 still mention the `k=0`
  provider (2 only because the `k=0` theorems of record were preserved).
- **THE REFRAMING THAT MATTERS:** the smooth-category requirement went from "everywhere, silently free
  at k=0" to **one named, Mathlib-absent brick: a smooth handle attachment for the surgery trace.**
  Root cause traced in source: `SmoothSurgeryChartDatum.ofC0` sets `k:=0`;
  `ambientTraceBordism_concrete:199` builds `he_smooth` as `contMDiff_zero_iff.mpr he_cont` — the
  boundary embedding's "smoothness" is literally its continuity. Everything typed on it
  (`ktHandleAttachment`, `SeamCollarDatum`, `SurgeredEndDatum`, `capstoneB`, `KRSResidualRow` → the H
  leaf's provider) is genuinely C⁰. **So H, when discharged, needs the smooth handle attachment —
  which is on H's critical path regardless.** Two smaller C⁰-tied items: `PinPlusCharPairTetherGate`,
  and the `hA`/`hB` supplier group (generalizes but hit a deterministic `isDefEq` heartbeat wall at
  `PinPlusKTSphereProdCohomology:167` — reverted rather than add `maxHeartbeats`; statable at any `k`
  as hypotheses). 4 friction patterns logged for the catalog (namespace-scope `variable {k}`,
  PinPlusCertK eager-synth, `_`-placeholder inference loss, the `ModelProd` isDefEq blowup).
- **Fence `5qH-capstone-regularity-level-k0-vs-k1-unbridged` → RESOLVED BY ROUTE (b)** in
  SETTLED_FORKS + the `PinPlusRegularityFence.lean` header; its "no separating witness yet" bullet is
  retired (`exists_carrier_element_not_smooth` landed earlier). No new no-go; no transport attempted.

**═══ 2026-07-21 late — wt1 #212 COLLAR-PAIR MERGED (lead-reviewed; headline CORRECTED) ═══**
- **VERIFIED AND RATIFIED — the row went 4 → 3.** `PinPlusTraceCapstoneCollarPairMatch.lean`, build
  10272 clean. BOTH `CollarPairGeomEnd.toSeamRow` AND `CollarPairBuild.toSeamRow` exist, so the
  4-obligation end row *and* the original frozen eight-field build both factor through
  `CollarPairSeamRow` = {`hseamMatch`, `hseamHit`, `hq0det`}. The collapse is not an artifact of the
  canonical core. Both ↔ certificates (`qZero_boundary_mem_iff_seamMatch_mem`,
  `qGen_boundary_mem_iff_seamMatchGen_mem`) and the vacuity-attack theorem are axiom-pure.
  `hseamHit` IS retained in `CollarPairSeamRow` (I printed the fields), so the seam row is **not**
  degenerately inhabitable by the `seamCore = ∅` configuration the attack exhibits.
- **✅ HEADLINE CONFIRMED — the row IS at TWO obligations, and the whole chain reaches `hasClass`.**
  (I first recorded this as unsupported; that was MY error, retracted below.) Reading the module:
  `CollarPairSeamRow.toHasClass` (:336) and `CollarPairCoreRow.toHasClass` (:609) BOTH produce the
  exact `CapstoneAmbientSupply.hasClass` field type — the latter by firing
  `capstone_hasClass_ofCoreChains` directly, no corrector/split/bridge/partition. Chain:
  **`CollarPairGeomEnd`(4) → `SeamRow`(3) → `CoreRow`(2) → `hasClass`**, with
  `CollarPairSeamRow.toCoreRow` (:~640) the 3 → 2 production.
- **Why "two" is right despite SEVEN structure fields:** `CollarPairCoreRow` = `z`, `hz`, `cHa`
  (data) + `hcHa`, `hdetHa`, `hbd`, `hdetAB`. Along `toCoreRow` at the canonical `cHa :=
  diskDetectChain`, `hcHa`/`hdetHa` are discharged by the BANKED `diskDetectChain_hc`/`_hdet`, so the
  genuine remaining obligations are exactly `hbd` + `hdetAB`. `cHa` free = pick a non-canonical disk
  chain and you owe those two yourself. The report's table was accurate.
- **The anti-fake guard is intact:** `CollarPairCoreRow.qGen_ne_zero_of_seamCore_nonempty` exists and
  derives nonzero-ness *from the two obligations* whenever `seamCore` is nonempty — a consequence,
  not an added assumption. The vacuity attack's target (`seamMatch` free at `seamCore = ∅`) is not in
  `CoreRow`'s obligation set at all, so the attack does not reach the terminal row; carrying
  `hseamHit` when inhabiting remains prudent, not required.
- **✅ CLOSED (was left open by me, now traced): the shared-`cCore` co-adaptation is NOT on the path
  to `hasClass`.** `CollarPairGeomEnd.toHasClass` was the previous live route; the new chain reaches
  the identical conclusion type via SeamRow/CoreRow, and no link mentions the co-adaptation. The
  codex dossier's hardest item (450–800 LOC) **should not be built**. Filed to `SETTLED_FORKS.md`.
- **⛔ MY OWN ERROR, RECORDED (lead, 2026-07-21):** I first reported CoreRow as "an orphan, referenced
  by nothing" and its guard as "does not resolve", on the strength of (a) `lean_local_search
  "CollarPairCoreRow"` returning only the structure — it does **not** surface namespaced members —
  and (b) a `grep` that excluded the defining file, which is exactly where the in-module consumers
  live. **Both were absence-of-evidence read as evidence-of-absence.** `lean_local_search` returning
  one hit is NOT a reference check; use `lean_references`, or read the module. This is the same class
  as the Paper-10 error earlier today, in the opposite direction (over-harsh instead of over-credulous).

**═══ 2026-07-21 late — wt2 K10 SPAN 1–4 MERGED (lead-reviewed) ═══**
- **The K3→row bridge opened** (`IntOrientationMod2Lift.lean`, `IntersectionMatrixBasisChange.lean`,
  `KummerK3E1Package.lean`; build 10271 clean, all headliners `#print axioms` pure). The `orient` atom
  collapses to ONE homological input: `intOrientation_of_h3_twoTorsionFree`. `kummerK3IntH2Basis` +
  `rank = 22` (`rfl`, honest — a `Basis (Fin 22)` cannot be fabricated). Ledger
  `kummerK3E1Atoms_of_residuals : KummerK3E1Residuals → Nonempty KummerK3E1Atoms` with exactly THREE
  residuals: H₁(K3) free · H₃(K3) 2-torsion-free · the `IntPoincareDuality` datum.
- **TWO PREMISE CORRECTIONS the worker made, both validated:** (1) `IntOrientation`'s docstring calls
  its datum a "community-scale residual" needing an integral local-homology tower — **it does not**;
  the structure asks only for `c : H₄(M;ℤ)` with `red₄ c = [M]₂`. My brief repeated the docstring.
  (2) **`SpinSigmaAtomPkg.B` is a basis of `Cohomology X 2` while the whole Kummer arc computes
  `Homology`** — two bridges needed (UCT for the basis, cap-duality for the Gram); both built.
- **⚠ LEAD REVIEW FINDING (framing, not a defect):** `nonempty_intOrientation_iff_mem_range` is
  reported as "the equivalence certificate — the sufficient form over-claims nothing." **It is not
  that.** Its printed proof destructures the `IntOrientation` structure and reassembles it: the iff is
  a definitional eta-unfold of the structure's own two fields, TRUE and useful for *naming* the exact
  obligation (it is what validates premise correction (1)), but it says nothing about the H₃ criterion.
  **`intOrientation_of_h3_twoTorsionFree` remains strictly SUFFICIENT with no necessity companion** —
  do not cite it as sharp.
- **✅ verified, not taken on report:** the `[Module.Free ℤ (Homology KummerK3top 1)]` binder on
  `kummerK3IntH2Basis` is genuinely load-bearing — no global instance exists (synthesis fails on a bare
  `example`), so `KummerK3E1Residuals.h1Free` is consumed, not a decorative conjunct.
- **`interMatrix_intCongr_of_rank_eq` is real content** (any two `IntH2Basis` of equal rank have
  `IntCongr` Grams, via `gram_intCongr_of_bases`) ⟹ **the rank-22 basis being an arbitrary
  `Classical.choice` costs nothing**, because the row asks for `IntCongr`, not a Gram equality. Same
  payoff as the `SphereProdGramPin` retirement ruling.
- **⏳ DEFERRED, NOT FILED (correctly left to the lead by the worker):** the observation that the 16
  exceptional + 6 descended classes span a PROPER sublattice (cokernel `(ℤ/2)¹⁶`), so any "tabulate the
  16+6 block and stop" Gram plan fails on determinant grounds. **I have NOT traced this chain**, and the
  worker's citation misattributes `pairCokerEquiv`/`pairCoker_card` to `KummerPairHalving` (they live in
  `KummerPairTubeSeparation`). Recorded here as an untraced lead-observation; **not** filed to
  SETTLED_FORKS or the registry until traced — filing a binding fence on an unverified chain is the
  error class caught earlier today.

**═══ 2026-07-21 (detail → `LAB_NOTEBOOK.md` §"the K7/weld/S²×S² round"; pointers only here) ═══**
- **S²×S² lane CLOSED, unconditional.** `hcross_pm` (E–Z cross value is a unit) → `sphereProd_interMatrix_intCongr_hyp` : `II(S²×S²) ≅ Hyp` **sharp, no hypotheses**. `SphereProdGramPin` RETIRED — its residue is kernel-characterized as *choice-dependent* (`sphereProdGramPin_iff`), encoded in `KERNEL_NOGO_REGISTRY` + `SETTLED_FORKS` as the general rule: **state Gram targets as `IntCongr`, never a literal matrix equality on a chosen basis.**
- **ℝP³ weld seam SMOOTH, global.** `contMDiff_bdryMapRP3`; the equator fell to `annulusTrivFun_bdryMap` (both hemisphere branches are the *same* map through the annulus trivialization). Weld topology done: all three chart families open + covering (`isOpen_cover_three_families`).
- **K7 → ONE FACT.** `TubeParity` bypassed; residual now **16 existence witnesses** — `pairH2TwoTorsionFree_iff_exceptional_halvable`. Remaining: `H₂(ResE,∂ResE;ℤ) ≅ ℤ`. LES provably cannot finish it; live route = local-homology MV over the two base hemispheres.
- **⚠ BLOCKER (wt2, decided):** both piece atlases are on `(𝓡 3).prod (𝓡∂ 1)`; `StrMfd` needs `[I.Boundaryless]` and the consumer is at `𝓡 4`. **DECISION = route (b)**: build on `(𝓡 3).prod 𝓘(ℝ,ℝ)`, transport ONCE at the end (`reshapeModel` pattern). Transport lemma is being built FIRST so the risk surfaces early.
- **⚠ THREE lead-brief route errors this session** (all worker- or lead-corrected): the "canonical circle generator" chase (no such generator — `circleH1EquivInt` ends in an arbitrary `Nonempty` choice); the "RP3E is not a smoothness source" fence (a worker gave it one); and **route (c) `openDuality_univ_bij_of_hcoreGInt` is INAPPLICABLE, not "gated"** — it needs a *closed* 4-manifold, `ResE` has boundary. → standing rule: ground a brief's load-bearing route claim in the Lean before dispatch.
- **STANDING ASK, now in every brief:** deviate from the briefed route freely, but supply an **EQUIVALENCE CERTIFICATE** — prove the substituted obligation ⟺ the target, not merely sufficient. Two workers met this bar unprompted.
**═══ PRESENT STATE (consolidated 2026-07-20 late; rounds 3–7 chronicle verbatim → HISTORY) ═══**
- **16-conv floor:** **(a) completeness H** — numerics findim×4 GENERIC conditional on {`CollarSplitDatum`, hYAB} (both Lane-H2-by-construction inputs; no concrete `SeamCollarDatum` exists in-tree — named input everywhere); deep residuals = on-W nondeg14/23 · hwf Steenrod–Kronecker pair · hasClass (#212 repair, shape gate running via codex) · Lane H-1 (compression disk; codex dossier running) · Lane H-2 (packaging; supplies csd+hYAB by construction). **(b) K3/Kummer:** K0–K3 ✓ · K4′ core ✓ + K4′′ apparatus ✓ (packaging ⟳wt3) · K5′ ✓ (chart certs ⟳wt3-same-block) · K6′a ✓ ALL legs (BaseS2-ident ⟳wt2) → **K6′b 16-fold weld = NEXT CONVERGENCE** → K7 (feeder ready mod BaseS2-ident) → K8a (Novikov σ) → K8b (adjudication running via codex) → K9/K10. **(c) row-side {`a` total-E1-bundle, hCob, hBase}** — design-gated (lead queue 4: a-elimination + Benedetti handle-trade scoping). **(d) hcolD** — LANE OPENED: B0 Wu-sector split LANDED (`PinPlusKTWuSectorSplit.lean`, `3f9c2e2c`, #print-pure: `pushedSurfClass_eq_zero_iff_wuNull` via hchar + Kronecker faithfulness, + witness routing + the B1-branch handle `wuNull_of_surfClass_eq_zero`); **B4 ALSO LANDED** (`0bfbe86f`, #print-pure): `TauMembraneWeldDatum.ofRankZero` (rank-zero τ ⟹ hq/hlagK self-discharge honestly — subsingleton enhancement; geometric fields = the honest residual) + `TraceMembraneLeaves.ofRankZeroTauMembrane` (the B5/B6 consumer composite) + `rankZero_brown_preserved` (collapse-sector Brown invariance hypothesis-free). **B1 INTERFACE ALSO LANDED** (`bf975035`, #print-pure, codex-dossier option (c) VETTED-then-built): `PinPlusKTRankZeroBounding.lean` — `pinCharSurfaceOfBundled` (τ→PinCharSurface; H₁-basis via `homologyBasisOfCohomologyBasis`; the dossier's `pinCharSurfaceOfBundled` was its ONE invented name — everything else verified real), B1a `RankZeroSurfaceBoundingDatum` (rides frozen `PinCharSurface.Bounding` exact-boundary package `range e = ∂V`; pinCompat = `KernelSpinVanishing`; derived B4 views incl. `chartQ` by definitional `change`), B1b `RankZeroSurfaceWeldAnchor` (HAQ/weld/hQ/glueτ = B4's literal inputs), `toLeaves` composite (zero transport gap), `membraneSpinKill`. **hcolD lane state: B0 ✓ B1-interface ✓ B4 ✓ B5/B6 = thin assembly next; the DEEP fronts = B1-INHABITATION (WuNullCarrier ⟹ embedded bounding Q — Fable) + B2 (TerminalCharacteristicExtensionDatum — Fable wall).** #212 dossier ALSO returned + VETTED (all 6 spot-checked citations real): full `CollarPairBuild` freeze + producer skeleton + hcoreHit anti-fake tether + sizing 1,480–2,650 LOC (hardest = shared cCore/houtPair/canonical-disk hbridge; upstream stop-condition hφtop+hS_sphere needed for totality) — `codex_212_collarpair_design.md` = the #212 build spec. B2 = the Fable wall (task #297). **(e) hker** — unstarted lane.
- **✅ ReblockToK3 PROVED (merged `ccf93a50`): K8b floor = `StableNegRank16` ALONE** (`hk3_of_stable16` single-hypothesis; + the reusable block-permutation congruence toolkit + the hcross Stokes core `kronecker_cup_stokes_leg` with the full engine list signature-verified).
- **★★★ FABLE CASCADE COMPLETE (merged `e84987b2`, ZERO residuals, all #print-pure): THE T⁴° + Q-SIDE CERTIFICATES DONE** — via Fable's OWN third route (the RE-CENTERED arg inverse: tᵢ = arg(yᵢ·cᵢ⁻¹) keeps every ratio within ‖w‖<3/4<π of 1 — always in slitPlane; the §2e branch-cut objection DISSOLVED; no Circle.exp-mfderiv brick needed): the full inverse substrate + (1d-IC) + **`isManifold_puncturedTorus`** (parametric to ω) + **`instChartedSpaceFreeQuotient` + `isManifold_freeQuotient`** (KummerQuotientManifold.lean, 1005 lines — the τ-twisted transition classes + deck-branch local-constancy + the generic qmk-descent lemma). **The weld's Q-side input is COMPLETE.** hcross B2 chain-side also merged (`d8f9551495`): the assembly heart + reconciliation infra; residual = ONE cochain round-trip atom + B1/B3/B4/B5 (exact plan recorded). E-side wrapper + 16/36 pairs merged (`62bdbe97`+).
- **★★★ K6′b THE WELD — TOPOLOGICALLY DONE (merged `66aee296`, #print-pure): `KummerK3` EXISTS** — the 16-fold quotient carrier, T2 (proper-map route) + compact + nonempty, the pieces meet EXACTLY on the seam (`qImage_inter_eImage`; the boundaries welded shut per `seam_eq_qBoundary_image`), the closed-embedding floor. **DESIGN RISK #2 PASSED**: the presentation chain composed cleanly (`scaleToChart` = the ℝ⁴≅ℂ² reindex + ρ-rescale intertwining both −id antipodes; `weldMk_seam` explicit). Smooth K3 ChartedSpace/IsManifold = gated on the E-side certificate (⟳ #288) + the assembly (`qBdryHomeo` surjectivity = the noted next brick).
- **★ K7 OPENER LANDED (merged `ff32580e`, #print-pure):** `KummerK7Opener.lean` — `rp3H0EquivInt` + `seamHomeo : EIndex × RP3 ≃ₜ seam` + `seamHomologyEquivInt` + the conditional MV exactness statements + `K7SeamCoverHyp` (the interior-cover geometric input, gated on wt2's collars) + `kummerK3_b2_target`. ⚠ Friction law: pin `(X := KummerK3top)` explicitly — coercion inference causes isDefEq heartbeat blowup. K7 residuals = the wt3 continuation block below.
- **★ K7 WARM-UP B LANDED (wt3 #292 merged `932e5939`, #print-pure): `finProdHnEquivInt`** (the 16-fold integral disjoint-union homology tool, iterated clopen split) **+ `eIndexProdHnEquivInt`** — composed onto `seamHomologyEquivInt`: **Hₙ(seam) ≅ ⊕₁₆ Hₙ(ℝP³)**. Also banked: `homologyCongrInt` (homeo → integral-homology ≃ₗ — use over inline patterns). ⚠ Friction laws: never annotate `sub someSet` (ambient TopCat can't infer from a bare Set); mark heavy homology-equiv builders `attribute [local irreducible]` (isDefEq heartbeat walls on .trans/.prodCongr). **Warm-up A (puncturedTorus path-connectedness) HONESTLY REFUSED** — chartBall ⊊ metric-ball strictly (shell points defeat the coordinate-slide), the real route = locally-path-connected (interior ℝ⁴-like + boundary half-ball) + connected ⟹ path-connected — a genuine dedicated block, QUEUED (qH0/freeQuotient_pathConnected stay conditional meanwhile). ℝP³ recon: NO integral ℝP²/ℝP³ in-tree (mod-2 ladders only); double-cover transfer = primary (covering data all in KummerResolutionPiece).
- **★ TRANSFER ENGINE CORE LANDED (wt3 #294 merged `e3013ef6`, all #print-pure): `KummerRP3Covering.lean`** (28 decls) — GENERIC norm/diff/tau chain+homology operators for an arbitrary involution (Smith identities D∘N=0/N∘D=0; chain-map facts), the covering relation `projHom_tauHom` (p_*∘τ_*=p_*), the RP3 instantiation + **`negS3_free` (the freeness crux)**. ⚠ TWO HONEST FINDINGS (recorded for the finisher): **no integral SES→LES tool exists in-tree** (SingularPairLES is mod-2 pairs; the norm-sequence LES must be built from scratch); **the SES-(I) middle map is the DIFFERENCE coordinate** (chain-verified — not naive p_*/tr); ONE SES gives only periodicity — BOTH interlocking SESs needed for H_{≥4}=0 termination. No homology group closed in #294 (engine-only, honest).
- **★★★ FABLE ℝP³ BLOCK LANDED (wt3 #295, 7 commits merged `9ca1f6c9`, lead-axchecked #print-pure): H₂(ℝP³;ℤ)=0 + seam H₂=0 (the b₂ seam input DONE) + H₁≅ℤ/2 + generator pin + seam H₁≅(ℤ/2)¹⁶** — via: `rp3_isCoveringMap` · the S³ package (`sphHomeoS3`, `s3H3EquivInt`) · **τ⁎=1 by phase-rotation homotopy (NO degree theory)** · GENERIC `SingularInvolutionSmithInt` (ker D#=im N#/ker N#=im D#, any free involution) · the integral transfer (`p#∘tr=2`, `ker p#=im D#`) · **`ChainComplexLESInt` — the generic SES→LES zig-zag engine (any ring, any levelwise SES; the atlas's flagged missing tool, now BANKED)** · the SES-I/III instantiations + solve. Generic-vs-specific split recorded (LES engine + Smith = verbatim for the Q = T⁴°/τ consumer). ⚙ NEW FRICTION LAWS: the LES engine is ring-generic BY NECESSITY ([Module ℤ] vars → AddCommGroup.toIntModule diamond on submodule subtypes); `lean_verify` false-positives on the literal token "op*que" in docstrings. **HONEST RESIDUAL (Fable-verified: the two LESes alone canNOT terminate — the ℝP^∞ pattern survives both): ONE geometric input H₄=H₅=0** (4-chart lune MV route identified) **→ then k₃=k₄=0 + H₃≅ℤ + the p⁎[S³]=2·[ℝP³] pin close algebraically** (⟳ #296, Opus).
- **🔍 AUDIT #1 DISPOSITIONS (codex 5.6-sol xhigh, `codex_strength_audit_20260721.md`, lead-verified 2026-07-21):** the RP³→K7→Q→puncture arc = STRUCTURALLY SOUND + CARRIER-FAITHFUL (real welded K3, real Q, exact no-go, computed isos — all confirmed-strong). Live findings & owners: **(B1-BLOCKER) separability overclaim** — `Δ≫μ = 𝟙+𝟙` is the FPdim-2 composite, NOT `IsSeparableAlgebra` (requires `Δ≫μ = 𝟙`, Kock §2.4; char-2 kills it); LEAD-VERIFIED vs FrobeniusAlgebra.lean; the two Frobenius laws themselves are genuine. REPAIR (lead brick, queued): normalized `Δ' = ½•Δ`/`ε' = 2•ε` under `Invertible (2:k)` → genuine `IsFrobeniusAlgebra` + `IsSeparableAlgebra` (may need a `Linear k` prelude on Center or the Center.ext componentwise route). **(B2-BLOCKER) the K3→row bridge is absent** — `kummerK3_b2_window` never reaches `K3RealizingElement`/σ=−16/hCob/hBase (the known K8a/K9/K10 packaging arc, now sharply itemized: smooth 𝓡4+spin carrier on the weld quotient, integral orientation, explicit rank-22 basis, torsion close, Gram WITH Q-side/exceptional cross terms, SpinSigmaAtomPkg/K3RealizingElement packaging). wt2's smooth-weld dispatch = step 1, IN FLIGHT. **(M3) B0↛B1 + `toLeaves` uncalled** — type-compatible with `traceTethered_of_leaves` (no transport gap) but needs the WuNullCarrier→BoundingDatum construction (= the queued B1-inhabitation Fable front) + a lead composing theorem (toLeaves + TraceWAdmLeaves → the tethered consumer). **(M4 — BINDING FENCE) the B1 `H1Iso` PD-mismatch, rank-zero-masked**: `pinCharSurfaceOfBundled` uses Kronecker-dual H₁ coordinates while `τ.hpolar` ties `q.B` to cup products in H¹ coordinates — `kernelL` may denote a DIFFERENT sublattice than Taylor's; harmless at rank zero (current sole consumption), **must build the surface-PD bridge (`H1Iso = basis ∘ PD⁻¹` + `kernelL = ker boundingBInc`) BEFORE any nonzero-rank reuse**; pin field consumer-dead until then. **(M5)** B2-Stokes stranded before `hcross` = wt1 #289, IN FLIGHT. **(m6)** 16/36 → ALREADY REMEDIATED (`ec4f03e2` isManifold_resE 36/36 postdates the audit baseline). **(m7)** stranded terminals mostly by-design; the seam no-go registry IS committed (`b515e765`, constants.py — outside the auditor's lean/ root).
- **⛔ MERGE-CYCLE DISCIPLINE (re-instated 2026-07-21 after operator catch — the strength pass had degraded to build+#print+grep):** every worker merge = lake build + fresh `#print axioms` **+ the LEAD'S OWN statement-level strength review of each headliner** (open the actual theorem statements; vacuity/triviality/carrier-faithfulness per the CLAUDE.md checklist; spot-trace ≥1 downstream consumer on the faithful carrier). The lead assigns ⟹ the lead assesses — per-merge review is NOT delegated. Codex 5.6-sol = ON-DEMAND only: (a) a full-context catch-up when review debt accumulated (audit #1, running, covers the slipped 07-20/21 span), (b) planning/refinement dossiers (CollarPairBuild, B1), (c) gate rounds. No standing batch-audit cadence. Grep is NEVER a review. Audit #1 RUNNING: `codex_strength_audit_20260721.md` (scope = ALL 2026-07-20/21 merges incl. the lead's own bricks; findings → remediation builds, never walk-backs).
- **⟳ IN FLIGHT (corrected 2026-07-21 — the operator caught a STALE 'running' claim: the pre-compaction wt1/wt2 agents had ended, their partials ALREADY MERGED at `d8f95514`/`443e54c0`, slots idle; both REDISPATCHED):** wt1 = hcross peel CONTINUATION → hcross_pm (#289: B2 chain-side DONE on main — two-leg cover sum + seam restriction/transport + assembly heart + cochain reconciliation; remaining slices → hcross_pm → feed the Gram-pin consumer) · wt2 = E-side IsManifold FINISHER (#288: `contDiffOn_reshapeConj` + 16/36 pairs DONE on main; remaining ~20 annulus pairs + 6×6 dispatch → ResE certificate → **THE SMOOTH WELD unblocks**) · wt3 = **K7 FINALE: torsion-freeness of H₂(K3;ℤ) → `kummerK3_b2_target` (#303)** — routes: δ₁-kills-torsion (Torsion ⊆ im Σ₂ + the landed Torsion ∩ im Σ₂ = ⊥ forces ⊥) / H₁(K3)+UCT leg / δ₁ rows 2–5 counting. **#302 LANDED (3 commits → merged `2ca23ff9`, #print-pure, STRENGTH-REVIEWED — real weld carrier, conditional lands on the opener's pre-declared target): `kummerK3_H2_rank : rank H₂(K3;ℤ) = 22` UNCONDITIONAL** (Delta1Window:286) + `freePartEquiv` (H₂/T ≅ ℤ²²) + torsion = finite elementary-2 ≤ 2¹⁵ with `Torsion ∩ im Σ₂ = ⊥` + `cokerSigma2Equiv` (coker Σ₂ ≅ im δ₁) + `mem_range_k7Delta_one_iff` (im δ₁ = ker(H₁collar→H₁qThick); E-leg auto-dead) + **δ₁-matrix row 1 `delta1_image_parity`** (im δ₁ ⊆ parity-zero hyperplane; `card_cokerSigma2_le` ≤ 2¹⁵) via the Q-side deck functional `qDeck` (norm-parity, all 16 seams choice-free) + H₁-window (`k7Delta_zero_eq_zero`, `h1K3_surjective_from_Q`) + the named-residual conditionals (`kummerK3_H2_equiv_of_torsion_free`, `kummerK3_b2_target_of_torsion_free` :305). RESIDUAL = EXACTLY `Torsion(H₂(K3;ℤ)) = ⊥`. δ₁ rows 2–5 + sufficiency 2¹¹ ≤ |im δ₁| = the secondary sharpening (winding cocycles; transfer provably kills them — KummerCircleInvolutionWind is the seed). The 5 #302 modules got the lead's inline strength review on merge (carrier + target-Prop traced); no batch-audit queue — codex stays on-demand. **#301 ✓** (UNCONDITIONAL b₂=22 window + qH2EquivInt; detail → HISTORY reshard 4). Umbrella registered by the worker this time. **#300 ✓** (the Q-side Smith-transfer engine, qH2EquivOfWindow; detail → HISTORY reshard 4). ⚠ WORKER SLIP (recurred): new modules NOT registered in the umbrella — lead patched (`e7573825`); registration now demanded in briefs. **#299 ✓** (K7 collar-thickened MV + b₂-window pieces + seam-cover kernel no-go; detail → HISTORY reshard 4). 5 fresh friction laws (irreducible-on-ℝ-arith-defs, sub named-X arg, linarith defeq-ascription no-op, Tendsto.min, junk-value 1/0 flow totality) → catalog next batch. **#298 ✓** (unconditional H_*(ℝP³;ℤ) table + transfer pin; detail → HISTORY reshard 4). 7 fresh friction laws → catalog (`8756ea01`). **#296 MERGED (`04bb90c5`→main `41f53427`, #print-pure): the top-degree Smith solve** — `rp3H3EquivInt : H₃(ℝP³;ℤ)≅ℤ` + `rp3_homology_high : n≥4 ⟹ Hₙ=0` (both conditional ONLY on h4/h5), transfer chain iso `rHmlEquivAHml`, `inclBH_diffH_eq_zero` (D_*=0), engine `Hmap_bijective_of_bijective`. TRACED dead ends (do not re-derive): pure-LES/transfer termination CANNOT close (ℝP^∞ 2-torsion survives both Smith LESes; h4 AND h5 both needed); direct transferChainInt/codRestrict reduction = whnf heartbeat wall (route = Hmap_comp of the single chain map (2:ℤ)•id; chain-level pin p_#∘tr=2 banked). · codex×2 (read-only xhigh): #212 `CollarPairBuild` design (`codex_212_collarpair_design.md`) + hcolD-B1 `RankZeroSurfaceBoundingDatum` design (`codex_hcolD_B1_design.md`) — vet both dossiers before adopting.
- **ROUND-8/9 + CODEX-QUARTET + T⁴°-cert + K7-feeder landing detail → HISTORY (2026-07-20 reshard 2; all content preserved verbatim there).** Headlines that remain live: (★) linchpin + `electricMonObj` banked · K8b floor = {`stable_neg_rank16` (escalation-owned) · ReblockToK3 DONE} · E-side ChartedSpace done (annulus trivialization; "arc charts" claim refuted) · `instChartedSpacePuncturedTorus` + `metric-vs-chart-radius-coverage` settled fork · K7 feeder closed (`resEH2EquivInt` + generator pin) · H-1 dossier adopted (Thm 5.2 attribution; transvection dead; the tether interface — now BUILT, see the H-1 landing above) · #212 8-point spec (consumer now BUILT, see above).
- **PINNED (binding):** ρ = 1/2 chart-ball excisions · S³/±1 antipodal boundary presentation (both weld sides literal) · `attribute [local instance] SpinSigmaRoute.chartW6` friction · the `sphereDiskPinned` assembly forms = current strongest · `t4m` two-witness sign (intrinsic) · scripted `codex exec` needs `--skip-git-repo-check` · check umbrella registration at every merge (2 worker misses).
- **ENGINES BANKED THIS ARC (reusable):** spine-peel (CircleWindingCocycle + TorusCrossPeel Y-generic right-peel) · all-degree htpy-equiv finiteness transfer (deg-0 included) · the collar-slide toolkit + CollarSplitDatum · collarHomeo (16 collars ≃ₜ Euclidean shell) · mvDatumOfPieceFiniteness (homeo demands eliminated) · bdryHomeoRP3 (proper-map T2 route) · zeroSectionHomologyEquivInt (all-degree ℤ iso) · capstone_boundary_hBd two-ends split.
- **LEAD LESSONS (encoded):** SETTLED_FORKS before briefs (goldfish applies to briefs) · lean_local_search = decl names not modules · reset-slot immediately before dispatch · workers' honest refusals/premise-corrections are the mechanism — every round shrank the floor.

- **ROUND-2 + lead design passes (superseded detail → HISTORY):** K1-b partial → NOW DONE (ROUND-4); completeness §4 absorbed + three-lane H design → `H_GEOMETRIC_LEG_DESIGN.md` (binding, premise-corrected in place); K4–K10 Route B → `KUMMER_K4K10_DESIGN.md` (binding); SymTFT S0/S1+S2-prefix landed (ledger `docs/roadmaps/SymTFT_S0S1_prime_Roadmap.md`; continuation #263). Row-side floor + lessons: ROUND-3 block below.
- **★ Lane H-1 tether interface: LANDED** (`d9373ba0`, `KTCompletenessTether.lean`, 9 headliners #print-pure; H-2 dispatch-ready). Detail → HISTORY (reshard 3).
- **★ #212 consumer half: LANDED** (`ba271a11`, `CapstoneSeamCorrectorT` + derived `toHasClass`; the PRODUCER spec = the vetted `codex_212_collarpair_design.md` dossier). Detail → HISTORY (reshard 3).
- **★★ CODEX PAIR #5+#6 LANDED + VETTED + ADOPTED (lead-verified in-tree, merged `88746fb8`):**
  **(5) hcolD DEMAND-NARROWED → SectorIsGeometric** (`codex_hcolD_route.md`): the dC leaf consumes ONLY the class-level `SectorIsGeometric` (verified: `nonempty_ktSpinPresentationDatum_of_row:150` takes hsec; `rankZeroClassCollapse_of_sectorIsGeometric` = the one-step form's only strengthening); `T2DataBordismGrp`'s Quot-closure gives bordism TRANSITIVITY free ⟹ **STAGED bordisms suffice — no combined single-trace W**. New headline wiring `kt_equiv_zmod16_of_residuals_sector` + Rokhlin twin (PinPlusKTCollapseDischarge §5, #print-pure) = the weakest honest collapse hypothesis. ⚠ ROUTE CAUTION (SETTLED_FORKS): the one-sphere/3-handle collapse is UNSOUND without a framing theorem (rank-0 + hchar do NOT force normal Euler = 0). The winning construction = the global KT characteristic-bordism route; brick table B0–B7 in the dossier (hardest = B2 characteristic-extension; the B7 class-level assembly branch is 20–60 LOC and now DONE). G9-4/KTKernelCard confirmed the wrong trade (completeness-strength).
  **(6) ROW-SIDE STATUS-PREMISE CORRECTED** (`codex_rowside_scoping.md`): **NO unconditional row instance exists at HEAD** — #257's "collapse" was interface-level only. The TRUE row-side floor: {`a` = the global total-E1 assignments (R3 inhabitation + R4 genuine σ-descent/hbord — the two foundational walls; `a` is NOT eliminable by restructuring, only replaceable by equivalent global content) · `hdvd` (R5 — the global Rokhlin `topo : 2∣σ/8` field, a SEPARATE obligation) · `hCob`/`hBase` (Benedetti transferred at ALGEBRA level only — the {hCob,hBase}→RealizesSphereProducts induction is live generic; the geometric witnesses were NEVER built; hardest brick **A1 = IntCongr → embedded framed geometry**, then A2 trace/outgoing-form + A3 rank-zero base; the char-surface surgery lanes do NOT help — different axis, settled fork) · `hs2s2` (**R1 = the concrete S²×S² row slot, ~250–600 LOC via the banked SphereProdGramPin/hcross atoms — DISPATCH-READY**) · K3 completion (R6 = K7/K8/K9/K10)}. R0 (empty-end p↝q adapter, 40–100 LOC pure wiring) = a quick lead brick. Full brick table R0–R7/A1–A3/Z in the dossier (estimates advisory only, per the ignore-PM rule).
- **★ R0 adapter: LANDED** (`6c04772f`, `isDataBordant_of_wadm`). Detail → HISTORY (reshard 3).
- **★ SymTFT S2: CLOSED end-to-end** (comonoid apex + separability + incorners `b27d41b1`/`6e385ca9`/`7a92485c`; BOTH Frobenius laws `43c54cd7` — see the live #283-COMPLETE line above). Full landing detail + reusable corner tricks → HISTORY (reshard 3).
- **Harvest TODO (live):** vacuity/strength-check the Kummer + completeness + wiring worker returns ([[feedback-verify-substrate-strength-not-docstrings]]); periodic re-check of docstring-trusted claims.

**═══ 2026-07-18 OPERATOR DECISION — K3/Kummer A-vs-B RESOLVED → OPTION A (BUILD KUMMER) ═══**
- **The operator GREENLIT Option A (2026-07-18): build the Kummer K3 generator UNCONDITIONALLY** (the faithful, no-posit end-state), NOT carry `K3RealizingElement` as a tracked Prop. This resolves the long-deferred A-vs-B fork (`Lit-Search/Phase-5qH/K3_generator_realization_verdict_20260717.md`) and **unblocks the whole Freeze-A/generator cluster** (row/g, hCob, hBase, hker, hΦg — all route through the K3 generator). Directed: fan the **Kummer opener → wt2** + **completeness (W-D/H) lead-design-then-build → wt3**.
- **THE CLOSE-OUT'S REMAINING STRUCTURE (traced 2026-07-18):** the headline `kt_equiv_zmod16_of_residuals_freezeAtoms_ofReducedAtoms` is conditional on 8 atoms. Of them: **hBbord** = P23 (wt1, active) + b-wiring (the ONLY K3-independent clean brick); **row/g · hCob · hBase · hker · hΦg** = the K3/Kummer cluster (now GREENLIT via Option A); **H** = the completeness summit (W-D, K3-independent but deepest — the faithful-carrier `KRSResidualRow` provider; ⚠ NOT the vacated `smith_inflow_z16` layer); **hcolD** = the rank-0→empty collapse bordism ("no in-tree tether does — round-9 freeze", a deep geometric arc).
  - **K0 + completeness-opener landing detail → HISTORY (reshard 2).** Live summary: `KummerK3Base.lean` = the 11-brick dossier + K0 `TorusFour := Circle⁴` certificate (no Mathlib `AddCircle` manifold — routed around; K9 re-charts `𝓡1⁴`→`𝓡 4`); `KTCompletenessProvider.lean` = the algebraic gateway unconditional (`exists_isotropic_of_brown_zero` genuine Gauss-sum) + the completeness-chain strength-verified (`kt_equiv_zmod16_of_leaves` genuinely faithful).
- **THE KUMMER TARGET (`K3RealizingElement a`, `PinPlusKTSpinSigmaStock.lean:261`):** a concrete `g : StrMfd (spinEmptyData prov)` (T2 + nonempty closed spin 4-mfd) + `pkg : SpinSigmaAtomPkg` (orient/basis/PD) + `hrank : b₂=22` + `hk3 : IntCongr (interMatrix (a.fc g)(a.B g)) k3Form` (form = 2(−E₈)⊕3H, σ=−16). Banked: `k3Form` (`SpinSigmaGenerator.lean:122`, `IsEvenUnimodular` ✓), `interMatrix` (`IntersectionMatrixInt.lean:83`), `IntCongr` (`HyperbolicNormalForm.lean:28`). Construction (K3 verdict, elementary-smooth): T⁴=(ℝ/ℤ)⁴ [Mathlib `AddCircle`] → involution τ(w)=−w (16 fixed 2-torsion pts) → quotient (16 A₁ orbifold pts) → resolve each (Euler −2 disk bundle over S² = A₁ resolution) → K3 (H₂=ℤ²², form 2(−E₈)⊕3H). Reusable machinery: `SingularSurgeryManifold`/`HandleD5`/`ChartsConcrete`, `PinPlusKTSurgeryTrace`, the stock-StrMfd witnesses (S⁴/S²×S²/ℝP⁴), + the S²×S² H₂/interMatrix arc as the form-computation template. **A LARGE novel multi-arc program (opener = decomposition dossier + T⁴ foundation; expect a #119–#178-scale effort).**

**═══ ARCHIVED FRONTIER BLOCKS (verbatim → `LAB_NOTEBOOK_HISTORY.md`, 2026-07-20 size protocol) ═══**
- **2026-07-17 POST-COMPACT RESUME** (slot state, Kummer K0/T⁴-homology landings, B-03 He3A genuine build, strength-assessment queue+results) — superseded by the 07-18/07-20 blocks above; full text in HISTORY.
- **2026-07-16 CLOSE-OUT ARM LIVE-STATE CHRONICLE** (the continuously-updated running log: P5/P23 landing chain, K3/E1 feasibility design pass, route (c′) adoption, friction laws) — landed content is all on main; full text in HISTORY. Friction laws remain binding: TERM-MODE ONLY on SphereDisk/SphereProd concrete types (never rw/show — whnf walls; LinearMap.congr_fun/congrArg/.trans; pin `X := TopCat.of SphereDisk`; pre-elaborate composed pullbacks; depth-1 ext); authoritative axiom check = fresh `lake env lean` `#print axioms` (lean_verify can false-report sorryAx).

## ⚖ Decisions & dead-ends (binding summary; full log → `LAB_NOTEBOOK_HISTORY.md`)
- ⚖ **THE COLLAR FORK (lead ruling, 2026-07-13 late):** both [W,∂W] walls reduce to the Mathlib-absent
  collar-neighbourhood theorem (∂W is CLOSED; in-tree relative-MV needs OPEN second elements). RULING:
  **explicit-collar specialization** — the datum is only consumed for CONSTRUCTED W's (op bordisms =
  cylinders/sums with explicit product collars; future Bor witnesses carry their data by construction,
  the provider pattern). The GENERAL collar theorem = a named DEFERRED slice, not critical-path.
  Wall 2 is reduced to `Injective (relIncl (∂W ⊆ Kᶜ))` (`determinedByInteriorPoints_of_interiorInjective`).
- ✅ **W-D ROUTE = KT §5 DIRECT (lead live-layer call, 2026-07-13 late, dossier-backed — W_D_ROUTE_
  DOSSIER.md):** the extension `0→ℤ/2→G→ℤ/8→0` with G ↠ ℤ/8 = the proven `charPairBrown_surjective`;
  spin side = the empty-Σ specialization of the built carrier (NO second-carrier program); no external
  stable ℤ/16 (smith_inflow_z16 is vacuous-as-stated on the vacated carrier — restatement-needed
  family). **THE critical node = the non-split bit `8·[ℝP⁴] ≠ 0`** (mod-8 door blind to {0,8}) —
  staged early via the structural Kummer argument (option 1, in-tree assets), ψ/index = deep fallback.
  W-D terminal Props get their own vacuity gates before consumption.
- ✅ **RE-BASE LANDED (operator, 2026-07-13 planning session — THE current binding plan):** (D1) **layered
  rebuild** (keep frameworks + T2 layer + relation-free assets; rebuild the Pin⁺ INSTANCE with
  structure-extension `Bor`, computed ABK, genuine `revStr`; lift ℝP⁴ to smooth); (D2) **W-D route =
  live-layer selection, KT-lean** (the 07-03 "BINDING Smith-LES" pin was stale — predated the KT §5 fetch);
  (D3) **exec map RETIRED/frozen** — this INDEX is the single live execution-state doc. Plus the standing
  **vacuity-attack gate** (roadmap §3). Durable roadmap = the 2026-07-13 durable-only rewrite.
- 🛑 **SOUNDNESS FINDING (2026-07-13, kernel-checked):** the T2-less `Bordism` relation COLLAPSES ⇒ the `DataBordismGrp` carrier (thin AND tied) is NON-FAITHFUL; every `DataBordismGrp`-quantified completeness/bounding Prop (L2/L3, σ-route door, Freeze B, `hbound`) is true-but-VACUOUS. Genuine target = the **T2 + smooth (`k≥1`)** faithful carrier — with the THIRD leg (2026-07-13 design audit): the instance's `Bor` must be structure-extension, not grade bookkeeping. No-go `nonhausdorff-bordism-collapse` (registry + SETTLED_FORKS + `KernelNoGos.lean`). This is a RETURN to buried roadmap §9.2 (2026-07-03: "ℤ/16 is a smooth-category statement; capstone at k=∞").
- ⛔ **Kernel no-gos** (machine-fed negative frontier — NEVER re-derive): **17 forks / 37 aliases** in `KERNEL_NOGO_REGISTRY` → `KernelNoGos.lean` (surfaced by `/skeft-qa:frontier`). Headliners: `nonhausdorff-bordism-collapse`, `free-membrane-kernel-kills-nonsplit`, `untethered-membrane-factors-relation`, `enriques-datum-refuted-as-shaped`, `genuine-gm-carrier-eight-torsion`, `mfd-equals-H1-dead-end`, `nogo_lattice_arf_not_sigma8`.
- ⛔ **Route no-go:** mod-2 relative-cohomology MV + the `relCohomMvConnecting`/class-lift close are FIELD-based (perfect Kronecker pairing, non-degeneracy) — FALSE over ℤ (torsion). Use the torsion-safe / Route-B chain-safe routes. (Full rationale in the history shard.)
- **Option A SETTLED** (operator, 2026-07-04): genuine unconditional L4, ZERO posits. B is a retired phantom — never re-surface.
- **Keystone RE-ANCHORED** (2026-07-06): KT geometric exact-seq close (`Ω₄^{Spin}≅ℤ`), NOT the spectral `smith_inflow_z16`/ABP tower (route artifact, demoted).

## 🗂 Shard index
- **`LAB_NOTEBOOK_W41.md`** — frozen · 2026-07-16. (topic tags)
- **`LAB_NOTEBOOK_W1.md` … `LAB_NOTEBOOK_W40.md`** — frozen chronological shards (2026-07-12 → 2026-07-20; W40 extended by the 2026-07-20 shard op). Open by number on demand.
- `LAB_NOTEBOOK_HISTORY.md` — subsumed frontier detail + full Decisions/dead-ends log.
- Per-effort detail: `E1…E5/LAB_NOTEBOOK_INDEX.md` (each with its own shards + verified sources).
- Plan: `PHASE5QH_EXECUTION_MAP.md`; binding no-gos: `../SETTLED_FORKS.md` + atlas negative frontier.
