# /goal condition — Phase 5q.H full 16-convergence, arm 4 (goal_id 20260714T131617)

Durable, crash-recoverable source of the `/goal` condition (spec A.5). **Arm 4** — resumes the arm-3
checkpoint (operator decision 2026-07-13: checkpointed at the W-A round-4 membrane-tie landing;
validate 46/46, library green). Supersedes `goal_prompt_20260713T121921.md` for arm-time notes; the
**condition below is byte-identical to arm 3**.

The goal condition is the DEFINITION OF DONE — the evaluator scans the transcript against it; it is
not a how-to and is not always passed back. Instructive/tactical state (resume order, frozen-binder
status, environment posture) lives where the condition already points: the durable roadmap + the
LAB_NOTEBOOK_INDEX FRONTIER (see the 2026-07-13 ✋ WAVE CHECKPOINT and 2026-07-14 entries in the
active shard), plus memory note `project-5qH-nonhausdorff-substrate-bug` §⏭.

## The condition (what `/goal` is set with)

```
GOAL: Finish the genuine, fully UNCONDITIONAL 16-convergence — Ω₄^{Pin⁺} ≅ ℤ/16 formalized in Lean 4 on a FAITHFUL carrier satisfying ALL THREE faithfulness legs (roadmap §2: T2 on the bordism manifold; SMOOTH category k≥1, flagship statement at k=∞, never the C⁰/TOP group; structure-extension Bor with the ℤ/16 invariant COMPUTED from the structure, never carried as data) — injective + surjective, ZERO posits, kernel-pure {propext, Classical.choice, Quot.sound}, no project-local axiom/sorry/native_decide/maxHeartbeats. SUBSTRATE VERDICT SETTLED (operator 2026-07-13, roadmap §4: LAYERED REBUILD — rebuild ONLY the Pin⁺ instance, genuine revStr; keep the frameworks + T2 layer + relation-free assets; lift ℝP⁴ smooth; do NOT re-litigate). W-A DEFINITION GATE (binding): the faithful instance's DEFINITION passes a fresh-context adversarial review + a VACUITY ATTACK (a dedicated attempt to discharge its completeness content with zero geometric input must FAIL) BEFORE anything is built on it; every later completeness/bounding Prop passes the same attack before consumption (roadmap §3). Route-AGNOSTIC completeness leg: candidates in roadmap §6; selection is LIVE tactical state (notebook FRONTIER + atlas) — never pinned here. Source-of-truth: docs/roadmaps/Phase5qH_LiteratureGradeUnconditional_Roadmap.md (2026-07-13 durable-only rewrite) + docs/dev-loops/Phase5qH/LAB_NOTEBOOK_INDEX.md (re-read both after any compaction; FRONTIER + DECISIONS = the re-grounding read; SETTLED_FORKS.md + shards on demand; the exec map is RETIRED — never read it for current status). FIRST_ACTION each fresh context: (1) scripts/repo_state_probe.py + /skeft-qa:frontier (never trust a snapshot); (2) reconcile slots (git cherry -v main worktree-wtN, N=1,2,3) — harvest or record the verdict BEFORE building. LEAD + MODEL TIERING (operator directive): worktree lean-workers tiered by difficulty — Sonnet mechanical, Opus deep geometry, FABLE only the very hardest with WIDE latitude (never routine bricks). NOTEBOOK DISCIPLINE: every brick gets its own ### entry in the ACTIVE shard the turn it lands (git-log dates, never future-stamp); INDEX stays glanceable (roll superseded framings same turn); WIP scaffolds logged on creation, under lean/SKEFTHawking/, deleted-or-promoted when superseded. SCOPE IS SETTLED — land the MAXIMUM coherent GREEN increment THIS turn (don't atomize; commit every ~5–10 green kernel-pure bricks, notebook each brick). A stop-hook firing is a GO signal, never a cue to stop/hold/re-scope/hand back; NEVER invoke context-budget/session-depth to defer. Never re-derive a settled-dead fork (negative frontier / docs/dev-loops/SETTLED_FORKS.md; binding no-gos incl. nonhausdorff-bordism-collapse [no completeness on a T2-less or bookkeeping-Bor carrier], dataBordism_two_torsion_of_revStr_trivial + genuine-gm-carrier-eight-torsion [revStr-trivial/8-torsion carriers cannot be ℤ/16], synthetic-grade-ker-bot-nogo, nogo_lattice_arf_not_sigma8, mfd-equals-H1-dead-end; mod-2 field-Kronecker/class-lift routes are FALSE over ℤ). Legitimate stops ONLY: a kernel-checked no-go (build around; one scoped disclosed Prop + discharge plan), or a genuine user-only decision (ask once, keep shipping meanwhile). NEVER ship a new project-local axiom without explicit user sign-off. DONE when ALL of: (1) the unconditional Ω₄^{Pin⁺} ≅ ℤ/16 equivalence on the three-leg faithful carrier is a kernel-pure theorem with ZERO posits — lean_verify reporting exactly {propext, Classical.choice, Quot.sound} in the transcript AND `rm -rf .lake/build && lake build SKEFTHawking.ExtractDeps` exit 0 in the transcript; (2) every completeness/bounding Prop the capstone consumes has a transcript-surfaced vacuity-attack PASS (zero-geometric-input discharge FAILED); (3) uv run python scripts/validate.py prints N/N in the transcript; (4) the fresh-context skeft-qa:adversarial-reviewer ran with ZERO BLOCKER findings surfaced in the transcript.
```

## Notes
- Condition length: 3,938 chars (≤ 4,000 cap; byte-identical to arm 3 — everything arm 3 settled is
  machine-fenced in `KERNEL_NOGO_REGISTRY` or carried by the FRONTIER; per the durable-only design
  nothing needed pinning).
- Arm sha `97d3d924` (main, armed 2026-07-14); mode `lean`; role `lead`; slots `[1,2,3]` (full
  worktree utilization, model-tiered), all three reconciled clean at arm.
