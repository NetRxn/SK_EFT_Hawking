# Goal prompt — Phase 5q.H, Phase 1 (goal_id `20260704T101129`)

> **⛔ SUPERSEDED / DO NOT RE-ARM (2026-07-04).** The managed loop was ended (`/goal-end`, marker removed) and this goal text is **stale**: it frames "the *entire Phase 1* — the **in-tree** geometric derivation," which the goal-loop turn-1 audit **disproved** — Phase 1 is a from-scratch, Mathlib-absent geometric substrate build, and the capstone's carrier `pinPlusGMData` does not exist. See `temporary/working-docs/16Convergence_Reconciliation_Audit_2026-07-04.md`. No goal should be re-armed until the plan reflects the G+S from-scratch shape and the A-vs-B (disclosed-form vs build-the-substrate) decision is made.

**Roadmap:** `docs/roadmaps/Phase5qH_LiteratureGradeUnconditional_Roadmap.md` (§10 is the wave path).
**Notebook:** `docs/dev-loops/Phase5qH/LAB_NOTEBOOK_INDEX.md`.
**Scope:** the *entire Phase 1* — the in-tree geometric derivation reducing `Ω₄^{Pin⁺}≅ℤ/16` to the single **required** completeness Prop `smith_inflow_z16` (the ABP `Ω₆^{Pin⁻}≅ℤ/16` input). Phase 2 (discharging that Prop — the ABP spectral tower, no elementary substitute) is OUT of scope for this goal.
**Crux (settled, do not re-litigate):** per `Lit-Search/Phase-5qH/ABK_injectivity_routes_lemma_DAG_20260703.md` §4, the ℤ/16 completeness input is REQUIRED and NON-elementary. Rokhlin `16∣σ` (elementary) is the separate GM detection base case (`hyp:rokhlin_sigma_mod_16`), not the completeness.

## /goal condition (durable source; ≤4000 chars)

```
GOAL: land the Phase-1 injective Pin⁺ capstone in disclosed form — omega4PinPlusGM_equiv_zmod16_of_smith_inflow : (h : smith_inflow_z16) → DataBordismGrp (pinPlusGMData) ≃+ ZMod 16 (injective + surjective + invariant computed-from-structure, kernel-pure) — reducing the classification to EXACTLY the one required completeness Prop smith_inflow_z16 (the ABP Ω₆^{Pin⁻}≅ℤ/16 input; its discharge is Phase 2, OUT of scope here). Execute the §10 Phase-1 waves (P1.1 Rokhlin/topo discharge; P1.2 Smith-LES injectivity architecture; P1.3 genuine-carrier upgrade). Source-of-truth: docs/roadmaps/Phase5qH_LiteratureGradeUnconditional_Roadmap.md (read §10 + the §9.3 correction banner + the §10 crux note, then §2–§4) + docs/dev-loops/Phase5qH/LAB_NOTEBOOK_INDEX.md (re-read both after any compaction). Primary sources — read DIRECTLY, never via summary: Lit-Search/Phase-5qH/KirbyTaylor_PinStructures_LMS151.pdf + Lit-Search/Phase-5qH/ABK_injectivity_routes_lemma_DAG_20260703.md. FIRST_ACTION each turn: run scripts/repo_state_probe.py for live sorry/commit state; read docs/dev-loops/SETTLED_FORKS.md before ANY impossibility reasoning. SETTLED (do not re-litigate): the completeness/ℤ/16 input is REQUIRED and NON-elementary (= Phase 2, the ABP tower) — do NOT build it or the spectra stack here; divisibility-vs-completeness is resolved (§10 crux note); the free-grade ker=⊥ no-go is BANNED. SCOPE IS SETTLED — build the next brick THIS turn; a stop-hook firing is a GO signal, never a cue to stop/hold/re-scope/hand back/estimate effort. Legitimate stops ONLY: a kernel-checked no-go, or a genuine user-only decision. Kernel-purity non-negotiable: axioms exactly {propext, Classical.choice, Quot.sound}; NO new project-local axiom without explicit user sign-off; no sorry/native_decide/maxHeartbeats in proof bodies; the required completeness input is the ONE named tracked Prop smith_inflow_z16 surfaced in the capstone signature, never an axiom. DONE when: (1) the injective capstone exists on the genuine GM carrier, kernel-pure, injective + surjective, with smith_inflow_z16 the SINGLE Prop in its signature + registered, AND hyp:rokhlin_sigma_mod_16 / SmoothSpinManifold4.topo discharged in-tree (P1.1); (2) rm -rf .lake/build && lake build SKEFTHawking.ExtractDeps exits 0 and validate.py prints N/N in the transcript; (3) the fresh-context skeft-qa:adversarial-reviewer ran with ZERO BLOCKER findings surfaced in the transcript.
```
