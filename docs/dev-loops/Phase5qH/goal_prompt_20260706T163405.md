# /goal condition — Phase 5q.H full 16-convergence (goal_id 20260706T163405)

Durable, crash-recoverable source of the `/goal` condition (spec A.5). Route-AGNOSTIC by explicit user
instruction: this file never pins a carrier, wall (W1/W2), or route — those are live tactical state
(atlas FRONTIER + `LAB_NOTEBOOK_INDEX.md` FRONTIER + `SETTLED_FORKS.md`).

## The condition (what `/goal` is set with)

```
GOAL: Finish the genuine, fully UNCONDITIONAL 16-convergence — Ω₄^{Pin⁺} ≅ ℤ/16 formalized in Lean 4 on a FAITHFUL carrier (ℤ/16 genuinely COMPUTED as the invariant, injective + surjective, ZERO posits), kernel-pure {propext, Classical.choice, Quot.sound}, with EVERY remaining completeness/PD/orientation/Rokhlin input discharged — no project-local axiom, no sorry, no native_decide, no maxHeartbeats. Path- and carrier-AGNOSTIC: which faithful carrier and which route (spectral vs geometric, cover-induction vs other) are LIVE tactical state — the loop picks the highest-leverage open node from the atlas FRONTIER + notebook, and this condition NEVER pins one. Source-of-truth: docs/roadmaps/Phase5qH_LiteratureGradeUnconditional_Roadmap.md + docs/dev-loops/Phase5qH/LAB_NOTEBOOK_INDEX.md (re-read the roadmap + notebook INDEX after any compaction — its FRONTIER + DECISIONS blocks are the re-grounding read; open shards + E1–E5 sub-notebooks + SETTLED_FORKS.md on demand). FIRST_ACTION each fresh context: run scripts/repo_state_probe.py + /skeft-qa:frontier for the REAL current state (never trust a snapshot). SCOPE IS SETTLED — land the MAXIMUM coherent GREEN increment THIS turn (as much as you can finish cleanly; do NOT atomize into one tiny brick — per-turn throughput matters; commit every ~5–10 green kernel-pure bricks, updating the notebook each brick). A stop-hook firing is a GO signal, never a cue to stop/hold/re-scope/hand back, and NEVER invoke context-budget/session-depth to defer (auto-compaction is handled). Never re-derive a settled-dead fork (negative frontier / docs/dev-loops/SETTLED_FORKS.md; binding no-gos incl. genuine-gm-carrier-eight-torsion, nogo_lattice_arf_not_sigma8, mfd-equals-H1-dead-end, synthetic-grade-ker-bot-nogo). Legitimate stops ONLY: a kernel-checked no-go (build around it; carry one scoped disclosed Prop + discharge plan), or a genuine user-only decision (ask once, keep shipping meanwhile). NEVER ship a new project-local axiom without explicit user sign-off (DR recs advisory). DONE when: (1) the unconditional Ω₄^{Pin⁺} ≅ ℤ/16 equivalence on a faithful carrier is a kernel-pure theorem with ZERO posits/axioms/sorries — evidenced by lean_verify reporting exactly {propext, Classical.choice, Quot.sound} surfaced in the transcript AND `rm -rf .lake/build && lake build SKEFTHawking.ExtractDeps` exit 0 in the transcript; (2) `uv run python scripts/validate.py` prints N/N (all checks pass) in the transcript; (3) the fresh-context skeft-qa:adversarial-reviewer ran with ZERO BLOCKER findings surfaced in the transcript.
```

## Notes
- **End state = the genuine unconditional `Ω₄^{Pin⁺} ≅ ℤ/16`** (the "full 16 convergence"), ZERO posits.
  The atlas KEYSTONE at arm time is `smith_inflow_z16` (gates 12); the integral-PD chain
  (`intCapIso`/`intPoincareDuality_perfectPairing`/`rokhlin_sigma_mod_16`) is the W1 track. These are
  LIVE frontier state, not pinned here.
- Arm sha `1a315479` (post relCohomMvConnectingInt δ complete); mode `lean`; solo; main-only.
