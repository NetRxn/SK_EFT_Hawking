# Goal prompt — 20260716T073016 (Phase 5q.H: close out the 16-convergence)

- **goal_id:** `20260716T073016`
- **armed:** 2026-07-16 (arm_sha `0c53e18f`, the handoff-doc commit)
- **role:** lead (orchestrates Fable/Opus/Sonnet subagents at discretion — user directive)
- **mode:** lean · **slots:** wt1/wt2/wt3
- **roadmap:** `docs/dev-loops/Phase5qH/HANDOFF_16_CONVERGENCE.md`
- **notebook INDEX:** `docs/dev-loops/Phase5qH/LAB_NOTEBOOK_INDEX.md`
- **predecessor:** arm-3 goal `20260713T121921` (cleared by operator); arm 4 ran loop-less to the
  2026-07-16 SAVE-STATE. This is a NEW goal armed on the handoff plan.

## The /goal condition (durable source — the marker's `goal` field is the fast-read copy)

GOAL (Phase 5q.H close-out): SK_EFT_Hawking reaches the genuinely UNCONDITIONAL 16-convergence — Omega_4^{Pin+} equiv ZMod 16 (kt_equiv_zmod16, or its final recorded name) proved in Lean 4 on the FAITHFUL tethered carrier (pinPlusCharPairData over CharPairBorRealizedTethered: T2 bordism manifolds, smooth k>=1 data, structure-extension Bor with the Brown invariant COMPUTED), as an explicit additive equivalence, injective + surjective, with ZERO posits: no hypothesis-carrying leaf remains on the final statement or anywhere in its dependency closure; no project-local axiom / sorry / native_decide / maxHeartbeats; kernel axioms of the final theorems' closure exactly within {propext, Classical.choice, Quot.sound}. Scope includes the W-E Rokhlin corollary + the W-F capstone (k=infinity statement + Omega_5 recast) per the roadmap.

SOURCE OF TRUTH (re-read after any compaction; open shards on demand): roadmap = docs/dev-loops/Phase5qH/HANDOFF_16_CONVERGENCE.md (the dependency-ordered remaining-work plan); notebook INDEX = docs/dev-loops/Phase5qH/LAB_NOTEBOOK_INDEX.md (FRONTIER + DECISIONS). Never re-derive a settled-dead fork: KERNEL_NOGO_REGISTRY + docs/dev-loops/SETTLED_FORKS.md + the negative frontier are binding; name the relevant forks in every worker brief.

POSTURE: scope is SETTLED — land the MAXIMUM coherent GREEN kernel-pure increment THIS turn (do NOT atomize); a stop-hook firing is a GO signal, never a cue to stop/hold/re-scope/hand back. Legitimate stops ONLY: a kernel-checked no-go, or a genuine user-only decision (ask once, keep shipping meanwhile). Lead orchestrates subagents at discretion: Sonnet mechanical / Opus deep geometry / Fable gate rounds + hardest problems; at most 2 heavy Lean workers at once.

GATE DISCIPLINE (settled lock): every NEW completeness Prop / leaf shape passes a Fable vacuity-attack gate round BEFORE its discharge is consumed; a gate FAIL is fixed before consumption; the recorded binding gate specs (the gate modules + notebook DECISIONS) hold. New project-local axioms require explicit user sign-off (none expected).

DONE when ALL of the following are surfaced in the transcript:
(1) kt_equiv_zmod16 (or its final recorded name) proved UNCONDITIONALLY on the tethered carrier — every previously-gated leaf discharged; the provider inhabited unconditionally; lean_verify on the final theorems prints the pure kernel axiom set.
(2) The W-E + W-F capstone theorems landed, same purity bar.
(3) The trusted clean rebuild (rm -rf .lake/build && lake build SKEFTHawking.ExtractDeps) completes clean, shown in the transcript.
(4) uv run python scripts/validate.py prints ALL checks passed (N/N, incl. nogo_substrate_integrity), shown in the transcript.
(5) The fresh-context skeft-qa:adversarial-reviewer ran on the close-out claim set with ZERO BLOCKER findings, surfaced in the transcript.
(6) The notebook INDEX FRONTIER + the roadmap record the closure; all work committed on main (NEVER pushed).

NOT complete while any hypothesis-carrying stand-in remains in the final statement's closure, or any consumed shape's gate round is pending.
