# Goal prompt — 20260727T003055 (Phase 5q.H: close out the 16-convergence AT FULL STRENGTH)

- **goal_id:** `20260727T003055`
- **armed:** 2026-07-27 (arm_sha `12d34743`)
- **role:** lead (orchestrates Opus-5 / Sonnet subagents, tiered by difficulty — **no Fable**, user directive)
- **mode:** lean · **slots:** wt1/wt2/wt3
- **plan (roadmap_path):** `docs/dev-loops/Phase5qH/HANDOFF_16_CONVERGENCE.md` (fully re-verified and
  rewritten 2026-07-27 immediately before arming — plan-currency check passed)
- **notebook INDEX:** `docs/dev-loops/Phase5qH/LAB_NOTEBOOK_INDEX.md`
- **durable roadmap:** `docs/roadmaps/Phase5qH_LiteratureGradeUnconditional_Roadmap.md`
- **predecessor:** goal `20260716T073016` (the first close-out arm). This is a NEW goal, armed on the
  refreshed plan, with the **smooth-category (full-strength) bar made explicit** in the condition —
  the predecessor's condition did not fence the C⁰ fork.

## Arm-time ground state (verified in-tree, not carried forward)

`lake build SKEFTHawking.ExtractDeps` exit 0, 10,274 jobs · `validate.py` 48/48 PASSED ·
30 kernel no-go forks + 39 SETTLED_FORKS prose entries · `nogo_substrate_integrity` green.

## The /goal condition (durable source — the marker's `goal` field is the fast-read copy)

GOAL (Phase 5q.H — FULL-STRENGTH close-out): SK_EFT_Hawking reaches the genuinely UNCONDITIONAL Kirby-Taylor 16-convergence — Omega_4^{Pin+} equiv ZMod 16 — proved in Lean 4 on the FAITHFUL tethered carrier (pinPlusCharPairData over CharPairBorRealizedTethered: T2 bordism manifolds, structure-extension Bor, Brown invariant COMPUTED) and in the SMOOTH category (k >= 1; flagship instantiation at k = top). An explicit additive equivalence, injective + surjective, with ZERO posits: no hypothesis-carrying leaf anywhere in the final statement's dependency closure; no project-local axiom / sorry / native_decide / maxHeartbeats; kernel axioms exactly {propext, Classical.choice, Quot.sound}. A C^0-only (k = 0) form does NOT satisfy this goal — at k = 0 the IsManifold binder is free and the honest group is the wrong one; smooth-category results are RE-DECLARED k-generically, never transported (fork k0-to-k1-transport-refuted). Scope includes the W-E Rokhlin corollary and the W-F capstone (k = infinity statement + Omega_5 recast).

SOURCE OF TRUTH (re-read after any compaction; open shards on demand): plan = docs/dev-loops/Phase5qH/HANDOFF_16_CONVERGENCE.md; live state = docs/dev-loops/Phase5qH/LAB_NOTEBOOK_INDEX.md (FRONTIER + DECISIONS); objective/constraints/gates/sources = docs/roadmaps/Phase5qH_LiteratureGradeUnconditional_Roadmap.md. Never re-derive a settled-dead fork: KERNEL_NOGO_REGISTRY + docs/dev-loops/SETTLED_FORKS.md + the atlas negative frontier are binding; name the relevant forks in every worker brief.

POSTURE: scope is SETTLED — land the MAXIMUM coherent GREEN kernel-pure increment THIS turn (do NOT atomize; per-turn throughput matters); a stop-hook firing is a GO signal, never a cue to stop/hold/re-scope/hand back. Legitimate stops ONLY: a kernel-checked no-go, or a genuine user-only decision (ask once, keep shipping meanwhile).

LEAD DISCIPLINE (binding): the lead pulls and VERIFIES context ahead of every dispatch — a brief's load-bearing route claims are checked in the Lean by the lead before it goes out, never delegated to the worker writing the proof and never taken on a report or a docstring. Explore/research agents may be used liberally; the lead verifies every important finding itself. Delegation is Opus-5 and Sonnet tiered by difficulty; do NOT use Fable. Every worker merge gets the lead's own statement-level strength review (vacuity / triviality / carrier-faithfulness per CLAUDE.md) before it lands — build + #print + grep is NOT a review. Every NEW completeness/bounding Prop passes a zero-geometric-input vacuity attack before its discharge is consumed. New project-local axioms require explicit user sign-off.

DONE when ALL of the following are surfaced in the transcript:
(1) the headline equivalence proved UNCONDITIONALLY on the tethered carrier IN THE SMOOTH CATEGORY — every gated leaf discharged, the provider inhabited unconditionally, and a fresh #print axioms on the final theorems printing exactly {propext, Classical.choice, Quot.sound}.
(2) the W-E Rokhlin corollary and the W-F capstone landed at the same purity bar.
(3) rm -rf .lake/build && lake build SKEFTHawking.ExtractDeps completes clean, shown in the transcript.
(4) uv run python scripts/validate.py prints ALL CHECKS PASSED (N/N, incl. nogo_substrate_integrity), shown in the transcript.
(5) a fresh-context skeft-qa:adversarial-reviewer ran on the close-out claim set with ZERO BLOCKER findings, surfaced in the transcript.
(6) the notebook INDEX FRONTIER + the plan record the closure; all work committed on main (NEVER pushed).

NOT complete while any hypothesis-carrying stand-in remains in the final statement's closure, while the strongest form is C^0-only, or while any consumed shape's gate round is pending.
