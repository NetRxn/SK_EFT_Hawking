# Goal prompt — Phase 6EE at full strength

**goal_id:** `20260730T032457` · **armed:** 2026-07-30 · **role:** solo · **mode:** lean
**arm_sha:** `b8f4954755ff2b0bfb6a0e2ca2026a13baeab50c`
**roadmap:** `docs/roadmaps/Phase6EE_Roadmap.md`
**notebook INDEX:** `docs/dev-loops/Phase6EE/LAB_NOTEBOOK_INDEX.md`

> **This file is the durable, crash-recoverable source of the `/goal` condition.** The gitignored
> marker at `.claude/dev-harness/managed/<session>.json` is only the fast-read copy. If the marker is
> lost, re-invoke `/skeft-qa:goal-prompt`, recover the condition below, and **reuse this same
> `goal_id`** — do not mint a new one for the same goal.

## The `/goal` condition (verbatim)

```
GOAL: Ship Phase 6EE — the 6E* series capstone — at full strength: the control layer
(Control/RotatingWave.lean, Control/DriveCalibration.lean) and the composite-ceiling layer
(Control/CompositeReadoutCeilings.lean), each root-imported. Source-of-truth:
docs/roadmaps/Phase6EE_Roadmap.md + docs/dev-loops/Phase6EE/LAB_NOTEBOOK_INDEX.md — re-read BOTH
after any compaction; the roadmap's per-wave AC is the declaration-level truth; open notebook shards
on demand. SCOPE IS SETTLED — land the MAXIMUM coherent GREEN increment THIS turn (as much as you can
finish cleanly; do NOT atomize into one tiny brick — per-turn throughput matters). A stop-hook firing
is a GO signal, never a cue to stop/hold/re-scope/hand back. Never re-derive a settled-dead fork:
consult the notebook DECISIONS block, docs/dev-loops/SETTLED_FORKS.md, and the atlas negative
frontier. Legitimate stops ONLY: a kernel-checked no-go, or a genuine user-only decision (ask once,
keep shipping meanwhile).

NON-NEGOTIABLE: kernel-pure {propext, Classical.choice, Quot.sound}; zero sorry; zero new project
axioms without explicit user sign-off; zero native_decide; zero set_option maxHeartbeats in proof
bodies — a heartbeat wall means wrong proof architecture, decompose into `have` sub-lemmas.
Preemptive-strengthening BEFORE writing each statement. THIS PHASE'S PRIORITY DEFECT CLASS IS THE
COMPOSITE TAUTOLOGY: a ceiling that is not strictly sharper than its inputs is a restated hypothesis,
not a theorem — audit for that specifically every wave. Every composite states its attribution /
independence hypotheses explicitly; a silently fail-open composite is the worst defect available
here. RWA bounds are BOUNDS: no theorem may assert the co-rotating reduction as an equality, and
every calibration statement inherits the remainder. Carry-over lock from 6ED (kernel no-go
`spectrum_determines_multiband_model`): a spectrum- or determinant-level check cannot certify a
model's identification — any multi-band Hamiltonian written here also ships an eigenvector-level
invariant. Never push.

DONE when ALL of: (1) the roadmap's Wave 1, Wave 2 and Wave 3 AC are covered by shipped, root-imported
declarations; (2) `lake build SKEFTHawking.ExtractDeps` prints "Build completed successfully" in the
transcript, with zero sorry / axiom / native_decide / maxHeartbeats in the new modules; (3)
validate.py prints 50/50 (or a higher N/N) in the transcript; (4) the roadmap's Phase Definition of
Done is met — Status flipped to COMPLETE with a dated shipped-declarations list, the 6E* series-close
summary recorded, Inventory + counts refreshed; (5) the fresh-context skeft-qa:adversarial-reviewer
ran with ZERO BLOCKER findings surfaced in the transcript.
```

## Why this scope

"6EE Full strength" is the whole phase. That is defensible as one `/goal` here rather than open
scope, because the end state is measurable at the declaration level (three named modules against a
frozen per-wave AC) and the same shape closed 6EB, 6EC and 6ED in this session's run.

## Pre-arm plan-currency check (done 2026-07-30, before arming)

Two roadmap defects found and fixed in `b8f49547` — both would have thrashed turn 1:

1. The **bootstrap-reads list** — the literal first instruction a worker follows — pointed at
   `lean/SKEFTHawking/OpenSystems/DampedTwoLevel.lean`. No `OpenSystems/` directory exists; the
   modules are `SKEFTHawking.DampedTwoLevel` and `SKEFTHawking.LindbladSemigroup`.
2. "Absent → build: Kramers-degeneracy statement" **confirmed** for two-level control, but the repo
   already owns Θ-algebra in `MajoranaKramers` (Majorana/DIII) — a coordinate-don't-duplicate
   guardrail was added, in the shape of the 6CA/6ED QWZ one.

Also verified against `lean_deps.json`: all five named `Reuse` theorems resolve; `rotatingWave` /
`Rabi` are genuinely absent; both UNKNOWN-1 candidate routes exist (`MatrixBCHCubicMathlibPR` 2
decls, `MatrixExpLocalHomeomorphMathlibPR` 14).

**Atlas cross-check.** The KEYSTONE is `hyp:rokhlin_sigma_mod_16` (gates 11), which belongs to the 5q
topology lane, not 6EE. That is expected — 6EE is a fresh construction phase with no open gating
hypothesis — but it means the atlas cannot aim fan-out here, and the roadmap's own wave order is the
sequencing authority.

## Predecessor state at arm time

6EA, 6EB, 6EC and 6ED are all **COMPLETE** and reviewed at zero BLOCKER, so every mechanism floor
Wave 3 composes already exists. 6ED closed 2026-07-30 with all four waves (the Pareto clause was
overridden by operator directive).
