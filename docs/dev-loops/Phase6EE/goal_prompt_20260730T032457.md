# Goal prompt — Phase 6EE at full strength

**goal_id:** `20260730T032457` · **armed:** 2026-07-30 · **role:** solo · **mode:** lean
**arm_sha:** `b8f4954755ff2b0bfb6a0e2ca2026a13baeab50c`
**roadmap:** `docs/roadmaps/Phase6EE_Roadmap.md`
**notebook INDEX:** `docs/dev-loops/Phase6EE/LAB_NOTEBOOK_INDEX.md`

> **This file is the durable, crash-recoverable source of the `/goal` condition.** The gitignored
> marker at `.claude/dev-harness/managed/<session>.json` is only the fast-read copy. If the marker is
> lost, re-invoke `/skeft-qa:goal-prompt`, recover the condition below, and **reuse this same
> `goal_id`** — do not mint a new one for the same goal.

## The `/goal` condition (verbatim)

> **Authoring note (2026-07-30, operator correction).** The condition is read by a small evaluator
> agent at the end of each turn. That agent **cannot run tools and cannot open referenced files** — it
> judges only what is literally surfaced in the transcript. So the condition must be
> **acceptance-criteria centric and self-contained**: naming "the roadmap's Wave-1 AC" is *unevaluable*
> because the evaluator cannot read the roadmap. Posture, kernel-purity locks and anti-escape rules do
> **not** belong here; they are carried for the *implementer* by the `goal-prompt` always-on core, the
> `goal-dev` skill, this file, the notebook's Standing-Posture block, and `CLAUDE.md`. The first draft
> of this condition made exactly that mistake and was rewritten.

```
GOAL: Phase 6EE — the 6E* series capstone — shipped at full strength: three new Lean modules,
each root-imported into lean/SKEFTHawking.lean.

Judge ONLY what is visible in the transcript; you cannot open files. DONE requires ALL FIVE:

(1) THREE MODULES SHIPPED AND ROOT-IMPORTED, each stated as such in the transcript:
 (a) lean/SKEFTHawking/Control/RotatingWave.lean — the co-rotating (rotating-wave) reduction as a
 convention-explicit definition; an explicit-constant remainder INEQUALITY of Bloch-Siegert scale
 (order Omega/omega), never an equality; the rotation-angle calibration identity for the co-rotating
 propagator with a projected drive element; and norm_num witnesses at BOTH a parameter point where
 the remainder is small and one where it is order-unity.
 (b) lean/SKEFTHawking/Control/DriveCalibration.lean — the SIGNED projected drive element; transverse
 AND longitudinal duration/phase calibration identities carrying their fail conditions (zero matrix
 element, sign-inverted target) as explicit hypotheses rather than absorbed magnitudes; a
 matrix-element suppression bound with a strict-suppression witness; and a finite-dimensional
 Kramers-degeneracy statement for an antiunitary time reversal with T^2 = -1.
 (c) lean/SKEFTHawking/Control/CompositeReadoutCeilings.lean — end-to-end assignment-fidelity
 ceilings composing the relaxation, thermal, photon-budget (6EA), filtered-readout (6EB) and
 detector (6EC) floors, each with its attribution/independence hypotheses explicit; a
 worst-mechanism combined ceiling PLUS the strictly-sharper form under stated independence, with the
 DIFFERENCE between the two forms itself witnessed; and per-ceiling norm_num witness PAIRS — one
 budget point where the ceiling bites, one where it does not.

(2) The transcript shows `lake build SKEFTHawking.ExtractDeps` printing "Build completed
successfully", AND states that the new modules carry zero sorry, zero new project axioms, zero
native_decide, and zero set_option maxHeartbeats.

(3) The transcript shows validate.py printing 50/50 or a higher N/N.

(4) The transcript shows docs/roadmaps/Phase6EE_Roadmap.md updated to Status COMPLETE with a dated
shipped-declarations list and a 6E* series-close summary, and the Inventory + counts refreshed.

(5) The transcript shows the skeft-qa:adversarial-reviewer having run on this phase in a fresh
context, reporting ZERO BLOCKER findings.

Any of the five not visibly demonstrated in the transcript => NOT met.

(Implementer's source-of-truth, not an evaluation criterion:
docs/roadmaps/Phase6EE_Roadmap.md + docs/dev-loops/Phase6EE/LAB_NOTEBOOK_INDEX.md.)
```

## Why this scope

"6EE Full strength" is the whole phase. Defensible as one `/goal` rather than open scope, because the
end state is enumerated at the declaration level *inside the condition itself* — the evaluator never
has to consult the roadmap — and the same shape closed 6EB, 6EC and 6ED in this session's run.

## Where the posture lives (deliberately NOT in the condition)

Kernel-purity `{propext, Classical.choice, Quot.sound}`, zero sorry, no new project axioms without
sign-off, no `native_decide`, no `maxHeartbeats` in proof bodies, preemptive-strengthening, never
push — plus this phase's two specific locks:

- **The composite tautology is this phase's priority defect class.** A ceiling that is not strictly
  sharper than its inputs is a restated hypothesis, not a theorem. A silently *fail-open* composite is
  the worst defect available here.
- **RWA bounds are BOUNDS.** No theorem may assert the co-rotating reduction as an equality; every
  calibration statement inherits the remainder.
- **Carry-over from 6ED** (kernel no-go `spectrum_determines_multiband_model`): a spectrum- or
  determinant-level check cannot certify a model's identification. Any multi-band Hamiltonian written
  here also ships an eigenvector-level invariant.

These are recorded in the notebook's Standing-Posture block, which the implementer re-reads on every
re-anchor.

## Pre-arm plan-currency check (done 2026-07-30, before arming)

Two roadmap defects found and fixed in `b8f49547` — both would have thrashed turn 1:

1. The **bootstrap-reads list** — the literal first instruction a worker follows — pointed at
   `lean/SKEFTHawking/OpenSystems/DampedTwoLevel.lean`. No `OpenSystems/` directory exists; the
   modules are `SKEFTHawking.DampedTwoLevel` and `SKEFTHawking.LindbladSemigroup`.
2. "Absent → build: Kramers-degeneracy statement" **confirmed** for two-level control, but the repo
   already owns Θ-algebra in `MajoranaKramers` (Majorana/DIII) — a coordinate-don't-duplicate
   guardrail was added, in the shape of the 6CA/6ED QWZ one.

Also verified against `lean_deps.json`: all five named `Reuse` theorems resolve; `rotatingWave` /
`Rabi` are genuinely absent; both UNKNOWN-1 candidate routes exist (`MatrixBCHCubicMathlibPR` 2
decls, `MatrixExpLocalHomeomorphMathlibPR` 14).

**Atlas cross-check.** The KEYSTONE is `hyp:rokhlin_sigma_mod_16` (gates 11), which belongs to the 5q
topology lane, not 6EE. That is expected — 6EE is a fresh construction phase with no open gating
hypothesis — but it means the atlas cannot aim fan-out here, and the roadmap's own wave order is the
sequencing authority.

## Predecessor state at arm time

6EA, 6EB, 6EC and 6ED are all **COMPLETE** and reviewed at zero BLOCKER, so every mechanism floor
Wave 3 composes already exists. 6ED closed 2026-07-30 with all four waves (the Pareto clause was
overridden by operator directive).
