# Goal prompt — Phase 6EE at full strength

**goal_id:** `20260730T032457` · **armed:** 2026-07-30 · **role:** solo · **mode:** lean
**arm_sha:** `b8f4954755ff2b0bfb6a0e2ca2026a13baeab50c`
**roadmap:** `docs/roadmaps/Phase6EE_Roadmap.md`
**notebook INDEX:** `docs/dev-loops/Phase6EE/LAB_NOTEBOOK_INDEX.md`

> **This file is the durable, crash-recoverable source of the `/goal` condition.** The gitignored
> marker at `.claude/dev-harness/managed/<session>.json` is only the fast-read copy. If the marker is
> lost, re-invoke `/skeft-qa:goal-prompt`, recover the condition below, and **reuse this same
> `goal_id`** — do not mint a new one for the same goal.

## The `/goal` condition (verbatim)

```
GOAL: Phase 6EE — the capstone of the 6E* verified-device-physics-metrology series — is COMPLETE at
full strength. Source-of-truth: docs/roadmaps/Phase6EE_Roadmap.md +
docs/dev-loops/Phase6EE/LAB_NOTEBOOK_INDEX.md. Scope is settled; a stop-hook firing means continue
with the next increment of real work. Legitimate stops: a kernel-checked no-go, or a decision only
the operator can make.

END STATE — three new root-imported Lean modules close the series' two missing layers:

- lean/SKEFTHawking/Control/RotatingWave.lean: the co-rotating (rotating-wave) reduction as a
convention-explicit definition; the difference between the exact and reduced propagators bounded by
an explicit-constant INEQUALITY of Bloch-Siegert scale (order Omega/omega), the reduction never
asserted as an equality; the rotation-angle calibration identity for the co-rotating propagator with
a projected drive element; norm_num witnesses at both a parameter point where the remainder is small
and one where it is order-unity.

- lean/SKEFTHawking/Control/DriveCalibration.lean: the signed projected drive element; transverse and
longitudinal duration/phase calibration identities carrying their fail conditions (zero matrix
element, sign-inverted target) as explicit hypotheses rather than absorbed magnitudes; a
matrix-element suppression bound with a strict-suppression witness; a finite-dimensional
Kramers-degeneracy statement for an antiunitary time reversal with T^2 = -1.

- lean/SKEFTHawking/Control/CompositeReadoutCeilings.lean: end-to-end assignment-fidelity ceilings
composing the relaxation, thermal, photon-budget (6EA), filtered-readout (6EB) and detector (6EC)
floors, each carrying its attribution/independence hypotheses explicitly; a worst-mechanism combined
ceiling together with the strictly-sharper form under stated independence, and the difference between
the two forms itself witnessed; per-ceiling norm_num witness pairs — one budget point where the
ceiling bites, one where it does not.

FULL STRENGTH — every acceptance item above ships as stated rather than de-scoped, narrowed, or
stood in for by a placeholder or vacuous theorem; and every BLOCKER, MAJOR or IMPORTANT review
finding is remediated before close rather than deferred.

COMPLETION CRITERIA:
1. The three modules are shipped and root-imported into lean/SKEFTHawking.lean, covering the content
above.
2. lake build SKEFTHawking.ExtractDeps reports "Build completed successfully", and the new modules
carry zero sorry, zero new project axioms, zero native_decide, zero set_option maxHeartbeats.
3. validate.py reports 50/50 or a higher N/N.
4. docs/roadmaps/Phase6EE_Roadmap.md records Status COMPLETE with a dated shipped-declarations list
and the 6E* series-close summary; Inventory and counts refreshed with the Control/ family.
5. A fresh-context skeft-qa:adversarial-reviewer run on this phase reports zero BLOCKER findings.
```

## Scope

"6EE at full strength" is the whole phase — Waves 1, 2 and 3. That is a measurable end state rather
than open scope because the acceptance content is enumerated at the declaration level inside the
condition itself, and the same phase-sized shape closed 6EB, 6EC and 6ED.

## Where "full strength" comes from

Not a freehand term. It is the conjunction of:

- **`docs/WAVE_EXECUTION_PIPELINE.md`** — the 14 stages and their gates, "no skipping stages": Stage 5
  (zero sorry, counts match), Stage 7/12 (`validate.py` all checks pass), Stage 13 (fresh-context
  sweep shows zero BLOCKERs).
- **`docs/dev-loops/PRE_DECISIONS.md` PD-5** (operator-set) — BLOCKER/MAJOR/IMPORTANT findings are
  fixed in-session; deferral needs explicit operator sign-off asked for as a question. MINOR defaults
  toward clean.
- **`CLAUDE.md` quality standard** — correctness over expediency; never label approximate methods
  exact; no toy results.
- **The phase's own Definition of Done** in `Phase6EE_Roadmap.md`, whose fourth box additionally
  requires the `6E*` series-close summary.

## Phase-specific locks (implementer-side; deliberately not in the condition)

Kernel-purity `{propext, Classical.choice, Quot.sound}`, zero sorry, no new project axioms without
sign-off, no `native_decide`, no `maxHeartbeats` in proof bodies, preemptive-strengthening, never
push. Plus:

- **The composite tautology is this phase's priority defect class.** A ceiling that is not strictly
  sharper than its inputs is a restated hypothesis, not a theorem. A silently *fail-open* composite is
  the worst defect available here.
- **RWA bounds are bounds.** No theorem may assert the co-rotating reduction as an equality; every
  calibration statement inherits the remainder.
- **Carry-over from 6ED** (kernel no-go `spectrum_determines_multiband_model`): a spectrum- or
  determinant-level check cannot certify a model's identification. Any multi-band Hamiltonian written
  here also ships an eigenvector-level invariant.

These live in the notebook's Standing-Posture block, which the implementer re-reads on every
re-anchor.

## Plan-currency check (2026-07-30, before arming)

Two roadmap defects found and fixed in `b8f49547`:

1. The bootstrap-reads list pointed at `lean/SKEFTHawking/OpenSystems/DampedTwoLevel.lean`. No
   `OpenSystems/` directory exists; the modules are `SKEFTHawking.DampedTwoLevel` and
   `SKEFTHawking.LindbladSemigroup`.
2. "Absent → build: Kramers-degeneracy statement" holds for two-level control, but the repo already
   owns Θ-algebra in `MajoranaKramers` (Majorana/DIII). A coordinate-don't-duplicate guardrail was
   added, in the shape of the 6CA/6ED QWZ one.

Verified against `lean_deps.json`: all five named `Reuse` theorems resolve; `rotatingWave` / `Rabi`
are absent; both UNKNOWN-1 candidate routes exist (`MatrixBCHCubicMathlibPR` 2 decls,
`MatrixExpLocalHomeomorphMathlibPR` 14).

**Atlas cross-check.** KEYSTONE is `hyp:rokhlin_sigma_mod_16` (gates 11), in the 5q topology lane —
6EE is fresh construction with no open gating hypothesis, so the atlas cannot aim fan-out here and the
roadmap's wave order is the sequencing authority.

## Predecessor state

6EA, 6EB, 6EC and 6ED are all COMPLETE and reviewed at zero BLOCKER, so every mechanism floor Wave 3
composes already exists. 6ED closed 2026-07-30 with all four waves.
