# /goal — Phase 5q.G (genuine unconditional Pin⁺ ℤ/16; clean-room restart)

**goal_id:** `20260624T151857` · **role:** solo · **session:** `c8db1624-e44d-4e57-b887-669e6416ff9c`
**Armed:** 2026-06-24 · **arm_sha:** `997eceed901fab6c0e9a1e4207bc2a18083f7463`
The gitignored marker is the fast-read copy; **this tracked file is the durable, crash-recoverable source** (spec A.5).

**Why 5q.G (not a 5q.F re-arm):** 5q.F's in-flight L2 state was re-seed-polluted — the marker's frozen
CURRENT-STATE re-injected the banned kronecker route every compaction (committed as bug example `5d7eb484`).
5q.G is a clean restart seeded ONLY with source/git/kernel-VETTED bedrock + correctly-typed no-gos. The G1
boundary audit (done 2026-06-24, interactive) found the exact-chain χ unreachable by construction; the
corrected target (the ∈-boundaries PAIRING close, cap spine) lives in the live FRONTIER, NOT in this condition.

**Authoring discipline (the 5q.F lesson, baked in):** this condition holds **durable rules only** — no
current-state, no "do X first" kickoff, no engine names as a close-path, **no turn-cap** (the Haiku evaluator
can't count turns; the v4.2 stall-detector + `skeft-qa:coach` are the non-convergence backstop). The transient
next-step lives in the recomputed FRONTIER. The granular G1→G6 acceptance criteria are **inlined** because the
done-evaluator sees only the transcript, not the roadmap/notebook.

---

## Operative `/goal` condition (verbatim — pass to `/goal`; mirrored in the marker)

```
GOAL: Phase 5q.G — drive the GENUINE unconditional Ω₄^{Pin⁺} ≅ ℤ/16 (and Ω₅ ≅ ℤ/16) via the geometric w₂/Wu tower, dropping every disclosed binder and the posited carrier. Advance gates G1→G6; the live gate + next brick are in the FRONTIER (recomputed each turn — NO current-state in this condition). KERNEL-PURE.

SOURCE-OF-TRUTH (re-read after ANY compaction, BOTH end-to-end): roadmap docs/roadmaps/Phase5qG_GenuineUnconditional_Roadmap.md + notebook Lit-Search/Phase-5qG/LAB_NOTEBOOK_INDEX.md (FRONTIER + settled-forks register; LAB_NOTEBOOK.md on demand) + docs/dev-loops/SETTLED_FORKS.md + docs/dev-loops/PRE_DECISIONS.md.

FIRST_ACTION every turn: grep SETTLED_FORKS.md BEFORE any "is X impossible / needs a banned thing / re-derive whether X works" reasoning; then do the next brick the FRONTIER names. THE GRIND IS THE WORK — one kernel-pure brick/turn. Lateral motion (route-shop / re-derive a settled fork / re-open a banned route / "should I ask?") IS the failure mode.

ALTITUDE LOCK (by ALTITUDE not name): the L2 close SPINE stays at CAP altitude (cap-Leibniz); σR joins via the pairing adjunction (relKroneckerH_relCohomMvConnecting) as a SUB-step / the final ∈-boundaries discharge, NEVER as the spine. Any framing of _of_crossRealization / kronecker_pd_fold_fund / of_hcup_linked as "the close spine" or a "whnf-dodge breakthrough" IS the re-seed tell → STOP (SETTLED_FORKS kronecker-altitude-respine). A tactical whnf is dodged AT cap altitude (def-head-match), never by re-spining. VERIFY state by ROUTE/ALTITUDE, not "commit present."

KERNEL-PURITY: axioms EXACTLY {propext, Classical.choice, Quot.sound}; NO new axiom/sorry/native_decide/maxHeartbeats; existing STATEMENTS unchanged. Commit GREEN shards; NEVER push.

DONE — the GOAL is met only when ALL of these hold AND are SURFACED in the transcript (the done-evaluator sees ONLY the transcript, not the roadmap/notebook):
1. (G1/L2) subHomConnecting_openDuality is zero-sorry, in the built library, with #print axioms = {propext,Classical.choice,Quot.sound}; PoincareDual4Mid AND PoincareDual4Lo are theorems (no longer hypothesis-structures).
2. (G2-3/L3-L4) wuW2_eq_zero_iff is instantiated on the genuine PD from (1); the faithful Pin⁺ grade is constructed and ker(faithful abkGrade) = ⊥ is proved kernel-pure (the Ω^O floor collapsed via w₂ bordism-invariance).
3. (G4/L5) the faithful grade's surjectivity / ABK ≤16 completeness is proved kernel-pure, and the Guillou–Marin hGM relation is discharged (no posited signature=1).
4. (G5) a full-carrier DataBordismGrp(pinPlusData I) ≃+ ZMod 16 (faithful grade, ker=⊥) is proved; Ω₅ ≅ ℤ/16 is recast on this genuine carrier; Omega4PinPlusBordism (signature posit) and the adamsAbutment def are no longer load-bearing for any 16-result; CommonOrigin.sixteen_convergence_* carries NO SmithInflow / pin4_abutment / PinPlusBordismLandmark binder (show the signatures in-transcript).
5. (G6) rm -rf .lake/build && lake build SKEFTHawking.ExtractDeps clean (exit 0); validate.py prints N/N (≥ its check count); #print axioms = {propext,Classical.choice,Quot.sound} on every apex theorem in 1–4; a fresh skeft-qa:adversarial-reviewer (separate context) returns ZERO BLOCKER; counts/inventory/INDEX synced.

POSTURE: scope SETTLED — do the next FRONTIER brick THIS turn; a stop-hook firing is a GO signal, never a cue to stop/hold/re-scope/hand back. Legitimate stops ONLY: a kernel-checked no-go (carry ONE disclosed Prop + discharge plan) or a genuine user-only decision (coach / ask once, keep shipping).
```
