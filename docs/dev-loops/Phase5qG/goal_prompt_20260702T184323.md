# /goal — Phase 5q.G at full strength (all worktree slots; success = merged to main)

**goal_id:** `20260702T184323` · **role:** solo · **session:** `cf78a84e-3fb5-408c-a308-af290c366a08`
**Armed:** 2026-07-02 · **arm_sha:** `9bdf443b48a922394ecef9dea4ad652541576c0b` · **mode:** lean
The gitignored marker is the fast-read copy; **this tracked file is the durable, crash-recoverable source** (spec A.5).

**2026-07-02 amendment (same goal_id — user re-scope before launch):** the original composition restricted
the venue to wt2 ("worktree 2" in the user's ask referred to *where the most recent L2 work lives*, not a
lock). The user cleared **all three worktree slots** for this goal and set **success = everything merged back
to `main` after passing adversarial reviews**. Merging slot branches into local `main` is therefore
authorized for this goal (pushing remains user-only).

**Relation to `goal_prompt_20260624T151857.md`:** that arm was the main-loop clean-room restart of 5q.G.
This is a new goal instance: the L2 line's committed, user+Aristotle-authorized wt2 apex architecture is the
live line, and the tactical anchor is the **Phase-5qF/L2 lab notebook**
(`Lit-Search/Phase-5qF/L2/LAB_NOTEBOOK_INDEX.md` — its consolidated FRONTIER head is the resume point).
The G1→G6 DONE criteria carry over in substance from the prior arm.

**Authoring discipline (unchanged):** durable rules only — no current-state, no next-brick, no engine
names as a close-path, no commit SHAs as state (the one SHA below appears only inside a BAN), no turn-cap.
The transient next-step lives in the notebook FRONTIER, recomputed each turn.

---

## Operative `/goal` condition (verbatim — pass to `/goal`; mirrored in the marker)

```
GOAL: Phase 5q.G at FULL STRENGTH — the GENUINE unconditional Ω₄^{Pin⁺} ≅ ℤ/16 (and Ω₅ ≅ ℤ/16) via the geometric w₂/Wu tower, dropping every disclosed binder and the posited carrier. Advance gates G1→G6; the live gate + next brick live in the notebook FRONTIER (recomputed each turn; NO current-state here). KERNEL-PURE. SUCCESS = all work merged back to main after passing fresh-context adversarial review.

VENUE & INTEGRATION: Lean dev runs in worktree slots wt1/wt2/wt3 (SK_EFT_Hawking/.claude/worktrees/wtN, branch worktree-wtN, server mcp__lean-lsp-wtN__*). Solo on one slot for a tightly-coupled chain; fan out via /reset-slot + skeft-qa:lean-worker only when the DAG genuinely branches (≤2 workers concurrent). Never reset --hard/clean a slot; stage ONLY explicit own paths (never add -A/commit -a); commit GREEN increments on the slot branch. Merging slot branches into LOCAL main IS authorized (--ff-only where possible; re-run the full gate after each merge); NEVER push (user action).

SOURCE-OF-TRUTH (re-read after ANY compaction): roadmap docs/roadmaps/Phase5qG_GenuineUnconditional_Roadmap.md (newer of main/wt2 copy) + notebook Lit-Search/Phase-5qF/L2/LAB_NOTEBOOK_INDEX.md (consolidated FRONTIER head + its ⛔ settled-forks/DECISIONS register; LAB_NOTEBOOK.md shards on demand) + docs/dev-loops/SETTLED_FORKS.md + docs/dev-loops/PRE_DECISIONS.md.

FIRST_ACTION every turn: read the notebook INDEX's FRONTIER + ⛔ register BEFORE any 'is X impossible / re-derive whether X works' reasoning; then build the next FRONTIER brick. THE GRIND IS THE WORK — one kernel-pure brick/turn. Lateral motion (route-shop / re-derive a settled fork / re-open a banned route / 'should I ask?') IS the failure mode.

ARCHITECTURE LOCK: the committed wt2 apex architecture for the L2 close is SETTLED (user+Aristotle-authorized) — never re-architect the apex chain; never re-open Route C (mathlib SnakeInput; user-banned); never re-seed routes recorded banned in SETTLED_FORKS / the notebook ⛔ register (incl. the a79f1912-reverted cup bricks). The STATEMENT of subHomConnecting_openDuality stays byte-identical — only proof bodies change.

KERNEL-PURITY: axioms EXACTLY {propext, Classical.choice, Quot.sound} on every apex theorem; NO new axiom/sorry/native_decide/maxHeartbeats; existing statements unchanged.

DONE — met only when ALL hold AND are SURFACED in the transcript (the evaluator sees ONLY the transcript):
1. (G1/L2) subHomConnecting_openDuality is zero-sorry in the built library with #print axioms = {propext,Classical.choice,Quot.sound}; PoincareDual4Mid AND PoincareDual4Lo are theorems (no longer hypothesis-structures).
2. (G2-3/L3-L4) wuW2_eq_zero_iff is instantiated on the genuine PD from (1); the faithful Pin⁺ grade is constructed and ker(faithful abkGrade) = ⊥ proved kernel-pure.
3. (G4/L5) faithful-grade surjectivity / ABK ≤16 completeness proved kernel-pure; the Guillou–Marin hGM relation discharged (no posited signature=1).
4. (G5) a full-carrier DataBordismGrp(pinPlusData I) ≃+ ZMod 16 (faithful grade, ker=⊥) is proved; Ω₅ ≅ ℤ/16 recast on this genuine carrier; Omega4PinPlusBordism (signature posit) and adamsAbutment no longer load-bearing for any 16-result; CommonOrigin.sixteen_convergence_* carries NO SmithInflow / pin4_abutment / PinPlusBordismLandmark binder (signatures shown in-transcript).
5. (G6) a fresh skeft-qa:adversarial-reviewer (separate context) returns ZERO BLOCKER ZERO MAJOR, surfaced in the transcript; THEN all slot work is merged into local main, and ON MAIN: rm -rf .lake/build && lake build SKEFTHawking.ExtractDeps exits 0 and validate.py prints N/N, both surfaced; counts/inventory/notebook INDEX synced; NOTHING pushed.

POSTURE: scope SETTLED — do the next chunk of FRONTIER work THIS turn; a stop-hook firing is a GO signal, never a cue to stop/hold/re-scope/hand back. Legitimate stops ONLY: a kernel-checked no-go (carry ONE disclosed Prop + discharge plan) or a genuine user-only decision (coach / ask once, keep shipping meanwhile).
```
