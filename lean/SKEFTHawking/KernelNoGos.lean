/-
# Kernel-verified NO-GO fence — the consolidated register (GENERATED)

**DO NOT HAND-EDIT.** This module is regenerated by `scripts/gen_kernel_nogos_module.py`
from the single source of truth `KERNEL_NOGO_REGISTRY` (`src/core/constants.py`) and is kept
fresh by the `sync_manifest` staleness edge (pre-commit + `/sync`). It gathers EVERY
kernel-checked no-go of the project into one place so future work — and fresh-context
workers — see the provably-false forks that must NEVER be re-derived, and so the compiler
itself enforces their presence: if a backing refutation theorem is renamed or removed, this
module fails to build (the fence is down → investigate before proceeding).

Companion registers: the machine `KERNEL_NOGO_REGISTRY` (Python; feeds the atlas negative
frontier + `validate.py --check nogo_substrate_integrity`) and the prose
`docs/dev-loops/SETTLED_FORKS.md` (policy/route bans, not kernel-encodable).

## The register (fork-id → FALSE statement → backing theorem[s])

1. `fk-mod2-membrane-index-cannot-reach-nonorientable-stratum` [refutation]
   The Freedman-Kirby MOD-2 membrane index D.F + O(D) + d(C) -- blueprint Route A nodes [G2]/[Q1]/[Q2], FK
   Lemma 2.6.1 -- produces the pin-minus class of a NONORIENTABLE characteristic surface, so building out
   the membrane layer suffices to supply `hgm` on the whole char-pair carrier. FALSE (2026-07-28, wt2,
   lead-verified: I re-derived the polarization step and checked the RP2 instantiation myself). Any mu :
   (iota -> ZMod 2) -> ZMod 2 satisfying the [Q2] refinement identity mu (x+y) = mu x + mu y + B x y FORCES
   B v v = 0 for every v: put x = y = v, use mu 0 = 0 and 2 * mu v = 0 in ZMod 2. So a mod-2 index can only
   refine an ALTERNATING form, i.e. it reaches the ORIENTABLE stratum and kernel-checkably only that one.
   Instantiated at stdQuadratic 1 -- the RP2 surface half of the (RP4, RP2) generator, where B 1 1 = 1 --
   `no_mod2_index_on_rp2` is a flat refutation by `decide`. NOT FIXABLE BY MORE MEMBRANE WORK: the
   obstruction is the polarization identity itself, not a missing lemma or a weak construction. CONSEQUENCE
   FOR ROUTE A: reaching the nonorientable stratum requires the Guillou-Marin Z/4-RESOLUTION index. **
   RESOLVED 2026-07-28 (3e15199c): the split is now PINNED from the primary source, read directly by the
   lead -- Klug arXiv:2011.12418v3 SECTION 6 p. 18, following Matsumoto [Mat86] and Guillou-Marin [GM77].
   NOTE the earlier version of this entry cited section 2; that was WRONG -- section 2 is the ORIENTABLE
   mod-2 case and Klug states the orientability assumption there explicitly. The split is e_F(x) = n(D) +
   2(D.F) + 2 d(C) (mod 4), coefficients 1,2,2, with the half-integrality living in n(D) -- the framing,
   counting right-handed HALF-twists under the convention that (r_D, C, e1, e2) agrees with the ambient
   orientation of X -- and NOT in a doubled D.F, so the shape this entry warned against guessing was indeed
   the wrong one. Built as a construction in GMZ4MembraneIndex.lean; corpus note at
   Lit-Search/Phase-5qH/Klug-GuillouMarin-Z4-membrane-index-coefficient-split.md. THIS REFUTATION IS
   UNAFFECTED: the mod-2 index still cannot reach the nonorientable stratum, and
   `GMZ4MembraneIndex.orientable_framing_forces_alternating` re-derives exactly this obstruction whenever
   the framing is a whole-twist count -- the kernel-checked statement that the half-twists are the
   load-bearing part. ** ALSO SETTLED HERE (a second, independent refutation): with the normal-framing
   comparison as FREE data, [Q1] in its natural universal shape is FALSE --
   `indexWellDefined_false_of_framedMembrane` shows the 2-pi twist leaves the membrane untouched and flips
   the index. This is the same failure mode `CharSurfaceFKVacuity` found one level up, reappearing at the
   framing; the repair is a rigidity tie (NormalFramingTie), and `not_tied_twist` plus
   `trivialTie_rigid_but_uninhabited` show both that the repair bites and that inhabitedness is separately
   load-bearing. The same theorem is the P5 repackaging check: the membrane layer is strictly MORE data than
   the enhancement it produces, which a field-carrying MembraneIndexData could never be. SCOPE: this refutes
   the mod-2 index on the nonorientable stratum and the free-framing [Q1]; it does NOT refute [FK] itself,
   nor the mod-2 index on the orientable stratum, where it is exactly right.
   backing: `no_mod2_index_on_rp2`, `refines_forces_alternating`, `not_exists_index_of_B_self_ne_zero`, `indexWellDefined_false_of_framedMembrane`

2. `k3-e1-forall-orientation-routes-are-vacuous` [refutation]
   A geometric hypothesis about the welded Kummer K3 may be quantified over ALL integral orientations -- the
   shape `forall o : IntOrientation KummerK3, <Gram / unimodularity / signature / cap-dual-span statement at
   o>` used by EVERY published entry point into `Nonempty KummerK3E1Atoms`. FALSE (2026-07-28, solo lead
   build, kernel-checked). TWO INDEPENDENT DEFECTS, both rooted in `IntOrientation` carrying only
   {fundClass, redCompat}. (1) SCALING (the destructive one, `IntOrientationScaling`): redCompat says the
   mod-2 reduction of fundClass is the canonical class; reduction is Z-linear into a ZMod 2-module, so `n .
   fundClass` satisfies it for EVERY ODD n, and `IntOrientation.smulOdd` constructs it. Scaling the
   fundamental class scales the intersection form, hence the Gram, hence multiplies det by n^rank. At n = 3,
   rank 22: a determinant cannot be a unit both before and after, so `forall o, IsUnimodular (interMatrix
   [M]_o B)` is false for ANY carrier with an inhabited IntOrientation and positive rank. This is a defect
   of the STRUCTURE, not of K3. (2) SIGN (`IntOrientationReverse` / `KummerK3ForallOrientationFalse`):
   reversal is a genuine second orientation (redCompat cannot see a sign) and negates the form, so any
   forall-o hypothesis carrying a sign-bearing Gram demands both G and -G. This one is IRREDUCIBLE -- no
   strengthening of IntOrientation can rescue it. WHAT IS REFUTED AT K3 (an orientation exists
   unconditionally via KummerK3SeamTransport.nonempty_intOrientation_kummerK3_uncond, so all of this is
   unconditional): `pdInput` (hence `KummerK3E1Residuals` is UNINHABITABLE), `hsig` (forall o, latticeSig
   (kummerK3Gram o) = -16), `hgram`, `hk3`, `hfam`, and `hgeo` (the terminal cap-dual datum, whose spanning
   half alone already yields pdInput). Therefore `nonempty_kummerK3E1Atoms_of_pd`, `_of_gram`, `_of_hk3`,
   `_of_stable`, `_of_stable_of_geometric`, and all five `KummerK3E1Unconditional` entry points are
   VACUOUSLY TRUE -- provable, but undischargeable. WHAT IS NOT REFUTED: `KummerK3E1Atoms` itself bundles
   ONE orientation with PD against that orientation; that shape is correct and reachable. THE REPAIR (built
   in the same turn, `KummerK3E1Repair` section 2): every route restated at a single supplied orientation --
   `nonempty_kummerK3E1Atoms_at`, `_of_hk3_at`, `_of_capDualSpan_at`, `kummerK3_hk3_at`. These are NOT
   refutable by the argument above (one orientation carries no reversal and no rescaling). The repair also
   exposes an over-specification the forall-o shape had hidden: integral PD at ONE orientation is by itself
   the whole E1 atom triple -- no evenness, no Gram, no lattice theory -- so the Gram lane serves the sigma
   = -16 congruence that OTHER consumers want, not the E1 atoms. DO NOT re-derive a forall-o geometric
   hypothesis on this carrier; pin the orientation.
   backing: `not_forall_intOrientation_isUnimodular`, `not_forall_intOrientation_pdInput`, `not_kummerK3E1Residuals`, `not_forall_latticeSig_neg16`, `not_forall_gram`, `not_forall_geoData`, `forall_orientation_kummerSubForm_family_false`

3. `gm-pin-torsor-free-shadow-weaker-than-van-der-blij` [structural_forcing]
   The pin-minus-FREE content of the Guillou-Marin congruence -- the part of GM that survives quotienting by
   the H^1(F;Z/2) enhancement torsor, i.e. what one can assert about sigma WITHOUT choosing a pin-minus
   structure on the characteristic surface -- combined with the lattice input the project already owns (van
   der Blij, 8 | sigma), suffices to prove Rokhlin (16 | sigma) on the atlas KEYSTONE
   hyp:rokhlin_sigma_mod_16. FALSE (2026-07-27, wt2, lead-verified: I read the ceiling statement and the
   three supporting reductions myself). WITNESS: sigma = 8 satisfies 8 | sigma AND satisfies the full
   torsor-invariant GM shadow for every beta = 0 quadratic refinement Q and every shift w, yet not (16 | 8).
   MECHANISM, pinned exactly: mod 4 the doubled Brown invariant IS shift-invariant
   (reduce16to4_doubleBrown_shift), mod 8 it is NOT (reduce16to8_doubleBrown_not_shift_invariant exhibits
   residues 2 vs 14 on the two RP^2 enhancements), and doubleBrown_stdShift3_injective gives four classes
   with four distinct residues, so the enhancement ambiguity subgroup is EXACTLY 4*Z/16. Consequence: the
   torsor-free shadow of [FK] carries ZERO marginal content over the lattice bound already in tree -- it is
   strictly weaker, not merely equal. SCOPE: this refutes the pin-minus-free route to hdvd; it does NOT
   refute [FK] itself, whose content lives precisely in the pin-minus class w. The admissible statement
   layer is therefore a predicate on w (GMTorsor / PinTorsor.gmrelation_shift_iff), not on sigma-arithmetic.
   Do NOT dispatch a search for a pin-structure-free Rokhlin route; the geometry must enter through the
   enhancement.
   backing: `lattice_plus_torsor_free_shadow_cannot_prove_rokhlin`, `reduce16to4_doubleBrown_shift`, `reduce16to8_doubleBrown_not_shift_invariant`, `doubleBrown_stdShift3_injective`

4. `gm-triple-level-layer-is-decided-by-sigma-alone` [structural_forcing]
   There exists a statement layer for [FK]-at-general-sigma, phrased as a predicate P on the INVARIANT
   TRIPLE (sigma, F.F, 2*beta), that (i) follows from Guillou-Marin and (ii) suffices for Rokhlin at spin,
   yet whose spin specialization is strictly weaker than 16 | sigma -- i.e. that is extensionally
   non-circular. FALSE (2026-07-27, wt2, lead-verified: I read triple_layer_forcing and re-derived both
   directions myself before accepting it). Any such P satisfies P (sigma,0,0) <-> 16 | sigma for EVERY
   sigma: (ii) gives the forward direction, and (i) gives the reverse because at the spin triple the GM
   defect vanishes exactly when 16 | sigma. No shape escapes -- bundled, existential, universal, relational
   or bordism-flavoured. CORRECTION THIS SUPERSEDES: the earlier fork
   gm-empty-surface-specialization-is-circular stated the defect as the BICONDITIONAL between the layer
   hypothesis and 16 | sigma. That criterion is now known to be unsatisfiable, because the TRUE
   Guillou-Marin theorem has the same property at the spin triple; judging a candidate layer by it rejects
   everything including the real theorem. The correct criterion is INTENSIONAL -- what DATA the hypothesis
   is a predicate on (a pin-minus class and a surface, versus sigma-arithmetic).
   forcing_collapse_is_spin_specific gives the forcing teeth by showing the collapse is specific to the spin
   locus rather than a degeneracy of triple-level predicates generally. ALSO SETTLED: the
   bordism-by-generators route at triple level -- generators (CP^2,CP^1) = (1,1,0) and (RP^4,RP^2) =
   (0,-2,2), the latter tied to the in-tree GM_rp4 by rp4_eq_tripleOf -- is likewise decided by sigma alone
   (spin_triple_mem_closure_iff_sixteen_dvd); it becomes a genuine reduction ONLY when typed over manifolds,
   where the char-pair bordism group maps NON-SURJECTIVELY onto triples, and that non-surjectivity is the
   entire geometric content. SCOPE: this is a forcing result about a design space, NOT a refutation of [FK]
   or of hdvd.
   backing: `triple_layer_forcing`, `no_triple_layer_escapes_the_biconditional`, `spin_triple_mem_closure_iff_sixteen_dvd`, `forcing_collapse_is_spin_specific`

5. `freeze-a-atoms-satisfiable-with-zero-geometry` [structural_forcing]
   Building the Freeze-A primitives AS STATED -- SpinSigmaPresentation.HandleTradeCobordism (the raw
   handle-trade cobordism, primitive 1 of the freeze-atoms-not-composable-from-sigma-trace honest floor) and
   HyperbolicBase (primitive 2, the rank-0 nullbordism) -- discharges genuine manifold-surgery content and
   therefore advances hA. FALSE (2026-07-27, wt3, lead-verified: I read the Sylvester chain and re-ran the
   axiom prints myself). BOTH atoms are satisfiable with ZERO geometry. Root cause: SpinSigmaPresentation
   carries rank and form as FREE disclosed data, tied to the manifolds only through sig_eq -- i.e. only
   through the signature -- so nothing forces rank to behave like b_2. Concretely, collapsedPresentation
   sets rank = 2 at the s2s2 slot and 0 elsewhere; the handle-trade hypothesis then fires only at the slot
   with m = 0, and the conclusion is met by the UNIT CYLINDER with p-prime = empty via (mapCylinder
   (Diffeomorph.sumEmpty ..)).symm. No handle is attached anywhere. HyperbolicBase is met vacuously (a
   presentation may contain no rank-0 manifold at all). NOT AN ARTEFACT OF ABSTRACT xi:
   collapsedSphereProd_handleTradeCobordism lands the same dodge on the GENUINE carrier spinEmptyData prov
   with the genuine sphereProdSpinElement prov in the s2s2 slot. NOT ITSELF VACUOUS:
   collapsedPresentation_hypothesis_fires proves the antecedent genuinely fires. SCOPE -- this refutes the
   ATOMS AS STATED, NOT the route: both dodges have rank <= 2 and sig = 0, and that is structurally fatal
   with no new hypothesis, via abs_sig_le_rank (Sylvester |sigma| <= b_2 lifted through sig_eq) ->
   sixteen_le_rank_of_generator (sigma(K3) = -16 forces 16 <= rank g) -> no_generator_of_rank_le_two. So a
   surgery foundation must state primitive (1) on an ALREADY-CONSTRAINED presentation or its theorem is met
   by a cylinder. RE-SCOPING CONSEQUENCE (handleTradeConclusion_on_split): the primitive-(1) conclusion is
   FREE on the split locus q + S2xS2 (the commBor cylinder is the required cobordism), so the irreducible
   Benedetti content is REDUCTION TO THE SPLIT LOCUS, not production of a cobordism -- aim future work
   there. CAUTION: FaithfulRank (b_2(empty) = 0 plus additivity) refutes both dodges but is a SOUNDNESS
   SIDE-CONDITION, not a claimed-inhabited structure; no abstract inhabitant is constructed and none should
   be assumed.
   backing: `collapsedPresentation_handleTradeCobordism`, `collapsedSphereProd_handleTradeCobordism`, `constRankTwoSphereProd_hyperbolicBase`, `no_generator_of_rank_le_two`

6. `hker-single-witness-extraction-is-equivalent-to-gluing` [structural_forcing]
   The single tethered null-bordism witness that hker needs -- extracting one IsT2DataBordant from
   T2DataBordismGrp.mk = 0 -- can be obtained by a cleverer use of the quotient, a reformulation of the
   extraction, or any route that AVOIDS proving bordism gluing (transitivity). FALSE (2026-07-27, wt2,
   lead-verified: I read both directions of the iff and re-checked refl/symm are binder-free).
   gluesT2_iff_singleWitness proves the extraction is EQUIVALENT to gluing, not merely implied by it: the
   reverse direction routes the two hypotheses through Quot.sound and back, so any means of performing the
   extraction for all p,q IS a proof of transitivity. Combined with refl (reflCylinder) and symm (symmBor)
   being UNCONDITIONAL in tree, gluing is the SOLE missing equivalence law. Do not dispatch a search for a
   shortcut; the geometric content is irreducible. SCOPE: this is a forcing result, NOT a refutation --
   GluesT2 is TRUE mathematically (every bordism admits a collar); it is an open unformalized node, not a
   dead fork. The wall is precisely typed: SeamGlueChart bundles the exactly-four residual Bordism fields
   (chartW, mfdW, he_smooth, he_boundary) and Bordism.ofSeamGlueChart assembles the composite from it plus
   the eight generically-constructed pieces, so 8 of 12 fields are already discharged and the residue is
   charts-at-the-seam = the collar neighbourhood theorem. Mathlib names the same gap in its own
   Geometry/Manifold/Bordism.lean and has neither the collar theorem nor boundary-is-a-submanifold. ALSO
   SETTLED HERE: the Hausdorff refinement is FREE along the weld (t2Space_glueCarrier, proper-map route), so
   the NonHausdorffBordismCollapse repair adds no obstruction to gluing.
   backing: `gluesT2_iff_singleWitness`, `refl`, `symm`

7. `hcolD-same-carrier-collapse-refuted-on-wu-witness` [structural_forcing]
   The hcolD rank-zero collapse to an empty characteristic surface can be realized WITHOUT changing the
   carrier -- i.e. some CharPairStrBundled structure on a fixed closed 4-manifold s that carries a Wu
   witness (a degree-2 class a0 with nonzero PD-paired self-cup-square, mu (cupH24 a0 a0) != 0) has an EMPTY
   characteristic surface, so the terminal KT section-5 move could be performed in-place on s (or reached by
   a cap inside M x I). FALSE (2026-07-27, wt2, lead-verified: I read both proofs and re-derived the
   non-vacuity myself, not taken on the report). On any such carrier there is NO characteristic-pair
   structure with empty surface at all: emptiness makes the surface cohomology subsingleton, so the pushed
   surface class is forced to 0, contradicting the witness via the banked B0 pushforward
   (surfClass_pushforward_witness). Dually, endpoint_wuNullCarrier shows every collapse endpoint is
   necessarily Wu-null. Together the terminal move is FORCED to change the carrier, and the
   reflexive-cylinder shortcut that makes the already-empty fibre free provably cannot reach the Wu-witness
   sector. NON-VACUITY (both directions checked, since a one-sided check would not have caught a dressed-up
   tautology): the hypothesis is satisfiable in-tree -- S2xS2 with a0 = e1+e2 has self-intersection 2 != 0
   (also true of CP2, K3) -- and the conclusion is not free, since empty-surf structures are genuinely
   realizable (they are exactly what the empty-Sigma spin sector and rankZeroCollapseDatum_of_isEmpty_surf
   construct). SCOPE (not overclaimed): this refutes the SAME-CARRIER route and the in-M-x-I cap; it does
   NOT refute hcolD itself, which remains open via KT characteristic extension (M,F) -> (M1,F1) with V1
   normal bundle trivialised, realised as SurgeredEndDatum + weld. Also do NOT dispatch the "one sphere, one
   index-3 handle" variant: rank 0 plus hchar do not force normal Euler number e = 0 in the required
   generality.
   backing: `no_isEmpty_surf_of_wu_witness`
   ⚠ UNRESOLVED backing (not found in-tree — check the registry): `SKEFTHawking.PinPlusKTRankZeroCollapseSupply.RankZeroCollapseDatum.endpoint_wuNullCarrier`

8. `capstone-binary-partition-detection-uninhabitable` [structural_forcing]
   The connected capstone's hasClass can be driven through the binary complementary partition {U,
   U-complement} at U := range fromCyl -- i.e. PinPlusTraceCapstoneRelFund.CapstoneRelFundPartitionDatum is
   inhabitable at the field docstring's intended U. FALSE (2026-07-27, wt3, lead-verified by #print axioms
   on all three backings): at a core seam point the closed cylinder piece is FACE-FLAT (its chart there is a
   half-space / star-convex chart), so its local homology H_(k+2)(sub U, sub U minus x) VANISHES IN EVERY
   DEGREE (faceLocalHomology_zero_of_starConvexChart -- degree-general, decoupled from the chart dimension);
   hence restrictBd of any class factors through the zero group and RestrictsToRelGenOn fails for EVERY
   alphaU and EVERY gen (not_restrictsToRelGenOn_of_faceVanish, specialised at the seam by
   not_restrictsToRelGenOn_cylRange_at_seamCore). PROMOTED FROM PROSE 2026-07-27: the prose entry
   (2026-07-16) recorded this as kernel-encodable-but-deferred pending the boundary-face local-homology
   lemma; that lemma is now formalized in both models (closedBall_faceLocalHomology_zero for the disk,
   cylTopFace_localHomology_zero for the cylinder), so the ban is now machine-enforced. NON-VACUITY: the
   face hypothesis carries the content, not the degree -- exists_ne_zero_closedBall_interiorLocalHomology
   and exists_ne_zero_cylInteriorLocalHomology exhibit NONZERO local homology at an INTERIOR point of the
   SAME carrier in the SAME degree. SCOPE (not overclaimed): this refutes the datum at U = range fromCyl,
   the intended choice; the fully general "every binary complementary partition of a connected W" statement
   is NOT claimed here. The partition/clopen-split engines remain VALID for their disconnected-cylinder uses
   (clopen pieces have no interior frontier) -- the ban is ONLY their application to the connected capstone.
   backing: `not_restrictsToRelGenOn_cylRange_at_seamCore`, `not_restrictsToRelGenOn_of_faceVanish`, `faceLocalHomology_zero_of_starConvexChart`

9. `collar-pair-hdetAB-one-sided-congruence-routes-dead` [structural_forcing]
   CollarPairCoreRow.hdetAB can be discharged by the engine layer's advertised one-sided congruence route --
   i.e. write qGen = p + e with p the SeamCollarChainDatum collar chain and e an away-error supported off
   the seam point, discard e, and detect with p via relClassOf_eq_of_congr, taking p or e to be one of the
   two pieces already in hand (the pushed cylinder prism or the pushed disk chain). FALSE (2026-07-27, wt3,
   lead-verified by #print axioms on all three backings): at a core seam point the pushed cylinder prism is
   SUPPORTED at every seam point (cylPush_notMem_compl_seamPoint), so it can never be the away-error e; its
   boundary is not even a {seamPoint a}-complement chain (collarChain_ne_cylPush), so it can never be the
   collar chain p; and the disk piece cannot be p either (collarChain_ne_diskPush, from the banked
   diskBoundaryPush_notMem_bd_of_hbd). Consequence: the collar chain of ANY congruence-route discharge of
   hdetAB must be a genuinely THIRD chain, constructed rather than selected from the two pieces the glued
   chain already has. Do NOT dispatch "discharge hdetAB by discarding one side of qGen" at any depth.
   backing: `cylPush_notMem_compl_seamPoint`, `collarChain_ne_cylPush`, `collarChain_ne_diskPush`

10. `collar-pair-core-row-alone-is-vacuous-at-empty-seamcore` [vacuity]
   Inhabiting CollarPairCoreRow on its own is sufficient to consume the H (KRS leaf) atom -- i.e.
   CollarPairCoreRow.toHasClass may be fired from the two-obligation row without carrying hseamHit. FALSE
   (2026-07-27, wt3, lead-verified): at seamCore = the empty set BOTH obligations discharge with ZERO
   geometric input -- hbd_of_seamCore_empty (SeamLocal, generalizing the frozen cHa := diskDetectChain form
   to every sphere-supported cHa) and hdetAB_of_seamCore_empty (SeamDetect) -- and
   nonempty_collarPairCoreRow_of_seamCore_empty assembles the WHOLE row there, with z/hz from
   exists_fundCycle and the disk triple from the banked canonical chain. Earlier rounds freed one obligation
   each; together they free the row. So toHasClass would fire on a row containing no geometry. Consequence:
   hseamHit (seamCore NONEMPTY) is a PROVED NECESSITY as a side condition on every consumption of H, not
   advice; any new entry point must carry it and say so in its docstring. NOT claimed: that seamCore = empty
   is geometrically realizable -- this is a scope/soundness fact about the interface, not about the
   geometry.
   backing: `hdetAB_of_seamCore_empty`, `hbd_of_seamCore_empty`

11. `nonhausdorff-bordism-collapse` [refutation]
   The in-tree `Bordism` relation (BordismGroup.lean:37-42) is a faithful bordism theory usable for a
   completeness/injectivity/bounding Prop. FALSE: `Bordism.W` is required compact/charted/IsManifold but NOT
   Hausdorff, so the non-Hausdorff bug-eyed interval B ([0,1] w/ doubled origin, compact + real-analytic + 3
   boundary points) makes W = s.M × B an admissible bordism (s⊔s)⊔s → ∅ for EVERY closed s; with the in-tree
   2-torsion (3x=0 ∧ 2x=0 ⟹ x=0) BordismGrp X 0 I is the TRIVIAL group ([ℝP⁴]=0, a falsifier — false for
   genuine unoriented bordism), and DataBordismGrp mk p = mk q ↔ grade16 equal (no geometric content). ⇒
   hbound and every DataBordismGrp-quantified Prop (incl. Freeze B `SphereProductBounds := mk = 0`) is
   VACUOUS. FIX: require `[t2W : T2Space W]` — the honest relation is `T2TangentialBordism.IsT2DataBordant`;
   real keystone re-anchors to `hboundT2`. NOTE: even T2 at k=0 is TOPOLOGICAL bordism (KS breaks ℤ/16);
   literature-grade needs the SMOOTH (k=∞) + T2 carrier.
   backing: `bordismGrp_subsingleton`, `bordismGrp_rp4_eq_zero`, `dataBordismGMTied_mk_eq_iff_grade16_eq`

12. `comp-twist-doubling-incompatible` [structural_forcing]
   A tangential datum can carry an H¹-coordinate field `comp` with reversal twist `comp ↦ comp + w₁` (the P
   ↦ P⊗ε coordinate), anchored by a restriction-compatibility Bor condition. FALSE (W-A gate, Fable vacuity
   attack 2026-07-13): the mandatory `negBor` inhabits Bor on the DOUBLING bordism — a product cylinder
   whose two boundary inclusions are homotopic — so any '∃x : H¹(W) restricting to the end-comps' condition
   forces comp(revStr σ) = comp σ, contradicting the twist on any non-orientable s (w₁ ≠ 0); the
   uniformly-twisted variant dies on `cylBor` (shift 0). The datum is JOINTLY UNINSTANTIABLE with the
   TangentialData op interface. FIX space: a per-boundary-component collar/co-orientation datum with a
   w₁(W)-corrected compat (any v2 must evade these theorems' hypotheses), or drop the comp field (KT §5 puts
   the odd-bit content in the w₁-dual 3-manifold V / ψ, not in an H¹ coordinate).
   backing: `no_comp_twist_of_doubling_rigid`, `not_doubling_rigid_of_comp_twist`, `no_uniform_comp_twist_of_cylinder_rigid`

13. `membrane-level-nonhausdorff-collapse` [refutation]
   A manifold-typed WITNESS datum inside a carrier or relation (the membrane/3-manifold Q, the surface Σ,
   any auxiliary manifold field) inherits honesty from the T2 fence on the ambient bordism W. FALSE (W-A
   gate, Fable vacuity attack 2026-07-13): the bug-eyed collapse is dimension-generic —
   `qLevelTripleMembrane` is a compact non-Hausdorff membrane with THREE boundary copies of Σ
   (kernel-checked non-T2), so a T2-less Q-encoding lets ker(H₁(∂Q)→H₁(Q)) be adversarially chosen,
   Taylor-Thm-1.1 extension conditions lose their teeth, and the computed Brown/abk8 invariant fails
   bordism-invariance. RULE: every manifold-typed datum needs its OWN T2 (+ compactness/charted)
   certificate; the W-level fence does not propagate.
   backing: `qLevelTripleMembrane_not_t2`

14. `free-membrane-kernel-kills-nonsplit` [refutation]
   The as-built CharPair carrier (Bor with the membrane kernel L carried as a FREE Submodule field, the
   geometric membrane Q deferred) supports the KT §5 non-split content — KTNonSplit (8•[ℝP⁴] ≠ 0) is
   open/dischargeable on it. FALSE (W-D vacuity gate round 3, Fable, 2026-07-13, kernel-checked FOR EVERY
   PROVIDER): with L free, the UN-reversed double σ⊔σ bounds the plain doubling cylinder whenever q⊕q admits
   ANY metabolic Lagrangian (brown ∈ {0,4}); the e₈/extended-Hamming graph of x ↦ x + (Σx)·𝟙 (q∘φ = −q,
   16-point decide) certifies one for [ℝP⁴]⁴ that NO membrane in (ℝP⁴)⁴×I realizes ⟹ 8•[ℝP⁴] = 0, the binder
   pair {KTKernelCard, KTNonSplit} is jointly unsatisfiable, and ⟨[ℝP⁴]⟩ is ≤ ℤ/8. The anti-collapse engine
   protects only Witt-class (brown) equality — exactly the part KT p.217 says cannot see [Kummer]. ROUND-3
   OF THE PATTERN: every deferred geometric tie converts completeness content into falsehood (rounds 1-2:
   comp-twist, membrane-T2, taylor-leg). FIX: CharPairBor must carry the certified membrane Q with L
   COMPUTED as ker(H₁(∂Q)→H₁(Q)) + the item-3 relative characteristic tie (the frozen v4 spec,
   under-implemented by the interim build); acceptance test = the honest cylinder membrane's ANTI-DIAGONAL
   kernel excludes the e₈ graph (doubleKillerBor must break). Also: KTKernelCard quantifies over
   hchar-untethered fake classes (fakeRP4RankZero) — tie (n,q,surf) or restrict the quantifier before
   restating. UPDATE (arm-4 re-gate migration + round-4.5 self-attack, 2026-07-14): the FIX's first half
   LANDED — pinPlusCharPairData's Bor is the membrane-TIED CharPairBorTied in-place (L = ker mem.bInc, all 8
   op witnesses tied, PinPlusCharPairData §9.6; acceptance tests green in PinPlusCharPairMembraneTie). The
   backing refutations PERSIST on the migrated carrier via the SYNTHETIC-bInc replay (doubleKillerBorTied,
   PinPlusKTVacuityGateWD §4.5: GeoMembrane.bInc is still un-tethered, and graphBInc phiLin has kernel
   exactly the e₈ graph) — i.e. the tie NARROWS the hole to precisely the geometric-realization obligation
   without closing it; binders stay FROZEN. Remaining discharge: require realization data on the membrane
   datum (GeoRealizationData/GeoMembrane.ofGeometric, PinPlusCharPairMembraneGeoRealization) + the
   (n,q,surf) tie, then the fresh Fable re-gate. UPDATE 2 (THE FLIP, 2026-07-15): the FIX is COMPLETE —
   pinPlusCharPairData's Bor is CharPairBorRealized (all 8 op witnesses realized via GeoRealizationTied:
   derived bases from the carried (n,q,surf) tie + per-object certs + kernels computed from real membrane
   topology; provider = CharPairWProviderPinned). The instance-level refutations (ktKernelRep_eq_zero,
   ktNonSplit_false, kt_binders_unsatisfiable, ktKummerTarget_unsatisfiable,
   ktRP4Class_addOrderOf_dvd_eight) no longer type-check against the flipped carrier and are REMOVED
   (PinPlusKTVacuityGateWD §5 conversion banner); backing re-pointed to the retained structure-level engines
   (L44_metabolic + doubleKillerBInc_ker — the kernel-checked e₈ record over the algebraic-core structures,
   which persist as the historical exploit shapes). The W-D binders {KTKernelCard, KTNonSplit} are GENUINELY
   OPEN (not refuted, not discharged) — their discharge is W-D's gated work and must pass the round-6
   vacuity gate before consumption.
   backing: `L44_metabolic`, `doubleKillerBInc_ker`

15. `tied-carrier-inhabitation-equiv-free` [structural_forcing]
   The membrane-tied CharPairBorTied (L = ker mem.bInc) is a strictly finer bordism-witness class than the
   refuted free-L CharPairBor — the tie alone filters geometrically-unrealizable Lagrangians. FALSE (W-A
   re-gate round 5, Fable, 2026-07-14, kernel-checked): EVERY submodule of the joint boundary space is the
   computed kernel of a synthetic membrane (GeoMembrane.ofSubmodule via a quotient basis; ofSubmodule_L
   exact), so Nonempty (CharPairBorTied b σ τ) ↔ Nonempty (CharPairBor b σ τ)
   (charPairBorTied_nonempty_iff_free) — the tied carrier is inhabitation-EQUIVALENT to the free one, and
   every round-3/free-form exploit transfers wholesale (round-4.5's doubleKillerBorTied is one instance of
   this general fact). Also: GeoMembrane.top (mid = 0, kernel ⊤) inhabits for ALL end forms and
   JointLagrangian is VACUOUS at ⊤ (jointLagrangian_top) — only the Taylor leg ever blocks a degenerate
   membrane. RULE: the tie is a SHAPE, not a filter, until mem is forced through geometric-realization data
   carrying the (n,q,surf) basis tie. NOTE (THE FLIP, 2026-07-15): the live carrier's Bor is now
   CharPairBorRealized — the strict refinement this fork says the tie alone is not; the fork stays TRUE at
   the tied level (the backing theorems are structure-level and persist) and is the permanent record of WHY
   the realized refinement is load-bearing.
   backing: `charPairBorTied_nonempty_iff_free`, `jointLagrangian_top`
   ⚠ UNRESOLVED backing (not found in-tree — check the registry): `SKEFTHawking.PinPlusCharPairGeoRealizationGate.GeoMembrane.ofSubmodule_L`

16. `realization-seam-basis-gauge-launders-e8` [refutation]
   Topological certificates on GeoRealizationData (T2, compactness, closed-embedding ι, dimension,
   membrane-in-W) suffice to make GeoMembrane.ofGeometric honest — a fully-certified realization cannot
   carry the e₈ kernel. FALSE (W-A re-gate round 5, Fable, 2026-07-14, kernel-checked): the free basis
   fields eσ/eτ admit a gauge action (GeoRealizationData.gaugeσ) that FIXES every topological field
   (gaugeσ_bdry/Q/U/ι all rfl — every space/map certificate is gauge-blind) while moving the computed kernel
   by a block gauge (ker_transportedBInc_gaugeσ); the killerGauge (id ⊞ phiLin through finSumFinEquiv) maps
   the honest doubling anti-diagonal EXACTLY onto the e₈ kernel (map_killerGauge_ker_negBorBInc), so ANY
   realization of negBor's own design-mandatory membrane yields a certified ofGeometric image hosting the
   un-reversed double (doubleKillerBorGeoRealized, conditional on the cylinder realization the roadmap
   itself requires). General engine: the graph of ANY isometry is a block-gauged anti-diagonal
   (map_blockCongr_cylLagrangian) — the half-lives-half-dies geometric signature is NOT gauge-invariant.
   Also kernel-irrelevant: the eQ freedom (ker_transportedBInc_gaugeQ). RULE: the H₁ bases must be DERIVED
   from the carrier's (n,q,surf) tie (hpolar/hchar-anchored identification of the realization's ends with
   the carried surfaces), never free fields; topological certificates are necessary but collectively blind.
   NOTE (THE FLIP, 2026-07-15): the closing mechanism this fork mandates is LIVE — GeoRealizationTied's
   derivedEσ/τ (toData_eσ = rfl, nothing to post-compose) is the Bor consumed by the flipped
   pinPlusCharPairData; the fork stays TRUE over the free GeoRealizationData (backing persists) as the
   record of the gauge exploit the derived bases kill.
   backing: `map_killerGauge_ker_negBorBInc`, `ker_transportedBInc_gaugeσ`, `doubleKillerGeoMem_L`, `map_blockCongr_cylLagrangian`

17. `wadm-sqop-gauge-w2-filter-vacuous` [refutation]
   WAdm/hwu (wuW2 P14 P23 = 0) is a w₂(W) = 0 filter — discharging a CharPairWProvider certifies genuine
   Pin⁺-admissibility of the bordisms it covers. FALSE (W-A re-gate round 5, Fable, 2026-07-14,
   kernel-checked): LefschetzWuDatum.sqOp is a FREE field constrained by neither nondeg nor dimeq; zeroing
   it makes both Wu classes vanish (wuClass_zeroSq) and hwu hold for EVERY W whatever its honest w₂
   (wuW2_zeroSq), so a full CharPairWProvider is dischargeable from bare Lefschetz-duality data with ZERO
   Steenrod input (charPairWProviderOfDuality) — bordisms with genuine w₂(W) ≠ 0 pass. Same
   free-field-plus-self-referential-condition shape as round-3's free L; infects CharPairBorTied.P14/P23/hwu
   directly (the Bor carries copies). RULE: sqOp (and mu/cup) must be PINNED to the substrate's actual
   relative Steenrod tower (relSq1/relSq2) / fundamental-class evaluation / cup product — definitionally or
   via certificate fields — in both WAdm and the Bor's own data; no provider instantiation is acceptable
   without the pin (cylinderP14/P23's sqOp := relSq1/relSq2 is the precedent). UPDATE (arm-4 round 2,
   2026-07-14): the PIN LAYER LANDED (PinPlusWAdmPinned) — certificate pins on ALL THREE fields (the audit
   found cup/mu are a FAITHFULNESS hole even though nondeg blocks zeroing them: a perfect-but-wrong pairing
   decouples wuClass from the manifold's actual w2): LefschetzWuPinned14/23 + WAdmPinned +
   CharPairWProviderPinned + CharPairBorTiedPinned. Discrimination kernel-checked both ways:
   sqOpPinned23_zeroSq_iff (the zeroSq shortcut satisfies the pin IFF the honest relSq = 0),
   not_sqOpPinned23_ofLefschetzNoWu (the F3 provider engine hands out UN-pinnable data wherever relSq2 !=
   0), cylinderP14_pinned/cylinderP23_pinned (honest non-vacuity witnesses),
   CharPairBorTiedPinned.wuFunctional23_honest (a pinned Bor's hwu is the genuine w2-condition). Remaining:
   fold the pinned refinement into the live carrier's Bor consumption (post basis-tie merge), then re-gate.
   UPDATE 2 (arm-4 R1, 2026-07-14): the FAKE-CLASS half is CLOSED — the hchar characteristic-surface tie
   landed on CharPairStrBundled (Guillou-Marin: kroneckerH a (emb-pushforward of surfClass) = mu(a cup a),
   Nonempty-guarded; all four bundle witnesses supply it; RP4 via the DISCHARGED cruxPullbackGen + the mu(a
   cup a) = mu(a cup x^2) bridge), and the fake exhibit is UNINHABITABLE: fakeRP4RankZero no longer
   type-checks and is removed; kernel-checked kill theorems
   RP4CharPairWitness.rp4_bundle_surfClass_pushforward_ne_zero (every RP4 char-pair bundle has nonvanishing
   pushed-forward surface class) + no_empty_surface_bundle_on_rp4. KTKernelCard is no longer honestly-false
   via fake classes. The SOLE remaining discharge item on this fork = the GeoMembrane bInc geometric
   realization (the synthetic-bInc refutations persist; binders stay frozen) + the fresh Fable re-gate.
   UPDATE 3 (THE FLIP, 2026-07-15): the pinned provider is LIVE — pinPlusCharPairData now takes
   CharPairWProviderPinned and its Bor is CharPairBorRealized (P14/P23 carry pin14/pin23 by construction),
   so hwu on every live bordism witness is the HONEST w₂-condition; the bInc realization discharge item is
   COMPLETE. Residual on this fork = the round-6 re-gate.
   backing: `wuW2_zeroSq`, `wuClass_zeroSq`

18. `taylor-leg-end-convention-trap` [structural_forcing]
   The structured-bordism Taylor extension leg ('boundary classes bounding in the membrane Q have vanishing
   enhancement') can be stated as a PLAIN joint sum q_σ ⊕ q_τ, or σ-side-only, vanishing on
   ker(H₁(∂Q)→H₁(Q)). FALSE (W-A re-gate, Fable round 2, 2026-07-13 — both failures fire through the HONEST
   T2 cylinder, no certificate blocks them): plain-joint forces 2q = 0 on cylinder-kernel classes (the
   anti-diagonal), killing every odd enhancement value — the ℝP²/ℝP⁴ witness (q(gen)=1, 1+1=2≠0 in ℤ/4)
   becomes uninstantiable (cylBor totality fails); σ-side-only is vacuous on cylinders, making
   Bor(reflCylinder) relate ARBITRARY structures — the torsor collapses and the Brown/abk8 map is
   ill-defined (1 ≠ −1 in ℤ/8 erased). The ONLY correct form negates the τ-END (per Bor-end, not per
   boundary component): q_σ ⊕ (Z4Quadratic.neg q_τ) vanishes on ker(H₁(∂Q;ℤ/2)→H₁(Q;ℤ/2)). Under that form
   all 12 TangentialData ops instantiate and Brown-invariance is forced (Lagrangian + Gauss factorization).
   backing: `no_plain_end_pairing_of_cylinder`, `not_cylinder_plain_pairing_of_odd_value`, `not_cylinder_bor_of_invariant_ne`

19. `untethered-membrane-factors-relation` [structural_forcing]
   The flipped carrier (Bor = CharPairBorRealized: GeoRealizationTied with derived bases + computed kernels
   + pinned provider) supports the KT non-split content — the abstract compact-T2 membrane suffices; no
   explicit tether of the realization's Q to the bordism's W is needed. FALSE (W-A gate ROUND 6, Fable,
   2026-07-15, kernel-encoded — ROUND 6 OF THE DEFERRED-TIE PATTERN): GeoRealizationTied has NO tether to
   b.W (Q is an arbitrary compact-T2 TopCat — not a manifold, not in W). Consequences, kernel-checked: (1)
   CharPairBorRealized.transport — any realized witness transports VERBATIM to ANY bordism between the same
   ends; (2) isT2DataBordant_pinPlusCharPair_factors — the structured relation FACTORS as (∃ unstructured T2
   bordism) ∧ ends-only HasEndsRealization, so given a provider, hwu filters NOTHING at the relation level;
   (3) ktNonSplit_false_of_e8 — ONE mathematically-TRUE abstract hypothesis (E8MembraneRealization: a
   compact-T2 pair with the e₈-graph H₁-kernel — constructible as 8 RP²'s + an arc tree + four 2-cells, only
   missing CW/MV machinery in-tree) reproduces the ENTIRE pre-flip refutation chain (8•[RP⁴] = 0, binders
   jointly unsatisfiable, addOrderOf ∣ 8). KTNonSplit is mathematically FALSE on the untethered shape; W-D
   binder discharge must NOT open on it (an apparent discharge = an error elsewhere). Positive gate evidence
   retained: idRealizationTied blocked ONLY by hlag (not_jointLagrangian_bot); the un-reversed in-tree
   doubling blocked by htaylor (unreversed_double_diag_not_taylorLeg); in-tree engines reach only
   homeo-graph/block-diagonal kernels (e₈ unreachable at the in-tree horizon) — the exploit needs the
   abstract-Q freedom, which is exactly what the tether removes. FROZEN ROUND-6 SPEC: (1) THE W-TETHER —
   CharPairBorRealized gains ιW : C(Q, b.W) (honest form a closed embedding) with commuting glue (real.ι,
   homσ/homτ, the ends' emb, b's boundary structure); (2) manifold discipline on Q (charted/dimension
   certificates — abstract compact-T2 admits CW pathologies); (3) provider-quantification fix
   (CharPairWProviderPinned's ∀-all-bordisms wadm is likely UNINHABITABLE — pinned hwu is impossible on
   w₂(W) ≠ 0 bordisms — risking vacuity in BOTH directions; restrict to the op-bordism family actually
   consumed or per-op admissibility data). Then round-7 re-gate.
   backing: `isT2DataBordant_pinPlusCharPair_factors`, `ktNonSplit_false_of_e8`
   ⚠ UNRESOLVED backing (not found in-tree — check the registry): `SKEFTHawking.PinPlusCharPairFlipGate.CharPairBorRealized.transport`

20. `nogo_lattice_arf_not_sigma8` [refutation]
   The lattice Arf bridge σ/8 ≡ Arf(q̄) mod 2 — deriving Rokhlin mod-16 from the intersection FORM alone.
   FALSE: E₈ has Arf(q̄)=0 but σ/8=1; Rokhlin mod-16 is irreducibly geometric (a characteristic-SURFACE Arf,
   not the lattice Arf). Kills Phase 5q.C and any form-only mod-16 shortcut.
   backing: `lattice_arf_bridge_refuted`

21. `mfd-equals-H1-dead-end` [structural_forcing]
   The `Mfd := H¹` tangential-data construction yields a genuine ℤ/16 / ker=⊥. FALSE: a datum whose
   structure-reversal (revStr) is trivial is FORCED 2-torsion, never order-16.
   backing: `dataBordism_two_torsion_of_revStr_trivial`

22. `synthetic-grade-ker-bot-nogo` [structural_forcing]
   ker(abkGrade)=⊥ / card≤16 UNCONDITIONALLY for ANY free-per-manifold grade. FALSE: the ℝP⁴ grade-0 witness
   (w₂=0, [ℝP⁴]≠0∈Ω₄^O) has no unoriented null-bordism, so the free grade is never injective. ker=⊥ requires
   the grade TIED to the structure (the GM carrier), never a better proof on a free-grade datum.
   backing: `dataBordism_two_torsion_of_revStr_trivial`

23. `synthetic-smith-map-to-tied-carrier` [structural_forcing]
   The Smith map into the 5q.H TIED carrier pinPlusGMTiedData can be built SYNTHETICALLY — map every
   neighbor class [M,σ] to [emptySM, (σ,0)] and transport the grade (the smithDataHom shortcut). FALSE
   (kernel-forced): the tie htie (reduce16to2 grade16 = swTotalNe) forces every tied structure on an EMPTY
   carrier to even grade (gmTiedStr_grade_even_of_isEmpty), so the odd generator grade16 = 1 is
   uninhabitable on emptySM (gmTiedStr_empty_grade16_ne_one) — an odd grade requires a real w₁⁴ = 1
   manifold. The tie that defeats synthetic-grade-ker-bot-nogo simultaneously blocks every synthetic Smith
   map; the geometric Smith map into the tied carrier is irreducibly geometric (N1b).
   backing: `gmTiedStr_grade_even_of_isEmpty`, `gmTiedStr_empty_grade16_ne_one`

24. `5qH-injectivity-routes-all-equal-one-completeness-prop` [structural_forcing]
   The old-tied-carrier injectivity Props — Thom (hthom: SW-trivial Pin⁺ 4-manifold bounds), KT §5 (hle:
   ker(reduce16to8∘abkGMTied16) ⊆ range(n↦n•g8)), and grade-0 injectivity (hbound) — are DISTINCT OPEN nodes
   on pinPlusGMTiedData (k:=0) worth route-shopping between. FALSE: that carrier is VACATED, so none of them
   is open there — hbound is an UNCONDITIONAL THEOREM on it (grade0_eq_zero_of_nonHausdorff, proved with
   ZERO geometric input via the non-Hausdorff collapse), and mk p = mk q ↔ grade16 p = grade16 q
   (dataBordismGMTied_mk_eq_iff_grade16_eq), i.e. the relation is pure ZMod-16 bookkeeping. Every Prop in
   this family is therefore free on that carrier and carries no completeness content; route-shopping among
   them, or re-deriving "which route is closer", is settled-moot BECAUSE THE CARRIER IS DEAD — not because a
   route was shown superior.
   SCOPE LIMIT — ⚠ READ BEFORE CITING. What is kernel-proved is exactly three ONE-WAY implications, all
   stated on the vacated pinPlusGMTiedData (k:=0) carrier: hthom ⟹ hbound (grade0_bounds_of_thom), hbound ⟹
   hle (spin_range_ge_of_grade0_inj), and hbound ⟹ the old-carrier iso
   (omega4PinPlusGMTied_equiv_zmod16_via_kt_of_grade0). So: hbound is SUFFICIENT to feed those old
   capstones. NOT proved, anywhere in-tree: (i) any REVERSE implication (hle ⟹ hbound, hbound ⟹ hthom); (ii)
   ANY theorem relating the Smith leg (smith_inflow_z16 / SmithInflow / Ω₆^{Pin⁻}) to hbound in either
   direction — the Smith leg has ZERO backing here; (iii) that the routes are equivalent on the FAITHFUL
   carrier (pinPlusCharPairData); (iv) that any node is UNAVOIDABLE. Do NOT cite this entry as "the three
   routes are kernel-proved equivalent", as "the terminal node is a single geometric completeness input", or
   as evidence that a node cannot be routed around — it fences the vacated carrier and nothing else. The
   live keystone is the KT lane on the faithful carrier (SETTLED_FORKS 2026-07-06 user-directed re-anchor,
   which DEMOTED smith_inflow_z16 to an alternative route).
   backing: `grade0_eq_zero_of_nonHausdorff`, `dataBordismGMTied_mk_eq_iff_grade16_eq`, `spin_range_ge_of_grade0_inj`, `omega4PinPlusGMTied_equiv_zmod16_via_kt_of_grade0`, `grade0_bounds_of_thom`

25. `5qH-fg-ek-over-Z-blocked` [refutation]
   The mod-2 Erdős–Kaplansky finiteness forcing (SingularUCFinite: self-duality forces finite dimension)
   transports to ℤ — in particular dualization over ℤ stays in the f.g./countable size class, so PD + UCT
   self-duality would force H²(M;ℤ) finitely generated. FALSE: the ℤ-dual of the COUNTABLE free module ℕ →₀
   ℤ is the UNCOUNTABLE, non-f.g. Baer–Specker group ℕ → ℤ (kernel: dual_blowup_not_finite); and the ℤ-EK
   statement itself is refuted in the literature by Specker 1950 — (⊕ℤ) ⊕ ℤ^ℕ is self-dual and not f.g.
   (slenderness of ℤ; Mathlib-absent, formalization queued). Scout-verified vs FNOP arXiv:1910.07372 +
   Blass–Göbel math/9405206; verdict file
   Lit-Search/Phase-5qH/FG_via_PD_duality_forcing_verdict_20260712.md.
   backing: `dual_blowup_not_finite`, `not_finite_baerSpecker`

26. `genuine-gm-carrier-eight-torsion` [refutation]
   The genuine ℤ/16 lives directly on the thin GM carrier: DataBordismGrp(pinPlusGMData) ≃+ ZMod 16. FALSE:
   pinPlusGMData's bordism relation records ONLY the mod-8 Brown grade (q.brown ∈ ZMod 8), so
   cylinder-doubling makes every class 8-torsion (pinPlusGMData_eight_torsion: 8•x=0) ⟹
   IsEmpty(DataBordismGrp(pinPlusGMData) ≃+ ZMod 16). The ℤ/16 odd bit is the Smith-LES EXTENSION onto a
   distinct carrier (the tied/extension carrier, whose mod-8 shadow is abkGM8 via forgetTie), NEVER a
   surface grade on pinPlusGMData itself. Do NOT target `omega4PinPlusGM_equiv_zmod16 :
   DataBordismGrp(pinPlusGMData) ≃+ ZMod 16` as literally stated — reframe to the Smith-LES extension
   carrier (E3) or an enriched (σ,F•F)-carrying GM carrier.
   backing: `pinPlusGMData_not_equiv_zmod16`, `pinPlusGMData_eight_torsion`

27. `enriques-datum-refuted-as-shaped` [structural_forcing]
   EnriquesDatum as shipped carries independent Enriques-geometry content (w₂ ≠ 0, π₁ = ℤ/2, the line
   bundle) usable as the W-D B-leaf. FALSE-AS-SHAPED (gate round 10, G10-6): its [Ha] fields collapse —
   Nonempty (EnriquesDatum prov) ↔ EmptySigmaRepresentable prov (ktKernelRep prov)
   (enriquesDatum_iff_kummerRep), i.e. it is bare KummerWitness.1 in decorative dressing; the "Enriques"
   content has no formal footprint. Do NOT construct or consume EnriquesDatum as an independent B-leaf. The
   frozen replacement B-target is GeometricSpinRepresentable, absorbed by the C-leaf:
   KTSpinPresentationDatum ⟹ EnriquesDatum (enriquesDatum_of_ktSpinPresentationDatum, via
   emptySigmaRepresentable_of_geometric) — discharge the C-leaf and B follows; a standalone "Enriques wave"
   duplicates work into a vacuous shape.
   backing: `enriquesDatum_iff_kummerRep`, `enriquesDatum_of_ktSpinPresentationDatum`, `emptySigmaRepresentable_of_geometric`

28. `geometric-phi-does-not-close-hfwd-fakeability` [structural_forcing]
   Fixing Φ := spinForgetPhi (the geometric forgetful map) in the dA leaf makes hfwd a genuine geometric
   obligation not derivable from the conclusion. FALSE (gate round 11): given the presentation row {hA, hB,
   hg, hdvd, hΦg, h2}, hfwd on the geometric Φ is DERIVABLE from KTNonSplit with zero geometry (R.generates
   writes x = n•[g]; Φx = n•k₀; nonsplit + 2-torsion force n even ⟹ 32 ∣ σ(x)) — the locating iff
   spinForgetPhi_hfwd_iff_ktNonSplit pins hfwd at exactly KTNonSplit strength. Consequence (binding round-11
   spec): NO statement shape closes the dA/hfwd circularity risk — every hfwd/dA discharge claim requires a
   non-circularity audit BY PROOF INSPECTION (the per-instance Div32BoundingDatum supply is equally
   shape-fakeable, round 10 §3). The audit-friendly honest construction target is the kernel
   characterization ker Φ ⊆ doubles (spinForgetPhi_hfwd_of_ker_sub_doubles — hfwd free from Rokhlin on
   doubles, consuming zero k₀ facts).
   backing: `spinForgetPhi_hfwd_of_ktNonSplit`, `spinForgetPhi_hfwd_iff_ktNonSplit`

29. `dual-spin-opened-construction-conclusion-fakeable` [structural_forcing]
   Opening DualSpinFromW into DualSpinConstruction (Vspace/ιV/Vspin/hcob/hcover fields) makes the dual-spin
   supply a genuine geometric obligation not derivable from the conclusion. FALSE (gate round 12): on an
   unpinned ambient, Nonempty (DualSpinConstruction PUnit sigM) ↔ 32 ∣ sigM — every field including hcob
   inhabits from bare arithmetic (the σ-onto realization engine spinOfSigMul16 supplies genuine
   SmoothSpinManifold4 witnesses for every multiple of 16), and modulo the presentation row the whole opened
   supply ⟺ the hfwd conclusion. The mechanism: amb is a FREE TopCat field. Consequence (binding round-12
   spec 1): dA supply claims pass by DATA INSPECTION only — amb must be TopCat.of b.W of the actual tethered
   witness and edge the geometric ∂E(V); statement-shape audits are insufficient at every opening depth.
   backing: `nonempty_dualSpinConstruction_iff_thirtytwo_dvd`, `nonempty_ktSharpnessSupplyConstr_iff_hfwd`, `spinOfSigMul16_sig`

30. `hker-opener-supplyGeo-is-non-reducing` [structural_forcing]
   Inhabiting KTSharpnessSupplyGeo and firing kerPhiSubDoubles_of_row_of_supplyGeo is a reducing step on
   hker -- i.e. the opened geometric sharpness supply is a strictly weaker obligation than the hfwd
   conclusion it feeds. FALSE (2026-07-27, lead): nonempty_ktSharpnessSupplyGeo_iff_hfwd proves Nonempty
   (KTSharpnessSupplyGeo prov R) <-> (forall x, spinForgetPhi prov x = 0 -> 32 | R.sig x), i.e. the consumed
   supply is EQUIVALENT to the hfwd conclusion. The opener theorem is TRUE but reduces nothing: supplying
   its input is exactly as hard as assuming its target. Backward direction takes amb := TopCat.of PUnit, and
   the companion fork hker-ambient-pin-does-not-restore-geometry shows no other choice of ambient (including
   the genuine tethered b.W, or any ambient FAMILY) helps. Consequence: do NOT dispatch "inhabit
   KTSharpnessSupplyGeo" as an hker brick at any depth; hker needs a NEW interface that carries the
   submanifold and DERIVES its intersection lattice (SpinSigmaAtomPkg pattern) plus the w1-duality tie.
   backing: `nonempty_ktSharpnessSupplyGeo_iff_hfwd`

31. `hker-ambient-pin-does-not-restore-geometry` [structural_forcing]
   Pinning the ambient — requiring amb x hx = TopCat.of b.W of the genuine tethered witness, as binding
   round-12 spec 1 demands — restores geometric content to the dA/hker dual-spin supply, so an amb-pinned
   KTSharpnessSupplyGeo variant would be real progress on hker. FALSE (2026-07-27, lead): the
   conclusion-equivalence holds at EVERY Hausdorff ambient W, not just the trivial PUnit of the round-12
   exhibit — nonempty_dualSpinFromW_iff_thirtytwo_dvd proves Nonempty (DualSpinFromW W sigM) ↔ 32 ∣ sigM for
   all T2 W, and dualSpinFamily_iff_pointwise_thirtytwo_dvd lifts it to an arbitrary ambient FAMILY (the
   strongest pin the spec could ask for). MECHANISM (the structural root, deeper than the round-12 amb
   note): SmoothSpinManifold4 is PURE LATTICE DATA (rank/form/even_unimod/topo) with NO underlying space, so
   in DualSpinFromW the topological half (Vspace/iotaV/hclosed) and the arithmetic half (Vspin/hdouble) are
   DISCONNECTED BY CONSTRUCTION — the empty submanifold closed-embeds into any T2 W
   (isClosedEmbedding_empty) while spinOfSigMul16 supplies the lattice. CONSEQUENCE: hker cannot be
   discharged through ANY shape built over SmoothSpinManifold4 + a bare embedded Vspace; the interface
   cannot express "V is the w1(W)-dual submanifold AND Vspin is its intersection lattice". A genuine hker
   discharge needs an interface carrying the submanifold and DERIVING its lattice (the SpinSigmaAtomPkg
   pattern: fundamental class + H2 basis + Poincare duality on an actual manifold) plus the w1-duality tie.
   Do NOT dispatch amb-pinning as an hker brick.
   backing: `nonempty_dualSpinFromW_iff_thirtytwo_dvd`, `dualSpinFamily_iff_pointwise_thirtytwo_dvd`, `isClosedEmbedding_empty`

32. `novikov-substrate-synthetic-inhabitation` [structural_forcing]
   The NovikovRealPairLES substrate (or any of the four equivalent σ-descent atom formulations) constitutes
   progress toward the Thom bordism-invariance of σ beyond the bare hbord statement. FALSE (gate round 12):
   the diagonal Lagrangian + synthetic quotient inhabit the substrate with zero geometry whenever σ agrees
   (nonempty_novikovBoundaryRestriction_iff_sig_eq — the Witt step kernel-encoded via
   exists_lagrangian_of_latticeSig_eq_zero), and novikovLagrangian_iff_hbord proves ALL FOUR formulations
   (classical Lagrangian / boundary-restriction half-dim / Lefschetz co-isotropy / the pair-LES substrate)
   are kernel-equivalent to hbord itself. Consequence (binding round-12 spec 2): a Novikov-atom discharge
   must exhibit a GENUINE bounding-W tower (the relative cap + the geometric restriction data of an actual
   bordism witness) — linear-algebra Lagrangian constructions are zero progress at both grades.
   backing: `nonempty_novikovRealPairLES_diag`, `nonempty_novikovBoundaryRestriction_iff_sig_eq`, `novikovLagrangian_iff_hbord`

33. `novikov-geometric-tower-carrier-conclusion-fakeable` [structural_forcing]
   The NovikovGeometricPairLESData field row (the #196 genuine-tower carrier:
   rest2/delta/pairing/hexact/hnondeg/hadjDot) is a stronger-than-conclusion carrier whose inhabitation
   certifies genuine bounding-W tower progress. FALSE (gate round 13): the substrate rebuilds the carrier
   (novikovGeometricPairLESDataOfRealPairLES — pairing := half the substrate pairing, hadjDot DERIVED from
   hadj via the polar-form symmetry), so carrier ⟺ substrate
   (nonempty_novikovGeometricPairLESData_iff_realPairLES), and at block pairs Nonempty (Data (blockDiag A
   (-B))) ↔ σ(A) = σ(B) — the round-12 Witt step + ofLagrangian + the reverse map populate EVERY field from
   a bare Lagrangian with zero bordism geometry. hadjDot moved the fork-20 perimeter; it did not close it.
   Consequence (binding round-13 spec 1): Novikov-lane carrier populations are progress ONLY if
   rest2/delta/pairing are the tensor-R base-changes of the genuine integral tower maps of an actual
   relative cycle — audited by DATA INSPECTION; a quotient-of-coordinate-space H3rel is zero progress by
   construction.
   backing: `novikovGeometricPairLESDataOfRealPairLES`, `nonempty_novikovGeometricPairLESData_iff_realPairLES`, `nonempty_novikovGeometricPairLESData_iff_sig_eq`, `nonempty_novikovGeometricPairLESData_diag`

34. `seam-transfer-open-support-uninhabitable` [refutation]
   The CapstoneSeamTransfer / shared-cSeam CapstoneSeamTransferSeam consumption shape (the #184-#207
   hasClass route: the top-face/disk-boundary splits with OPEN-complement supports hwOut/hvOut plus the
   literal htransfer equality) is inhabitable for a genuine fundamental cycle z and a proper attaching
   region, and the 3x-circled closed-S co-adaptation barrier (#198/#204/#207) is a machinery gap. FALSE (the
   #210 lead adjudication): htransfer FORCES wAtt into the CLOSED attach image (injective-mapDomain support
   transport + fromCyl_image_compl disjointness — wAtt_mem_subspaceChains_range_phi), char-2 boundary
   algebra then forces BOTH split pieces to be CYCLES (boundary_wAtt_eq_zero via
   subspaceChains_inf_compl_eq_bot), so z@top decomposes as a seam-supported cycle + an off-seam cycle;
   whenever the two regions have null 4-cycle classes (H4(S1xD3;Z/2)=0, H4 of an open 4-region = 0 — the
   genuine consumption) and z@top does not bound (z fundamental), the shape is EMPTY
   (isEmpty_capstoneSeamTransfer_of_null + the Seam corollary). Consequence: the transfer route to hasClass
   is dead AS SHIPPED; the honest repair = CLOSED-complement supports (wOut/vOut in the complement of the
   INTERIOR of the attach region — the classical two-rel-pieces-sharing-the-interface picture), a NEW
   gate-pending consumption shape whose interface terms cancel mod 2.
   backing: `wAtt_mem_subspaceChains_range_phi`, `boundary_wAtt_eq_zero`, `isEmpty_capstoneSeamTransfer_of_null`, `isEmpty_capstoneSeamTransferSeam_of_null`

35. `k0-to-k1-transport-refuted` [refutation]
   Every k = 0 charted space over the R^4 model is also a k >= 1 manifold -- i.e. the `k := 0` binder in the
   live KT provider is harmless generality that transports to the smooth category for free. FALSE (kernel
   refutation, 2026-07-21). Mathlib registers an UNCONDITIONAL `instance : IsManifold I 0 M` for every
   charted space (contDiffGroupoid 0 I = continuousGroupoid H), so at k = 0 the IsManifold field is not weak
   but FREE (PinPlusRegularityFence.isManifoldZero_free). And no generic transport can repair it:
   no_generic_zero_to_one_transport refutes `forall M, IsManifold (R^4) 0 M -> IsManifold (R^4) 1 M` via
   TwistedR4 = R^4 with atlas {id, twist 0}, whose transition carries the kink min t (t/2) with one-sided
   derivatives 1 and 1/2 at 0 (not_differentiableAt_kinkInv). At the CARRIER level,
   exists_carrier_element_not_smooth exhibits a legal `SingularManifold PUnit 0 (R^4)` element -- built by
   adjoining one twisted chart to the project's own RP4 -- that is not a k >= 1 manifold. CONSUMER CHECK
   (why this is not idle): CharPairWProviderPerOp (R^4) 0 is hard-wired in 45 files and a tree-wide grep
   finds NO provider at k >= 1; residualProv (PinPlusKTAssemblyResiduals.lean:70) is the only provider and
   is declared at literal 0. The C0 generality is not unexercised -- it is the only thing exercised. SCOPE
   (worker-stated, lead-confirmed; DO NOT OVERSTATE): this separates OBJECT CLASSES, not bordism groups.
   Bordism could still identify the extra objects, and whether the k = 0 GROUP differs is OPEN and NOT
   claimed here; Kirby-Siebenmann is not formalized. TwistedRP4 IS homeomorphic to RP4 and does admit some
   smooth structure -- what fails is that its ATLAS, the datum SingularManifold actually bundles, is not
   C^1. So this upgrades the roadmap-leg-2 fence from 'the conclusion is uninterpreted' to 'the k := 0
   binder is not harmless generality and no generic transport can exist'. It is NOT a refutation of the KT
   Z/16 mathematics.
   backing: `no_generic_zero_to_one_transport`, `exists_carrier_element_not_smooth`

36. `collar-pair-closed-seam-attached-collar-bridge-is-FALSE` [refutation]
   PinPlusTraceSeamResidualNarrow.ClosedSeamAttachedCollarBridge S a -- the closed-S attached-collar bridge,
   believed to be hctrlH's blocking atom and readable as an open-neighbourhood-to-closed-S collar
   deformation retraction. FALSE (kernel refutation, 2026-07-21): collar_bridge_refuted exhibits closed
   nonempty S = {e_0} in the 5-sphere, U = univ (satisfying the engine's own cover hypothesis sphere subset
   U union S-complement verbatim), and a chain a in subspaceChains (U inter sphere) with NOT (Bridge S a).
   The witness is one concrete great-circle 4-simplex v |-> cos(pi*v_0/2)*e_0 + sin(pi*v_0/2)*e_1, norm 1
   throughout, running from e_0 in S to e_1 not in S. ANATOMY (both directions,
   attachedBridge_iff_support_dichotomy): the bridge holds IFF no support simplex of a straddles S -- so it
   has NO collar content whatsoever, and the 'collar deformation-retraction' reading in
   PinPlusTraceSeamResidualNarrow section 3 is WRONG: a retraction gives homotopy/homology invariance, never
   the chain-level EQUALITY the bridge demands. CONSEQUENCE, and the reason this is not a wall: the bridge
   was never hctrlH's blocker. The predecessor reduction of hctrlH to it was STRICTLY TOO STRONG -- the
   bridge demands a correction supported in the CLOSED complement sphere minus S, while
   CollarPairBuild.hctrlH's companion houtH only asks for the OPEN complement sphere minus (Subtype.val
   image of K), off the builder-chosen shrunk closed core K (a free field documented K inside int A). That
   is the open-cover engine's native granularity, so hctrlH + houtH are supplied bridge-free by
   exists_ctrlHandle_split_offCore. SCOPE (do not overstate): this kills the ATOM, not the row. The producer
   is SUFFICIENT, not equivalent -- it exhibits one admissible (muH, cCore, outH) triple, claims no cCore
   uniqueness, and does NOT supply the co-adaptation with the cylinder side, which is now carried by hctrlC.
   Non-vacuity pinned: exists_nonempty_core_of_sphere_mem shows a NONEMPTY closed core meeting the
   interiority hypothesis exists whenever U meets the sphere, so this is not a statement about the K = empty
   case that hcoreHit rules out.
   backing: `collar_bridge_refuted`, `attachedBridge_iff_support_dichotomy`

37. `collar-pair-face-row-forces-seam-to-meet-boundary` [structural_forcing]
   A CollarPairGeomFace row (the houtPair producer: the row's own supports plus the seam-annulus containment
   hseamAnn) can be inhabited with an ENTIRELY INTERIOR seam -- i.e. with no seam point of the surgered end
   lying in dW, so that the collar-pair route never has to pay for the seam. FALSE (structural forcing,
   2026-07-21): in a face row the two fields hKoffBd + hseamAnn PIN the shrunk core, K_eq_compl_seamPreimage
   giving F.K = (seamPoint preimage of dW)^c. An entirely interior seam therefore forces K = univ, which
   degenerates houtC/houtH to exactly the open-complement supports fenced by
   collar-pair-open-complement-annulus-is-refuted-shape, and not_collarAnnulusOpen_of_null kills those under
   the null/non-bounding hypotheses. Hence exists_seamPoint_mem_bd_of_null: no face row has an entirely
   interior seam; read on the datum (exists_seamPoint_mem_range_eM_of_null), range d.eM' MUST contain a seam
   point. Companion: topFaceShrunk_eq_topFace_inter_preimage shows that at the forced core the coarse
   support EQUALS topface INTER fromCyl^-1(dW), so the route is tight -- no slack between shrunk-core and
   maximal granularity. SCOPE (worker-stated in both docstrings, lead-confirmed -- do not overstate): this
   does NOT close the coarse houtPair route; it states the route's PRICE. It says nothing about
   CollarPairGeomCore, which carries no hseamAnn field. CollarPairGeomFace is a SUFFICIENT producer, NOT an
   equivalent row (nonempty_collarPairGeomCore_of_face is one-directional), so the obligation count stays at
   FIVE.
   backing: `exists_seamPoint_mem_bd_of_null`, `exists_seamPoint_mem_range_eM'_of_null`

38. `collar-pair-open-complement-annulus-is-refuted-shape` [structural_forcing]
   houtPair (the collar-annulus weld obligation of the #212 collar-pair row) can be discharged for free by
   reading its three required boundary-supports straight off the in-tree SurgeredEndDatum, whose
   d.topFaceCovered (fromCyl '' (topface \ range phi) subset dW), d.sphereFaceCovered (fromHandle '' (sphere
   \ S) subset dW) and bottom-face fact are exactly the three supports hbd_ofTransfer consumes. FALSE
   (structural forcing, 2026-07-21): a collar-annulus refinement at that OPEN-COMPLEMENT granularity, taken
   on top of the row's own hctrlC/hctrlH, CONSTRUCTS a verbatim inhabitant of CapstoneSeamTransferSeam
   (collarAnnulusOpen_toSeamTransferSeam: the seam core absorbs the annulus at cSeam := cCore + ann, cHa :=
   diskDetectChain, and the two residuals are verbatim its wOut/vOut with verbatim its hwOut/hvOut supports)
   -- i.e. the shape refuted by seam-transfer-open-support-uninhabitable. not_collarAnnulusOpen_of_null then
   turns that refinement into False under the identical null/non-bounding hypotheses backing
   isEmpty_capstoneSeamTransferSeam_of_null. So the free-looking SurgeredEndDatum instantiation of houtPair
   is settled-dead. SCOPE (worker-stated, lead-confirmed -- do not overstate): only the ONE direction
   `open-complement refinement ==> refuted structure` is proved. It is NOT proved that every collar-annulus
   refinement is impossible: a refinement at the strictly COARSER closed-complement granularity the #210
   repair adopted (topface \ phi '' K for a shrunk core K) is excluded by nothing here, and
   houtPair_of_bdMem / houtPair_of_bdImageSubset remain its LIVE route. Cite as 'the free-looking
   d.topFaceCovered instantiation is closed', NEVER as 'the collar-annulus refinement is closed'.
   backing: `collarAnnulusOpen_toSeamTransferSeam`, `not_collarAnnulusOpen_of_null`

39. `collar-pair-maximal-core-reenters-refuted-support` [structural_forcing]
   The CollarPairGeom/CollarPairGeomUnsub row's anti-fake tether hcoreHit can be made free of charge by
   enlarging the #210 shrunk core K toward its limit K = univ, keeping the rest of the collar-pair split
   data intact. FALSE (structural forcing, 2026-07-21): at K = univ the tether IS indeed a consequence of
   the top-face split (coreHit_of_univ - cCore lands in subspaceChains of univ-complement = the empty set,
   hence cCore = 0, so z@top = outC), but at K = univ the remainder support collapses to `(univ x {top}) \
   phi '' univ = (univ x {top}) \ range phi` (houtC_support_univ_eq_refuted), which is VERBATIM the
   OPEN-complement signature refuted by seam-transfer-open-support-uninhabitable. So the maximal-core
   shortcut purchases the tether only by re-entering the settled-dead shape, and hcoreHit must remain a
   genuine obligation of the row. SCOPE (lead-corrected, do not overstate): only the direction `K = univ ==>
   tether free AND support refuted` is proved. There is NO uniqueness/converse theorem - a strictly smaller
   K that still discharges hcoreHit is NOT excluded, merely unexplored. Cite this fence as 'the obvious
   maximal-core shortcut is closed', never as 'every hcoreHit shortcut is closed'.
   backing: `coreHit_of_univ`, `houtC_support_univ_eq_refuted`

40. `k7-seam-cover-interior-fails` [refutation]
   K7SeamCoverHyp (the K7 opener's un-thickened MV cover hypothesis): the INTERIORS of the two closed
   Kummer-weld pieces (the Q-image and the 16 closed E-images) cover the welded K3 carrier, so the
   Mayer-Vietoris assembly can run on the un-thickened pieces directly. FALSE (kernel refutation
   k7SeamCoverHyp_false): a seam point (fiberNorm = 1, the glued RP3 locus) is interior to NEITHER piece -
   E-side, every neighbourhood meets fiber radius < 1 (the inward deform path); Q-side, every neighbourhood
   meets the E-exterior along the outward chart ray (via circle_exp_injOn_one ->
   centeredChartParam_injOn_double, chart injectivity on the doubled ball). Consequence: "discharge
   K7SeamCoverHyp" is settled-dead; the UNIQUE route is the collar-thickened cover (qThick = qImage union
   outer half-collars fiberNorm >= 1/2) whose interior-cover hypothesis IS discharged (k7_hcov,
   KummerK7MVAssembly) and through which the whole K7 accounting now runs unconditionally.
   backing: `k7SeamCoverHyp_false`

41. `gram-literal-equality-is-choice-dependent` [structural_forcing]
   SphereProdGramPin - the LITERAL matrix equality `interMatrix fc B = sphereProdFormDatum` on the computed
   rank-2 basis - is a disclosed GEOMETRIC residual of the S2xS2 intersection form, dischargeable once the
   Kunneth/EZ cross value is pinned. FALSE AS FRAMED (structural forcing, 2026-07-21): the exact
   computed-basis Gram is `!![-(2*s*u*eps), u*eps; u*eps, 0]` (sphereProd_interMatrix_computed_eq), so the
   pin holds IFF `s = 0 AND u*eps = 1` (sphereProdGramPin_iff). Neither conjunct is available: (1) `s` is
   the alpha-coordinate of the Exists.choose split generator `deltaGen`, pinned only modulo `sumInto` whose
   fst_* image is NONZERO (sumInto_prodFst) - so `deltaGen |-> deltaGen + k*sumInto 1` shifts `s` by `k` and
   the (0,0) entry by `-2*k*u*eps`, making the Prop INDEPENDENT of the in-tree data, not merely unproven;
   (2) `u`, `eps` are pinned only as units, an orientation/sign gap. Note this also kills the prior hope
   that normalizing the cross value to literally 1 would suffice - a normalized eps still leaves the
   diagonal free. The GEOMETRIC content the pin stood in for IS in tree and unconditional:
   SphereProdBasisIdInt.sphereProd_interMatrix_intCongr_hyp proves II(S2xS2) integrally CONGRUENT to Hyp
   itself. Every consumer conclusion is congruence-invariant, so the congruence supersedes the equality;
   hypothesis-free replacements are
   SphereProdGramPinRetire.sphereProd_s2s2_{hyp,evenUnimodular,latticeSig,htopo}'. GENERAL RULE (applies to
   every Gram atom incl. the K3/T4 one): state Gram targets as `IntCongr ... <form>`, NEVER as a literal
   matrix equality on a chosen basis - the extra content of an equality over a congruence is basis
   normalization, not geometry, and is generically unreachable whenever any basis vector comes from a
   choice.
   backing: `sphereProd_interMatrix_computed_eq`, `sphereProdGramPin_iff`

42. `kronecker-dual-is-not-the-h1-enhancement-transport` [refutation]
   The Kronecker/UCT dual of the carried cohomology basis - `homologyBasisOfCohomologyBasis basis`, the
   value `pinCharSurfaceOfBundled` puts in `PinCharSurface.H1Iso` (and the value
   `GeoRealizationTied.derivedEsigma`/`derivedEtau` put in the seam) - is the identification of
   `H1(Sigma;Z/2)` that the enhancement `q` lives on, so `q` may be evaluated at those coordinates at ANY
   rank. FALSE (audit M4, kernel-checked 2026-07-21). The carrier's `hpolar` pins `q.B` to the cup pairing
   in the COHOMOLOGY basis `e`; the Kronecker dual is the transport that makes the KRONECKER pairing the dot
   product (`kroneckerH_eq_dotProduct`), a different normalization. The two differ by the Gram operator of
   `q.B` (`homologyCoords_eq_gramMap`), and the difference is not cosmetic: (1) VARIANCE MISMATCH - a gauge
   `e |-> g o e` moves the derived homology coordinates by the transpose-inverse (`homologyCoords_gauge`)
   while `hpolar` forces `q` to move by `g` (`hpolar_gaugePullback`), so `q o (Kronecker transport)` is not
   gauge-invariant (`not_forall_kroneckerTransport_gauge_invariant`, rank-2 transvection witness); the
   gauged carrier is a genuine `CharPairStrBundled` with the SAME surf/emb/surfClass/hchar and the SAME
   Brown grade (`gaugeBundled_*`, `gaugeBundled_brown`), so the value read is not a function of the
   geometry. (2) TRUTH-VALUE FLIP - for the genus-1 model in a symplectic basis the Gram operator is the
   coordinate SWAP (`gramMap_hyperbolic2`), and on the a-cycle metabolizer `q` vanishes while at its
   raw-Kronecker coordinates `q = 2 != 0` (`hyperbolic2_taylor_flip`): `TaylorKernelVanishing` is FALSE
   exactly where Taylor Thm 1.1 says it must be TRUE. SCOPE (do not overstate): the defect is strictly
   NONZERO-RANK. At rank 0 the two transports are EQUAL (`homologyBasisPD_eq_of_rank_zero`,
   `pinCharSurfaceOfBundledPD_eq_of_rank_zero`), so the entire live rank-zero
   `PinPlusKTRankZeroBounding`/`toLeaves` chain is sound and unaffected; and `gramMap_stdQuadratic` shows
   the raw transport is accidentally correct whenever the chosen basis is orthonormal for the intersection
   form (which a positive-genus surface's symplectic sector is not). REPAIR (SHIPPED, no hypothesis needed):
   use `homologyBasisPD Q e := (homologyBasisOfCohomologyBasis e).trans (gramEquiv Q).symm` -
   `Z4Quadratic.nondeg` IS the Gram operator's invertibility - which is the Poincare-dual transport
   (`homologyBasisPD_pd`: `homologyBasisPD Q e (pd a) = e a`), is gauge-covariant (`homologyBasisPD_gauge`)
   and yields a gauge-INVARIANT enhancement value (`q_homologyBasisPD_gauge_invariant`); carrier-level entry
   point `pinCharSurfaceOfBundledPD`. BLAST RADIUS: the same raw transport feeds
   `TaylorLegVanishes`/`JointLagrangian` through `GeoRealizationTied.derivedEsigma`/`derivedEtau`
   (PinPlusCharPairRealizationTied), so any nonzero-rank use of the realized carrier's Taylor leg needs the
   same correction.
   backing: `homologyCoords_gauge`, `not_forall_kroneckerTransport_gauge_invariant`, `hyperbolic2_taylor_flip`, `gramMap_hyperbolic2`, `pinCharSurfaceOfBundled_gauge_H1Iso`

-/
import SKEFTHawking.BordismGroup
import SKEFTHawking.CharSurfacePDBundled
import SKEFTHawking.CharSurfacePDTransport
import SKEFTHawking.FGDualityNoGo
import SKEFTHawking.GMPinTorsorCeiling
import SKEFTHawking.GMTripleLayerForcing
import SKEFTHawking.HandleTradeAtomVacuity
import SKEFTHawking.HandleTradeAtomVacuityConcrete
import SKEFTHawking.IntOrientationScaling
import SKEFTHawking.KummerK3E1Repair
import SKEFTHawking.KummerK3ForallOrientationFalse
import SKEFTHawking.KummerK7SeamCoverNoGo
import SKEFTHawking.MembraneIndex
import SKEFTHawking.NonHausdorffBordismCollapse
import SKEFTHawking.PinPlusCharPairFlipGate
import SKEFTHawking.PinPlusCharPairGeoRealizationGate
import SKEFTHawking.PinPlusCompTorsorNoGo
import SKEFTHawking.PinPlusGMDataZ16
import SKEFTHawking.PinPlusGMWitness
import SKEFTHawking.PinPlusGenuineCarrierIso
import SKEFTHawking.PinPlusKTHkerAmbPinGate
import SKEFTHawking.PinPlusKTLeafGate
import SKEFTHawking.PinPlusKTRankZeroCollapseSupply
import SKEFTHawking.PinPlusKTStepGate
import SKEFTHawking.PinPlusKTVacuityGateWD
import SKEFTHawking.PinPlusRegularitySeparation
import SKEFTHawking.PinPlusRegularitySeparationCarrier
import SKEFTHawking.PinPlusResidualGate
import SKEFTHawking.PinPlusRoundThirteenGate
import SKEFTHawking.PinPlusTaylorConventionNoGo
import SKEFTHawking.PinPlusTraceCapstoneCollarPairCore
import SKEFTHawking.PinPlusTraceCapstoneCollarPairFace
import SKEFTHawking.PinPlusTraceCapstoneCollarPairGeom
import SKEFTHawking.PinPlusTraceCapstoneCollarPairSeamDetect
import SKEFTHawking.PinPlusTraceCapstoneCollarPairSeamLocal
import SKEFTHawking.PinPlusTraceCapstoneCollarPairSeamLocalHom
import SKEFTHawking.PinPlusTraceLeafGate
import SKEFTHawking.PinPlusTraceSeamCollarBridgeNoGo
import SKEFTHawking.PinPlusTraceSeamTransferNoGo
import SKEFTHawking.RokhlinArfNoGo
import SKEFTHawking.SingularFaceLocalHomologyVanish
import SKEFTHawking.SingularFacePieceDetect
import SKEFTHawking.SphereProdGramPinRetire
import SKEFTHawking.SymTFTAudit.DrinfeldCenter
import SKEFTHawking.SyntheticSmithNoGo
import SKEFTHawking.T2BordismGluing
import SKEFTHawking.UnorientedThomCapstone

namespace SKEFTHawking.KernelNoGos

/-- NO-GO [`fk-mod2-membrane-index-cannot-reach-nonorientable-stratum`] — do NOT re-derive. FALSE: The Freedman-Kirby MOD-2 membrane index D.F + O(D) + d(C) -- blueprint Route A nodes [G2]/[Q1]/[Q2], FK Lemma 2.6.1 -- produces the pin-minus class of a NONORIENTABLE characteristic surface, so building out the membrane layer suffices to supply `hgm` on the whole char-pair carrier. Backing refutation: `SKEFTHawking.MembraneIndex.no_mod2_index_on_rp2`. -/
alias nogo_no_mod2_index_on_rp2 := SKEFTHawking.MembraneIndex.no_mod2_index_on_rp2

/-- NO-GO [`fk-mod2-membrane-index-cannot-reach-nonorientable-stratum`] — do NOT re-derive. FALSE: The Freedman-Kirby MOD-2 membrane index D.F + O(D) + d(C) -- blueprint Route A nodes [G2]/[Q1]/[Q2], FK Lemma 2.6.1 -- produces the pin-minus class of a NONORIENTABLE characteristic surface, so building out the membrane layer suffices to supply `hgm` on the whole char-pair carrier. Backing refutation: `SKEFTHawking.MembraneIndex.refines_forces_alternating`. -/
alias nogo_refines_forces_alternating := SKEFTHawking.MembraneIndex.refines_forces_alternating

/-- NO-GO [`fk-mod2-membrane-index-cannot-reach-nonorientable-stratum`] — do NOT re-derive. FALSE: The Freedman-Kirby MOD-2 membrane index D.F + O(D) + d(C) -- blueprint Route A nodes [G2]/[Q1]/[Q2], FK Lemma 2.6.1 -- produces the pin-minus class of a NONORIENTABLE characteristic surface, so building out the membrane layer suffices to supply `hgm` on the whole char-pair carrier. Backing refutation: `SKEFTHawking.MembraneIndex.not_exists_index_of_B_self_ne_zero`. -/
alias nogo_not_exists_index_of_B_self_ne_zero := SKEFTHawking.MembraneIndex.not_exists_index_of_B_self_ne_zero

/-- NO-GO [`fk-mod2-membrane-index-cannot-reach-nonorientable-stratum`] — do NOT re-derive. FALSE: The Freedman-Kirby MOD-2 membrane index D.F + O(D) + d(C) -- blueprint Route A nodes [G2]/[Q1]/[Q2], FK Lemma 2.6.1 -- produces the pin-minus class of a NONORIENTABLE characteristic surface, so building out the membrane layer suffices to supply `hgm` on the whole char-pair carrier. Backing refutation: `SKEFTHawking.MembraneIndex.FramedMembrane.indexWellDefined_false_of_framedMembrane`. -/
alias nogo_indexWellDefined_false_of_framedMembrane := SKEFTHawking.MembraneIndex.FramedMembrane.indexWellDefined_false_of_framedMembrane

/-- NO-GO [`k3-e1-forall-orientation-routes-are-vacuous`] — do NOT re-derive. FALSE: A geometric hypothesis about the welded Kummer K3 may be quantified over ALL integral orientations -- the shape `forall o : IntOrientation KummerK3, <Gram / unimodularity / signature / cap-dual-span statement at o>` used by EVERY published entry point into `Nonempty KummerK3E1Atoms`. Backing refutation: `SKEFTHawking.IntOrientationScaling.not_forall_intOrientation_isUnimodular`. -/
alias nogo_not_forall_intOrientation_isUnimodular := SKEFTHawking.IntOrientationScaling.not_forall_intOrientation_isUnimodular

/-- NO-GO [`k3-e1-forall-orientation-routes-are-vacuous`] — do NOT re-derive. FALSE: A geometric hypothesis about the welded Kummer K3 may be quantified over ALL integral orientations -- the shape `forall o : IntOrientation KummerK3, <Gram / unimodularity / signature / cap-dual-span statement at o>` used by EVERY published entry point into `Nonempty KummerK3E1Atoms`. Backing refutation: `SKEFTHawking.KummerK3E1Repair.not_forall_intOrientation_pdInput`. -/
alias nogo_not_forall_intOrientation_pdInput := SKEFTHawking.KummerK3E1Repair.not_forall_intOrientation_pdInput

/-- NO-GO [`k3-e1-forall-orientation-routes-are-vacuous`] — do NOT re-derive. FALSE: A geometric hypothesis about the welded Kummer K3 may be quantified over ALL integral orientations -- the shape `forall o : IntOrientation KummerK3, <Gram / unimodularity / signature / cap-dual-span statement at o>` used by EVERY published entry point into `Nonempty KummerK3E1Atoms`. Backing refutation: `SKEFTHawking.KummerK3E1Repair.not_kummerK3E1Residuals`. -/
alias nogo_not_kummerK3E1Residuals := SKEFTHawking.KummerK3E1Repair.not_kummerK3E1Residuals

/-- NO-GO [`k3-e1-forall-orientation-routes-are-vacuous`] — do NOT re-derive. FALSE: A geometric hypothesis about the welded Kummer K3 may be quantified over ALL integral orientations -- the shape `forall o : IntOrientation KummerK3, <Gram / unimodularity / signature / cap-dual-span statement at o>` used by EVERY published entry point into `Nonempty KummerK3E1Atoms`. Backing refutation: `SKEFTHawking.KummerK3E1Repair.not_forall_latticeSig_neg16`. -/
alias nogo_not_forall_latticeSig_neg16 := SKEFTHawking.KummerK3E1Repair.not_forall_latticeSig_neg16

/-- NO-GO [`k3-e1-forall-orientation-routes-are-vacuous`] — do NOT re-derive. FALSE: A geometric hypothesis about the welded Kummer K3 may be quantified over ALL integral orientations -- the shape `forall o : IntOrientation KummerK3, <Gram / unimodularity / signature / cap-dual-span statement at o>` used by EVERY published entry point into `Nonempty KummerK3E1Atoms`. Backing refutation: `SKEFTHawking.KummerK3E1Repair.not_forall_gram`. -/
alias nogo_not_forall_gram := SKEFTHawking.KummerK3E1Repair.not_forall_gram

/-- NO-GO [`k3-e1-forall-orientation-routes-are-vacuous`] — do NOT re-derive. FALSE: A geometric hypothesis about the welded Kummer K3 may be quantified over ALL integral orientations -- the shape `forall o : IntOrientation KummerK3, <Gram / unimodularity / signature / cap-dual-span statement at o>` used by EVERY published entry point into `Nonempty KummerK3E1Atoms`. Backing refutation: `SKEFTHawking.KummerK3E1Repair.not_forall_geoData`. -/
alias nogo_not_forall_geoData := SKEFTHawking.KummerK3E1Repair.not_forall_geoData

/-- NO-GO [`k3-e1-forall-orientation-routes-are-vacuous`] — do NOT re-derive. FALSE: A geometric hypothesis about the welded Kummer K3 may be quantified over ALL integral orientations -- the shape `forall o : IntOrientation KummerK3, <Gram / unimodularity / signature / cap-dual-span statement at o>` used by EVERY published entry point into `Nonempty KummerK3E1Atoms`. Backing refutation: `SKEFTHawking.KummerK3ForallOrientationFalse.forall_orientation_kummerSubForm_family_false`. -/
alias nogo_forall_orientation_kummerSubForm_family_false := SKEFTHawking.KummerK3ForallOrientationFalse.forall_orientation_kummerSubForm_family_false

/-- NO-GO [`gm-pin-torsor-free-shadow-weaker-than-van-der-blij`] — do NOT re-derive. FALSE: The pin-minus-FREE content of the Guillou-Marin congruence -- the part of GM that survives quotienting by the H^1(F;Z/2) enhancement torsor, i.e. Backing refutation: `SKEFTHawking.GMTorsor.lattice_plus_torsor_free_shadow_cannot_prove_rokhlin`. -/
alias nogo_lattice_plus_torsor_free_shadow_cannot_prove_rokhlin := SKEFTHawking.GMTorsor.lattice_plus_torsor_free_shadow_cannot_prove_rokhlin

/-- NO-GO [`gm-pin-torsor-free-shadow-weaker-than-van-der-blij`] — do NOT re-derive. FALSE: The pin-minus-FREE content of the Guillou-Marin congruence -- the part of GM that survives quotienting by the H^1(F;Z/2) enhancement torsor, i.e. Backing refutation: `SKEFTHawking.GMTorsor.reduce16to4_doubleBrown_shift`. -/
alias nogo_reduce16to4_doubleBrown_shift := SKEFTHawking.GMTorsor.reduce16to4_doubleBrown_shift

/-- NO-GO [`gm-pin-torsor-free-shadow-weaker-than-van-der-blij`] — do NOT re-derive. FALSE: The pin-minus-FREE content of the Guillou-Marin congruence -- the part of GM that survives quotienting by the H^1(F;Z/2) enhancement torsor, i.e. Backing refutation: `SKEFTHawking.GMTorsor.reduce16to8_doubleBrown_not_shift_invariant`. -/
alias nogo_reduce16to8_doubleBrown_not_shift_invariant := SKEFTHawking.GMTorsor.reduce16to8_doubleBrown_not_shift_invariant

/-- NO-GO [`gm-pin-torsor-free-shadow-weaker-than-van-der-blij`] — do NOT re-derive. FALSE: The pin-minus-FREE content of the Guillou-Marin congruence -- the part of GM that survives quotienting by the H^1(F;Z/2) enhancement torsor, i.e. Backing refutation: `SKEFTHawking.GMTorsor.doubleBrown_stdShift3_injective`. -/
alias nogo_doubleBrown_stdShift3_injective := SKEFTHawking.GMTorsor.doubleBrown_stdShift3_injective

/-- NO-GO [`gm-triple-level-layer-is-decided-by-sigma-alone`] — do NOT re-derive. FALSE: There exists a statement layer for [FK]-at-general-sigma, phrased as a predicate P on the INVARIANT TRIPLE (sigma, F.F, 2*beta), that (i) follows from Guillou-Marin and (ii) suffices for Rokhlin at spin, yet whose spin specialization is strictly weaker than 16 | sigma -- i.e. Backing refutation: `SKEFTHawking.GMTripleLayer.triple_layer_forcing`. -/
alias nogo_triple_layer_forcing := SKEFTHawking.GMTripleLayer.triple_layer_forcing

/-- NO-GO [`gm-triple-level-layer-is-decided-by-sigma-alone`] — do NOT re-derive. FALSE: There exists a statement layer for [FK]-at-general-sigma, phrased as a predicate P on the INVARIANT TRIPLE (sigma, F.F, 2*beta), that (i) follows from Guillou-Marin and (ii) suffices for Rokhlin at spin, yet whose spin specialization is strictly weaker than 16 | sigma -- i.e. Backing refutation: `SKEFTHawking.GMTripleLayer.no_triple_layer_escapes_the_biconditional`. -/
alias nogo_no_triple_layer_escapes_the_biconditional := SKEFTHawking.GMTripleLayer.no_triple_layer_escapes_the_biconditional

/-- NO-GO [`gm-triple-level-layer-is-decided-by-sigma-alone`] — do NOT re-derive. FALSE: There exists a statement layer for [FK]-at-general-sigma, phrased as a predicate P on the INVARIANT TRIPLE (sigma, F.F, 2*beta), that (i) follows from Guillou-Marin and (ii) suffices for Rokhlin at spin, yet whose spin specialization is strictly weaker than 16 | sigma -- i.e. Backing refutation: `SKEFTHawking.GMTripleLayer.spin_triple_mem_closure_iff_sixteen_dvd`. -/
alias nogo_spin_triple_mem_closure_iff_sixteen_dvd := SKEFTHawking.GMTripleLayer.spin_triple_mem_closure_iff_sixteen_dvd

/-- NO-GO [`gm-triple-level-layer-is-decided-by-sigma-alone`] — do NOT re-derive. FALSE: There exists a statement layer for [FK]-at-general-sigma, phrased as a predicate P on the INVARIANT TRIPLE (sigma, F.F, 2*beta), that (i) follows from Guillou-Marin and (ii) suffices for Rokhlin at spin, yet whose spin specialization is strictly weaker than 16 | sigma -- i.e. Backing refutation: `SKEFTHawking.GMTripleLayer.forcing_collapse_is_spin_specific`. -/
alias nogo_forcing_collapse_is_spin_specific := SKEFTHawking.GMTripleLayer.forcing_collapse_is_spin_specific

/-- NO-GO [`freeze-a-atoms-satisfiable-with-zero-geometry`] — do NOT re-derive. FALSE: Building the Freeze-A primitives AS STATED -- SpinSigmaPresentation.HandleTradeCobordism (the raw handle-trade cobordism, primitive 1 of the freeze-atoms-not-composable-from-sigma-trace honest floor) and HyperbolicBase (primitive 2, the rank-0 nullbordism) -- discharges genuine manifold-surgery content and therefore advances hA. Backing refutation: `SKEFTHawking.SpinSigmaRoute.collapsedPresentation_handleTradeCobordism`. -/
alias nogo_collapsedPresentation_handleTradeCobordism := SKEFTHawking.SpinSigmaRoute.collapsedPresentation_handleTradeCobordism

/-- NO-GO [`freeze-a-atoms-satisfiable-with-zero-geometry`] — do NOT re-derive. FALSE: Building the Freeze-A primitives AS STATED -- SpinSigmaPresentation.HandleTradeCobordism (the raw handle-trade cobordism, primitive 1 of the freeze-atoms-not-composable-from-sigma-trace honest floor) and HyperbolicBase (primitive 2, the rank-0 nullbordism) -- discharges genuine manifold-surgery content and therefore advances hA. Backing refutation: `SKEFTHawking.HandleTradeAtomVacuityConcrete.collapsedSphereProd_handleTradeCobordism`. -/
alias nogo_collapsedSphereProd_handleTradeCobordism := SKEFTHawking.HandleTradeAtomVacuityConcrete.collapsedSphereProd_handleTradeCobordism

/-- NO-GO [`freeze-a-atoms-satisfiable-with-zero-geometry`] — do NOT re-derive. FALSE: Building the Freeze-A primitives AS STATED -- SpinSigmaPresentation.HandleTradeCobordism (the raw handle-trade cobordism, primitive 1 of the freeze-atoms-not-composable-from-sigma-trace honest floor) and HyperbolicBase (primitive 2, the rank-0 nullbordism) -- discharges genuine manifold-surgery content and therefore advances hA. Backing refutation: `SKEFTHawking.HandleTradeAtomVacuityConcrete.constRankTwoSphereProd_hyperbolicBase`. -/
alias nogo_constRankTwoSphereProd_hyperbolicBase := SKEFTHawking.HandleTradeAtomVacuityConcrete.constRankTwoSphereProd_hyperbolicBase

/-- NO-GO [`freeze-a-atoms-satisfiable-with-zero-geometry`] — do NOT re-derive. FALSE: Building the Freeze-A primitives AS STATED -- SpinSigmaPresentation.HandleTradeCobordism (the raw handle-trade cobordism, primitive 1 of the freeze-atoms-not-composable-from-sigma-trace honest floor) and HyperbolicBase (primitive 2, the rank-0 nullbordism) -- discharges genuine manifold-surgery content and therefore advances hA. Backing refutation: `SKEFTHawking.SpinSigmaRoute.no_generator_of_rank_le_two`. -/
alias nogo_no_generator_of_rank_le_two := SKEFTHawking.SpinSigmaRoute.no_generator_of_rank_le_two

/-- NO-GO [`hker-single-witness-extraction-is-equivalent-to-gluing`] — do NOT re-derive. FALSE: The single tethered null-bordism witness that hker needs -- extracting one IsT2DataBordant from T2DataBordismGrp.mk = 0 -- can be obtained by a cleverer use of the quotient, a reformulation of the extraction, or any route that AVOIDS proving bordism gluing (transitivity). Backing refutation: `SKEFTHawking.T2BordismGluing.gluesT2_iff_singleWitness`. -/
alias nogo_gluesT2_iff_singleWitness := SKEFTHawking.T2BordismGluing.gluesT2_iff_singleWitness

/-- NO-GO [`hker-single-witness-extraction-is-equivalent-to-gluing`] — do NOT re-derive. FALSE: The single tethered null-bordism witness that hker needs -- extracting one IsT2DataBordant from T2DataBordismGrp.mk = 0 -- can be obtained by a cleverer use of the quotient, a reformulation of the extraction, or any route that AVOIDS proving bordism gluing (transitivity). Backing refutation: `SKEFTHawking.T2BordismGluing.IsT2DataBordant.refl`. -/
alias nogo_refl := SKEFTHawking.T2BordismGluing.IsT2DataBordant.refl

/-- NO-GO [`hker-single-witness-extraction-is-equivalent-to-gluing`] — do NOT re-derive. FALSE: The single tethered null-bordism witness that hker needs -- extracting one IsT2DataBordant from T2DataBordismGrp.mk = 0 -- can be obtained by a cleverer use of the quotient, a reformulation of the extraction, or any route that AVOIDS proving bordism gluing (transitivity). Backing refutation: `SKEFTHawking.T2BordismGluing.IsT2DataBordant.symm`. -/
alias nogo_symm := SKEFTHawking.T2BordismGluing.IsT2DataBordant.symm

/-- NO-GO [`hcolD-same-carrier-collapse-refuted-on-wu-witness`] — do NOT re-derive. FALSE: The hcolD rank-zero collapse to an empty characteristic surface can be realized WITHOUT changing the carrier -- i.e. Backing refutation: `SKEFTHawking.PinPlusKTRankZeroCollapseSupply.no_isEmpty_surf_of_wu_witness`. -/
alias nogo_no_isEmpty_surf_of_wu_witness := SKEFTHawking.PinPlusKTRankZeroCollapseSupply.no_isEmpty_surf_of_wu_witness

/-- NO-GO [`capstone-binary-partition-detection-uninhabitable`] — do NOT re-derive. FALSE: The connected capstone's hasClass can be driven through the binary complementary partition {U, U-complement} at U := range fromCyl -- i.e. Backing refutation: `SKEFTHawking.PinPlusTraceCapstoneCollarPairSeamLocalHom.not_restrictsToRelGenOn_cylRange_at_seamCore`. -/
alias nogo_not_restrictsToRelGenOn_cylRange_at_seamCore := SKEFTHawking.PinPlusTraceCapstoneCollarPairSeamLocalHom.not_restrictsToRelGenOn_cylRange_at_seamCore

/-- NO-GO [`capstone-binary-partition-detection-uninhabitable`] — do NOT re-derive. FALSE: The connected capstone's hasClass can be driven through the binary complementary partition {U, U-complement} at U := range fromCyl -- i.e. Backing refutation: `SKEFTHawking.SingularFacePieceDetect.not_restrictsToRelGenOn_of_faceVanish`. -/
alias nogo_not_restrictsToRelGenOn_of_faceVanish := SKEFTHawking.SingularFacePieceDetect.not_restrictsToRelGenOn_of_faceVanish

/-- NO-GO [`capstone-binary-partition-detection-uninhabitable`] — do NOT re-derive. FALSE: The connected capstone's hasClass can be driven through the binary complementary partition {U, U-complement} at U := range fromCyl -- i.e. Backing refutation: `SKEFTHawking.SingularFaceLocalHomologyVanish.faceLocalHomology_zero_of_starConvexChart`. -/
alias nogo_faceLocalHomology_zero_of_starConvexChart := SKEFTHawking.SingularFaceLocalHomologyVanish.faceLocalHomology_zero_of_starConvexChart

/-- NO-GO [`collar-pair-hdetAB-one-sided-congruence-routes-dead`] — do NOT re-derive. FALSE: CollarPairCoreRow.hdetAB can be discharged by the engine layer's advertised one-sided congruence route -- i.e. Backing refutation: `SKEFTHawking.PinPlusTraceCapstoneCollarPairSeamDetect.cylPush_notMem_compl_seamPoint`. -/
alias nogo_cylPush_notMem_compl_seamPoint := SKEFTHawking.PinPlusTraceCapstoneCollarPairSeamDetect.cylPush_notMem_compl_seamPoint

/-- NO-GO [`collar-pair-hdetAB-one-sided-congruence-routes-dead`] — do NOT re-derive. FALSE: CollarPairCoreRow.hdetAB can be discharged by the engine layer's advertised one-sided congruence route -- i.e. Backing refutation: `SKEFTHawking.PinPlusTraceCapstoneCollarPairSeamDetect.collarChain_ne_cylPush`. -/
alias nogo_collarChain_ne_cylPush := SKEFTHawking.PinPlusTraceCapstoneCollarPairSeamDetect.collarChain_ne_cylPush

/-- NO-GO [`collar-pair-hdetAB-one-sided-congruence-routes-dead`] — do NOT re-derive. FALSE: CollarPairCoreRow.hdetAB can be discharged by the engine layer's advertised one-sided congruence route -- i.e. Backing refutation: `SKEFTHawking.PinPlusTraceCapstoneCollarPairSeamDetect.collarChain_ne_diskPush`. -/
alias nogo_collarChain_ne_diskPush := SKEFTHawking.PinPlusTraceCapstoneCollarPairSeamDetect.collarChain_ne_diskPush

/-- NO-GO [`collar-pair-core-row-alone-is-vacuous-at-empty-seamcore`] — do NOT re-derive. FALSE: Inhabiting CollarPairCoreRow on its own is sufficient to consume the H (KRS leaf) atom -- i.e. Backing refutation: `SKEFTHawking.PinPlusTraceCapstoneCollarPairSeamDetect.hdetAB_of_seamCore_empty`. -/
alias nogo_hdetAB_of_seamCore_empty := SKEFTHawking.PinPlusTraceCapstoneCollarPairSeamDetect.hdetAB_of_seamCore_empty

/-- NO-GO [`collar-pair-core-row-alone-is-vacuous-at-empty-seamcore`] — do NOT re-derive. FALSE: Inhabiting CollarPairCoreRow on its own is sufficient to consume the H (KRS leaf) atom -- i.e. Backing refutation: `SKEFTHawking.PinPlusTraceCapstoneCollarPairSeamLocal.hbd_of_seamCore_empty`. -/
alias nogo_hbd_of_seamCore_empty := SKEFTHawking.PinPlusTraceCapstoneCollarPairSeamLocal.hbd_of_seamCore_empty

/-- NO-GO [`nonhausdorff-bordism-collapse`] — do NOT re-derive. FALSE: The in-tree `Bordism` relation (BordismGroup.lean:37-42) is a faithful bordism theory usable for a completeness/injectivity/bounding Prop. Backing refutation: `SKEFTHawking.NonHausdorffBordismCollapse.bordismGrp_subsingleton`. -/
alias nogo_bordismGrp_subsingleton := SKEFTHawking.NonHausdorffBordismCollapse.bordismGrp_subsingleton

/-- NO-GO [`nonhausdorff-bordism-collapse`] — do NOT re-derive. FALSE: The in-tree `Bordism` relation (BordismGroup.lean:37-42) is a faithful bordism theory usable for a completeness/injectivity/bounding Prop. Backing refutation: `SKEFTHawking.NonHausdorffBordismCollapse.bordismGrp_rp4_eq_zero`. -/
alias nogo_bordismGrp_rp4_eq_zero := SKEFTHawking.NonHausdorffBordismCollapse.bordismGrp_rp4_eq_zero

/-- NO-GO [`nonhausdorff-bordism-collapse`] — do NOT re-derive. FALSE: The in-tree `Bordism` relation (BordismGroup.lean:37-42) is a faithful bordism theory usable for a completeness/injectivity/bounding Prop. Backing refutation: `SKEFTHawking.NonHausdorffBordismCollapse.dataBordismGMTied_mk_eq_iff_grade16_eq`. -/
alias nogo_dataBordismGMTied_mk_eq_iff_grade16_eq := SKEFTHawking.NonHausdorffBordismCollapse.dataBordismGMTied_mk_eq_iff_grade16_eq

/-- NO-GO [`comp-twist-doubling-incompatible`] — do NOT re-derive. FALSE: A tangential datum can carry an H¹-coordinate field `comp` with reversal twist `comp ↦ comp + w₁` (the P ↦ P⊗ε coordinate), anchored by a restriction-compatibility Bor condition. Backing refutation: `SKEFTHawking.PinPlusCompTorsorNoGo.no_comp_twist_of_doubling_rigid`. -/
alias nogo_no_comp_twist_of_doubling_rigid := SKEFTHawking.PinPlusCompTorsorNoGo.no_comp_twist_of_doubling_rigid

/-- NO-GO [`comp-twist-doubling-incompatible`] — do NOT re-derive. FALSE: A tangential datum can carry an H¹-coordinate field `comp` with reversal twist `comp ↦ comp + w₁` (the P ↦ P⊗ε coordinate), anchored by a restriction-compatibility Bor condition. Backing refutation: `SKEFTHawking.PinPlusCompTorsorNoGo.not_doubling_rigid_of_comp_twist`. -/
alias nogo_not_doubling_rigid_of_comp_twist := SKEFTHawking.PinPlusCompTorsorNoGo.not_doubling_rigid_of_comp_twist

/-- NO-GO [`comp-twist-doubling-incompatible`] — do NOT re-derive. FALSE: A tangential datum can carry an H¹-coordinate field `comp` with reversal twist `comp ↦ comp + w₁` (the P ↦ P⊗ε coordinate), anchored by a restriction-compatibility Bor condition. Backing refutation: `SKEFTHawking.PinPlusCompTorsorNoGo.no_uniform_comp_twist_of_cylinder_rigid`. -/
alias nogo_no_uniform_comp_twist_of_cylinder_rigid := SKEFTHawking.PinPlusCompTorsorNoGo.no_uniform_comp_twist_of_cylinder_rigid

/-- NO-GO [`membrane-level-nonhausdorff-collapse`] — do NOT re-derive. FALSE: A manifold-typed WITNESS datum inside a carrier or relation (the membrane/3-manifold Q, the surface Σ, any auxiliary manifold field) inherits honesty from the T2 fence on the ambient bordism W. Backing refutation: `SKEFTHawking.PinPlusCompTorsorNoGo.qLevelTripleMembrane_not_t2`. -/
alias nogo_qLevelTripleMembrane_not_t2 := SKEFTHawking.PinPlusCompTorsorNoGo.qLevelTripleMembrane_not_t2

/-- NO-GO [`free-membrane-kernel-kills-nonsplit`] — do NOT re-derive. FALSE: The as-built CharPair carrier (Bor with the membrane kernel L carried as a FREE Submodule field, the geometric membrane Q deferred) supports the KT §5 non-split content — KTNonSplit (8•[ℝP⁴] ≠ 0) is open/dischargeable on it. Backing refutation: `SKEFTHawking.PinPlusKTVacuityGateWD.L44_metabolic`. -/
alias nogo_L44_metabolic := SKEFTHawking.PinPlusKTVacuityGateWD.L44_metabolic

/-- NO-GO [`free-membrane-kernel-kills-nonsplit`] — do NOT re-derive. FALSE: The as-built CharPair carrier (Bor with the membrane kernel L carried as a FREE Submodule field, the geometric membrane Q deferred) supports the KT §5 non-split content — KTNonSplit (8•[ℝP⁴] ≠ 0) is open/dischargeable on it. Backing refutation: `SKEFTHawking.PinPlusKTVacuityGateWD.doubleKillerBInc_ker`. -/
alias nogo_doubleKillerBInc_ker := SKEFTHawking.PinPlusKTVacuityGateWD.doubleKillerBInc_ker

/-- NO-GO [`tied-carrier-inhabitation-equiv-free`] — do NOT re-derive. FALSE: The membrane-tied CharPairBorTied (L = ker mem.bInc) is a strictly finer bordism-witness class than the refuted free-L CharPairBor — the tie alone filters geometrically-unrealizable Lagrangians. Backing refutation: `SKEFTHawking.PinPlusCharPairGeoRealizationGate.charPairBorTied_nonempty_iff_free`. -/
alias nogo_charPairBorTied_nonempty_iff_free := SKEFTHawking.PinPlusCharPairGeoRealizationGate.charPairBorTied_nonempty_iff_free

/-- NO-GO [`tied-carrier-inhabitation-equiv-free`] — do NOT re-derive. FALSE: The membrane-tied CharPairBorTied (L = ker mem.bInc) is a strictly finer bordism-witness class than the refuted free-L CharPairBor — the tie alone filters geometrically-unrealizable Lagrangians. Backing refutation: `SKEFTHawking.PinPlusCharPairGeoRealizationGate.jointLagrangian_top`. -/
alias nogo_jointLagrangian_top := SKEFTHawking.PinPlusCharPairGeoRealizationGate.jointLagrangian_top

/-- NO-GO [`realization-seam-basis-gauge-launders-e8`] — do NOT re-derive. FALSE: Topological certificates on GeoRealizationData (T2, compactness, closed-embedding ι, dimension, membrane-in-W) suffice to make GeoMembrane.ofGeometric honest — a fully-certified realization cannot carry the e₈ kernel. Backing refutation: `SKEFTHawking.PinPlusCharPairGeoRealizationGate.map_killerGauge_ker_negBorBInc`. -/
alias nogo_map_killerGauge_ker_negBorBInc := SKEFTHawking.PinPlusCharPairGeoRealizationGate.map_killerGauge_ker_negBorBInc

/-- NO-GO [`realization-seam-basis-gauge-launders-e8`] — do NOT re-derive. FALSE: Topological certificates on GeoRealizationData (T2, compactness, closed-embedding ι, dimension, membrane-in-W) suffice to make GeoMembrane.ofGeometric honest — a fully-certified realization cannot carry the e₈ kernel. Backing refutation: `SKEFTHawking.PinPlusCharPairGeoRealizationGate.ker_transportedBInc_gaugeσ`. -/
alias nogo_ker_transportedBInc_gaugeσ := SKEFTHawking.PinPlusCharPairGeoRealizationGate.ker_transportedBInc_gaugeσ

/-- NO-GO [`realization-seam-basis-gauge-launders-e8`] — do NOT re-derive. FALSE: Topological certificates on GeoRealizationData (T2, compactness, closed-embedding ι, dimension, membrane-in-W) suffice to make GeoMembrane.ofGeometric honest — a fully-certified realization cannot carry the e₈ kernel. Backing refutation: `SKEFTHawking.PinPlusCharPairGeoRealizationGate.doubleKillerGeoMem_L`. -/
alias nogo_doubleKillerGeoMem_L := SKEFTHawking.PinPlusCharPairGeoRealizationGate.doubleKillerGeoMem_L

/-- NO-GO [`realization-seam-basis-gauge-launders-e8`] — do NOT re-derive. FALSE: Topological certificates on GeoRealizationData (T2, compactness, closed-embedding ι, dimension, membrane-in-W) suffice to make GeoMembrane.ofGeometric honest — a fully-certified realization cannot carry the e₈ kernel. Backing refutation: `SKEFTHawking.PinPlusCharPairGeoRealizationGate.map_blockCongr_cylLagrangian`. -/
alias nogo_map_blockCongr_cylLagrangian := SKEFTHawking.PinPlusCharPairGeoRealizationGate.map_blockCongr_cylLagrangian

/-- NO-GO [`wadm-sqop-gauge-w2-filter-vacuous`] — do NOT re-derive. FALSE: WAdm/hwu (wuW2 P14 P23 = 0) is a w₂(W) = 0 filter — discharging a CharPairWProvider certifies genuine Pin⁺-admissibility of the bordisms it covers. Backing refutation: `SKEFTHawking.PinPlusCharPairGeoRealizationGate.wuW2_zeroSq`. -/
alias nogo_wuW2_zeroSq := SKEFTHawking.PinPlusCharPairGeoRealizationGate.wuW2_zeroSq

/-- NO-GO [`wadm-sqop-gauge-w2-filter-vacuous`] — do NOT re-derive. FALSE: WAdm/hwu (wuW2 P14 P23 = 0) is a w₂(W) = 0 filter — discharging a CharPairWProvider certifies genuine Pin⁺-admissibility of the bordisms it covers. Backing refutation: `SKEFTHawking.PinPlusCharPairGeoRealizationGate.wuClass_zeroSq`. -/
alias nogo_wuClass_zeroSq := SKEFTHawking.PinPlusCharPairGeoRealizationGate.wuClass_zeroSq

/-- NO-GO [`taylor-leg-end-convention-trap`] — do NOT re-derive. FALSE: The structured-bordism Taylor extension leg ('boundary classes bounding in the membrane Q have vanishing enhancement') can be stated as a PLAIN joint sum q_σ ⊕ q_τ, or σ-side-only, vanishing on ker(H₁(∂Q)→H₁(Q)). Backing refutation: `SKEFTHawking.PinPlusTaylorConventionNoGo.no_plain_end_pairing_of_cylinder`. -/
alias nogo_no_plain_end_pairing_of_cylinder := SKEFTHawking.PinPlusTaylorConventionNoGo.no_plain_end_pairing_of_cylinder

/-- NO-GO [`taylor-leg-end-convention-trap`] — do NOT re-derive. FALSE: The structured-bordism Taylor extension leg ('boundary classes bounding in the membrane Q have vanishing enhancement') can be stated as a PLAIN joint sum q_σ ⊕ q_τ, or σ-side-only, vanishing on ker(H₁(∂Q)→H₁(Q)). Backing refutation: `SKEFTHawking.PinPlusTaylorConventionNoGo.not_cylinder_plain_pairing_of_odd_value`. -/
alias nogo_not_cylinder_plain_pairing_of_odd_value := SKEFTHawking.PinPlusTaylorConventionNoGo.not_cylinder_plain_pairing_of_odd_value

/-- NO-GO [`taylor-leg-end-convention-trap`] — do NOT re-derive. FALSE: The structured-bordism Taylor extension leg ('boundary classes bounding in the membrane Q have vanishing enhancement') can be stated as a PLAIN joint sum q_σ ⊕ q_τ, or σ-side-only, vanishing on ker(H₁(∂Q)→H₁(Q)). Backing refutation: `SKEFTHawking.PinPlusTaylorConventionNoGo.not_cylinder_bor_of_invariant_ne`. -/
alias nogo_not_cylinder_bor_of_invariant_ne := SKEFTHawking.PinPlusTaylorConventionNoGo.not_cylinder_bor_of_invariant_ne

/-- NO-GO [`untethered-membrane-factors-relation`] — do NOT re-derive. FALSE: The flipped carrier (Bor = CharPairBorRealized: GeoRealizationTied with derived bases + computed kernels + pinned provider) supports the KT non-split content — the abstract compact-T2 membrane suffices; no explicit tether of the realization's Q to the bordism's W is needed. Backing refutation: `SKEFTHawking.PinPlusCharPairFlipGate.isT2DataBordant_pinPlusCharPair_factors`. -/
alias nogo_isT2DataBordant_pinPlusCharPair_factors := SKEFTHawking.PinPlusCharPairFlipGate.isT2DataBordant_pinPlusCharPair_factors

/-- NO-GO [`untethered-membrane-factors-relation`] — do NOT re-derive. FALSE: The flipped carrier (Bor = CharPairBorRealized: GeoRealizationTied with derived bases + computed kernels + pinned provider) supports the KT non-split content — the abstract compact-T2 membrane suffices; no explicit tether of the realization's Q to the bordism's W is needed. Backing refutation: `SKEFTHawking.PinPlusCharPairFlipGate.ktNonSplit_false_of_e8`. -/
alias nogo_ktNonSplit_false_of_e8 := SKEFTHawking.PinPlusCharPairFlipGate.ktNonSplit_false_of_e8

/-- NO-GO [`nogo_lattice_arf_not_sigma8`] — do NOT re-derive. FALSE: The lattice Arf bridge σ/8 ≡ Arf(q̄) mod 2 — deriving Rokhlin mod-16 from the intersection FORM alone. Backing refutation: `SKEFTHawking.RokhlinArfNoGo.lattice_arf_bridge_refuted`. -/
alias nogo_lattice_arf_bridge_refuted := SKEFTHawking.RokhlinArfNoGo.lattice_arf_bridge_refuted

/-- NO-GO [`mfd-equals-H1-dead-end`] — do NOT re-derive. FALSE: The `Mfd := H¹` tangential-data construction yields a genuine ℤ/16 / ker=⊥. Backing refutation: `SKEFTHawking.PinPlusGenuineCarrierIso.dataBordism_two_torsion_of_revStr_trivial`. -/
alias nogo_dataBordism_two_torsion_of_revStr_trivial := SKEFTHawking.PinPlusGenuineCarrierIso.dataBordism_two_torsion_of_revStr_trivial

/-- NO-GO [`synthetic-grade-ker-bot-nogo`] — do NOT re-derive. FALSE: ker(abkGrade)=⊥ / card≤16 UNCONDITIONALLY for ANY free-per-manifold grade. Backing refutation: `SKEFTHawking.PinPlusGenuineCarrierIso.dataBordism_two_torsion_of_revStr_trivial`. -/
alias nogo_dataBordism_two_torsion_of_revStr_trivial_2 := SKEFTHawking.PinPlusGenuineCarrierIso.dataBordism_two_torsion_of_revStr_trivial

/-- NO-GO [`synthetic-smith-map-to-tied-carrier`] — do NOT re-derive. FALSE: The Smith map into the 5q.H TIED carrier pinPlusGMTiedData can be built SYNTHETICALLY — map every neighbor class [M,σ] to [emptySM, (σ,0)] and transport the grade (the smithDataHom shortcut). Backing refutation: `SKEFTHawking.PinPlusGMTiedData.gmTiedStr_grade_even_of_isEmpty`. -/
alias nogo_gmTiedStr_grade_even_of_isEmpty := SKEFTHawking.PinPlusGMTiedData.gmTiedStr_grade_even_of_isEmpty

/-- NO-GO [`synthetic-smith-map-to-tied-carrier`] — do NOT re-derive. FALSE: The Smith map into the 5q.H TIED carrier pinPlusGMTiedData can be built SYNTHETICALLY — map every neighbor class [M,σ] to [emptySM, (σ,0)] and transport the grade (the smithDataHom shortcut). Backing refutation: `SKEFTHawking.PinPlusGMTiedData.gmTiedStr_empty_grade16_ne_one`. -/
alias nogo_gmTiedStr_empty_grade16_ne_one := SKEFTHawking.PinPlusGMTiedData.gmTiedStr_empty_grade16_ne_one

/-- NO-GO [`5qH-injectivity-routes-all-equal-one-completeness-prop`] — do NOT re-derive. FALSE: The old-tied-carrier injectivity Props — Thom (hthom: SW-trivial Pin⁺ 4-manifold bounds), KT §5 (hle: ker(reduce16to8∘abkGMTied16) ⊆ range(n↦n•g8)), and grade-0 injectivity (hbound) — are DISTINCT OPEN nodes on pinPlusGMTiedData (k:=0) worth route-shopping between. Backing refutation: `SKEFTHawking.NonHausdorffBordismCollapse.grade0_eq_zero_of_nonHausdorff`. ⚠ SCOPE-LIMITED — read the SCOPE LIMIT for this fork in the module docstring before citing it; it supports LESS than the headline suggests. -/
alias nogo_grade0_eq_zero_of_nonHausdorff := SKEFTHawking.NonHausdorffBordismCollapse.grade0_eq_zero_of_nonHausdorff

/-- NO-GO [`5qH-injectivity-routes-all-equal-one-completeness-prop`] — do NOT re-derive. FALSE: The old-tied-carrier injectivity Props — Thom (hthom: SW-trivial Pin⁺ 4-manifold bounds), KT §5 (hle: ker(reduce16to8∘abkGMTied16) ⊆ range(n↦n•g8)), and grade-0 injectivity (hbound) — are DISTINCT OPEN nodes on pinPlusGMTiedData (k:=0) worth route-shopping between. Backing refutation: `SKEFTHawking.NonHausdorffBordismCollapse.dataBordismGMTied_mk_eq_iff_grade16_eq`. ⚠ SCOPE-LIMITED — read the SCOPE LIMIT for this fork in the module docstring before citing it; it supports LESS than the headline suggests. -/
alias nogo_dataBordismGMTied_mk_eq_iff_grade16_eq_2 := SKEFTHawking.NonHausdorffBordismCollapse.dataBordismGMTied_mk_eq_iff_grade16_eq

/-- NO-GO [`5qH-injectivity-routes-all-equal-one-completeness-prop`] — do NOT re-derive. FALSE: The old-tied-carrier injectivity Props — Thom (hthom: SW-trivial Pin⁺ 4-manifold bounds), KT §5 (hle: ker(reduce16to8∘abkGMTied16) ⊆ range(n↦n•g8)), and grade-0 injectivity (hbound) — are DISTINCT OPEN nodes on pinPlusGMTiedData (k:=0) worth route-shopping between. Backing refutation: `SKEFTHawking.PinPlusGMWitness.spin_range_ge_of_grade0_inj`. ⚠ SCOPE-LIMITED — read the SCOPE LIMIT for this fork in the module docstring before citing it; it supports LESS than the headline suggests. -/
alias nogo_spin_range_ge_of_grade0_inj := SKEFTHawking.PinPlusGMWitness.spin_range_ge_of_grade0_inj

/-- NO-GO [`5qH-injectivity-routes-all-equal-one-completeness-prop`] — do NOT re-derive. FALSE: The old-tied-carrier injectivity Props — Thom (hthom: SW-trivial Pin⁺ 4-manifold bounds), KT §5 (hle: ker(reduce16to8∘abkGMTied16) ⊆ range(n↦n•g8)), and grade-0 injectivity (hbound) — are DISTINCT OPEN nodes on pinPlusGMTiedData (k:=0) worth route-shopping between. Backing refutation: `SKEFTHawking.PinPlusGMWitness.omega4PinPlusGMTied_equiv_zmod16_via_kt_of_grade0`. ⚠ SCOPE-LIMITED — read the SCOPE LIMIT for this fork in the module docstring before citing it; it supports LESS than the headline suggests. -/
alias nogo_omega4PinPlusGMTied_equiv_zmod16_via_kt_of_grade0 := SKEFTHawking.PinPlusGMWitness.omega4PinPlusGMTied_equiv_zmod16_via_kt_of_grade0

/-- NO-GO [`5qH-injectivity-routes-all-equal-one-completeness-prop`] — do NOT re-derive. FALSE: The old-tied-carrier injectivity Props — Thom (hthom: SW-trivial Pin⁺ 4-manifold bounds), KT §5 (hle: ker(reduce16to8∘abkGMTied16) ⊆ range(n↦n•g8)), and grade-0 injectivity (hbound) — are DISTINCT OPEN nodes on pinPlusGMTiedData (k:=0) worth route-shopping between. Backing refutation: `SKEFTHawking.UnorientedThomCapstone.grade0_bounds_of_thom`. ⚠ SCOPE-LIMITED — read the SCOPE LIMIT for this fork in the module docstring before citing it; it supports LESS than the headline suggests. -/
alias nogo_grade0_bounds_of_thom := SKEFTHawking.UnorientedThomCapstone.grade0_bounds_of_thom

/-- NO-GO [`5qH-fg-ek-over-Z-blocked`] — do NOT re-derive. FALSE: The mod-2 Erdős–Kaplansky finiteness forcing (SingularUCFinite: self-duality forces finite dimension) transports to ℤ — in particular dualization over ℤ stays in the f.g./countable size class, so PD + UCT self-duality would force H²(M;ℤ) finitely generated. Backing refutation: `SKEFTHawking.FGDualityNoGo.dual_blowup_not_finite`. -/
alias nogo_dual_blowup_not_finite := SKEFTHawking.FGDualityNoGo.dual_blowup_not_finite

/-- NO-GO [`5qH-fg-ek-over-Z-blocked`] — do NOT re-derive. FALSE: The mod-2 Erdős–Kaplansky finiteness forcing (SingularUCFinite: self-duality forces finite dimension) transports to ℤ — in particular dualization over ℤ stays in the f.g./countable size class, so PD + UCT self-duality would force H²(M;ℤ) finitely generated. Backing refutation: `SKEFTHawking.FGDualityNoGo.not_finite_baerSpecker`. -/
alias nogo_not_finite_baerSpecker := SKEFTHawking.FGDualityNoGo.not_finite_baerSpecker

/-- NO-GO [`genuine-gm-carrier-eight-torsion`] — do NOT re-derive. FALSE: The genuine ℤ/16 lives directly on the thin GM carrier: DataBordismGrp(pinPlusGMData) ≃+ ZMod 16. Backing refutation: `SKEFTHawking.PinPlusGMDataZ16.pinPlusGMData_not_equiv_zmod16`. -/
alias nogo_pinPlusGMData_not_equiv_zmod16 := SKEFTHawking.PinPlusGMDataZ16.pinPlusGMData_not_equiv_zmod16

/-- NO-GO [`genuine-gm-carrier-eight-torsion`] — do NOT re-derive. FALSE: The genuine ℤ/16 lives directly on the thin GM carrier: DataBordismGrp(pinPlusGMData) ≃+ ZMod 16. Backing refutation: `SKEFTHawking.PinPlusGMDataZ16.pinPlusGMData_eight_torsion`. -/
alias nogo_pinPlusGMData_eight_torsion := SKEFTHawking.PinPlusGMDataZ16.pinPlusGMData_eight_torsion

/-- NO-GO [`enriques-datum-refuted-as-shaped`] — do NOT re-derive. FALSE: EnriquesDatum as shipped carries independent Enriques-geometry content (w₂ ≠ 0, π₁ = ℤ/2, the line bundle) usable as the W-D B-leaf. Backing refutation: `SKEFTHawking.PinPlusKTLeafGate.enriquesDatum_iff_kummerRep`. -/
alias nogo_enriquesDatum_iff_kummerRep := SKEFTHawking.PinPlusKTLeafGate.enriquesDatum_iff_kummerRep

/-- NO-GO [`enriques-datum-refuted-as-shaped`] — do NOT re-derive. FALSE: EnriquesDatum as shipped carries independent Enriques-geometry content (w₂ ≠ 0, π₁ = ℤ/2, the line bundle) usable as the W-D B-leaf. Backing refutation: `SKEFTHawking.PinPlusKTLeafGate.enriquesDatum_of_ktSpinPresentationDatum`. -/
alias nogo_enriquesDatum_of_ktSpinPresentationDatum := SKEFTHawking.PinPlusKTLeafGate.enriquesDatum_of_ktSpinPresentationDatum

/-- NO-GO [`enriques-datum-refuted-as-shaped`] — do NOT re-derive. FALSE: EnriquesDatum as shipped carries independent Enriques-geometry content (w₂ ≠ 0, π₁ = ℤ/2, the line bundle) usable as the W-D B-leaf. Backing refutation: `SKEFTHawking.PinPlusKTStepGate.emptySigmaRepresentable_of_geometric`. -/
alias nogo_emptySigmaRepresentable_of_geometric := SKEFTHawking.PinPlusKTStepGate.emptySigmaRepresentable_of_geometric

/-- NO-GO [`geometric-phi-does-not-close-hfwd-fakeability`] — do NOT re-derive. FALSE: Fixing Φ := spinForgetPhi (the geometric forgetful map) in the dA leaf makes hfwd a genuine geometric obligation not derivable from the conclusion. Backing refutation: `SKEFTHawking.PinPlusTraceLeafGate.spinForgetPhi_hfwd_of_ktNonSplit`. -/
alias nogo_spinForgetPhi_hfwd_of_ktNonSplit := SKEFTHawking.PinPlusTraceLeafGate.spinForgetPhi_hfwd_of_ktNonSplit

/-- NO-GO [`geometric-phi-does-not-close-hfwd-fakeability`] — do NOT re-derive. FALSE: Fixing Φ := spinForgetPhi (the geometric forgetful map) in the dA leaf makes hfwd a genuine geometric obligation not derivable from the conclusion. Backing refutation: `SKEFTHawking.PinPlusTraceLeafGate.spinForgetPhi_hfwd_iff_ktNonSplit`. -/
alias nogo_spinForgetPhi_hfwd_iff_ktNonSplit := SKEFTHawking.PinPlusTraceLeafGate.spinForgetPhi_hfwd_iff_ktNonSplit

/-- NO-GO [`dual-spin-opened-construction-conclusion-fakeable`] — do NOT re-derive. FALSE: Opening DualSpinFromW into DualSpinConstruction (Vspace/ιV/Vspin/hcob/hcover fields) makes the dual-spin supply a genuine geometric obligation not derivable from the conclusion. Backing refutation: `SKEFTHawking.PinPlusResidualGate.nonempty_dualSpinConstruction_iff_thirtytwo_dvd`. -/
alias nogo_nonempty_dualSpinConstruction_iff_thirtytwo_dvd := SKEFTHawking.PinPlusResidualGate.nonempty_dualSpinConstruction_iff_thirtytwo_dvd

/-- NO-GO [`dual-spin-opened-construction-conclusion-fakeable`] — do NOT re-derive. FALSE: Opening DualSpinFromW into DualSpinConstruction (Vspace/ιV/Vspin/hcob/hcover fields) makes the dual-spin supply a genuine geometric obligation not derivable from the conclusion. Backing refutation: `SKEFTHawking.PinPlusResidualGate.nonempty_ktSharpnessSupplyConstr_iff_hfwd`. -/
alias nogo_nonempty_ktSharpnessSupplyConstr_iff_hfwd := SKEFTHawking.PinPlusResidualGate.nonempty_ktSharpnessSupplyConstr_iff_hfwd

/-- NO-GO [`dual-spin-opened-construction-conclusion-fakeable`] — do NOT re-derive. FALSE: Opening DualSpinFromW into DualSpinConstruction (Vspace/ιV/Vspin/hcob/hcover fields) makes the dual-spin supply a genuine geometric obligation not derivable from the conclusion. Backing refutation: `SKEFTHawking.PinPlusResidualGate.spinOfSigMul16_sig`. -/
alias nogo_spinOfSigMul16_sig := SKEFTHawking.PinPlusResidualGate.spinOfSigMul16_sig

/-- NO-GO [`hker-opener-supplyGeo-is-non-reducing`] — do NOT re-derive. FALSE: Inhabiting KTSharpnessSupplyGeo and firing kerPhiSubDoubles_of_row_of_supplyGeo is a reducing step on hker -- i.e. Backing refutation: `SKEFTHawking.PinPlusKTHkerAmbPinGate.nonempty_ktSharpnessSupplyGeo_iff_hfwd`. -/
alias nogo_nonempty_ktSharpnessSupplyGeo_iff_hfwd := SKEFTHawking.PinPlusKTHkerAmbPinGate.nonempty_ktSharpnessSupplyGeo_iff_hfwd

/-- NO-GO [`hker-ambient-pin-does-not-restore-geometry`] — do NOT re-derive. FALSE: Pinning the ambient — requiring amb x hx = TopCat.of b.W of the genuine tethered witness, as binding round-12 spec 1 demands — restores geometric content to the dA/hker dual-spin supply, so an amb-pinned KTSharpnessSupplyGeo variant would be real progress on hker. Backing refutation: `SKEFTHawking.PinPlusKTHkerAmbPinGate.nonempty_dualSpinFromW_iff_thirtytwo_dvd`. -/
alias nogo_nonempty_dualSpinFromW_iff_thirtytwo_dvd := SKEFTHawking.PinPlusKTHkerAmbPinGate.nonempty_dualSpinFromW_iff_thirtytwo_dvd

/-- NO-GO [`hker-ambient-pin-does-not-restore-geometry`] — do NOT re-derive. FALSE: Pinning the ambient — requiring amb x hx = TopCat.of b.W of the genuine tethered witness, as binding round-12 spec 1 demands — restores geometric content to the dA/hker dual-spin supply, so an amb-pinned KTSharpnessSupplyGeo variant would be real progress on hker. Backing refutation: `SKEFTHawking.PinPlusKTHkerAmbPinGate.dualSpinFamily_iff_pointwise_thirtytwo_dvd`. -/
alias nogo_dualSpinFamily_iff_pointwise_thirtytwo_dvd := SKEFTHawking.PinPlusKTHkerAmbPinGate.dualSpinFamily_iff_pointwise_thirtytwo_dvd

/-- NO-GO [`hker-ambient-pin-does-not-restore-geometry`] — do NOT re-derive. FALSE: Pinning the ambient — requiring amb x hx = TopCat.of b.W of the genuine tethered witness, as binding round-12 spec 1 demands — restores geometric content to the dA/hker dual-spin supply, so an amb-pinned KTSharpnessSupplyGeo variant would be real progress on hker. Backing refutation: `SKEFTHawking.PinPlusKTHkerAmbPinGate.isClosedEmbedding_empty`. -/
alias nogo_isClosedEmbedding_empty := SKEFTHawking.PinPlusKTHkerAmbPinGate.isClosedEmbedding_empty

/-- NO-GO [`novikov-substrate-synthetic-inhabitation`] — do NOT re-derive. FALSE: The NovikovRealPairLES substrate (or any of the four equivalent σ-descent atom formulations) constitutes progress toward the Thom bordism-invariance of σ beyond the bare hbord statement. Backing refutation: `SKEFTHawking.PinPlusResidualGate.nonempty_novikovRealPairLES_diag`. -/
alias nogo_nonempty_novikovRealPairLES_diag := SKEFTHawking.PinPlusResidualGate.nonempty_novikovRealPairLES_diag

/-- NO-GO [`novikov-substrate-synthetic-inhabitation`] — do NOT re-derive. FALSE: The NovikovRealPairLES substrate (or any of the four equivalent σ-descent atom formulations) constitutes progress toward the Thom bordism-invariance of σ beyond the bare hbord statement. Backing refutation: `SKEFTHawking.PinPlusResidualGate.nonempty_novikovBoundaryRestriction_iff_sig_eq`. -/
alias nogo_nonempty_novikovBoundaryRestriction_iff_sig_eq := SKEFTHawking.PinPlusResidualGate.nonempty_novikovBoundaryRestriction_iff_sig_eq

/-- NO-GO [`novikov-substrate-synthetic-inhabitation`] — do NOT re-derive. FALSE: The NovikovRealPairLES substrate (or any of the four equivalent σ-descent atom formulations) constitutes progress toward the Thom bordism-invariance of σ beyond the bare hbord statement. Backing refutation: `SKEFTHawking.PinPlusResidualGate.novikovLagrangian_iff_hbord`. -/
alias nogo_novikovLagrangian_iff_hbord := SKEFTHawking.PinPlusResidualGate.novikovLagrangian_iff_hbord

/-- NO-GO [`novikov-geometric-tower-carrier-conclusion-fakeable`] — do NOT re-derive. FALSE: The NovikovGeometricPairLESData field row (the #196 genuine-tower carrier: rest2/delta/pairing/hexact/hnondeg/hadjDot) is a stronger-than-conclusion carrier whose inhabitation certifies genuine bounding-W tower progress. Backing refutation: `SKEFTHawking.PinPlusRoundThirteenGate.novikovGeometricPairLESDataOfRealPairLES`. -/
alias nogo_novikovGeometricPairLESDataOfRealPairLES := SKEFTHawking.PinPlusRoundThirteenGate.novikovGeometricPairLESDataOfRealPairLES

/-- NO-GO [`novikov-geometric-tower-carrier-conclusion-fakeable`] — do NOT re-derive. FALSE: The NovikovGeometricPairLESData field row (the #196 genuine-tower carrier: rest2/delta/pairing/hexact/hnondeg/hadjDot) is a stronger-than-conclusion carrier whose inhabitation certifies genuine bounding-W tower progress. Backing refutation: `SKEFTHawking.PinPlusRoundThirteenGate.nonempty_novikovGeometricPairLESData_iff_realPairLES`. -/
alias nogo_nonempty_novikovGeometricPairLESData_iff_realPairLES := SKEFTHawking.PinPlusRoundThirteenGate.nonempty_novikovGeometricPairLESData_iff_realPairLES

/-- NO-GO [`novikov-geometric-tower-carrier-conclusion-fakeable`] — do NOT re-derive. FALSE: The NovikovGeometricPairLESData field row (the #196 genuine-tower carrier: rest2/delta/pairing/hexact/hnondeg/hadjDot) is a stronger-than-conclusion carrier whose inhabitation certifies genuine bounding-W tower progress. Backing refutation: `SKEFTHawking.PinPlusRoundThirteenGate.nonempty_novikovGeometricPairLESData_iff_sig_eq`. -/
alias nogo_nonempty_novikovGeometricPairLESData_iff_sig_eq := SKEFTHawking.PinPlusRoundThirteenGate.nonempty_novikovGeometricPairLESData_iff_sig_eq

/-- NO-GO [`novikov-geometric-tower-carrier-conclusion-fakeable`] — do NOT re-derive. FALSE: The NovikovGeometricPairLESData field row (the #196 genuine-tower carrier: rest2/delta/pairing/hexact/hnondeg/hadjDot) is a stronger-than-conclusion carrier whose inhabitation certifies genuine bounding-W tower progress. Backing refutation: `SKEFTHawking.PinPlusRoundThirteenGate.nonempty_novikovGeometricPairLESData_diag`. -/
alias nogo_nonempty_novikovGeometricPairLESData_diag := SKEFTHawking.PinPlusRoundThirteenGate.nonempty_novikovGeometricPairLESData_diag

/-- NO-GO [`seam-transfer-open-support-uninhabitable`] — do NOT re-derive. FALSE: The CapstoneSeamTransfer / shared-cSeam CapstoneSeamTransferSeam consumption shape (the #184-#207 hasClass route: the top-face/disk-boundary splits with OPEN-complement supports hwOut/hvOut plus the literal htransfer equality) is inhabitable for a genuine fundamental cycle z and a proper attaching region, and the 3x-circled closed-S co-adaptation barrier (#198/#204/#207) is a machinery gap. Backing refutation: `SKEFTHawking.PinPlusTraceSeamTransferNoGo.wAtt_mem_subspaceChains_range_phi`. -/
alias nogo_wAtt_mem_subspaceChains_range_phi := SKEFTHawking.PinPlusTraceSeamTransferNoGo.wAtt_mem_subspaceChains_range_phi

/-- NO-GO [`seam-transfer-open-support-uninhabitable`] — do NOT re-derive. FALSE: The CapstoneSeamTransfer / shared-cSeam CapstoneSeamTransferSeam consumption shape (the #184-#207 hasClass route: the top-face/disk-boundary splits with OPEN-complement supports hwOut/hvOut plus the literal htransfer equality) is inhabitable for a genuine fundamental cycle z and a proper attaching region, and the 3x-circled closed-S co-adaptation barrier (#198/#204/#207) is a machinery gap. Backing refutation: `SKEFTHawking.PinPlusTraceSeamTransferNoGo.boundary_wAtt_eq_zero`. -/
alias nogo_boundary_wAtt_eq_zero := SKEFTHawking.PinPlusTraceSeamTransferNoGo.boundary_wAtt_eq_zero

/-- NO-GO [`seam-transfer-open-support-uninhabitable`] — do NOT re-derive. FALSE: The CapstoneSeamTransfer / shared-cSeam CapstoneSeamTransferSeam consumption shape (the #184-#207 hasClass route: the top-face/disk-boundary splits with OPEN-complement supports hwOut/hvOut plus the literal htransfer equality) is inhabitable for a genuine fundamental cycle z and a proper attaching region, and the 3x-circled closed-S co-adaptation barrier (#198/#204/#207) is a machinery gap. Backing refutation: `SKEFTHawking.PinPlusTraceSeamTransferNoGo.isEmpty_capstoneSeamTransfer_of_null`. -/
alias nogo_isEmpty_capstoneSeamTransfer_of_null := SKEFTHawking.PinPlusTraceSeamTransferNoGo.isEmpty_capstoneSeamTransfer_of_null

/-- NO-GO [`seam-transfer-open-support-uninhabitable`] — do NOT re-derive. FALSE: The CapstoneSeamTransfer / shared-cSeam CapstoneSeamTransferSeam consumption shape (the #184-#207 hasClass route: the top-face/disk-boundary splits with OPEN-complement supports hwOut/hvOut plus the literal htransfer equality) is inhabitable for a genuine fundamental cycle z and a proper attaching region, and the 3x-circled closed-S co-adaptation barrier (#198/#204/#207) is a machinery gap. Backing refutation: `SKEFTHawking.PinPlusTraceSeamTransferNoGo.isEmpty_capstoneSeamTransferSeam_of_null`. -/
alias nogo_isEmpty_capstoneSeamTransferSeam_of_null := SKEFTHawking.PinPlusTraceSeamTransferNoGo.isEmpty_capstoneSeamTransferSeam_of_null

/-- NO-GO [`k0-to-k1-transport-refuted`] — do NOT re-derive. FALSE: Every k = 0 charted space over the R^4 model is also a k >= 1 manifold -- i.e. Backing refutation: `SKEFTHawking.PinPlusRegularitySeparation.no_generic_zero_to_one_transport`. -/
alias nogo_no_generic_zero_to_one_transport := SKEFTHawking.PinPlusRegularitySeparation.no_generic_zero_to_one_transport

/-- NO-GO [`k0-to-k1-transport-refuted`] — do NOT re-derive. FALSE: Every k = 0 charted space over the R^4 model is also a k >= 1 manifold -- i.e. Backing refutation: `SKEFTHawking.PinPlusRegularitySeparationCarrier.exists_carrier_element_not_smooth`. -/
alias nogo_exists_carrier_element_not_smooth := SKEFTHawking.PinPlusRegularitySeparationCarrier.exists_carrier_element_not_smooth

/-- NO-GO [`collar-pair-closed-seam-attached-collar-bridge-is-FALSE`] — do NOT re-derive. FALSE: PinPlusTraceSeamResidualNarrow.ClosedSeamAttachedCollarBridge S a -- the closed-S attached-collar bridge, believed to be hctrlH's blocking atom and readable as an open-neighbourhood-to-closed-S collar deformation retraction. Backing refutation: `SKEFTHawking.PinPlusTraceSeamCollarBridgeNoGo.collar_bridge_refuted`. -/
alias nogo_collar_bridge_refuted := SKEFTHawking.PinPlusTraceSeamCollarBridgeNoGo.collar_bridge_refuted

/-- NO-GO [`collar-pair-closed-seam-attached-collar-bridge-is-FALSE`] — do NOT re-derive. FALSE: PinPlusTraceSeamResidualNarrow.ClosedSeamAttachedCollarBridge S a -- the closed-S attached-collar bridge, believed to be hctrlH's blocking atom and readable as an open-neighbourhood-to-closed-S collar deformation retraction. Backing refutation: `SKEFTHawking.PinPlusTraceSeamCollarBridgeNoGo.attachedBridge_iff_support_dichotomy`. -/
alias nogo_attachedBridge_iff_support_dichotomy := SKEFTHawking.PinPlusTraceSeamCollarBridgeNoGo.attachedBridge_iff_support_dichotomy

/-- NO-GO [`collar-pair-face-row-forces-seam-to-meet-boundary`] — do NOT re-derive. FALSE: A CollarPairGeomFace row (the houtPair producer: the row's own supports plus the seam-annulus containment hseamAnn) can be inhabited with an ENTIRELY INTERIOR seam -- i.e. Backing refutation: `SKEFTHawking.PinPlusTraceCapstoneCollarPairFace.CollarPairGeomFace.exists_seamPoint_mem_bd_of_null`. -/
alias nogo_exists_seamPoint_mem_bd_of_null := SKEFTHawking.PinPlusTraceCapstoneCollarPairFace.CollarPairGeomFace.exists_seamPoint_mem_bd_of_null

/-- NO-GO [`collar-pair-face-row-forces-seam-to-meet-boundary`] — do NOT re-derive. FALSE: A CollarPairGeomFace row (the houtPair producer: the row's own supports plus the seam-annulus containment hseamAnn) can be inhabited with an ENTIRELY INTERIOR seam -- i.e. Backing refutation: `SKEFTHawking.PinPlusTraceCapstoneCollarPairFace.CollarPairGeomFace.exists_seamPoint_mem_range_eM'_of_null`. -/
alias nogo_exists_seamPoint_mem_range_eM'_of_null := SKEFTHawking.PinPlusTraceCapstoneCollarPairFace.CollarPairGeomFace.exists_seamPoint_mem_range_eM'_of_null

/-- NO-GO [`collar-pair-open-complement-annulus-is-refuted-shape`] — do NOT re-derive. FALSE: houtPair (the collar-annulus weld obligation of the #212 collar-pair row) can be discharged for free by reading its three required boundary-supports straight off the in-tree SurgeredEndDatum, whose d.topFaceCovered (fromCyl '' (topface \ range phi) subset dW), d.sphereFaceCovered (fromHandle '' (sphere \ S) subset dW) and bottom-face fact are exactly the three supports hbd_ofTransfer consumes. Backing refutation: `SKEFTHawking.PinPlusTraceCapstoneCollarPairCore.collarAnnulusOpen_toSeamTransferSeam`. -/
alias nogo_collarAnnulusOpen_toSeamTransferSeam := SKEFTHawking.PinPlusTraceCapstoneCollarPairCore.collarAnnulusOpen_toSeamTransferSeam

/-- NO-GO [`collar-pair-open-complement-annulus-is-refuted-shape`] — do NOT re-derive. FALSE: houtPair (the collar-annulus weld obligation of the #212 collar-pair row) can be discharged for free by reading its three required boundary-supports straight off the in-tree SurgeredEndDatum, whose d.topFaceCovered (fromCyl '' (topface \ range phi) subset dW), d.sphereFaceCovered (fromHandle '' (sphere \ S) subset dW) and bottom-face fact are exactly the three supports hbd_ofTransfer consumes. Backing refutation: `SKEFTHawking.PinPlusTraceCapstoneCollarPairCore.not_collarAnnulusOpen_of_null`. -/
alias nogo_not_collarAnnulusOpen_of_null := SKEFTHawking.PinPlusTraceCapstoneCollarPairCore.not_collarAnnulusOpen_of_null

/-- NO-GO [`collar-pair-maximal-core-reenters-refuted-support`] — do NOT re-derive. FALSE: The CollarPairGeom/CollarPairGeomUnsub row's anti-fake tether hcoreHit can be made free of charge by enlarging the #210 shrunk core K toward its limit K = univ, keeping the rest of the collar-pair split data intact. Backing refutation: `SKEFTHawking.PinPlusTraceCapstoneCollarPairGeom.coreHit_of_univ`. -/
alias nogo_coreHit_of_univ := SKEFTHawking.PinPlusTraceCapstoneCollarPairGeom.coreHit_of_univ

/-- NO-GO [`collar-pair-maximal-core-reenters-refuted-support`] — do NOT re-derive. FALSE: The CollarPairGeom/CollarPairGeomUnsub row's anti-fake tether hcoreHit can be made free of charge by enlarging the #210 shrunk core K toward its limit K = univ, keeping the rest of the collar-pair split data intact. Backing refutation: `SKEFTHawking.PinPlusTraceCapstoneCollarPairGeom.houtC_support_univ_eq_refuted`. -/
alias nogo_houtC_support_univ_eq_refuted := SKEFTHawking.PinPlusTraceCapstoneCollarPairGeom.houtC_support_univ_eq_refuted

/-- NO-GO [`k7-seam-cover-interior-fails`] — do NOT re-derive. FALSE: K7SeamCoverHyp (the K7 opener's un-thickened MV cover hypothesis): the INTERIORS of the two closed Kummer-weld pieces (the Q-image and the 16 closed E-images) cover the welded K3 carrier, so the Mayer-Vietoris assembly can run on the un-thickened pieces directly. Backing refutation: `SKEFTHawking.KummerK7SeamCoverNoGo.k7SeamCoverHyp_false`. -/
alias nogo_k7SeamCoverHyp_false := SKEFTHawking.KummerK7SeamCoverNoGo.k7SeamCoverHyp_false

/-- NO-GO [`gram-literal-equality-is-choice-dependent`] — do NOT re-derive. FALSE: SphereProdGramPin - the LITERAL matrix equality `interMatrix fc B = sphereProdFormDatum` on the computed rank-2 basis - is a disclosed GEOMETRIC residual of the S2xS2 intersection form, dischargeable once the Kunneth/EZ cross value is pinned. Backing refutation: `SKEFTHawking.SphereProdGramPinRetire.sphereProd_interMatrix_computed_eq`. -/
alias nogo_sphereProd_interMatrix_computed_eq := SKEFTHawking.SphereProdGramPinRetire.sphereProd_interMatrix_computed_eq

/-- NO-GO [`gram-literal-equality-is-choice-dependent`] — do NOT re-derive. FALSE: SphereProdGramPin - the LITERAL matrix equality `interMatrix fc B = sphereProdFormDatum` on the computed rank-2 basis - is a disclosed GEOMETRIC residual of the S2xS2 intersection form, dischargeable once the Kunneth/EZ cross value is pinned. Backing refutation: `SKEFTHawking.SphereProdGramPinRetire.sphereProdGramPin_iff`. -/
alias nogo_sphereProdGramPin_iff := SKEFTHawking.SphereProdGramPinRetire.sphereProdGramPin_iff

/-- NO-GO [`kronecker-dual-is-not-the-h1-enhancement-transport`] — do NOT re-derive. FALSE: The Kronecker/UCT dual of the carried cohomology basis - `homologyBasisOfCohomologyBasis basis`, the value `pinCharSurfaceOfBundled` puts in `PinCharSurface.H1Iso` (and the value `GeoRealizationTied.derivedEsigma`/`derivedEtau` put in the seam) - is the identification of `H1(Sigma;Z/2)` that the enhancement `q` lives on, so `q` may be evaluated at those coordinates at ANY rank. Backing refutation: `SKEFTHawking.CharSurfacePDTransport.homologyCoords_gauge`. -/
alias nogo_homologyCoords_gauge := SKEFTHawking.CharSurfacePDTransport.homologyCoords_gauge

/-- NO-GO [`kronecker-dual-is-not-the-h1-enhancement-transport`] — do NOT re-derive. FALSE: The Kronecker/UCT dual of the carried cohomology basis - `homologyBasisOfCohomologyBasis basis`, the value `pinCharSurfaceOfBundled` puts in `PinCharSurface.H1Iso` (and the value `GeoRealizationTied.derivedEsigma`/`derivedEtau` put in the seam) - is the identification of `H1(Sigma;Z/2)` that the enhancement `q` lives on, so `q` may be evaluated at those coordinates at ANY rank. Backing refutation: `SKEFTHawking.CharSurfacePDTransport.not_forall_kroneckerTransport_gauge_invariant`. -/
alias nogo_not_forall_kroneckerTransport_gauge_invariant := SKEFTHawking.CharSurfacePDTransport.not_forall_kroneckerTransport_gauge_invariant

/-- NO-GO [`kronecker-dual-is-not-the-h1-enhancement-transport`] — do NOT re-derive. FALSE: The Kronecker/UCT dual of the carried cohomology basis - `homologyBasisOfCohomologyBasis basis`, the value `pinCharSurfaceOfBundled` puts in `PinCharSurface.H1Iso` (and the value `GeoRealizationTied.derivedEsigma`/`derivedEtau` put in the seam) - is the identification of `H1(Sigma;Z/2)` that the enhancement `q` lives on, so `q` may be evaluated at those coordinates at ANY rank. Backing refutation: `SKEFTHawking.CharSurfacePDTransport.hyperbolic2_taylor_flip`. -/
alias nogo_hyperbolic2_taylor_flip := SKEFTHawking.CharSurfacePDTransport.hyperbolic2_taylor_flip

/-- NO-GO [`kronecker-dual-is-not-the-h1-enhancement-transport`] — do NOT re-derive. FALSE: The Kronecker/UCT dual of the carried cohomology basis - `homologyBasisOfCohomologyBasis basis`, the value `pinCharSurfaceOfBundled` puts in `PinCharSurface.H1Iso` (and the value `GeoRealizationTied.derivedEsigma`/`derivedEtau` put in the seam) - is the identification of `H1(Sigma;Z/2)` that the enhancement `q` lives on, so `q` may be evaluated at those coordinates at ANY rank. Backing refutation: `SKEFTHawking.CharSurfacePDTransport.gramMap_hyperbolic2`. -/
alias nogo_gramMap_hyperbolic2 := SKEFTHawking.CharSurfacePDTransport.gramMap_hyperbolic2

/-- NO-GO [`kronecker-dual-is-not-the-h1-enhancement-transport`] — do NOT re-derive. FALSE: The Kronecker/UCT dual of the carried cohomology basis - `homologyBasisOfCohomologyBasis basis`, the value `pinCharSurfaceOfBundled` puts in `PinCharSurface.H1Iso` (and the value `GeoRealizationTied.derivedEsigma`/`derivedEtau` put in the seam) - is the identification of `H1(Sigma;Z/2)` that the enhancement `q` lives on, so `q` may be evaluated at those coordinates at ANY rank. Backing refutation: `SKEFTHawking.CharSurfacePDBundled.pinCharSurfaceOfBundled_gauge_H1Iso`. -/
alias nogo_pinCharSurfaceOfBundled_gauge_H1Iso := SKEFTHawking.CharSurfacePDBundled.pinCharSurfaceOfBundled_gauge_H1Iso

end SKEFTHawking.KernelNoGos
