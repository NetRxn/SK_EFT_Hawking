{
  "name": "skeft-local",
  "owner": {
    "name": "SK-EFT Hawking project"
  },
  "metadata": {
    "description": "Local marketplace for SK_EFT_Hawking in-repo developer plugins. Marketplace catalog is tracked in-repo; plugins are enabled per-machine via .claude/settings.local.json enabledPlugins (untracked).",
    "version": "1.0.0"
  },
  "plugins": [
    {
      "name": "skeft-qa",
      "source": "./.claude/plugins/skeft-qa",
      "description": "Fresh-context Stage-13 paper review (does it render, is it backed, is it wrong, does it land) plus the loop-durability dev-harness that keeps native /goal loops on-track across compactions and guards web egress fail-closed. Component roster lives in the plugin's README.md, not here, so this line cannot go stale."
    }
  ]
}
