\documentclass[aps,prl,twocolumn,superscriptaddress,nofootinbib]{revtex4-2}
\usepackage{amsmath,amssymb,amsfonts}
\usepackage{graphicx}
\usepackage{hyperref}
\usepackage{braket}

%% Canonical theorem / module / Aristotle counts. Regenerated by
%% scripts/update_counts.py. Use \totaltheorems{}, \aristotleproved{},
%% \leanmodules{}, \axiomcount{} instead of inline literals (NumericalFreshness
%% Gate 9 — Stage-13 fix-pass 2026-05-11).
\input{../../docs/counts.tex}
\newcommand{\mathlibcommit}{5e932f97}

%% Bundle: L2 (tier 2, target PRL)
%% Source manifest: papers/L2/source_manifest.md
%% Source paper: paper10_modular_generation
%% Phase 7b sub-wave 7b.3 — L2 lift

\begin{document}

\title{Three Generations of Standard-Model Fermions from Modular Invariance:\\
A Machine-Checked Derivation}

\author{John G. Roehm}
\affiliation{Independent Researcher}
\email{jgroehm@gmail.com}

\date{\today}

\begin{abstract}
We present a formally verified derivation
that the number of Standard-Model fermion generations satisfies
$N_f \equiv 0 \pmod{3}$,
following the global modular constraint of Wang~\cite{Wang2024}. The
chain combines two independent ingredients machine-checked in Lean~4:
the chiral central charge $c_- = 8 N_f$ derived from $16$ left-handed
Weyl components per generation in the with-$\nu_R$ convention, and
the framing-anomaly constraint $24 \mid c_-$ following from
$\eta(\tau+1) = e^{2\pi i/24}\,\eta(\tau)$ via Mathlib's
\texttt{Complex.exp\_add}. The minimal nontrivial solution is
$N_f = 3$. Without right-handed neutrinos, $c_- = 15/2$ is fractional
for any odd $N_f$ (in particular for the observed $N_f = 3$),
giving a formal independent argument for $\nu_R$ from modular
invariance alone. The algebraic kernel of the Rokhlin bound is
verified through homological degree~5 by an explicit minimal free
resolution of $\mathbb{F}_2$ over the Steenrod sub-algebra $A(1)$
(verified via \texttt{A1Resolution.lean}: chain-complex property
$d_n \cdot d_{n+1} = 0$ for $n=1,\ldots,4$ via \texttt{native\_decide}
on explicit $\mathbb{F}_2$ matrices, kernel cardinalities for
$d_1,d_2,d_3$, and RREF rank certificates $d_4=9$, $d_5=15$);
applying the standard minimal-resolution-implies-Ext-dimension lemma
yields $\dim \mathrm{Ext}^n_{A(1)}(\mathbb{F}_2,\mathbb{F}_2) =
1,2,2,2,3,4$ for $n = 0, \ldots, 5$. The change-of-rings adjunction
$\mathrm{Hom}_A(A \otimes_{A(1)} M, N) \cong \mathrm{Hom}_{A(1)}(M, N)$
enters as a standard Cartan-Eilenberg homological-algebra theorem
(textbook content; the algebraic Ext-over-$A(1)$ kernel above is what
this Letter contributes at the machine-checked level). The Rokhlin
divisibility $16 \mid \sigma(M)$ is itself a kernel-pure Lean theorem
(\texttt{SmoothSpinManifold4.rokhlin}): for an even-unimodular
intersection form it follows from the machine-checked algebraic half
$8 \mid \sigma$ together with the single topological input
$2 \mid \sigma/8$ ($\hat{A}$-genus even). Three textbook topology
results (ko cohomology, ASS convergence, ABP splitting) --- all
established results in the algebraic-topology literature, tracked
pending Mathlib formalization rather than open problems --- enter
only as
commentary on the separate bordism-group identification
$\Omega_5^{\mathrm{Spin}^{\mathbb{Z}_4}} \cong \mathbb{Z}_{16}$. The
verification is fully automated by
\texttt{lake build} on a \totaltheorems{}-theorem \leanmodules{}-module
library with \axiomcount{} axioms and zero
\texttt{sorry}.
\end{abstract}

\maketitle

%% ══════════════════════════════════════════════════════════════════
\section{Introduction}

The Standard Model has three generations of fermions, and the
perturbative gauge anomalies cancel within each generation
independently~\cite{GrossJackiw1972}. The question of why nature
chose three rather than one, two, or four is therefore unresolved at
the perturbative level. Wang~\cite{Wang2024} proposed that a
\emph{global} modular constraint --- $24 \mid c_-$ on the chiral
central charge of the partition function under the modular
$T$-transformation $\tau \to \tau+1$ --- combined with the SM's
fermion content $c_- = 8 N_f$, forces $N_f \equiv 0 \pmod{3}$. The
proposal recasts the empirical three-generation count as the minimal
output of a topological consistency requirement and is therefore
falsifiable by any internally consistent SM extension violating the
constraint.

This Letter records a machine-checked
formalization of this chain in Lean~4 with Mathlib. Every coefficient on the path from
modular forms to particle counting is either derived from Mathlib
infrastructure or registered as a tracked assumption with explicit
hypothesis status. The two independent pieces converge through the
arithmetic identity $24 = 8 \times 3$, where $8$ is fixed by the SM
fermion content and $24$ is fixed by the Dedekind eta function.

%% ══════════════════════════════════════════════════════════════════
\section{The chiral central charge from SM fermion content}
\label{sec:central_charge}

In the with-$\nu_R$ convention each SM generation contains $16$
left-handed Weyl components:

\begin{center}
\begin{tabular}{lccc}
\hline
Multiplet & $SU(3) \times SU(2)$ & Components & $c$ \\
\hline
$Q_L$ & $(\mathbf{3}, \mathbf{2})$ & 6 & 3 \\
$\bar{u}_R$ & $(\bar{\mathbf{3}}, \mathbf{1})$ & 3 & 3/2 \\
$\bar{d}_R$ & $(\bar{\mathbf{3}}, \mathbf{1})$ & 3 & 3/2 \\
$L$ & $(\mathbf{1}, \mathbf{2})$ & 2 & 1 \\
$\bar{e}_R$ & $(\mathbf{1}, \mathbf{1})$ & 1 & 1/2 \\
$\bar{\nu}_R$ & $(\mathbf{1}, \mathbf{1})$ & 1 & 1/2 \\
\hline
Total & & 16 & 8 \\
\hline
\end{tabular}
\end{center}

Each Weyl fermion contributes $c = 1/2$ to the chiral central charge
upon $4 \to 2$ dimensional reduction~\cite{AlvarezGaumeWitten1984}.
The coefficient $8$ in $c_- = 8 N_f$ is therefore not free: it is
\emph{derived} from the count
$\texttt{total\_components\_with\_nu\_R} = 16$ by the Lean theorem
\texttt{WangBridge.fermion\_count\_gives\_central\_charge}.

\paragraph{Without $\nu_R$.} Removing right-handed neutrinos drops
the Weyl count to $15$ and gives $c_- = 15/2$ per generation. A
fractional central charge is a gravitational anomaly: the 2D theory
is mathematically inconsistent~\cite{AlvarezGaumeWitten1984}. The
Lean theorem
\texttt{WangBridge.central\_charge\_fractional\_without\_nu\_R}
encodes this by constructing an explicit contradiction from the
assumption $15/2 \in \mathbb{N}$, complementing the
$\mathbb{Z}_{16}$ anomaly argument of
Garc\'ia-Etxebarria and Montero~\cite{GarciaEtxebarria2019}.

%% ══════════════════════════════════════════════════════════════════
\section{The framing anomaly from the Dedekind eta function}
\label{sec:framing}

The Dedekind eta function
\begin{equation}
\eta(\tau) = q^{1/24} \prod_{n=1}^{\infty}(1 - q^n),
\quad q = e^{2\pi i \tau},
\end{equation}
satisfies $\eta(\tau+1) = e^{2\pi i/24}\,\eta(\tau)$. Under the
$T$-transformation $\tau \to \tau+1$, the infinite product is
invariant since $q(\tau+1) = q(\tau)$, while the prefactor $q^{1/24}$
shifts by $e^{2\pi i / 24}$. The shift is verified in
\texttt{ModularInvarianceConstraint.qParam\_shift} by reduction to
Mathlib's \texttt{Complex.exp\_add}; no project-originated lemma is
required. The ``24'' in the modular phase has two complementary
origins: analytically, the exponent in the $q$-expansion of
$\eta(\tau)$~\cite{Rademacher1973}; physically, the Casimir energy
$E_0 = -c/24$ on a 2D conformal field theory torus, traceable to
zeta regularization $\zeta(-1) = -1/12$ and the framing anomaly on a
spin 3-manifold~\cite{diFrancesco1997}.

The 2D partition function transforms as
$Z(\tau+1) = e^{2\pi i c_-/24}\,Z(\tau)$. Modular invariance demands
$e^{2\pi i c_-/24} = 1$, equivalently $24 \mid c_-$, formalized as
\texttt{ModularInvarianceConstraint.framing\_anomaly\_constraint}.
Combining with $c_- = 8 N_f$:
\begin{equation}
24 \mid 8 N_f \quad \Longleftrightarrow \quad 3 \mid N_f.
\label{eq:gen_constraint}
\end{equation}
The constraint is sharp: $24 \nmid 8$ and $24 \nmid 16$, but
$24 \mid 24$. The factorization $24 = 8 \times 3$ separates the
mathematical input (zeta regularization gives $24$) from the physical
input (fermion counting gives $8$); their ratio is the generation
constraint.

\begin{figure}[t]
\centering
\includegraphics[width=\columnwidth]{figures/fig75_modular_invariance_phase.png}
\caption{The $T$-transformation phase $e^{2\pi i c_-/24}$ at
$c_- = 8 N_f$ for $N_f = 1,\ldots,12$. The phase cycles through cube
roots of unity $\{1, \omega, \omega^2\}$, with phase $= 1$ (modular
invariant, blue ring) only for $N_f \equiv 0 \pmod{3}$
(i.e.\ $N_f \in \{3, 6, 9, 12\}$). Grey markers denote excluded
generation counts $N_f \in \{1,2,4,5,7,8,10,11\}$.}
\label{fig:phase}
\end{figure}

\begin{figure}[t]
\centering
\includegraphics[width=\columnwidth]{figures/fig73_sm_generation_constraint.png}
\caption{Chiral central charge $c_- = 8 N_f$ versus the modular
invariance band $c_- \equiv 0 \pmod{24}$. Blue bars denote $N_f$
divisible by three (modular invariant). Grey bars denote excluded
generation counts.}
\label{fig:gen_constraint}
\end{figure}

%% ══════════════════════════════════════════════════════════════════
\section{Machine-checked $\mathrm{Ext}^n_{A(1)}$ computation through degree 5}
\label{sec:ext}

The Rokhlin bound --- that
$\sigma(M) \equiv 0 \pmod{16}$ on a closed spin 4-manifold rather than
the weaker $\sigma \equiv 0 \pmod{8}$ --- is what
distinguishes the $\mathbb{Z}_{16}$ anomaly classification from a
purely $E_8$-lattice-driven $\mathbb{Z}_8$, and is now a kernel-pure
Lean theorem (\texttt{SmoothSpinManifold4.rokhlin}; see the close of
this section). The $\mathrm{Ext}_{A(1)}$ computation below supplies the
algebraic substrate of the Adams-spectral-sequence route to the same
bound. To formalize this we
compute the graded $\mathbb{F}_2$-dimensions of
$\mathrm{Ext}^n_{A(1)}(\mathbb{F}_2, \mathbb{F}_2)$ through
homological degree five, where $A(1) = \langle \mathrm{Sq}^1,
\mathrm{Sq}^2 \rangle$ is the eight-dimensional sub-Hopf algebra of
the Steenrod algebra. The computation is shipped in three modules.
\texttt{A1Ring.lean} formalizes the left regular representation, the
Adem relation $\mathrm{Sq}^1 \mathrm{Sq}^1 = 0$ and the Cartan
relation $\mathrm{Sq}^1 \mathrm{Sq}^2 = \mathrm{Sq}^2 \mathrm{Sq}^1
+ \mathrm{Sq}^3$ via \texttt{native\_decide}.
\texttt{A1Resolution.lean} constructs explicit $\mathbb{F}_2$
differential matrices $d_1, \ldots, d_5$, verifies the chain-complex
identity $d_n \cdot d_{n+1} = 0$ (\texttt{d1\_d2\_zero} through
\texttt{d4\_d5\_zero}, by \texttt{native\_decide} on explicit
matrices), discharges kernel cardinalities for $d_1, d_2, d_3$ by
enumeration, and supplies RREF rank certificates
\texttt{d4\_rank\_9} and \texttt{d5\_rank\_15} (the $P_4 \cdot d_4 =
\mathrm{rref}_4$ identity is verified by \texttt{native\_decide}
together with the invertibility witness $P_4 \cdot P_4^{-1} = I$).
\texttt{A1Ext.lean} records the resolution-rank values
\begin{equation}
1, 2, 2, 2, 3, 4 \quad (n = 0, 1, 2, 3, 4, 5),
\end{equation}
which the standard minimal-resolution-implies-Ext-dimension lemma
identifies as
$\dim_{\mathbb{F}_2} \mathrm{Ext}^n_{A(1)}(\mathbb{F}_2, \mathbb{F}_2)$
through degree five.

The change-of-rings adjunction
$\mathrm{Hom}_A(A \otimes_{A(1)} M, N) \cong \mathrm{Hom}_{A(1)}(M, N)$
is the standard Cartan-Eilenberg
result~\cite{Adams1974}; the algebraic kernel formally
discharged by the modules above is the
$\mathrm{Ext}_{A(1)}$ computation at the resolution + chain-complex
+ rank-witness level, which is what the Adams spectral sequence
ultimately consumes once the change-of-rings reduction is
applied. \texttt{ChangeOfRings.lean} records this reduction at the
predicate-level (the substantive Mathlib formalization of the
adjunction over a non-commutative sub-Hopf-algebra is itself a
homological-algebra development outside the scope of this Letter).

Three textbook topology results bridge from $\mathrm{Ext}_{A(1)}$
to the bordism group $\Omega_5^{\mathrm{Spin}^{\mathbb{Z}_4}} \cong
\mathbb{Z}_{16}$: ko cohomology of a point through degree
five~\cite{Adams1974}, Adams spectral sequence
convergence~\cite{BeaudryCampbell}, and the ABP splitting
$\mathrm{MSpin} \cong \mathrm{ko} \vee \mathrm{ko}\langle 2 \rangle
\vee \cdots$~\cite{ABP1967}. Each is an established result in the
algebraic-topology literature, tracked pending Mathlib
formalization of the requisite infrastructure (spectra, the Adams
spectral sequence, Thom spectra) --- a library gap, not an open
problem. Each enters as standard textbook
content cited at the bibliographic level, documenting this
bordism-group identification; the algebraic-Ext kernel
above is what this Letter formalizes at the machine-checked level.
The Rokhlin divisibility $16 \mid \sigma(M)$ itself, by contrast, is
not deferred to these citations: it is a kernel-pure Lean theorem
(\texttt{SmoothSpinManifold4.rokhlin}), established from the
even-unimodular intersection form via the machine-checked half
$8 \mid \sigma$ (\texttt{eight\_dvd\_latticeSig}, by van der Blij
classification --- Hasse--Minkowski and theta-modularity, both
discharged) together with the single topological factor-of-two input
$2 \mid \sigma/8$.

%% ══════════════════════════════════════════════════════════════════
\section{The 16-convergence and the role of $\nu_R$}

The number 16 appears in four formally distinct settings:
\textit{(i)} the Weyl-component count per SM generation in the
with-$\nu_R$ convention; \textit{(ii)} the bordism group
$\Omega_5^{\mathrm{Spin}^{\mathbb{Z}_4}} \cong \mathbb{Z}_{16}$
classifying global anomalies of theories with a $\mathbb{Z}_4$
symmetry~\cite{GarciaEtxebarria2019}; \textit{(iii)} the Rokhlin
divisor in the signature of a closed spin 4-manifold,
$\sigma(M) \equiv 0 \pmod{16}$~\cite{Rokhlin1952}; and \textit{(iv)}
the period of the Kitaev periodic table of free-fermion topological
superconductors in symmetry class~D~\cite{Kitaev2009}, reduced from
$\mathbb{Z}$ to $\mathbb{Z}_{16}$ under
interactions~\cite{FidkowskiKitaev2010}.

Of these, the Rokhlin divisor (iii) is itself an unconditional
kernel-pure Lean theorem: \texttt{SmoothSpinManifold4.rokhlin} proves
$16 \mid \sigma(M)$ for an even-unimodular intersection form from the
machine-checked half $8 \mid \sigma$ (\texttt{eight\_dvd\_latticeSig})
plus the single topological input $2 \mid \sigma/8$, with no global
Rokhlin hypothesis and no project axiom (standard axioms only). The
unconditional enumeration of the four occurrences is recorded as
\texttt{SpinRokhlinInterface.sixteen\_convergence\_unconditional},
whose $16 \mid \sigma$ conjunct is now this theorem rather than an
assumed input. Wang~\cite{Wang2024} establishes the
algebraic-topology \emph{equivalence} between (i)--(iii) via the
string-bordism $\mathbb{Z}_{24}$ class and the associated Smith
homomorphism; that four-way equivalence, and the bordism-group
identification $\Omega_5^{\mathrm{Spin}^{\mathbb{Z}_4}} \cong
\mathbb{Z}_{16}$, remain external inputs --- but the divisibility
$16 \mid \sigma$ itself is no longer among them. The Kitaev
relationship to (iv) is suggestive but not formalized.

The constraints from the $\mathbb{Z}_{16}$ anomaly and modular
invariance behave differently with respect to $\nu_R$:

\begin{center}
\begin{tabular}{lccc}
\hline
& $\mathbb{Z}_{16}$ ($\nu_R$ in) & $\mathbb{Z}_{16}$ ($\nu_R$ out) & Modular inv. \\
\hline
Constraint & none & $16 \mid N_f$ & $3 \mid N_f$ \\
Minimal $N_f$ & --- & $16$ & $3$ \\
Combined min & $\mathbf{3}$ & $\mathrm{lcm}(16,3) = \mathbf{48}$ & --- \\
\hline
\end{tabular}
\end{center}

With $\nu_R$, the $\mathbb{Z}_{16}$ anomaly cancels for every $N_f$
(\texttt{RokhlinBridge.z16\_anomaly\_always\_cancels\_with\_nu\_R}),
so the only remaining constraint is modular invariance and the
minimal solution is $N_f = 3$. Without $\nu_R$, both constraints must
hold simultaneously, forcing
$N_f = \mathrm{lcm}(16,3) = 48$
(\texttt{RokhlinBridge.constraints\_without\_nu\_R}). Since $N_f = 3$
is observed, the joint constraint is direct evidence for $\nu_R$.
The Rokhlin bound is sharp: the K3 surface saturates with
$\sigma = -16$, while the $E_8$ manifold has $\sigma = 8$ but is not
spin --- formalized as
\texttt{RokhlinBridge.rokhlin\_strictly\_stronger}: $16 \nmid 8$.

%% ══════════════════════════════════════════════════════════════════
\section{Conclusion}

Combining $c_- = 8 N_f$ from SM fermion counting with $24 \mid c_-$
from Dedekind-eta modular invariance gives $N_f \equiv 0 \pmod{3}$,
with $N_f = 3$ as the minimal nontrivial solution. The chain is
machine-checked end to end: the coefficient $8$ via Mathlib finite
sums in \texttt{SMFermionData.lean}; the modular factor $24$ via
\texttt{Complex.exp\_add} in \texttt{ModularInvarianceConstraint.lean};
the divisibility step via
\texttt{Nat.Coprime.dvd\_of\_dvd\_mul\_left} in
\texttt{GenerationConstraint.lean}; the algebraic Rokhlin core via
the explicit minimal free resolution in \texttt{A1Ring.lean},
\texttt{A1Resolution.lean}, and \texttt{A1Ext.lean}; the
change-of-rings step in \texttt{ChangeOfRings.lean}; and the Rokhlin
divisibility $16 \mid \sigma$ itself as the unconditional kernel-pure
theorem \texttt{SmoothSpinManifold4.rokhlin}, modulo the single
topological input $2 \mid \sigma/8$. The
$24 \mid c_-$ constraint admits a deeper structural reading: the
five-step chain spin-bordism $\Omega_5^{\mathrm{Spin}}(BG_{\mathrm{SM}})$
$\to$ anomaly polynomial $\to$ modular invariance $\to$
Niemeier-lattice classification~\cite{Niemeier1973} $\to$ Schellekens'
classification of meromorphic $c=24$ CFTs~\cite{Schellekens1993},
geometrically completed for the strongly-rational, weight-1-non-trivial
subset by M\"oller--Scheithauer~\cite{MollerScheithauer2024}, places
$24 \mid c_-$ as a corollary of holomorphic-VOA classification rather
than a stand-alone modular-consistency requirement; the deep companion
paper~\cite{Roehm2026D2} carries the full derivation. The
without-$\nu_R$ argument upgrades from suggestive to formal: a
fractional central charge $c_- = 15/2$ is gravitationally anomalous;
no fractional natural number exists; therefore $\nu_R$ exists if the
modular framing-anomaly constraint applies to the SM. The full
verification builds in $\sim 30~\mathrm{s}$ on commodity hardware
under Lean~4 v4.29.1 against Mathlib commit \texttt{\mathlibcommit{}}
(library state: \totaltheorems{} theorems across \leanmodules{}
modules, \axiomcount{} axioms, zero \texttt{sorry}; the formerly
asserted \texttt{gapped\_interface\_axiom} is now a tracked Prop
\texttt{TPFConjecture}, not used in this chain). The Mathlib infrastructure that formalizes modular
forms for analytic number theory directly constrains the
Standard-Model generation count.

\begin{acknowledgments}
Lean~4 proofs verified by \texttt{lake build} (v4.29.1, Mathlib
commit \texttt{\mathlibcommit{}}). Automated proofs by the
Aristotle theorem prover (Harmonic).
\end{acknowledgments}

\begin{thebibliography}{99}
\bibitem{Wang2024}
J.~Wang, ``Family Puzzle, Framing Topology, $c_-=24$ and
$3(E_8)_1$ Conformal Field Theories: $48/16 = 45/15 = 24/8 = 3$,''
Phys.\ Rev.\ D \textbf{110}, 125028 (2024) [arXiv:2312.14928].

\bibitem{AlvarezGaumeWitten1984}
L.~Alvarez-Gaum\'e and E.~Witten,
Nucl.\ Phys.\ B \textbf{234}, 269 (1984).

\bibitem{GarciaEtxebarria2019}
I.~Garc\'ia-Etxebarria and M.~Montero,
JHEP \textbf{08}, 003 (2019) [arXiv:1808.00009].

\bibitem{Rademacher1973}
H.~Rademacher, \emph{Topics in Analytic Number Theory}
(Springer, 1973).

\bibitem{diFrancesco1997}
P.~di~Francesco, P.~Mathieu, and D.~S\'en\'echal,
\emph{Conformal Field Theory} (Springer, 1997).

\bibitem{GrossJackiw1972}
D.~Gross and R.~Jackiw,
Phys.\ Rev.\ D \textbf{6}, 477 (1972).

\bibitem{Rokhlin1952}
V.~Rokhlin, Dokl.\ Akad.\ Nauk SSSR \textbf{84}, 221 (1952).

\bibitem{Kitaev2009}
A.~Kitaev,
AIP Conf.\ Proc.\ \textbf{1134}, 22 (2009)
[arXiv:0901.2686].

\bibitem{FidkowskiKitaev2010}
L.~Fidkowski and A.~Kitaev,
Phys.\ Rev.\ B \textbf{81}, 134509 (2010)
[arXiv:0904.2197].

\bibitem{Adams1974}
J.~F.~Adams,
\emph{Stable Homotopy and Generalised Homology}
(University of Chicago Press, 1974).

\bibitem{ABP1967}
D.~W.~Anderson, E.~H.~Brown~Jr., and F.~P.~Peterson,
Ann.\ Math.\ \textbf{86}, 271 (1967).

\bibitem{BeaudryCampbell}
A.~Beaudry and J.~A.~Campbell,
Contemp.\ Math.\ \textbf{718}, 89 (2018)
[arXiv:1801.07530].
%% Phase 6o.ε L2 paired-splash absorption (D.3 GATE 3, 2026-05-06)

\bibitem{Niemeier1973}
H.-V.~Niemeier,
``Definite quadratische Formen der Dimension 24 und Diskriminante 1,''
J.\ Number Theory \textbf{5}, 142 (1973).

\bibitem{Schellekens1993}
A.~N.~Schellekens,
``Meromorphic $c=24$ conformal field theories,''
Comm.\ Math.\ Phys.\ \textbf{153}, 159 (1993),
arXiv:hep-th/9205072.

\bibitem{MollerScheithauer2024}
S.~M\"oller and N.~R.~Scheithauer,
``A geometric classification of the holomorphic vertex operator
algebras of central charge $24$,''
Algebra \& Number Theory \textbf{18}, 1891 (2024),
arXiv:2112.12291.

\bibitem{Roehm2026D2}
J.~Roehm, ``Anomaly constraints on Standard Model particle content,''
in preparation (D2 deep companion to L2, 2026).

\end{thebibliography}

\end{document}
