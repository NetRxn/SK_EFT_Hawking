\documentclass[12pt]{article}
\usepackage[utf8]{inputenc}
\usepackage{amsmath,amssymb,amsthm}
\usepackage{hyperref}
\usepackage{graphicx}
\usepackage{booktabs}
\usepackage{listings}
\usepackage{array}
\usepackage[margin=1in]{geometry}

\lstset{basicstyle=\ttfamily\small, breaklines=true, frame=single}

\title{A Twelve-Stage Verification Pipeline for\\Computational Physics Research}

\author{John Roehm}
\date{Draft --- April 2026}

% Counts are pulled from the canonical registry so this paper can never
% drift from the live codebase. Regenerate via:
%   uv run python scripts/update_counts.py
\input{../../docs/counts.tex}

\begin{document}

\maketitle

\begin{abstract}
We describe a twelve-stage pipeline for conducting computational physics research with continuous machine verification. The pipeline integrates Python numerics, Lean~4 formal proofs, and the Aristotle automated theorem prover into a unified workflow where every formula has a formal theorem, every computed quantity has verified bounds, and every paper claim traces to computation. Applied to a research program spanning dissipative effective field theory, analog Hawking radiation, emergent gravity, quantum groups, and topological quantum field theory, the pipeline has produced \totaltheorems{} machine-checked theorems across \leanmodules{} Lean modules with \sorrycount{} remaining gaps (\sorrypercent\%), while maintaining 16 automated validation checks and full parameter provenance. We present the architecture, its ten invariants, the automated theorem prover integration that routinely compresses months of manual proof work to hours, and a case study where the pipeline caught a real convention bug in pentagon equation verification. The methodology is domain-agnostic and immediately applicable to any research program combining numerical computation with formal verification.
\end{abstract}

% ============================================================
\section{Introduction}
\label{sec:intro}

Computational physics research faces a growing reproducibility crisis. Numerical results are published without verified bounds, paper claims diverge from underlying computation over time, and there is no machine-checkable connection between theoretical statements and their computational implementations. The standard workflow---write code, produce figures, write paper, submit---provides no structural guarantee that the published claims match the actual computation.

We address this with a twelve-stage pipeline that makes verification \emph{continuous} rather than post-hoc. Each stage has a gate that must pass before proceeding to the next, and ten invariants must hold at all times. The pipeline integrates three verification layers: Python numerics for fast iteration, Lean~4 formal proofs for mathematical certainty, and the Aristotle automated theorem prover for filling proof gaps.

We have applied this pipeline to a research program spanning \papercount{} papers, producing:
\begin{itemize}
\item \totaltheorems{} machine-checked theorems across \leanmodules{} Lean modules (\sorrypercent\% gap rate)
\item \aristotleproved{} theorems proved by the Aristotle automated prover across \aristotleruns{} submitted runs
\item 16 automated cross-layer validation checks
\item Full parameter provenance for every experimental value
\item Zero heartbeat overrides (no performance workarounds in the proof system)
\end{itemize}

The pipeline is domain-agnostic: it requires only that the research combines numerical computation with formal claims. We discuss applicability to pharmaceutical research (FDA SaMD algorithmic vigilance), financial modeling, and climate science.

% ============================================================
\section{Pipeline Architecture}
\label{sec:pipeline}

The twelve stages execute in strict order, each with a gate:

\begin{table}[h]
\centering
\small
\begin{tabular}{clp{6cm}}
\toprule
\textbf{Stage} & \textbf{Name} & \textbf{Gate} \\
\midrule
1 & Constants \& Parameters & Imports succeed; all parameters have provenance \\
2 & Formulas & Every function has a Lean theorem reference \\
3 & Lean Theorems (stubs) & \texttt{lake build} compiles (sorry warnings OK) \\
4 & Aristotle & All sorry filled; \texttt{lake build} clean \\
5 & Build Verification & Zero sorry; theorem counts match documentation \\
6 & Python Tests & All tests pass; every module has a test file \\
7 & Cross-Layer Validation & All 16 checks pass \\
8 & Visualizations & All PNGs generated from validated data \\
9 & Figure Review & LLM visual review all PASS \\
10 & Paper Draft & Every claim traces to computation within 0.5\% \\
11 & Notebooks & Reproducible narratives execute without errors \\
12 & Document Sync & Full validation passes; counts consistent \\
\bottomrule
\end{tabular}
\caption{The twelve pipeline stages with gates.}
\label{tab:stages}
\end{table}

The canonical source of truth principle is fundamental: physical constants live in exactly one file (\texttt{constants.py}), formulas in exactly one file (\texttt{formulas.py}), and figure functions in exactly one file (\texttt{visualizations.py}). All other code imports from these canonical sources. This prevents the drift that plagues research codebases where the same formula appears in multiple files with subtle variations.

% ============================================================
\section{Pipeline Invariants}
\label{sec:invariants}

Ten invariants must hold at \emph{all times}, not just at stage completion:

\begin{enumerate}
\item \textbf{Canonical formulas.} \texttt{formulas.py} is the only place physics formulas live. All other code imports from it.
\item \textbf{Canonical constants.} \texttt{constants.py} is the only place experimental parameters and the theorem registry live.
\item \textbf{Canonical visualizations.} \texttt{visualizations.py} is the only place figure functions live.
\item \textbf{Every formula has a Lean theorem.} Every Lean theorem has a proof (zero sorry target).
\item \textbf{Every computed quantity has bounds.} Physical bounds are tested and enforced by validation.
\item \textbf{Every paper claim traces to computation.} Numerical claims match formula output within 0.5\%.
\item \textbf{Narrative derives from data.} Feasibility claims need computed support; no hallucinated optimism.
\item \textbf{Every parameter has verified provenance.} Each value traces to a published source.
\item \textbf{Placeholder theorems are non-load-bearing.} Theorems proved as \texttt{True := trivial} are documentation markers only.
\item \textbf{No heartbeat overrides.} Expensive typeclass synthesis is resolved via architectural fixes, not workarounds.
\end{enumerate}

Invariant~9 addresses a subtle inflation problem: \placeholdertheorems{} of our \totaltheorems{} theorems are \texttt{True := trivial} placeholders that serve as documentation markers. These are tracked in a registry (\texttt{PLACEHOLDER\_THEOREMS}) and excluded from substantive theorem counts; the substantive count is \substantivetheorems{}. The automated counting system (\texttt{update\_counts.py}) identifies them via the Lean environment's type metadata.

% ============================================================
\section{Three-Layer Verification}
\label{sec:three_layer}

The three verification layers interact in a discovery--formalization--automation cycle:

\textbf{Layer 1: Python numerics.} Fast iteration for physical intuition. A formula is implemented in \texttt{formulas.py} with a docstring referencing the planned Lean theorem name. Python tests verify correctness, edge cases, and physical bounds.

\textbf{Layer 2: Lean~4 formal proofs.} Mathematical certainty. The formula is stated as a Lean theorem, initially with \texttt{sorry} placeholders. The theorem statement is the strongest form possible---no weakening for proof convenience. Manual proofs are written for theorems within reach.

\textbf{Layer 3: Aristotle automated prover.} Fills sorry gaps. The entire Lean project is submitted with strategy hints (``PROVIDED SOLUTION'' in docstrings). Aristotle uses tactic synthesis, term search, and machine learning to find proofs. Results are manually reviewed before integration---no blind acceptance.

\emph{Example: Tetrad gap equation.} The critical coupling $G_c = 8\pi^2/(N_f \Lambda^2)$ was derived in Python (Layer~1), stated as a Lean theorem with sorry (Layer~2), and proved by Aristotle in run \texttt{79e07d55} (Layer~3). The three-layer cycle completed in one session.

% ============================================================
\section{Automated Theorem Prover Integration}
\label{sec:aristotle}

Aristotle is a commercial automated theorem prover for Lean~4. Our integration follows a priority batching protocol:

\textbf{Priority batching.} Sorry gaps are organized into batches by priority (P1 = highest) and submitted sequentially. Each submission sends the entire Lean project, so parallel submissions waste budget. The current batch plan has 33 sorry across 4 batches.

\textbf{PROVIDED SOLUTION hints.} Each sorry theorem includes a docstring describing the expected proof strategy. This guides Aristotle's search and dramatically improves success rates. For example, the Serre coproduct hint describes the 4-phase tactic strategy that worked for the rank-1 case.

\textbf{Quality verification.} Every Aristotle result is manually reviewed before integration:
\begin{itemize}
\item Does the proof exercise the hypotheses? (Not vacuous)
\item Does \texttt{lake build} pass after integration?
\item Are there any heartbeat overrides? (Invariant~10 violation)
\end{itemize}

\textbf{Results.} \aristotleproved{} theorems proved automatically across \aristotleruns{} runs. The most impactful single run (\texttt{78dcc5f4}) proved 25 theorems spanning S-matrix unitarity, restricted quantum groups, ribbon categories, spin bordism, E8 lattice verification, and verified statistics in a single session. The compression ratio---months of estimated manual proof work compressed to hours of automated proving---is the pipeline's most dramatic efficiency gain.

\textbf{Failure mode.} Aristotle occasionally returns OUT\_OF\_BUDGET, indicating that the proof search exceeded the allocated computation. The resolution is to split the batch, improve hints, or attempt manual proof. One notable failure led to the discovery of a convention bug (Section~\ref{sec:case_study}).

% ============================================================
\section{Validation Infrastructure}
\label{sec:validation}

The \texttt{validate.py} script runs 16 automated checks:

\begin{table}[h]
\centering
\small
\begin{tabular}{clp{5cm}}
\toprule
\textbf{Check} & \textbf{Name} & \textbf{What it verifies} \\
\midrule
1 & formulas & Python formulas reference valid Lean theorems \\
2 & numerical & Experimental parameters match reference values \\
3 & identities & Mathematical identities and boundary conditions \\
4 & paper\_table & Paper table values match solver output \\
5 & theorems & Theorem registry is self-consistent \\
6 & notebooks & No physics re-implementation in notebooks \\
7 & lean\_source & Key theorem names found in Lean source \\
8 & cgl\_fdr & CGL FDR derivation produces correct results \\
9 & lean\_build & Lean project builds cleanly \\
10 & viz\_consistency & Notebook visualizations use imported physics \\
11 & notebook\_exec & All notebooks execute without errors \\
12 & physical\_bounds & All computed quantities within physical bounds \\
13 & cross\_path & Different code paths agree within tolerance \\
14 & paper\_provenance & Paper claims trace to computations \\
15 & param\_provenance & All parameters have verified provenance \\
16 & graph\_integrity & Knowledge graph integrity \\
\bottomrule
\end{tabular}
\caption{The 16 automated validation checks.}
\label{tab:checks}
\end{table}

In addition, two LLM-powered review agents provide semantic validation:
\begin{itemize}
\item \textbf{Claims-reviewer}: Reads each paper's \texttt{.tex} file and cross-references numerical values against computation, Lean theorem status, and parameter provenance.
\item \textbf{Figure-reviewer}: Inspects generated figures for rendering issues, label accuracy, and consistency with the data.
\end{itemize}

% ============================================================
\section{Automated Counts System}
\label{sec:counts}

A recurring source of inefficiency was manual synchronization of theorem counts across 10+ documentation files. We resolved this with an automated counting system:

\begin{enumerate}
\item \texttt{ExtractDeps.lean}: A meta-programming script that extracts the full declaration taxonomy from the Lean environment, producing \texttt{lean\_deps.json}.
\item \texttt{update\_counts.py}: Reads \texttt{lean\_deps.json} plus Python directory scans, producing \texttt{counts.json} (single source of truth) and \texttt{counts.tex} (LaTeX macros).
\item Papers use \verb|\input{counts.tex}| and macros like \verb|\totaltheorems| for auto-updated counts.
\end{enumerate}

The system distinguishes substantive theorems from placeholders by checking the Lean type: theorems with type \texttt{True} are classified as placeholders. This eliminated a ${\sim}4\%$ count inflation from documentation-marker theorems.

% ============================================================
\section{Case Study: The Pentagon Convention Bug}
\label{sec:case_study}

The pipeline's most dramatic contribution was catching a convention bug in the Ising MTC pentagon equation.

\textbf{Phase 1: False proof.} The Aristotle automated prover solved the pentagon equation using case analysis (\texttt{fin\_cases} + \texttt{ring}), producing a proof that compiled and type-checked. The proof appeared valid.

\textbf{Phase 2: Discovery.} When we rewrote the F-symbols using a different convention (switching from a phase-ambiguous source to Kitaev's canonical Appendix~E convention), the pentagon equation failed. Investigation revealed that the original Aristotle proof was \emph{vacuously true}: the F-symbol function returned incorrect values for ${\sim}55$ of 243 input tuples, making the pentagon equation hold trivially (both sides evaluated to zero for inadmissible inputs).

\textbf{Phase 3: Resolution.} Deep research identified the correct convention (Kitaev 2006, Appendix~E), including the two crucial $-1$ entries ($F^{\psi\sigma\psi}_\sigma[\sigma,\sigma] = -1$ and $F^{\sigma\psi\sigma}_\psi[\sigma,\sigma] = -1$). The corrected F-symbols were verified by \texttt{native\_decide} over $\mathbb{Q}(\sqrt{2})$ in ${\sim}2$ seconds. All 243 pentagon instances now check against the actual algebraic identities, not vacuously.

\textbf{Lesson.} A formally verified proof can still be wrong if the \emph{definitions} it operates on are wrong. The pipeline's multi-layer approach---where the same mathematical content is expressed in both Python (numerical) and Lean (formal)---creates cross-checks that catch such errors. The convention bug would not have been detected by formal verification alone; it required the interaction between computational verification (\texttt{native\_decide}) and the formalization infrastructure.

% ============================================================
\section{Scaling and Applicability}
\label{sec:scaling}

\textbf{Current scale.} The pipeline manages \totaltheorems{} theorems, \leanmodules{} Lean modules, \papercount{} papers, \notebookcount{} notebooks, \figurecount{} figures, and \pythonmodules{} Python modules. Growth rate is approximately 200 theorems per intensive session. The automated counts system and validation infrastructure scale linearly.

\textbf{Bottlenecks.} Three factors limit throughput: (1) Aristotle budget constraints for proof search, (2) Lean 4 typeclass synthesis for complex categorical hierarchies (resolved by architectural caching, not heartbeat overrides), and (3) number field consolidation for algebraic verification at higher degree extensions.

\textbf{Applicability.} The pipeline requires only two ingredients: (1) numerical computation producing quantitative claims, and (2) formal statements of the mathematical content. Domains where this applies include:
\begin{itemize}
\item \textbf{Pharmaceutical research}: FDA Software as a Medical Device (SaMD) regulation requires algorithmic transparency. The pipeline's provenance tracking and cross-layer validation directly address algorithmic vigilance requirements.
\item \textbf{Financial modeling}: Verified risk calculations where model drift from documented methodology is a regulatory concern.
\item \textbf{Climate science}: Verified model predictions where the connection between code and published projections must be auditable.
\end{itemize}

% ============================================================
\section{Related Work}
\label{sec:related}

\textbf{Physics formalizations.} PhysLean (formerly HepLean)~\cite{physlean} formalizes the quantum harmonic oscillator and Higgs potential in Lean~4 but does not include a verification pipeline or cross-layer validation. The Lean-QuantumInfo project (Perimeter Institute) formalizes quantum information theory.

\textbf{Verified computation.} CompCert~\cite{compcert} verifies a C compiler; seL4~\cite{sel4} verifies an operating system kernel. These are single-artifact verification projects, whereas our pipeline verifies an ongoing research program with continuously evolving content.

\textbf{Reproducibility tools.} Jupyter notebooks, papermill, and DVC provide computational reproducibility without formal verification. Our pipeline adds the formal layer: not just ``the code runs and produces the same output'' but ``the output satisfies machine-checked mathematical properties.''

\textbf{Automated theorem proving.} Lean Copilot~\cite{leancipilot} and AlphaProof~\cite{alphaproof} represent the frontier of AI-assisted theorem proving. Aristotle occupies a complementary niche: rather than proving competition problems, it fills sorry gaps in applied formalization projects where strategy hints significantly constrain the search space.

% ============================================================
\section{Conclusion}
\label{sec:conclusion}

The twelve-stage pipeline transforms computational physics methodology from ``write code, produce figures, write paper'' to a continuous verification process where every stage has a machine-checkable gate. The ten invariants prevent the drift between computation and publication that plagues traditional research workflows. The three-layer verification architecture (Python + Lean + Aristotle) provides both fast iteration and mathematical certainty, with the automated prover compressing proof work by orders of magnitude.

The pipeline caught a real convention bug in pentagon equation verification that would have propagated undetected through traditional methods. This single instance justifies the verification overhead: the cost of finding and fixing the bug \emph{after publication} would have been far greater than the cost of continuous verification.

The methodology is open-source, domain-agnostic, and immediately applicable. We encourage adoption in any research domain where the connection between computation and published claims must be trustworthy.

\begin{thebibliography}{99}
\bibitem{physlean} J.~Tooby-Smith, ``PhysLean: Lean~4 for physics,'' \url{https://github.com/HEPLean/PhysLean} (2024).
\bibitem{compcert} X.~Leroy, ``Formal verification of a realistic compiler,'' \emph{Comm.\ ACM}\ \textbf{52}(7), 107--115 (2009).
\bibitem{sel4} G.~Klein et al., ``seL4: Formal verification of an OS kernel,'' \emph{Proc.\ ACM SOSP} (2009).
\bibitem{leancipilot} P.~Song et al., ``Towards large language models as copilots for theorem proving in Lean,'' arXiv:2404.12534 (2024).
\bibitem{alphaproof} Google DeepMind, ``AI achieves silver-medal standard solving International Mathematical Olympiad problems,'' (2024).
\end{thebibliography}

\end{document}
