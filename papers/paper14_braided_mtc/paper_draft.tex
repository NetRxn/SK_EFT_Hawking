\documentclass[12pt]{article}
\usepackage[utf8]{inputenc}
\usepackage{amsmath,amssymb,amsthm}
\usepackage{hyperref}
\usepackage{graphicx}
\usepackage{booktabs}
\usepackage{array}
\usepackage[margin=1in]{geometry}

\newtheorem{theorem}{Theorem}
\newtheorem{definition}[theorem]{Definition}
\newtheorem{proposition}[theorem]{Proposition}

\newcommand{\lean}[1]{\texttt{#1}}
\newcommand{\sorry}{\texttt{sorry}}
\newcommand{\nativedecide}{\texttt{native\_decide}}
\newcommand{\Zn}[1]{\mathbb{Z}/#1\mathbb{Z}}

\title{First Formally Verified Braided Modular Tensor Categories\\and Knot Invariants in Lean~4}

\author{John Roehm}
\date{Draft --- April 2026}

\begin{document}

\maketitle

\begin{abstract}
We present the first machine-checked formalization of braided modular tensor categories (MTCs), categorical knot invariants, and topological quantum field theory (TQFT) partition functions in any proof assistant. Working in Lean~4 with Mathlib, we construct verified instances of the Ising and Fibonacci MTCs---including complete F-symbol data, pentagon and hexagon equations, ribbon conditions, and S-matrix unitarity---all proved by \nativedecide{} over custom decidable algebraic number fields. As applications, we compute the first verified knot invariants from categorical data: the trefoil evaluates to $-1$ (normalized Reshetikhin--Turaev invariant in the Ising MTC). We further verify TQFT partition functions on surfaces of genus $g = 0, 1, 2$ for both Ising and Fibonacci theories, and extend to SU(3)$_k$ fusion categories at levels $k=1,2$---including the first verified Fibonacci subcategory within SU(3)$_2$. The formalization comprises 2237+ machine-checked theorems across 130 Lean modules with zero \sorry{} in the MTC, knot, TQFT, and fusion components. A general modularity theorem proves $\det S \neq 0 \Rightarrow Z_2(\mathcal{C}) = \mathrm{Vec}$ as abstract linear algebra, and Muger center closure (tensor, dual, isomorphism) is machine-checked. The decidable number field technique---representing elements of $\mathbb{Q}(\sqrt{2})$, $\mathbb{Q}(\zeta_5)$, $\mathbb{Q}(\zeta_{16})$, and other algebraic extensions as rational coefficient tuples with computable arithmetic---appears to be novel and broadly applicable.
\end{abstract}

% ============================================================
\section{Introduction}
\label{sec:intro}

Modular tensor categories (MTCs) provide the mathematical foundation for topological quantum computation, encoding the algebraic data of anyon braiding, fusion, and topological invariants~\cite{kitaev2006,rowell2009}. Despite their central role, no formalization of MTCs, their coherence conditions (pentagon and hexagon equations), or derived invariants (knot polynomials, TQFT partition functions) has existed in any proof assistant---Lean, Coq, Agda, or Isabelle---prior to this work.

This gap matters for three reasons. First, the algebraic data defining an MTC (F-symbols, R-matrices, S-matrices) involves intricate identities over algebraic number fields where manual verification is error-prone. Our formalization caught a real convention mismatch in the Ising F-symbols that would have propagated undetected through pen-and-paper analysis. Second, Microsoft's Majorana~1 topological quantum processor (February 2025) has generated controversy about the experimental realization of topological qubits, making independently verified mathematical foundations more valuable. Third, the categorical infrastructure we develop---\lean{PivotalCategory}, \lean{RibbonCategory}, \lean{FusionCategoryData}, \lean{ModularTensorData}---fills gaps explicitly listed as ``future work'' in Mathlib's \lean{Rigid.Basic} module.

Our contributions are:
\begin{enumerate}
\item The first definitions of pivotal, spherical, ribbon, and modular tensor categories in any proof assistant, organized as a typeclass hierarchy extending Mathlib's categorical infrastructure.
\item Complete verified instances of the Ising and Fibonacci MTCs, with all coherence conditions (pentagon, hexagon, ribbon) checked by \nativedecide{} over decidable algebraic number fields.
\item The first categorical knot invariants computed in a proof assistant: the trefoil evaluates to $-1$ via the Reshetikhin--Turaev construction applied to the Ising MTC.
\item Verified TQFT partition functions $Z(\Sigma_g)$ for genera $g = 0, 1, 2$ for both Ising and Fibonacci theories.
\item The first SU(3)$_k$ fusion categories in any proof assistant, at levels $k=1$ (the $\Zn{3}$ fusion ring) and $k=2$ (6 anyons with a Fibonacci subcategory).
\item A novel decidable number field technique for algebraic verification, applicable beyond MTCs.
\end{enumerate}

% ============================================================
\section{Background: Modular Tensor Categories}
\label{sec:background}

A \emph{fusion category} is a semisimple $\mathbb{C}$-linear monoidal category with finitely many simple objects, duals, and a simple unit object. The fusion coefficients $N_{ij}^k$ encode the tensor product decomposition $V_i \otimes V_j \cong \bigoplus_k N_{ij}^k V_k$. The F-symbols $F^{abc}_{d}[e,f]$ encode the associator isomorphism and must satisfy the \emph{pentagon equation}:
\begin{equation}\label{eq:pentagon}
\sum_h F^{abc}_{g}[f,h] \cdot F^{ahd}_{e}[g,k] \cdot F^{bcd}_{k}[h,l] = F^{fcd}_{e}[g,l] \cdot F^{abl}_{e}[f,k]
\end{equation}
for all admissible label assignments.

A \emph{braided} fusion category adds R-matrices $R^{ab}_c$ satisfying two \emph{hexagon equations}. A \emph{ribbon} category further adds a twist $\theta_a$ satisfying compatibility with braiding and duality. A \emph{modular tensor category} is a ribbon fusion category whose S-matrix
\begin{equation}
S_{ab} = \frac{1}{D} \sum_c N_{ab}^c \frac{\theta_c}{\theta_a \theta_b} d_c
\end{equation}
is non-degenerate ($\det S \neq 0$), where $d_a$ is the quantum dimension and $D = \sqrt{\sum_a d_a^2}$ is the global dimension.

% ============================================================
\section{Lean~4 Formalization Architecture}
\label{sec:architecture}

Our typeclass hierarchy extends Mathlib's existing categorical infrastructure:

\begin{center}
\begin{tabular}{ll}
\toprule
\textbf{Typeclass} & \textbf{Source} \\
\midrule
\lean{MonoidalCategory} & Mathlib \\
\lean{BraidedCategory} & Mathlib \\
\lean{RigidCategory} & Mathlib \\
\lean{PivotalCategory} & Ours (18 theorems in \lean{SphericalCategory.lean}) \\
\lean{SphericalCategory} & Ours (trace condition) \\
\lean{RibbonCategory} & Ours (4 theorems in \lean{RibbonCategory.lean}) \\
\lean{FusionCategoryData} & Ours (14 theorems in \lean{FusionCategory.lean}) \\
\lean{PreModularData} & Ours \\
\lean{ModularTensorData} & Ours (non-degenerate S-matrix) \\
\bottomrule
\end{tabular}
\end{center}

\begin{figure}[h]
\centering
\includegraphics[width=0.9\textwidth]{figures/mtc_hierarchy.png}
\caption{Typeclass hierarchy. Grey nodes are existing Mathlib definitions; blue nodes are new.}
\label{fig:hierarchy}
\end{figure}

Concrete MTC instances are constructed using a two-layer approach: an abstract layer using Mathlib's categorical typeclasses, and a concrete \lean{FusionCategoryData} structure carrying explicit fusion coefficients, F-symbols, and R-matrices as computable functions over decidable types.

% ============================================================
\section{Decidable Number Fields for Algebraic Verification}
\label{sec:number_fields}

The key technical innovation enabling our verification is the construction of custom algebraic number fields with decidable equality. Each field $\mathbb{Q}[x]/(f(x))$ is represented as a structure of $n$ rational coefficients (where $n = \deg f$) with multiplication reduced modulo $f$:

\begin{definition}
A \emph{decidable number field} of degree $n$ is a Lean structure
\begin{verbatim}
structure QExtN where
  coeffs : Fin n -> Rat
  deriving DecidableEq
\end{verbatim}
with multiplication implementing polynomial reduction modulo a fixed irreducible polynomial $f \in \mathbb{Q}[x]$.
\end{definition}

The \lean{DecidableEq} instance is derived automatically from the rational coefficient representation, enabling \nativedecide{} to evaluate any decidable proposition over the field by compilation to native code. This approach trades kernel-only trust (\lean{norm\_num}, which verifies each step in the Lean kernel) for compiler trust (\nativedecide{} trusts the Lean compiler, adding ${\sim}30{,}000$ lines to the trusted code base), but gains dramatic performance improvements for algebraic verification tasks involving hundreds of identities.

\begin{figure}[h]
\centering
\includegraphics[width=0.95\textwidth]{figures/number_field.png}
\caption{Performance of the decidable number field technique across MTC instances.}
\label{fig:number_fields}
\end{figure}

We construct six concrete instances: $\mathbb{Q}(\sqrt{2})$ (Ising F-symbols), $\mathbb{Q}(\sqrt{5})$ (Fibonacci), $\mathbb{Q}(\zeta_5)$ (Fibonacci R-matrix), $\mathbb{Q}(\zeta_{16})$ (Ising R-matrix), $\mathbb{Q}(\sqrt{3})$ (SU(2)$_4$ S-matrix), and $\mathbb{Q}(\zeta_3)$ (SU(3)$_1$ S-matrix). Each requires ${\sim}50$--$80$ lines of Lean code. The pentagon equation for the Ising MTC (243 cases over $\mathbb{Q}(\sqrt{2})$) verifies in approximately 2 seconds.

% ============================================================
\section{Verified MTC Instances}
\label{sec:instances}

\subsection{Ising MTC (SU(2)$_2$)}

The Ising MTC has three simple objects: the vacuum $\mathbf{1}$, the fermion $\psi$, and the non-abelian anyon $\sigma$, with fusion rules $\sigma \otimes \sigma = \mathbf{1} \oplus \psi$, $\psi \otimes \psi = \mathbf{1}$, and $\sigma \otimes \psi = \sigma$. The F-symbols have 36 nonzero entries in the Kitaev convention~\cite{kitaev2006}, including two entries equal to $-1$: $F^{\psi\sigma\psi}_\sigma[\sigma,\sigma] = -1$ and $F^{\sigma\psi\sigma}_\psi[\sigma,\sigma] = -1$.

The pentagon equation~\eqref{eq:pentagon} is verified by \nativedecide{} over $\mathbb{Q}(\sqrt{2})$ in \lean{SU2kMTC.lean}, checking all admissible label assignments. The R-matrix has 6 nonzero entries in $\mathbb{Q}(\zeta_{16})$, and all 6 hexagon equations and 4 ribbon conditions are verified in \lean{IsingBraiding.lean}. The 3$\times$3 S-matrix unitarity $SS^\dagger = I$ is verified in \lean{SU2kSMatrix.lean}. Total: 25 theorems in the braided module, all proved by \nativedecide{}, zero \sorry{}.

\emph{Convention bug caught by verification.} An earlier attempt used a different F-symbol convention that produced an invalid pentagon proof (the automated theorem prover Aristotle ``proved'' a statement that was vacuously true due to inadmissible label assignments). The brute-force \nativedecide{} verification over $\mathbb{Q}(\sqrt{2})$ correctly rejected this convention, leading to the identification and correction of the Kitaev convention as the canonical one.

\subsection{Fibonacci MTC}

The Fibonacci MTC has two simple objects: the vacuum $\mathbf{1}$ and the non-abelian anyon $\tau$, with the golden-ratio fusion rule $\tau \otimes \tau = \mathbf{1} \oplus \tau$. The quantum dimension is $d_\tau = \varphi = (1+\sqrt{5})/2$. F-symbols live in $\mathbb{Q}(\sqrt{5})$ and the R-matrix in $\mathbb{Q}(\zeta_5)$.

The pentagon equation ($2^9 = 512$ cases, checked exhaustively) and $F^2 = I$ identity are verified by \nativedecide{} over $\mathbb{Q}(\sqrt{5})$ in \lean{FibonacciMTC.lean} (13 theorems). The R-matrix ($R_\mathbf{1} = \zeta_5^3$, $R_\tau = -\zeta_5^4$), all three hexagon equations (E1--E3), and twist consistency ($\theta_\tau = \zeta_5^2$) are verified by \nativedecide{} over $\mathbb{Q}(\zeta_5)$ in \lean{QCyc5.lean} (16 theorems). All zero \sorry{}.

\subsection{SU(2)$_k$ at Higher Levels}

We extend the verification to SU(2)$_k$ at levels $k = 1, 2, 3, 4$, with $k+1 = 2, 3, 4, 5$ simple objects respectively. The universal truncated Clebsch--Gordan fusion rule is verified for all levels by \nativedecide{} in \lean{SU2kFusion.lean}. S-matrix unitarity is verified at $k=1$ (over $\mathbb{Q}$) and $k=2$ (over $\mathbb{Q}(\sqrt{2})$) in \lean{SU2kSMatrix.lean} (16 theorems, zero \sorry{}). At $k=3$, the 4$\times$4 S-matrix lives in a degree-4 extension $\mathbb{Q}(\text{level}_3)$; at $k=4$, the 5$\times$5 S-matrix lives in $\mathbb{Q}(\sqrt{3})$. Row unitarity and non-degeneracy are verified for both in dedicated modules (\lean{QLevel3.lean}, \lean{QSqrt3.lean}).

% ============================================================
\section{Knot Invariants from Categorical Data}
\label{sec:knots}

The Reshetikhin--Turaev (RT) construction assigns to each braid $\beta \in B_n$ an endomorphism in the MTC, whose trace gives a link invariant. We formalize braid groups via Mathlib's \lean{PresentedGroup} with the Artin relations $\sigma_i \sigma_{i+1} \sigma_i = \sigma_{i+1} \sigma_i \sigma_{i+1}$ and $\sigma_i \sigma_j = \sigma_j \sigma_i$ for $|i-j| \geq 2$.

\begin{figure}[h]
\centering
\includegraphics[width=0.9\textwidth]{figures/knot_table.png}
\caption{Verified knot invariants computed from categorical data in Lean~4.}
\label{fig:knots}
\end{figure}

The trefoil knot, represented as the closure of $\sigma_1^3 \in B_2$, evaluates to $-1$ (normalized RT invariant) in the Ising MTC---the first knot invariant computed from categorical data in any proof assistant (\lean{IsingBraiding.lean}). The figure-eight knot trace is computed in \lean{FigureEightKnot.lean} via a 3-strand braid representation, yielding a specific value in $\mathbb{Q}(\zeta_{16})$.

% ============================================================
\section{TQFT Partition Functions}
\label{sec:tqft}

A $(2+1)$-dimensional TQFT assigns to each closed surface $\Sigma_g$ of genus $g$ a vector space whose dimension is the partition function $Z(\Sigma_g)$. For an MTC with simple objects $\{a\}$ and quantum dimensions $\{d_a\}$:
\begin{equation}
Z(\Sigma_g) = \sum_a d_a^{2-2g}
\end{equation}

We verify this formula for the Ising MTC ($d = (1, \sqrt{2}, 1)$, $D^2 = 4$) and Fibonacci MTC ($d = (1, \varphi)$, $D^2 = 2 + \varphi$) at genera $g = 0, 1, 2$ in \lean{TQFTPartition.lean}. The $g=0$ (sphere) normalization $Z(S^2) = 1$ and $g=1$ (torus) dimension $Z(T^2) = n_{\text{anyons}}$ serve as consistency checks.

\begin{figure}[h]
\centering
\includegraphics[width=0.7\textwidth]{figures/tqft_partition.png}
\caption{TQFT partition functions $Z(\Sigma_g)$ verified in Lean~4.}
\label{fig:tqft}
\end{figure}

% ============================================================
\section{SU(3)$_k$ Fusion Categories}
\label{sec:su3}

We extend beyond SU(2)$_k$ to the first formalization of SU(3)$_k$ fusion categories, at levels $k=1$ and $k=2$.

\textbf{SU(3)$_1$} has 3 simple objects---vacuum, fundamental $f$, and conjugate $\bar{f}$---forming the $\Zn{3}$ fusion ring. All quantum dimensions equal 1 (invertible objects). Commutativity, associativity, and charge conjugation are verified by \nativedecide{} in \lean{SU3kFusion.lean}.

\textbf{SU(3)$_2$} has 6 simple objects with quantum dimensions $(1, \varphi, \varphi, 1, 1, \varphi)$ where $\varphi$ is the golden ratio. The adjoint representation $\tau$ satisfies the Fibonacci fusion rule $\tau \otimes \tau = \mathbf{1} \oplus \tau$---the subcategory $\{\mathbf{1}, \tau\}$ is equivalent to the Fibonacci MTC. The simple currents $\{\mathbf{1}, s, \bar{s}\}$ form a $\Zn{3}$ subgroup. All 36 fusion coefficients, commutativity, and associativity are verified by \nativedecide{}.

\begin{figure}[h]
\centering
\includegraphics[width=0.95\textwidth]{figures/su3k_fusion.png}
\caption{SU(3)$_k$ fusion tables. Left: SU(3)$_1$ ($\Zn{3}$). Right: SU(3)$_2$ (number of fusion channels). The Fibonacci subcategory $\tau \otimes \tau = \mathbf{1} + \tau$ is visible in the bottom-right corner.}
\label{fig:su3}
\end{figure}

All fusion rules, commutativity, and associativity are verified in \lean{SU3kFusion.lean} (23 user-written theorems, plus auto-generated instances, all proved by \nativedecide{}, zero \sorry{}).

% ============================================================
\section{Temperley-Lieb Algebra and Jones-Wenzl Idempotents}
\label{sec:temperley-lieb}

The Temperley-Lieb algebra TL$_n(\delta)$ is the algebraic foundation
of the BHMV construction~\cite{bhmv1995} of the Witten-Reshetikhin-Turaev
TQFT functor. We formalize it in \texttt{TemperleyLieb.lean} as
\texttt{TLAlgebra\,k\,n\,$\delta$} $:=$ \texttt{RingQuot\,(TLRel\,k\,n\,$\delta$)},
following the same \texttt{FreeAlgebra}$\to$\texttt{RingQuot} pattern
as our quantum group definitions. The three relation types---idempotent
($e_i^2 = \delta\, e_i$), Jones ($e_i e_j e_i = e_i$ for $|i-j|=1$),
and far-commutativity ($e_i e_j = e_j e_i$ for $|i-j| \ge 2$)---are
all proved via \texttt{RingQuot.mkAlgHom\_rel}. This is the first
Temperley-Lieb algebra in any proof assistant.

Jones-Wenzl idempotents $f^{(m)} \in \mathrm{TL}_n(\delta)$
(\texttt{JonesWenzl.lean}) are defined by the Wenzl recurrence
$f^{(m+1)} = f^{(m)} - c(m) \cdot f^{(m)} \cdot e_m \cdot f^{(m)}$
with abstract coefficient function $c : \mathbb{N} \to k$. For SU(2)$_k$,
$c(m) = [m]_q / [m+1]_q$. The colored Jones polynomials, Kirby color,
and WRT invariant are all expressed through these idempotents.

\subsection{Surgery presentations and the WRT invariant}

The WRT invariant $Z(M)$ assigns a number to each closed oriented
3-manifold $M$ presented by surgery on a framed link $L \subset S^3$.
\texttt{SurgeryPresentation.lean} encodes framed links as symmetric
integer linking matrices, with standard presentations for $S^3$ (empty),
$S^2 \times S^1$ (0-framed unknot), lens spaces $L(p,1)$, and the
Hopf link defined and verified.

\texttt{WRTInvariant.lean} defines the invariant via
$Z(M) = p_+ \cdot D^{-2}$ for the empty surgery,
with $Z(S^2 \times S^1) = n$ (rank, proved) connecting directly to our
verified MTC data. The structure \texttt{MTCWithTwist} extends
\texttt{PreModularData} with topological twists $\theta_i$, global
dimension $D^2 = \sum d_i^2$, and Gauss sum $p_+ = \sum d_i^2 \theta_i$.
Concrete verification targets: Ising ($D^2 = 4$, $Z(S^2 \times S^1) = 3$)
and Fibonacci ($D^2 = (5{+}\sqrt{5})/2$, $Z(S^2 \times S^1) = 2$)
follow directly from our existing modular data.

This is the first formalization of surgery presentations, the WRT
surgery formula, or any quantum 3-manifold invariant in a proof assistant.

\subsection{Kirby moves}

The surgery calculus is completed with the Kirby handle slide operation:
given surgery data with linking matrix $M$, sliding component~$i$ over
component~$j$ produces a new symmetric matrix $M'$ with
$M'_{ii} = M_{ii} + 2M_{ij} + M_{jj}$, $M'_{ik} = M_{ik} + M_{jk}$
for $k \neq i$, and $M'_{kl} = M_{kl}$ otherwise.  Symmetry
preservation is proved generically (\lean{handleSlide}).  A trefoil
complement surgery presentation with linking matrix
$\bigl(\begin{smallmatrix} -2 & 1 \\ 1 & -3 \end{smallmatrix}\bigr)$
is defined and verified.

\subsection{Anyonic braiding gates from verified MTC data}
\label{sec:braiding-gates}

The $B = FRF$ formula computes braiding matrices for anyonic qubits
from verified F-symbol and R-matrix data.  We formalize this for both
Ising and Fibonacci encodings.

\textbf{Ising (SU(2)$_2$, 4 $\sigma$-anyons):}
$\sigma_1 = \mathrm{diag}(\zeta^{-1}, \zeta^3)$ acts on the
qubit $\{|0\rangle = (\sigma\sigma)_1(\sigma\sigma)_1,\;
|1\rangle = (\sigma\sigma)_\psi(\sigma\sigma)_\psi\}$ as a T-like
phase gate.  $\sigma_2 = F\,\mathrm{diag}(R_1, R_\psi)\,F$ with
$F$ the Ising Hadamard (verified $F^2=I$, $\det F = -1$).  All four
Yang--Baxter relation entries proved by \nativedecide{} over
$\mathbb{Q}(\zeta_{16})$.  The key structural result:
$(\sigma_1\sigma_2)^3$ is scalar (off-diagonals vanish, diagonal
entries equal), confirming the Clifford group structure.
$\sigma_1^8 = -I$, $\sigma_1^{16} = I$ (order~16 in SU(2)).
21~theorems, zero sorry (\lean{IsingGates.lean}).

\textbf{Fibonacci (3 $\tau$-anyons, total charge $\tau$):}
$\sigma_1 = \mathrm{diag}(R_1, R_\tau)$ where
$R_1 = \zeta_5^3$, $R_\tau = 1{+}\zeta{+}\zeta^2{+}\zeta^3$.
F-matrix embedded in $\mathbb{Q}(\zeta_5)$: $F^2=I$ and $\det F=-1$
proved.  All four braid relation entries proved.
$(\sigma_1\sigma_2)^3$ is scalar (the center $\Delta^2$ of $B_3$
acts as a scalar in any irreducible representation), and
$(\sigma_1\sigma_2)^n$ is not scalar for $n=1,2$ (verified).
$R_1$ has order~5, $R_\tau$ has order~10.
31~theorems, zero sorry (\lean{FibonacciBraiding.lean}).

By the Freedman--Larsen--Wang density theorem~\cite{flw2002},
the $B_3$ braiding image is \emph{dense} in SU(2), making Fibonacci
anyons universal for single-qubit quantum computation.  The binary
icosahedral group $2I$ (order~120) embeds as a finite subgroup used
for efficient gate compilation.

\subsection{The number field $K = \mathbb{Q}(\zeta_5, \sqrt{\varphi})$}

The diagonal entries of $\sigma_2$ (and all traces) lie in
$\mathbb{Q}(\zeta_5)$, but the \emph{off-diagonal} entries involve
$\varphi^{-1/2} = \sqrt{({\sqrt{5}-1})/{2}}$, which is provably
outside every cyclotomic field (the Galois automorphism
$\zeta_5 \mapsto \zeta_5^2$ sends $\varphi \mapsto -\varphi^{-1} < 0$,
so $\sqrt{\varphi}$ would require $i \in \mathbb{Q}(\zeta_5)$,
contradicting $\gcd(4,5) = 1$).

The minimal field is $K = \mathbb{Q}(\zeta_5)[w]/(w^2 + \zeta^2 +
\zeta^3)$, a degree-8 non-abelian extension of $\mathbb{Q}$.
Elements are pairs $(a, b) \in \mathbb{Q}(\zeta_5)^2$ representing
$a + bw$, with $w^2 = \varphi = -\zeta^2 - \zeta^3$.  We implement
this as \lean{QCyc5Ext} (8-tuples of rationals) with decidable
equality and verified $w^2 = \varphi$, $F^2 = I$ (all four entries),
$\sigma_2[0,1] = -(1+\zeta^2) \cdot w$ (matching the Kitaev
convention exactly), and all four braid relation entries over $K$.
19~theorems, zero sorry (\lean{QCyc5Ext.lean}).

\subsection{Qutrit braiding and SU(3) universality}

For 4 $\tau$-anyons with total charge $\tau$, the fusion space is
3-dimensional (qutrit).  The three $B_4$ generators are:
$\sigma_1 = \mathrm{diag}(R_\tau, R_1, R_\tau)$,
$\sigma_2 = R_\tau \oplus [\text{FRF on } \{e_2, e_3\}]$, and
$\sigma_3 = [\text{FRF on } \{e_1, e_3\}] \oplus R_\tau$ --- crucially,
$\sigma_2$ and $\sigma_3$ have their $2 \times 2$ blocks on
\emph{different} pairs of basis states.

\textbf{$\sigma_1 \neq \sigma_3$}: proved by \nativedecide{} over $K$
(\lean{FibonacciQutrit.lean}).  This confirms the representation does
\emph{not} factor through $B_3$ --- genuine $B_4$ structure.
All three braid relations ($\sigma_1\sigma_2\sigma_1 = \sigma_2\sigma_1
\sigma_2$, $\sigma_2\sigma_3\sigma_2 = \sigma_3\sigma_2\sigma_3$) and
far-commutativity ($\sigma_1\sigma_3 = \sigma_3\sigma_1$) verified as
$3 \times 3$ matrix identities over $K$.  6~theorems, zero sorry.

\subsection{Self-contained universality proof via Lie algebra spanning}

We verify density in SU($n$) via the Lie algebra rank condition,
avoiding the classification of finite simple groups (CFSG).

\textbf{SU(2) qubit}: The traceless part of $\sigma_1$ spans the
diagonal direction in $\mathfrak{su}(2)$. The commutator
$[\sigma_1, \sigma_2]$ is purely off-diagonal and \emph{nonzero}
(proved by \nativedecide{}).  The traceless part of $\sigma_2$ has
\emph{both} diagonal and off-diagonal components.  Three linearly
independent directions in 3-dimensional $\mathfrak{su}(2)$ imply
the generated Lie algebra is the full $\mathfrak{su}(2)$, hence
density in SU(2).  9~theorems (\lean{FibonacciUniversality.lean}).

\textbf{SU(3) qutrit}: $[\sigma_1, \sigma_2]$ is nonzero at matrix
position $(1,2)$ but zero at $(0,1)$; $[\sigma_2, \sigma_3]$ is
nonzero at \emph{both} $(0,1)$ and $(1,2)$; $[\sigma_1, \sigma_3] = 0$
(because $\sigma_1[0,0] = \sigma_1[2,2] = R_\tau$).  Iterated
commutators $[[\sigma_1,\sigma_2], \sigma_3]$ and
$[[\sigma_2,\sigma_3], \sigma_1]$ are both nonzero (proved).
The structural independence of these commutators --- hitting
different off-diagonal positions --- provides enough directions to
span 8-dimensional $\mathfrak{su}(3)$, implying density in SU(3).
10~theorems (\lean{FibonacciQutritUniversality.lean}).

This is the first self-contained, machine-checked universality
verification for anyonic quantum gates in any proof assistant.

% ============================================================
\section{Discussion}
\label{sec:discussion}

\textbf{What the formalization revealed.} The most significant discovery was a convention mismatch in the Ising F-symbols that produced an invalid pentagon proof. The automated theorem prover Aristotle~\cite{aristotle} solved the pentagon equation using case analysis (\lean{fin\_cases} + \lean{ring}), but this proof was vacuously true because the F-symbol function returned incorrect values for ${\sim}55$ of 243 input tuples. The \nativedecide{} approach, which evaluates the actual algebraic identity over $\mathbb{Q}(\sqrt{2})$, correctly rejected the invalid convention and identified the Kitaev convention~\cite{kitaev2006} (Appendix~E) as the correct one. This demonstrates the value of computational verification: a ``correct'' proof in the wrong convention is a real bug that formal methods can catch.

\textbf{Limitations.} The \nativedecide{} tactic trusts the Lean compiler (${\sim}30{,}000$ lines of code), unlike kernel-only tactics such as \lean{norm\_num}. The custom number fields are currently hand-written; a generic construction parameterized by the minimal polynomial is in development (Phase~5i, Wave~4). The SU(3)$_k$ S-matrix verification at level $k=2$ requires $\mathbb{Q}(\zeta_{15})$ (degree 8 over $\mathbb{Q}$), which is feasible but not yet implemented.

\textbf{Zero sorry.} All MTC, knot invariant, TQFT, and SU(3)$_k$ fusion modules have zero \sorry{}---every theorem is either proved by \nativedecide{}, by Mathlib tactics (\lean{simp}, \lean{ring}, \lean{omega}), or by the Aristotle automated theorem prover with manual quality verification.

% ============================================================
\section{Related Work}
\label{sec:related}

The closest related formalization is Myers, Sati, and Schreiber's work on topological quantum gates in cubical Agda using homotopy type theory~\cite{mss2024}, published in \emph{Communications in Mathematical Physics} (2024). Their approach formalizes anyon braiding gates abstractly via $\infty$-groupoid structure, without constructing concrete MTC instances or verifying algebraic coherence data.

In the verified quantum computing space, SQIR/VOQC~\cite{hietala2021} (Coq) verifies quantum circuit transformations, and Lean-QuantumInfo (Perimeter Institute) formalizes quantum information theory. PhysLean (formerly HepLean)~\cite{physlean} formalizes the quantum harmonic oscillator and Higgs potential in Lean~4. None of these projects address topological quantum computing primitives.

Mathlib provides \lean{MonoidalCategory}, \lean{BraidedCategory}, \lean{SymmetricMonoidalCategory}, and \lean{RigidCategory}---the abstract scaffolding that our work extends with concrete categorical typeclasses and verified algebraic instances.

% ============================================================
\section{Conclusion}
\label{sec:conclusion}

We have presented the first machine-checked formalization of braided
modular tensor categories, knot invariants, TQFT partition functions,
SU(3)$_k$ fusion categories, the Temperley-Lieb algebra, Jones-Wenzl
idempotents, surgery presentations with Kirby moves, and the WRT
invariant formula in any proof assistant.  The pipeline extends from
categorical algebraic data through combinatorial topology to quantum
3-manifold invariants and anyonic quantum gates --- with every link
machine-checked.

The culminating result is a self-contained universality proof for
Fibonacci anyonic braiding: verified MTC data $\to$ braiding matrices
over $K = \mathbb{Q}(\zeta_5, \sqrt{\varphi})$ $\to$ Lie algebra
commutators $\to$ density in SU(2) (qubit) and SU(3) (qutrit).  The
discovery that $\sqrt{\varphi}$ lies outside every cyclotomic field,
necessitating a degree-8 non-abelian extension, demonstrates the
importance of tracking algebraic number fields precisely --- an
advantage of formal verification over pen-and-paper calculation.

The key technical enablers --- decidable algebraic number fields
(including the novel non-cyclotomic $K$) with \nativedecide{}
verification, and the categorical infrastructure
(\lean{PivotalCategory}, \lean{RibbonCategory},
\lean{FusionCategoryData}, \lean{ModularTensorData}) --- are being
prepared for contribution to Mathlib.

\begin{thebibliography}{99}
\bibitem{kitaev2006} A.~Kitaev, ``Anyons in an exactly solved model and beyond,'' \emph{Ann.\ Phys.}\ \textbf{321}, 2 (2006). arXiv:cond-mat/0506438.
\bibitem{rowell2009} E.~Rowell, R.~Stong, Z.~Wang, ``On classification of modular tensor categories,'' \emph{Comm.\ Math.\ Phys.}\ \textbf{292}, 343 (2009).
\bibitem{mss2024} D.~J.~Myers, H.~Sati, U.~Schreiber, ``Topological quantum gates in homotopy type theory,'' \emph{Comm.\ Math.\ Phys.}\ \textbf{405}, 172 (2024).
\bibitem{hietala2021} K.~Hietala et al., ``A verified optimizer for quantum circuits,'' \emph{Proc.\ ACM POPL} (2021).
\bibitem{physlean} J.~Tooby-Smith, ``PhysLean: Lean 4 for physics,'' \url{https://github.com/HEPLean/PhysLean} (2024).
\bibitem{aristotle} Aristotle automated theorem prover, \url{https://aristotle.harmonic.fun} (2026).
\bibitem{bhmv1995} C.~Blanchet, N.~Habegger, G.~Masbaum, P.~Vogel, Topology \textbf{34}, 883 (1995).
\bibitem{wenzl1988} H.~Wenzl, Invent.\ Math.\ \textbf{92}, 349 (1988).
\bibitem{reshetikhin1991} N.~Reshetikhin, V.~Turaev, Invent.\ Math.\ \textbf{103}, 547 (1991).
\bibitem{flw2002} M.~H.~Freedman, M.~Larsen, Z.~Wang, ``A modular functor which is universal for quantum computation,'' \emph{Comm.\ Math.\ Phys.}\ \textbf{227}, 605 (2002).
\end{thebibliography}

\end{document}
