---
paper: D3
bundle: D3
bundle_target: D3
tier: 1
reviewer: lead
model: claude-opus-5
review_date: 2026-08-15T00:00:00Z
readiness_gates_version: 1
kind: stage10-redraft
prior_reviews:
  - papers/AutomatedReviews/2026-05-04-bundle-stage13/D3.md
  - papers/AutomatedReviews/2026-05-06-bundle-stage13/D3.md
  - docs/audits/stage13_attribution_sweep_2026-06-10.md#d3
  - papers/AutomatedReviews/2026-08-15-l3-stage10-redraft/L3.md
---

# D3 — findings surfaced by the Stage-10 full redraft

## Summary

**2 CRITICAL, 3 BLOCKER, 8 REQUIRED, 3 RECOMMENDED.** Every one was found while drafting, by
reading the Lean statements and the held primary sources rather than by
reviewing the superseded manuscript. The redraft states only what the substrate
supports, so none is a live defect *in the new draft*; they are defects in the
substrate, the citation registry, the bundle metadata, one validation check,
and the portfolio routing, which the new draft had to route around.

The three blockers are not stylistic. Two are misreadings of held primary
sources that sit on this bundle's spine and propagate to the flagship: the
Christensen--Duff attribution for the coefficient that fixes Newton's constant
is the wrong paper at the wrong heat-kernel order, and the Sen comparison that
is the entropy section's sharpest external check compares two coefficients of
different logarithms. The third is a validation check that reports GREEN over a
population it cannot see.

**A structural note that is not a finding but conditions all of them.** D3's 69
open findings are almost entirely inherited: 63 of them are keyed to
*contributing source papers*, not to D3, and the single blocking one
(`review:2026-04-25-0135-internal-adversarial:paper20_scalar_rung_REINVOCATION:1.11`)
is against a source paper whose content the charter never asked D3 to carry.
See §5.1.

---

## Findings

### 0.1 — 🛑 CRITICAL — The substrate's Dirac `a_4` coefficients do not reproduce the published Dirac `a_4`, and two of three signs disagree

- **Severity:** critical
- **Lane:** `research`
- **⚠️ OPERATOR DECISION. NOT ACTED ON.** Correcting this changes what D3 concludes rather than fixing a false statement about what D3 concludes. The redraft therefore states the substrate's own coefficients, attributes them to this work rather than to the literature, and does not apply the correction. The ruling is the operator's.
- **Verify:** `cd "$REPO" && uv run python -c "
from src.core.formulas import higher_curvature_microscopic_stelle as s
a,b,g = s(16.0)
assert abs(a) < 1e-12, f'R^2 Stelle coefficient is {a}, published Dirac value is 0'"`
  *What it asserts:* asserts the `R^2` Stelle coefficient equals the published Dirac value of zero. Exits 1 at HEAD.
- **Gate:** ComputationCorrectness
- **Location:** `lean/SKEFTHawking/HeatKernelExpansion.lean` (`a4_R_sq_coef`, `a4_Ricci_sq_coef`, `a4_Riemann_sq_coef`); `src/core/formulas.py` (`seeley_dewitt_a4_basis`, `higher_curvature_microscopic_stelle`); consumed by `MicroscopicCoefficientMatch.higherCurvature_stelle_sum_eq` and by `NonlinearDiffInvariance`; propagated to `papers/D3` and `papers/F`.
- **Observed:** The substrate encodes `(c_R^2, c_Ric^2, c_Riem^2) = (-5, +7, -12)/(12*180)` per species in units of `(4π)^-2`.
- **Evidence:** Derived twice, independently, from the held primary source `Lit-Search/Phase-6e/primary-sources/Vassilevich2003.pdf`, by two drafters working from different sections and without contact.
  - From Vassilevich Eq. (4.28) with the Dirac data (`tr 1 = 4`, `E = -R/4`, `tr(Ω_μν Ω^μν) = -(1/2) R_μνρσ²`): `a_4 = (4π)^-2 (1/360)[5R² - 8Ric² - 7Riem²]` per Dirac species, i.e. `(+30, -48, -42)/(12*180)`.
  - Independently from Vassilevich Eq. (4.35) with the spin-1/2 row of his Table 1, `(a,b,c,d) = (-7/2, -11, 6, 0)`: the same values.
  - Cross-checked against the textbook Dirac Weyl-anomaly coefficients `c = 1/20`, `a = 11/360`. The published values reproduce them exactly. The substrate's do not.
  - The two sets are not proportional: termwise ratios are `-1/6`, `-7/48`, `+2/7`. **Two of three signs disagree.**
- **Blast radius if the correction is applied.** The implied Stelle triple becomes `(α, β, γ) = (0, -1/20, +11/360) N_f (4π)^-2` with aggregate `-7 N_f/360 (4π)^-2`, not `-7 N_f/810 (4π)^-2`. The sign conclusion of `higherCurvature_stelle_sum_negative` SURVIVES, as do `β < 0` and `γ > 0`. What does not survive: `α < 0` (the published value is exactly zero), the closed form in `higherCurvature_stelle_sum_eq`, and every rendered Stelle snippet.
- **Expected:** The substrate's `a_4` reproduces the published Dirac `a_4`, or the substrate documents the convention under which it does not and the manuscript never attributes the values to the heat-kernel literature.
- **Fix:** Operator ruling first. If the published values are correct, the correction lands in `HeatKernelExpansion.lean` and propagates through `HigherCurvatureStructure`, `NonlinearDiffInvariance` and `MicroscopicCoefficientMatch`. The redraft attributes the basis to the literature and the values to this work, so it is correct either way.
- **Cache:** local primary source (PDF), extracted and read in full by two independent readers.

### 0.2 — 🛑 CRITICAL — The `a_2` coefficient that fixes Newton's constant omits the Dirac index trace that `a_0` carries

- **Severity:** critical
- **Lane:** `research`
- **⚠️ OPERATOR DECISION. NOT ACTED ON.** This one reaches the number in the abstract. Correcting it changes `G_N^Sak` by a factor of four, and with it the matching locus, the regime-partition mass, the entropy prefactor, the emergent cosmological constant and the torsion amplitude. That is a change to what D3 concludes and it is not this redraft's to make.
- **Verify:** `cd "$REPO" && uv run python -c "
import json; d=json.load(open('lean/lean_deps.json'))
n={e['name'] for e in d}
assert 'SKEFTHawking.HeatKernelExpansion.a2_R_coefficient_eq_gilkey_trace' in n, 'no theorem reconciles a_2 with the traced Gilkey form'"`
  *What it asserts:* asserts the substrate carries a statement reconciling its `a_2` normalisation with the standard traced form, in either direction. Exits 1 at HEAD.
- **Gate:** ComputationCorrectness
- **Location:** `lean/SKEFTHawking/HeatKernelExpansion.lean:102` (`a0_dirac`), `:128` (`a2_R_coefficient`), `:233` (`G_N_from_a2`).
- **Observed:** `a0_dirac N_f = 4 * N_f * (4π)^-2`, whose docstring states "The factor 4 = `tr 𝟙_4` is the Dirac-spinor index trace per species". `a2_R_coefficient N_f = -(N_f/12) * (4π)^-2`, which carries no such factor. The two coefficients of the same expansion, for the same operator, disagree about whether the spinor trace has been taken.
- **Evidence:** Vassilevich Eq. (4.27), `a_2 = (4π)^{-n/2} (1/6) ∫ tr_V {6E + R}`, with the Lichnerowicz endomorphism `E = -R/4` that the module's own docstring declares. Then `6E + R = -R/2`, and `tr_V(1) = 4 N_f` over the spinor bundle for `N_f` Dirac species, giving `a_2^R = -N_f/(3 (4π)^2)`, four times the module's value in magnitude.
- **Blast radius if the correction is applied.** `G_N_from_a2` is defined as `12π/(N_f Λ²)` and that value follows from the module's own `-N_f/12`, so this is not a downstream arithmetic slip: the definition is consistent with the untraced `a_2`. With the traced `a_2` the same matching step gives `G_N = 3π/(N_f Λ²)`. Every number in this bundle that consumes `G_N` moves with it.
- **Expected:** `a_0` and `a_2` agree about the trace convention, and whichever convention is chosen, a statement reconciles the substrate's `a_2` with the standard traced form.
- **Fix:** Operator ruling. If the traced form is correct, `a2_R_coefficient` and `G_N_from_a2` both change and the manuscript's central number changes with them. If the module's convention is deliberate, it needs to be stated and a reconciling theorem needs to exist, because the internal disagreement between `a_0` and `a_2` is what makes the convention unreadable from the source. The redraft states both coefficients exactly as the substrate defines them and asserts no agreement with a published value.
- **Cache:** local primary source (PDF), read in full.

### 1.1 — 🔴 BLOCKER — The heat-kernel coefficient that fixes Newton's constant is attributed to the wrong Christensen--Duff paper, at the wrong order

- **Severity:** blocker
- **Lane:** `research`
- **Verify:** `cd "$REPO" && uv run python -c "from src.core.citations import CITATION_REGISTRY as C; cd8=C.get('ChristensenDuff1978'); assert cd8 is None or 'a_4' in str(cd8.get('provides')) or 'anomal' in str(cd8.get('title','')).lower() and 'a_2' not in str(cd8.get('provides')), 'CD1978 still advertised as the a_2 Dirac source'; assert 'ChristensenDuff1979' in C and C['ChristensenDuff1979'].get('doi_verified') is True" && ! grep -q 'Vassilevich Eq. 4.37' papers/D3/paper_draft.tex && ! grep -qE 'ChristensenDuff1978[^}]*\}[^.]*a_2|a_2[^.]*ChristensenDuff1978' papers/D3/paper_draft.tex`
  *What it asserts:* asserts the `a_2` Dirac coefficient is no longer sourced to `ChristensenDuff1978`, that the false equation pointer is gone from the draft, and that the paper Vassilevich actually credits is registered and verified. Exits 1 at HEAD.
- **Gate:** CitationIntegrity
- **Location:** `papers/D3/paper_draft.tex:10` (file header, `single-coefficient identity ... (Vassilevich Eq. 4.37)`), `:92-93`, `:1410-1412`, `:1425-1428`; propagated to `papers/F/paper_draft.tex`; `src/core/citations.py` (`ChristensenDuff1978`).
- **Observed:** The draft calls the Sakharov closed form "Vassilevich's Christensen--Duff coefficient ... extracted from a Dirac heat-kernel expansion at order $a_2$", cites `\cite{ChristensenDuff1978,Vassilevich2003}` for it, and its file header pins the identity to "Vassilevich Eq. 4.37".
- **Evidence:** Read directly from the held primary source,
  `Lit-Search/Phase-6e/primary-sources/Vassilevich2003.pdf`, extracted in full.
  - Vassilevich's own sentence, verbatim: "one can use the analysis of Christensen and Duff [127] who calculated **a4** for arbitrary spin fields". Christensen and Duff supply him with $a_4$, not $a_2$.
  - His `[127]` is "New gravitational index theorems and supertheorems," *Nucl. Phys.* **B154** (1979) 301. That is `ChristensenDuff1979`, a separate and already-registered entry.
  - `CITATION_REGISTRY['ChristensenDuff1978']` is "Axial and conformal **anomalies** for arbitrary spin in gravity and supergravity," *Phys. Lett. B* (1978), with `primary_source_path=None` and `doi_verified=None`. It is a different paper on a different subject, and no text of it is held.
  - Vassilevich's Eq. (4.37) is the DeWitt ansatz for a massless scalar heat kernel. It contains no Dirac coefficient and no $G_N$.
- **Expected:** The $a_4$ arbitrary-spin coefficients are Christensen--Duff (1979). The $a_2$ Dirac coefficient is the standard heat-kernel result collected in Vassilevich's review, and should be cited to the source that is actually held and read.
- **Fix:** Re-source $a_2$ to `Vassilevich2003`; re-source the $a_4$ basis to `ChristensenDuff1979`; delete the Eq. (4.37) pointer; acquire a primary source for `ChristensenDuff1979` (currently `.abstract.txt` only) or drop the entry in favour of the review. Propagate to `papers/F`. The redraft states it correctly.
- **Cache:** local primary source (PDF), extracted and read in full.

### 1.2 — 🔴 BLOCKER — Sen's logarithmic coefficient and Kaul--Majumdar's are compared across different logarithms

- **Severity:** blocker
- **Lane:** `research`
- **Verify:** `cd "$REPO" && uv run python -c "import re,pathlib; t=pathlib.Path('papers/D3/paper_draft.tex').read_text(); m=re.search(r'212/45.{0,400}', t, re.S); assert m, 'Sen comparison absent'; s=m.group(0); assert ('ln a' in s or '\\\\ln a' in s or 'log a' in s), 'Sen coefficient still compared without naming its logarithm argument'"`
  *What it asserts:* asserts the manuscript names the logarithm each coefficient multiplies wherever it puts the two side by side. A silent deletion of the comparison fails this too, since the passage must still exist. Exits 1 at HEAD.
- **Gate:** ComputationCorrectness
- **Location:** `papers/D3/paper_draft.tex:833-836`; `lean/SKEFTHawking/BHEntropyMicroscopic.lean` (`sen_4d_disagrees_with_kaul_majumdar`, `sen_4d_quantitative_disagreement_bound`); `papers/D3/bundle_metadata.json` (`apex_theorems[*].claims` for both).
- **Observed:** The draft writes $+(212/45-3)=77/45\approx+1.71$ and says it "disagrees with the Kaul--Majumdar $-3/2$", placing the two numbers in direct comparison.
- **Evidence:** `Lit-Search/Phase-6a/primary-sources/Sen2013Schwarzschild.pdf`, p. 6 and footnote 3. Sen: "the logarithmic correction to $S_{singlet}$ is given by $\Delta S_{singlet} = (\frac{212}{45} - 3)\ln a$"; and, explicitly, "the result of [30] gives a logarithmic correction of $-\frac{3}{2}\ln A_H = -3\ln a$ ... Converting this to number of states per unit interval in mass gives a result of $-2\ln a$."
  $77/45$ multiplies $\ln a$; $-3/2$ multiplies $\ln A_H$. Sen's own like-for-like pair is $77/45$ against $-2$.
- **Expected:** The disagreement is real and survives; the stated comparison does not. Either convert to a common logarithm or quote Sen's own pairing.
- **Fix:** State the comparison in one logarithm and say which. Correct the two apex `claims` strings, which currently describe the quantitative disagreement without naming the convention that makes it quantitative. The redraft states it correctly.
- **Cache:** local primary source (PDF), pp. 1--8 read.

### 1.3 — 🔴 BLOCKER — `citation_primary_sources_present` counts a CrossRef metadata stub as a primary source

- **Severity:** blocker
- **Lane:** `harness`
- **Verify:** `cd "$REPO" && uv run python -c "import pathlib,re; s=pathlib.Path('scripts/validation/checks/citations.py').read_text(); assert '.json' not in re.search(r'def .*(cache|resolve).{0,4000}', s, re.S).group(0) or 'metadata stub' in s, 'json still accepted as a cache extension without a stub carve-out'" && uv run python scripts/validate.py --check citation_primary_sources_present --no-archive 2>&1 | grep -qiE 'stub|metadata-only'`
  *What it asserts:* asserts the check distinguishes a bibliographic record from a text, and reports the stub population rather than absorbing it into the full-text count. Exits 1 at HEAD.
- **Gate:** CitationIntegrity
- **Location:** `scripts/validation/checks/citations.py:389`, `:565`.
- **Observed:** The check passes globally, reports `0 need cache / 0 missing-from-registry`, and says nothing about D3. It resolves caches by globbing `Lit-Search/*/primary-sources/<bibkey>.{pdf,tex,abstract.txt,json}` rather than by reading the registry's own `primary_source_path`, and only `.abstract.txt` is flagged as abstract-only.
- **Evidence:** A CrossRef `.json` record carries title, authors, journal, pages and DOI. It carries no text of the paper. Four load-bearing D3 citations resolve to exactly such a file and are counted inside the check's "full text" figure: `ChristensenDuff1978`, `GellMannOakesRenner1968`, `HughesDrever1960`, `Israel1986ThirdLaw`. The registry is honest about all four, recording `primary_source_path=None` for three of them; the check does not read that field. Conversely `Sen2013`, `Balbinot2005` and `KehleUnger2024` are recorded as `None` while real PDFs sit on disk, so the registry and the filesystem disagree in both directions.
- **Expected:** A `.json` metadata record is a bibliographic stub, reported as such, and never counted as the source. The registry's `primary_source_path` is the field the check reads.
- **Fix:** Read `primary_source_path`; classify `.json` alongside `.abstract.txt` as metadata-only; report both populations separately from full text. This is the same failure shape as an assertion keyed on a proxy rather than on what the mechanism returned.
- **Cache:** N/A.

### 2.1 — 🟡 REQUIRED — "Without invoking a pointwise Null Energy Condition" is attached to Glorioso--Liu in D3, who neither assume nor target it

- **Severity:** required
- **Lane:** `research`
- **Verify:** `cd "$REPO" && ! grep -qi 'without invoking a pointwise' papers/D3/paper_draft.tex`
  *What it asserts:* asserts the vacuous attribution is gone from D3's manuscript. The same phrase was removed from the Lean module for L3; this is the prose copy that survived. Exits 1 at HEAD.
- **Gate:** CitationIntegrity
- **Location:** `papers/D3/paper_draft.tex:921-923`.
- **Observed:** "$dA/dt \ge 0$ via the Glorioso--Liu Schwinger--Keldysh entropy-current monotonicity, *without invoking a pointwise Null Energy Condition*; this is the substrate's entropy-current upgrade relative to classical Hawking 1971."
- **Evidence:** `Lit-Search/Phase-1-and-Background/primary-sources/GloriosoLiu2018.pdf`, extracted in full: zero occurrences of "null energy", "energy condition", "NEC", "pointwise", "area theorem", "horizon" or "black hole". The paper proves a second law for dissipative many-body systems from a $Z_2$ dynamical KMS symmetry and a classical remnant of unitarity. The statement is true and vacuous as an attribution.
- **Expected:** If the contrast with the area-theorem route is wanted, it is this work's own comparison and needs its own grounding for the area-theorem side.
- **Fix:** Drop the phrase or restate it as our comparison with a citation for the other side. The redraft omits it.
- **Cache:** local primary source (PDF), read in full.

### 2.2 — 🟡 REQUIRED — A "Volovik--Jannes" criterion is cited to two works by neither author

- **Severity:** required
- **Lane:** `research`
- **Verify:** `cd "$REPO" && ! grep -q 'Volovik--Jannes four-condition Sakharov criterion~\\cite{JacobsonVolovik1998' papers/D3/paper_draft.tex && uv run python -c "from src.core.citations import CITATION_REGISTRY as C; assert C['JannesVolovik2012'].get('primary_source_path'), 'JannesVolovik2012 holds no source'"`
  *What it asserts:* asserts the attribution no longer points at works by neither author, and that the paper it should point at is held. Exits 1 at HEAD.
- **Gate:** CitationIntegrity
- **Location:** `papers/D3/paper_draft.tex:1475-1476`.
- **Observed:** "the Volovik--Jannes four-condition Sakharov criterion~`\cite{JacobsonVolovik1998,Volovik2003BraneBH}`".
- **Evidence:** `JacobsonVolovik1998` is Jacobson and Volovik, "Event horizons and ergoregions in ³He"; its full text contains no occurrence of "Sakharov" and no four-condition criterion. `Volovik2003BraneBH` is Volovik alone, and its only Sakharov content is the passing phrase "similar to the Sakharov gravity [8] induced by quantum fluctuations". Neither has Jannes as an author. The registry holds `JannesVolovik2012` (Jannes and Volovik, arXiv:1108.5086) at `Lit-Search/Phase-6m/primary-sources/JannesVolovik2012.pdf`.
- **Expected:** The induced-gravity conditions are Sakharov's; the ³He-A instantiation is Jannes and Volovik.
- **Fix:** Re-cite. `JannesVolovik2012` carries `doi_verified=None` and should be verified. The redraft cites it correctly.
- **Cache:** local primary sources (PDF), both cited works extracted and searched in full.

### 2.3 — 🟡 REQUIRED — The Frolov--Fursaev quarter coefficient carries no citation apparatus at all

- **Severity:** required
- **Lane:** `research`
- **Verify:** `cd "$REPO" && ! grep -q 'hep-th/9607104' papers/D3/paper_draft.tex && uv run python -c "from src.core.citations import CITATION_REGISTRY as C; assert any(k.startswith('FrolovFursaev') for k in C), 'no FrolovFursaev registry entry'"`
  *What it asserts:* asserts the bare inline identifier is replaced by a registered, citable entry. Exits 1 at HEAD.
- **Gate:** CitationIntegrity
- **Location:** `papers/D3/paper_draft.tex:775-781`, `:795`.
- **Observed:** The heat-kernel-to-Einstein--Hilbert ratio claim's only pointer is the inline string "FFZ, hep-th/9607104". There is no `\cite`, no `\bibitem`, and no `FrolovFursaev*` key in `CITATION_REGISTRY`.
- **Evidence:** Direct read of the draft and of the registry. Because the claim carries no bibkey, no citation check can ever ask it the cache question: it is externally anchored and structurally invisible to the citation gates.
- **Expected:** An externally anchored claim carries a bibkey, and the bibkey carries a source.
- **Fix:** Register the entry, acquire the source, add the `\bibitem`, and cite. The redraft cites it.
- **Cache:** N/A.

### 2.4 — 🟡 REQUIRED — Three cited bibkeys are the unbacked twin of a held entry

- **Severity:** required
- **Lane:** `research`
- **Verify:** `cd "$REPO" && ! grep -q 'cite{Sen2013}' papers/D3/paper_draft.tex && ! grep -q 'cite{Balbinot2005}' papers/D3/paper_draft.tex && uv run python -c "from src.core.citations import CITATION_REGISTRY as C; assert C['KehleUnger2024'].get('primary_source_path'), 'KehleUnger2024 path still unrecorded while the PDF is on disk'"`
  *What it asserts:* asserts the manuscript cites the twin that carries the source, and that the registry records paths for files that exist. Exits 1 at HEAD.
- **Gate:** CitationIntegrity
- **Location:** `papers/D3/paper_draft.tex:834` (`Sen2013`), `:899` and `:2616-2620` (`Balbinot2005`), `:926-928` (`KehleUnger2024`).
- **Observed:** Each cited key has `primary_source_path=None` and `doi_verified=None`, while a sibling key or an on-disk file carries the verified source: `Sen2013Schwarzschild.pdf`, `Balbinot2005PRD.pdf`, `Phase-1-and-Background/primary-sources/KehleUnger2024.pdf`. `Israel1986` and `Israel1986ThirdLaw` are a fourth such pair, where the "held" path is itself a metadata stub (see §1.3).
- **Evidence:** Registry read for each key; files located on disk. The physics is unaffected in every case, so this is bookkeeping, but it is bookkeeping that makes a citation gate report clean over an unverified citation.
- **Expected:** One entry per work, carrying the path to the source actually held.
- **Fix:** Merge the duplicate pairs and record the paths. The redraft cites `Balbinot2005PRD` and `Sen2013Schwarzschild`.
- **Cache:** N/A.

### 3.1 — 🟡 REQUIRED — The bundle metadata attributes the regime biconditional to a theorem that does not state it

- **Severity:** required
- **Lane:** `lean`
- **Verify:** `cd "$REPO" && uv run python -c "import json; m=json.load(open('papers/D3/bundle_metadata.json')); a=[x for x in m['apex_theorems'] if x['name'].endswith('regime_partition_criterion')][0]; assert 'iff M > M_c' not in a['claims'] and 'biconditional' not in a['claims'].lower(), 'claims string still promises the M > M_c biconditional'"`
  *What it asserts:* asserts the metadata describes the theorem's own statement. Exits 1 at HEAD.
- **Gate:** LeanProofSubstance
- **Location:** `papers/D3/bundle_metadata.json` (`apex_theorems`, `BHThermodynamicsFourLaws.regime_partition_criterion`); `lean/SKEFTHawking/BHThermodynamicsFourLaws.lean`.
- **Observed:** The `claims` string reads "the regime-partition biconditional: Schwarzschild branch iff $M > M_c$. Shared character-for-character with L3".
- **Evidence:** Type read from `lean/lean_deps.json`. `regime_partition_criterion` states: given `H_RegimePartition`, if `classify b p = Regime.Schwarzschild` then `0 < dT_dt_evap`, and if `classify b p = Regime.ADWExtremality` then `dT_dt_evap < 0`. It is a statement about evaporation signs, and both signs are fields of the hypothesis structure it consumes. The $M > M_c$ biconditional is a *different* theorem in the same module, `classify_Schwarzschild_iff`, whose type is `classify b p = Regime.Schwarzschild ↔ b.M > M_c p`.
- **Expected:** The biconditional is credited to the theorem that proves it, and the criterion is described as transporting its branch signs.
- **Fix:** Correct the `claims` string and add `classify_Schwarzschild_iff` to the apex list. The redraft cites `classify_Schwarzschild_iff` for the biconditional and says plainly that the signs are hypotheses.
- **Cache:** N/A.

### 3.2 — 🟡 REQUIRED — The bundle metadata reads `a0_dirac_linear` as fixing a value, and its statement is a homogeneity property

- **Severity:** required
- **Lane:** `lean`
- **Verify:** `cd "$REPO" && uv run python -c "import json; m=json.load(open('papers/D3/bundle_metadata.json')); a=[x for x in m['apex_theorems'] if x['name'].endswith('a0_dirac_linear')][0]; assert 'a0 = N_f' not in a['claims'], 'claims string still asserts a value the statement does not fix'"`
  *What it asserts:* asserts the metadata describes homogeneity rather than a value. Exits 1 at HEAD.
- **Gate:** LeanProofSubstance
- **Location:** `papers/D3/bundle_metadata.json` (`apex_theorems`, `HeatKernelExpansion.a0_dirac_linear`); `papers/D3/paper_draft.tex:1424`.
- **Observed:** The `claims` string reads "a0 = N_f, the coefficient that later reproduces the cosmological-constant problem", and the draft writes "$a_0(N_f) = N_f$ (one species per Dirac fermion; `a0_dirac_pos`, `a0_dirac_linear`)".
- **Evidence:** Type read from `lean/lean_deps.json`: `∀ (k N_f : ℝ), a0_dirac (k * N_f) = k * a0_dirac N_f`. That is homogeneity of degree one in the species count. It fixes no value, and neither does `a0_dirac_pos`, which asserts positivity.
- **Expected:** The prose and the metadata say what is proved: the leading coefficient scales linearly with the species count.
- **Fix:** Restate both. If the value is wanted as a theorem, it is a one-line addition. The redraft states homogeneity.
- **Cache:** N/A.

### 3.3 — 🔵 RECOMMENDED — The substrate-class partition is `rfl` on hand-declared Boolean records

- **Severity:** recommended
- **Lane:** `lean`
- **Verify:** `cd "$REPO" && uv run python -c "import json; d=json.load(open('lean/lean_deps.json')); e=[x for x in d if x['name'].endswith('CrooksAnalogHawking.horizonCrooks_substrate_partition')][0]; deps=set(e.get('name_deps_project',[])+e.get('value_deps_project',[])); assert any('WorkDistribution' in x and 'zero' not in x for x in deps), 'partition still rests on placeholder detailed-balance witnesses'"`
  *What it asserts:* asserts the partition consumes a non-placeholder detailed-balance witness. Exits 1 at HEAD.
- **Gate:** LeanProofSubstance
- **Location:** `lean/SKEFTHawking/JacobsonThermoGRDarkEnergy.lean:285-330`; `lean/SKEFTHawking/CrooksAnalogHawking/`.
- **Observed:** `SakharovConditions` is a record of four Booleans; `volovikJannes_he3a` sets all four to `true` and `flsBEC` sets two to `false`, both by hand; `volovik_jannes_he3a_satisfies_sakharov_criterion` is `rfl`. The detailed-balance witnesses are `WorkDistribution.zero` with $\sigma \equiv 0$.
- **Evidence:** Source read directly. The classification is a faithful *record* of a physical argument (depletion-sector atoms do not propagate on the condensate effective metric) and it is not a derivation of that argument.
- **Expected:** Either the witnesses become substantive, or the manuscript presents the partition as a recorded classification with its stated mechanism.
- **Fix:** The redraft takes the second path explicitly and in the open, rather than in a footnote.
- **Cache:** N/A.

### 4.1 — 🟡 REQUIRED — The heating/cooling contrast between the two ³He-A systems is frame-dependent; only the end temperature is not

- **Severity:** required
- **Lane:** `research`
- **Verify:** `cd "$REPO" && uv run python -c "from src.core.citations import CITATION_REGISTRY as C; p=' '.join(C['JacobsonKoike2002']['provides']); assert 'wall frame' in p or 'Killing' in p, 'JK-2002 evaporation sign recorded without naming its frame'; assert 'T_{H, sf}' in p or 'superfluid frame' in p, 'the superfluid-frame relation is not recorded'"`
  *What it asserts:* asserts the registry records which frame the sign statement holds in, and records the Doppler-shifted superfluid-frame relation alongside it. Exits 1 at HEAD.
- **Gate:** CitationIntegrity
- **Location:** `src/core/citations.py` (`JacobsonKoike2002['provides']`); `lean/SKEFTHawking/BHThermodynamicsFourLaws.lean` docstring; `papers/D3/paper_draft.tex:899-905`; `papers/F/paper_draft.tex`.
- **Observed:** The registry, corrected earlier today, records "dT_H/dv < 0 monotonically; radiation slows v ⇒ T_H ↑ toward the NON-ZERO constant T_H(0)" and calls this "THE heating contrast case".
- **Evidence:** `Lit-Search/Phase-6a/primary-sources/JacobsonKoike2002.source/bhbu.tex`, read in full at the relevant equations. Eq. (E:TH) gives $T_H(v) = T_H(0)(1 - v^2/c_\perp^2)$ and the source states immediately that this "gives the Hawking temperature **in the wall reference frame** (the ``Killing temperature'' in the language of general relativity)". The next equation, (E:Tsf), gives the asymptotic superfluid frame: $T_{H,sf} = T_H(0)(1 + v/c_\perp)$, which **decreases** toward $T_H(0)$ as radiation slows the wall. The source's own summary is frame-independent: "The Hawking temperature in the frame of the superfluid is thus never less than $T_H(0)$."
- **Expected:** The sign of $dT/dt$ under back-reaction is frame-dependent for this system. The frame-independent contrast, and the one the regime partition actually needs, is that one family of textures ends at zero temperature and the other ends at a finite one.
- **Fix:** Record the frame in the registry entry and in the module docstring, and state the contrast by end temperature. The correction landed earlier today recorded the wall-frame sign only, which is correct but incomplete, and a referee reading `bhbu.tex` would find the other equation two lines later. The redraft states the contrast by end temperature.
- **Cache:** local primary source (arXiv TeX), read in full.

### 5.1 — 🟡 REQUIRED — Cutting D3's charter-absent sections leaves four source papers with no destination but the flagship

- **Severity:** required
- **Lane:** `process`
- **Verify:** `cd "$REPO" && uv run python -c "import sys; sys.path.insert(0,'scripts'); from bundle_migration import parse_mapping, MAPPING_DOC; a=parse_mapping(MAPPING_DOC.read_text()); orphans=[p for p in ('paper20_scalar_rung','paper21_majorana_rung','paper22_ew_phase_transition','paper33_ewbg_chirality_wall') if set(a[p]['bundle_destinations']) <= {'D3','F'}]; assert not orphans, f'still routed only through D3/F: {orphans}'"`
  *What it asserts:* asserts each of the four has a destination that is not D3 and not the flagship. Exits 1 at HEAD.
- **Gate:** CrossPaperConsistency
- **Location:** `docs/PAPER_DRAFT_MAPPING.md:43-45`, `:52`; `docs/PAPER_STRATEGY.md:94-102`.
- **Observed:** The charter's D3 content sentence names the gap equation, gauge erasure, the Wen deficit, the fracton no-go, the linearised equations, the gravitational-wave result, the entropy count, the four laws by regime, and the QCD cross-bridges. It does not name the scalar rung, the Majorana rung, the electroweak phase transition or the baryogenesis bridge. Neither does the apex list: none of the 92 apex theorems covers any of them. They reached D3 by source registration, which the procedure has since made explicit is not a section grant.
- **Evidence:** The mapping routes all four only to D3 and to F. The single blocking finding against D3's aggregate is against `paper20_scalar_rung`, i.e. against content the charter never asked D3 to carry; removing the sections removes the blocker as a side effect, which is a signal that the content was misplaced rather than that the finding was wrong.
- **Expected:** Content with no charter and no apex coverage in its only deep-paper destination needs a destination decided deliberately.
- **Fix:** **This is a portfolio routing decision and it belongs to the operator, not to this redraft.** The redraft cuts the sections from D3 on charter grounds and does not delete, retract or reclassify the source material. The four source drafts are untouched and the flagship still carries them. The open question is where the matter-sector and electroweak content publishes.
- **Cache:** N/A.

### 5.2 — 🔵 RECOMMENDED — D3 shipped no figures behind deferrals whose reasons were process statements, and two of the deferred figures already existed

- **Severity:** recommended
- **Lane:** `figure`
- **Verify:** `cd "$REPO" && test $(grep -c 'includegraphics' papers/D3/paper_draft.tex) -ge 4 && ! grep -q 'L1/D3 split' papers/D3/paper_draft.tex`
  *What it asserts:* asserts the bundle ships at least its tier's figure floor as rendered figures, and that no deferral reason names an internal work-allocation question. Exits 1 at HEAD.
- **Gate:** NarrativeGrounding
- **Location:** `papers/D3/paper_draft.tex:376`, `:427`, `:749`, `:1056`.
- **Observed:** Four `\figuredeferred` declarations and zero `\includegraphics`. `bundle_figure_adequacy` passes, because a deferral satisfies the floor. One deferral reason reads "L1 carries this figure ... so its framing waits on the L1/D3 split being settled": a work-allocation question, surfaced to the reader, standing in for a figure whose PNG was already rendered at `figures/fig_c_GW_vs_ligo_constraint.png`. The bifurcation deferral is similarly answerable from existing renders.
- **Evidence:** Direct read; renders located. `figures/fig_T_H_evolution_regime_partition.png`, regenerated earlier today, and `figures/fig_torsion_obs_bound.png` were not referenced at all.
- **Expected:** A deferral reason is a physics or data reason a referee can weigh. The lattice deferral qualifies; the others did not.
- **Fix:** The redraft ships four rendered figures and retains one deferral with a reason that survives reading.
- **Cache:** N/A.

### 5.3 — 🔵 RECOMMENDED — The chiral-symmetry-breaking match is headed "PDG" and uses different values, against a source held abstract-only and two editions stale

- **Severity:** recommended
- **Lane:** `research`
- **Verify:** `cd "$REPO" && uv run python -c "from src.core.citations import CITATION_REGISTRY as C; p=C['PDG2024'].get('primary_source_path',''); assert p and not p.endswith('.abstract.txt'), 'PDG entry is still abstract-only'"`
  *What it asserts:* asserts the particle-data citation resolves to a source carrying the quoted numbers. Exits 1 at HEAD.
- **Gate:** ParameterProvenance
- **Location:** `papers/D3/paper_draft.tex:219`, `:1307`, `:1318-1332`.
- **Observed:** A subsection headed "GMOR PDG match" quotes "the more precise PDG 2024 central values", while the discharged theorem uses a lattice-averaging set, which the draft admits at `:1331-1332`. The pion mass used matches neither the neutral nor the charged pion of the quoted particle-data set.
- **Evidence:** `Lit-Search/Phase-5z/primary-sources/PDG2024.abstract.txt` is a publisher blurb containing no numbers. The edition is two behind as of this review.
- **Expected:** The heading names the data set the theorem uses, and the citation resolves to a source that carries the values.
- **Fix:** Re-head the subsection, cite the lattice-averaging source for the values it supplies, and acquire a current particle-data source. The redraft states the comparison structurally and names the data set.
- **Cache:** abstract only (flagged, not counted as the source).

---

## Mechanical state after the redraft

Reported at integration in `papers/D3/change_log.md`.
