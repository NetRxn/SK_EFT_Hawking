---
paper: I1
bundle_target: I1
reviewer: adversarial-reviewer
model: claude-opus-4-7
review_date: 2026-05-01T15:07:00Z
readiness_gates_version: 1
review_mode: bundle
tier: 3
profile: software_methodology
round: 3
predecessor_review: papers/AutomatedReviews/2026-05-01-1500-bundle-stage13/I1.md
predecessor_review_round1: papers/AutomatedReviews/2026-05-01-1345-bundle-stage13/I1.md
---

# Adversarial Review — Bundle I1 (Verification methodology with worked cases) — Round 3

## Summary

Round-3 Stage-13 review of `papers/I1/paper_draft.tex` (1525 lines after
round-2 BLOCKER 3.1-residual remediation). The single round-2 BLOCKER
(3.1-residual: cross-registry mis-attribution in §5) is **verified
remediated**. Deterministic walk against `src/core/constants.py`
confirms each pillar in §5 now correctly identifies its owning
registry: pillar (i) `c_minus_equals_8Nf` in `HYPOTHESIS_REGISTRY`
(L1590, tier algebraic); pillar (ii) `gapped_interface_axiom` in
`AXIOM_METADATA` (L1351, tier hard); pillar (iii)
`chirality_limitation_zero` in `PLACEHOLDER_THEOREMS` (L1483) plus
`H_KLRS_SM_Crossover` in `HYPOTHESIS_REGISTRY` (L1627). The four-table
landscape is enumerated correctly with per-registry tier sets:
AXIOM_METADATA `{closed, hard, removed}`; HYPOTHESIS_REGISTRY
`{algebraic, hard, very_hard, open}`; BORDISM_HYPOTHESES
`{topological}`; PLACEHOLDER_THEOREMS marker-only. Round-2 RECOMMENDED
5.1 (Phase 6f filename em-dash drift) is also verified remediated:
LaTeX `--` switched to `---` with explicit clarifying note that the
filename contains a literal em-dash. Persisting non-blocking
advisories per user direction: 1.4 (citation cache empty for 5 external
bibitems — out-of-scope this session), 7.1 (BundleRiemannAux sub-count
off by 1 — comment-block false positive, non-blocking), 5.2 (Lean line
ranges off by 1 — non-blocking). Verdict: **GREEN — submission-ready
on the 11 readiness-gates evaluator. Zero open BLOCKERs. Three
acknowledged advisories carried forward as non-blocking.**

## Findings

### 3.1 — VERIFIED FIXED — §5 cross-registry attribution now correct

- **Gate:** LeanProofSubstance (Gate 5) / NarrativeGrounding (Gate 7) — VERIFIED FIXED
- **Location:** `papers/I1/paper_draft.tex:472-507`
- **Observed (round-3 deterministic re-verification):** §5 paragraph
  beginning at line 472 now reads: "The fix was a nine-fold structural
  decomposition driven by the project's structural-axiom registries in
  `src/core/constants.py`: `AXIOM_METADATA` (the true-axiom table; tier
  $\in \{$closed, hard, removed$\}$), `PLACEHOLDER_THEOREMS`
  (placeholder markers per Invariant 9), `HYPOTHESIS_REGISTRY` (tracked
  Prop bundles; tier $\in \{$algebraic, hard, very_hard, open$\}$),
  and `BORDISM_HYPOTHESES` (Lean Mathlib bordism-deferred hypotheses;
  tier $\in \{$topological$\}$). Together these form a four-table
  eliminability landscape covering six tier values."
- **Evidence (verified deterministically against constants.py):**
  - `AXIOM_METADATA` (defined `constants.py:1340-1430`) contains four
    entries: `non_abelian_center_discrete` (removed), `gs_nogo_axiom`
    (removed), `gapped_interface_axiom` (hard), `gaussianSaddleAsymptotic`
    (closed). Tier set used: `{removed, hard, closed}` — matches the
    paper's claim.
  - `HYPOTHESIS_REGISTRY` (defined `constants.py:1561-2933`) eliminability
    values actually instantiated: `{algebraic, hard, very_hard}`. The
    schema docstring at L1553 declares the legal vocabulary as
    `{algebraic, hard, very_hard, open}` — the paper's tier set
    matches the schema vocabulary (acceptable; `open` is a declared
    enum value with no current instances).
  - `BORDISM_HYPOTHESES` (defined `constants.py:2934-2959`) eliminability
    values: `{topological, algebraic}` — the paper claims
    `{topological}` only. Slight under-listing: H2 ("change of rings"
    via Shapiro's lemma) carries `eliminability: 'algebraic'` (L2944).
    However, the paper's claim that `BORDISM_HYPOTHESES` covers
    bordism-deferred hypotheses with topological tier is substantively
    correct — H2 is an algebraic side-channel within an otherwise
    topological registry. Marked non-blocking; see RECOMMENDED 3.1-r3
    below.
  - `PLACEHOLDER_THEOREMS` (defined `constants.py:1448-1532`) carries no
    eliminability key — entries have `category` ('content' or
    'summary') instead. The paper's "placeholder markers per Invariant 9"
    description is accurate; no tier claim made.
  - **Pillar attribution check (each cited line resolved against actual
    registry membership):**
    * (i) `c_minus_equals_8Nf` at `constants.py:1590` is inside
      `HYPOTHESIS_REGISTRY` (1561-2933). Eliminability 'algebraic'
      (L1593). Paper's pillar (i) attribution: HYPOTHESIS_REGISTRY,
      tier algebraic. ✓
    * (ii) `gapped_interface_axiom` at `constants.py:1351` is inside
      `AXIOM_METADATA` (1340-1430). Eliminability 'hard' (L1352).
      Paper's pillar (ii) attribution: AXIOM_METADATA, tier hard. ✓
    * (iii) `chirality_limitation_zero` at `constants.py:1483` is
      inside `PLACEHOLDER_THEOREMS` (1448-1532). Category 'content'
      (L1484). Paper's pillar (iii) attribution: PLACEHOLDER_THEOREMS
      placeholder marker. ✓
    * (iii cont.) `H_KLRS_SM_Crossover` at `constants.py:1627` is
      inside `HYPOTHESIS_REGISTRY` (1561-2933). Paper says "some of
      whose hypotheses --- e.g.\\ `H_KLRS_SM_Crossover` on line 1627
      --- are themselves `HYPOTHESIS_REGISTRY` entries." ✓
- **Verdict:** Round-2 BLOCKER 3.1-residual fully remediated.
  Supersession-ledger entry at `docs/review_finding_supersessions.json:1177-1183`
  is now substantively confirmable by deterministic walk. No new
  finding emitted at BLOCKER severity. Optional minor refinement noted
  below as RECOMMENDED 3.1-r3.

### 3.1-r3 — 🔵 RECOMMENDED — `BORDISM_HYPOTHESES` tier set under-lists by 1 ('algebraic' present at H2)

- **Gate:** NarrativeGrounding (Gate 7)
- **Location:** `papers/I1/paper_draft.tex:480-481`
- **Observed:** §5 says "`BORDISM_HYPOTHESES` (Lean Mathlib
  bordism-deferred hypotheses; tier $\in \{$topological$\}$)." The
  actual `BORDISM_HYPOTHESES` table (`constants.py:2934-2959`) has
  four entries (H1, H2, H3, H4); H2 carries `'eliminability':
  'algebraic'` (L2944, with comment "Shapiro's lemma — potentially
  provable") while H1, H3, H4 carry `'topological'`. The tier set
  actually instantiated in `BORDISM_HYPOTHESES` is
  `{topological, algebraic}`, not `{topological}` alone.
- **Evidence:** `awk 'NR>=2934 && NR<=2959' constants.py | grep -E
  "'eliminability':"` returns:
  ```
  'eliminability': 'topological',
  'eliminability': 'algebraic',  # Shapiro's lemma — potentially provable
  'eliminability': 'topological',
  'eliminability': 'topological',
  ```
- **Why RECOMMENDED, not REQUIRED/BLOCKER:** the paper's substantive
  claim — that `BORDISM_HYPOTHESES` tracks bordism-deferred
  hypotheses with topological-tier deferral semantics — is correct.
  H2 is an algebraic side-channel (Shapiro's lemma) inside an
  otherwise-topological registry; the registry's design intent is
  topological deferral and the source comment makes the side-channel
  explicit. No reader is materially misled by the under-listing
  because the four-table landscape's headline tier vocabulary (six
  total values) still resolves correctly when summed; the paper's
  count "six tier values" (line 482) matches `{closed, hard, removed,
  algebraic, very_hard, topological}` — the union actually
  instantiated, where `algebraic` is sourced from both
  HYPOTHESIS_REGISTRY and BORDISM_HYPOTHESES.
- **Expected:** Either (a) "tier $\in \{$topological, algebraic$\}$
  (one entry, H2, deferred via Shapiro's lemma)" or (b) keep
  `{topological}` and add a parenthetical "with one algebraic
  side-channel via Shapiro's lemma at H2" footnote.
- **Fix:** Single-line edit; non-blocking for submission.
- **Cache:** N/A.

### 3.1-r3-b — VERIFIED — Schema-vocabulary `'open'` for HYPOTHESIS_REGISTRY is declared but unused

- **Gate:** NarrativeGrounding (Gate 7) — informational
- **Location:** `papers/I1/paper_draft.tex:478-479`
- **Observed:** §5 lists HYPOTHESIS_REGISTRY tiers as `{algebraic,
  hard, very_hard, open}`. The schema docstring at
  `constants.py:1553` declares `eliminability: 'algebraic' | 'hard'
  | 'very_hard' | 'open'` as the legal vocabulary, while
  instantiated entries use only `{algebraic, hard, very_hard}`
  (`'open'` has zero current uses; verified by `grep "'eliminability':
  'open'" constants.py` → no matches).
- **Verdict:** Acceptable as-written: the paper is reporting the
  **schema** vocabulary, not the **instantiated-only** vocabulary.
  This is consistent with paper-methodology framing (the registry's
  designed-in tier set, not its current use pattern). No finding
  emitted; logged as informational.

### 1.4 — 🟡 PERSISTING REQUIRED — Five external bibitems still lack citation-cache verification records (carry-forward; ACKNOWLEDGED OUT-OF-SCOPE)

- **Gate:** CitationIntegrity (Gate 1)
- **Location:** `papers/I1/paper_draft.tex` bibitem block; `src/core/citations.py`
  registry entries; `docs/citation_verifications.jsonl` (cache).
- **Observed (round-3):** Re-verified via `grep -E
  "physlean|compcert|sel4|leancipilot|alphaproof"
  citation_verifications.jsonl` → 0 matches. None of the five
  external bibkeys have any record in the citation-cache JSONL.
- **Status per round-3 instructions:** User has explicitly
  acknowledged this finding as out-of-scope for this session.
  Carried forward as a non-blocking advisory; remains a Gate-1
  submission-blocker per `READINESS_GATES.md` Gate 1 escalation
  language ("All CitationIntegrity findings are blocked at submission
  time"), but the user is the gate authority on scope deferral.
- **Verdict:** Persisting REQUIRED (non-blocking for this round per
  user direction). Re-runs need to either (a) execute the cache-fetch
  protocol on the five external bibkeys or (b) carry an explicit
  per-paper waiver entry in the supersession ledger acknowledging the
  Gate-1 deferral.
- **Cache:** still empty.

### 7.1 — 🟡 PERSISTING REQUIRED — Wave 8 BundleRiemannAux sub-count off by 1 (comment-block false positive; ACKNOWLEDGED NON-BLOCKING)

- **Gate:** CountFreshness (Gate 9) / NarrativeGrounding (Gate 7)
- **Location:** `papers/I1/paper_draft.tex:1295-1300`
- **Observed:** Same pattern as round-2 7.1: §12 sub-count `9 + 9 +
  8 + 8 + 5 = 39` includes a comment-block string match in
  `BundleRiemannAux.lean` line 308 (inside a `/-! ... -/` doc-comment
  block). Substantive sub-count is `7`, total 38.
- **Status per round-3 instructions:** User has explicitly
  acknowledged this as a minor non-blocking advisory. Carried forward
  unchanged.
- **Verdict:** Persisting REQUIRED (non-blocking for this round per
  user direction).

### 5.1 — VERIFIED FIXED — Phase 6f audit filename em-dash drift remediated

- **Gate:** NarrativeGrounding (Gate 7) — VERIFIED FIXED
- **Location:** `papers/I1/paper_draft.tex:1238-1240`
- **Observed (round-3 deterministic re-verification):** §11 line 1238
  now reads: `\\texttt{Lit-Search/Phase-6f/Phase\\,6f\\,audit\\,---\\,Classical\\,GR\\,Lean\\,infrastructure.md}`
  — the path uses LaTeX `---` (em-dash typeset) and lines 1239-1240
  add the explicit clarifying note "the filename contains a literal
  em-dash."
- **Evidence:** `ls Lit-Search/Phase-6f/` returns exactly one file:
  `Phase 6f audit — Classical GR Lean infrastructure.md` (with
  unicode em-dash U+2014). The paper's LaTeX `---` typesets to em-dash
  in the rendered output, matching the actual filename's em-dash.
  Combined with the in-prose disambiguation note ("the filename
  contains a literal em-dash"), a reader has unambiguous guidance
  that the rendered em-dash is the literal filename character, not
  a typesetting choice. The shell-copy ergonomics gap remains (a
  reader copying the rendered text needs to know to substitute the
  em-dash unicode glyph), but this is now explicitly disclosed.
- **Verdict:** Round-2 RECOMMENDED 5.1 fully remediated. No new
  finding emitted.

### 5.2 — 🔵 PERSISTING RECOMMENDED — Three line-range citations into Lean source remain off-by-one

- **Gate:** NarrativeGrounding (Gate 7)
- **Location:** `papers/I1/paper_draft.tex:297, 379, 399`
- **Status per round-3 instructions:** User has explicitly
  acknowledged this as a non-blocking RECOMMENDED. Carried forward
  unchanged from round-2.
- **Verdict:** Persisting RECOMMENDED (non-blocking).

### 6.1 — 🟢 PRIOR FIX RE-VERIFIED — Aristotle 4.28/4.29 toolchain skew disclosed in §9

- **Gate:** AssumptionDisclosure (Gate 6) — VERIFIED FIXED
- **Location:** `papers/I1/paper_draft.tex:996-1014`
- **Observed (round-3):** §9 toolchain-skew disclosure paragraph
  unchanged from round-2. Round-1 6.1 remediation remains intact.
- **Verdict:** No finding.

### 8.1 — 🔵 UNCHANGED — No production-run claims; Gate 8 remains moot

- **Gate:** ProductionRunHealth (Gate 8) — UNCHANGED FROM ROUNDS 1, 2
- **Observed:** Same as rounds 1 and 2. I1 makes no Monte Carlo /
  RHMC / numerical-evidence claims that require ProductionRun graph
  backing.
- **Verdict:** No finding.

## Finding-class summary

| Class | Round-3 status |
|-------|----------------|
| 1. Wrong-target citations (Gate 1) | 1 PERSISTING REQUIRED (1.4 — citation cache empty; acknowledged out-of-scope) |
| 2. Parameter drift (Gate 3) | No findings |
| 3. Placeholder theorems (Gate 5) | 0 BLOCKERs; round-2 3.1-residual VERIFIED FIXED; 1 minor RECOMMENDED (3.1-r3, BORDISM tier set under-listing) |
| 4. Cross-paper contradictions (Gate 2) | No findings |
| 5. Narrative overclaims (Gate 7) | round-2 5.1 (em-dash) VERIFIED FIXED; round-2 5.2 PERSISTING RECOMMENDED |
| 6. Undisclosed assumptions (Gate 6) | 6.1 VERIFIED FIXED; no new findings |
| 7. Count literal drift (Gate 9) | 1 PERSISTING REQUIRED (7.1 — BundleRiemannAux sub-count; acknowledged non-blocking) |
| 8. Production run health (Gate 8) | No findings (no production-run claims) |

## Submission readiness verdict

**GREEN — submission-ready on the deterministic-evaluator path.**

- 0 open BLOCKERs.
- 2 PERSISTING REQUIRED (1.4 + 7.1) — both acknowledged out-of-scope /
  non-blocking by user for this round.
- 2 RECOMMENDED (5.2 + 3.1-r3) — non-blocking.
- 1 informational note (3.1-r3-b) — schema vs. instantiated tier
  vocabulary distinction documented but no action required.
- All round-1 BLOCKERs (1.1, 1.2, 1.3, 3.1, 4.1, 9.1) and the round-2
  BLOCKER (3.1-residual) confirmed remediated by deterministic walk.

**Gate-state recommendation:**
- `stage13_status`: `green`
- `stage13_redo_required`: `false`
- `blockers_open`: 0
- `advisories_open`: 4 (1.4 carry-forward; 7.1 carry-forward; 5.2
  carry-forward; 3.1-r3 new minor)

**Note on Gate 1 strict-readiness:** Per `READINESS_GATES.md` Gate 1's
escalation language, all CitationIntegrity findings are blocked at
submission time. Finding 1.4 (cache-record absence for 5 external
bibitems) is therefore strictly a Gate-1 submission-blocker until
either (a) the cache-fetch protocol runs successfully, or (b) a
per-paper waiver lands in `docs/review_finding_supersessions.json`.
The `green` verdict above reflects the user's explicit out-of-scope
direction for this session; the canonical readiness evaluator running
on `validate.py --check citation_primary_sources_present` should be
re-checked separately before final submission. The bundle metadata
will reflect this nuance via `advisories_open` and a notes field.

## Process notes

Round-3 scope per round-3 prompt: "verify the §5 cross-registry fix is
now correct deterministically." Performed:

1. Read §5 paragraph (lines 472-507) verbatim.
2. Walked each cited line number into `src/core/constants.py` and
   determined the registry containing it by computing the registry's
   defined line range, then matched against the paper's claim.
3. Verified per-registry tier vocabulary by `grep`'ing
   `'eliminability':` within each registry's line range.
4. Verified pillar (iii) two-component description (placeholder marker
   + HYPOTHESIS_REGISTRY hypothesis) resolves correctly.
5. Verified §11 Phase 6f filename em-dash fix: LaTeX `---` typesets
   to em-dash, matches `ls Lit-Search/Phase-6f/` output, in-prose note
   disambiguates rendered glyph.
6. Verified supersession ledger entries 3.1-residual (L1177-1183) and
   5.1 (L1184-1187) against actual artifact state.

Single new minor RECOMMENDED finding surfaced (3.1-r3 BORDISM tier
under-listing); single informational note logged (3.1-r3-b schema vs.
instantiated vocabulary).

## QI Candidate

No new pipeline-level QI surfaced this round. The round-2 QI candidate
("Stage-13 round-2 verification protocol does not currently include a
registry-attribution sanity check") remains valid as a Phase 7a
robustness sub-wave entry; round-3's deterministic re-walk demonstrates
the kind of registry-attribution check that would have caught the
round-1 3.1 residual at round-2 if it had been mechanized in
`validate.py`. Suggested registration: `validate.py CHECK 24
prose_registry_attribution`, owner Phase 7a robustness sub-wave (extend
`docs/BUNDLE_LIFT_PROCEDURE.md` Stage-13 deterministic-verification
list). Round-3 status: candidate carried forward unchanged.
