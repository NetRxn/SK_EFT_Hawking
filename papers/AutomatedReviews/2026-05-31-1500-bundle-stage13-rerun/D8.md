---
paper: D8
bundle_target: D8
reviewer: adversarial-reviewer
model: claude-opus-4-8
review_date: 2026-05-31T15:00:00Z
readiness_gates_version: 1
prior_review: papers/AutomatedReviews/2026-05-31-bundle-stage13/D8.md
---

# Adversarial Review (RE-RUN, post-remediation) — D8 (Kernel-Verified Universal Quantum Gate Compilation)

## Summary

Findings: **0 BLOCKER, 0 REQUIRED, 2 RECOMMENDED.** Both prior REQUIRED
findings are **resolved**: 5.1 (prior-art positioning) — §1 now cites VOQC/SQIR
(`Hietala2020VOQC`) and the verified-QC survey (`Lewis2021VerifQC`), draws the
exact approximation-vs-exact-optimization distinction, and abstract line 27 is
softened to "what is, to our knowledge, the first…"; 5.2 (first-claim ledger) —
`papers/D8/first_claims.md` exists and documents all four first-claims with
prior-art search evidence and HOLDS verdicts. Both new citations were
**fresh-fetched this run and match exactly** (1912.02250 = VOQC Hietala et al.;
2110.01320 = Lewis–Soudjani–Zuliani survey, **not** Qbricks — the prior pass's
mis-ID is correctly corrected in both the registry note and the cached primary
source). The §1 prior-art paragraph makes **no inaccurate claim** about VOQC or
the survey. The §6 dyadic-vs-rational rewording now matches the `IsRatℂ`
theorem type (6.2 resolved); the §3 SU(d) conditional-substrate disclosure is
present (6.1 resolved). Module-attribution fix verified:
`cliffordCCZLiteral_dense` is declared in `CliffordCCZSU8Density.lean:130`.
**No new BLOCKER or REQUIRED findings appeared.** The two remaining RECOMMENDED
findings are cosmetic (registry verification-boolean hygiene; bibkey year-suffix
artifacts) and carry over from the prior pass. **D8 is now submission-ready
(GREEN): zero BLOCKER, zero open REQUIRED.**

## Findings

### 1.1 — (no finding) — Two new citations fresh-fetched, both `match`

- **Gate:** CitationIntegrity (Gate 1) — **PASS, no finding.** Recorded for audit trail.
- `Hietala2020VOQC` (arXiv:1912.02250): fetched title "A Verified Optimizer for
  Quantum Circuits", authors Hietala, Rand, Hung, Wu, Hicks — **exact match** to
  the registry entry (`citations.py:6178`) and bib (`bibliography.bib:96`). VOQC/SQIR
  Coq, POPL 2021, DOI 10.1145/3434318. Verdict `match`.
- `Lewis2021VerifQC` (arXiv:2110.01320): fetched title "Formal Verification of
  Quantum Programs: Theory, Tools and Challenges", authors Lewis, Soudjani,
  Zuliani — **exact match** to registry (`citations.py:6195`) and bib
  (`bibliography.bib:107`). Confirmed to be a **survey**, NOT the Qbricks tool
  paper. The prior reviewer's "Qbricks" mis-identification of this ID is
  correctly recorded as such in both the registry `notes` field and the cached
  primary source (`Lewis2021VerifQC.abstract.txt`). Verdict `match`.
- Both registry entries carry `primary_source_path` pointing at existing files
  under `Lit-Search/Phase-1-and-Background/primary-sources/` (workspace-relative;
  both present and read this run). Both `used_in: ['papers/D8/paper_draft.tex']`.
- Two verification records appended to `docs/citation_verifications.jsonl` this
  run (verifier `adversarial-reviewer D8 stage13-rerun`), both verdict `match`.

### 5.1 — (RESOLVED) — Prior-art positioning against verified-QC frameworks

- **Gate:** NarrativeGrounding (Gate 7) / FirstClaimVerification (Gate 10) — **RESOLVED.**
- **Prior:** abstract said flatly "We report the first machine-checked
  development … as a theory" with zero prior-art positioning.
- **Now:** abstract line 27 reads "We report **what is, to our knowledge, the
  first** machine-checked development …". A new §1 paragraph (`paper_draft.tex:82–93`)
  positions D8 against VOQC/SQIR (`\cite{Hietala2020VOQC}`, "verifies
  equivalence-preserving circuit optimizations") and the verified-QC landscape
  survey (`\cite{Lewis2021VerifQC}`, "Why3/Qbricks, Isabelle, and others"),
  drawing the distinction explicitly: "approximation versus exact optimization …
  a target unitary can be approached to arbitrary precision with bounded circuit
  length, which is a different question from verifying that a given exact circuit
  is correctly optimized."
- **Attribution accuracy (class-5 check on the new text):** the paragraph's
  characterization of VOQC ("equivalence-preserving circuit optimizations") is
  accurate against the fetched abstract; the survey is correctly characterized as
  covering "Why3/Qbricks, Isabelle, and others" — accurate, and it does NOT
  mis-attribute the survey as Qbricks itself. The "none develops the quantitative
  approximation theory" claim is defensible and consistent with the prior pass's
  independent SQIR-repo inspection. No inaccurate attribution introduced.
- **Verdict:** REQUIRED 5.1 genuinely remediated; no overclaim remains.

### 5.2 — (RESOLVED) — First-claim ledger exists

- **Gate:** FirstClaimVerification (Gate 10) — **RESOLVED.**
- `papers/D8/first_claims.md` exists and documents all four first-claims
  (theory-not-algorithm; SU(d) quantitative SK; unconditional Toffoli lower
  bound; T-free CCZ-essential SU(8) density), each with its draft location, the
  prior-art search performed (VOQC/SQIR; Lewis survey; Mathlib; Mukhopadhyay
  source), the negative result, and a HOLDS verdict. Dated 2026-05-31, attributed
  to the Stage-13 search. Satisfies the Gate-10 procedural obligation as a
  committed per-bundle ledger.
- **Verdict:** REQUIRED 5.2 genuinely remediated.

### 6.1 — (RESOLVED) — SU(d) conditional-substrate disclosure

- **Gate:** AssumptionDisclosure (Gate 6) — **RESOLVED to a non-finding.**
- §3 (`paper_draft.tex:195–198`) now states the generic SU(d) headline "is
  parametrized over the alphabet's density witness and a word-length hypothesis;
  the per-alphabet instantiations of Sec.~\ref{sec:alphabets} discharge these,
  and their headlines are unconditional." The conditional substrate vs.
  unconditional instances distinction is disclosed at the point of the headline.
  Matches the theorem signature (`IsDenseInSUd_gs gs` + `WordLengthFreeGroupLike`
  hypotheses). No undisclosed load-bearing assumption.

### 6.2 — (RESOLVED) — Dyadic vs rational prose now matches theorem type

- **Gate:** AssumptionDisclosure (Gate 6) — **RESOLVED to a non-finding.**
- §6 (`paper_draft.tex:349–352`) now reads: entries "are rational (Lemma~3.10,
  `channelRep\_interp\_isRat`, which kernel-verifies rationality of the entries;
  the stronger dyadic $\mathbb Z[1/2]$ form of Mukhopadhyay's Lemma~3.10 follows
  from the half-integer CCZ row structure of Theorem~3.8)."
- Verified the Lean theorem type: `channelRep_interp_isRat` at
  `MukhopadhyayToffoliUnconditional.lean:117` asserts `IsRatℂ (channelRep …)`
  (rationality). The prose now (a) attributes only rationality to the Lean
  theorem and (b) attributes the stronger dyadic ℤ[1/2] form to Mukhopadhyay's
  paper, not to the Lean proof. Prose now matches the theorem type. No
  correctness gap; the unconditional Toffoli bound does not depend on the
  stronger dyadic statement.

### 1.2 — 🔵 RECOMMENDED — registry verification-boolean hygiene (carryover)

- **Gate:** CitationIntegrity (Gate 1)
- **Location:** `src/core/citations.py` D8 block, incl. the two new entries
  (`:6178`, `:6195`).
- **Observed:** All D8 entries (including the two new ones) carry
  `doi_verified: None`; verification is recorded only in free-text `notes`. The
  two new DOI-bearing/arXiv-bearing entries are web-verified (this run) but the
  boolean fields the Gate-1 evaluator keys off are not populated.
- **Expected:** For mechanical Gate-1 pass, set `doi_verified`/verification
  booleans where a verification date is recorded in `notes`. Advisory at draft
  stage; same systemic issue flagged in the prior pass (its 1.2) and its QI
  candidate. Not a regression.
- **Fix:** registry-hygiene backfill pass across the D8 block. Low priority.
- **Cache:** two fresh-fetch records appended this run (both `match`).

### 1.3 — 🔵 RECOMMENDED — bibkey year-suffix artifacts (carryover/extended)

- **Gate:** CitationIntegrity (Gate 1)
- **Location:** `bibliography.bib` / registry — `Hietala2020VOQC` (key says 2020;
  record + bib `year = 2021`, POPL 2021), plus the prior pass's `AharonovArad2017`
  (key 2017; actual 2011).
- **Observed:** Bibkey name strings encode a year that does not match the record's
  `year` field. The bib records themselves are correct and resolve to the right
  primary sources (the bibitem→primary correspondence holds), so these are
  cosmetic key-name artifacts, not wrong-target citations. Not a Gate-1 blocker.
- **Fix:** optionally rename keys for clarity (`Hietala2021VOQC`,
  `AharonovArad2011`) across draft+registry+bib. Low priority.
- **Cache:** Hietala fresh-fetched this run, verdict `match` (record correct).

### 3 / 4 / 7 / 8 — (no findings — prior-pass substantive checks still valid)

- **LeanProofSubstance (Gate 5):** the .tex theorem references are unchanged from
  the prior pass except the §5 module-attribution fix
  (`cliffordCCZLiteral_dense` → `CliffordCCZSU8Density.lean`). **Verified:** the
  declaration `theorem cliffordCCZLiteral_dense : IsDenseInSUd_gs
  cliffordCCZLiteralGeneratingSetSU8` exists at `CliffordCCZSU8Density.lean:130`
  (the only module declaring it). Attribution now correct. The prior pass's
  substantive analysis of `channelSde2_le_toffoliCost`, `cliffordCCZLiteral_dense`,
  and `cliffordOnly_not_dense` (all substantive, hC/hCCZ genuinely discharged,
  kernel-pure, 0 project-local axioms) stands.
- **CrossPaperConsistency (Gate 2):** the two new bibkeys
  (`Hietala2020VOQC`, `Lewis2021VerifQC`) are used **only** in D8 (grep across all
  `papers/*/bibliography.bib` + `paper_draft.tex`) — no cross-paper bibkey/ID
  collision introduced. No new cross-paper contradiction.
- **CountFreshness (Gate 9) / ProductionRunHealth (Gate 8):** unchanged —
  sourceless (D.4) theory bundle with no count-literal macros and no
  ProductionRun-backed numerical-evidence claims. Light, as scoped.

## Anchor-list specific verifications (all still PASS)

- **SU(8) duality honesty:** Route A (T-based, CCZ over-complete, "not load-bearing"
  flagged at `:299`) vs Route B (T-free, CCZ-essential) kept distinct;
  CCZ-essentiality attributed only to 6z literal route, backed by
  `cliffordOnly_not_dense`. PASS.
- **Unconditional vs parametric Toffoli bound:** stated unconditional
  (`channelSde2_le_toffoliCost`), parametric→discharged transition named (`:358–361`),
  Conjecture 4.8 explicitly OUT (`:369–371`). PASS.
- **Length-exponent honesty:** `log5/log(3/2)≈3.97` with explicit rejection of
  `log5/log2≈2.32` (`:241–244`). PASS.
- **Public/private seam:** runtime compiler, vendor tuning, certificate formats,
  length-optimality kept OUT (`:386–404`); no private-repo/commercial reference
  (grep clean). PASS.
- **No project-local axioms:** kernel-pure headline set unchanged. PASS.

## QI Candidate

Carryover (unchanged from prior pass; not introduced by this remediation):
the D8 registry block (now including the two new entries) records citation
verification only in free-text `notes` rather than `arxiv_verified`/`doi_verified`
booleans. A registry-hygiene pass backfilling the booleans wherever `notes`
records a verification date would let the Gate-1 evaluator pass these
mechanically. Likely affects every recently-added registry block.
