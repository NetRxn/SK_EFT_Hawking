---
paper: D10
reviewer: adversarial-reviewer
model: claude-opus-4-8
review_date: 2026-07-01T02:46:57Z
readiness_gates_version: 1
---

# Adversarial Review — D10 (Kernel-Verified Foundations of Computational Quantum Chemistry and Open-System Dynamics)

## Summary

Fresh-context Stage-13 review focused on the just-completed molecular-Hamiltonian discharge edit (hkin, hrel, hpot, hreal presented as discharged rather than disclosed). Zero BLOCKER and zero MAJOR findings. Every "unconditional"/"discharged" claim in the paper is backed by a real, `theorem`-form, sorry-free, kernel-pure Lean declaration of exactly the stated shape, independently verified via `#print axioms` (all six headlines = `{propext, Classical.choice, Quot.sound}`). All 34 project-local theorem names cited in the paper resolve to live declarations; the one PhysLib name (`MatrixMap.of_kraus_CP`) is a genuine imported theorem the proof invokes. All 17 bibkeys are registered; three ITP novelty citations were fresh-fetched and match. Two advisory (RECOMMENDED) items only: a `rfl`-body definitional theorem cited honestly as "is defined" (Gate 5), and registry entries carrying `doi=None` for real-DOI references (Gate 1 hygiene, pre-existing). Paper is submission-ready with respect to the discharge edit; the two advisories do not block.

## Findings

### 3.1 — 🔵 RECOMMENDED — `landauer_conductance_def` is a `rfl` definitional-unfolding theorem flagged as a PlaceholderMarker

- **Gate:** LeanProofSubstance
- **Location:** `papers/D10/paper_draft.tex:159`; Lean `lean/SKEFTHawking/LandauerConductance.lean:113-114`
- **Observed:** `theorem landauer_conductance_def (G₀ : ℝ) (T : ℝ → ℝ) (μ β : ℝ) : landauerConductance G₀ T μ β = G₀ * ∫ E, T E * fermiWindow μ β E := rfl`. The body is `rfl` on a definitional-equality of `landauerConductance`. The graph reports it as a `PlaceholderMarker` node.
- **Evidence:** `#print axioms` not needed — body is literally `rfl`. `build_graph.py` PlaceholderMarker set contains `landauer_conductance_def`.
- **Expected:** Gate 5 blocks `rfl`/`trivial` bodies on **non-trivial** statements. This statement is a definitional unfolding (trivially true by construction), and the paper cites it with honest framing — "The linear-response conductance ... **is defined** (`landauer_conductance_def`)" — not as a substantive verified result. The substantive content around it (`fermiWindow_integral_eq_one`, `landauerConductance_const_transmission'`) is real. Because the statement is definitionally trivial AND the prose labels it a definition, this is not an overclaim.
- **Fix:** Optional. Either (a) leave as-is (framing is accurate), or (b) drop the explicit `\thm{landauer_conductance_def}` citation and refer to the definition `landauerConductance` directly, to keep the cited-theorem set free of `rfl` placeholders. No submission block.
- **Cache:** n/a (Lean-body inspection)

### 1.1 — 🔵 RECOMMENDED — real-DOI references carry `doi=None` in CITATION_REGISTRY (textbook-exempt bucket over-broad)

- **Gate:** CitationIntegrity
- **Location:** `src/core/citations.py` registry entries for `meirWingreen1992`, `hohenbergKohn1964`, `landauer1957`, `buttiker1986`, `levy1979`, `lieb1983`, `leanQI2025`, `coqq2022`, `survey2021`, `isabelleCBO`; `papers/D10/bibliography.bib`
- **Observed:** All 17 D10 bibkeys resolve to registry entries with `doi=None, arxiv=None, primary_source_path=None`, so all fall under the Invariant-11 textbook/pre-DOI exemption. But `bibliography.bib` carries correct real DOIs for several (e.g. Meir–Wingreen `10.1103/PhysRevLett.68.2512`, Hohenberg–Kohn `10.1103/PhysRev.136.B864`, Landauer `10.1147/rd.13.0223`) and correct arXiv IDs for the ITP refs. The registry `doi=None` means the Gate-1 evaluator cannot machine-verify these DOIs even though the `.bib` metadata is correct.
- **Evidence:** `citation_primary_sources_present` PASSES (D10 keys among 151 textbook-exempt, 0 missing-from-registry). `.bib` DOIs/authors/venues independently confirmed correct (Meir–Wingreen PRL 68 2512 1992; HK Phys Rev 136 B864 1964; Landauer IBM JRD 1 223 1957 — all canonical and accurate). Three ITP arXiv IDs fresh-fetched and matched (see 1.2).
- **Expected:** For maximal Gate-1 machine-verifiability, the real-DOI/arXiv references should carry their DOI/arXiv in the registry with `doi_verified: True`, rather than being swept into the textbook exemption. No wrong-target or metadata-mismatch risk was found; the `.bib` a reader sees is correct. This is registry-hygiene debt, pre-existing (not introduced by the discharge edit).
- **Fix:** Populate `doi`/`arxiv` fields for the non-textbook D10 refs in `CITATION_REGISTRY` and set `doi_verified`/`arxiv_verified` after a Crossref/arXiv fetch; keep genuine textbooks (Datta, Kato, Reed–Simon) exempt.
- **Cache:** cache-verified (primary_sources_present) + fresh-fetch for the three ITP refs below

### 1.2 — 🟢 (no finding) — ITP novelty citations fresh-fetched, all match

- **Gate:** CitationIntegrity
- **Location:** `papers/D10/bibliography.bib`
- **Observed / Evidence:** No cache entries existed for the four ITP references, so the three arXiv-bearing ones were fresh-fetched:
  - `survey2021` arXiv:2110.01320 → "Formal Verification of Quantum Programs: Theory, Tools and Challenges", Lewis/Soudjani/Zuliani — MATCH.
  - `leanQI2025` arXiv:2510.08672 → "A Formalization of the Generalized Quantum Stein's Lemma in Lean", Meiburg/Lessa/Soldati — MATCH (corrected author list from a prior round confirmed correct).
  - `coqq2022` arXiv:2207.11350 → "CoqQ: Foundational Verification of Quantum Programs", Zhou/Barthe/Strub/Liu/Ying — MATCH (corrected author list confirmed correct).
  New cache records appended to `docs/citation_verifications.jsonl`. Recorded here so the verification is tracked, not as a defect.
- **Cache:** fresh-fetch

## Verification ledger (skeptical checks that PASSED — recorded for the re-review trail)

- **`molecularHamiltonian_essSelfAdjoint`** (CoulombRelativeBound.lean:2471): `theorem`, args `(N m hm nuclei)` only — NO hkin/hrel/hpot argument; concludes `IsSelfAdjoint (kineticOperator.closure + potentialOperator)`. `#print axioms` = `{propext, Classical.choice, Quot.sound}`. Paper's "fully unconditional" (L184/L197) is supported.
- **`coulomb_isRelBounded`** (CoulombRelativeBound.lean:2447): proves `∃ a b, 0≤a ∧ a<1 ∧ 0≤b ∧ IsRelBounded kineticOperator.closure potentialOperator a b`; no sorry; axioms clean. Built from `coulomb_isRelBounded_core` + `IsRelBounded.extend_to_closure`.
- **`IsRelBounded.extend_to_closure`** (CoulombRelativeBound.lean:2367): exists, substantive Cauchy-sequence graph-limit proof (NOT a placeholder); axioms clean. Paper's "absent from Mathlib and built in-tree" (L194) is plausible and the lemma is real.
- **`kineticOperator_isSelfAdjoint_closure`** (hkin, KineticEssentialSelfAdjoint.lean:456): `theorem`, args `(N m hm nuclei)`; axioms clean.
- **`molecularPotentialOperator_isSymmetric`** (hpot): axioms clean.
- **`traceDist_lindblad_monotone`** (hreal, LindbladCPSemigroup.lean:575): signature `(H hH L {t} ht {ρ σ} hρ hσ)` — NO hreal/Kraus-realization argument; Kraus channel extracted internally via `.exists_kraus`. Axioms clean. Paper's "discharged rather than disclosed ... takes no disclosed CPTP-realization argument" (L262-264) is supported.
- **Superseded `molecularHamiltonian_essSelfAdjoint_of_hpot_hrel`** (KineticEssentialSelfAdjoint.lean:471): still exists, discloses `hpot`+`hrel` args; axioms clean. Paper's supersession note (L195-196) is accurate.
- **Kernel purity:** counts.json = 0 axioms / 0 sorry globally; the three discharge files contain no `sorry`/`axiom`/`native_decide`/`maxHeartbeats` (only in docstrings). All six headlines `#print axioms` = exactly the three standard classical axioms.
- **Gate 6 (assumptions):** variational/Levy–Lieb semibounded-functional inputs remain disclosed (L122-124, L293-294) as tracked `Prop`s / structure fields (`DensityVariational`, `LevyLiebData`), correctly NOT claimed unconditional. `hohenberg_kohn_variational` body is a genuine bundle (`ciInf_le` + non-degeneracy field), honestly flagged load-bearing.
- **Gate 8 (production runs):** the two "simulation" mentions describe the downstream literature, not a D10 run; no numerical-evidence claim is made — nothing to back.
- **Gate 5 name resolution:** all 34 project-local `\thm{}` names resolve to live declarations; `MatrixMap.of_kraus_CP` is a real PhysLib theorem (Physlib/QuantumInfo/.../Unbundled.lean:555) invoked by `lindblad_generator_CP`.
- **Gate 10 first-claims:** all "first ... in any proof assistant" claims hedged "To our knowledge", backed by `prior_art_novelty.md` (research-scout 2026-06-30) + `\cite{survey2021}`, with the required CPTP and abstract-perturbation carve-outs implemented in prose (L83-92); molecular-Coulomb "first" scoped exactly as the novelty doc requires.

## QI Candidate (optional)

Process-level (not D10-specific): the Invariant-11 textbook/pre-DOI exemption is currently absorbing references that DO have resolvable DOIs/arXiv IDs (see 1.1). This weakens Gate-1 machine-verifiability across the bundle set — a reference with a correct DOI in `.bib` but `doi=None` in the registry is verified only by human eyeball, not by the evaluator. Recommend a `validate.py` sub-check that flags registry entries whose paired `.bib` entry carries a `doi=`/`eprint=` field while the registry has `doi=None`/`arxiv=None` and is marked textbook-exempt. This also echoes the prior-round QI candidate (scout-sourced bibitems need author lists fetch-verified before Gate 1).
