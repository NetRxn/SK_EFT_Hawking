---
paper: D4
bundle: D4
reviewer: adversarial-reviewer
model: claude-opus-4-7
review_date: 2026-05-11T17:30:00Z
review_round: re-invocation (verifies fixes from 2026-05-11-1550-bundle-stage13)
prior_review: papers/AutomatedReviews/2026-05-11-1550-bundle-stage13/D4.md
readiness_gates_version: 1
---

# Adversarial Review — D4 (re-invocation)

## Summary

Re-invocation pass verifying closure of the 14 findings from the Wave-5 D4 fix-pass
(5 BLOCKER + 5 REQUIRED + 4 RECOMMENDED). **All 14 prior findings verified
closed**; **no new BLOCKER findings**. One **REQUIRED**-severity registry-vs-bibitem
drift identified (Roehm2026Strategy title says "13-bundle" in the bibitem, registry
says "14-bundle"); this is a pre-existing project-wide drift (also affects D3, F, I3)
that was not surfaced in the Wave-5 review. Paper is submission-ready modulo the one
new REQUIRED finding, which is a copy-edit-level fix.

Verdict: **GREEN with one REQUIRED** (down from 5 BLOCKER / 5 REQUIRED / 4 RECOMMENDED).

## Verification of prior findings

| # | Class | Prior severity | Closure verified | Method |
|---|------|----------------|-------|--------|
| 1.1 | Kitaev2003 stub registry | BLOCKER | YES | `src/core/citations.py:5763-5781` — full title, authors `'Kitaev, A. Yu.'`, arxiv `quant-ph/9707021`, DOI `10.1016/S0003-4916(02)00018-0`, `doi_verified: '2026-05-11'`, `papers/D4/paper_draft.tex` in `used_in`, primary-source PDF cached |
| 1.2 | §7 zero citations | BLOCKER | YES | `paper_draft.tex:512-513` cites `HaydenPreskill2007, PastawskiYoshidaHarlowPreskill2015, AlmheiriMarolfPolchinskiSully2013` at section opening; all three bibitems present (`:818-822, 836-840, 872-877`) and all three registry entries have `papers/D4/paper_draft.tex` in `used_in` |
| 1.3 | §8 zero citations | REQUIRED | YES | `paper_draft.tex:611-613` cites `RyuTakayanagi2006, LewkowyczMaldacena2013, CasiniHuerta2009, HolzheyLarsenWilczek1994`; also at `:622, :632`; all four bibitems present and registered |
| 1.4 | Lickorish citation | RECOMMENDED | YES | `paper_draft.tex:394, 410` cite `Lickorish1997`; bibitem at `:860-864`; registry has DOI `10.1007/978-1-4612-0691-0` |
| 2.1 | D4↔D2 mutual punt on Z(Vec_S3) | BLOCKER | YES | D4 §4 rewritten (`paper_draft.tex:299-358`): explicitly claims both toric-code abelian anchor (consumed by D2) AND `Z(Vec_S3) ≃ Rep(D(S_3))` non-abelian extension (lives natively in D4 via `S3CenterAnyons.lean`). D2 `paper_draft.tex:558-561, 952-953` references to `Roehm2026D4` now have a real target |
| 2.2 | §4 minimal-MTC self-contradiction | BLOCKER | YES | Abstract `:48` and §4.1 `:302-307` consistently identify toric-code `Z(Vec_{Z₂})` as the minimal anchor used by D2; `Z(Vec_S3)` is the non-abelian extension shipping in D4 |
| 2.3 | D4↔D3 §8 RT/CH one-sided | REQUIRED | ACCEPTABLY ADDRESSED | D4 §8.5 (`:666-681`) softened to forward-pointing: "any future D3 absorption of RT/CH content should reference `rt_eq_kaulMajumdar_iff_trivial_reduced_area` as the bridge" — explicitly disclaimed as one-sided. Bundle metadata flags this as a carryover advisory still |
| 5.1 | DoublonGate.lean / berryPhase_eq_pi phantom | BLOCKER | YES | All references to `DoublonGate.lean` and `berryPhase_eq_pi` removed from D4 (`grep -c "DoublonGate\|berryPhase_eq_pi" → 0`). §6 (`:435-500`) now cites only `FermiHubbardDimer.lean` with `darkVec`, `dark_state_in_kernel`, `darkVec_ne_zero`, `darkStateθ_in_kernel`, and the substantive `U_SWAP_singlet_darkVec` sign-flip theorem (`FermiHubbardDimer.lean:1571`). Berry-phase / adiabatic-drag content honestly disclosed as deferred (T13–T15) |
| 5.2 | WRT theorems described as proved | REQUIRED | YES | §5.1 (`:380-385`) and §5.2 (`:401-405`) explicitly say "We *define* `WRTInvariant.wrtS2xS1`/`wrtS3`" and call the companion theorems "definitional sanity-check (`rfl` on the definitional unfolding), not a proof of the Verlinde-formula chain" / "definitional (`rfl`-discharged on the definitional unfolding), encoding the standard Kirby-colour normalisation rather than proving Kirby-move invariance from first principles" |
| 5.3 | Figure-eight "in flight" stale | RECOMMENDED | YES | §3.5 (`:280-292`) now closes with `FigureEightKnot.figure_eight_trace_neg_one` and `FigureEightKnot.figure_eight_normalized` (both exist in `lean/SKEFTHawking/FigureEightKnot.lean:117, 121`) with concrete value `RT(4₁, σ) = -1/√2`. §1 (`:126-128`) also reflects closure |
| 6.1 | H_HorizonBoundaryCondition True placeholders | REQUIRED | YES | §7.1 (`:517-538`) explicitly enumerates: "three substantive (`positivity`: S ≥ 0 for A ≥ 0; `areaLeading`: 0 < κ_area; `secondLaw`: S_horizon monotone) and two explicit-`True`-placeholder fields (`modularInvariant`, intended for (ST)³=S² modular-group action at the abstract level; `anomalyMatch`, intended for Walker–Wang anomaly inflow at the abstract level)." §8.4 (`:648-664`) reiterates. Lean structure (`BHEntropyMicroscopic.lean:373-379`) confirmed to have 5 fields as described |
| 7.1 | 16 first-claim instances | BLOCKER | YES | `grep -niE "to our knowledge\|first machine-checked\|first formally verified\|first formalised\|first formalisation\|first formalization\|first computed\|first concrete\|first SPT\|first symmetry-protected\|first non-trivial\|first in any proof assistant" paper_draft.tex` returns ZERO matches. All 16 instances removed; "First-formalisation register" §9.2 renamed/rewritten as "Program-internal formalisation register" (`:707-720`) |
| 7.2 | 11 "in flight" hedges | REQUIRED | YES | `grep -n "in flight" paper_draft.tex` returns ZERO matches. Deferred work relocated to honest §9 "Open problems" framing (`:722-739`) with explicit Mathlib-upstream dependency or future-work tag |
| 7.3 | "infrastructure-grade infrastructure" tautology | RECOMMENDED | YES | Phrase no longer present; `:88-89` reads "consolidates seven formally verified threads spanning these three pillars into a single substrate account suitable for downstream topological-quantum-computation applications" |
| 7.4 | "second-shipped Tier-1" sequencing claim | RECOMMENDED | YES | Phrase no longer present in `paper_draft.tex`; §9 submission-gate paragraph (`:806-814`) now describes Phase-7 close-out gate, no relative-ordering claim |

## Findings

### 1.1 — 🟡 REQUIRED — Roehm2026Strategy bibitem title "13-bundle" disagrees with registry "14-bundle"

- **Gate:** CitationIntegrity (Gate 1)
- **Location:** `papers/D4/paper_draft.tex:884-887`; `src/core/citations.py:Roehm2026Strategy`
- **Observed:**
  ```
  Bibitem (D4 paper_draft.tex:886): "SK-EFT Hawking 13-bundle publication architecture"
  Registry title:                   "SK-EFT Hawking 14-bundle publication architecture (project paper-strategy frame)"
  PAPER_STRATEGY.md:27 (authoritative): "fourteen publication targets" (1 flagship + 5 Tier-1 deep + 3 Tier-2 PRL + 3 Tier-3 infrastructure + 2 Tier-4 experimental = 14) — explicit: "(Bundle architecture went 13 → 14 in Phase 6n Session 4 with the I3 authorization for 'Verified Stochastic Calculus for Mathlib4')"
  ```
- **Evidence:** Per Gate 1 ("bibitem title matches the registry title — exact or registry's known-aliases list"), a numeric disagreement between bibitem and registry is a mismatch. The same drift exists in `papers/D3/paper_draft.tex:2650`, `papers/F/paper_draft.tex:2055`, and `papers/I3/paper_draft.tex:1269` (verified via `grep -rn "13-bundle publication"`). Project-wide stale bibitem text not caught by Wave-5 fix-pass.
- **Expected:** D4 (and the three companion papers) bibitem text must read "SK-EFT Hawking 14-bundle publication architecture" to match registry and current PAPER_STRATEGY.md.
- **Fix:** Mechanical sed/edit replacing "13-bundle publication architecture" → "14-bundle publication architecture" in `papers/D4/paper_draft.tex:886` (one line). Recompile. Apply to D3, F, I3 bibitems in parallel via Gate-11 FixPropagation.
- **Cache:** N/A (self-reference, in-prep)

## Closure of all 14 Wave-5 findings

All 14 prior findings verified `fixed` against current `paper_draft.tex` and supporting Lean source. Lake build clean per re-invocation context (counts: 5858 thm / 5833 substantive / 322 modules / 0 sorry / 1 axiom). Supersession ledger has 33 D4 entries: 30 `fixed` + 2 `accepted` + 1 `applied` (no `open`).

Specific verifications:

- **Anti-placeholder scan clean.** Zero "in flight", "tracked separately", "provisional name", or "provisional module" occurrences in `paper_draft.tex`.
- **First-claim scan clean.** Zero matches across 11-token regex including "first machine-checked", "first formally verified", "to our knowledge ... the first", etc.
- **Phantom Lean module scan clean.** Zero matches for `DoublonGate\|berryPhase_eq_pi`. All cited theorems in §6 (`darkVec`, `dark_state_in_kernel`, `darkStateθ_in_kernel`, `darkVec_ne_zero`, `U_SWAP_singlet_darkVec` for the sign-flip claim) verified to exist in `FermiHubbardDimer.lean`.
- **Citation bibitem-vs-registry cross-check.** 14 bibitems verified; 13 match cleanly (1 false-positive `ReshetikhinTuraev1991` is `$3$-manifolds` LaTeX vs plaintext, content agrees); 1 real drift (the `Roehm2026Strategy` finding above).
- **Lean theorem existence verification.** 14 of 14 cited Lean theorem names in §7 / §8 / §3.5 / §5 / §6 confirmed present in their respective `.lean` files (`QECHolographyBridge.lean`, `RTCasiniHuertaBounds.lean`, `FermiHubbardDimer.lean`, `FigureEightKnot.lean`, `WRTInvariant.lean`, `S3CenterAnyons.lean`, `BHEntropyMicroscopic.lean`).
- **Cited theorems have substantive proofs.** `code_distance_scaling_matches_anyonic_fusion_iff_fusion_in_admissible_class` proves both an iff and an implication; `recovery_at_scrambling_bound` proves a non-trivial bound; `rt_eq_kaulMajumdar_iff_trivial_reduced_area` proves a real biconditional with `Real.log = 0` extraction; `trivialAbelian_violates_admissibility` derives contradiction from `1 < 1`. WRT theorems honestly disclosed as `rfl`-on-definition in the paper prose (R3 closure).
- **Cross-bundle bridges.** D4↔D2 (Drinfeld centre): D2 `:558-561, :952-953` says non-abelian S3 ships in D4; D4 `:332-347` actually ships `S3CenterAnyons.lean` content with 8-anyon spectrum claim. Bidirectional. D4↔D3 (QEC/§7): D4 explicitly forward-bridges; D3 carries the cross-reference.

## QI Candidate

**QI candidate (re-invocation): "Numeric drift in self-reference titles."** The "13-bundle" → "14-bundle" architecture transition (Phase 6n Session 4) updated `PAPER_STRATEGY.md` and `CITATION_REGISTRY` but did not propagate to the inline `\bibitem{Roehm2026Strategy}` bodies in four papers (D3, D4, F, I3). This is a structural pipeline gap: `validate.py --check citation_integrity` should compare bibitem text against registry `title` field (currently only checks the bibkey is present). Adding a string-similarity check between bibitem and registry title at the Stage-12 gate would have caught this in the same pass that fixed the registry. Companion: any registry `title` change should trigger a `bundle_metadata.json` field `last_self_reference_audit` that the per-paper Stage 13 checks against.

**QI candidate (carry-forward from Wave-5):** Stage 13 still lacks the "section-without-citations" detector that Wave-5 flagged. §7 / §8 are now fixed manually, but the gap persists for any newly drafted section. Phase 7 absorption should prioritize this validator.
