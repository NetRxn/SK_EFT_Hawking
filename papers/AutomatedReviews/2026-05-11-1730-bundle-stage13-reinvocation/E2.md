---
bundle: E2
paper: E2_graphene_dean_kim_lucas_PRL
reviewer: adversarial-reviewer
model: claude-opus-4-7-1m
review_date: 2026-05-11T17:30:00Z
readiness_gates_version: 1
round: reinvocation
verdict: GREEN_NO_NEW_BLOCKERS
blockers_open: 0
findings_total: 4
findings_blocker: 0
findings_required: 0
findings_recommended: 4
---

# Adversarial Review — E2 (Dean-Kim-Lucas Graphene PRL) — re-invocation 2026-05-11-1730

## Summary

Re-invocation pass following the 2026-05-11 Wave-7 Stage-13 fix-pass on
bundle E2 (`papers/E2/paper_draft.tex`, 497 lines, "Detecting Analog
Hawking Radiation in the Bilayer-Graphene Dirac Fluid"). The fix-pass
was scoped to close 11 fixed + 2 accepted findings from the prior
review at `papers/AutomatedReviews/2026-05-11-1550-bundle-stage13/E2.md`
(2 BLOCKER + 5 REQUIRED + 4 RECOMMENDED + 13 total / 1 deferred cache
warm-up + 1 deferred SI{} retrofit).

**Verdict: ALL prior BLOCKERs and REQUIREDs closed cleanly; no new
BLOCKERs introduced.** Verification against every closure category:

1. **Zhao2023 bibitem** — verified replaced at L461-466 with canonical
   8-author form (W.~Zhao, S.~Wang, S.~Chen, Z.~Zhang, K.~Watanabe,
   T.~Taniguchi, A.~Zettl, F.~Wang), actual title "Observation of
   hydrodynamic plasmons and energy waves in graphene", Nature 614:688
   (2023), DOI 10.1038/s41586-022-05619-8 — matches
   `CITATION_REGISTRY['Zhao2023']` at `src/core/citations.py:6579-6595`
   character-for-character. PRIOR BLOCKER 1.1 closed.
2. **Counts macroised** — verified `\input{../../docs/counts.tex}` at
   L8; conclusion at L403-405 uses `\substantivetheorems{}` /
   `\leanmodules{}` / `\axiomcount{}` / `\sorrycount{}` macros; current
   canonical values from auto-generated `docs/counts.tex` are 5833 /
   322 / 1 / 0 (re-validated this round via direct read). PRIOR
   BLOCKER 2.1 closed.
3. **κ-vs-D arithmetic reconciliation** — verified L102 reads κ ≈
   2.0e12/s (was 2.2e12) with explicit hedging "estimated from the
   deep-research-derived nozzle velocity profile rather than the
   order-of-magnitude identification c_s/L ≈ 2.2e12/s". Arithmetic
   check: D = κ·l_ee/c_s = 2.0e12 × 51e-9 / 4.4e5 = 0.2318 ≈ 0.23 ✓.
   T_H = ℏκ/(2π k_B) = 1.055e-34 × 2.0e12 / (2π × 1.381e-23) ≈ 2.43 K
   ≈ 2.4 K ✓. Internally consistent. PRIOR REQUIRED 3.1 closed.
4. **l_mr corrected to 5 µm** — verified L226 reads "canonical
   momentum-relaxation mean-free path l_mr ≈ \SI{5}{\micro\meter}";
   matches `GRAPHENE_PLATFORMS['Dean_bilayer_nozzle']['l_mr_um'] = 5.0`
   at `src/core/constants.py:223`. PRIOR REQUIRED 3.2 closed.
5. **η/s device-transport disclosure** — verified L199-205 contains
   substantive disclosure: "Majumdar et al.'s η/s bound is reported
   for the thermal-regime effective dynamic viscosity in the cleanest
   hBN-encapsulated samples; we adopt it here for the Dean-equivalent
   ultra-clean bilayer-Dirac-fluid regime — a modeling assumption since
   the Dean nozzle device (Geurs2025) is plausibly within the same
   universality class but is not itself the device measured in
   [Majumdar2025]". PRIOR REQUIRED 6.1 closed (per spec wording in
   the prior review).
6. **"First electronic sonic horizon" removed** — verified L66-68 now
   reads "establishes a hydrodynamic-electron sonic horizon" (was
   "created the first electronic sonic horizon"). No "first" / "first
   in any proof assistant" / "first formally verified" residue remains
   in body or abstract. PRIOR RECOMMENDED 7.2 closed.
7. **`hawking_temp_from_surface_gravity` definitional rfl-disclosure**
   — verified L93-98 substantive prose disclosure: "whose name-binding
   to surface gravity is encoded definitionally in
   AcousticMetric.hawking_temp_from_surface_gravity (a rfl-discharge
   on the Basic.hawkingTemp definitional unfolding); the substantive
   universality content lives upstream in the project's surface-gravity
   / Killing-horizon chain (Basic and AcousticMetric.SonicHorizon)".
   E1-pattern alignment confirmed. PRIOR RECOMMENDED 5.1 closed.
8. **`fdt_consistency unfold;ring` scope disclosure** — verified
   L263-272 substantive prose: "Although the underlying Lean theorem
   GrapheneNoiseFormula.fdt_consistency reduces to arithmetic on the
   closed-form expression (unfold; ring after definitional unfolding
   of hawkingNoisePSD), the load-bearing two-route equivalence between
   the Keldysh FDT derivation and the Landauer-Büttiker multichannel
   derivation is established at the paper level by the matching of
   prefactor coefficients and channel content; the Lean artifact
   certifies the algebraic identity of the closed form, not the
   equivalence of the two derivations." PRIOR RECOMMENDED 5.2 closed.
9. **Labels added; \ref → \eqref** — verified L349
   `\label{eq:falsifiable_window}` on the [0.5, 4] boxed equation;
   L399 cites via `\eqref{eq:falsifiable_window}`; L300 uses
   `\eqref{eq:noise_psd}`; no remaining `\ref{}` to equation labels
   (`grep -E "\\\\ref\{eq:"` returns zero matches). PRIOR
   RECOMMENDED 7.4 closed.
10. **Six dangling-bibitem closures** — verified body cites for all
    six previously-dangling bibitems: Unruh1981 (L62), Visser1998 (L63),
    CGL2017a/CGL2017b (L87), Romatschke2010 (L75), Falque2025 (L109).
    Cross-check: bibitem set (17) matches `\cite{}` set (17 unique)
    exactly. PRIOR REQUIRED 1.2 closed.
11. **Self-history annotation removed** — verified the parenthetical
    about the prior wrong-prefactor 2e²/π is no longer in the body;
    `grep -E "earlier version|prior version|wrong prefactor|off by a
    factor"` returns zero matches. PRIOR RECOMMENDED 7.3 closed.

LaTeX recompile clean (paper_draft.pdf regenerated 2026-05-11 12:09;
only standard revtex4-2 cosmetic warnings — no errors, no missing
references). Lake build clean (per task brief: 5858 thm / 5833
substantive / 322 mods / 0 sorry / 1 axiom; validate.py 27/27 PASS).
13 supersession-ledger entries from Wave 7 verified at
`docs/review_finding_supersessions.json` (greps for "E2:1.1" through
"E2:7.4" all resolve). `paper_provenance` check ran clean this round.

**Counts:** 🔴 0 BLOCKER · 🟡 0 REQUIRED · 🔵 4 RECOMMENDED · 4 total
findings.

**Gates affected:** Gate 1 (CitationIntegrity — registry-hygiene
advisories only); Gate 9 (NumericalFreshness — pre-existing accepted
SI{} retrofit deferral, advisory); Gate 7 (NarrativeGrounding —
project-internal-jargon residue advisory).

**Submission-ready:** Bundle close-ready pending re-greening of any
companion-bundle gates that depend on E2; bundle-level Gate-1 +
Gate-2 + Gate-3 + Gate-5 + Gate-6 + Gate-7 + Gate-9 all pass at this
round's evidence. All 4 new findings are RECOMMENDED (cosmetic/process
hygiene); none reopen a readiness gate.

## Findings

### 1.1 — 🔵 RECOMMENDED — `Zhao2023` and `Geurs2025` registry `used_in` lists missing E2 entry

- **Gate:** CitationIntegrity (Gate 1) — registry-hygiene advisory
- **Location:** `src/core/citations.py:5526` (Geurs2025) and `:6591-6592`
  (Zhao2023)
- **Observed:**
  - `CITATION_REGISTRY['Geurs2025']['used_in'] =
    ['papers/paper16_graphene_sk_eft/paper_draft.tex']` —
    E2 missing despite 5 body cites at L67, L109, L370, L401, L447.
  - `CITATION_REGISTRY['Zhao2023']['used_in'] =
    ['papers/paper16_graphene_sk_eft/paper_draft.tex',
    'papers/D1/paper_draft.tex']` — E2 missing despite the
    Wave-7-fixed bibitem at L461-466 + body cite at L78.
- **Evidence:** `grep -n "Zhao2023\|Geurs2025"
  papers/E2/paper_draft.tex` returns body cites at the lines above.
  Cross-reference: the Wave-7 supersession-ledger entry for Zhao2023
  *did* update the bibitem text but left the registry `used_in`
  list untouched. Same for Geurs2025 (pre-existing miss from the
  Phase-7 Session-3 fix).
- **Verdict:** Process miss — the bibitem-text fix landed but the
  registry-side `used_in` list propagation did not. The registry is
  the project's tracker for cross-bundle citation reuse; an incomplete
  `used_in` will silently undercount cross-bundle propagation work
  whenever a future fix needs to enumerate "which bundles cite
  Zhao2023?".
- **Expected:** Append `'papers/E2/paper_draft.tex'` to both `used_in`
  lists. Mechanical edit.
- **Severity:** RECOMMENDED — does not affect rendered paper or any
  Stage-13 gate evaluation; pure pipeline-bookkeeping hygiene.
- **Cache:** N/A.

### 1.2 — 🔵 RECOMMENDED — `Geurs2025` and `Majumdar2025` bibitems retain "et al." form despite registry full author list

- **Gate:** CitationIntegrity (Gate 1) — bibitem-vs-registry consistency
- **Location:** `papers/E2/paper_draft.tex:446-449` (Geurs2025) and
  `:451-455` (Majumdar2025)
- **Observed:**
  - Geurs2025 (E2): `J.~Geurs \emph{et al.},` — registry has full
    13-author list (J.~Geurs, T.~A.~Webb, Y.~Guo, I.~Keren,
    J.~H.~Farrell, J.~Xu, K.~Watanabe, T.~Taniguchi, D.~N.~Basov,
    J.~Hone, A.~Lucas, A.~Pasupathy, C.~R.~Dean).
  - Majumdar2025 (E2): `A.~Majumdar \emph{et al.},` — registry has
    full 9-author list (A.~Majumdar, N.~Chadha, P.~Pal, A.~Gugnani,
    B.~Ghawri, K.~Watanabe, T.~Taniguchi, S.~Mukerjee, A.~Ghosh).
- **Evidence:** The Wave-7 fix-pass adopted the full-8-author form for
  Zhao2023 (per the registry note recording "D1 used_in added
  2026-05-11"). Internal consistency would suggest the same treatment
  for Geurs2025 + Majumdar2025; the registry form is the project's
  canonical author list.
- **Verdict:** PRL house-style allows "et al." for >5-author papers,
  so the bibitem is not technically wrong; but the Zhao2023 fix
  established a precedent that the project prefers the canonical
  full-author form when registry data is available. Either decision
  (universal full-author or universal "et al." for ≥10-author papers)
  is defensible; the current state is inconsistent.
- **Expected:** Either (a) expand Geurs2025 + Majumdar2025 bibitems to
  the full author list (consistent with Zhao2023 Wave-7 form), OR (b)
  collapse Zhao2023 to "W.~Zhao \emph{et al.}" (consistent with
  Geurs2025 + Majumdar2025) and add a registry note acknowledging the
  policy. Option (a) is the lower-cost fix and matches Wave-7
  precedent.
- **Severity:** RECOMMENDED — cosmetic; not gate-blocking. Not a
  reader-impact issue (a reader following the arXiv ID gets the
  correct paper); only an internal style consistency matter.
- **Cache:** N/A.

### 7.1 — 🔵 RECOMMENDED — Project-internal jargon "Phase 5w" leaks into body prose

- **Gate:** NarrativeGrounding (Gate 7) — internal-process residue
- **Location:** `papers/E2/paper_draft.tex:150-151`
- **Observed:** "...applies directly to the (t,x) sector via the
  binding lemma `DiracFluidWKB.toExactWKB`, which is checked in ∼92%
  of the Phase 5w transferred theorems."
- **Evidence:** "Phase 5w" is a project-internal phase tag from the
  pipeline's wave-execution schema (per CLAUDE.md / Phase-5w
  deep-research folder structure). External PRL readers have no
  context for "Phase 5w" and will read the phrase as a typo or as a
  reference to an undefined process. The same ∼92% number can be
  reported without the internal tag.
- **Verdict:** Internal-process language inappropriate for a PRL
  submission. The Wave-7 fix-pass scrubbed "Wave 6" / "Wave 7" / etc.
  references project-wide; this Phase-5w residue was missed.
- **Expected:** Rephrase to drop the "Phase 5w" tag, e.g., "...applies
  directly to the (t,x) sector via the binding lemma
  `DiracFluidWKB.toExactWKB`, which is verified for ∼92% of the
  transferred 1+1D theorems in the Lean chain." (Or, if the precise
  92% number is load-bearing and provenance-traced, anchor it via a
  `\cite{}` to the supporting computation/table.)
- **Severity:** RECOMMENDED — cosmetic + external-reader confusion;
  not gate-blocking, not a load-bearing claim.
- **Cache:** N/A.

### 7.2 — 🔵 RECOMMENDED — Carry-forward: inline `\SI{}` literals (Gate-9 retrofit deferred per task brief)

- **Gate:** NumericalFreshness (Gate 9) — pre-disclosed deferral
- **Location:** Throughout E2; ~20 device-parameter + observable
  literals (lines 29, 33, 39, 40, 47, 69, 102, 105, 154, 157, 173,
  192, 195, 211, 226, 282, 302, 306).
- **Observed:** No `papers/E2/tables.py` exists (verified via `ls
  papers/E2/tables/` returns empty); device parameters + predicted
  observables remain hard-coded `\SI{...}` rather than sourced through
  `\input{tables/<id>.tex}` autogen.
- **Evidence:** The task brief explicitly lists this as an accepted
  deferral: "7.1 SI{} retrofit deferred." The prior review's
  Finding 7.1 was labeled REQUIRED in the original; Wave-7 fix-pass
  scope did not include the retrofit. The supersession ledger
  acknowledges the deferral as accepted-with-note.
- **Verdict:** This finding is **carry-forward, not new**. Severity
  downgraded from REQUIRED to RECOMMENDED for this re-invocation per
  the accepted-deferral status — submission-time retrofit obligation
  remains, but does not block close at this round.
- **Expected:** Future submission-readiness pass: build
  `papers/E2/tables.py` spec for the Dean device-parameter table +
  the predicted-observables table; replace inline `\SI{}` literals
  with `\input{tables/dean_device_params.tex}` /
  `\input{tables/predicted_observables.tex}`; tag any remaining prose
  literals via `\pipelinevalue{}` macros.
- **Severity:** RECOMMENDED — pre-disclosed accepted deferral;
  surfaces here only for the re-invocation audit trail.
- **Cache:** N/A.

## QI Candidates

**QI-1 (registry `used_in` propagation hook on bibitem-text fixes).**
Finding 1.1 is the second instance of an `used_in` list not updating
when a bibitem-text fix lands (Wave-7 fixed Zhao2023's body text in
E2 but missed the registry-side `used_in` propagation; Geurs2025
shows the same pre-existing miss from a prior round). The fix is
mechanical but easy to miss. **Proposal:** add a
`validate.py --check bibitem_used_in_coherence` that, for every paper
`P` and every `\bibitem{key}` block in `P/paper_draft.tex`, asserts
`P/paper_draft.tex ∈ CITATION_REGISTRY[key]['used_in']`. FAIL severity
appropriate. Catch rate: every E2-class drift, in <1s.

**QI-2 (full-author-list vs `et al.` consistency check across
bibitems within a single paper).** Finding 1.2 is a Wave-7 precedent
mismatch — Zhao2023 was expanded to full-author form but Geurs2025
and Majumdar2025 retained "et al." despite all three having full
registry entries. **Proposal:** add a per-paper
`validate.py --check bibitem_author_form_consistency` that flags
papers where some bibitems use the full registry author list and
others use "et al." within the same `\begin{thebibliography}` block.
WARN severity. Helps the bundle-lift author land a consistent style
in one pass.

**QI-3 (internal-phase-tag residue scan).** Finding 7.1 ("Phase 5w
transferred theorems") is the same project-internal-jargon residue
class flagged in past reviews on other bundles ("Wave 6", "sub-wave
7b.4", etc.). The Wave-7 fix-pass scrubbed most of these but missed
"Phase 5w". **Proposal:** extend `validate.py --check
paper_internal_jargon` (if it exists; otherwise add it) to scan paper
body prose for `Phase \d+[a-z]*`, `Wave \d+[a-z]*\.?\d*`, `sub-wave
\d+`, and `[Ss]ession \d+` patterns outside `%` comment lines. WARN
severity at draft; FAIL at submission.
