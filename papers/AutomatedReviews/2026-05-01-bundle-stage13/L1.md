---
paper: L1
bundle: L1
bundle_target: L1
tier: 2
reviewer: adversarial-reviewer
model: claude-opus-4-7
review_date: 2026-05-01T18:30:00Z
readiness_gates_version: 1
---

# Adversarial Review — L1 (GW170817 / vestigial-graviton PRL splash)

## Summary

Eight findings: **6 BLOCKER, 2 REQUIRED, 0 RECOMMENDED**. Gates affected:
CitationIntegrity (3 BLOCKERs), LeanProofSubstance (2 BLOCKERs),
NarrativeGrounding (1 BLOCKER), CountFreshness (1 REQUIRED),
ParameterProvenance (1 REQUIRED). The bundle is **NOT submission-ready**.
Two of the BLOCKERs are particularly severe: (a) the paper cites a Lean
theorem (`second_sound_graviton_not_derived_DOF`) that the Lean source
explicitly documents as REMOVED on 2026-04-25 because it was a
∃-absorption tautology; and (b) the paper misrepresents the third
conjunct of the central tracked-hypothesis bundle
`H_VestigialModeIsGraviton`, claiming a positivity-of-c_GW conjunct that
the actual Lean definition does not contain — the actual Lean P3' is a
quantitative `|Δc/c| < 1/2` sub-half-deviation bound. These are
descriptions of artifacts the bundle does not have, not minor wording
drift. Before re-running Stage 13, all BLOCKERs must be resolved and
the appended-bundle workflow should reverify counts and bibitem hashes.

---

## Findings

### 1.1 — 🔴 BLOCKER — Volovik2024Vestigial bibitem cites wrong page

- **Gate:** CitationIntegrity
- **Location:** `papers/L1/paper_draft.tex:300-302`
- **Observed:** Bibitem text reads `G.~E.~Volovik, JETP Lett.\ \textbf{119}, 564 (2024); DOI: 10.1134/S002136402460006X.` — **page 564**.
- **Evidence:** Crossref API for DOI `10.1134/S002136402460006X` returns:
  - Title: `"Fermionic Quartet and Vestigial Gravity"`
  - Journal: JETP Letters
  - Volume: 119
  - Pages: **330–334**
  - Year: 2024
  Fetched 2026-05-01 via `https://api.crossref.org/works/10.1134/S002136402460006X`.
- **Expected:** `JETP Lett.\ \textbf{119}, 330 (2024)` (or page 330–334).
- **Fix:** Correct bibitem to page **330** (not 564). Also add the actual title `"Fermionic Quartet and Vestigial Gravity"` (currently L1 omits the title entirely; per `BUNDLE_LIFT_PROCEDURE.md` and the project bibitem convention, titles should be present). Update `src/core/citations.py:3047` registry entry: title is currently `"Vestigial gravity from two-step condensation"` which is also wrong; correct registry title to `"Fermionic Quartet and Vestigial Gravity"`. Then refresh `docs/citation_verifications.jsonl` with a fresh `match` record carrying the new `bibitem_hash`. Cascade: `papers/paper25_gravitational_waves/paper_draft.tex:300` carries the same wrong page (564) and the wrong title `"Vestigial gravity from two-step condensation"`.
- **Cache:** fresh-fetch (no prior cache record for this bibkey).

### 1.2 — 🔴 BLOCKER — Volovik2022Counting bibitem cites wrong volume AND wrong page

- **Gate:** CitationIntegrity
- **Location:** `papers/L1/paper_draft.tex:304-306`
- **Observed:** Bibitem text reads `G.~E.~Volovik, J.~Low Temp.\ Phys.\ \textbf{208}, 117 (2022); DOI: 10.1007/s10909-022-02694-z.` — **vol 208, page 117**.
- **Evidence:** Crossref API for DOI `10.1007/s10909-022-02694-z` returns:
  - Title: `"Gravity from Symmetry Breaking Phase Transition"`
  - Journal: Journal of Low Temperature Physics
  - Volume: **207**
  - Pages: **127–137**
  - Year: 2022
  Fetched 2026-05-01 via `https://api.crossref.org/works/10.1007/s10909-022-02694-z`.
- **Expected:** `J.\ Low Temp.\ Phys.\ \textbf{207}, 127 (2022)`.
- **Fix:** Correct vol/page to **207, 127**. Add the actual title `"Gravity from Symmetry Breaking Phase Transition"`. Note `papers/paper25_gravitational_waves/paper_draft.tex:304-306` already has the correct vol/page (207, 127) but a different (also incorrect) title — paper25 says `"Effective gravity, Goldstone bosons and pre-geometric phase"`, the registry says the same. **L1 differs from paper25 on the volume and page**, so this is *also* a Class-4 cross-paper inconsistency: the same bibkey points to different (vol, page) tuples in the two drafts. Update registry `src/core/citations.py:3030` (currently has correct vol/page 207/127 but the wrong title) to the verified Crossref title.
- **Cache:** fresh-fetch (no prior cache record for this bibkey).

### 1.3 — 🔴 BLOCKER — Roehm2026LinearizedEFE self-citation has fabricated title and registry mismatch

- **Gate:** CitationIntegrity
- **Location:** `papers/L1/paper_draft.tex:339-342`
- **Observed:** Bibitem reads:
  ```
  J.~G.~Roehm, ``Linearized emergent Einstein equations from the
  Akama--Diakonov--Wetterich four-fermion theory'',
  \emph{SK--EFT-Hawking project, in preparation}, 2026.
  ```
- **Evidence:**
  1. The actual companion paper exists as `papers/paper23_linearized_efe/paper_draft.tex`. Its `\title{...}` line is:
     `Linearized Einstein Equations from Anderson--Higgs Tetrad Condensation`
     (verified via `grep -m1 "^\\title" papers/paper23_linearized_efe/paper_draft.tex`).
  2. Registry `src/core/citations.py:5785` has `Roehm2026LinearizedEFE` with `'authors': "...Linearized Einstein Equations from Anderson--Higgs Tetrad Condensation..."`, `inprep: False`, `doi: None`, `arxiv: None`, `primary_source_path: None`, `notes: 'Auto-generated stub ... Title not present in source bibitem.'`
  3. The bundle's `bundle_metadata.json` claims `last_stage10_review: green`, but the L1 bibitem invents a title that matches neither the existing draft nor the registry stub.
- **Expected:** Bibitem must cite the companion paper's actual title `Linearized Einstein Equations from Anderson--Higgs Tetrad Condensation`, OR the registry entry must be updated to reflect a deliberate working title and the corresponding paper23 file must be retitled. Additionally, since paper23 has not yet been submitted to a journal, the registry should carry `inprep: True` (currently `False`) so the citation falls under Pipeline Invariant #11 in-prep exemption.
- **Fix:** (a) Replace the L1 bibitem title with `Linearized Einstein Equations from Anderson--Higgs Tetrad Condensation`. (b) Set `src/core/citations.py:5795` `inprep: True` and add `papers/L1/paper_draft.tex` to `used_in`. (c) Cache record with verdict `match` after the bibitem text and registry agree.
- **Cache:** N/A (in-prep companion; no external metadata to fetch).

### 3.1 — 🔴 BLOCKER — `second_sound_graviton_not_derived_DOF` cited as a theorem; theorem does not exist (was deleted as ∃-absorption tautology)

- **Gate:** LeanProofSubstance
- **Location:** `papers/L1/paper_draft.tex:225-231`, also previewed in abstract context line 50.
- **Observed:** The paper writes:
  ```
  We encode this as the existential
  \texttt{second_sound_graviton_not_derived_DOF}: for any
  positive c, there exists a positive χ_vest such that
  c_GW(χ_vest, c) = c — specifically χ_vest = 1 works.
  ```
- **Evidence:**
  - `grep -n "second_sound_graviton_not_derived_DOF" lean/SKEFTHawking/GravitationalWaves.lean` returns three matches (lines 250, 472, 506) — **all in docstrings/comments, none in a `theorem` or `def` declaration**.
  - The Lean source explicitly states (lines 247–261 in `lean/SKEFTHawking/GravitationalWaves.lean`):
    ```
    Strengthening note (2026-04-25, post-Wave-2 audit): the original
    second_sound_graviton_not_derived_DOF theorem was an
    ∀c, ∃χ, c_GW χ c = c statement, trivially discharged by χ=1.
    That is the ∃-absorption anti-pattern from
    feedback_subagent_lean_quality.md ... We replace it with the
    disjointness theorem natural_range_disjoint_from_ligo_window
    ```
  - The paper's quoted statement (`for any positive c, there exists a positive χ such that c_GW(χ,c) = c`) is the EXACT informal description of the deleted ∃-absorption theorem.
  - `grep -c "^theorem second_sound_graviton_not_derived_DOF" lean/SKEFTHawking/GravitationalWaves.lean` returns 0.
- **Expected:** The paper should either (a) cite the replacement theorem `natural_range_disjoint_from_ligo_window` (lean line 284) with its actual disjointness statement, or (b) cite `second_sound_graviton_natural_range_universally_falsified` (lean line 302) as the frame-independent form. Neither has the form the paper claims.
- **Fix:** Rewrite the "Phase 5y H1 caveat as a meta-theorem" paragraph (paper lines 221-232). The replacement theorem is *not* an "any-c-there-exists-χ" statement; it is a **disjointness** statement (the natural range and the GW170817-compatible window are disjoint). The paper's current narrative reverses the actual mathematical content. Use the Lean docstring at `GravitationalWaves.lean:272-283` as the model for the corrected description.
- **Cache:** N/A.

### 3.2 — 🔴 BLOCKER — `H_VestigialModeIsGraviton` definition misrepresented (P3 conjunct invented)

- **Gate:** LeanProofSubstance
- **Location:** `papers/L1/paper_draft.tex:202-219`
- **Observed:** The paper writes:
  ```
  H_VestigialModeIsGraviton(χ_vest)
    ≡ χ_vest > 0
      ∧ LigoSatisfied(Δc/c(χ_vest))
      ∧ ∀c>0, c_GW(χ_vest, c) > 0,

  demanding positivity (P1), GW170817 compatibility (P2), and
  positivity of c_GW for any positive light speed (P3).
  ```
  And lists three falsifiers: `*_fails_at_natural_lower` (P2), `*_fails_at_natural_upper` (P2), `*_fails_at_zero` (P1).
- **Evidence:** The actual Lean definition at `lean/SKEFTHawking/GravitationalWaves.lean:326-329` is:
  ```
  def H_VestigialModeIsGraviton (χ_vest : ℝ) : Prop :=
    0 < χ_vest ∧
    LigoSatisfied (c_GW_deviation χ_vest) ∧
    |c_GW_deviation χ_vest| < 1 / 2
  ```
  The third conjunct is `|c_GW_deviation χ_vest| < 1/2`, NOT `∀c>0, c_GW(χ_vest, c) > 0`. The Lean module's docstring (lines 263-269) explicitly notes the original positivity P3 was REMOVED because it was *redundant given P1*, and replaced with the load-bearing quantitative bound P3' "sub-half-deviation". The Lean source ships **four** falsifiers, not three: the fourth is `H_VestigialModeIsGraviton_fails_at_quarter` (lean line 363) which specifically isolates P3' independence at χ=1/4 (passes P1, but violates the |Δ|<1/2 bound).
- **Expected:** Paper must accurately describe the actual P3' = `|c_GW_deviation χ_vest| < 1/2` and report all four falsifiers, including `_fails_at_quarter`. The current text describes a definition that does not exist in the bundle.
- **Fix:** Rewrite paper lines 202-219:
  - Replace the third conjunct in the displayed equation with `|c_GW_deviation(χ_vest)| < 1/2` (sub-half-deviation).
  - Replace "positivity of c_GW for any positive light speed (P3)" with "sub-half-deviation `|Δc/c| < 1/2` (P3')".
  - Add the fourth falsifier `*_fails_at_quarter` (witnessed at χ=1/4, where Δc/c = -1/2 saturates the bound).
  - Optional but recommended: explain why the Lean P3' is preferable to the paper's invented P3 — the project's discipline (`feedback_post_wave_strengthening_audit.md` P2 bundle-redundancy rule) forbids redundant conjuncts.
- **Cache:** N/A.

### 5.1 — 🔴 BLOCKER — Theorem-count internal contradiction (abstract: 21; reproducibility: 17)

- **Gate:** NarrativeGrounding (also CrossPaperConsistency since paper25 has the same drift)
- **Location:** `papers/L1/paper_draft.tex:47` vs `papers/L1/paper_draft.tex:288-289`
- **Observed:**
  - Abstract line 47: `(\texttt{GravitationalWaves.lean}, 21 theorems, zero \texttt{sorry}, zero new axioms)`
  - Reproducibility line 288-289: `\texttt{GravitationalWaves.lean} (17 theorems, zero \texttt{sorry}, zero new axioms, zero \texttt{maxHeartbeats} overrides)`
- **Evidence:** `grep -c "^theorem " lean/SKEFTHawking/GravitationalWaves.lean` = **21**. The bundle's own `bundle_metadata.json:22` notes (`Phase 7b sub-wave 7b.0 initial lift: paper25 said '17 theorems / 43 pytest cases'; current values are 21 / 49 ... Both L1 and paper25 updated to current counts`). The bundle metadata claims both papers were updated, but the L1 reproducibility section was missed; paper25 still has the same 17-theorems claim at `paper25_gravitational_waves/paper_draft.tex:395`.
- **Expected:** Both abstract and reproducibility sections must report **21 theorems** consistently. Per Pipeline Invariant on inline literals, the number should ideally come from a `\input{counts.tex}` macro or a paper-local count macro (the paper already uses `\input{../../docs/counts.tex}` at line 14, but no specific macro for the GravitationalWaves theorem count is referenced). A `\gravWavesTheorems` macro could be added to `docs/counts.tex` per the existing pattern (`\fhdTotal`, `\bhEntropyTotal`, etc.).
- **Fix:** Replace `(17 theorems, ...)` at line 288 with `(21 theorems, ...)` (or a counts-macro if added). Cascade fix to `papers/paper25_gravitational_waves/paper_draft.tex:395` per `count_literals` validate-check warning.
- **Cache:** N/A.

### 5.2 — 🔴 BLOCKER — Falsifier-count narrative drift (paper claims 3, Lean has 4)

- **Gate:** NarrativeGrounding
- **Location:** `papers/L1/paper_draft.tex:213-219`
- **Observed:** The paper says: `three explicit falsifier theorems prove non-vacuity: *_fails_at_natural_lower (P2), *_fails_at_natural_upper (P2), *_fails_at_zero (P1).`
- **Evidence:** `lean/SKEFTHawking/GravitationalWaves.lean` ships **four** `H_VestigialModeIsGraviton_fails_at_*` theorems: `_natural_lower` (line 340), `_natural_upper` (line 346), `_zero` (line 352), `_quarter` (line 363). The Lean module-summary docstring (lines 486-488) explicitly lists "FOUR explicit falsifiers (1/4 added: passes P1 but fails P3' at exactly |Δ|=1/2, isolating P3' independence)". The fourth falsifier exists specifically to prove P3' independence.
- **Expected:** Report all four falsifiers, OR explain narratively why `_fails_at_quarter` is omitted. Since BLOCKER 3.2 already requires the paper to discuss P3' instead of inventing P3, finding 5.2 is the natural consequence: the P3'-independence falsifier `_fails_at_quarter` *must* appear when P3' is described.
- **Fix:** Add the fourth falsifier to the list at line 218. Treat as a cascading fix from BLOCKER 3.2.
- **Cache:** N/A.

### 7.1 — 🟡 REQUIRED — Inline count literals not macro-bound (CountFreshness)

- **Gate:** CountFreshness
- **Location:** `papers/L1/paper_draft.tex:47` ("21 theorems"), `papers/L1/paper_draft.tex:288` ("17 theorems"), `papers/L1/paper_draft.tex:292` ("49 pytest cases")
- **Observed:** L1 inputs `../../docs/counts.tex` at line 14 but does not consume a per-module count macro; the theorem count and pytest count are written as inline literals.
- **Evidence:** `validate.py --check count_literals` warns paper25 (the source of L1) on this exact pattern: `paper25_gravitational_waves — USES MACROS but 1 count-literal matches: L395 "17 theorems"`. L1 inherits and amplifies the warning by adding two more inline numerics. The validator does not yet scan `papers/L1/`, but the same hygiene applies. The numerics WILL drift again on the next Lean addition.
- **Expected:** Add `\newcommand{\gravWavesTheorems}{21}` and `\newcommand{\gravWavesTests}{49}` to `docs/counts.tex` (per the existing `\fhdTotal`/`\bhEntropyTotal` pattern), have `scripts/update_counts.py` populate them automatically, and replace inline literals in L1 + paper25 with the macros.
- **Fix:** (a) Extend `scripts/update_counts.py` to emit the two new macros. (b) Replace inline literals with `\gravWavesTheorems` and `\gravWavesTests` in L1 and paper25. (c) Re-run `update_counts.py` and re-check.
- **Cache:** N/A.

### 2.1 — 🟡 REQUIRED — `chi_vest_natural_lower/upper` provenance entry not human-verified

- **Gate:** ParameterProvenance
- **Location:** `src/core/provenance.py:2400-2402, 2413-2415`
- **Observed:** Both `GW.CHI_VEST_NATURAL_LOWER` (= 0.1) and `GW.CHI_VEST_NATURAL_UPPER` (= 10.0) entries have `'human_verified_date': None` and `'human_verified_notes': None`.
- **Evidence:** `Read provenance.py:2389-2415`. These two parameters are load-bearing for the entire L1 falsification (the natural range frames both endpoint falsifier theorems). Provenance is `tier: PROJECTED` with `llm_verified_date: 2026-04-25`.
- **Expected:** Per the adversarial-reviewer agent spec, `unverified` (= LLM-verified only, `human_verified_date` missing) is **REQUIRED** severity — paper can stay in draft state but cannot advance to submission. Per `CLAUDE.md` Pipeline Invariant 8: "human verification (via provenance dashboard) unblocks paper submission".
- **Fix:** Run `uv run python scripts/provenance_dashboard.py` and have the user human-verify both entries against Vergeles 2025 RPA bubble integral primary source (`Lit-Search/Phase-5d/primary-sources/Vergeles2025.pdf`). Set `human_verified_date` and `human_verified_notes`.
- **Cache:** N/A.

---

## QI Candidate

**Pattern:** Bundle-lift propagation gap on theorem-name renames after
post-wave strengthening. Phase 6a Wave 2 strengthened
`GravitationalWaves.lean` on 2026-04-25 — replacing
`second_sound_graviton_not_derived_DOF` with
`natural_range_disjoint_from_ligo_window`, and modifying
`H_VestigialModeIsGraviton`'s third conjunct (P3 → P3'). The Lean
artifact was updated correctly; the source paper25 was *not* updated;
the L1 bundle lift on 2026-05-01 inherited the stale prose without
flagging it.

This is a recurrence of memory `feedback_python_lean_refs_drift.md` (P6
cross-module bridge integrity). The pipeline currently has no automated
check that verifies every `\verb|theorem_name|` and `\texttt{theorem_name}`
in a paper draft is a live declaration in the cited Lean module.

**Recommended QI register entry:**
- Add a new validate.py check `paper_lean_refs_live` that greps every
  `\verb|*|` / `\texttt{*}` pattern that *looks like* a Lean theorem
  identifier in `papers/*/paper_draft.tex` and verifies it appears in
  the corresponding Lean module as an actual `theorem`/`def`/`lemma`
  declaration (not just in a comment).
- Cross-link to `audit_paper_lean_refs.py` if it exists; extend it if
  needed.
- This would have caught BLOCKERs 3.1 (`second_sound_graviton_not_derived_DOF`)
  and 3.2 (`H_VestigialModeIsGraviton` P3 misdescription) at lift time
  rather than at Stage 13.

Estimated impact: high — narrative drift after strengthening passes is
the most consequential category of misdescription (it asserts the
existence of artifacts that explicitly do not exist).

---

## Pre-conditions check

- Stage 9 figure review: GREEN (per bundle_metadata.json:14, single
  figure lifted unchanged from paper25 which is already-verified).
- Stage 10 claims review: GREEN (per bundle_metadata.json:15).
- `validate.py --check citation_primary_sources_present`: PASS at
  2026-05-01T18:10:53Z (319 bibkeys, 202 cached / 10 inprep / 107
  textbook-exempt). However, **the verified-cache match is per
  `bibitem_hash`**; L1's bibitems carry shorter, title-omitting strings
  whose hashes differ from paper25's, so a strict re-verification at the
  L1-bundle level is needed regardless of the per-paper cache state.
  The CHECK 21 pass is a *sufficient* condition for Stage-13 invocation,
  but the per-bibitem verification I performed in this review is
  *necessary* — and it surfaced BLOCKERs 1.1, 1.2, 1.3 that the cache
  layer did not catch (because no fresh fetch was triggered for the
  L1-specific bibitem hashes).
- `validate.py --check counts_fresh`: PASS at 2026-05-01T18:10:57Z
  (counts.json reflects 5229 theorems / 243 modules / 0 sorry).
- Citation cache state: 6 records exist for the bibkeys cited by L1
  (Vergeles2025 / Abbott2017GW170817 / Wetterich-related),
  but **none for L1-specific bibitem text**; Volovik2024Vestigial,
  Volovik2022Counting, Roehm2026LinearizedEFE, Akama1978, Diakonov2011,
  VladimirovDiakonov2012, HebeckerWetterich2003 had no L1-anchored
  records. Adversarial-reviewer fresh-fetched the four DOI/arXiv
  resources that returned via Crossref/arXiv (Volovik 2024, Volovik
  2022, Akama 1978, Diakonov 2011, VladimirovDiakonov 2012,
  HebeckerWetterich 2003, Wetterich 2022, Abbott 2017, Vergeles 2025);
  Wetterich2022 + Akama1978 + VladimirovDiakonov2012 + HebeckerWetterich2003 + Diakonov2011 verified `match`, Volovik 2024/2022 verified `wrong_target` per BLOCKERs 1.1/1.2.

Stage 13 invocation was gated-clean per the user-provided pre-condition
statement; the BLOCKERs surfaced here represent *new* findings from
ruthless review rather than known-stale state.
