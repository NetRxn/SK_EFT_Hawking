---
paper: D12
bundle_target: D12
reviewer: adversarial-reviewer
model: claude-opus-5
review_date: 2026-07-31T21:03:26Z
readiness_gates_version: 1
head_commit: 89d10728
round: 11
---

# Adversarial Review — D12

## Summary

2 BLOCKER / 14 REQUIRED / 17 RECOMMENDED (plus one class-8 PASS note). Stages 1–12 are green
(`validate.py` 57/57, 1115 warnings, archived
`validation_20260731T210932Z.json`), so this review is meaningful. **The paper
itself is now clean of blockers**: round-10 6.1 is genuinely closed — §5 names
all six modelling binders of `bolometer_error_floor`, both new ones are
load-bearing (I re-derived the falsifiers rather than trusting the
rename-to-underscore test, which only shows the proof *uses* them), and §1's
replacement is true. Both remaining blockers are in the closure machinery and
both are the same error one level up: the guard is scoped to `status: fixed`, so
a two-key record `{"finding_id": X, "status": "accepted"}` still closes a 🔴
BLOCKER — and `accepted` is the one status Gate 11 discards outright; and
`review_docs_mint_findings`'s content scope requires the same `<digits><dash>`
heading shape the extractor requires, so the six documents on disk that mint zero
are exactly the six the check skips. Six is also not the complete set of
load-bearing capstone binders — `hs` is missing — but that is REQUIRED, not
blocking, and I say why below. D12 is not submission-ready.

## Environment

`uv run python scripts/validate.py` → **57/57, 1115 warnings**, exit 0, archived
to `docs/validation/reports/validation_20260731T210932Z.json`. Stage 13's
precondition is met.

Compile gate (`uv run python scripts/compile_bundle_pdf.py D12`; no `pdflatex`
was run inside `papers/`): `OK pages=10 overfull=0 tex_errors=0
unresolved_refs_in_pdf=0 dropped_class_opts=0`. Judged from the **rendered PDF**,
not the log — I read pages 1–2 as images: title, author, abstract, §I and §II
render correctly, the `\thm{}` macro breaks long Lean names at underscores,
cross-references resolve as live links (§VI, §II, §III, §IV all bound).

Working tree at `89d10728`: 134 dirty tracked paths on arrival (132
`figures/*.png` + `lean/lean_deps.json.hash`), plus one untracked
`docs/dev-loops/proposals/prose-bridged-claims-gate.md`. None is a D12 artifact
and none was produced by me except the two citation-cache appends disclosed in
the compliance note.

## Findings

### 1.1 — 🟡 REQUIRED — `IrwinHilton2005` is still trusted unqualified for the loop-gain convention while the paper itself discloses that source as unread, fifth consecutive round

- **Gate:** CitationIntegrity
- **Location:** `papers/D12/paper_draft.tex:369-370` vs `:440-443`; `Lit-Search/Phase-6E/primary-sources/IrwinHilton2005.abstract.txt`
- **Observed:** `:369-370` — "The loop gain \thm{loopGain} is signed, and \thm{loopGain\_eq\_irwinHilton} proves it equals the instrument-literature convention~\cite{IrwinHilton2005,Irwin1995}", with no qualifier. `:440-443` — "We attach~\cite{IrwinHilton2005} as the standard review in which this layer is presented, but we have not inspected its text: we hold that source only as a resolved DOI record, so the attribution of this specific closed form to it should be read as provisional."
- **Evidence:** `git show 89d10728 -- papers/D12/paper_draft.tex` touches exactly two hunks, `:85-93` (§1) and `:662-674` (§5). Neither cited line moved. The registry note for `IrwinHilton2005` still reads "Source of the loop-gain convention L = P_J*alpha/(G*T) that 6EC proves equivalent to its own definition (`loopGain_eq_irwinHilton`)" — i.e. the project's own record asserts the loop-gain attribution from a source it simultaneously records as unread. `Irwin1995` is the stronger citation and carries the same claim: its `doi.org` redirect slug independently confirms title, volume, issue and page.
- **Expected:** Extend the `:440` provisional qualifier to the loop-gain sentence, or narrow `:370` to `Irwin1995` alone.
- **Fix:** One clause at `:370`, mirrored into `CITATION_REGISTRY['IrwinHilton2005'].notes`.
- **Severity note:** REQUIRED on the basis of `READINESS_GATES.md:44` — "All CitationIntegrity findings are `blocked` at submission time." A Gate-1 finding cannot be advisory at submission.
- **Cache:** cache-verified (`verdict: match`, `is_stale: False`, hash `925b58102c` recomputed this round)
- **Lane:** `prose`

### 1.2 — 🔵 RECOMMENDED — `scripts/citation_cache.stats()` still raises on the cache it summarizes

- **Gate:** CitationIntegrity
- **Location:** `scripts/citation_cache.py` (`is_stale`)
- **Observed:** `from citation_cache import stats; stats()` → `TypeError: can't subtract offset-naive and offset-aware datetimes`. Re-run this round; unchanged from rounds 9 and 10.
- **Evidence:** `is_stale` catches `(KeyError, ValueError)`; the failure is a `TypeError` at `now - verified`, raised by records whose `verified_date` is a bare `"YYYY-MM-DD"` string. No D12 bibkey is among them — I computed `is_stale` for all 14 individually and every one returned `False` — but the aggregate entry point the CLI and dashboard call is dead for every caller.
- **Expected:** `except (KeyError, ValueError, TypeError)`, or normalise naive timestamps to UTC on read.
- **Fix:** One `except` clause.
- **Cache:** n/a

### 1.3 — 🟡 REQUIRED — none of D12's six arXiv-bearing registry entries carries `arxiv_verified`, so Gate 1's documented pass criterion still cannot be met

- **Gate:** CitationIntegrity
- **Location:** `src/core/citations.py`; `docs/READINESS_GATES.md:33,40`
- **Observed:** Gate 1 "Passes iff … Every registry entry has `arxiv_verified == True` AND `doi_verified == True` (where applicable)". All six D12 arXiv entries — `MeiburgLessaSoldati2025`, `coqq2022`, `UnruhCBO2025`, `EchenimMhalla2021`, `RashidHasan2017`, `Degenne2025Kernels` — carry no `arxiv_verified` key at all. I dumped every registry field for all 14 D12 keys this round: `doi_verified` is present on the eight DOI-bearing entries; `arxiv_verified` is present on none.
- **Evidence:** `_eval_citation_integrity` implements a weaker predicate and D12's gate reads `passed — all bibkeys registered`. The two fresh verification records I appended this round land in `docs/citation_verifications.jsonl` and in nothing the gate reads. Two canonical artifacts disagree about what Gate 1 is.
- **Expected:** Either set `arxiv_verified` on the `promote_primary_sources.py` write path and have the Stage-13 cache append feed it, or amend `READINESS_GATES.md` Gate 1 to state the deferral the evaluator implements.
- **Fix:** One writer change or one doc paragraph.
- **Cache:** fresh-fetch (see the note below)
- **Lane:** `infra`

*(All 14 bibitems resolve, all 14 are in `CITATION_REGISTRY`, all 14 are cited, none is uncited, and all 14 hit `docs/citation_verifications.jsonl` at the **recomputed current** `bibitem_hash` with `verdict: match` and `is_stale == False` — I recomputed every hash rather than trusting the key. I then fresh-fetched the two entries carrying the most load that no round had verified for **content** rather than metadata: `arXiv:2510.04070` → "Markov kernels in Mathlib's probability library", Rémy Degenne, v1 5 Oct 2025 (v2 23 Apr 2026) — **match**; its abstract names Kullback–Leibler divergence, which supports `:610`, and names neither `TestingLowerBounds` nor f-divergences, so the p.26 `[LD24]` quotation remains the sole basis for the `LuccioliDegenne2024` metadata, exactly as that bibitem says. `arXiv:2510.08672` → "A Formalization of the Generalized Quantum Stein's Lemma in Lean", Alex Meiburg / Leonardo A. Lessa / Rodolfo R. Soldati, 9 Oct 2025 — **match**; the abstract confirms quantum hypothesis testing but names neither `OptimalHypothesisRate` nor PhysLib, so `:591-592` rests on direct checkout inspection rather than on the citation — which is what the paper claims at `:114-116`. Two records appended under reviewer `adversarial-reviewer-round11`. Unchanged nit, still not raised: `coqq2022` pairs year 2022 (arXiv) with DOI `10.1145/3571222` (POPL'23).)*

---

### 2.1 — 🟡 REQUIRED — `MATHER_1982_GRADIENT_REDUCTION` still has `human_verified_date: None`; the gate is right to keep blocking and I will keep re-filing it

- **Gate:** ParameterProvenance
- **Location:** `src/core/provenance.py`; graph node `paper:D12`
- **Observed:** Re-measured end-to-end rather than read from the diff: `PAPER_DEPENDENCIES['D12']['parameters'] == ['MATHER_1982_GRADIENT_REDUCTION']`; the live graph carries `paper:D12 --DEPENDS_ON--> param:MATHER_1982_GRADIENT_REDUCTION` (`D12 OUT: {CLAIMS: 1, DEPENDS_ON: 1, HAS_FIGURE: 3, CITES_SOURCE: 14}`); Gate 3 reads `blocked — 1 parameters lack human_verified_date`; `value: 0.3`, `tier: LITERATURE`, `llm_verified_date: '2026-07-31'`, `human_verified_date: None`.
- **Evidence:** This is an outstanding **operator** action, not an author defect, and a gate that reports an outstanding operator action as `blocked` is a correct gate (`READINESS_GATES.md:69`; `CLAUDE.md` invariant 8 — "LLM verification unblocks computation; human verification unblocks paper submission"). What must not happen is an auto-verify path, a `tier == 'LITERATURE'` exemption, or a reviewer-set date. You said you will not forge it; that is right, and the correct consequence is that this finding stays open.
- **Expected:** One dashboard sign-off against `Lit-Search/Phase-6E/primary-sources/Mather1982.abstract.txt`.
- **Fix:** Operator action.
- **Cache:** n/a
- **Lane:** `prose`

### 2.2 — 🟡 REQUIRED — restated precisely as requested: Gate 4 evaluates a hand-curated 4 of D12's 27 formulas, and the 23 it excludes contain the one that flips the gate

- **Gate:** ComputationCorrectness
- **Location:** `scripts/readiness_gates.py:257-300`; `src/core/provenance.py` `PAPER_DEPENDENCIES['D12']['formulas']`; `src/core/formulas.py` (D12 region); `tests/test_bundle_formulas_d11_d12.py`
- **Observed:** Gate 4 reads `passed — all 4 grounded formulas have substantive tests`, evaluated over `['phonon_psd_gradient_factor', 'phonon_psd_gamma', 'f_link_bath_referred', 'assignment_fidelity']`.
- **Evidence:** The precise statement you asked for, re-measured from the live graph this round, not inherited:
  1. **Why the list is the whole gate.** `_eval_computation_correctness` derives its formula set from `CLAIMS→GROUNDED_IN` plus `paper.meta['formulas']`. `paper:D12` has **zero `GROUNDED_IN` edges** (its only outgoing types are `CLAIMS`, `DEPENDS_ON`, `HAS_FIGURE`, `CITES_SOURCE`). So the union degenerates to the hand-written 4-element list, and the list is authored by the same party the gate audits.
  2. **The true population.** The D12 region of `formulas.py` (`poisson_avg_error_floor` … `bloch_siegert_scale`) contains **27** public functions. Measured coverage across all 27 from the live `VERIFIES` edges:
     ```
     strong (golden / identity / roundtrip) : 26
     bounds-or-unknown only                 :  1   gaussian_tail_birnbaum_lower  ['bounds']
     no coverage at all                     :  0
     ```
  3. **Why that one matters.** `gaussian_tail_birnbaum_lower` is the Birnbaum–Feller lower bound the paper calls its own contribution — `:601-604`, "has no Mathlib counterpart, and it is what makes the ceilings of Section~\ref{sec:control} bite at a finite operating point rather than only at zero budget" — and every one of the `76\,\%`, `95\,\%`, `50\,\%`, `80\,\%` figures at `:568-571` is a ratio computed from it. Its only assertion is an inequality against the true tail. A constant that made the bound *smaller* would still pass. That is verbatim the failure mode `READINESS_GATES.md:80` says Gate 4 exists to catch.
  4. **The consequence, stated as a prediction.** Under Gate 4's own blocking rule — "a grounded formula whose VERIFIES edges are all `test_kind ∈ {bounds, unknown}`" — extending `PAPER_DEPENDENCIES['D12']['formulas']` to the true 27 flips D12's Gate 4 from `passed` to `blocked`, today, with no other change. The gate currently passes because the list is short, not because the code is covered.
- **Expected:** Derive Gate 4's formula set from the paper (the `Sentence`/`ProseClaim` layer already parses the draft) rather than from an author-curated list; or, cheaper and sufficient now, extend the list to all 27 **and** pin a golden value for `gaussian_tail_birnbaum_lower` — `φ(1)·1/2 = 0.12098536225957168` and `φ(2)·2/5 = 0.021596386605275228`, both of which I recomputed this round and both of which are already implicit in the paper's own 76 %/95 % sentence.
- **Fix:** One list extension plus two `pytest.approx` assertions; or one evaluator change.
- **Cache:** n/a
- **Lane:** `infra`

### 2.3 — 🔵 RECOMMENDED — "attains every PSD reduction between 0 and 56 %" states an unattained supremum as attained, in three artifacts

- **Gate:** ParameterProvenance
- **Location:** `papers/D12/paper_draft.tex:453-457`; `src/core/formulas.py` (γ docstring); `Lit-Search/Phase-6E/primary-sources/Mather1982.abstract.txt`
- **Observed:** Unchanged at `:453-456`: "the largest amplitude reduction the closed form **admits** is $1 - \sqrt{n/(2n+1)} = 33\,\%$" and "the closed form **attains** \emph{every} PSD reduction between $0$ and $56\,\%$".
- **Evidence:** Recomputed, not inherited. $\gamma(r,n)$ decreases strictly in $r$ with $\lim_{r\to\infty}\gamma = n/(2n+1) = 4/9$, so $1-4/9 = 0.5555556$ is a supremum approached and never attained; likewise $1-\sqrt{4/9} = 0.3333333$. The project's own test says so in terms — "the maxima, which are SUPREMA -- approached as r -> infinity, never attained". Reporting $55.56$ as $56$ additionally claims attainment on the empty interval $(55.56\,\%,\,56\,\%]$.
- **Expected:** "attains every PSD reduction strictly below $1-n/(2n+1) = 55.6\,\%$"; "the supremum of the amplitude reduction is $33\,\%$".
- **Fix:** One clause at `:454`, mirrored into `formulas.py`.
- **Cache:** n/a

### 2.4 — 🔵 RECOMMENDED — the $r^{-2}$ in $r^{-(n+1)}$ is still attributed to $G$, which supplies only $r^{-(n-1)}$

- **Gate:** ParameterProvenance
- **Location:** `papers/D12/paper_draft.tex:433-436`; `src/core/formulas.py`
- **Observed:** Unchanged: "the physical re-referencing of $G$ via $G \propto T^{n-1}$ --- the two differ only in bookkeeping and yield the same expression, since $r^{-(n-1)}\cdot r^{-2} = r^{-(n+1)}$."
- **Evidence:** With $F(t,n)=\frac{n}{2n+1}\frac{t^{2n+1}-1}{t^{n}-1}$ and $t=1/r$, $F(1/r,n)=\frac{n}{2n+1}\frac{r^{2n+1}-1}{r^{n}-1}\,r^{-(n+1)}$ exactly. The second reading needs **two** re-referencing factors and the project's own primary-source cache names both: "Using G ∝ T^(n-1), G(T_s) = G(T_b) · r^-(n-1) **and T_s² = T_b² · r^-2**". The paper names only the $G$ half and then produces $r^{-2}$ from nowhere.
- **Expected:** "the re-referencing of $T^2$ and $G$ from the bath to the bolometer, via $T_s^2 = T_b^2 r^{-2}$ and $G \propto T^{n-1}$".
- **Fix:** One clause at `:435`.
- **Cache:** n/a

### 2.5 — 🔵 RECOMMENDED — neither γ function carries a `Lean:` reference and `formula_grounding` is structurally blind to a function with none

- **Gate:** ComputationCorrectness
- **Location:** `src/core/formulas.py` (`f_link_bath_referred`, `phonon_psd_gradient_factor`); `scripts/validate.py::check_formula_grounding`
- **Observed:** `f_link_bath_referred` carries no reference line at all; `phonon_psd_gradient_factor` offers `Provenance: Mather (1982)…` and no `Lean:`. Pipeline Invariant #4 and the Stage-2 gate both require every function to reference a Lean theorem.
- **Evidence:** `check_formula_grounding` iterates the references that *exist*, so a function with zero references contributes zero entries and is silently clean. This is the same shape as 2.2: the check measures what was declared. The paper does not overclaim here — `:438-439` says the identity is verified in `tests/test\_bundle\_formulas\_d11\_d12.py` and never that it is kernel-checked.
- **Expected:** `Lean: phononPSDGamma, phononPSD_eq_phononPSDGamma_one` on the first; an explicit `Lean: none — literature-quoted closed form, not formalized` on the second; plus a `formula_grounding` sub-check that every public function carries ≥1 reference line.
- **Fix:** Two docstring lines plus one sub-check.
- **Cache:** n/a

### 2.6 — 🔵 RECOMMENDED — the `_FORMULA_MODULE_ALIASES` allow-list is global and still never validated against the importing module

- **Gate:** ComputationCorrectness
- **Location:** `scripts/build_graph.py:3545` and `:3565`
- **Observed:** `_FORMULA_MODULE_ALIASES = {"F", "formulas", "src.core.formulas"}` is consulted for every `PythonTest` node regardless of what that module bound the name to. Unchanged from round 10.
- **Evidence:** I re-verified it manufactures no false coverage **today** — every `F` / `formulas` binding under `tests/` resolves to `src.core.formulas`, in four modules. But the guard is a name convention, not a resolution: a future `import numpy.fft as F` in any test file silently manufactures coverage for every formula whose bare name collides, and Gate 4 is the consumer. The extractor already reads each test module's source to build `referenced_names`; resolving the alias from that module's own `import` lines costs one pass.
- **Expected:** Per-module alias resolution; fail closed (no edge) when the alias does not resolve to `src.core.formulas`.
- **Fix:** One import-scan plus one dict lookup.
- **Cache:** n/a

---

### 3.1 — 🔵 RECOMMENDED — `correlated_pair_sigma_eq_sqrt_two_mul` REPRODUCES a sixth time: bare real-√ algebra described as quantifying a witness pair

- **Gate:** LeanProofSubstance
- **Location:** `papers/D12/paper_draft.tex:325-327`; `lean/SKEFTHawking/Detection/NEPAlgebra.lean:470-474`
- **Observed:** Both artifacts byte-identical to rounds 6–10. Paper: "…exhibits a fully-correlated pair whose total power is twice what quadrature predicts, with the discrepancy quantified exactly as $\sqrt{2}$ by \thm{correlated\_pair\_sigma\_eq\_sqrt\_two\_mul}." Theorem: `theorem correlated_pair_sigma_eq_sqrt_two_mul (S : ℝ) : √(4 * S) = √2 * √(S + S)`.
- **Evidence:** The statement mentions neither `IsUncorrelatedAt`, nor the witness functionals of `quadrature_uncorrelated_hypothesis_load_bearing`, nor `sigma_eq_responsivity_nep_sqrt_enbw`. It is unconditional in `S` including `S < 0`, where both sides are Lean's junk `0`, so it survives any edit to the witness. The witness itself is substantive; what is missing is the link, and the verb "quantified" asserts the link exists.
- **Expected:** "the corresponding output-noise ratio is $\sqrt2$, recorded arithmetically as \thm{…}", or a one-line corollary instantiating the identity at the witness's own `V`/`Vtot`.
- **Fix:** One clause at `:326-327`.
- **Cache:** n/a

### 3.2 — 🔵 RECOMMENDED — the Lean discloses that `enbw_dcGain_hypothesis_load_bearing` runs through the `x/0 = 0` junk value; the paper still does not

- **Gate:** LeanProofSubstance
- **Location:** `papers/D12/paper_draft.tex:290-291`; `lean/SKEFTHawking/Detection/FilterFloors.lean:471-497`
- **Observed:** Paper: "The hypothesis that DC gain is nonvanishing is non-droppable, witnessed by \thm{enbw\_dcGain\_hypothesis\_load\_bearing}."
- **Evidence:** The witness is $h(y)=y-1$ on $[0,2]$; the proof establishes $\int_0^2 (y-1) = 0$ and closes through `enbw_of_dcGain_eq_zero`, i.e. `enbw h 2 = 0` and `0 * 2 < 1/2`. As a claim about the formal theorem this is correct and I am not calling it unsound. The Lean file discloses the reading explicitly ("Physically this is not a Lean artefact: a zero-DC-gain filter rejects the signal entirely…") and the `enbw` docstring flags the degenerate branch. The paper carries neither sentence, so the one witness in the draft whose refutation runs through a division convention is the one the draft says least about — in a paper that elsewhere makes a virtue of disclosing exactly these branches (`filteredSNR_of_variance_eq_zero`).
- **Expected:** One parenthetical at `:291` importing the Lean's own sentence.
- **Fix:** One parenthetical.
- **Cache:** n/a

*(No other class-3 findings, and I re-derived the substrate rather than inheriting it. All **175** `\thm{}`/`\texttt{}` identifiers in the draft were resolved against a fresh `lean_deps.json` read: **160 resolve to project declarations**; the 15 that do not are Mathlib names (`HasSubgaussianMGF.measure_ge_le`, `bayesRisk_le_bayesRisk_comp`, `cdf`, `gaussianReal`, `tsum`), the three core axioms, literal words (`sorry`, `native_decide`, `maxHeartbeats`, `TODO`, `infotheo`) and version strings. **Zero** intersection with the 576-name `PlaceholderMarker` set. **Zero** occurrences of any of the 13 stale names the anchor list enumerates. I went further than round 10 on the substrate claim, because round 10's check was module-local and the paper's results are not: I computed the **full project dependency closure** of all 582 declarations in the thirteen root-imported modules — **779 declarations across 26 modules**, 13 of them outside D12's own thirteen (`QuantumNetwork.HelstromDiscrimination`, `ReadoutRelaxationBound`, `ThermalAssignmentFloor`, `GrapheneNoiseFormula`, …). Over that whole closure: `axiom_deps_project` empty for every declaration; `axiom_deps_core` exactly `{propext, Classical.choice, Quot.sound}`; zero `sorry`. The abstract's kernel-purity sentence is therefore true in the stronger sense a reader will take it, not only in the scoped sense §1 states. I also verified the abstract's PhysLib parenthetical at closure level rather than by grep: PhysLib **is** genuinely in the import closure (`SKEFTHawking.QuantumNetwork.ThermalAssignmentFloor` imports `Physlib.StatisticalMechanics.CanonicalEnsemble.TwoState`, and `twoState_excited_probability` really does consume `CanonicalEnsemble.twoState_probability_snd`), so `:118-120` is accurate — **and** that consumer is **not** in the reachable closure: the only `ThermalAssignmentFloor` declarations D12 reaches are `thermalExcitedPop` (`1/(1+exp x)`, no PhysLib content) and `avgAssignmentError_combined_floor`. Zero reachable declarations have a PhysLib object in their type. "PhysLib appears in the import closure but no result reported here consumes it" is exactly true. `speedupWitness_johnsonNEP_double : speedupWitness.johnsonNEP kB T = 2 * speedupWitness.johnsonNEPNaive kB T` confirms `:415-416`'s "low by exactly two, not four" against the anchor list's `|1-ℒ|` mandate. The PhysLib Fidelity claims at `:234-238` are correct in every particular: `lean/.lake/packages/Physlib/QuantumInfo/States/Mixed/Fidelity.lean:26-27` does say "the quantum version of the Bhattacharyya coefficient", and the open TODO block does contain "Matches with classical (squared) Bhattacharyya coefficient".)*

---

### 4.1 — 🟡 REQUIRED — the same primary source is still rendered with reversed author order in D10 and correctly in D12, at source; the duplicate-key population is still 32

- **Gate:** CrossPaperConsistency
- **Location:** `papers/D10/bibliography.bib:49-56`; `papers/D12/paper_draft.tex:754-755`; `src/core/citations.py`
- **Observed:**
  ```
  papers/D10/bibliography.bib:50   author = {Caballero, Jose Manuel Rodriguez and Unruh, Dominique}
  papers/D12/paper_draft.tex:754   D.~Unruh and J.~M.~Rodr\'iguez~Caballero
  arXiv:2512.05878 (round-10 fetch)  1. Dominique Unruh  2. José Manuel Rodríguez Caballero
  ```
  D12 is right; D10 is wrong, and it is wrong in the `.bib` source, so re-rendering the `.bbl` will not correct it. Unchanged since round 10.
- **Evidence:** Gate 2's literal blocking condition ("a bibkey whose `arxiv_id` differs across papers") still does not fire, because the *keys* differ rather than the IDs — I re-confirmed no bibkey carries two different arXiv IDs across any bundle draft. The duplicate-title population is **32** normalized titles under ≥2 registry keys, identical to round 10. The three that touch D12:
  ```
  "A Formalization of the Generalized Quantum Stein's Lemma in Lean"
     Meiburg2025PhysLib       arxiv=2510.08672  used_in=[D8]
     MeiburgLessaSoldati2025  arxiv=2510.08672  used_in=[D12]
     leanQI2025               arxiv=None        used_in=[D10]
  "Complex Bounded Operators in Isabelle/HOL"
     UnruhCBO2025             arxiv=2512.05878  used_in=[D12]
     isabelleCBO              arxiv=None        used_in=[D10]
  "Markov kernels in Mathlib's probability library"
     Degenne2025MarkovKernels arxiv=2510.04070  used_in=[I3]
     Degenne2025Kernels       arxiv=2510.04070  used_in=[D12]
  ```
  Both D10 keys carry `arxiv: None` in the registry while `papers/D10/bibliography.bib` carries the eprint IDs — the registry is strictly less informative than the artifact it governs.
- **Expected:** Correct `papers/D10/bibliography.bib:50` to `{Unruh, Dominique and Rodriguez Caballero, Jose Manuel}`; alias `leanQI2025 → MeiburgLessaSoldati2025` and `isabelleCBO → UnruhCBO2025`; add a duplicate-normalised-title/DOI/arXiv detector to `validate.py`.
- **Fix:** One `.bib` line, two registry aliases, one detector.
- **Cache:** cache-verified (`UnruhCBO2025`, hash `e238304306`, round-10 fetch, `is_stale: False`)
- **Lane:** `prose`

*(No cross-paper contradictions. I re-walked the seams the anchor list names: D9↔D12 — `quarter_sqrtFidelity_sq_le_povmAvgError` and `IsBinaryPOVM` appear in no draft but D12's and live under `QuantumNetwork/HelstromDiscrimination.lean`, which is in D12's reachable closure, exactly as `:243-246` says; D1↔D12 — `johnsonNyquistPSD` is reached through `GrapheneNoiseFormula`, which the closure confirms, and the `4·k_B·T·σ_Q` prefactor is convention-pinned end-to-end. The paper's "two open prior-art checks" count is consistent across the abstract `:65`, §5 `:638` and the conclusion `:704`, and matches `prior_art_novelty.md §3`.)*

---

### 5.1 — 🟡 REQUIRED — restated precisely as requested: the ENBW parenthetical disambiguates the **symbols**, and the reader's problem is the **functionals**

- **Gate:** NarrativeGrounding
- **Location:** `papers/D12/paper_draft.tex:297-309`
- **Observed:** The constant is right at all four sites — `:50` (abstract), `:273` (§3.1 headline), `:305` (this parenthetical) and `:688` (conclusion) all say $1/2$, matching `FilterFloors.lean:197-201` (`1 / 2 ≤ enbw h T * T`); the draft's only $1/4$ is the Le Cam prefactor at `:144`, which is correct. What remains is the sentence's content. The paper quotes the instrument convention "$\mathrm{ENBW} = 1/(4\tau)$ for a single-pole lock-in filter of time constant $\tau$", then resolves the apparent conflict entirely by distinguishing the *letters*: "(The time constant is written $\tau$, not $T$: thirty lines above, $T$ is the observation window … and the two are different quantities.)"
- **Evidence:** Distinguishing $\tau$ from $T$ does not remove the contradiction, because $T$ is a free parameter and a reader may legitimately set $T = \tau$. Doing so gives instrument-$\mathrm{ENBW}\cdot T = 1/4 < 1/2$ and the theorem reads false. The actual resolution is that these are two different *functionals*, not one functional at two arguments. `FilterFloors.lean:124-125`:
  ```lean
  noncomputable def enbw (h : ℝ → ℝ) (T : ℝ) : ℝ :=
    (∫ x in (0:ℝ)..T, h x ^ 2) / (2 * (∫ x in (0:ℝ)..T, h x) ^ 2)
  ```
  — a **time-domain ratio of the impulse response over the same finite window `T` that multiplies it**. The instrument's $1/(4\tau)$ is a **frequency-domain** noise bandwidth of an **infinite-support** single-pole response over the whole line. Evaluating the paper's own `enbw` on $h(x)=e^{-x/\tau}$ over $[0,T]$ closes it in one line: with $u = T/\tau$,
  $$\mathrm{enbw}(h,T)\cdot T \;=\; \frac{u}{4}\cdot\frac{1+e^{-u}}{1-e^{-u}},$$
  monotone increasing with limit $1/2$ as $u\to 0^+$ — so the single-pole filter satisfies the theorem at **every** window and never approaches $1/4$ ($u=0.01 \to 0.50000$; $u=1 \to 0.54099$; $u=5 \to 1.26696$; $u=20 \to 5.00000$). That computation *is* the disambiguation, and it is one sentence long.
- **Expected:** Replace the symbol paragraph with the functional one: "…and the two are different *functionals*, not one functional at two arguments: \thm{enbw} is a time-domain ratio over the same finite window that multiplies it, whereas $1/(4\tau)$ is a frequency-domain bandwidth of an infinite-support response. Evaluating \thm{enbw} on a single-pole response over $[0,T]$ gives $\tfrac{u}{4}\tfrac{1+e^{-u}}{1-e^{-u}} \ge \tfrac12$ at every $T$, with $u = T/\tau$."
- **Fix:** One sentence at `:303-308`, replacing the current parenthetical rather than adding to it.
- **Cache:** n/a

### 5.2 — 🟡 REQUIRED — the paragraph that declares the highest prior-art risk still withholds the name and the `\cite`, and still describes the project with the characterization this project retracted as unverified

- **Gate:** NarrativeGrounding
- **Location:** `papers/D12/paper_draft.tex:638-641`; cf. `:610`, `:704`; `papers/D12/prior_art_novelty.md §3`
- **Observed:** "First, an out-of-Mathlib Lean project **on information theory and hypothesis testing** is under active development; a Bayes-binary-risk--divergence lower bound there would be substantially our Le Cam floor, and it is the highest prior-art risk in this work." No name, no `\cite`. Unchanged.
- **Evidence:** Two defects in one sentence.
  1. **The withheld citation.** §5 names and cites the project two paragraphs earlier (`:610`, `\cite{LuccioliDegenne2024,Degenne2025Kernels}`) and the conclusion names it again (`:704`). Only the paragraph that *declares the obligation* withholds it, so a reader acting on that paragraph alone cannot discharge it.
  2. **The retracted characterization.** `prior_art_novelty.md §3` records the retraction verbatim: "⚠️ This entry previously gave the title as 'Information theory and hypothesis testing, in Lean'. That subtitle came from a web-search snippet, not from any source this project read … Corrected 2026-07-31 after a D12 Stage-13 BLOCKER; the only verified title is the bare token." The retraction was applied to the registry title, the bibitem and the cache — and **not** to the prose that seeded them, where the same words now stand as a factual description of a third party's work. I re-fetched `arXiv:2510.04070` this round specifically to see how far the description can be supported: its abstract names entropy and Kullback–Leibler divergence and nothing about hypothesis testing, and the p.26 `[LD24]` quotation supports "developed Mathlib's KL divergence", "data-processing inequality for f-divergences" and "the chain rule for KL". "on information theory and hypothesis testing" asserts more than any whitelisted primary states.
- **Expected:** "First, \textsc{TestingLowerBounds}~\cite{LuccioliDegenne2024} — the out-of-Mathlib Lean project in which Mathlib's Kullback--Leibler divergence and a general $f$-divergence data-processing inequality were developed~\cite{Degenne2025Kernels} — is under active development; …". That names it, cites it, and asserts only what a whitelisted primary states.
- **Fix:** One sentence at `:638`.
- **Cache:** n/a
- **Lane:** `prose`

### 5.3 — 🔵 RECOMMENDED — the conclusion's composite-bite sentence still never received the §4 qualification, fifth round

- **Gate:** NarrativeGrounding
- **Location:** `papers/D12/paper_draft.tex:695-699`; cf. `:574-580`
- **Observed:** "Two of those composites are shown to bite at a \emph{non-degenerate} operating point --- matched budget $2$, ceiling $22/25$ … --- and not merely at the signal-free zero-budget point, which is what the Birnbaum--Feller lower bound buys."
- **Evidence:** Byte-identical across six commits. It is not false — both proofs do conclude `assignmentFidelity e0 e1 ≤ 22/25` at a signal-carrying point with the full composite binder list. What it omits is what §4 `:574-580` and the Lean's own header note both spend several lines saying: each proof lower-bounds the max by its detection branch and discards the relaxation term, so `22/25` is the single-mechanism value and holds uniformly in `t` and `T1`. A referee reading only the conclusion takes "the composites bite" to mean the composite *bound* is what bites, which §4 explicitly disclaims.
- **Expected:** One clause: "…, though the numerical value is the single-mechanism one (§\ref{sec:control})".
- **Fix:** One clause at `:697`.
- **Cache:** n/a

### 5.4 — 🔵 RECOMMENDED — the $f$-divergence generator and "the affinity is its complement" still disagree by a factor 2, in three artifacts

- **Gate:** NarrativeGrounding
- **Location:** `papers/D12/paper_draft.tex:250-251`; `Lit-Search/Phase-6E/primary-sources/LuccioliDegenne2024.abstract.txt`; `papers/D12/prior_art_novelty.md §3`
- **Observed:** Unchanged: "The squared Hellinger distance is an $f$-divergence with generator $f(t) = (\sqrt{t}-1)^2$, and the affinity is its complement".
- **Evidence:** With $f(t)=(\sqrt t-1)^2$, $D_f(P\|Q)=\sum_i(\sqrt{p_i}-\sqrt{q_i})^2 = 2-2\,\mathrm{BC}$, so the complement relation $H^2 = 1-\mathrm{BC}$ holds only for the $\tfrac12$-normalised squared Hellinger, generator $\tfrac12(\sqrt t-1)^2$. Both the primary-source cache and `prior_art_novelty.md` repeat the unnormalised generator alongside the complement relation. The paragraph's operative claim — that a general $f$-divergence DPI plausibly subsumes the affinity DPI — is unaffected, since BC is an affine decreasing function of $D_f$ under either normalisation, which is why this is advisory.
- **Expected:** $f(t)=\tfrac12(\sqrt t-1)^2$, or "an affine function of" in place of "the complement of".
- **Fix:** One symbol at `:250`, mirrored into two artifacts.
- **Cache:** n/a

### 5.5 — 🔵 RECOMMENDED — the `prxquantum` correction is factually right, but eight lines of first-person internal-review narration still sit at the top of the file a referee receives

- **Gate:** NarrativeGrounding
- **Location:** `papers/D12/paper_draft.tex:1-10`
- **Observed:** The submitted source still opens with: "⚠️ CORRECTION 2026-07-31 (D12 round-9): the first version of this note claimed the drop meant 'every page count and Overfull reading was against the wrong configuration'. That was wrong, and I asserted it without compiling both."
- **Evidence:** I verified the substance and it is correct — `compile_bundle_pdf.py D12` returns `pages=10 overfull=0 dropped_class_opts=0`, judged from the rendered PDF. The same account already lives verbatim in `papers/D12/bundle_metadata.json` `notes`, i.e. in a place designed to hold it. Journals receive the `.tex`; the provenance narrative belongs in the metadata, and one factual line belongs in the source.
- **Expected:** One line — "% Class option `prx`: revtex4-2 has no `prxquantum` substyle and would drop it; `\rtx@apsprx` is a no-op so this is a correctness fix, not a layout change. Re-measure against the journal's own class before submission." — and the narrative left in `bundle_metadata.json`.
- **Fix:** Delete seven lines from `:4-10`.
- **Cache:** n/a

### 5.6 — 🔵 RECOMMENDED — Fig. 1's caption quotes the crossover $N_a = 51.40$ without the $N_b$ it depends on, while the neighbouring panel establishes $N_b = 5$, where the crossover is $6.48$

- **Gate:** NarrativeGrounding
- **Location:** `papers/D12/paper_draft.tex:164-172`
- **Observed:** "(A) … certified at $148\times$ for $(N_b,N_a) = (5,10)$. **(B)** Fail-open as an average-error screen: beyond the crossover at $N_a = 51.40$ …" Unchanged.
- **Evidence:** The crossover solves $2\sqrt{N_b}(\sqrt{N_a}-\sqrt{N_b}) = \log 4$ and is a function of $N_b$. Recomputed this round: at $N_b = 50$ it is $51.39590\ldots \to 51.40$ ✓; at the $N_b = 5$ the caption has just established for panel A it is $6.4824$. The figure itself does state the parameter — `visualizations.py:14858-14859` sets the subplot titles "(A) false-strict miss bound, N_b = 5" / "(B) fail-open screen, N_b = 50" — so a reader looking at the plot can recover it, which is why this is advisory. The caption alone cannot be read correctly.
- **Expected:** "beyond the crossover at $N_a = 51.40$ (at $N_b = 50$)".
- **Fix:** Three words at `:167`.
- **Cache:** n/a

*(Every other class-5 check passes and I recomputed each numeral independently rather than inheriting round 10's arithmetic. $Q(1)=0.15865525393145705$; the Birnbaum–Feller value $0.12098536225957168$ is $76.257\,\%$ of it and $0.021596386605275224$ is $94.929\,\%$ of $Q(2)$, so the $76\,\%$/$95\,\%$ pair at `:568-570` is the **bound**'s ratio, the only reading under which both numbers are right — the rational enclosure $3/25$ is $75.6\,\%$ at $z=1$ but $35\,\%$ at $z=2$, so the sentence's grammatical antecedent is not its intended one; both round to $76\,\%$ at $z=1$ by coincidence, so nothing is misstated. Mills fractions are exactly $z^2/(1+z^2) = 0.5000$ and $0.8000$ ✓. $e^5 = 148.4131591$ ✓; true ratio at $(50,60)$ is $3491.9554$ ✓; Mills vacuity root $0.37223890$ ✓; $x = 2Q(1)/(1-2Q(1)) = 0.4647947734915441$ and $x/(2(1+x))$ returns $Q(1)$ to 16 digits ✓; $\gamma(2,4)=0.4731481481$, $1-\gamma = 52.685\,\%$, $1-\sqrt\gamma = 31.214\,\%$, difference $21.4710 \to 21.5$ ✓; $r^\* = 1.1913538$ for a 30 % PSD reduction ✓; $22/25 = 0.88$ ✓. **No unhedged first-claim exists**: `:627-636` is scoped to "the ecosystems we actually assessed" and `:623` explicitly disclaims absence in HOL Light; `:638-645` flags the two open checks. Class 8 note: the draft contains **zero** occurrences of "Monte Carlo", "numerical evidence", "simulation", "simulated" or "we simulate" — grepped case-insensitively.)*

---

### 6.1 — 🟡 REQUIRED — the direct answer: six is **not** the complete set. `hs : IntervalIntegrable (fun x => s x ^ 2) volume 0 Tw` is load-bearing and undisclosed, while §5 and §1 both make completeness claims

- **Gate:** AssumptionDisclosure
- **Location:** `papers/D12/paper_draft.tex:667-674`, `:88-93`; `lean/SKEFTHawking/Electrothermal/BolometricFloors.lean:793-806`
- **Observed:** §5 now names six and closes "The capstone is only as strong as those \emph{six} identifications." §1 says "the capstone \thm{bolometer\_error\_floor} carries six, enumerated in \S\ref{sec:scope}". The theorem carries **thirteen** binders:
  ```lean
  (hkB : 0 < kB) (hT : 0 < T) (hG : 0 < m.G)
  (hindep  : IsUncorrelatedAt Finset.univ Vtot V)          -- disclosed
  (hphonon : IsThermalFluctuationLimited (V 0) m kB T Tw)  -- disclosed
  (hjohnson: IsWhiteFilteredVariance (V 1) (m.johnsonNEP kB T ^ 2) Tw)  -- disclosed
  (hTw : 0 ≤ Tw)
  (hadm : IsAdmissibleFilter Tw s hf)                      -- disclosed
  (hs  : IntervalIntegrable (fun x => s x ^ 2) volume 0 Tw)  -- ← NOT disclosed
  (hσ : 0 < σ) (hμle : μ₀ ≤ μ₁)
  (hμ  : μ₁ - μ₀ = ∫ x in (0:ℝ)..Tw, hf x * s x)           -- disclosed (new)
  (hσV : σ = Real.sqrt (Vtot hf))                          -- disclosed (new)
  ```
- **Evidence:** I did not take the commit's verification at face value, because it does not establish what the paper asserts: renaming `hμ`/`hσV` to the underscore form proves the **proof consumes them**, which is compatible with their being redundant. I re-derived the falsifiers directly, and the paper's claim is nevertheless **true**. `matchedBudget S₀ T s = √(2·(∫₀ᵀ s²)/S₀)` and `IsWhiteFilteredVariance V S₀ T` unfolds to `∀ h, V h = S₀/2 · ∫₀ᵀ h²`, so:
  - **drop `hμ`**: take `μ₁ - μ₀` arbitrarily large with `σ` fixed by `hσV`; the midpoint threshold drives `avgAssignmentError → 0` while `Q(budget/2) > 0`. False. (With `hμ` intact, Cauchy–Schwarz gives `μ₁-μ₀ ≤ budget·σ`, which is exactly what the conclusion needs — so `hμ` is the cap.) ✓
  - **drop `hσV`**: take `σ` arbitrarily small at fixed `Δμ = ∫ hf·s > 0`; same collapse. False. ✓
  So §5's two new sentences are accurate and the round-10 blocker is genuinely closed.
  **What is not accurate is "six".** `hs` is load-bearing, and here is the explicit counterexample: on `Tw = 1` take `s(x) = x^{-1/2}` and `hf(x) = x^{1/2}`. Then `IsAdmissibleFilter 1 s hf` **holds** — its two fields are `∫₀¹ hf² = 1/2` and `∫₀¹ hf·s = 1`, both finite — and `hμ`, `hσV`, `hσ`, `hμle`, `hTw` and the three positivity binders are all satisfiable. But `s²` is not interval-integrable, so Lean's `∫₀¹ s²` returns the junk value `0`, `matchedBudget = √(2·0/S₀) = 0`, and the conclusion becomes `gaussianQ 0 = 1/2 ≤ avgAssignmentError`. The actual average error at the midpoint threshold is `Q(1/(2σ)) = Q(1/√S₀) < 1/2` for every `S₀ > 0`. **The conclusion is false without `hs`.**
  I checked the other six too, so the finding is exact rather than a sweep: `hkB`, `hT`, `hG`, `hσ` are ordinary positivity conditions and no reader is misled by their omission; and `hμle` is **not** load-bearing for this theorem's truth — with `hμ` intact, `μ₁ < μ₀` forces `Q(a)+Q(b) > 1` for every finite threshold, i.e. `avgAssignmentError > 1/2 ≥ gaussianQ(budget/2)`, so the statement survives dropping it (it is load-bearing only for the proof route through `error_floor_from_budget`). `hs` is the sole omission.
  The paper's own disclosure standard is what makes this a finding rather than pedantry: §5 lists `IsAdmissibleFilter` — a finite-energy-plus-integrability condition on the **filter** — as one of the six, while omitting the identical condition on the **template**. `hs` and `IsAdmissibleFilter.sq_integrable` are the same kind of hypothesis; the paper discloses one and not the other, then asserts the list is complete.
- **Expected:** Either add a seventh item ("and square-integrability of the template on the window, $\int_0^{T_w} s^2 < \infty$, the template-side counterpart of \thm{IsAdmissibleFilter}") and say "seven"; or drop both completeness sentences and say "the capstone's modelling binders include", which costs nothing and cannot go stale. `phonon_floor_of_psd_ge` and `phonon_only_error_floor` (`:498-502`) carry the identical binder and the identical gap.
- **Severity note:** REQUIRED, not blocking, and I want the reasoning on record so a later round can re-adjudicate rather than re-litigate. `READINESS_GATES.md:122` blocks on "an assumed hypothesis that is load-bearing for a cited result but does not appear in the paper", and `hs` meets that text literally. I do not escalate because (a) the omitted binder is a regularity condition no physical template violates, so no *physical* claim in the paper is overstated by its absence — unlike round 10's `hμ`/`hσV`, which were the glue between two halves of an inequality; and (b) both completeness sentences are scoped to the word "identifications", and `hs` is not one. Applying `:122` at full strictness would also block on `hσ` and the three positivity binders and make Gate 6 unsatisfiable for any theorem in any paper, which is plainly not its intent.
- **Fix:** One clause in §5 plus one numeral in §1.
- **Cache:** n/a

### 6.2 — 🔵 RECOMMENDED — §1's "carries six" cross-reference does not land: §5 calls four of the six "modelling binders" and only two "identifications"

- **Gate:** AssumptionDisclosure
- **Location:** `papers/D12/paper_draft.tex:86-89` vs `:661-674`
- **Observed:** §1's sentence is about *identifications*: "The \emph{identification} of any symbol with a physical instrument … Where such an identification is needed it appears as an \emph{explicit binder} … the capstone carries six, enumerated in \S\ref{sec:scope}". §5 introduces four of the six as "the modelling binders of the capstone" and only the last two as identifications ("Two further binders belong in the same list … which identifies …").
- **Evidence:** A reader following the pointer and counting *identifications* in §5 finds two, not six. `IsAdmissibleFilter` in particular is an admissibility condition on the filter (finite window energy plus existence of the deflection integral — I read the structure), not an identification of a symbol with a physical instrument. The §1 replacement is otherwise **true** and I checked its load-bearing half independently: nothing in D12 assumes a physical identification silently inside a proof (the substrate is zero-`sorry` and kernel-pure over the full 779-declaration closure, so it cannot), and the two cases where physics lives in a *definition* rather than a binder — `IsWhiteFilteredVariance` (Parseval + flatness) and `IsShotFilteredMoments` (Campbell's theorem) — are both disclosed at `:657-660`. The retraction of the old absolute is quoted in place, which is the right handling.
- **Expected:** "…carries six modelling binders, two of them identifications in this sense, enumerated in §\ref{sec:scope}".
- **Fix:** Four words at `:89`.
- **Cache:** n/a

### 6.3 — 🔵 RECOMMENDED — `avgError_ge_gaussianQ_sharp` carries an undisclosed ordering binder $\mu_0 \le \mu_1$

- **Gate:** AssumptionDisclosure
- **Location:** `papers/D12/paper_draft.tex:209-213`; `lean/SKEFTHawking/Detection/GaussianThreshold.lean:785-788`
- **Observed:** "The headline is the sharp separation-budget floor \thm{avgError\_ge\_gaussianQ\_sharp}: no threshold placement achieves an average error below $Q\!\left(\Delta\mu/2\sigma\right)$." §2.3 nowhere states the branch-ordering convention.
- **Evidence:** `(hσ : 0 < σ) (hμ : μ₀ ≤ μ₁) (hz : (μ₁ - μ₀) / (2 * σ) ≤ z₀)`. Unlike in the capstone (6.1), here the ordering binder **is** load-bearing: with a signed $\Delta\mu$ and $\mu_1 < \mu_0$, $Q(\Delta\mu/2\sigma) > 1/2$ while the achievable average error approaches $1/2$ from above, so the statement fails. Low severity because the convention is the obvious one — but §5 enumerates twelve modelling Props by name while this binder of the section's own headline theorem goes unnamed.
- **Expected:** "for a signal branch of the larger mean ($\mu_0 \le \mu_1$, an explicit binder)".
- **Fix:** One parenthetical at `:210`.
- **Cache:** n/a

### 6.4 — 🔵 RECOMMENDED — "for every admissible filter" describes a theorem that does not take `IsAdmissibleFilter`, in a paper where that identifier is one of the twelve disclosed Props

- **Gate:** AssumptionDisclosure
- **Location:** `papers/D12/paper_draft.tex:272-274`; `lean/SKEFTHawking/Detection/FilterFloors.lean:197-201`; cf. `:651`
- **Observed:** "the central result is \thm{enbw\_mul\_window\_ge\_half}: for every **admissible filter** with nonvanishing DC gain, $\mathrm{ENBW}\cdot T \ge 1/2$."
- **Evidence:** The binders are `(h : ℝ → ℝ) (T : ℝ) (hT : 0 < T) (hint : IntervalIntegrable h volume 0 T) (hsq : IntervalIntegrable (fun x => h x ^ 2) volume 0 T) (hDC : (∫ x in (0:ℝ)..T, h x) ≠ 0)`. `IsAdmissibleFilter` is not among them — it is declared in a different module (`MatchedFilter.lean:135`), is `\thm{}`-cited in this paper at `:651` as one of the twelve disclosed hypotheses, and the floor does not consume it. The direction is conservative (the theorem is more general than the prose says), so nothing is overclaimed; the defect is that a reader mapping prose onto the disclosed-Prop list attaches the wrong hypothesis to the section's central result, and the two integrability conditions that *are* load-bearing are never mentioned.
- **Expected:** "for every filter integrable with its square on the window and with nonvanishing DC gain" — noting that `IsAdmissibleFilter` enters only at the matched-filter and capstone layers.
- **Fix:** One clause at `:273`.
- **Cache:** n/a

### 6.5 — 🔵 RECOMMENDED — "independent of responsivity" omits the $R \neq 0$ binder that makes it true

- **Gate:** AssumptionDisclosure
- **Location:** `papers/D12/paper_draft.tex:332-334`; `lean/SKEFTHawking/Detection/NEPAlgebra.lean:584-587`
- **Observed:** "The end-to-end ceiling is \thm{snrChain\_le\_window\_ceiling}, attained by the matched boxcar … and independent of responsivity (\thm{snrChain\_independent\_of\_responsivity})."
- **Evidence:** `theorem snrChain_independent_of_responsivity {R P_sig nepVal enbwVal : ℝ} (hR : R ≠ 0) : snrChain R P_sig nepVal enbwVal = P_sig / (nepVal * √enbwVal)`. At `R = 0` the chain returns Lean's junk `0` and the independence fails. Same class as 6.3/6.4 and the same low severity — no physical claim moves, since a zero-responsivity readout is not a readout — but this paper's whole discipline is that conventions and degenerate branches are stated, and it states the analogous `c = 0` branch for `power_unsigned_characterization_false` two paragraphs later.
- **Expected:** "independent of responsivity (\thm{snrChain\_independent\_of\_responsivity}, for any nonzero $R$)".
- **Fix:** Four words at `:334`.
- **Cache:** n/a

*(The rest of class 6 re-verified from signatures rather than inherited. The "Twelve propositions" enumeration at `:647-655` is exactly right — I enumerated every `Prop`-valued `def`/`abbrev`/`structure` across the thirteen modules and got exactly the twelve the paper lists, with no thirteenth; the anchor list's "Tracked-Prop disclosure (11 of them)" is the stale artifact, not the paper, since it omits `IsThermalFluctuationLimitedGamma`. "Thirteen root-imported modules" at `:108` is exactly right: `lean/SKEFTHawking.lean` root-imports 6 `Detection.*` + 3 `Electrothermal.*` + 4 `Control.*`. `poisson_avgError_floor`'s sole hypothesis really is `hδ : IsCountRule δ`, so `:147` is exact. The composite-bite theorems inherit their composites' full binder lists including `hattr0`/`hattr1`, which `:550` discloses.)*

---

### 7.1 — 🟡 REQUIRED — D12's entire numeric surface still sits outside every freshness and provenance gate

- **Gate:** NumericalFreshness
- **Location:** `papers/D12/paper_draft.tex` throughout; `validate.py --check count_literals` / `numerical_literals` / `counts_fresh` / `tables_fresh` / `paper_provenance`
- **Observed:** Re-run this round, all five green and all five vacuous for this bundle:
  ```
  counts_fresh        1/1 checks passed
  count_literals      ✓ D12 — no count literals found
  numerical_literals  ✓ D12 — no inline unit-bearing literals in body
  tables_fresh        1/1 checks passed
  paper_provenance    ✓ D12/figures — 3 referenced figure(s) resolved
  ```
  while the draft carries at least `0.4731`, `53 %`, `31 %`, `52.7`, `31.2`, `21.5`, `22`, `33 %`, `56 %`, `4/9`, `0.444`, `1.19`, `30 %`, `22/25`, `3/25`, `0.15866`, `148`, `148.41`, `51.40`, `3491.96`, `76 %`, `95 %`, `50 %`, `80 %`, `0.37`, `0.4648`, `0.46479`.
- **Evidence:** No `REPORTS` edge exists from `paper:D12` to any `CountMetric`, `papers/D12/tables/` feeds no prose, and Gate 9 therefore passes on an empty set. The `DEPENDS_ON` edge added at round 9 reaches exactly one parameter and no derived value. Every numeral above is correct today — I recomputed all of them this round — but rounds 6, 7 and 8 each found a wrong one in this unguarded surface (`23`, then the 21.5 derivation, then `0.4651`), and the only thing that caught each was a reviewer. 2.2 compounds it: the one Python function whose value feeds four of these numerals is also the one with bounds-only coverage.
- **Expected:** A `\pipelinevalue{}` chain or a `papers/D12/tables.py` spec for the derived quantities; or at minimum register the derived values in `PARAMETER_PROVENANCE` as `DERIVED` with `DEPENDS_ON` edges, extending the mechanism round 9 built for one parameter.
- **Fix:** One tables spec plus a macro retrofit.
- **Cache:** n/a
- **Lane:** `prose`

### 7.2 — 🔵 RECOMMENDED — spelled-out tree counts REPRODUCE outside the macro chain; both correct today, both invisible to `count_literals`

- **Gate:** NumericalFreshness
- **Location:** `papers/D12/paper_draft.tex:108` ("Thirteen root-imported modules"), `:647` ("Twelve propositions carry modelling or admissibility content")
- **Observed:** Both unchanged; both hand-written counts of tree contents rather than `\input{counts.tex}` macros.
- **Evidence:** Both verified correct at review time by direct enumeration (13 root imports; 12 `Prop`-valued declarations). `count_literals` reports "no count literals found" because the detector does not match spelled-out numerals, so the class is unguarded across every bundle.
- **Expected:** `d12modules` / `d12modelprops` macros in `docs/counts.tex`, or extend `count_literals` to catch spelled-out numerals before "modules"/"propositions"/"theorems".
- **Fix:** Two macros, or one regex.
- **Cache:** n/a

---

### 8.1 — 🔴 BLOCKER — the guard now runs on the value that ships, but it is scoped to `status: fixed`; the two-key bypass survives verbatim as `{"finding_id": X, "status": "accepted"}`, and `accepted` is the one status Gate 11 discards outright

- **Gate:** FixPropagation
- **Location:** `scripts/build_graph.py:1701-1706` (the ledger override) and `:1708-1735` (the bar); `scripts/readiness_gates.py::_eval_fix_propagation`
- **Observed:** You asked whether the current guard is sufficient without point (b). **It is not, and the reason is independent of (b).** The bar is now correctly placed after the override and reads `meta['status']`, but its first conjunct is
  ```python
  if (meta.get('status') == 'fixed'
          and severity in ('critical', 'major', 'blocker')):
  ```
  so any ledger record whose `status` is anything other than `'fixed'` never reaches it. `_eval_fix_propagation` then partitions `accepted` out of `open_findings` **before** the severity filter, so an `accepted` finding never enters `r.blockers`, never sets `state = 'blocked'`, and never drives `needs-recheck`.
- **Evidence:** Mutation-tested in-process against a monkeypatched `PROJECT_ROOT` in a `tempfile.mkdtemp()` tree — nothing in the repo was touched and the tree was removed. Eight synthetic 🔴 findings, eight ledger shapes; I deliberately extended past the six you enumerated:
  ```
  §    ledger record                                                emitted meta.status
  9.1  {}                                                (one key)  open      ← guard works
  9.2  {"status":"fixed"}                                           open      REJECTED
  9.3  {"status":"accepted"}                             (two keys) accepted  ← CLOSES A BLOCKER
  9.4  {"status":"accepted","evidence":<60ch>}                      accepted  ← CLOSES A BLOCKER
  9.5  {"status":"fixed","evidence":<60ch>}                         open      REJECTED
  9.6  {"status":"fixed","evidence":<60ch>,"date":…}                fixed     ← closes (old schema)
  9.7  {"status":"fixed","evidence":<60ch>,"commit":…}              fixed     ← closes (new schema)
  9.8  {"status":"closed"}                               (two keys) closed    ← arbitrary token accepted
  ```
  **9.3 is the whole finding.** Two keys. No evidence, no commit, no note, no date, no `superseded_by`. It is round-10's minimal bypass with one word changed, and it takes a 🔴 BLOCKER out of Gate 11's blocking set. Round 10 already showed you this row and already recommended, as item (2) of its Expected, that the bar apply to `accepted` too; that half was not built. The six shapes you enumerated all set `status: fixed`, which is the one branch the guard covers — the same shape of self-flattering enumeration as round 10, one level up.
  **9.8 is a second, smaller hole:** an arbitrary status token is written straight onto the node. `_eval_fix_propagation` happens to treat unknown tokens as open, so `closed` does not currently escape Gate 11 — but `check_accepted_findings_carry_rationale` only inspects `status == "accepted"`, and `bundle_readiness.py` counts on the narrower `status == "open"`, so `closed` makes a finding invisible to the bundle heatmap while remaining blocking in the gate. No enumeration of legal status values exists anywhere.
  **Live measurement.** The bar is no longer inert and I want that on record: **8** blocking-severity findings are now reopened with `blocking_closure_rejected` set, **5** of them flagging D12 — including round-10's own 8.1, whose heading contains the word "fixed" and which heading-parse had therefore closed. Of 805 ledger records, **0** `fixed` records lack an anchor, so the schema-tolerant bar rejects none of the historical corpus; your judgement not to require `commit` was right. But **122** findings sit at `accepted`, **27 of them at blocking severity** (8 critical, 19 major), and every one of those is closed through the branch the guard does not inspect.
  **D12's own exposure through this path is nil** — all 24 D12 ledger records are `status: fixed`, none is `accepted` — so this does not change D12's gate today. It is an unguarded channel on the gate D12 is currently blocked at, and the pass criterion for this stage is the machinery being sound, not the bundle happening not to have used the hole.
- **Expected:** Two changes, plus the (b) specification you asked for.
  1. **Extend the bar to every non-`open` status.** Replace `meta.get('status') == 'fixed'` with `meta.get('status') != 'open'`, and require, for blocking severities: an explicit status in a closed enumeration `{fixed, accepted}`; ≥40 characters of rationale; a provenance anchor (`commit` or `date`); and, for `accepted` only, an `accepted_by` naming a decision-maker. An acceptance is a decision and a decision has an owner.
  2. **Surface accepted blockers rather than deleting them.** `accepted` at blocking severity should move to a dedicated `r.accepted_blockers` list reported in `GateResult.notes`, so the dashboard reads "0 open, 27 accepted at blocking severity" instead of "0 open". This is 8.5 and it is the same defect seen from the gate rather than from the extractor.
  3. **(b), specified tightly enough to build.** The defect is that *one document mints the finding, sets its severity and sets its status*, so a document that omits the glyph is born `advisory` and a heading containing the word "fixed" is born closed. Three requirements:
     - **(3a) Birth-status invariant.** A `ReviewFinding` minted from a review document is born `status: 'open'` at **every** severity. Heading-parse closure is removed entirely, not merely overridden for blocking severities. Concretely: delete the two `status = 'fixed'` assignments at `build_graph.py:1651-1656` and let the ledger be the sole status-transition channel. This is a one-time re-open of the ~370 non-blocking findings currently closed by heading text, which is the correct state — none of them has a closure record. It is also what makes 8.4 load-bearing rather than incidental.
     - **(3b) Severity is a declared field, not an inferred glyph.** Parse severity from a required body key — `- **Severity:** BLOCKER|REQUIRED|RECOMMENDED` — and add a `validate.py` leg that FAILS any review document containing a finding-shaped heading whose body lacks that key. Today, omitting the glyph silently yields `advisory`; under this rule, omitting the declaration is a red build. Keep the glyph as a rendering convention; stop deriving semantics from it. This also retires 8.8.
     - **(3c) Recurrence reopens.** When a finding body declares that a prior finding reproduces — this document declares reproduction 5 times — the named prior `finding_id` must be reopened, overriding any ledger record. A finding a later fresh-context round re-derives is by definition not fixed, and the closing party does not get to keep the ledger entry. Implement as: parse `round-N X.Y REPRODUCES` (and the bare `REPRODUCES` form) from the body, resolve to the prior id via the review-date index, force `status='open'`, record the reopening id in `meta['reopened_by']`.
     Together, (1)–(3) mean no artifact authored by the party seeking closure can set severity or status: only the ledger sets status, only the finding declares severity, and only a fresh review can clear a recurrence.
- **Fix:** One predicate widening plus one enumeration (≈6 lines); one list field on `GateResult`; one severity-declaration parser plus one validator leg; one recurrence parser.
- **Cache:** n/a

### 8.2 — 🔴 BLOCKER — the content scope requires the same `<digits><dash>` heading shape the extractor requires, so the six documents on disk that mint zero are exactly the six the check skips; four of their heading forms are already shipped

- **Gate:** FixPropagation
- **Location:** `scripts/validate.py:3838` (`_FINDING_SHAPED_HEADING`) and `:3884-3886` (the scope `continue`); `scripts/build_graph.py:1421-1433` (`_REVIEW_SECTION_RE`)
- **Observed:** You asked me to find a heading form still minting nothing and to check the content scoping does not now skip something real. Both, and they are the same defect: the scope predicate and the extractor share the blind spot.
  ```python
  _FINDING_SHAPED_HEADING = re.compile(r'^#{3,5}\s+\S*\d[\w.\-]*\s*[—\-–:]', re.M)
  ```
  It requires a digit-run immediately followed by a dash or colon — the exact fragment that drifts. A document whose headings put anything between the number and the dash, or the number after the severity word, is not "finding-shaped" to the check and is not parseable by the extractor either. The extractor widening was real and I verified it: total findings 1190 → **1292**, and every form you list (`### R-1`, `### V.1`, `### F-3`, `### N1.1`, `#### Anchor 1`, `### BLOCKER 1.1`, `### 3.1.b`) now mints.
- **Evidence:** Measured against the live extractor over all `papers/AutomatedReviews/*/*.md`. **Six** documents carry severity-marked headings and mint **zero** nodes, and **all six are out of the check's scope**, so `review_docs_mint_findings` passes without looking at any of them:
  ```
  sev-headings  minted   in check scope   document
      15           0        SKIPPED       2026-04-10/CitationReview-01.md
       7           0        SKIPPED       2026-04-10/ComprehensiveReview-01.md
      17           0        SKIPPED       2026-04-26-1955-lean-substance-audit/lean_project_audit.md
       5           0        SKIPPED       2026-06-08-bundle-stage13/L2.md
       2           0        SKIPPED       2026-04-25-1609-internal-adversarial/paper20_scalar_rung.md
       1           0        SKIPPED       2026-05-01-bundle-stage13/I2-figures.md
  ```
  These are not statistics documents. `lean_project_audit.md` declares four BLOCKERs in its first four headings (`### 1. Class 6 BLOCKER — 14 heartbeat overrides on Uqsl3Hopf.lean theorem bodies`, `### 4. Class 1 BLOCKER — RotorModesGapped : Prop := True is vacuous`); `CitationReview-01.md` declares four 🔴 wrong-citation findings (`### 1. Paper 7 — [TPF2024] = arXiv:2411.18738 🔴`); `L2.md` declares a CRITICAL (`### BLOCK-1 (Class HD, CRITICAL) — Rokhlin/16∣σ framed as tracked hypothesis`). Forty-seven severity-marked headings, zero graph nodes, zero check coverage.
  The forms, tested against both regexes:
  ```
  MINTS  in-scope   ### 1.1 — 🔴 BLOCKER — x            (this document's form)
  ZERO   SKIPPED    ### 1. Paper 7 — q                  ← CitationReview-01.md
  ZERO   SKIPPED    ### 1. Class 6 BLOCKER — w          ← lean_project_audit.md
  ZERO   SKIPPED    ### BLOCK-1 (Class HD, CRITICAL) —  ← L2.md
  ZERO   SKIPPED    ### I.1 Count Metadata Inconsistency ← ComprehensiveReview-01.md
  ZERO   SKIPPED    ### 7.1 R3 — 🔴 → ✅ CLOSED — s      ← paper20_scalar_rung.md
  ZERO   SKIPPED    ### 🔴 BLOCKER 1.1 — t              (glyph-first; not yet on disk)
  ZERO   in-scope   ### 1.1.2.3 — u                     (four dot-parts; correctly caught)
  ```
  So "finding-bearing docs minting zero: 8 → 0" is a measurement taken **inside the scope the check defines**, and the scope is defined by the property that drifts. The one form the check does catch (`### 1.1.2.3 —`) is the one the extractor's `{0,2}` dot-part limit rejects while the looser scope regex accepts — which is the shape the check was built for, and it works. Everything else in the class is invisible.
  Two residuals, both re-measured and both unchanged from round 10:
  - **The predicate is still `minted ≥ 1`,** so a document losing 19 of 20 headings passes. **Nine** in-scope documents currently mint fewer nodes than they have severity-marked headings, worst `2026-05-04-bundle-stage13/D1.md` at 1 minted / 9 severity headings — and there the loss is not the regex (7 of the 9 headings match it) but silent `seen_ids` de-duplication on a repeated section number within one document. `minted ≥ 1` cannot express either loss mode.
  - **`papers/D12/bundle_metadata.json:19` still reads `"stage13_review_doc": null`** beside `"last_stage13_review": "2026-07-31"`. Grepping `scripts/` for the key returns exactly one hit — `bundle_source_manifest.py:141`, which *writes* `None` at creation. Two canonical documents mandate the field (`BUNDLE_DIRECTORY_SCHEMA.md:86`, `BUNDLE_LIFT_PROCEDURE.md:243`) and one roadmap repeats it; no code reads it.
- **Expected:**
  (a) Scope on the document's own **front matter**, not on its headings: `reviewer:` present in a YAML block is what distinguishes a review from an aggregation, and it cannot share a blind spot with the extractor. Have `bundle_readiness.py` stamp `generated_by: bundle_readiness.py` on its aggregations and exempt on that key.
  (b) Strengthen the predicate from `minted ≥ 1` to `minted == (count of headings carrying a severity marker)`, computable from the same two inputs, which catches both partial-regex loss and `seen_ids` collisions.
  (c) Make the `seen_ids` collision loud: a duplicate `finding_id` within one document is a malformed document, not a node to drop silently.
  (d) Either make `stage13_review_doc` mandatory-and-read, or delete it from all four documents. A field three canonical documents require and no code reads is worse than no field.
- **Fix:** One front-matter scope predicate, one equality instead of an inequality, one warning on collision.
- **Cache:** n/a

### 8.3 — 🟡 REQUIRED — `readiness_submission_gate`'s `ImportError` handler still returns `passed=True`, and the AST sweep still finds twelve such handlers

- **Gate:** FixPropagation
- **Location:** `scripts/validate.py:4431` (and 11 siblings)
- **Observed:** Unchanged:
  ```python
  try:
      from build_graph import build_graph_json
  except ImportError as exc:
      return CheckResult(passed=True, details=[
          Detail("import", True, f"build_graph not available ({exc}); skipping", warning=True)])
  ```
  Ten lines below, the `if not gates:` branch that round 8 corrected reads "FAIL, not warn … Zero gate nodes is not 'no problems found'". The same function holds both postures.
- **Evidence:** I re-ran the AST sweep of `scripts/validate.py` for `CheckResult(passed=True)` returns inside `except` handlers: **12 hits**, at lines `623, 1956, 1959, 1971, 2112, 2381, 4093, 4431, 5382, 7008, 7016, 7292` — the identical set, line numbers shifted by the round-10 edits. Sixth consecutive round in which "not evaluated is spelled passed" is corrected at the site a finding named and nowhere else.
- **Expected:** `passed=False` here; then run the sweep as a test so the class closes instead of being repaired one site per round.
- **Fix:** Three lines plus one test.
- **Cache:** n/a
- **Lane:** `infra`

### 8.4 — 🟡 REQUIRED — `ledger_ids_resolve` still fails open on its own exception, and after 8.1 the ledger is the only thing that can close a blocker

- **Gate:** FixPropagation
- **Location:** `scripts/validate.py:3420-3423`
- **Observed:** Unchanged:
  ```python
  except Exception as exc:  # pragma: no cover
      roster_details.append(Detail(
          "ledger_ids_resolve", True,
          f"ledger integrity scan skipped ({type(exc).__name__}: {exc})", warning=True))
  ```
- **Evidence:** Forty lines earlier the sibling handler reads "FAIL, not pass: this guard exists to make invisible findings visible, so a build error in the scan must not be indistinguishable from a clean scan". Both were shipped in the same commit and only one was corrected. The path is live: the scan reads `_g`, bound inside the *previous* `try` block, so any failure of the graph build raises `UnboundLocalError` here and this handler swallows it. `ledger_ids_resolve` is the only referential guard on the ledger, and under 8.1's fix the ledger becomes the sole status-transition channel for every severity. A guard on the closure channel that reports green when it did not run is the wrong shape for that job.
- **Expected:** `passed=False` on this handler, and `_g` computed once outside both blocks so the dependency is explicit.
- **Fix:** Two lines.
- **Cache:** n/a

### 8.5 — 🟡 REQUIRED — restated as requested: 27 blocking-severity `accepted` findings remain invisible to Gate 11, and after the round-10 repair `accepted` is now strictly the cheapest closure in the system

- **Gate:** FixPropagation
- **Location:** `scripts/readiness_gates.py::_eval_fix_propagation`; `scripts/validate.py::check_accepted_findings_carry_rationale`
- **Observed:** Measured on the live graph this round: **1292** `ReviewFinding` nodes, **122** at `status: accepted`, of which **27** are at blocking severity — **8 critical, 19 major** — the same 27 rounds 8, 9 and 10 counted. `_eval_fix_propagation` partitions `accepted` out of `open_findings` before the severity filter runs, so none of the 27 enters `r.blockers`, sets `state = 'blocked'`, or drives `needs-recheck`. They appear only inside a parenthetical in the evidence line.
- **Evidence:** The precise restatement you asked for is that **round 10's repair inverted the incentive rather than leaving it unchanged.** Before it, both paths were unguarded and neither bar bound. Now:
  ```
  close a blocking finding as `fixed`     → status=fixed + >=40 chars rationale + (commit or date)
  close a blocking finding as `accepted`  → status=accepted + >=40 chars rationale.  Nothing else.
  ```
  and only the second removes the finding from the gate's blocking set. The rationale floor works and I re-ran it: 140 accepted ledger records, **0** below 40 characters. So the written-justification half is genuinely enforced; what is not enforced is a provenance anchor, an owner, or gate visibility. For **D12 specifically the exposure is nil** — all 24 D12 ledger records are `status: fixed` and none is `accepted` — so this neither helps nor hurts this bundle's gate today. It is a project-wide surface on the gate D12 is blocked at, and it is the mechanism 8.1 leaves open.
- **Expected:** Require a blocking-severity `accepted` record to carry `accepted_by` and a review date in addition to its rationale; surface the population in `GateResult.notes` and a dedicated `r.accepted_blockers` list so the dashboard reads "0 open, 27 accepted at blocking severity".
- **Fix:** One notes string, one list field, one validator condition.
- **Cache:** n/a
- **Lane:** `infra`

### 8.6 — 🟡 REQUIRED — restated as requested: `LATE_PHASE6_ABSORPTION_PROTOCOL.md:184` states a clearing rule that two writers violate on every run, and it is quoted into `READINESS_GATES.md:258`

- **Gate:** FixPropagation
- **Location:** `docs/LATE_PHASE6_ABSORPTION_PROTOCOL.md:184`; `docs/READINESS_GATES.md:258`; `scripts/check_bundle_source_freshness.py:201-206`; `scripts/bundle_append.py:318`
- **Observed:** The doc line, verbatim: "**Update:** `bundle_metadata.json.stage{9,10,13}_status` updated; `freshness_stale` cleared once `blockers_open == 0` and all three statuses GREEN." `READINESS_GATES.md:258` repeats it as "`freshness_stale` clears only once `blockers_open == 0` and all three stage statuses are GREEN". The code:
  ```python
  # Clear stale flag if previously set
  if md.get("freshness_stale"):
      md["freshness_stale"] = False
  ```
  in the `else` branch of a **source-mtime** comparison, unconditionally, with no reference to blockers or stage statuses; and `bundle_append.py:318` clears it on every lift with the comment "we just lifted; no longer stale".
- **Evidence:** The field's actual meaning is "a source paper is newer than `last_lift`", which is independent of blocker count. `papers/D12/bundle_metadata.json` currently reads `freshness_stale: false` beside `blockers_open: 49` and `readiness: "RED"` — correct data under the code's definition, and a direct contradiction of the two canonical documents. This is the residual round 10 identified after withdrawing its own finding, and it is a reviewer-error generator, not merely untidy: I am instructed to treat `READINESS_GATES.md` as defining what "correct" means, and it defines an invariant nothing holds. It produced two wrong findings from round 8/9 and one wrong remediation from you; it will produce a third from the next fresh-context reviewer, who has no way to know the doc is the unreliable artifact. I re-verified D12's blob is internally consistent otherwise: `blockers_open: 49`, `advisories_open: 65`, `open_findings: 114`, `blocked_p1_gates: ["FixPropagation", "ParameterProvenance"]`, `readiness: "RED"` — all matching the gate evaluation I ran independently, and `bundle_metadata_matches_graph` passes.
- **Expected:** Pick one, and (a) is right. (a) Delete the clearing condition from both documents and state the field's actual definition — "set when a source paper is newer than `last_lift`; cleared on the next lift" — and move the Stage-F exit gate onto `stage13_redo_required`, which `bundle_append.py` already sets on lift and which no other writer clears. (b) Implement `:184` and delete the mtime clearer. Overloading an input signal with an exit condition is what made this ambiguous.
- **Fix:** Two doc lines, or one field rename.
- **Cache:** n/a

### 8.7 — 🟡 REQUIRED — no supersession record exists for any D12 finding from rounds 7–10, so 49 blocking findings flag D12 and even the one you demonstrably fixed this round stays open

- **Gate:** FixPropagation
- **Location:** `docs/review_finding_supersessions.json`; graph node `paper:D12`
- **Observed:** `paper:D12` carries **127** incoming `FLAGS` edges: **114 open, 13 fixed, 0 accepted**, of which **49 are open at blocking severity**. Gate 11 reads `blocked — 49 open review findings at severity blocker/critical/major (114 open in total)`. The open blocking population by source document:
  ```
  15  2026-07-31-2022-internal-adversarial/D12.md   (round 10)
  11  2026-07-31-1823-internal-adversarial/D12.md   (round  9)
  10  2026-07-31-1730-internal-adversarial/D12.md   (round  8)
   8  2026-07-31-1524-internal-adversarial/D12.md   (round  7)
   4  2026-07-31-1210-reconstructed-round6/D12.md
   1  2026-07-31-0829-internal-adversarial/D12.md
  ```
  All 24 D12 ledger records target the two oldest documents. **Zero** records exist for rounds 7, 8, 9 or 10.
- **Evidence:** This is not a machinery defect — it is the machinery working — but it is a live obstruction to the gate and worth stating plainly because the fix loop has drifted from `BUNDLE_LIFT_PROCEDURE.md` §11 ("Author fixes → supersession ledger append → re-invoke same reviewer fresh context"). The ledger append step is being skipped. Concretely: round-10's 6.1 is genuinely and correctly repaired in the draft at `89d10728`, I verified the repair independently above, and its `ReviewFinding` node is still `open` and still counted among the 49 — because nothing wrote a record. `READINESS_GATES.md:196` requires "every finding marked `fixed` [to have] a SUPERSEDES edge … or a documented commit reference"; a fix with neither is invisible to the gate in exactly the way an unfixed one is. D12 cannot leave RED on this gate by fixing findings alone.
- **Expected:** Append one ledger record per closed finding for rounds 7–10, each carrying `status`, ≥40 characters of evidence and the commit that contains the fix (as you did for the earlier rounds, and verified per-artifact from `git log` — I re-checked all 13 D12 records that name a commit and every one resolves to a real object with a plausible subject).
- **Fix:** Ledger appends; no code change.
- **Cache:** n/a
- **Lane:** `infra`

### 8.8 — 🔵 RECOMMENDED — the `_SEV_GLYPHS` repair is real, but the file-level `BLOCKER` escalation beside it still rewrites every finding in a document to critical

- **Gate:** FixPropagation
- **Location:** `scripts/build_graph.py:1630-1648`
- **Observed:** Your E4 repair is genuine and I verified it by measurement rather than by reading: for all eleven recent D11/D12 review documents the minted severity distribution now equals the heading-glyph distribution exactly (round-10 D12: 3 critical / 12 major / 14 minor + 1 advisory, against 3 🔴 / 12 🟡 / 14 🔵 declared and one glyph-free heading). Heading-authoritative severity works. But immediately below it:
  ```python
  if _BLOCKER_RE.search(heading) or _BLOCKER_RE.search(body[:1000]):
      severity = 'critical'
  elif file_has_critical_marker:
      severity = 'critical'
  ```
  where `file_has_critical_marker = bool(_BLOCKER_RE.search(source))` over the whole file and `_BLOCKER_RE` matches an asterisk-prefixed BLOCKER token (bare or bolded) or an explicit `severity:` declaration of criticality. (Both literal forms are omitted from this document on purpose — see the demonstration below.)
- **Evidence:** Both branches set `critical`, and the comment's promise — "all findings inherit critical severity unless they explicitly downgrade in body" — has no implementation; there is no downgrade path. So a single asterisk-bolded BLOCKER token anywhere in a review document rewrites **every** finding in that document to `critical`, glyph and heading notwithstanding, which then routes all of them through 8.1's blocking-closure bar. **I demonstrated this on this very document.** My first draft quoted the two literal marker forms inside this finding's own evidence; extracting it minted **34 findings, all 34 `critical`**, against 2 🔴 / 14 🟡 / 17 🔵 declared in the headings — a 32-finding false escalation on D12's already-blocked Gate 11, produced by nothing but quoting a regex. I removed the two literals and re-extracted; severity now tracks the glyphs exactly. It has not fired on the recent corpus only because these reports write `🔴 BLOCKER` unbolded, and a reviewer who quotes the anchor list's own sentence about unhedged first-claims would escalate their entire report. The body-slice branch has the same shape as the glyph bug you just repaired: a finding that *mentions* another finding's severity inherits it.
- **Expected:** Delete the `file_has_critical_marker` fallback; restrict `_BLOCKER_RE` escalation to the heading, matching the glyph rule you just landed. Retired entirely by 8.1's (3b).
- **Fix:** Four lines.
- **Cache:** n/a

### 8.9 — Class 8 ProductionRunHealth: PASS, no finding for D12

D12 makes no simulation-evidence claim. I grepped the draft case-insensitively for "Monte Carlo", "numerical evidence", "simulation", "simulated" and "we simulate" — **zero** occurrences; every quantitative statement is a kernel-checked rational enclosure or a closed-form evaluation, and I recomputed all of them. The graph carries 18 `ProductionRun` nodes and **zero** edges of any type touching `paper:D12` (`D12 OUT: CLAIMS 1, DEPENDS_ON 1, HAS_FIGURE 3, CITES_SOURCE 14`; `D12 IN: FLAGS 127`), which is correct for a pure-formalization bundle. Aside, unchanged and not a D12 finding: 17 of the 18 `ProductionRun` nodes carry `status: unknown`, a blocking status under Gate 8 for any paper that ever links one.

---

## Verification of the repairs you asked me to attack

| item | verdict | how I checked it |
|---|---|---|
| **8.1 — the guard runs after the ledger override** | **placement correct; scope wrong — the two-key bypass survives at `accepted`** | Mutation-tested eight ledger shapes in a temp tree, deliberately extending past your six. Your six (all `status: fixed`) behave exactly as you describe. `{"finding_id": X, "status": "accepted"}` — two keys, no rationale, no anchor — emits `accepted`, and `_eval_fix_propagation` partitions `accepted` out before the severity filter, so it removes a 🔴 BLOCKER from Gate 11. `{"status": "closed"}` writes an arbitrary token no consumer validates. The bar itself is sound and no longer inert: 8 blocking findings are now reopened with `blocking_closure_rejected`, 5 of them D12's, including round-10's own 8.1. Your schema-tolerance judgement was right — 0 of 805 `fixed` records lack an anchor, so requiring `commit` would have reopened 629 well-formed closures. Finding 8.1, with the (b) specification you asked for. |
| **8.2 — content scoping** | **the scope shares the extractor's blind spot; six real documents skipped** | The extractor widening is genuine: 1190 → 1292 findings, and all seven forms you enumerated now mint. But `_FINDING_SHAPED_HEADING` requires `<digits><dash>`, the exact fragment that drifts, so the six documents that mint zero are precisely the six the check skips — 47 severity-marked headings including four declared BLOCKERs in `lean_project_audit.md`, four 🔴 in `CitationReview-01.md` and a CRITICAL in `2026-06-08-bundle-stage13/L2.md`. Four of their heading forms are already on disk (`### 1. Paper 7 —`, `### 1. Class 6 BLOCKER —`, `### BLOCK-1 (…) —`, `### I.1 Count …`), plus glyph-first `### 🔴 BLOCKER 1.1 —`. "8 → 0" is measured inside the scope the check defines. Finding 8.2. |
| **6.1 — the capstone binders** | **the fix is right; "six" is still not complete** | §5's two new sentences are accurate and I re-derived both falsifiers rather than accepting the rename-to-underscore test, which only shows the proof *uses* the binders. Dropping `hμ` lets `Δμ` exceed `budget·σ`; dropping `hσV` lets `σ` collapse; both falsify. §1's replacement is true, and I verified its load-bearing half at closure level. But the theorem has thirteen binders and `hs : IntervalIntegrable (fun x => s x ^ 2) volume 0 Tw` is a seventh load-bearing one — explicit counterexample `s = x^{-1/2}`, `hf = x^{1/2}` on `[0,1]`, where `IsAdmissibleFilter` holds, `matchedBudget` collapses to the junk `0`, and the conclusion is false. `hμle` I checked and it is **not** load-bearing here, so the finding is exactly one binder wide. Finding 6.1 (REQUIRED, with the escalation reasoning on record). |
| **`_SEV_GLYPHS` (E4)** | **genuine, verified by measurement** | Minted severity now equals declared heading-glyph severity for all eleven recent D11/D12 reports. The sibling `_BLOCKER_RE` file-level escalation is untouched and rewrites every finding in a document to critical on one asterisk-bolded BLOCKER token — I tripped it on this document's own first draft (34 findings, all 34 critical, against 2/14/17 declared) and then neutralized it. Finding 8.8. |
| **ledger commit corrections** | **verified** | All 13 D12 ledger records naming a commit resolve to real objects with plausible subjects; 0 dangling. |
| **the `page` first-page form** | **verified** | `IrwinHilton2005.page == "63"`, matching the bibitem's `p.~63`; `find_unregistered_citations` matches by substring, so the range form would indeed be unmatchable. Correct call. |
| **round-10 REQUIREDs and RECOMMENDEDs** | **all REPRODUCE** | `git show 89d10728 -- papers/D12/paper_draft.tex` touches exactly two hunks (§1 `:85-93`, §5 `:662-674`). Every other paper-side finding from round 10 is byte-identical: 1.1, 1.2, 1.3, 2.1–2.6, 3.1, 3.2, 4.1, 5.1–5.6, 6.2, 6.3, 7.1, 7.2, 8.3, 8.4, 8.5, 8.6. Re-filed above with fresh measurements, not inherited text. |

## Compliance note

I ran no git-mutating command — only `git log`, `git show`, `git status`,
`git cat-file`, `git rev-parse`. I ran no `pdflatex` inside `papers/`; the only
compile was `uv run python scripts/compile_bundle_pdf.py D12`, which rewrote the
gitignored `papers/D12/paper_draft.pdf`, and I judged it from the rendered PDF
pages, not the log. All mutation testing was in-process against a monkeypatched
`build_graph.PROJECT_ROOT` in a `tempfile.mkdtemp()` tree, which was removed;
**no repository file was mutated by it.** Files I wrote: this report, and two
verification records appended to `docs/citation_verifications.jsonl`
(`Degenne2025Kernels`, `MeiburgLessaSoldati2025`) — that file was backed up to
the scratch directory before the append. `lean/lean_deps.json.hash` and the 132
`figures/*.png` were already dirty on arrival and I did not touch them.

## QI Candidate

**Systemic: a guard's scope predicate must not be able to share a blind spot with
the thing it guards — and twice now, in two different files, it has.**

This round found the same structural error in two independent places, and both
had just been repaired:

1. `review_docs_mint_findings` asks "does this document look like it carries
   findings?" with a regex requiring `<digits><dash>` — the exact heading
   fragment whose drift is the failure it exists to detect. Result: the six
   documents that mint zero are the six it skips.
2. `build_graph`'s blocking-closure bar asks "was this closure justified?" only
   when `status == 'fixed'` — while the status that actually removes a finding
   from the gate is `accepted`, which the bar never inspects.

In both cases the repair was verified by enumerating cases *the repaired code
handles*, and in both cases the surviving hole is one token away from a case that
was enumerated. That is not carelessness; it is the predictable consequence of a
guard and its subject being written by the same author in the same sitting from
the same mental model.

Proposed, as a testable rule rather than an exhortation: **every guard added in
response to a finding must ship with an adversarial test whose input is generated
from the complement of the guard's own predicate, not from examples.** For 8.2:
enumerate the heading forms present on disk mechanically and assert
`extractor_matches == scope_matches` for every one, so the two regexes cannot
diverge silently. For 8.1: enumerate the distinct `status` values present in the
ledger mechanically and assert every one is either in a closed legal set or
rejected, so a new token cannot appear unguarded. Both are ten-line tests and
both would have failed on the code shipped at `89d10728`.

**Also, carried forward and each measured above:**

1. **Five of D12's eleven gates still pass on empty evidence sets** —
   `CrossPaperConsistency` (0 CONTRADICTS edges exist project-wide),
   `ComputationCorrectness` (4 of 27, from a hand-written list),
   `AssumptionDisclosure` (0 ASSUMES edges, for a paper with a
   hypothesis-disclosure section), `NarrativeGrounding` (0 ProseClaim links, for
   a 26-line abstract of claims), `NumericalFreshness` (27 derived numerals in
   the prose). Finding 6.1 is again the concrete cost: a load-bearing
   undisclosed binder under a gate whose evaluator has nothing to evaluate.
   `GateResult` should gain an `evidence_count`, and a gate evaluating on an
   empty set should report `open` — already in the status model — rather than
   `passed`.
2. **"Not evaluated" is still spelled "passed", sixth round running** (8.3). The
   AST sweep finds the identical 12 handlers. Make the sweep a test; the
   one-site-per-round cadence has now cost six rounds.
3. **Canonical documents remain a source of reviewer error** (8.6, 8.2, 1.3).
   Three still state things the code does not do:
   `LATE_PHASE6_ABSORPTION_PROTOCOL.md:184` (a clearing rule two writers violate
   on every run, quoted into `READINESS_GATES.md:258`),
   `BUNDLE_DIRECTORY_SCHEMA.md:86` (a field three documents require and no code
   reads), and `READINESS_GATES.md:33` (an `arxiv_verified` criterion no D12
   entry meets). A fresh-context reviewer is instructed to treat these as
   defining "correct", so each divergence manufactures a wrong finding.
   Doc-vs-implementation divergence should itself be a validate leg wherever the
   doc names a field or a path.
