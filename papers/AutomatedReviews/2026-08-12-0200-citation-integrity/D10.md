---
paper: D10
reviewer: lead (mechanical sweep)
model: claude-opus-5
review_date: 2026-08-12T02:00:00Z
readiness_gates_version: 1
kind: citation-integrity
---

# Citation Integrity Sweep — D10

## Summary

Mechanical sweep of `papers/D10/paper_draft.tex` for keys cited in reader-visible prose with
no `\bibitem` defining them. **17 BLOCKER.** Every key below is present in
`CITATION_REGISTRY`, so this is a rendering gap, not a sourcing gap: the sources are known and
recorded, and the manuscript does not emit their bibliography entries. Each renders as an
undefined citation in the compiled PDF.

**Method.** LaTeX comments are stripped before matching, so a key surviving only in a
commented-out block does not count as cited. Matching is exact-key on the contents of
`\cite*{...}` and `\bibitem{...}`, never substring — a substring test on this corpus is
wrong, and produced a false finding during this same triage (`Crossley2017` matches
`Crossley2017II`). Reproduce with `scripts/check_undefined_citations.py`.

**Provenance.** Found during the triage of the open-critical backlog (ADR-012 P2), not by a
reviewer pass. Triage surfacing new defects is itself a result and is recorded in that ADR.

Gate affected: CitationIntegrity (1). Per Stage 13, citation findings of any kind are BLOCKER
at submission time, no exceptions.

### 1.1 — 🔴 BLOCKER — `buttiker1986` is cited with no `\bibitem`

`buttiker1986` appears in a `\cite` in reader-visible prose of `papers/D10/paper_draft.tex`, and no
`\bibitem{buttiker1986}` defines it, so the reference renders undefined. The key resolves in
`CITATION_REGISTRY`, so the repair is to emit the entry from the registry rather than to find
a source.
- **Severity:** blocker

- **Lane:** `prose`
- **Location:** `papers/D10/paper_draft.tex`
- **Gate:** CitationIntegrity (1)
- **Verify:** `uv run python scripts/check_undefined_citations.py D10 --key buttiker1986`

### 1.2 — 🔴 BLOCKER — `coqq2022` is cited with no `\bibitem`

`coqq2022` appears in a `\cite` in reader-visible prose of `papers/D10/paper_draft.tex`, and no
`\bibitem{coqq2022}` defines it, so the reference renders undefined. The key resolves in
`CITATION_REGISTRY`, so the repair is to emit the entry from the registry rather than to find
a source.
- **Severity:** blocker

- **Lane:** `prose`
- **Location:** `papers/D10/paper_draft.tex`
- **Gate:** CitationIntegrity (1)
- **Verify:** `uv run python scripts/check_undefined_citations.py D10 --key coqq2022`

### 1.3 — 🔴 BLOCKER — `datta1995` is cited with no `\bibitem`

`datta1995` appears in a `\cite` in reader-visible prose of `papers/D10/paper_draft.tex`, and no
`\bibitem{datta1995}` defines it, so the reference renders undefined. The key resolves in
`CITATION_REGISTRY`, so the repair is to emit the entry from the registry rather than to find
a source.
- **Severity:** blocker

- **Lane:** `prose`
- **Location:** `papers/D10/paper_draft.tex`
- **Gate:** CitationIntegrity (1)
- **Verify:** `uv run python scripts/check_undefined_citations.py D10 --key datta1995`

### 1.4 — 🔴 BLOCKER — `gksl1976` is cited with no `\bibitem`

`gksl1976` appears in a `\cite` in reader-visible prose of `papers/D10/paper_draft.tex`, and no
`\bibitem{gksl1976}` defines it, so the reference renders undefined. The key resolves in
`CITATION_REGISTRY`, so the repair is to emit the entry from the registry rather than to find
a source.
- **Severity:** blocker

- **Lane:** `prose`
- **Location:** `papers/D10/paper_draft.tex`
- **Gate:** CitationIntegrity (1)
- **Verify:** `uv run python scripts/check_undefined_citations.py D10 --key gksl1976`

### 1.5 — 🔴 BLOCKER — `hohenbergKohn1964` is cited with no `\bibitem`

`hohenbergKohn1964` appears in a `\cite` in reader-visible prose of `papers/D10/paper_draft.tex`, and no
`\bibitem{hohenbergKohn1964}` defines it, so the reference renders undefined. The key resolves in
`CITATION_REGISTRY`, so the repair is to emit the entry from the registry rather than to find
a source.
- **Severity:** blocker

- **Lane:** `prose`
- **Location:** `papers/D10/paper_draft.tex`
- **Gate:** CitationIntegrity (1)
- **Verify:** `uv run python scripts/check_undefined_citations.py D10 --key hohenbergKohn1964`

### 1.6 — 🔴 BLOCKER — `isabelleCBO` is cited with no `\bibitem`

`isabelleCBO` appears in a `\cite` in reader-visible prose of `papers/D10/paper_draft.tex`, and no
`\bibitem{isabelleCBO}` defines it, so the reference renders undefined. The key resolves in
`CITATION_REGISTRY`, so the repair is to emit the entry from the registry rather than to find
a source.
- **Severity:** blocker

- **Lane:** `prose`
- **Location:** `papers/D10/paper_draft.tex`
- **Gate:** CitationIntegrity (1)
- **Verify:** `uv run python scripts/check_undefined_citations.py D10 --key isabelleCBO`

### 1.7 — 🔴 BLOCKER — `kato1995` is cited with no `\bibitem`

`kato1995` appears in a `\cite` in reader-visible prose of `papers/D10/paper_draft.tex`, and no
`\bibitem{kato1995}` defines it, so the reference renders undefined. The key resolves in
`CITATION_REGISTRY`, so the repair is to emit the entry from the registry rather than to find
a source.
- **Severity:** blocker

- **Lane:** `prose`
- **Location:** `papers/D10/paper_draft.tex`
- **Gate:** CitationIntegrity (1)
- **Verify:** `uv run python scripts/check_undefined_citations.py D10 --key kato1995`

### 1.8 — 🔴 BLOCKER — `landauer1957` is cited with no `\bibitem`

`landauer1957` appears in a `\cite` in reader-visible prose of `papers/D10/paper_draft.tex`, and no
`\bibitem{landauer1957}` defines it, so the reference renders undefined. The key resolves in
`CITATION_REGISTRY`, so the repair is to emit the entry from the registry rather than to find
a source.
- **Severity:** blocker

- **Lane:** `prose`
- **Location:** `papers/D10/paper_draft.tex`
- **Gate:** CitationIntegrity (1)
- **Verify:** `uv run python scripts/check_undefined_citations.py D10 --key landauer1957`

### 1.9 — 🔴 BLOCKER — `lean4` is cited with no `\bibitem`

`lean4` appears in a `\cite` in reader-visible prose of `papers/D10/paper_draft.tex`, and no
`\bibitem{lean4}` defines it, so the reference renders undefined. The key resolves in
`CITATION_REGISTRY`, so the repair is to emit the entry from the registry rather than to find
a source.
- **Severity:** blocker

- **Lane:** `prose`
- **Location:** `papers/D10/paper_draft.tex`
- **Gate:** CitationIntegrity (1)
- **Verify:** `uv run python scripts/check_undefined_citations.py D10 --key lean4`

### 1.10 — 🔴 BLOCKER — `leanQI2025` is cited with no `\bibitem`

`leanQI2025` appears in a `\cite` in reader-visible prose of `papers/D10/paper_draft.tex`, and no
`\bibitem{leanQI2025}` defines it, so the reference renders undefined. The key resolves in
`CITATION_REGISTRY`, so the repair is to emit the entry from the registry rather than to find
a source.
- **Severity:** blocker

- **Lane:** `prose`
- **Location:** `papers/D10/paper_draft.tex`
- **Gate:** CitationIntegrity (1)
- **Verify:** `uv run python scripts/check_undefined_citations.py D10 --key leanQI2025`

### 1.11 — 🔴 BLOCKER — `levy1979` is cited with no `\bibitem`

`levy1979` appears in a `\cite` in reader-visible prose of `papers/D10/paper_draft.tex`, and no
`\bibitem{levy1979}` defines it, so the reference renders undefined. The key resolves in
`CITATION_REGISTRY`, so the repair is to emit the entry from the registry rather than to find
a source.
- **Severity:** blocker

- **Lane:** `prose`
- **Location:** `papers/D10/paper_draft.tex`
- **Gate:** CitationIntegrity (1)
- **Verify:** `uv run python scripts/check_undefined_citations.py D10 --key levy1979`

### 1.12 — 🔴 BLOCKER — `lieb1983` is cited with no `\bibitem`

`lieb1983` appears in a `\cite` in reader-visible prose of `papers/D10/paper_draft.tex`, and no
`\bibitem{lieb1983}` defines it, so the reference renders undefined. The key resolves in
`CITATION_REGISTRY`, so the repair is to emit the entry from the registry rather than to find
a source.
- **Severity:** blocker

- **Lane:** `prose`
- **Location:** `papers/D10/paper_draft.tex`
- **Gate:** CitationIntegrity (1)
- **Verify:** `uv run python scripts/check_undefined_citations.py D10 --key lieb1983`

### 1.13 — 🔴 BLOCKER — `lindblad1976` is cited with no `\bibitem`

`lindblad1976` appears in a `\cite` in reader-visible prose of `papers/D10/paper_draft.tex`, and no
`\bibitem{lindblad1976}` defines it, so the reference renders undefined. The key resolves in
`CITATION_REGISTRY`, so the repair is to emit the entry from the registry rather than to find
a source.
- **Severity:** blocker

- **Lane:** `prose`
- **Location:** `papers/D10/paper_draft.tex`
- **Gate:** CitationIntegrity (1)
- **Verify:** `uv run python scripts/check_undefined_citations.py D10 --key lindblad1976`

### 1.14 — 🔴 BLOCKER — `mathlib` is cited with no `\bibitem`

`mathlib` appears in a `\cite` in reader-visible prose of `papers/D10/paper_draft.tex`, and no
`\bibitem{mathlib}` defines it, so the reference renders undefined. The key resolves in
`CITATION_REGISTRY`, so the repair is to emit the entry from the registry rather than to find
a source.
- **Severity:** blocker

- **Lane:** `prose`
- **Location:** `papers/D10/paper_draft.tex`
- **Gate:** CitationIntegrity (1)
- **Verify:** `uv run python scripts/check_undefined_citations.py D10 --key mathlib`

### 1.15 — 🔴 BLOCKER — `meirWingreen1992` is cited with no `\bibitem`

`meirWingreen1992` appears in a `\cite` in reader-visible prose of `papers/D10/paper_draft.tex`, and no
`\bibitem{meirWingreen1992}` defines it, so the reference renders undefined. The key resolves in
`CITATION_REGISTRY`, so the repair is to emit the entry from the registry rather than to find
a source.
- **Severity:** blocker

- **Lane:** `prose`
- **Location:** `papers/D10/paper_draft.tex`
- **Gate:** CitationIntegrity (1)
- **Verify:** `uv run python scripts/check_undefined_citations.py D10 --key meirWingreen1992`

### 1.16 — 🔴 BLOCKER — `reedSimon2` is cited with no `\bibitem`

`reedSimon2` appears in a `\cite` in reader-visible prose of `papers/D10/paper_draft.tex`, and no
`\bibitem{reedSimon2}` defines it, so the reference renders undefined. The key resolves in
`CITATION_REGISTRY`, so the repair is to emit the entry from the registry rather than to find
a source.
- **Severity:** blocker

- **Lane:** `prose`
- **Location:** `papers/D10/paper_draft.tex`
- **Gate:** CitationIntegrity (1)
- **Verify:** `uv run python scripts/check_undefined_citations.py D10 --key reedSimon2`

### 1.17 — 🔴 BLOCKER — `survey2021` is cited with no `\bibitem`

`survey2021` appears in a `\cite` in reader-visible prose of `papers/D10/paper_draft.tex`, and no
`\bibitem{survey2021}` defines it, so the reference renders undefined. The key resolves in
`CITATION_REGISTRY`, so the repair is to emit the entry from the registry rather than to find
a source.
- **Severity:** blocker

- **Lane:** `prose`
- **Location:** `papers/D10/paper_draft.tex`
- **Gate:** CitationIntegrity (1)
- **Verify:** `uv run python scripts/check_undefined_citations.py D10 --key survey2021`
