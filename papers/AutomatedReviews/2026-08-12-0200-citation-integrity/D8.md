---
paper: D8
reviewer: lead (mechanical sweep)
model: claude-opus-5
review_date: 2026-08-12T02:00:00Z
readiness_gates_version: 1
kind: citation-integrity
---

# Citation Integrity Sweep — D8

## Summary

Mechanical sweep of `papers/D8/paper_draft.tex` for keys cited in reader-visible prose with
no `\bibitem` defining them. **17 BLOCKER.** Every key below is present in
`CITATION_REGISTRY`, so this is a rendering gap, not a sourcing gap: the sources are known and
recorded, and the manuscript does not emit their bibliography entries. Each renders as an
undefined citation in the compiled PDF.

**Method.** LaTeX comments are stripped before matching, so a key surviving only in a
commented-out block does not count as cited. Matching is exact-key on the contents of
`\cite*{...}` and `\bibitem{...}`, never substring — a substring test on this corpus is
wrong, and produced a false finding during this same triage (`Crossley2017` matches
`Crossley2017II`). Reproduce with `scripts/check_undefined_citations.py`.

**Provenance.** Found during the triage of the open-critical backlog (ADR-012 P2), not by a
reviewer pass. Triage surfacing new defects is itself a result and is recorded in that ADR.

Gate affected: CitationIntegrity (1). Per Stage 13, citation findings of any kind are BLOCKER
at submission time, no exceptions.

### 1.1 — 🔴 BLOCKER — `AKN1998` is cited with no `\bibitem`

`AKN1998` appears in a `\cite` in reader-visible prose of `papers/D8/paper_draft.tex`, and no
`\bibitem{AKN1998}` defines it, so the reference renders undefined. The key resolves in
`CITATION_REGISTRY`, so the repair is to emit the entry from the registry rather than to find
a source.
- **Severity:** blocker

- **Lane:** `prose`
- **Location:** `papers/D8/paper_draft.tex`
- **Gate:** CitationIntegrity (1)
- **Verify:** `uv run python scripts/check_undefined_citations.py D8 --key AKN1998`

### 1.2 — 🔴 BLOCKER — `AaronsonGottesman2004` is cited with no `\bibitem`

`AaronsonGottesman2004` appears in a `\cite` in reader-visible prose of `papers/D8/paper_draft.tex`, and no
`\bibitem{AaronsonGottesman2004}` defines it, so the reference renders undefined. The key resolves in
`CITATION_REGISTRY`, so the repair is to emit the entry from the registry rather than to find
a source.
- **Severity:** blocker

- **Lane:** `prose`
- **Location:** `papers/D8/paper_draft.tex`
- **Gate:** CitationIntegrity (1)
- **Verify:** `uv run python scripts/check_undefined_citations.py D8 --key AaronsonGottesman2004`

### 1.3 — 🔴 BLOCKER — `AharonovArad2011` is cited with no `\bibitem`

`AharonovArad2011` appears in a `\cite` in reader-visible prose of `papers/D8/paper_draft.tex`, and no
`\bibitem{AharonovArad2011}` defines it, so the reference renders undefined. The key resolves in
`CITATION_REGISTRY`, so the repair is to emit the entry from the registry rather than to find
a source.
- **Severity:** blocker

- **Lane:** `prose`
- **Location:** `papers/D8/paper_draft.tex`
- **Gate:** CitationIntegrity (1)
- **Verify:** `uv run python scripts/check_undefined_citations.py D8 --key AharonovArad2011`

### 1.4 — 🔴 BLOCKER — `BMPRV1999` is cited with no `\bibitem`

`BMPRV1999` appears in a `\cite` in reader-visible prose of `papers/D8/paper_draft.tex`, and no
`\bibitem{BMPRV1999}` defines it, so the reference renders undefined. The key resolves in
`CITATION_REGISTRY`, so the repair is to emit the entry from the registry rather than to find
a source.
- **Severity:** blocker

- **Lane:** `prose`
- **Location:** `papers/D8/paper_draft.tex`
- **Gate:** CitationIntegrity (1)
- **Verify:** `uv run python scripts/check_undefined_citations.py D8 --key BMPRV1999`

### 1.5 — 🔴 BLOCKER — `Brylinski2002` is cited with no `\bibitem`

`Brylinski2002` appears in a `\cite` in reader-visible prose of `papers/D8/paper_draft.tex`, and no
`\bibitem{Brylinski2002}` defines it, so the reference renders undefined. The key resolves in
`CITATION_REGISTRY`, so the repair is to emit the entry from the registry rather than to find
a source.
- **Severity:** blocker

- **Lane:** `prose`
- **Location:** `papers/D8/paper_draft.tex`
- **Gate:** CitationIntegrity (1)
- **Verify:** `uv run python scripts/check_undefined_citations.py D8 --key Brylinski2002`

### 1.6 — 🔴 BLOCKER — `DawsonNielsen2006` is cited with no `\bibitem`

`DawsonNielsen2006` appears in a `\cite` in reader-visible prose of `papers/D8/paper_draft.tex`, and no
`\bibitem{DawsonNielsen2006}` defines it, so the reference renders undefined. The key resolves in
`CITATION_REGISTRY`, so the repair is to emit the entry from the registry rather than to find
a source.
- **Severity:** blocker

- **Lane:** `prose`
- **Location:** `papers/D8/paper_draft.tex`
- **Gate:** CitationIntegrity (1)
- **Verify:** `uv run python scripts/check_undefined_citations.py D8 --key DawsonNielsen2006`

### 1.7 — 🔴 BLOCKER — `GilesSelinger2013` is cited with no `\bibitem`

`GilesSelinger2013` appears in a `\cite` in reader-visible prose of `papers/D8/paper_draft.tex`, and no
`\bibitem{GilesSelinger2013}` defines it, so the reference renders undefined. The key resolves in
`CITATION_REGISTRY`, so the repair is to emit the entry from the registry rather than to find
a source.
- **Severity:** blocker

- **Lane:** `prose`
- **Location:** `papers/D8/paper_draft.tex`
- **Gate:** CitationIntegrity (1)
- **Verify:** `uv run python scripts/check_undefined_citations.py D8 --key GilesSelinger2013`

### 1.8 — 🔴 BLOCKER — `Hietala2020VOQC` is cited with no `\bibitem`

`Hietala2020VOQC` appears in a `\cite` in reader-visible prose of `papers/D8/paper_draft.tex`, and no
`\bibitem{Hietala2020VOQC}` defines it, so the reference renders undefined. The key resolves in
`CITATION_REGISTRY`, so the repair is to emit the entry from the registry rather than to find
a source.
- **Severity:** blocker

- **Lane:** `prose`
- **Location:** `papers/D8/paper_draft.tex`
- **Gate:** CitationIntegrity (1)
- **Verify:** `uv run python scripts/check_undefined_citations.py D8 --key Hietala2020VOQC`

### 1.9 — 🔴 BLOCKER — `KMM2013` is cited with no `\bibitem`

`KMM2013` appears in a `\cite` in reader-visible prose of `papers/D8/paper_draft.tex`, and no
`\bibitem{KMM2013}` defines it, so the reference renders undefined. The key resolves in
`CITATION_REGISTRY`, so the repair is to emit the entry from the registry rather than to find
a source.
- **Severity:** blocker

- **Lane:** `prose`
- **Location:** `papers/D8/paper_draft.tex`
- **Gate:** CitationIntegrity (1)
- **Verify:** `uv run python scripts/check_undefined_citations.py D8 --key KMM2013`

### 1.10 — 🔴 BLOCKER — `KMM2013ancilla` is cited with no `\bibitem`

`KMM2013ancilla` appears in a `\cite` in reader-visible prose of `papers/D8/paper_draft.tex`, and no
`\bibitem{KMM2013ancilla}` defines it, so the reference renders undefined. The key resolves in
`CITATION_REGISTRY`, so the repair is to emit the entry from the registry rather than to find
a source.
- **Severity:** blocker

- **Lane:** `prose`
- **Location:** `papers/D8/paper_draft.tex`
- **Gate:** CitationIntegrity (1)
- **Verify:** `uv run python scripts/check_undefined_citations.py D8 --key KMM2013ancilla`

### 1.11 — 🔴 BLOCKER — `KitaevShenVyalyi2002` is cited with no `\bibitem`

`KitaevShenVyalyi2002` appears in a `\cite` in reader-visible prose of `papers/D8/paper_draft.tex`, and no
`\bibitem{KitaevShenVyalyi2002}` defines it, so the reference renders undefined. The key resolves in
`CITATION_REGISTRY`, so the repair is to emit the entry from the registry rather than to find
a source.
- **Severity:** blocker

- **Lane:** `prose`
- **Location:** `papers/D8/paper_draft.tex`
- **Gate:** CitationIntegrity (1)
- **Verify:** `uv run python scripts/check_undefined_citations.py D8 --key KitaevShenVyalyi2002`

### 1.12 — 🔴 BLOCKER — `Lewis2021VerifQC` is cited with no `\bibitem`

`Lewis2021VerifQC` appears in a `\cite` in reader-visible prose of `papers/D8/paper_draft.tex`, and no
`\bibitem{Lewis2021VerifQC}` defines it, so the reference renders undefined. The key resolves in
`CITATION_REGISTRY`, so the repair is to emit the entry from the registry rather than to find
a source.
- **Severity:** blocker

- **Lane:** `prose`
- **Location:** `papers/D8/paper_draft.tex`
- **Gate:** CitationIntegrity (1)
- **Verify:** `uv run python scripts/check_undefined_citations.py D8 --key Lewis2021VerifQC`

### 1.13 — 🔴 BLOCKER — `Meiburg2025PhysLib` is cited with no `\bibitem`

`Meiburg2025PhysLib` appears in a `\cite` in reader-visible prose of `papers/D8/paper_draft.tex`, and no
`\bibitem{Meiburg2025PhysLib}` defines it, so the reference renders undefined. The key resolves in
`CITATION_REGISTRY`, so the repair is to emit the entry from the registry rather than to find
a source.
- **Severity:** blocker

- **Lane:** `prose`
- **Location:** `papers/D8/paper_draft.tex`
- **Gate:** CitationIntegrity (1)
- **Verify:** `uv run python scripts/check_undefined_citations.py D8 --key Meiburg2025PhysLib`

### 1.14 — 🔴 BLOCKER — `Mukhopadhyay2024` is cited with no `\bibitem`

`Mukhopadhyay2024` appears in a `\cite` in reader-visible prose of `papers/D8/paper_draft.tex`, and no
`\bibitem{Mukhopadhyay2024}` defines it, so the reference renders undefined. The key resolves in
`CITATION_REGISTRY`, so the repair is to emit the entry from the registry rather than to find
a source.
- **Severity:** blocker

- **Lane:** `prose`
- **Location:** `papers/D8/paper_draft.tex`
- **Gate:** CitationIntegrity (1)
- **Verify:** `uv run python scripts/check_undefined_citations.py D8 --key Mukhopadhyay2024`

### 1.15 — 🔴 BLOCKER — `ReadRezayi1999` is cited with no `\bibitem`

`ReadRezayi1999` appears in a `\cite` in reader-visible prose of `papers/D8/paper_draft.tex`, and no
`\bibitem{ReadRezayi1999}` defines it, so the reference renders undefined. The key resolves in
`CITATION_REGISTRY`, so the repair is to emit the entry from the registry rather than to find
a source.
- **Severity:** blocker

- **Lane:** `prose`
- **Location:** `papers/D8/paper_draft.tex`
- **Gate:** CitationIntegrity (1)
- **Verify:** `uv run python scripts/check_undefined_citations.py D8 --key ReadRezayi1999`

### 1.16 — 🔴 BLOCKER — `RossSelinger2016` is cited with no `\bibitem`

`RossSelinger2016` appears in a `\cite` in reader-visible prose of `papers/D8/paper_draft.tex`, and no
`\bibitem{RossSelinger2016}` defines it, so the reference renders undefined. The key resolves in
`CITATION_REGISTRY`, so the repair is to emit the entry from the registry rather than to find
a source.
- **Severity:** blocker

- **Lane:** `prose`
- **Location:** `papers/D8/paper_draft.tex`
- **Gate:** CitationIntegrity (1)
- **Verify:** `uv run python scripts/check_undefined_citations.py D8 --key RossSelinger2016`

### 1.17 — 🔴 BLOCKER — `Selinger2015` is cited with no `\bibitem`

`Selinger2015` appears in a `\cite` in reader-visible prose of `papers/D8/paper_draft.tex`, and no
`\bibitem{Selinger2015}` defines it, so the reference renders undefined. The key resolves in
`CITATION_REGISTRY`, so the repair is to emit the entry from the registry rather than to find
a source.
- **Severity:** blocker

- **Lane:** `prose`
- **Location:** `papers/D8/paper_draft.tex`
- **Gate:** CitationIntegrity (1)
- **Verify:** `uv run python scripts/check_undefined_citations.py D8 --key Selinger2015`
