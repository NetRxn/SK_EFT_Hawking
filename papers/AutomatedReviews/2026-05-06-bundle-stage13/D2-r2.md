---
paper: D2
bundle_target: D2
reviewer: adversarial-reviewer
model: claude-opus-4-7
review_date: 2026-05-06T18:05:00Z
readiness_gates_version: 1
mode: bundle-aware-tier1
round: 2
supersedes: papers/AutomatedReviews/2026-05-06-bundle-stage13/D2.md
scope: Round-2 verification of round-1 BLOCKER closures (Class 1 cite resolution + Class 3 Lean-substance hedging) for §2.7 Schellekens chain
---

# Adversarial Review (Round 2) — D2 (bundle-aware, Tier 1)

## Summary

Round-2 verification of D2 round-1 BLOCKERs. Both round-1 BLOCKERs are
**closed cleanly via paper-prose softening** (the user did not rewrite
the Lean theorem; instead §2.7 was reframed to disclose the
predicate-level discharge transparently). Verdict: **YELLOW** —
all submission-blocking findings are remediated; one new minor
RECOMMENDED is introduced (Lean-source docstring leftover).
Round-1 REQUIREDs (5.1, 5.2) are fully addressed by the same prose
softening. Round-1 RECOMMENDED 1.2, 1.3, 6.1 remain in their
original status (advisory; non-submission-blocking). 2 BLOCKER
closed / 0 new BLOCKER / 0 new REQUIRED / 1 new RECOMMENDED.

## Findings

### V.1 — BLOCKER #1.1 (Round-1) — VERIFIED CLOSED — Bibkey resolution

- **Gate:** CitationIntegrity
- **Round-1 ledger:** `bundle-stage13:D2-2026-05-06-r1:blocker-1`
- **Status:** **fixed (verified)**
- **Verification:**
  - `grep -n "GarciaEtxebarria" papers/D2/paper_draft.tex` returns
    exactly 3 lines: `90:` (cite), `364:` (cite — formerly the
    defective `GarciaEtxebarriaMontero2019`), `926:` (bibitem).
    All three resolve to bibkey `GarciaEtxebarria2019`.
  - `grep -c "GarciaEtxebarriaMontero2019"` → 0. The undefined-bibkey
    cite is no longer present anywhere in the paper.
  - The bibitem at line 926 is the same `GarciaEtxebarria2019`
    bibitem (unmodified) pointing to JHEP 08, 003 (2019),
    arXiv:1808.00009 — fetched and verified `match` in round-1.
  - The fix used the **minimum-blast-radius path** (cite-side
    rename), not the registry-canonical path (bibitem rename).
    Round-1 ledger flagged this as acceptable. Note for follow-up:
    `src/core/citations.py:2090` still has registry key
    `GarciaEtxebarriaMontero2019`, so the in-paper bibkey
    `GarciaEtxebarria2019` and the registry bibkey diverge. This
    is a **citation-registry-vs-paper-bibkey drift** that does
    not block submission (the paper compiles cleanly; the registry
    is internal infrastructure) but is itself a small QI candidate
    for a future registry audit. Carrying as advisory only — not a
    finding.
- **Cache:** cache-verified (round-1 fetch of arXiv:1808.00009).

### V.2 — BLOCKER #3.1 (Round-1) — VERIFIED CLOSED — Lean-substance hedging

- **Gate:** LeanProofSubstance
- **Round-1 ledger:** `bundle-stage13:D2-2026-05-06-r1:blocker-2`
- **Status:** **fixed via prose-softening (verified)**
- **Verification:**
  - The Lean theorem body in `lean/SKEFTHawking/Schellekens/Chain.lean:82-86`
    is **unchanged** (still `True := trivial`). The fix path was
    NOT a Lean rewrite; it was a prose-side disclosure.
  - §2.7 line 390-393: "the constraint $24 \mid c_{-}$ is therefore
    not a project-internal modular-consistency requirement but a
    corollary, **at the level of the published classification**, of
    the $c=24$ holomorphic-VOA enumeration: any integer $c_{-}$
    realised by a strongly-rational holomorphic VOA in the
    Schellekens / Möller-Scheithauer class is a multiple of $24$
    **within the scope of those classifications**." The scope
    qualifiers ("at the level of the published classification",
    "within the scope of those classifications") are correctly
    applied at every claim site. The previously-flagged
    "**must be a multiple of 24** because no holomorphic VOA exists
    at the relevant fractional or sub-multiple values" overclaim
    (round-1 finding 5.2) is fully removed. `grep` for
    `must be a multiple` and `no holomorphic VOA exists` both
    return 0 hits.
  - §2.7 line 403-414 contains the load-bearing Lean-discharge
    disclosure: "**The current Lean discharge of the
    chain-of-classifications statement is via `trivial` on
    predicate fields whose substantive content is tracked outside
    the Lean library; the load-bearing $N_f \equiv 0 \pmod 3$
    result is established independently in §sec:modular via
    `GenerationConstraint.generation_mod3_constraint`, and this §
    does not depend on the Schellekens-chain Lean scaffold for its
    mathematical force.**" This is exactly the disclosure round-1
    asked for: it (a) discloses that the Lean body is `trivial`,
    (b) decouples §2.7's mathematical force from the scaffold,
    and (c) re-anchors the load-bearing 3-generation result on
    the substantively-proven `generation_mod3_constraint`.
  - "theorem-quality classification corollary" (round-1 finding
    5.1) is replaced at line 417 with "classification corollary
    tracked by a predicate-level Lean scaffold backed by recent
    published mathematical work" — promotion language is removed.
    `grep -c "theorem-quality" papers/D2/paper_draft.tex` → 0.
  - "the composed chain ships as <theorem>" → "the composed chain
    ships as **a predicate-level scaffold** <theorem>" at line 403.
- **Conclusion:** the round-1 LeanProofSubstance BLOCKER is closed
  by transparent disclosure. The artifact-vs-prose mismatch that
  triggered the BLOCKER is gone: prose now accurately describes
  what the Lean body discharges.
- **Cache:** N/A.

### V.3 — REQUIRED #5.1 (Round-1) — VERIFIED CLOSED

- **Gate:** NarrativeGrounding
- **Status:** **fixed (verified)**. Promotion language ("theorem-quality
  classification corollary") removed; replaced with hedged
  "classification corollary tracked by a predicate-level Lean
  scaffold." Aligns with V.2 closure.

### V.4 — REQUIRED #5.2 (Round-1) — VERIFIED CLOSED

- **Gate:** NarrativeGrounding
- **Status:** **fixed (verified)**. The unsourced "no holomorphic
  VOA exists at the relevant fractional or sub-multiple values"
  conflation is fully removed. Replacement at line 391-396 hedges
  to "any integer $c_-$ realised by a strongly-rational holomorphic
  VOA in the Schellekens / Möller-Scheithauer class is a multiple
  of $24$ within the scope of those classifications" with explicit
  scope-restriction (strongly-rational + weight-1-non-trivial
  subset; FLM moonshine flagged as not-applicable witness rather
  than counterexample). The replacement is exactly the
  scope-restricted form round-1 recommended.

### V.5 — RECOMMENDED #1.2 (Round-1) — UNCHANGED — Niemeier1973 cache

- **Gate:** CitationIntegrity (advisory)
- **Status:** **unchanged from round-1**. `Niemeier1973` registry
  entry at `src/core/citations.py:3603-3619` still has
  `doi_verified: False`, `arxiv: None`. Out of scope for round-2
  BLOCKER verification; advisory carry-over.

### V.6 — RECOMMENDED #1.3 (Round-1) — UNCHANGED — Cache append for new bibitems

- **Gate:** CitationIntegrity (advisory)
- **Status:** **unchanged from round-1**. The two `match` records
  for `Schellekens1993` (arXiv:hep-th/9205072) and
  `MollerScheithauer2024` (arXiv:2112.12291) are not yet appended
  to `docs/citation_verifications.jsonl`. Out of scope for round-2
  BLOCKER verification; advisory carry-over.

### V.7 — RECOMMENDED #6.1 (Round-1) — UNCHANGED — Mathlib-bordism scope

- **Gate:** AssumptionDisclosure (advisory)
- **Status:** **unchanged from round-1**. The §3 forward-reference
  to the Outlook bordism-scope-deferral disclosure is not added.
  Out of scope for round-2 BLOCKER verification.

### V.8 — 🔵 RECOMMENDED (NEW, round-2) — Lean docstring residual hedging discipline

- **Gate:** LeanProofSubstance (advisory)
- **Location:** `lean/SKEFTHawking/Schellekens/Chain.lean:76-77`
  (docstring of `schellekensChain_implies_24_divides_c_minus_iff_3_divides_N_gen`)
- **Observed:** The Lean docstring still contains
  "The substantive content: the substrate-classification framework
  now operates at the level of **theorem-quality classification
  corollaries** (Möller-Scheithauer 2024) rather than
  numerical/algebraic anchors." This is the same promotion phrasing
  the paper-side fix removed. The Lean docstring is not
  reader-visible from the paper PDF, but it is the canonical
  description for any future agent invoking
  `lean_hover_info` on this declaration; consistency between
  paper-side hedging and Lean-side docstring is the discipline.
- **Evidence:** `sed -n '70,90p' lean/SKEFTHawking/Schellekens/Chain.lean`.
- **Expected:** Replace "theorem-quality classification corollaries"
  in the docstring with the same hedge used in the paper
  ("classification corollaries tracked by predicate-level scaffolds
  pending substantive Lean discharge"). Add an explicit `True :=
  trivial` warning line in the docstring so any future tooling
  introspection sees the discharge state at the API surface.
- **Severity:** RECOMMENDED — does not block submission (Lean
  source docstrings are not part of the paper artifact); flagged for
  consistency with the project-wide preemptive-strengthening
  discipline.
- **Fix:** One-line edit to `Chain.lean:76-77`. Optionally also
  rename theorem (e.g., to
  `schellekensChain_predicate_scaffold`) so the symbol name no
  longer promises a biconditional ("iff") that the body does not
  assert. Theorem-rename is breaking for any external reference,
  so leave as-is unless a downstream rename pass is already
  scheduled.
- **Cache:** N/A.

## Verdict

**YELLOW (submission-ready pending advisory carry-overs).**

- 2/2 BLOCKERs closed cleanly.
- 2/2 REQUIREDs closed cleanly (by the same prose softening that
  closed BLOCKER #2).
- 1 new RECOMMENDED (V.8 Lean-docstring residual) introduced by the
  fix; non-submission-blocking.
- 3 round-1 RECOMMENDEDs (1.2, 1.3, 6.1) unchanged; non-submission-blocking.

Net: D2 §2.7 is now consistent end-to-end at the paper level. The
Lean substrate is unchanged but the paper transparently discloses
what it does and does not establish, and re-anchors the
mathematical load-bearing on the independently-proven
`GenerationConstraint.generation_mod3_constraint` rather than the
Schellekens-chain scaffold. This is the correct closure pattern.

## QI Candidate

**QI candidate (carry-over from round-1, refined):** the
predicate-scaffold-as-`True := trivial` pattern remains as
underlying Lean state. Round-2 closure is via prose disclosure,
not Lean rewrite. This is a legitimate fix path under the project's
hedging-discipline, but it accumulates an attack surface: any
future bundle absorption that re-uses the Schellekens-chain
substrate without re-reading §2.7's hedging will silently
re-introduce the over-claim. The systemic fix
(`validate.py` check that flags Lean theorems whose conclusion is
`True` and whose name promises substance) remains worth pursuing
in a Phase 6o++ infrastructure wave. Round-2 specifically observes
that the Lean docstring still carries the unhedged "theorem-quality
classification corollary" phrasing — see finding V.8 — which is
exactly the kind of residual that a `validate.py` Lean-docstring
hedging-consistency lint would catch.
