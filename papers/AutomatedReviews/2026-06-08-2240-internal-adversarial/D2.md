---
paper: D2
reviewer: adversarial-reviewer
model: claude-opus-4-8
review_date: 2026-06-08T22:40:00Z
readiness_gates_version: 1
---

# Adversarial Review — D2

## Summary

Six findings: 2 BLOCKER, 3 REQUIRED, 1 RECOMMENDED. Gates affected: NarrativeGrounding
(Gate 7), AssumptionDisclosure (Gate 6), NumericalFreshness (Gate 9), CitationIntegrity
(Gate 1, advisory only). The triggering change (`16 ∣ σ` now unconditional kernel-pure)
is verified independent and correct — the newly load-bearing Lean theorems
(`SmoothSpinManifold4.rokhlin`, `hasWeakIsotropicVector`, `weakIsotropic_rank_four`) are
substantive (Chevalley–Warning + Hensel + p-adic transfer) and kernel-pure
`{propext, Classical.choice, Quot.sound}` — so Class 3 is **clear, not a defect**. The
defects are all stale prose that under-describes a now-stronger artifact: D2 still frames
the Rokhlin `eight_dvd` field as a carried interface hypothesis with "no current Mathlib
substrate" (it is now a removed field / discharged theorem), and — separately, found during
the sweep — still calls `gapped_interface_axiom` the project's "one true axiom" (it was
converted to a tracked Prop on 2026-05-19; canonical `\axiomcount{}` is 0). **Not
submission-ready** until the stale Rokhlin and axiom-budget prose are corrected.

## Findings

### 5.1 — 🔴 BLOCKER — Abstract + intro + outlook describe `gapped_interface_axiom` as a live project axiom; it was converted to a tracked Prop and the canonical axiom count is 0

- **Gate:** NarrativeGrounding
- **Location:** `papers/D2/paper_draft.tex:74` (abstract), `:131-132` (intro), `:886-887`, `:978`, `:998-1000`, `:1058` (outlook/falsification)
- **Observed:** The paper repeatedly asserts a live axiom. Abstract L74: "with \totaltheorems{} theorems across \leanmodules{} modules with **one axiom** (\texttt{gapped\_interface\_axiom}…)". Intro L131-132: "one **true axiom** (\texttt{gapped\_interface\_axiom}…)". Outlook L886-887: "this is the **lone axiom** \texttt{gapped\_interface\_axiom} (in \texttt{SPTClassification}; 20 theorems plus **the 1 axiom**)". L978: "A single **true axiom**: \texttt{gapped\_interface\_axiom}". L998-1000: "The project's \texttt{lake build} **axiom tally remains 1**". L1058: "falsifies the project's **sole axiom**".
- **Evidence:** `lean/SKEFTHawking/SPTClassification.lean:6-9` and `:16` and `:219-226`: "packaged here as a **tracked-hypothesis Prop** (`TPFConjecture`) rather than a project-local axiom (**2026-05-19 posture change**…)" and "replaces the pre-2026-05-19 `axiom gapped_interface_axiom`". A grep of `lean/SKEFTHawking/` for `gapped_interface_axiom` returns only comment/docstring references — there is no live `axiom gapped_interface_axiom` declaration. `docs/counts.tex` (regenerated 2026-06-04): `\newcommand{\axiomcount}{0}`.
- **Expected:** The paper must report the current artifact state: zero project-local axioms; the 4+1D TPF gapped-interface statement is a tracked-hypothesis `Prop` (`TPFConjecture` in `SPTClassification`), consumed by downstream theorems as an explicit `(H : TPFConjecture)` hypothesis parameter, not asserted as an axiom. "axiom tally remains 1" is factually false against `\axiomcount{}=0`.
- **Fix:** Rewrite every "one/lone/single true axiom" reference to "one tracked-hypothesis Prop (`TPFConjecture`, formerly `axiom gapped_interface_axiom`, converted 2026-05-19)"; correct L998-1000 to "axiom tally is 0"; in the abstract replace the literal "one axiom" with the `\axiomcount{}` macro plus the tracked-Prop language (see also 9.1). The falsification claim (iii)/L1058 should say "falsifies the project's central tracked hypothesis `TPFConjecture`".
- **Cache:** n/a (Lean-source + counts.tex evidence)

### 5.2 — 🔴 BLOCKER — §2.4 frames the `8 ∣ σ` / `16 ∣ σ` content as an open classification problem "with no current Mathlib substrate"; both irreducible inputs are now discharged kernel-pure theorems

- **Gate:** NarrativeGrounding
- **Location:** `papers/D2/paper_draft.tex:367-378` (esp. L368-369 "The remaining input is solely the classification's *existence* statement", L373-376 "together with the two classical inputs **with no current Mathlib substrate** — Hasse–Minkowski … and the level-one theta-modularity …", L377-378 "**is assembled pending** multivariate Poisson summation")
- **Observed:** D2 says the `8 ∣ σ` algebraic half is still open, reducible to a classification-existence statement whose two pieces (Hasse–Minkowski, theta-modularity) have "no current Mathlib substrate" and are only "assembled pending" further infrastructure. This describes the pre-2026-06-08 state.
- **Evidence:** `lean/SKEFTHawking/RokhlinHMRankFour.lean:599-601` `eight_dvd_latticeSig` proves `8 ∣ latticeSig M` for **every** even unimodular form, unconditionally, via `eight_dvd_latticeSig_of_HM hasIsotropicVector`. `RokhlinFromHM.lean:54-56` `eight_dvd_latticeSig_of_HM` supplies the definite branch unconditionally via `eight_dvd_latticeSig_of_definite` (theta-modularity, **discharged**) and the indefinite branch via the **discharged** `hasIsotropicVector` (Hasse–Minkowski). `RokhlinHMRankFour.lean:579-592` `hasWeakIsotropicVector` discharges weak [HM] at every rank (rank≥5; rank 2; ranks 1,3 vacuous; rank 4 via Chevalley–Warning + Hensel `isotropic_padicInt_of_unit_det` + binary Hilbert reciprocity). `lean_verify` on `hasWeakIsotropicVector` and `weakIsotropic_rank_four`: both `{propext, Classical.choice, Quot.sound}`. `constants.py:2206-2208` `rokhlin_sigma_mod_16` status `'discharged'`; L2232-2242 "✅ DISCHARGED 2026-06-08: BOTH irreducible inputs are now kernel-pure THEOREMS … eight_dvd_latticeSig … and sixteen_dvd_latticeSig … are UNCONDITIONAL."
- **Expected:** §2.4 should state that both [HM] (Hasse–Minkowski, `hasIsotropicVector`) and [Θ] (theta-modularity, `eight_dvd_latticeSig_of_definite`) are now kernel-pure theorems with no Mathlib gap, hence `8 ∣ σ` (`eight_dvd_latticeSig`) and `16 ∣ σ` (`sixteen_dvd_latticeSig`, given the topological factor) are unconditional. The only remaining interface input is the genuinely-topological factor `2 ∣ σ/8` (Â-genus even / Arf=0).
- **Fix:** Replace L367-378 with the discharged-state description: cite `eight_dvd_latticeSig` / `sixteen_dvd_latticeSig` / `hasIsotropicVector` as theorems; delete "no current Mathlib substrate", "assembled pending multivariate Poisson summation", and "remaining input is solely the classification's existence statement". Retain only `2∣σ/8` as the outstanding topological interface input.
- **Cache:** n/a (Lean-source evidence)

### 6.1 — 🟡 REQUIRED — §2.4 prose cites a structure field `eight_dvd` that no longer exists on `SmoothSpinManifold4` (undisclosed/mis-disclosed assumption surface)

- **Gate:** AssumptionDisclosure
- **Location:** `papers/D2/paper_draft.tex:333-335` ("the algebraic residual is carried as the precise field \texttt{eight\_dvd}~$:~8\mid\mathrm{latticeSig}\,(\text{form})$, the isolated van der Blij bound")
- **Observed:** The paper tells the reader the `SmoothSpinManifold4` structure carries a load-bearing field `eight_dvd : 8 ∣ latticeSig form` as a tracked interface hypothesis. A reader auditing the assumption surface will conclude the `16 ∣ σ` result is conditional on a manifold-supplied `8 ∣ σ` field.
- **Evidence:** `lean/SKEFTHawking/SpinRokhlinInterface.lean`: the `structure SmoothSpinManifold4` now has only `rank`, `form`, `even_unimod`, `topo` — **the `eight_dvd` field is gone**. `8 ∣ σ` is a derived theorem `SmoothSpinManifold4.eight_dvd_sig` (`:= eight_dvd_latticeSig M.rank M.form M.even_unimod`), and the structure docstring states "The algebraic bound `8 ∣ σ` is **no longer an interface field** — it is now a kernel-pure theorem". `constants.py:2243-2244`: "The SmoothSpinManifold4 structure no longer carries the eight_dvd field — SmoothSpinManifold4.rokhlin (16|σ) is derived from even_unimod + topo (2|σ/8) ALONE."
- **Expected:** The paper must not represent a removed field as a carried assumption. The only structure-level hypotheses now are `even_unimod` (Wu's formula, topological) and `topo : 2 ∣ σ/8` (Â-genus even). `8 ∣ σ` is a theorem, not an input.
- **Fix:** Edit L333-335 to describe `even_unimod` + `topo` as the two structure fields, and `8 ∣ σ` as the derived theorem `SmoothSpinManifold4.eight_dvd_sig`. Severity REQUIRED rather than BLOCKER because the direction of error is conservative (the paper over-discloses an assumption that no longer exists), but the cited field name is non-existent in the artifact and will mislead a referee checking the Lean.
- **Cache:** n/a (Lean-source evidence)

### 9.1 — 🟡 REQUIRED — Abstract/outlook use inline count literals ("one axiom", per-module theorem counts) instead of canonical macros; "1 axiom" contradicts `\axiomcount{}=0`

- **Gate:** NumericalFreshness
- **Location:** `papers/D2/paper_draft.tex:74` ("one axiom"), `:887` ("the 1 axiom"), `:998-1000` ("axiom tally remains 1"); per-module literals at `:553` (48 theorems), `:615` (52), `:617` (30), `:743` (14), `:744` (28), `:768` (12), `:849` (24), `:857` (12), `:861` (12), `:887` (20), `:898` (20), `:920` (16)
- **Observed:** The paper hardcodes "one axiom" / "1 axiom" / "axiom tally remains 1" inline while the canonical `docs/counts.tex` macro `\axiomcount{}` expands to 0 — a direct numerical contradiction with the canonical source. The header comment (L10-13) instructs use of `\axiomcount{}` "instead of inline literals (NumericalFreshness Gate 9)", and D2 never uses the `\axiomcount{}` macro (only mentions it in the comment). Additionally ~12 per-module theorem-count literals are inline (not macro-backed); these were not flagged by `validate.py --check count_literals` (the checker targets canonical totals, not per-module counts) but are drift-prone.
- **Evidence:** `docs/counts.tex`: `\newcommand{\axiomcount}{0}`. `validate.py --check count_literals` passed (1/1, 17 warnings) but lists no D2 entry — confirming the per-module literals slip past the syntactic checker, so this is a latent freshness gap not caught upstream.
- **Expected:** Axiom count rendered via `\axiomcount{}` (→0) with tracked-Prop language (ties to 5.1). Per-module theorem counts should be macro-backed or moved into an autogen table per the Gate-9 remediation in READINESS_GATES.md.
- **Fix:** Replace "one axiom"/"1 axiom"/"axiom tally remains 1" with `\axiomcount{}`-backed phrasing; retrofit per-module counts into `papers/D2/tables.py` or `\input{}`-backed macros at submission.
- **Cache:** n/a

### 9.2 — 🔵 RECOMMENDED — Stale in-file editorial comment claims a GoltermanShamir2026 title that disagrees with both the bibitem and the registry

- **Gate:** NumericalFreshness / CitationIntegrity (advisory)
- **Location:** `papers/D2/paper_draft.tex:1234-1235` (LaTeX comment: "title corrected to \"Generalized no-go theorem for chiral fermions on a lattice\"")
- **Observed:** The 2026-05-04 cleanup comment asserts the GoltermanShamir2026 title was corrected to "Generalized no-go theorem for chiral fermions on a lattice", but the actual rendered bibitem (L1113) and `CITATION_REGISTRY['GoltermanShamir2026']` both carry the title "Symmetric mass generation and the Nielsen-Ninomiya theorem". The live bibitem and registry agree (so this is NOT a Gate-1 BLOCKER), but the editorial comment is internally contradictory and could mislead a future editor into "fixing" the correct title.
- **Evidence:** `src/core/citations.py` `GoltermanShamir2026`: `'title': 'Symmetric mass generation and the Nielsen-Ninomiya theorem'`, `'arxiv': '2603.15985'`, journal "arXiv preprint (contribution to Lattice 2025 proceedings)" — all matching the D2 bibitem. The comment's claimed title and the comment's claimed PRD coords ("PRD 113, 014503 (2026)") match neither the bibitem (which carries no DOI/PRD volume) nor the registry (`doi: None`, `volume: None`).
- **Expected:** Editorial comments should reflect the final state. Either delete the stale comment or correct it to match the registry/bibitem truth.
- **Fix:** Remove or update the L1233-1238 cleanup comment block to the registry's actual title and the arXiv-preprint venue.
- **Cache:** registry-verified (no fresh fetch required — bibitem ↔ registry agree; comment-only discrepancy)

### 1.1 — 🟡 REQUIRED — Citation registry lacks an explicit `arxiv_verified` boolean for D2's cited keys (cannot confirm Gate-1 fetch-freshness invariant programmatically)

- **Gate:** CitationIntegrity
- **Location:** `src/core/citations.py` (all 31 D2 bibkeys)
- **Observed:** All 31 D2 bibkeys resolve to `CITATION_REGISTRY` entries (good — no registry-absence BLOCKER). However, the registry entries expose `doi_verified` but no machine-readable `arxiv_verified` field; arXiv-verification is recorded only in free-text `notes` (e.g., GoltermanShamir2026 `notes`: "Verified 2026-05-11 via WebFetch arXiv:2603.15985"). Gate 1 passes-iff requires `arxiv_verified == True` per READINESS_GATES.md:35; that condition cannot be evaluated structurally for D2's keys.
- **Evidence:** `READINESS_GATES.md:35` ("Every registry entry has `arxiv_verified == True` AND `doi_verified == True`"). Parse of `citations.py` for D2 keys returns `doi_verified` present, `arxiv_verified` absent (only `notes`-text attestation). Change-relevant primary sources (Wang2024 arXiv:2312.14928, Rokhlin1952, DaiFreed1994) are registry-present; Wang2024/GS/GT/TPF all carry dated free-text WebFetch attestations.
- **Expected:** Either an `arxiv_verified` boolean per entry, or the gate evaluator must consume the `notes` attestation. As-is the gate's stated pass-criterion is unverifiable for D2.
- **Fix:** Add `arxiv_verified: True/<date>` to the registry schema for cited keys (mechanical), OR document that `notes`-attestation is the canonical evidence and update the gate evaluator. Flagged REQUIRED (draft-OK, blocks submission-time Gate-1 sign-off) per the protocol's "don't hide unknown" rule — not BLOCKER because every bibitem ↔ registry pair I could compare agrees on title/arXiv/venue.
- **Cache:** registry-verified for metadata agreement; arXiv re-fetch deferred (no live fetch performed this run — no `wrong_target` evidence surfaced, and the prioritized scope is the Rokhlin/axiom staleness)

## QI Candidate

**Systemic stale "single axiom" disclosure across the chirality bundle.** The
`gapped_interface_axiom → TPFConjecture` posture change (2026-05-19, `\axiomcount{}` 1→0)
was not propagated into paper prose. D2 (5.1) and `paper8_chirality_master/paper_draft.tex:459`
("the project's single axiom") both still describe a live axiom that no longer exists.
Companion drafts and any bundle that lifts SPTClassification content should be swept for
"single/lone/sole/one (true) axiom" language and for "axiom tally remains 1". Recommend a
`validate.py` check that greps paper `.tex` for axiom-count claims and diffs them against
`\axiomcount{}` in counts.tex (analogous to count_literals but axiom-specific), since the
current `count_literals` checker does not catch "one axiom" phrasing. Pairs with the
Rokhlin staleness as the same root cause: artifact discharged/strengthened, prose not refreshed.
