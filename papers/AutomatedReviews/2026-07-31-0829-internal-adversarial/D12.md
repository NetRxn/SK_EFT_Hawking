---
paper: D12
reviewer: adversarial-reviewer
model: claude-opus-5
review_date: 2026-07-31T08:29:00Z
readiness_gates_version: 1
---

# Adversarial Review — D12

## Summary

3 BLOCKER / 7 REQUIRED / 8 RECOMMENDED across gates CitationIntegrity,
ParameterProvenance, LeanProofSubstance, CrossPaperConsistency,
NarrativeGrounding, AssumptionDisclosure and NumericalFreshness. The substrate
built this round is real: I read every new declaration and the reversals are
earned — `gaussianQ_one_ge_rational` is arithmetically correct, the 22/25 ceiling
is genuinely non-degenerate, the γ ≤ 1 direction reversal is physically right and
its soundness argument holds, the PhysLib TODO claim is verbatim true at the pin,
and the three cross-layer composites are non-vacuous derived compositions. What
fails is the prose layer: three checkable falsehoods (an unsupported citation
title, a bound-tightness figure off by 25 points, and a Mathlib absence claim
refuted by one grep in the pinned checkout) plus a cluster of internal
contradictions in the scope section. D12 is **not** submission-ready; every
blocker is a mechanical text fix, none requires new substrate.

## Findings

### 1.1 — 🔴 BLOCKER — `LuccioliDegenne2024` bibitem asserts a title no verified source supports, and gives no URL

- **Gate:** CitationIntegrity
- **Location:** `papers/D12/paper_draft.tex:615-618`; `src/core/citations.py:10967-10990`
- **Observed:** The bibitem reads `L.~Luccioli and R.~Degenne, \emph{TestingLowerBounds: Information theory and hypothesis testing, in Lean} (2024).` The registry carries the same title. The registry note and the primary-source cache (`Lit-Search/Phase-6E/primary-sources/LuccioliDegenne2024.abstract.txt`) both state plainly that the repository was **never inspected** (github.com egress-blocked), and that everything known about this entry is quoted secondhand from `Degenne2025Kernels`.
- **Evidence:** I fetched arXiv:2510.04070 in full (PDF cached at `.../tool-results/webfetch-1785486282751-7h6cs7.pdf`). Its bibliography entry, line 1522 of the extracted text, reads verbatim:
  `[LD24] L. Luccioli and R. Degenne. TestingLowerBounds. 2024. URL: https://github.com/RemyDegenne/testing-lower-bounds.`
  The title in the only source the project can reach is the bare token **`TestingLowerBounds`**. The subtitle *"Information theory and hypothesis testing, in Lean"* appears nowhere in that primary; it first appears, unsourced, in `papers/D12/prior_art_novelty.md:145` and propagates into the registry and the bibitem. This is the invariant-#11 confabulation pattern (the paper40 incident) applied to a title field instead of an arXiv ID. Verdict recorded as `wrong_title`.
  Separately: the bibitem contains no URL, so the reference is unresolvable — for the work the paper itself calls *"the highest prior-art risk in this work."*
- **Expected:** A bibitem whose every field traces to a source the project actually read: title `TestingLowerBounds`, authors L. Luccioli and R. Degenne, year 2024, URL `https://github.com/RemyDegenne/testing-lower-bounds`, with the "as cited in Ref. [Degenne2025Kernels]" marker already present retained.
- **Fix:** Replace the title with `TestingLowerBounds`; add the URL; move the subtitle to a bracketed `[repository self-description, not independently verified]` gloss or delete it. Update `CITATION_REGISTRY['LuccioliDegenne2024']['title']` and the primary-source cache to match. Re-run the reviewer on class 1 only.
- **Cache:** fresh-fetch (record appended `2026-07-31T08:2x`, verdict `wrong_title`)

### 1.2 — 🔵 RECOMMENDED — `Mather1982` title/authors verified only by DOI redirect slug; landing page egress-blocked

- **Gate:** CitationIntegrity
- **Location:** `papers/D12/paper_draft.tex:610-613`
- **Observed:** No cache record existed for this bibkey at the current bibitem hash (`109c37205985…`). This is the citation that carries the whole γ ≤ 1 direction reversal, so it deserved a fresh check.
- **Evidence:** `https://doi.org/10.1364/AO.21.001125` → 302 → `https://opg.optica.org/abstract.cfm?URI=ao-21-6-1125`. The slug independently confirms journal `ao` (Applied Optics), **volume 21, issue 6, page 1125** — matching the bibitem's "Appl. Opt. **21**, 1125 (1982)". The landing page itself is denied by the egress guard (`opg.optica.org` not whitelisted), so the title string and author initial rest on the DOI resolution plus the hand-written cache file, not on a page that was read. Verdict `match`, evidence class explicitly partial.
- **Expected:** Same standard the bundle already applies to `Irwin1995` (best-verified: slug confirms title too).
- **Fix:** None required for submission; record the evidence class in the registry `notes` so a future reviewer doesn't re-derive it. Consider adding `opg.optica.org` to the greylist.
- **Cache:** fresh-fetch (record appended, verdict `match`, source `doi-resolved`)

---

### 2.1 — 🔴 BLOCKER — "at 95 % of truth" is wrong for the z = 1 Birnbaum–Feller bound; the true figure is 76 %

- **Gate:** ParameterProvenance (numerical drift)
- **Location:** `papers/D12/paper_draft.tex:495-496`; also `lean/SKEFTHawking/Detection/GaussianThreshold.lean:614` and `lean/SKEFTHawking/Control/CompositeReadoutCeilings.lean:611-612`
- **Observed:** The paper states the non-degenerate ceiling "rest[s] on \thm{gaussianQ\_one\_ge\_rational}, the rational enclosure $Q(1) \ge 3/25$ obtained from the Birnbaum--Feller bound **at $95\,\%$ of truth**."
- **Evidence:** Birnbaum–Feller at $z$ is $\frac{z}{1+z^2}\varphi(z)$. Computed:
  ```
  z=1  BF=0.120985  Q=0.158655  ratio=0.7626
  z=2  BF=0.021596  Q=0.022750  ratio=0.9493
  z=3  BF=0.001330  Q=0.001350  ratio=0.9849
  ```
  The 95 % figure is the **z = 2** ratio — correctly stated as such in `gaussianQ_two_ge_rational`'s own docstring ("`gaussianTail_birnbaum` at `z = 2` would give `(2/5)·φ(2) = 2.160e−2` — **95 % of truth**") — and was copied to the z = 1 site. At z = 1 the bound is 76.3 % of truth, and the shipped rational `3/25 = 0.12` is 75.6 % of $Q(1)=0.158655$. The claim overstates the tightness of the paper's own enabling constant by 25 percentage points.
  The **arithmetic of the theorem itself is correct**: $3/25 = 0.12 \le \tfrac12\varphi(1) = 0.120985$, and $1 - 3/25 = 22/25 = 0.88$, so the 22/25 ceiling and the refutation of a claimed 0.99 both stand. Only the quality claim is false.
- **Expected:** "at 76 % of truth", or drop the figure.
- **Fix:** Change `95\,\%` → `76\,\%` in `paper_draft.tex:496`; fix the same string in the two Lean docstrings so the source of truth and the paper agree.
- **Cache:** n/a

### 2.2 — 🟡 REQUIRED — the γ paragraph attributes to `Mather1982` a range and a closed form that come from the TES/Irwin–Hilton lineage, and the closed form has no pipeline artifact

- **Gate:** ParameterProvenance
- **Location:** `papers/D12/paper_draft.tex:393-405`
- **Observed:** Three separate sourcing problems in one paragraph.
  1. *"falling toward $1/2$ under strong loading~\cite{Mather1982}"*. The only Mather record the project holds (the cached abstract) says phonon noise is *"reduced by as much as 30 %"*. A 30 % reduction of the PSD is γ ≈ 0.7, not 1/2. The reconciliation — that Mather's 30 % is an *amplitude* reduction, hence γ ≈ 0.49 — is nowhere stated and is the reviewer's inference, not the paper's.
  2. The `[1/2, 1]` range and the closed form $\gamma = \frac{n}{2n+1}\frac{r^{2n+1}-1}{r^{n}-1}r^{-(n+1)}$ are the standard TES $F_{\rm link}$, whose canonical statement is in `IrwinHilton2005` — which is in the bibliography but **not cited at this sentence**. Mather is cited alone for both.
  3. As $r \to \infty$ the closed form tends to $n/(2n+1)$, which is $0.444$ at $n=4$ and $1/3$ at $n=1$ — i.e. γ falls **below** 1/2, so "falling toward 1/2" is not the asymptote for any $n$.
- **Evidence:** I recomputed the closed form independently: $\gamma(r{=}2, n{=}4) = \frac{4}{9}\cdot\frac{511}{15}\cdot 2^{-5} = 0.47315$, so the paper's "0.47" is right, and the form is algebraically identical to the standard detector-referred $F_{\rm link}(T_{\rm bath}/T_c, n)$ under $t = 1/r$ — I verified $F(0.5, 4) = 0.4732$ by the other route. **The number is correct; only the attribution and the asymptote wording are not.** Separately, neither the closed form nor the 0.47 evaluation exists as a `formulas.py` entry, a Lean theorem, a test, or a `PARAMETER_PROVENANCE` record — it lives only as prose in `Lit-Search/Phase-6E/primary-sources/Mather1982.abstract.txt`. `grep` over `src/core/provenance.py` returns no D12 parameter.
- **Expected:** Cite `\cite{Mather1982,IrwinHilton2005}` for the range and the closed form; state the asymptote as $n/(2n+1)$ rather than "toward 1/2"; either back the 0.47 evaluation with a computed artifact or label it explicitly as an illustrative hand evaluation of a quoted literature form.
- **Fix:** Two citation-key edits, one asymptote correction, one sentence of provenance labelling; optionally a three-line `formulas.py` function + golden test for $\gamma(r,n)$.
- **Cache:** n/a

---

### 3.1 — 🔵 RECOMMENDED — `correlated_pair_sigma_eq_sqrt_two_mul` is bare-real √ algebra; the paper describes it as quantifying a witness pair

- **Gate:** LeanProofSubstance
- **Location:** `papers/D12/paper_draft.tex:296-300`; `lean/SKEFTHawking/Detection/NEPAlgebra.lean:471-475`
- **Observed:** The paper: "\thm{quadrature\_uncorrelated\_hypothesis\_load\_bearing} exhibits a fully-correlated pair whose total power is twice what quadrature predicts, **with the discrepancy quantified exactly as $\sqrt{2}$ by** \thm{correlated\_pair\_sigma\_eq\_sqrt\_two\_mul}."
- **Evidence:** The theorem is `theorem correlated_pair_sigma_eq_sqrt_two_mul (S : ℝ) : √(4 * S) = √2 * √(S + S)`, proved by `Real.sqrt_mul`. It mentions neither `IsUncorrelatedAt`, nor `IsWhiteFilteredVariance`, nor the witness functionals `V`/`Vtot`, nor `sigma_eq_responsivity_nep_sqrt_enbw`. It is an identity about reals that would survive any edit to the witness. The load-bearing theorem is the neighbouring `quadrature_uncorrelated_hypothesis_load_bearing`, which *does* exhibit `V`, `Vtot` and `¬ IsUncorrelatedAt`, and whose numbers (4·S vs S+S) the identity happens to match. To the module's credit the docstring is honest about this ("Since the output noise goes as the square root of the PSD at fixed bandwidth …, the statement below is exactly the ratio of the two output noises"), but the *paper* elides that step. This is the project's own P3 anti-pattern ("multiplication-only theorems are not physics") and the pattern `CompositeReadoutCeilings.lean:502-505` explicitly warns against.
- **Expected:** Either state the √2 ratio at the witness's own `V`/`Vtot` (an instantiation, not a numeral identity), or reword the paper to "the corresponding output-noise ratio is $\sqrt2$, recorded arithmetically as \thm{correlated\_pair\_sigma\_eq\_sqrt\_two\_mul}".
- **Fix:** One-clause reword in `paper_draft.tex:299-300`, or a five-line corollary tying the identity to the witness.
- **Cache:** n/a

*(No other class-3 findings. I checked every one of the 150 `\thm{}` names in the draft against the tree: all resolve; none is in the current `PlaceholderMarker` set (593 markers, zero intersection); no cited theorem has an `rfl`/`trivial`/`Equiv.refl` body; the only ≤2-line bodies are definitions or genuine `le_trans`/`assignmentFidelity_le_of_floor` compositions. Kernel purity independently confirmed from `lean/lean_deps.json`: 576 declarations across the 13 modules, `axiom_deps_core ⊆ {propext, Classical.choice, Quot.sound}`, `axiom_deps_project = ∅`, no `sorry`/`native_decide`/`maxHeartbeats` in source.)*

---

### 4.1 — 🔵 RECOMMENDED — two registry keys for each of two primary sources shared with D10; D10's copies carry no arXiv ID

- **Gate:** CrossPaperConsistency
- **Location:** `src/core/citations.py` (`leanQI2025`, `isabelleCBO`, `MeiburgLessaSoldati2025`, `UnruhCBO2025`); `papers/D10/paper_draft.tex:86`
- **Observed:**
  ```
  leanQI2025               arxiv=None      "A Formalization of the Generalized Quantum Stein's Lemma in Lean"  used_in=[D10]
  MeiburgLessaSoldati2025  arxiv=2510.08672 "A Formalization of the Generalized Quantum Stein's Lemma in Lean"  used_in=[D12]
  isabelleCBO              arxiv=None      "Complex Bounded Operators in Isabelle/HOL"                          used_in=[D10]
  UnruhCBO2025             arxiv=2512.05878 "Complex Bounded Operators in Isabelle/HOL"                          used_in=[D12]
  ```
  D10 and D12 make the *same* carve-out about the *same* two works (D10:86 "already formalized in Lean's QuantumInfo layer, in Coq's CoqQ, and in Isabelle/AFP"; D12:501-505) under different keys, and the D10 copies are unverifiable (no arXiv, no DOI).
- **Evidence:** Registry dump above; `coqq2022` is the only key literally shared, and it agrees (2207.11350) in both.
- **Expected:** One registry entry per primary source. Gate 2's literal blocking condition ("a bibkey whose `arxiv_id` differs across papers") is not triggered, but the duplication defeats the "verified once, amortized" property the citation cache exists for — the D10 copies have never been arXiv-verified while the D12 copies have.
- **Fix:** Alias `leanQI2025 → MeiburgLessaSoldati2025` and `isabelleCBO → UnruhCBO2025` in the registry (or backfill the arXiv IDs into the D10 keys), and add a duplicate-title detector to `validate.py --check citation_registry_integrity`. See QI Candidate.
- **Cache:** cache-verified (both D12 keys `match`, fresh-fetched 2026-07-31)

---

### 5.1 — 🟡 REQUIRED — abstract and conclusion say **one** open prior-art check; introduction, §5, the bundle metadata and `prior_art_novelty.md` say **two**

- **Gate:** NarrativeGrounding
- **Location:** `papers/D12/paper_draft.tex:54` (abstract), `:90` (intro), `:542` (§5), `:587-588` (conclusion)
- **Observed:**
  - Abstract:54 — "… and **one** prior-art check remains open."
  - Intro:90 — "**Two** prior-art checks remain open and are flagged as blocking."
  - §5:542 — "\paragraph{**Two** open checks, flagged as blocking.}" — (i) TestingLowerBounds, (ii) Isabelle AFP + Coq `infotheo`.
  - Conclusion:587 — "And **one** prior-art check remains open: whether the \textsc{TestingLowerBounds} development carries a Le Cam average-error floor…"
- **Evidence:** `papers/D12/bundle_metadata.json` notes: "TWO PRIOR-ART CHECKS REMAIN OPEN AND BLOCK SUBMISSION". `papers/D12/prior_art_novelty.md:141` "⛔ TWO BLOCKING PRE-SUBMISSION CHECKS". `paper_draft.tex:12` header comment: "⛔ TWO BLOCKING PRE-SUBMISSION PRIOR-ART CHECKS".
- **Expected:** The abstract and conclusion are the two most-read passages, and both understate the paper's disclosed prior-art exposure by dropping the Isabelle-AFP/Coq-`infotheo` gap. The error runs in the direction of appearing more complete than the paper's own record says it is.
- **Fix:** "one" → "two" at :54 and :587, with the second check named in one clause.
- **Cache:** n/a

### 5.2 — 🔴 BLOCKER — §5 asserts Mathlib carries "no … Gaussian CDF"; `ProbabilityTheory.cdf` is in the pinned checkout and applies directly to `gaussianReal`

- **Gate:** NarrativeGrounding
- **Location:** `papers/D12/paper_draft.tex:190-191` (§2.3) and `:526-527` (§5)
- **Observed:** §5: "It carries no Hellinger divergence, no Bhattacharyya coefficient, no Neyman--Pearson lemma, no hypothesis test, and no error function **or Gaussian CDF**; the last is why \thm{gaussianQ} is defined project-locally here." §2.3 repeats it: "since Mathlib carries no error function or Gaussian CDF at our pin". Two sentences earlier the paper stakes its credibility on exactly this class of claim: "Mathlib's absence claims here were checked against the pinned revision by direct inspection of the checkout, not from documentation."
- **Evidence:** At Mathlib `81a5d257`, `lean/.lake/packages/mathlib/Mathlib/Probability/CDF.lean:57`:
  ```lean
  def cdf (μ : Measure ℝ) : StieltjesFunction ℝ
  ```
  with the documented property `ENNReal.ofReal (cdf μ x) = μ (Iic x)`. `gaussianReal 0 1 : Measure ℝ` is in `Mathlib.Probability.Distributions.Gaussian.Real`, which this project already imports (`Detection/GaussianThreshold.lean:1`). So `cdf (gaussianReal 0 1)` typechecks and **is** the standard-normal CDF at the pin. The project's own `gaussianQ_eq_measure` (`GaussianThreshold.lean:227`) proves `(gaussianReal 0 1).real (Set.Ioi z) = gaussianQ z`, i.e. it uses Mathlib's Gaussian measure to express the very object it says Mathlib lacks.
  The **erf half of the claim is correct**: `grep -rn "Real.erf\|erfc" mathlib/Mathlib/` returns nothing. The other Mathlib absence claims in the same sentence also check out — `Bhattacharyya` occurs only as an author name (`MeasureTheory/Measure/Tight.lean:4`), `Hellinger` only as Hellinger–Toeplitz, and there is no Neyman–Pearson lemma or hypothesis test.
- **Expected:** "Mathlib carries no error function and no analytic Gaussian-tail API — `ProbabilityTheory.cdf` applied to `gaussianReal` is available but supplies no closed form and no tail bounds — which is why \thm{gaussianQ} is defined project-locally." One can argue "Gaussian CDF" means a *named, erf-backed* Φ, and that a generic `cdf` combinator does not count; but the paper's own stated standard is literal accuracy established by direct inspection, and a referee grepping `cdf` in the checkout finds the file in one command.
- **Fix:** Reword both sites (`:190-191`, `:526-527`); mirror into `prior_art_novelty.md` §C4, which carries the same absence claim.
- **Cache:** n/a

### 5.3 — 🟡 REQUIRED — §5 says Coq and Isabelle *were* assessed, then says they were *not*

- **Gate:** NarrativeGrounding
- **Location:** `papers/D12/paper_draft.tex:532-549`
- **Observed:** Consecutive paragraphs:
  - :532-534 — "restricted to the ecosystems we actually assessed --- Lean/Mathlib and PhysLib by direct source inspection, **Coq and Isabelle by literature search** --- we are aware of no formalization of: …"
  - :537-538 — "…because **for the ecosystems named in the next paragraph we have no evidence either way**."
  - :545-546 — "Second, **the Isabelle Archive of Formal Proofs and the Coq \texttt{infotheo} library are unassessed.**"
  The "next paragraph" names Coq and Isabelle — the same two the previous paragraph just listed among "the ecosystems we actually assessed".
- **Evidence:** `prior_art_novelty.md` §4.1 is unambiguous: "every Isabelle **AFP** entry (`isa-afp.org` is denied by this repo's egress guard) — *Kraus Maps*, *Concentration Inequalities*, *Projective Measurements*, *Isabelle Marries Dirac*. … **Do not cite an AFP entry** until re-verified." §4.3 lists `10.1007/s10817-013-9298-1 (infotheo/Shannon)` among ten must-verify DOIs deliberately **not** cited.
- **Expected:** One consistent statement: Coq and Isabelle were assessed *only* via the two arXiv companions actually fetched (2207.11350, 2512.05878, 2103.08535); the AFP and `infotheo` were not reached at all.
- **Fix:** Narrow ":532-534" to name the three fetched arXiv sources rather than "Coq and Isabelle", so the following paragraph's carve-out is not a retraction.
- **Cache:** n/a

### 5.4 — 🟡 REQUIRED — the γ = 1 phonon floor is labelled "fail-open"; by the paper's own §2 definition it is the opposite

- **Gate:** NarrativeGrounding
- **Location:** `papers/D12/paper_draft.tex:407-414`; mirrored in `lean/SKEFTHawking/Electrothermal/BolometricFloors.lean:86-88` and `:249-251`
- **Observed:** §3.2: "Because $\gamma \le 1$, the $\gamma = 1$ expression *overstates* the phonon noise, so the floor computed from it *overstates the floor* for any detector with a real gradient across its link … --- **fail-open**, in the direction this layer is most concerned to avoid."
- **Evidence:** The paper defines fail-open two sections earlier, at :177-179: "whenever that gap exceeds $\log 4$, the *true* floor strictly *exceeds* the folklore value, so a screen built on it **admits configurations the true floor forbids**." Fail-open = the used bound sits **below** the true bound = too permissive. Here the shipped bound sits **above** the true bound: the γ = 1 floor forbids operating points a gradient-loaded detector can actually reach. That produces false *refutations*, not false acceptances. `CompositeReadoutCeilings.lean:16-17` states the same convention from the other side: "A composite that silently **under**-counts is fail-open and is the worst defect available here."
  The word "overclaim", used in the Lean docstring alongside "fail-open", is the correct label; "fail-open" contradicts it.
  **The underlying physics and the soundness argument are correct.** I verified: `IsThermalFluctuationLimited Vph m kB T Tw := IsWhiteFilteredVariance Vph (m.phononPSD kB T) Tw` and `IsWhiteFilteredVariance V S₀ T := ∀ h, V h = S₀/2 * ∫₀ᵀ h²` — an **equality**, so a gradient-loaded detector (true variance white at the γ < 1 PSD) genuinely fails the hypothesis and the shipped theorems say nothing about it. `phonon_floor_of_psd_ge` likewise carries a domination hypothesis such a detector fails. The reversal is earned; only the label is wrong.
- **Expected:** "an **overclaim** — the floor asserts more than the model licenses for a gradient-loaded detector — which is the direction this layer is most concerned to avoid", and drop "fail-open".
- **Fix:** One-phrase edit at `:410`; mirror into the two Lean docstrings so the two artifacts stop disagreeing with the paper's own §2 vocabulary.
- **Cache:** n/a

### 5.5 — 🟡 REQUIRED — "The composites bite at ordinary operating points" — no biting witness exists for any of the three composites

- **Gate:** NarrativeGrounding
- **Location:** `papers/D12/paper_draft.tex:578-582` (conclusion)
- **Observed:** "…and three cross-layer composites pair that chain with a device-layer relaxation floor on one attributed readout. **The composites bite at ordinary operating points, not only at the degenerate zero-budget one**, which is what the Birnbaum--Feller lower bound buys."
- **Evidence:** `grep -rn "relaxation_filtered_ceiling_bites\|relaxation_detector_chain_ceiling_bites\|relaxation_photon_ceiling_bites" lean/SKEFTHawking/` returns **nothing**. The two non-degenerate bites that exist —`filtered_readout_ceiling_bites_nondegenerate` (`CompositeReadoutCeilings.lean:628`) and `detector_chain_ceiling_bites_nondegenerate` (`:644`) — are corollaries of the **§3 single-mechanism** ceilings `filtered_readout_ceiling` / `detector_chain_ceiling`, not of the **§3.6 composites** `relaxation_filtered_ceiling` / `relaxation_detector_chain_ceiling` / `relaxation_photon_ceiling`. The only shipped statement about a composite at a named operating point is `relaxation_dominates_photon_at_separation_99`, which shows the *photon* branch does **not** bite there. §4.2 of the draft (`:488-496`) states this correctly — "For the filtered-readout and detector-chain ceilings we give biting witnesses" — so the conclusion contradicts the body.
  The implication is trivial (the composite bound is $1-\max(\cdot,\cdot) \le 1-\text{gaussianQ}(B/2)$), so the claim is *true* but unshipped: no theorem asserts it.
- **Expected:** Either reword the conclusion to attribute the bite to the mechanism ceilings, or ship the three-line corollaries.
- **Fix:** Preferred (per the operator's build-don't-walk-back posture): add `relaxation_filtered_ceiling_bites_nondegenerate` and `relaxation_detector_chain_ceiling_bites_nondegenerate` as `le_trans` corollaries through `le_max_right`; then the conclusion sentence is earned as written.
- **Cache:** n/a

### 5.6 — 🟡 REQUIRED — "an exact propagator norm greater than one" does not name the norm; the propagator is unitary

- **Gate:** NarrativeGrounding
- **Location:** `papers/D12/paper_draft.tex:450-452`
- **Observed:** "That the bound is not vacuous is \thm{norm\_rwaPropagator\_quarter\_turn}, which **computes an exact propagator norm greater than one** --- refuting the assumption that a unit Lipschitz constant suffices in the naive Duhamel estimate."
- **Evidence:** `RotatingWave.lean:1142-1151`:
  ```lean
  theorem norm_rwaPropagator_quarter_turn (ω Ω : ℝ) (hΩ : 0 < Ω) :
      ‖rwaPropagator ω ω Ω 0 1 0 (Real.pi / (2 * Ω))‖ = Real.sqrt 2
  ```
  proved via `linftyOpNorm_one_sub_I_sigmaX` — so `‖·‖` is the **L∞ operator norm** (max absolute row sum) on `Matrix (Fin 2) (Fin 2) ℂ`. The propagator factors as $\tfrac{\sqrt2}{2}(1 - i\sigma_x)$, which is unitary; its **spectral** norm is exactly 1. The $\sqrt2$ is a norm-convention artifact, and a reader who assumes the operator norm will read the sentence as false.
  This is the one place where the paper's own thesis — §3.1 "Convention is load-bearing and we treat it as such" — is not applied to itself.
- **Expected:** "…computes the exact $\ell^\infty$-operator norm $\sqrt2 > 1$ of a quarter-turn propagator, so the unit Lipschitz constant assumed in the naive Duhamel estimate is unavailable in the norm this development uses."
- **Fix:** Name the norm at `:451`.
- **Cache:** n/a

### 5.7 — 🔵 RECOMMENDED — "Three substantive mechanism ceilings" vs "two format anchors": all five have the same one-line shape

- **Gate:** NarrativeGrounding
- **Location:** `papers/D12/paper_draft.tex:463-470`
- **Observed:** The revised count is honest about *dropping* two, but the stated criterion doesn't separate them. All five bodies are one application of `assignmentFidelity_le_of_floor`:
  ```
  photon_budget_ceiling      := assignmentFidelity_le_of_floor (Detection.poisson_avgError_floor hδ)
  relaxation_ceiling         := assignmentFidelity_le_of_floor (avgAssignmentError_rational_floor …)
  relaxation_thermal_ceiling := assignmentFidelity_le_of_floor (avgAssignmentError_combined_floor …)
  filtered_readout_ceiling   := assignmentFidelity_le_of_floor (Detection.error_floor_from_budget …)
  detector_chain_ceiling     := assignmentFidelity_le_of_floor (m.bolometer_error_floor …)
  ```
  And all five have a bites/does-not-bite witness pair, so "each with a witness pair" does not distinguish the three either.
- **Evidence:** `CompositeReadoutCeilings.lean:235-290`, `:472-537`, `:566-716`.
- **Expected:** The real distinction is the *provenance and depth of the floor being re-cast* (6EA/6EB/6EC floors new in this series vs pre-existing QuantumNetwork floors) — which is what `:369-375` of the Lean says. Say that, rather than implying the ceiling theorems differ in substance.
- **Fix:** One-clause reword at `:468-470`.
- **Cache:** n/a

### 5.8 — 🔵 RECOMMENDED — the $f$-divergence generator and "the affinity is its complement" disagree by a factor 2

- **Gate:** NarrativeGrounding
- **Location:** `papers/D12/paper_draft.tex:232-238`
- **Observed:** "The squared Hellinger distance is an $f$-divergence with generator $f(t) = (\sqrt{t}-1)^2$, and the affinity is its complement…"
- **Evidence:** With $f(t)=(\sqrt t-1)^2$, $D_f(P\|Q) = \sum_i q_i(\sqrt{p_i/q_i}-1)^2 = 2 - 2\,\mathrm{BC} = 2(1-\mathrm{BC})$. The *complement* relation $H^2 = 1-\mathrm{BC}$ holds for the $\tfrac12$-normalised squared Hellinger, whose generator is $\tfrac12(\sqrt t-1)^2$. As written the two clauses are inconsistent by a factor 2.
  **The subsumption argument itself is sound**: BC is an affine decreasing function of $D_f$ under either convention, so a general $f$-divergence DPI does imply BC cannot decrease under a Markov kernel, which is exactly `affinity_le_binaryAffinity` (whose binders `HasSum p 1`, `HasSum q 1`, `IsCountRule δ` make the rule a kernel to a two-point space). And the disclosure discipline is exemplary — the secondhand status is flagged in the abstract (:53), §2.4 (:236-238), the bibitem (:617-618), the registry note and the primary-source cache, and absence is nowhere asserted.
- **Expected:** Use $f(t)=\tfrac12(\sqrt t-1)^2$, or say "an affine function of" instead of "the complement of".
- **Fix:** One-symbol edit at `:233`.
- **Cache:** n/a

### 5.9 — 🔵 RECOMMENDED — §5 describes TestingLowerBounds anonymously two paragraphs after citing it by name

- **Gate:** NarrativeGrounding
- **Location:** `papers/D12/paper_draft.tex:523-524` vs `:542-544`
- **Observed:** :523-524 names the project (`\cite{LuccioliDegenne2024,Degenne2025Kernels}`); :542-544 then says "an out-of-Mathlib Lean project on information theory and hypothesis testing is under active development" without naming it, so a reader cannot tell the open check is against the project already cited. The conclusion (:587-588) names it again.
- **Expected:** Name it in §5's blocking-checks paragraph.
- **Fix:** Insert `\cite{LuccioliDegenne2024}` at `:543`.
- **Cache:** n/a

---

### 6.1 — 🟡 REQUIRED — §5 names `IsResponsivityChain` as a binder of `bolometer_error_floor`; it is not one

- **Gate:** AssumptionDisclosure
- **Location:** `papers/D12/paper_draft.tex:557-560`
- **Observed:** "Two more deserve naming because they are **binders of the capstone \thm{bolometer\_error\_floor} itself**: \thm{IsThermalFluctuationLimited} … and \thm{IsResponsivityChain}, asserting the readout chain is linear in the responsivity. The capstone is only as strong as those two identifications."
- **Evidence:** `Electrothermal/BolometricFloors.lean:707-720` — the full binder list is `(hkB) (hT) (hG) (hindep : IsUncorrelatedAt …) (hphonon : IsThermalFluctuationLimited …) (hjohnson : IsWhiteFilteredVariance …) (hTw) (hadm : IsAdmissibleFilter …) (hs) (hσ) (hμle) (hμ) (hσV)`. `IsResponsivityChain` does **not** appear. `grep -rn "IsResponsivityChain" Detection/ Electrothermal/ Control/` returns five hits, all in `Detection/NEPAlgebra.lean` (definition at :158, consumed at :296 and :334) and none in `Electrothermal/`.
  The error runs toward over-disclosure, so no claim is weakened — but the sentence is a checkable falsehood about a named theorem's signature, in the one paragraph a referee reads to decide what the capstone assumes. The genuinely load-bearing binders the paragraph *omits* are `IsUncorrelatedAt` (which the module itself calls "the hypothesis 6EB proved non-droppable", `BolometricFloors.lean:722-723`) and `IsAdmissibleFilter`.
- **Expected:** Name `IsThermalFluctuationLimited`, `IsUncorrelatedAt`, the Johnson-channel `IsWhiteFilteredVariance` and `IsAdmissibleFilter`; move `IsResponsivityChain` to the 6EB NEP-chain discussion where it actually binds.
- **Fix:** Rewrite `:557-560` against the actual signature.
- **Cache:** n/a

### 6.2 — 🔵 RECOMMENDED — γ ≤ 1 is a theorem *hypothesis* and a literature premise, not a proved fact; the paper reads as if it were established

- **Gate:** AssumptionDisclosure
- **Location:** `papers/D12/paper_draft.tex:396-398`, `:407-410`
- **Observed:** "$\gamma \in (0,1]$, equal to $1$ exactly in the isothermal-link limit (\thm{phononPSD\_eq\_phononPSDGamma\_one})…" and "**Because $\gamma \le 1$**, the $\gamma = 1$ expression *overstates*…". The cited theorem supports only the γ = 1 endpoint.
- **Evidence:** `BolometricFloors.lean:228-234` — `phononPSDGamma_lt_phononPSD` takes `(hγ0 : 0 < γ) (hγ1 : γ < 1)` as **binders**; `gammaOne_phononFloor_overstates` (`:258-261`) inherits them. Nothing in the tree proves γ ≤ 1, and the closed form that would imply it is not formalized. The bound is a cited-literature premise plus a hypothesis, correctly so — but the paper's "Because γ ≤ 1" does not say that.
- **Expected:** "Under the literature bound $\gamma \le 1$ (carried as an explicit binder of \thm{phononPSDGamma\_lt\_phononPSD}), the $\gamma=1$ expression overstates…".
- **Fix:** One clause at `:408`.
- **Cache:** n/a

---

### 7.1 — 🔵 RECOMMENDED — two inline count literals outside the macro chain; both correct today, both will drift silently

- **Gate:** NumericalFreshness
- **Location:** `papers/D12/paper_draft.tex:92` ("Thirteen root-imported modules"), `:551` ("Eleven propositions carry modelling content")
- **Observed:** Both are hand-written counts of tree contents, not `\input{counts.tex}` macros.
- **Evidence:** Both verified correct at HEAD: `lean/SKEFTHawking.lean` root-imports exactly 13 `Detection.*`/`Electrothermal.*`/`Control.*` modules (lines 5082–5197); exactly 11 `Prop`-valued `def`s exist across those 13 modules (`IsWhiteFilteredVariance`, `IsResponsivityChain`, `IsUncorrelatedAt`, `IsCountRule`, `IsShotFilteredMoments`, `IsThermalFluctuationLimited`, `IsThermalFluctuationLimitedGamma`, `SolvesHeatBalance`, `PerturbationsDecay`, `SolvesDrivenBalance`, `SolvesSinusoidalBalance`). `validate.py --check count_literals` and `--check numerical_literals` emit **no** D12 warning (the patterns don't catch spelled-out numerals), and `counts_fresh` / `paper_provenance` both PASS.
  Evidence that this class does drift: `papers/D12/bundle_metadata.json` notes "all **565** declarations kernel-pure" while `lean_deps.json` now carries **576** declarations in those modules — an 11-declaration drift inside one round.
- **Expected:** Spelled-out counts of tree contents belong in the `\input{}` chain like every other count in the project.
- **Fix:** Add `d12modules`/`d12modelprops` macros to `docs/counts.tex` (derived from `lean_deps.json`), or extend `count_literals` to catch spelled-out numerals followed by "modules"/"propositions"/"theorems".
- **Cache:** n/a

---

### Class 8 — ProductionRunHealth: PASS, no findings

D12 makes no simulation-evidence claim. I scanned the draft for "Monte Carlo", "numerical evidence", "simulation" — none present; every quantitative statement is either a kernel-checked rational enclosure or a hand evaluation of a closed form. The graph carries 18 `ProductionRun` nodes, none linked to D12's claims, which is correct for a pure-formalization bundle. D12 *does* have a `Paper` node in the graph (titled "Kernel-Verified Detector and Readout Metrology: …"), so Stage-13 finding extraction will attach.

---

## What I verified and found sound

Recorded so the next round does not re-derive it:

- **Non-degenerate ceiling witnesses (change 1).** $3/25 = 0.12 \le \tfrac{1}{1+1^2}\varphi(1) = 0.120985$ — the Birnbaum–Feller step is arithmetically valid. `gaussianTail_birnbaum` is a real FTC-2 proof over `Set.Ioi z` with the sign-free extension handled once. The 22/25 ceiling is genuinely non-degenerate: matched budget 2 ⇒ $z=1$, ceiling $1-Q(1) \le 0.88$, refuting a claimed 0.99 at a signal-carrying operating point, versus the `matchedBudget = 0` witnesses which only give 1/2. Both `*_bites_nondegenerate` theorems are derived by **calling** their ceilings, so they inherit the full binder list.
- **Phonon γ direction reversal (change 2).** γ ≤ 1 is right in this module's bolometer-referred convention. I recomputed the closed form independently and it is algebraically identical to the standard detector-referred TES $F_{\rm link}$ under $t = 1/r$; $\gamma(2,4) = 0.47315$, so the paper's 0.47 is correct. The "overstates" direction follows, and the soundness argument is correct because `IsWhiteFilteredVariance` is an **equality** at the γ = 1 PSD, which a gradient-loaded detector fails. Only the "fail-open" label (5.4) and the sourcing (2.2) are wrong.
- **Prior-art disclosure (change 3).** The secondhand status of `LuccioliDegenne2024` is flagged in the abstract, §2.4, the bibitem, the registry and the cache file; absence is nowhere asserted. The $f$-divergence subsumption argument is sound (modulo 5.8's factor 2). The one defect is the title (1.1).
- **PhysLib TODO claim (change 4).** Verbatim true at pin `c4843367`: `QuantumInfo/States/Mixed/Fidelity.lean:136-139` is a four-item `--TODO:` list whose second item reads "Matches with classical (squared) Bhattacharyya coefficient", and the `fidelity` docstring at :27 does call it "the quantum version of the Bhattacharyya coefficient". `diagonalState_sqrtFidelity_eq_affinity` assumes only `∀ i, 0 ≤ p i` / `0 ≤ q i`, so "more general than the state-bound form" is fair. (Also unmentioned and to the paper's advantage: PhysLib's `fidelity_channel_nondecreasing:133` is `@[sorryful] … := sorry`, and D12 consumes none of it.)
- **Ceiling recount and composites (change 5).** `relaxation_filtered_ceiling`, `relaxation_detector_chain_ceiling` and `relaxation_photon_ceiling` are all genuine `combined_ceiling_max` applications over two named floors on one attributed readout, with the attribution carried as explicit `hattr0`/`hattr1` binders. Not vacuous. The `combined_floor_add` disjointness is real measure theory (`add_le_branch_error_of_disjoint`) and `combined_ceiling_gap_witness` is derived by instantiating an actual measure, not asserted.
- **Substrate hygiene.** 576 declarations across the 13 modules; `axiom_deps_core ⊆ {propext, Classical.choice, Quot.sound}`; `axiom_deps_project = ∅`; zero `sorry`/`native_decide`/`maxHeartbeats`. All 150 `\thm{}` names resolve; zero overlap with the 593-node `PlaceholderMarker` set.
- **Other Mathlib/PhysLib claims.** `HasSubgaussianMGF.measure_ge_le` exists at `SubGaussian.lean:334`/`:704`, docstringed "Chernoff bound", giving `exp(-ε²/(2c))` — at c = 1 exactly a factor 2 weaker than `gaussianTail_chernoff`. No `Real.erf`. No Bhattacharyya/Hellinger divergence, no Neyman–Pearson, no hypothesis test. PhysLib has `QuantumMechanics/Operators/Uncertainty.lean` and `CondensedMatter/Thermoelectric/` and no detector/metrology namespace. The two novelty greps in `prior_art_novelty.md` N3/N5 both return zero over `.lake/packages` — I re-ran them.
- **Figures.** All three checked pixel-against-claim. Fig. 1: crossover at $N_a = 51.40$ for $N_b = 50$ (I solved $2\sqrt{N_b}(\sqrt{N_a}-\sqrt{N_b}) = \log 4$ and got 51.397); gap exponent 9.5445 at (50,60) matches `brightGap_5060`; $e^5 = 148.41$ matches the 148× witness; the true ratio 3491.96 at (50,60) reproduces. Fig. 2: ramp ENBW·T = 2/3 > 1/2; Mills ratio crosses 1 at $z = 0.3726$, matching the caption's "$z \lesssim 0.37$". Fig. 3: $\tau_{\rm eff} = \tau/4$ at $\mathcal{L}=3$; $|1-\mathcal{L}|$ V-shape with the "naive overstates" band exactly on $0<\mathcal{L}<2$; stability-blindness witnesses at $\mathcal{L} = -3$ and $+5$ both giving 4. Caption-vs-figure agreement is clean throughout.
- **Freshness.** `counts_fresh` PASS, `paper_provenance` PASS, `count_literals`/`numerical_literals` emit no D12 warning, `paper_draft.log` shows 0 undefined references and 0 overfull boxes.

## QI Candidate

**Duplicate `CITATION_REGISTRY` entries for one primary source, with divergent verification state.** `leanQI2025`/`MeiburgLessaSoldati2025` and `isabelleCBO`/`UnruhCBO2025` are the same two works under four keys; the D10 pair carries `arxiv: None, doi: None` and has never been fetched, while the D12 pair carries verified arXiv IDs. The citation cache keys on `(bibkey, bibitem_hash)`, so a duplicate key is invisible to it by construction — verification does not amortize across a duplicated source, and two companion papers can ship inconsistent bibliographic records for the same reference while both pass Gate 1. This is a pipeline gap, not a D12 defect: add a `validate.py --check citation_registry_integrity` rule that flags registry entries sharing a normalised title (or DOI/arXiv ID) under different keys, and back-fill identifiers into whichever copy lacks them. Related: `count_literals` does not catch spelled-out numerals ("Thirteen root-imported modules", "Eleven propositions"), so hand-written tree counts in prose are unguarded across every bundle — see finding 7.1 and the 565→576 drift already visible inside `bundle_metadata.json`.
