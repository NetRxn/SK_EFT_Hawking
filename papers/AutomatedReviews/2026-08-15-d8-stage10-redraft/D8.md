# D8 — Stage-10 full redraft findings (2026-08-15)

Filed during the Stage-10 full redraft of bundle D8. Each item needs code,
substrate or new research and therefore could not be closed inside the
manuscript. Every `Verify` below was **executed** from the repository root and
**fails at HEAD**; the observed output is recorded under `Evidence`.

---

### 1.1 — `PAPER_STRATEGY.md` §D8 contradicts itself inside one paragraph

- **severity:** major
- **lane:** docs
- **Gate:** NarrativeGrounding
- **Location:** `docs/PAPER_STRATEGY.md`, the "Relationship to siblings" line of §D8 (line 152)
- **Observed:** One paragraph asserts both that "**D4** retains the Fibonacci-universality + topological/categorical-foundations focus" and that "The Phase 6p Fibonacci density + Phase 6t quantitative-SK content that the draft mapping previously routed to D4 §9.1–9.5 is **re-pointed to D8**". Those cannot both be true, and the document is the charter both bundles are drafted against. The machine state settles it against the second clause: D4's 62 apex theorems and D8's 35 share zero declarations, D4 declares the Fibonacci-specific chain (`fibonacci_density_F21_unconditional`, `solovayKitaev_dawson_nielsen_quantitative_fibonacci_strict{,_constructive_unconditional,_constructive_tight}`), and D8 declares none of it.
- **Evidence:** `same-paragraph both-claims: True`. Independently, the apex-set intersection is `[]` over both full names and short names, and D4's merged draft (`papers/D4/paper_draft.tex` lines 200–210, 456–468) explicitly cedes the general theory to D8 by citation while keeping Fibonacci.
- **Expected:** §D8's sibling paragraph states one boundary, matching the apex sets: D4 owns the Fibonacci alphabet and the anyonic foundations; D8 owns the alphabet-agnostic and dimension-agnostic theory.
- **Fix:** Rewrite the sibling clause in `PAPER_STRATEGY.md` §D8 to drop the re-pointing sentence, which the D4 redraft superseded. Same paragraph in §D4 if it mirrors the claim.
- **Verify:** `python3 -c "t=open('docs/PAPER_STRATEGY.md').read(); l=[x for x in t.split(chr(10)) if 'D4** retains the Fibonacci' in x][0]; import sys; sys.exit(0 if ('re-pointed to D8' not in l) else 1)"`

---

### 1.2 — Read–Rezayi compilation headlines are claimed by no bundle's apex set

- **severity:** major
- **lane:** metadata
- **Gate:** LeanProofSubstance
- **Location:** `papers/D8/bundle_metadata.json` (`apex_theorems`); declarations in `SKEFTHawking.FKLW.ReadRezayiK5Quantitative` and `SKEFTHawking.FKLW.ReadRezayiK7Quantitative`
- **Observed:** `SKEFTHawking.FKLW.GenericSU2.solovayKitaev_dawson_nielsen_quantitative_rr5_strict_constructive_tight` and its `rr7` counterpart exist, are theorems, and are kernel-pure (`axiom_deps_project: []`). No bundle in the corpus names them, or any `readRezayi` declaration, as an apex. D8's manuscript is the only place they are discussed, and D8 §4 now rests two of its four alphabet rows on them.
- **Evidence:** `apex entries naming rr5/rr7/readRezayi: []` across all 21 `papers/*/bundle_metadata.json`. The declarations resolve in `lean/lean_deps.json` with `kind: theorem`.
- **Expected:** A theorem a bundle's prose rests on is registered in that bundle's apex set with a `claims` annotation, so `bundle_apex_resolves` and `apex_theorem_claims_grounded` can see it.
- **Fix:** Add both declarations to D8's `apex_theorems` with `claims` strings naming §4 and the specific obstruction each discharges (Chebyshev factorization of `T_7(x)+1`; triple-angle collapse at `cos(pi/9)`). Re-run `bundle_metadata_matches_graph`.
- **Verify:** `python3 -c "import json; m=json.load(open('papers/D8/bundle_metadata.json')); ns=[t['name'] for t in m['apex_theorems']]; import sys; sys.exit(0 if any('rr5' in n for n in ns) and any('rr7' in n for n in ns) else 1)"`

---

### 1.3 — `l2_opNorm_kronecker_one_le` is claimed as an upstream contribution by two bundles

- **severity:** major
- **lane:** docs
- **Gate:** NarrativeGrounding
- **Location:** `papers/D9/paper_draft.tex` §"Library-contribution portfolio" (line 975); `papers/D8/bundle_metadata.json` apex entry for `SKEFTHawking.QuantumNetwork.KroneckerOpNorm.l2_opNorm_kronecker_one_le` (`claims`: "a named Mathlib-upstream candidate")
- **Observed:** Both manuscripts list the same lemma, with the same inequality typeset the same way, as their own candidate for upstream contribution. Two companion submissions claiming the same library contribution is visible to any referee who reads the pair. D9's own architecture prose (line 222) says the operator-norm layer lives in D9's corpus, which argues D9 is the owner.
- **Evidence:** `papers/D9/paper_draft.tex:975-978` names `\lean{l2\_opNorm\_kronecker\_one\_le}` inside its own contribution list; the D8 apex `claims` string names it an upstream candidate. The redrafted D8 manuscript no longer asserts ownership in prose (it cites the bound at use and the appendix table omits it), but the metadata claim persists.
- **Expected:** One owner. The declaration lives in `SKEFTHawking.QuantumNetwork.KroneckerOpNorm`, which is D9's module family.
- **Fix:** Amend the `claims` string on D8's apex entry to record the lemma as consumed from the certification layer rather than as a D8 upstream candidate, or move the apex entry to D9. Decide once and make both manuscripts agree.
- **Verify:** `python3 -c "import json,sys; m=json.load(open('papers/D8/bundle_metadata.json')); e=[t for t in m['apex_theorems'] if 'l2_opNorm_kronecker_one_le' in t['name']][0]; sys.exit(1 if 'upstream' in e['claims'] else 0)"`

---

### 1.4 — The ≈3.97 exponent was cited to a source whose held abstract states ≈2

- **severity:** major
- **lane:** citations
- **Gate:** CitationIntegrity
- **Location:** previously `papers/D8/paper_draft.tex` §3.2 and Fig. 3 caption; the same attribution pattern recurs in `papers/D4/paper_draft.tex` (line 412 carries a live `% TODO` about it) and in `lean/SKEFTHawking/FKLW/GenericSUdSkLengthExponent.lean` (module docstring attributes `log 5 / log(3/2) ≈ 3.97` to "Dawson & Nielsen … arXiv:quant-ph/0505030, §3.3")
- **Observed:** The held Dawson–Nielsen abstract, which is a verbatim arXiv abstract rather than a project paraphrase, states the gate-count exponent as "approximately 2". The value ≈3.97 is correct and is attested **verbatim in two full-text-held sources** (`Selinger2015`: "the Solovay-Kitaev algorithm achieves T-count O(log^c(1/ε)), where c is approximately 3.97"; `KMM2013ancilla` §1: "the number of gates in the resulting quantum circuit is O(log^3.97(1/ε))"). So the number stands and the citation is wrong. Fixed in the D8 manuscript this session; the Lean docstring and D4 are untouched.
- **Evidence:** `where c is approximately 2.` extracted from `Lit-Search/Phase-1-and-Background/primary-sources/DawsonNielsen2006.abstract.txt`.
- **Expected:** Dawson–Nielsen is cited for the recursion; Selinger 2015 and KMM 2013 (ancilla) are cited for the exponent value, wherever the value appears.
- **Fix:** Re-point the attribution in `GenericSUdSkLengthExponent.lean`'s docstring and in `SolovayKitaevLengthBound.lean` if it carries the same claim; resolve D4's `% TODO` at line 412 the same way. Note that the D4 stage-13 sweep's reconciliation ("the abstract's runtime-rounded ≈2") is wrong on its face: the abstract's `c` is the gate-count exponent in the same `O(log^c(1/ε))` gate-count expression, not a runtime figure.
- **Verify:** `grep -c "Dawson-Nielsen exponent\|Dawson & Nielsen" lean/SKEFTHawking/FKLW/GenericSUdSkLengthExponent.lean` (currently non-zero at the ≈3.97 attribution site)

---

### 1.5 — Three load-bearing primary sources are not held, and nine "abstract" caches are project paraphrases

- **severity:** major
- **lane:** research
- **Gate:** CitationIntegrity
- **Location:** `Lit-Search/Phase-1-and-Background/primary-sources/`; `docs/primary_sources_state.json`
- **Observed:** Of D8's 17 cite keys, 5 are held full-text, 11 abstract-only, and 1 (`KitaevShenVyalyi2002`, a textbook) not held. Three of the abstract-only entries are load-bearing: `RossSelinger2016` (arXiv:1403.2975) underwrites the entire Appendix-C chain and the Hypothesis 8.3 reference; `KMM2013` (arXiv:1206.5236) underwrites the exact-synthesis layer; `Mukhopadhyay2024` (arXiv:2401.08950) underwrites the resource theory and every section, lemma and conjecture number in §5.4. Separately, only 2 of the 11 abstract caches are verbatim arXiv abstracts; the other 9 are project-authored summaries, which cannot support an attribution check at all. The manuscript now carries `% TODO:` comments at the three affected claim sites.
- **Evidence:** `ls` on the primary-sources directory returns `RossSelinger2016.abstract.txt`, `KMM2013.abstract.txt`, `Mukhopadhyay2024.abstract.txt` and no corresponding PDFs.
- **Expected:** A bundle does not go green while its manuscript carries `% TODO:` markers disclaiming unverified attributions. All three sources are freely available on arXiv.
- **Fix:** Acquire the three PDFs into `Lit-Search/Phase-1-and-Background/primary-sources/`, verify the section/lemma/hypothesis numbering cited in D8 §5.4 and §7.2 against them, and remove the three `% TODO:` comments. Separately, re-header the 9 paraphrase caches so they are not mistakable for source text.
- **Verify:** `test -f ../Lit-Search/Phase-1-and-Background/primary-sources/RossSelinger2016.pdf && test -f ../Lit-Search/Phase-1-and-Background/primary-sources/KMM2013.pdf && test -f ../Lit-Search/Phase-1-and-Background/primary-sources/Mukhopadhyay2024.pdf`

---

### 1.6 — The Stage-13 "3 SUPPORTED / 7 CAVEAT / 0 NOT-SUPPORTED" split for D8 is not reconstructible

- **severity:** minor
- **lane:** docs
- **Gate:** CitationIntegrity
- **Location:** `docs/audits/stage13_attribution_sweep_2026-06-10.md` §D8 (lines 118–128); restated in `papers/D8/bundle_metadata.json` `notes`
- **Observed:** The D8 section carries an aggregate count and no per-anchor table, while sibling sections in the same document (for example L1) do carry one. Its own narrative names five anchors as verified against a stated SUPPORTED count of three, so the count and the prose cannot both be read at face value. The anchor roster it cites (`docs/agents/claims-reviewer-bundle-prompts.md:355-361`) names Kuperberg 2009, which appears nowhere in D8's bibliography or draft, and omits four of the papers the sweep says it verified full-text.
- **Evidence:** grep for `Kuperberg` in `papers/D8/bibliography.bib` and `papers/D8/paper_draft.tex` returns 0 in both; the §D8 block contains no `|` table row.
- **Expected:** A GREEN attribution verdict is itemized per anchor, and its anchor roster matches the bundle's actual citations.
- **Fix:** Re-derive the D8 anchor list from `bibliography.bib`, drop Kuperberg 2009 from the prompt roster, and record a per-anchor verdict table. This is a prerequisite for the sweep's verdict being usable as evidence downstream.
- **Verify:** `grep -A 12 '^## D8' docs/audits/stage13_attribution_sweep_2026-06-10.md | grep -c '^|'`

---

### 1.7 — Nine of D8's fourteen "open findings" are parser artifacts, and both blocked P1 gates rest on them

- **severity:** major
- **lane:** infra
- **Gate:** LeanProofSubstance
- **Location:** `scripts/build_graph.py::extract_review_finding_nodes` (line 2182); `papers/AutomatedReviews/2026-05-31-bundle-stage13/D8.md`; `papers/AutomatedReviews/2026-05-31-1500-bundle-stage13-rerun/D8.md`
- **Observed:** The `### N.N —` section splitter mints a finding node for every numbered section in a review document and defaults it to `open`. Nine of D8's fourteen open findings are sections whose own headings read `(no finding)`, `(RESOLVED)`, `(light, as scoped)` or `**PASS, no finding.**`, including the two closure reports for findings the ledger already records as `fixed`. `blocked_p1_gates = ["LeanProofSubstance","NarrativeGrounding"]` is populated exclusively by three of those nine, which is why `blockers_open = 0` and `critical_open = 0` while readiness reads YELLOW. Additionally, none of the fourteen declares a `Verify:` line, so `close_finding.py` cannot close any of them without one being supplied by hand.
- **Evidence:** `grep -i "verify:"` returns nothing in either D8 review document; the resolver yields 33 total / 14 open for D8, and the nine artifact sections carry the quoted headings.
- **Expected:** A section that states it found nothing does not become an open finding, and a bundle's blocked-gate list reflects live defects.
- **Fix:** Two options, and they are not equivalent. Either teach the extractor to skip sections whose heading or first line declares a non-finding (structural, fixes every bundle), or batch-close the nine with supplied verifies (local, leaves the mechanism in place). The first is correct; the second is what unblocks D8's gates today. Route through `architecture-change` before touching the extractor.
- **Verify:** `python3 -c "import json,sys; m=json.load(open('papers/D8/bundle_metadata.json')); sys.exit(1 if m['open_findings'] > 5 else 0)"`

---

### 1.8 — Two figures D8 needs cannot be produced from the current visualization module

- **severity:** minor
- **lane:** figures
- **Gate:** FigureIntegrity
- **Location:** `papers/D8/paper_draft.tex` (two `\figuredeferred` blocks: `fig:d8-coverage`, `fig:d8-length-comparison`); `src/core/visualizations.py`
- **Observed:** The existing coverage figure `figures/d8_fig1_alphabet_dimension_map.png` shows a Fibonacci row that the redraft no longer claims (see 1.1), so it can no longer be used as drawn. Separately, §7 is a section about circuit length and carries no length figure; the three verified bounds of Table~\ref{tab:mechanisms} can be plotted directly from the theorem statements with no solver run, but the plotting function does not exist and `visualizations.py` is the canonical home for figure functions. Both are declared with `\figuredeferred` naming the blocker, per the Stage-9 rule.
- **Evidence:** The draft contains two `\figuredeferred` invocations; `d8_fig1_alphabet_dimension_map.png` exists under `papers/D8/figures/` and depicts five alphabets including Fibonacci.
- **Expected:** A Tier-1 draft's figures match its claims, and a section about length carries a length figure.
- **Fix:** Add two functions to `src/core/visualizations.py`: a recut coverage map matching Table~\ref{tab:alphabets} (four alphabet families, no Fibonacci row, no wave labels), and a length-versus-precision comparison plotting the exponent-3.97 recursion bound, the Ross–Selinger `N_3 + 8k` bound and the KMM `50400k + 812` bound against precision. Then regenerate and replace both `\figuredeferred` blocks.
- **Verify:** `grep -c "figuredeferred" papers/D8/paper_draft.tex` (currently 2; expected 0)

---

### 1.9 — `cliffordTCompile_eq_recursion` resolves to a trivial witness and its apex `claims` string does not say so

- **severity:** minor
- **lane:** metadata
- **Gate:** LeanProofSubstance
- **Location:** `papers/D8/bundle_metadata.json`, apex entry for `SKEFTHawking.FKLW.GenericSU2.cliffordTCompile_eq_recursion`
- **Observed:** `apex_claims_not_vacuous` flags this declaration as resolving to a trivial `rfl` witness with the vacuity undisclosed in its `claims` string, which reads "§7 — termination / finite depth: the compiler equals the structural DN recursion at explicit finite depth". The statement is definitionally true because `cliffordTCompile` is *defined* as that recursion, and that is fine and even desirable, since it is what makes termination structural rather than argued. What is wrong is that the annotation presents it as a proved property rather than as the definitional unfolding it is. The redrafted manuscript states this correctly in the body (Lemma 4 and the sentence after it), so this is a metadata fix only.
- **Evidence:** `apex_claims_not_vacuous` reports one D8 hit, `cliffordTCompile_eq_recursion`, marked UNDISCLOSED.
- **Expected:** A `claims` string on a definitionally-true apex discloses that it is definitional.
- **Fix:** Amend the `claims` string to state that the equality is definitional and that its value is making termination structural. Two further D8 hits under `apex_theorem_claims_grounded` (`cliffordCCZLiteral_dense` claiming the numeral 63, `gridFindT_isSome_of_residual` claiming 3.2) are the same class: numerals in the annotation that do not appear in the statement. Both are accurate descriptions and both should say where the numeral comes from.
- **Verify:** `python3 -c "import json,sys; m=json.load(open('papers/D8/bundle_metadata.json')); e=[t for t in m['apex_theorems'] if t['name'].endswith('cliffordTCompile_eq_recursion')][0]; sys.exit(0 if 'definitional' in e['claims'].lower() else 1)"`

---

## Not filed, and why

- **The SU(d) polylog level spec is undischarged at general `d`.** `SkLevelPolylog_sud_spec` in `GenericSUdSkLevelPolylog.lean` is a predicate whose docstring says the substantive discharge "ships in follow-on sessions". This is **not** a defect in D8's claims: the general-dimension headline (`solovayKitaev_dawson_nielsen_quantitative_generic_sud_strict_constructive_tight`) does not consume that predicate, and the instantiations at `SU(4)` and `SU(8)` discharge the full headline including its word-length conjunct. The manuscript states the general theorem conditionally and the instantiations unconditionally, which is exactly the situation. Recorded here so a later reader does not re-derive the alarm.
- **`channelSde2_le_toffoliCost` carries `IsExactlyCliffordCCZ U`.** This is a domain hypothesis, not a smuggled one: `toffoliCost` is an `sInf` over exact Clifford+CCZ implementations and is meaningless off that domain. The manuscript now states the restriction in the abstract and in the theorem. No finding.
- **The rational-versus-dyadic gap in §5.4.** Settled in the body rather than filed: `sde2 q = max(0, -v_2(q))` is defined on all of `ℚ`, so the lower bound needs rationality only, and the dyadic refinement is interpretive. `MukhopadhyaySde2.lean` lines 45–48 say the same thing.
