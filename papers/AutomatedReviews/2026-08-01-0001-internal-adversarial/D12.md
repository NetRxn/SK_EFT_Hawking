---
paper: D12
bundle_target: D12
reviewer: adversarial-reviewer
model: claude-opus-5
review_date: 2026-08-01T00:01:37Z
readiness_gates_version: 1
head_commit: 03a4592e
round: 13
kind: adjudication
---

# Adversarial Review — D12 (round 13, adjudication round)

## Summary

I adjudicated all 76 open blocking findings against the tree at `03a4592e`: **31 REPAIRED,
14 PARTIALLY REPAIRED, 29 NOT REPAIRED, 2 WITHDRAWN-BY-A-LATER-ROUND.** Round-11 `5.1`
(ENBW) and `6.1` (capstone binder) are both genuinely repaired and I verified each from the
artifact, not from round 12's prose. The heading-parse closure deletion the round-12
commit message claimed but did not ship **is now actually shipped** — I verified it three
ways. On `2022:2.1` and `2022:5.2`: **do not re-file either**, but not for the reason you
gave — both ledger records name a finding whose subject their evidence does not describe,
and the underlying defects at those two IDs are genuinely unrepaired. That mis-targeting
runs to **three of the six D12 records you filed this session**, one of which is `fixed` at
HEAD right now, and it is my new blocker. D12 is not submission-ready.

## Environment

`uv run python scripts/validate.py` → **59/59 checks passed, 1114 warnings**, exit 0,
archived to `docs/validation/reports/validation_20260731T235108Z.json`. Stage 13's
precondition was met when this review began.

⚠️ The suite went RED **during** this review, from outside D12's scope. A concurrent D11
Stage-13 document landed at `papers/AutomatedReviews/2026-08-01-0009-internal-adversarial/D11.md`
and `review_severity_declared` — whose cutoff `2026-08-01` arrived at 00:00 UTC today —
failed it: "36 finding heading(s) but only 5 `- **Severity:**` line(s)". I verified the
failure is a **true positive** rather than the false-positive shape round-12 8.4 predicted:
`_HEADING` counts 36 and `_REVIEW_SECTION_RE` also finds exactly 36 findings in that file,
so zero non-finding headings inflate the count. That is a D11 artifact authored by another
agent and I did not touch it. Nothing in D12's scope regressed.

`papers/D12/paper_draft.tex` is **byte-identical** to what round 12 reviewed
(`md5 fab8068f34953371d63161317c24dc18` at both `fc624fc5` and `03a4592e`), so every
paper-side adjudication below is an adjudication of the same text — but I re-read and
re-derived rather than inheriting round 12's readings. No LaTeX was compiled: the D12 diff
since round 12 is `bundle_metadata.json` only (three counter fields), which changes no
float, macro or reference. I ran no `pdflatex`, inside `papers/` or anywhere.

Working tree on arrival: 133 dirty tracked paths (132 `figures/*.png`,
`lean/lean_deps.json.hash`). None is a D12 artifact. Files I wrote are in the compliance
note.

## Part A — Adjudication of the 76 open blocking findings

Verdict counts: **31 REPAIRED / 14 PARTIALLY REPAIRED / 29 NOT REPAIRED / 2 WITHDRAWN.**
Each block below is written to be quoted verbatim into a ledger record.

**A1 — REPAIRED — `1210:1.1` (Mather γ hedge into the fifth artifact).**
`lean/SKEFTHawking/Electrothermal/BolometricFloors.lean:203-228` now carries the paper's
full hedge: both convention readings, the two named failed predecessors ("Two earlier notes
here were wrong in opposite directions"), and the sentence "this project holds Mather in
abstract only". It also carries the *corrected* derivation `52.7 − 31.2` with the
parenthetical "the rounded figures give 22" — i.e. the round-8 repair propagated here too.
Verified by direct read at `03a4592e`.

**A2 — REPAIRED — `0829:5.5` and `1210:5.1` (the composites "bite").**
Both witnesses exist and are kernel-pure. `relaxation_filtered_ceiling_bites`
(`Control/CompositeReadoutCeilings.lean:687-707`) and
`relaxation_detector_chain_ceiling_bites` (`:709-...`) each carry the full composite binder
list and conclude `assignmentFidelity e0 e1 ≤ 22 / 25` under `hb : … matchedBudget … = 2`.
From `lean/lean_deps.json`: both have `axiom_deps_core = ['Classical.choice','Quot.sound','propext']`,
`axiom_deps_project = []`, no `sorry`. The paper's conclusion `:707-711` now claims exactly
**two** composites bite, not three, names both theorems, and `:577-580` states in terms that
the proofs discard the relaxation branch so `22/25` is the single-mechanism value. The
sentence the finding quoted ("The composites bite at ordinary operating points") is gone.

**A3 — REPAIRED — `1524:2.1` and `1730:2.1` (the "23 points" / "$53-31$" derivation).**
`paper_draft.tex:453` now reads "differ by $21.5$ points ($52.7 - 31.2$; the rounded figures
quoted above give $22$)", and `BolometricFloors.lean:220` mirrors it verbatim. I recomputed
at 30 dps rather than trusting either: `γ(2,4) = 0.473148148148`, PSD reduction
`52.685185 %`, amplitude reduction `31.214235 %`, difference `21.47095 → 21.5`;
`52.7 − 31.2 = 21.5` and `53 − 31 = 22`. Both artifacts are now correct and mutually
consistent.

**A4 — REPAIRED — `1524:2.4` (Mather's 30 % had no `PARAMETER_PROVENANCE` entry).**
`PARAMETER_PROVENANCE['MATHER_1982_GRADIENT_REDUCTION']` exists with `value 0.3`,
`tier LITERATURE`, `source 'Mather 1982, Applied Optics 21, 1125'`,
`doi '10.1364/AO.21.001125'`, `llm_verified_date '2026-07-31'`, and a `detail` field that
pins the missing convention explicitly. `PAPER_DEPENDENCIES['D12']['parameters'] ==
['MATHER_1982_GRADIENT_REDUCTION']` and Gate 3 now evaluates non-vacuously on it. The
registration half of this finding is done; the human-verification half is a *different*
finding (C4).

**A5 — REPAIRED — `1524:5.1`, `1730:5.1` (BLOCKER) and `1823:5.1` — the ENBW account.
This is one of the two you asked me to check, and it holds.** Three separate defects, all
gone: (i) the headline constant reads `$\mathrm{ENBW}\cdot T \ge 1/2$` at `:274`, matching
`FilterFloors.lean:197-201` (`1 / 2 ≤ enbw h T * T`) — there is no `1/4` statement of the
theorem anywhere in the draft; (ii) the lock-in convention is written `$1/(4\tau)$` with
"time constant $\tau$" at `:302`, so the symbol collision with the window `T` is gone; (iii)
the functional disambiguation `1823:5.1` asked for is at `:303-307` in almost the exact
words it specified, including "a frequency-domain bandwidth of an infinite-support
response". **I verified the new parenthetical `:308-310` independently rather than inheriting
round 12's check.** With `enbw h T := (∫₀^T h²) / (2 (∫₀^T h)²)` read from
`FilterFloors.lean:124-125`, a single-pole `h = e^{-x/τ}` gives
`enbw·T = (u/4)·(1+e^{-u})/(1-e^{-u}) = (u/4)·coth(u/2)` exactly. Numerically against a
2 000 001-point trapezoid of that same definition: `u = 0.01, 0.1, 1, 5, 20` →
`0.5000041667, 0.5004165972, 0.5409883534, 1.2669591373, 5.0000000207`, agreeing with the
closed form to 9–10 significant figures. At 50 decimal places `f(10^{-30}) = 0.5` exactly,
`f` is monotone increasing on a 4000-point grid over `(0,50]`, and
`d/du[u·coth(u/2)] = (cosh v·sinh v − v)/sinh²v > 0` for `v = u/2 > 0` since
`sinh(2v)/2 > v`. So "$\ge \tfrac12$ for every $u>0$", "$\to\tfrac12$ as $u\to0$" and "it
never approaches $1/4$" are each exactly right. **File the closure.**

**A6 — REPAIRED — `1524:5.2` and `1730:5.2` (the "inert" relaxation branch and the
`0.4651` crossover).** `paper_draft.tex:579-583` now says "each proof lower-bounds the
maximum by its detection branch and discards the relaxation term", and gives the crossover
as `0.4648` with the closed form `x = 2Q(1)/(1-2Q(1)) = 0.46479\ldots`.
`CompositeReadoutCeilings.lean:672-679` mirrors it and additionally records the correction
("round 7 stated 0.4651 here and in the paper without recomputing; 0.4651 solves to
Q = 0.158726, not Q(1) = 0.158655") and retracts "inert" explicitly. Recomputed at 30 dps:
`Q(1) = 0.15865525393145705`, `x = 0.46479477349154408`, and `x/(2(1+x))` returns
`0.15865525393145705` — a 17-digit round trip.

**A7 — REPAIRED — `1524:7.2` (`bundle_metadata` asserting "all 565 declarations").**
The `notes` field now says the count "is NOT frozen here — it drifts with every wave;
docs/counts.tex is the generated source", and names the finding that forced the change. No
declaration-count literal is asserted.

**A8 — REPAIRED — `1524:8.3` (readiness surfaces disagreeing with the graph).**
`papers/D12/bundle_metadata.json` reads `blockers_open: 76`, `advisories_open: 101`,
`open_findings: 177`, `readiness: "RED"`, `blocked_p1_gates: ["FixPropagation",
"ParameterProvenance"]`. The live Gate 11 evaluation reads "194 findings flag this paper
(17 fixed, 0 accepted, 177 open, 76 of them submission-blocking)" and the live blocked P1
set is exactly `{ParameterProvenance, FixPropagation}`. Every field agrees, and
`check_bundle_metadata_matches_graph` now asserts three of them
(`blockers_open`, `advisories_open`, `readiness`).

**A9 — REPAIRED — `1210:8.2` (orphan-finding scan failed open).**
`validate.py` `findings_reach_the_graph` except handler now emits
`Detail("findings_reach_the_graph", False, "orphan scan could not run (…) — treat as
unverified")`.

**A10 — REPAIRED — `1210:8.1` (`readiness_submission_gate` reported "all P1 passed" with
blockers open).** Live output: `D12 — 2 blocked: ParameterProvenance, FixPropagation`. The
evaluation is severity-aware and D12 is no longer in the "all P1 passed" bucket. (The check
still returns `passed=True` overall — that is the documented WARN-only rollout posture in
its own docstring, not this finding's defect.)

**A11 — REPAIRED — `1524:8.1`(a) and `1730:8.3` (the lost round-6 document).**
`papers/AutomatedReviews/2026-07-31-1210-reconstructed-round6/D12.md` exists (9 781 bytes)
and its provenance block carries exactly the coverage statement `1730:8.3` demanded: "This
file recovers 7 findings; round 6 raised 14", with `ad844e42`'s "5 BLOCKER / 9 REQUIRED"
quoted as the source, plus the second correction that rounds 4 and 5 have no D12 document
either. `1524:8.1`(b) — ledger records for the corresponding `0829` findings — is separate
and unmet (see C-group).

**A12 — REPAIRED — `1524:8.2` and `1730:8.2` (readiness checks failing open).**
`check_readiness_verdicts_agree` returns `passed=False` on both its `ImportError` path and
its heatmap `Exception` path, and on zero gate nodes. `check_readiness_submission_gate`
returns `passed=False` on zero gate nodes with the message "treat as unverified, not
passing". The rename-mutation route `1730:8.2` demonstrated no longer yields three green
checks — but note the *sibling* fail-open at the same function's `ImportError` handler
survives and is C10.

**A13 — REPAIRED — `1823:2.2` (`extract_verifies_edges` could not see the tests).**
Gate 4 for D12 now reads `passed — 23 formulas grounding paper claims (23 with
golden/identity/roundtrip, 0 bounds-only, 0 no tests)`. The extractor resolves the aliased
names; no formula is stranded at `unknown` coverage.

**A14 — REPAIRED — `1823:8.2`(a), `2022:8.2` and `2103:8.2` (`review_docs_mint_findings`
scope).** The scope predicate is now content-based (`_SEVERITY_HEADING` minus
`_RESOLVED_HEADING`) rather than directory- or numbering-based, and the live check reports
`130 document(s) carrying unresolved severity-labelled headings checked; 0 mint zero`.
**I mutation-tested it rather than trusting the number**: a planted document in an isolated
`tempfile.mkdtemp()` tree with `PROJECT_ROOT` monkeypatched, containing
`#### Finding A — BLOCKER — …` and `### Finding B: REQUIRED — …` (both shapes the extractor
does not match), minted 0 nodes and the check returned `passed=False` naming the file. The
guard is non-vacuous. `1823:8.2`(b) — `stage13_review_doc` still `null` — is not repaired
and is carried as round-12's advisory 8.8.

**A15 — REPAIRED — `2220:8.1` (BLOCKER: heading-parse closure still executing).
Verified three ways.** (i) Source: `scripts/build_graph.py:1704` is now `status = 'open'`
with nothing after it; the seven lines the round-12 commit message claimed to delete are
actually deleted (`git diff fc624fc5..HEAD -- scripts/build_graph.py`). (ii) Live corpus:
joining `extract_review_finding_nodes()` against `_load_supersession_ledger()` returns
**0** findings closed with no ledger record, down from the 73 round 12 measured. (iii)
Mutation, isolated temp tree, empty ledger: a `🔴`-glyph heading reading `✅ FIXED: the
widget is broken` mints `critical`/**`open`**, and the same heading with
`- **Severity:** recommended` mints `minor`/**`open`**. Round-12's cases B, C, E and F all
now come out `open`. **I also re-derived the 73 figure you reported and it is right**: with
the deleted regexes re-applied to every finding's own heading and the `fc624fc5` ledger, 88
findings closed by heading text alone — 6 critical, 9 major, 1 minor, 72 advisory — of which
the 15 blocking ones were already being forced back open by the severity bar, leaving
exactly **73 (72 advisory + 1 minor)**. That number was producible and is correct.

**A16 — REPAIRED — `2220:8.3` (a bolded marker anywhere in a file overrode every declared
severity).** `build_graph.py:1687-1692` now gates the `_BLOCKER_RE` / `file_has_critical_marker`
escalation on `if _decl is None:`. Mutation, isolated tree: a document whose Summary
contains the bolded marker in prose, carrying a `🟡`-glyph finding that declares
`- **Severity:** advisory`, mints **`advisory`** — the declaration survives. In the other
direction a `🟡` heading declaring `blocker` mints `critical`, so the field is authoritative
both ways. This document exercises the repair: I quoted the marker in prose deliberately
(see the compliance note).

**A17 — WITHDRAWN, NOT REPAIRED-OR-UNREPAIRED — `1730:8.5` and `1823:8.6` (`freshness_stale`
with `blockers_open > 0`).** Round 10 (`2022:8.6`) explicitly withdrew the premise: the
field is an mtime signal, not a readiness verdict, and the "fix" these two asked for would
have corrupted it. `docs/LATE_PHASE6_ABSORPTION_PROTOCOL.md:184` now states the actual
definition, dates the correction, names the finding that forced it, and identifies
`stage13_redo_required` as the Stage-F exit gate. `papers/D12/bundle_metadata.json` carries
`stage13_redo_required: true`. **File these two as `accepted` with a withdrawal rationale,
not as `fixed`** — nothing was repaired because nothing was broken. Seven bundles still
carry `freshness_stale: false` with `blockers_open > 0` (D7, D11, D12, I2, I3, L1, L3) and
that is now the documented correct state.

**A18 — WITHDRAWN — `2022:8.6` (the self-withdrawal itself).** Same disposition:
`accepted`, rationale = the withdrawal, anchor = the `LATE_PHASE6:184` correction.

**A19 — REPAIRED — `1823:5.2` (the false `prxquantum → prx` rationale).**
All three artifacts now carry the corrected account. `paper_draft.tex:4-10`: "Compiling both
gives IDENTICAL extracted text (same md5), 10 pages and 0 Overfull either way — `aps4-2.rtx`
defines `\rtx@apsprx` as a no-op, so no Stage-9 measurement was invalidated."
`scripts/compile_bundle_pdf.py:82-92` and `:122-126` say the same and keep the guard hard
for the reason that survives. `bundle_metadata.json` `notes` carries it. (That the narration
still sits at the top of the submitted `.tex` is round-12's advisory 5.4, a separate item.)

**A20 — REPAIRED — `2022:6.1` (BLOCKER: undisclosed capstone identifications). This is the
second item you asked me to check, and it holds.** I read the binder list from
`BolometricFloors.lean:793-806` myself: thirteen binders, with
`hμ : μ₁ - μ₀ = ∫ x in (0:ℝ)..Tw, hf x * s x` and `hσV : σ = Real.sqrt (Vtot hf)` — the two
equational identifications the finding named — present at `:804`, and
`hs : IntervalIntegrable (fun x => s x ^ 2) volume 0 Tw` at `:802`. `paper_draft.tex:670-680`
names all three, states both are load-bearing and why ("nothing else in the statement ties
the threshold pair $(\mu_0,\mu_1,\sigma)$ to the filtered channel"), and gives the
`s = x^{-1/2}, h_f = x^{1/2}` counterexample for the third. `:682-686` **drops the
completeness claim** rather than restating it at a new number, and names the Lean statement
as the authority — the one form that cannot go stale. **File the closure.**

**A21 — PARTIALLY REPAIRED — `2022:2.2`, `2103:2.2`, `2220:2.2` (Gate 4's formula list).**
The list went 4 → 23; Gate 4 evaluates non-vacuously and all 23 carry strong coverage. Two
residuals stand at HEAD: the D12 region of `formulas.py` has **27** public functions, so
`folklore_miss_floor`, `folklore_gap_exponent`, `gaussian_q`,
`gaussian_threshold_error_floor` and `snr_chain_window_ceiling` — the functions behind §2's
headline numerals — are still outside the gate; and `paper:D12` still has **zero**
`GROUNDED_IN` edges, so the gate's scope remains a hand-written list authored by the party
it audits. Gate 4 also surfaces a non-blocking WARN: "2 grounding formula(s) have no
resolving Lean theorem (unverified): f_link_bath_referred, phonon_psd_gradient_factor".

**A22 — PARTIALLY REPAIRED — `1823:8.1`, `2022:8.1`, `2103:8.1`, `2220:8.2` (the
blocking-closure bypass chain).** The bypass these findings actually demonstrated is closed:
mutation, isolated tree, a `{"finding_id": X, "status": "fixed"}` two-key record against a
finding minting `critical` is **rejected** (`blocking_closure_rejected` set, `status` forced
back to `open`), and with heading-parse gone (A15) no wording closes anything. What survives
is round-12 8.2's items 2 and 3, and I demonstrated three shapes that still close a finding
on a two-key record with no evidence and no anchor — see finding 13.2 below.

**A23 — PARTIALLY REPAIRED — `2220:8.5` (`recurrence_reopens_closures` detection rate).**
The primitive was replaced (common prefix → Jaccard over token sets) and this is a real,
measured improvement, not a restated constant. I built a labelled ground-truth set of
**26 recurrence pairs** from twelve chains the corpus itself declares
("fifth consecutive round", "REPRODUCES verbatim", "restated as requested") and ran the
check's exact `_norm` and predicate over it: **21/26 = 81 %**, against the 7/26 = 27 %
round 12 measured for the prefix version. Residual: the five misses sit at Jaccard 0.000,
0.071, 0.111, 0.333 and 0.400 — inside the band the code comment calls empty — and I
demonstrated the live consequence (finding 13.3). Note also that round-12 8.5's *proposed*
remediation is wrong and should not be implemented: widening the label past `heading[:50]`
**lowers** detection on the same ground truth (81 % at `heading[:80]`, 73 % at `[:120]`,
69 % at `[:200]`), because a longer heading diverges more between rounds. The threshold, not
the truncation, is what binds.

**A24 — PARTIALLY REPAIRED — `2220:8.6` (the recurrence summary's denominator).** The
string was rewritten and now names the excluded populations, which is progress. The
semantic defect the finding named is intact: it reports `318 blocking-severity closure(s)
compared`, but `compared += 1` sits **before** the inner loop, so it counts closures that
entered the loop, not closures with anything to compare against. Re-derived with the check's
own code on the live graph: of those 318, only **162** have at least one same-bundle
later-dated open finding. The summary still overstates its own coverage by **1.96×** — the
constant was refreshed, the arithmetic that made the constant look harmless was not. (Also
unreported: 3 blocking closures and 105 open findings carry no bundle/paper attribution and
can never match.)

**A25 — PARTIALLY REPAIRED — `2103:8.7`, `2220:8.7` (the ledger backlog).** Seven records
were filed at `03a4592e`, six of them D12, breaking a five-round drought — and three of the
six correctly close what they name (A5, A20, and `2103:8.6`). But 76 blocking findings still
flag D12, and **three of the six D12 records name a finding whose subject their evidence
does not describe** (finding 13.1). Net: the backlog is 3 records smaller and 3 records
worse.

**C-group — NOT REPAIRED. Each verified at `03a4592e` from the artifact.**

**C1 — `2022:1.1`, `2103:1.1`, `2220:1.1` — `IrwinHilton2005` unqualified for loop gain.**
`paper_draft.tex:372-373`: "proves it equals the instrument-literature
convention~\cite{IrwinHilton2005,Irwin1995}", no qualifier, while `:443-446` says of the same
source "we have not inspected its text: we hold that source only as a resolved DOI record".
Seventh consecutive round. Cache-verified (`hash 925b58102c`, `verdict match`, `is_stale
False`).

**C2 — `2220:1.2` — `RashidHasan2017` cited for a completed formalization.**
`:625-626` still reads "Transform methods and transfer-function algebra **are formalized** in
HOL Light". **I fresh-fetched `arxiv.org/abs/1705.10050` this round rather than inheriting
round 12's fetch.** Title and authors match the bibitem exactly ("Formalization of Transform
Methods using HOL Light"; Adnan Rashid, Osman Hasan) — not wrong-target. The abstract
verbatim: "we present **an ongoing project**, which focuses on the higher-order logic
formalization of transform methods"; "we present **a roadmap to achieve our objectives, the
current status and the future goals** for this project." A project-status report does not
establish that transfer-function algebra *is* formalized. Record appended.
Cache: fresh-fetch.

**C3 — `2022:1.3`, `2103:1.3`, `2220:1.3` — `arxiv_verified` absent.** I dumped every D12
bibitem's registry entry: `arxiv_verified` is `None` on **all six** arXiv-bearing keys
(`Degenne2025Kernels`, `EchenimMhalla2021`, `MeiburgLessaSoldati2025`, `RashidHasan2017`,
`UnruhCBO2025`, `coqq2022`), so `READINESS_GATES.md:33`'s stated Gate-1 criterion still
cannot be met while `_eval_citation_integrity` passes on a weaker predicate. All 14 bibitems
do resolve, all 14 are registered, and all 14 hit `docs/citation_verifications.jsonl` at
their **recomputed current** hash with `verdict: match` and `is_stale: False` — I recomputed
every hash from the live `.tex`.

**C4 — `1730:2.2`, `1823:2.1`, `2022:2.1`, `2103:2.1`, `2220:2.1` — `MATHER_1982_GRADIENT_REDUCTION`
is not human-verified.** Read from `src/core/provenance.py` at HEAD:
`human_verified_date: None`, `human_verified_notes: None`. Gate 3 reads
`blocked — 1 parameters depended on; 0 human-verified`, which is the correct verdict for an
outstanding operator action. `1730:2.2` also asked for a `DEPENDS_ON` edge — **that half is
repaired** (the edge exists and Gate 3 evaluates on it). The remaining half is one dashboard
sign-off and must not be discharged by an auto-verify path or a reviewer-set date.

**C5 — `2022:4.1`, `2103:4.1`, `2220:4.1` — reversed author order in D10.**
`papers/D10/bibliography.bib:49` reads
`author = {Caballero, Jose Manuel Rodriguez and Unruh, Dominique}`. **I fresh-fetched
`arxiv.org/abs/2512.05878`**: canonical order is 1. Dominique Unruh, 2. José Manuel
Rodríguez Caballero. `papers/D12/paper_draft.tex:766` is right; D10 is wrong at source, so
re-rendering the `.bbl` will not fix it. Gate 2's blocking condition still does not fire
because the two papers use different keys (`isabelleCBO` vs `UnruhCBO2025`) and the registry
carries `arxiv: None` for the D10 one. Record appended. Cache: fresh-fetch.

**C6 — `2022:5.2`, `2103:5.2`, `2220:5.1` — the highest-prior-art-risk paragraph withholds
the name and the `\cite`.** `:641-643` verbatim: "First, an out-of-Mathlib Lean project on
information theory and hypothesis testing is under active development…". No name, no cite —
while `:612-613` names and cites `\textsc{TestingLowerBounds}` two paragraphs earlier and
`:716-717` names it again. And the characterization "on information theory and hypothesis
testing" is the one `prior_art_novelty.md §3` records as retracted (it came from a
web-search snippet); the retraction reached the registry title, the bibitem and the cache,
and not the prose that seeded them.

**C7 — `1730:7.1`, `1823:7.1`, `2022:7.1`, `2103:7.1`, `2220:7.1` — D12's numeric surface is
outside every freshness gate.** Gate 9 evidence at HEAD: `0 REPORTS edges (0 stale); 0
inline numerical literals in body; 0 stale tables/*.tex files`, and it evaluates `passed` on
that empty set, while the draft carries ~28 derived numerals. Every one I recomputed this
round is correct — but rounds 6, 7 and 8 each found a wrong one in this surface and a
reviewer was the only thing that caught each. A21 compounds it: three of the five formulas
omitted from Gate 4's list are the ones behind eight of those numerals.

**C8 — `1823:8.3`, `2022:8.3`, `2103:8.3` — "not evaluated" spelled "passed".** AST sweep of
`scripts/validate.py` for `CheckResult(passed=True)` inside an `except` handler: **12 hits**,
at lines `623, 1956, 1959, 1971, 2112, 2381, 4297, 4648, 5599, 7225, 7233, 7509`. Line 4648
is `check_readiness_submission_gate`'s own `ImportError` handler — the exact site all three
findings name — twelve lines above the early return that *was* corrected. Eighth consecutive
round; the count has not moved.

**C9 — `1730:8.4`, `1823:8.4`, `2022:8.4`, `2103:8.4` — `ledger_ids_resolve` fails open.**
`scripts/validate.py:3421-3424`: the handler emits
`Detail("ledger_ids_resolve", True, f"ledger integrity scan skipped (…)", warning=True)`,
and the enclosing check computes `passed=all(d.passed for d in roster_details)`, so a crash
in the scan is indistinguishable from a clean scan. This matters more now than when it was
first raised: after A15 the ledger is the **sole** channel that can close anything, and this
is the guard on the ledger's referential integrity.

**C10 — `1823:8.5`, `2022:8.5`, `2103:8.5` — 27 blocking-severity `accepted` findings are
invisible to Gate 11.** Measured at HEAD: **27** findings project-wide are `accepted` at
critical/major severity (23 unattributed, plus one each in L1, D4, D3, E2). D12's
`GateResult` has no `accepted_blockers` field and its `notes` string is
"76 open review findings at severity blocker/critical/major (177 open in total)" — the
accepted population appears nowhere. D12 itself currently has `0 accepted`, so this is a
system exposure rather than a D12-local one, but the finding names
`_eval_fix_propagation` and that code is unchanged.

**C11 — `2220:8.4` — `review_severity_declared` has examined zero documents, and its
predicate is unsound.** Live output: `0 review document(s) dated >= 2026-08-01 checked; 0
with findings that do not declare severity`. The four defects round 12 enumerated are all
intact at HEAD: the predicate is the document-level count comparison `n_sev < n_head` (so
declarations are fungible across findings and can come from outside any finding);
`_HEADING = r'^#{3,5}\s+\S'` counts every h3–h5 heading, so a compliant report with an
`### Environment` section is a red build; the date comes from `md.parent.name[:10]`, a
directory name the author chooses, with nothing cross-checking the front-matter
`review_date:`; and `_SEV_LINE` matches the prefix only, so `- **Severity:** yes` passes here
and maps to `None` in `build_graph`. **This finding changed state during this review**: UTC
rolled past the `_CUTOFF` of `2026-08-01` at 00:00 today, so the check finally has documents
in scope. Its first two real measurements, both taken after I wrote this report: it passes
this document (3 finding headings, 3 declarations) and correctly fails a concurrent D11
document (36 headings, 5 declarations) — and I confirmed that failure is a **true positive**,
not the false-positive shape 8.4 predicted, because `_REVIEW_SECTION_RE` finds exactly 36
findings in that file too. So the check's core function works on its first live data. The
four predicate defects above are nonetheless all still present and all still exploitable; and
I had to write this document around one of them — `##` for every non-finding section, exactly
one `- **Severity:**` per `###` — which is itself the finding.

**C12 — `1524:8.1`(b) — ledger records for the `0829` findings.** Still unfiled. The
reconstruction (A11) discharged the (a) half only.

**C13 — `1823:8.2`(b) — `stage13_review_doc`.** `papers/D12/bundle_metadata.json:19` still
reads `null` beside `"last_stage13_review": "2026-07-31"`;
`BUNDLE_DIRECTORY_SCHEMA.md:53` mandates the field and no code reads it.

## Part B — New findings

### 13.1 — 🔴 BLOCKER — three of the six D12 ledger records filed at `03a4592e` close a finding their evidence does not describe; two would have falsely closed live unrepaired defects and one is `fixed` at HEAD

- **Severity:** blocker
- **Gate:** FixPropagation
- **Location:** `docs/review_finding_supersessions.json` (the six records added in `03a4592e`)
- **Observed:** I compared each new record's `evidence` text to the heading of the finding
  its `finding_id` names. Three disagree:

  ```
  finding_id                                     heading of that finding        evidence describes
  2026-07-31-2022 … :D12:5.1  (status: FIXED)    "the ENBW numerical             "ENBW constant 1/4 -> 1/2"
                                                  contradiction still stands:     = round-8 finding 1730:5.1
                                                  … the functionals"
  2026-07-31-2022 … :D12:5.2  (status: open)     "the paragraph that declares    "Crossover corrected to
                                                  the highest prior-art risk      2Q(1)/(1-2Q(1))"
                                                  still withholds the name"       = round-8 finding 1730:5.2
  2026-07-31-2022 … :D12:2.1  (status: open)     "MATHER_1982_GRADIENT_          "The 21.5-point figure and
                                                  REDUCTION still has             its derivation"
                                                  human_verified_date: None"      = round-8 finding 1730:2.1
  ```
  In every case the evidence describes the **round-8 (`1730`)** finding at the same section
  number, and the record was filed against the **round-10 (`2022`)** ID.
- **Evidence:** Two of the three defects at the IDs actually named are **not repaired**, so
  had they stood they would have been false closures of live findings. `2022:2.1` is the
  MATHER human-verification finding: `PARAMETER_PROVENANCE['MATHER_1982_GRADIENT_REDUCTION']
  ['human_verified_date']` is `None` at HEAD and Gate 3 reads `blocked`. `2022:5.2` is the
  withheld-`TestingLowerBounds`-citation finding: `paper_draft.tex:641-643` still contains
  no name and no `\cite`. Neither was repaired by anything in `03a4592e`.
  The third, `2022:5.1`, is **`fixed` right now** on evidence for a different finding; its
  outcome is accidentally correct (the functionals paragraph *is* repaired, A5), but the
  audit trail is false, and `1730:5.1` — a 🔴 BLOCKER — remains `open` with no record while
  the verification of *its* repair sits in another finding's record.
  Nothing catches this. `ledger_ids_resolve` tests only that the ID names an existing node
  (all three do); the blocking-closure bar tests only that `evidence` is ≥40 characters and
  an anchor exists (all three satisfy both). No guard compares the record's evidence to the
  finding it closes. What reversed two of the three was `recurrence_reopens_closures` firing
  for an unrelated reason — round 11 restates the same two findings at the same numbers —
  which is luck, not coverage.
- **Expected:** Re-file the three against the IDs their evidence actually describes
  (`1730:5.1`, `1730:5.2`, `1730:2.1`), each of which I adjudicate REPAIRED above (A5, A6,
  A3). Leave `2022:2.1` and `2022:5.2` **open** — those defects are live. Flip `2022:5.1`
  back to `open` and close it separately against A5's evidence if you want it closed.
- **Fix:** Three `finding_id` corrections plus one new guard leg: for every ledger record
  whose `finding_id` resolves, require a token-overlap floor between the record's `evidence`
  and the finding's own label, and fail on a record that closes a finding it does not
  describe. The `_norm` + Jaccard machinery `check_recurrence_reopens_closures` already
  ships is exactly the primitive this needs.
- **Cache:** n/a

### 13.2 — 🔴 BLOCKER — a declared severity is still a downgrade lever into the two-key ledger bypass; I demonstrated three shapes, in an isolated tree with an empty-but-for-the-record ledger

- **Severity:** blocker
- **Gate:** FixPropagation
- **Location:** `scripts/build_graph.py:1643-1655` (`_decl`) interacting with `:1795-1797`
  (the bar's `severity in ('critical', 'major', 'blocker')` guard)
- **Observed:** Round-12 8.2 specified three remediations. Item 1 (delete heading-parse)
  shipped and closed the *demonstrated* exploit. Items 2 and 3 did not, and the bar is still
  applied only at `critical`/`major`/`blocker`, so anything that lands a genuinely blocking
  finding below that line restores the bypass in full.
- **Evidence:** Mutation run in a `tempfile.mkdtemp()` tree with `build_graph.PROJECT_ROOT`
  monkeypatched and a ledger containing nothing but a **two-key** record
  `{"finding_id": X, "status": "fixed"}` — no evidence, no commit, no date. No repository
  file was read or written.
  ```
  case                                                      minted    status   bar
  🔴 heading, "- **Severity:** recommended"                 minor     fixed    NOT applied
  🔵 heading, "- **Severity:** blocker"                     critical  open     applied  <- control
  🔵 heading, "- **Severity:** blocker" past body[:1200]    minor     fixed    NOT applied
  🔵 heading, "- **Severity:** yes"                         minor     fixed    NOT applied
  ```
  Row 1 is the exploit round 12 named, with the closure now coming from the ledger instead
  of the heading. Row 3 is the window bug: `_decl` searches `body[:1200]` while
  `review_severity_declared` searches the whole file, so a long finding can declare `blocker`
  where the validator sees it and the extractor does not — and several findings in the
  round-12 document exceed 4 000 characters. Row 4 is the value bug: `_MAP.get('yes')`
  returns `None` and the code falls silently to glyph inference with no warning and no
  failure. The control row proves the bar itself works, so this is a scope defect, not a
  broken bar.
- **Expected:** Apply the bar at **every** severity — severity should decide how loudly a
  finding is reported, never whether the closure bar applies, and that is the same
  generalisation `_CLOSING_STATUSES` already made in the status dimension. Widen `_decl` to
  the whole body. Make an unrecognised declared value a hard error rather than a silent
  fallback.
- **Fix:** One predicate widening, one slice removal, one `else: raise`.
- **Cache:** n/a
- **Lane:** `infra`

### 13.3 — 🟡 REQUIRED — the recurrence guard's new threshold was calibrated on a set containing essentially one true positive, and it misses three of the closures you are about to file

- **Severity:** required
- **Gate:** FixPropagation
- **Location:** `scripts/validate.py::check_recurrence_reopens_closures` (`_MIN_OVERLAP = 0.50`)
- **Observed:** The code comment justifies the constant as "0.50 sits in the empty band
  between p99 and that pair", where "that pair" is a single true recurrence at 0.67. A
  threshold set from a distribution containing one labelled positive is the same shape as the
  30-character prefix set from label *lengths* — the quantity measured is not the quantity
  the predicate needs.
- **Evidence:** On the 26-pair labelled set I built from the corpus's own self-declared
  recurrence chains, five true recurrences sit **inside** the "empty band": Jaccard 0.000 and
  0.071 (the Gate-4 formula-list chain), 0.111 (the accepted-blockers chain), 0.333 (the
  MATHER chain) and 0.400 (the `ImportError` chain). Mutation test, in-process, with
  `_load_supersession_ledger` monkeypatched to add one conforming closure at a time and
  restored afterwards — no repository file written:
  ```
  closure planted                                        j        check    guard fired
  1823:8.3  ImportError chain (2022:8.3 still open)      0.400    PASS     NO
  1823:2.1  MATHER chain      (2022:2.1 still open)      0.333    PASS     NO
  2022:8.5  accepted chain    (2103:8.5 still open)      0.111    PASS     NO
  1823:7.1  numeric surface   (2022:7.1 still open)      1.000    FAIL     YES  <- control
  2022:8.4  ledger_ids        (2103:8.4 still open)      1.000    FAIL     YES  <- control
  ```
  The three that evade are findings I adjudicate NOT REPAIRED above (C8, C4, C10) and which
  you are likely to file records for. The two controls prove the guard is live, so this is a
  calibration gap, not a dead check. Separately, the check's own candidate set (closed →
  same-bundle later open) currently peaks at Jaccard **0.429** over 4 455 pairs, i.e. below
  the threshold — that is a property of a corpus with no stale blocking closure right now,
  not a structural incapacity, but it means "0 contradicted" is presently uninformative and
  the summary does not say so.
- **Expected:** Calibrate on the labelled pairs rather than on the unlabelled candidate
  distribution: the corpus's self-declared chains are a free labelled dataset, and a matcher
  scoring under ~23/26 on them has not been tested. At 0.333 detection is 23/26 with 416
  same-bundle pairs above threshold corpus-wide against 328 at 0.50 — a small precision cost
  for two more detections. Do **not** widen the label past `heading[:50]`: I measured that and
  it lowers detection (see A23).
- **Fix:** One constant, plus a `pytest` fixture of the labelled pairs so the next change to
  the matcher is measured against known answers.
- **Cache:** n/a
- **Lane:** `infra`

## Compliance note

I ran no git-mutating command — only `git log`, `git show`, `git diff`, `git status`,
`git rev-parse`. I ran no `pdflatex`, inside `papers/` or anywhere; the D12 diff since round
12 touches no LaTeX input.

All mutation testing was in-process against a monkeypatched `PROJECT_ROOT` or a
monkeypatched `_load_supersession_ledger`, in `tempfile.mkdtemp()` trees removed at the end
of each run, with the real loader restored. **No repository file was mutated by any of it.**

Files I wrote: this report, and two verification records appended to
`docs/citation_verifications.jsonl` (`RashidHasan2017`, `UnruhCBO2025`) after backing that
file up to the scratch directory as `citation_verifications.jsonl.bak-r13` (129 lines before,
131 after). The 132 `figures/*.png` and `lean/lean_deps.json.hash` were already dirty on
arrival and I did not touch them.

Three disclosures about this document. **(1)** I quoted `_BLOCKER_RE`'s bolded literal in
prose deliberately, in A16 and elsewhere, because A16 claims that no longer escalates
anything — extracting this document must mint exactly the three severities declared below
its three `###` headings (one blocker, one blocker, one required), and if it mints anything
else then A16 is wrong and this paragraph is the counterexample. That is the test, run on the
document that makes the claim. **(2)** Every non-finding section uses `##`, not `###`,
because C11 shows an `### Environment` heading would fail `review_severity_declared` despite
every finding declaring its severity. That is a workaround for an undocumented constraint,
not a preference. **(3)** The self-test promised in (1) was run: extracting this
document mints exactly **3** findings at exactly the declared severities
(`13.1 critical`, `13.2 critical`, `13.3 major`), all `open`, despite the bolded literal
appearing in prose — so A16 holds on the document that asserts it, and A15 holds on a
document whose headings contain the words "REPAIRED" and "FIXED" many times over.
`review_docs_mint_findings` passes on it (132 documents checked, 0 mint zero) and
`review_severity_declared` passes on it. Its verdict on this file is that check's first real
measurement and should be read as such rather than as a passing history.

Per rule 10, I have closed nothing. Every finding above remains `open`, including the 31 I
adjudicate REPAIRED. The adjudications are written to be quoted verbatim into ledger records;
filing them is yours.

## QI Candidate

**Systemic: the project has a verified-repair channel and an unverified-provenance channel,
and the ledger is both. Round 13 found that the reviewer's verification is reliable and the
record that transcribes it is not — three of six transcriptions this session named the wrong
finding, and the two that were caught were caught by a guard firing for an unrelated
reason.**

The existing register carries "a guard's scope predicate must not share a blind spot with
the thing it guards" and "every numeric a check prints must be the cardinality of the set it
actually iterated". This is the third member of the family and it is about *provenance text*
rather than *code*: **a record that asserts a fact about an artifact must be checkable
against that artifact by a machine, or it is prose.** Concretely, for the ledger: the record
already names a `finding_id` and carries free-text evidence, and the project already ships a
normalised-token-overlap function (`check_recurrence_reopens_closures._norm`). One leg that
requires overlap between a record's evidence and its target's own label would have caught all
three of this session's mis-targeted records at filing time, and would also catch the
D11 SHA-correction class ("I corrected a commit SHA by reading a commit *message* rather than
the artifact diff") by the same argument extended to `commit`: assert that
`git show <commit> -- <artifact>` is non-empty for the artifact the evidence names.

**Also carried forward, each measured this round:**

1. **Five of D12's eleven gates still pass on empty or curated evidence sets** —
   `CrossPaperConsistency` (0 CONTRADICTS edges project-wide), `ComputationCorrectness`
   (23 of 27, hand-written list, 0 `GROUNDED_IN` edges on `paper:D12`), `AssumptionDisclosure`
   (`0 hypotheses assumed by cited theorems` for a paper with a hypothesis-disclosure
   section), `NarrativeGrounding` (`5 abstract sentences; 0 flagged interesting; 0 SUPPORTS
   edges` for a 26-line abstract), `NumericalFreshness` (`0 REPORTS edges` for ~28 derived
   numerals). `GateResult` should gain an `evidence_count`, and a gate evaluating on an empty
   set should report `open`, not `passed`.
2. **"Not evaluated" is still spelled "passed", eighth round running** (C8) — the identical
   12 handlers. Making the AST sweep a `pytest` case converts a recurring finding into a
   build failure and is a one-line test.
3. **`scripts/citation_cache.stats()` still raises `TypeError: can't subtract offset-naive
   and offset-aware datetimes`** on the cache it summarizes, including on records written
   today. `is_stale` catches `(KeyError, ValueError)` only. No individual D12 bibkey is
   affected — I computed `is_stale` for all 14 and every one returned `False` — but the
   aggregate entry point the CLI and dashboard call is dead for every caller, fourth round
   running.
