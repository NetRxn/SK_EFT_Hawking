---
paper: I2
bundle_target: I2
reviewer: adversarial-reviewer
model: claude-opus-4-7[1m]
review_date: 2026-05-01T14:56:00Z
readiness_gates_version: 1
profile: Tier 3 — Infrastructure (software-paper review)
round: 1
---

# Adversarial Review — I2 (Verified statistical estimators + lean-tensor-categories)

## Summary

Six findings: 0 BLOCKER, 5 REQUIRED, 1 RECOMMENDED. Gates affected:
Gate 1 CitationIntegrity (1 REQUIRED — wolff2004 author-block mismatch);
Gate 7 NarrativeGrounding (3 REQUIRED — uncited Microsoft Quantum
attribution carried from round-1 advisory; an apparently-fabricated
"Steinberg's pivotal-category Coq library" attribution; a misattributed
"Agda \texttt{unimath}" reference in §3 prior-art search and a likely
hallucinated "Isabelle/AFP submission \emph{Tensor Networks}" in §9.1);
Gate 9 NumericalFreshness (1 REQUIRED — §5 declared count of 161 sums
incorrectly; actual is 164, the 3-module count-convention drift
carried from round-2 has a concrete numerical face);
Gate 4 ComputationCorrectness (1 REQUIRED — paper §2 line 232 cites
`tests/test_jackknife.py` which does not exist; correct path is
`tests/test_verified_statistics.py` as figure 4 caption shows);
Gate 5 LeanProofSubstance (1 RECOMMENDED — paper line 783 references
"the Fibonacci universality proof in `FibonacciBraiding.lean`" but no
universality theorem is shipped in that file; only infrastructure
lemmas for the trace-based universality argument). **Submission
verdict: not ready** — REQUIRED-class findings block submission. All
six findings are mechanically remediable in <1 day. No
submission-blocking BLOCKERs surfaced.

## Findings

### 1.1 — 🟡 REQUIRED — `wolff2004` author block mismatches arXiv

- **Gate:** CitationIntegrity
- **Location:** `papers/I2/paper_draft.tex:916-918`; mirror in
  `src/core/citations.py:6611-6626`
- **Observed:** Bibitem reads `U.~Wolff (ALPHA Collaboration)`. The
  `(ALPHA Collaboration)` parenthetical also appears in the registry's
  `authors` field.
- **Evidence:** `WebFetch` on `https://arxiv.org/abs/hep-lat/0306017`
  returns sole author "Ulli Wolff" with no ALPHA Collaboration
  affiliation. No collaboration tag in the published author block on
  Comput. Phys. Commun. 156, 143 (2004) per the standard arXiv-mirror
  metadata. Title and DOI verified-match
  (`10.1016/S0010-4655(03)00467-3`).
- **Expected:** `U.~Wolff` only, OR the parenthetical retained with an
  inline footnote acknowledging it is a community convention rather
  than the author block on the published paper. Wolff was an ALPHA
  member, so the convention is not strictly wrong, but the registry
  treats it as canonical author metadata rather than as a convention.
- **Fix:** Either (a) drop `(ALPHA Collaboration)` from the bibitem
  AND from `CITATION_REGISTRY['wolff2004']['authors']`, or (b) update
  the registry note to mark `(ALPHA Collaboration)` as a community
  convention and keep both bibitem and registry consistent.
- **Cache:** fresh-fetch (recorded as `wrong_author` on
  2026-05-01T14:56Z).

### 5.1 — 🟡 REQUIRED — "Steinberg's pivotal-category Coq library" — attribution not in dossier and not findable on GitHub

- **Gate:** NarrativeGrounding (attribution-claim)
- **Location:** `papers/I2/paper_draft.tex:822-824` ("Notable parallel
  efforts: Steinberg's pivotal-category Coq library (\texttt{Math-Components}
  extension, unmerged)")
- **Observed:** Paper attributes a Coq pivotal-category library to a
  person named "Steinberg" as an unmerged Math-Components extension.
- **Evidence:** `grep -i steinberg Lit-Search/Phase-5o/*.md` returns
  zero matches in the prior-art dossier. `grep -i steinberg Lit-Search/
  -r` shows all Steinberg references in the project are to *the
  Steinberg module* (a representation-theoretic mathematical object
  in Phase-5c documents), not to a person. GitHub search
  `pivotal+category+coq` returns 0 repositories. Phase-5o dossier
  `Mathlib contribution strategy for categorical and number field
  infrastructure.md` enumerates Coq prior art as ViCAR (string-diagram
  rewriting) and VyZX (ZX-calculus) — no Steinberg attribution.
- **Expected:** Either the actual reference (with full citation), or
  removal of the attribution.
- **Fix:** Drop the "Steinberg's pivotal-category Coq library" clause
  if the source cannot be located. If the author has a specific URL
  or arXiv ID in mind, add it as a citation. If the attribution was
  generated from training-data context (a plausible-but-wrong
  hallucination of a person + library), remove it outright.
- **Cache:** N/A (no bibitem; narrative-attribution-only)

### 5.2 — 🟡 REQUIRED — "Agda \texttt{unimath}" miscategorization in §3 prior-art search

- **Gate:** NarrativeGrounding (attribution-claim)
- **Location:** `papers/I2/paper_draft.tex:361` (within the
  "(Mathlib4, Mathcomp-Analysis, HOL~Light, HOL4, Isabelle/AFP, Mizar,
  Agda \texttt{unimath}, Coq's \texttt{Math-Components})" enumeration)
- **Observed:** Paper attributes UniMath to Agda.
- **Evidence:** `WebFetch` on `https://github.com/UniMath/UniMath`
  confirms UniMath is a Coq/Rocq library (99.7% Rocq prover code;
  description: "This Rocq library aims to formalize a substantial
  body of mathematics using the univalent point of view"). No Agda
  UniMath fork exists. Phase-5o dossier consistently uses
  `agda-categories` for the Agda categorical library, never
  `unimath`. This appears to be a name-confusion artifact between
  the Coq UniMath and Agda's `cubical`/`agda-categories` libraries.
- **Expected:** Coq's UniMath, OR Agda's `agda-categories` (whichever
  was actually consulted in the prior-art search). The dossier text
  in Phase-5o suggests the author meant `agda-categories`.
- **Fix:** Replace `Agda \texttt{unimath}` with either `Coq's
  \texttt{UniMath}` or `Agda's \texttt{agda-categories}`. If both were
  consulted, list both correctly.
- **Cache:** N/A

### 5.3 — 🟡 REQUIRED — "Isabelle/AFP submission \emph{Tensor Networks}" not findable in AFP

- **Gate:** NarrativeGrounding (attribution-claim)
- **Location:** `papers/I2/paper_draft.tex:827-829` ("the
  Isabelle/AFP submission \emph{Tensor Networks} (covers Penrose-style
  tensor diagrams, not the categorical hierarchy)")
- **Observed:** Paper attributes a "Tensor Networks" submission to
  Isabelle/AFP that covers Penrose-style tensor diagrams.
- **Evidence:** `WebFetch` on `https://www.isa-afp.org` returned no
  AFP entry titled "Tensor Networks" or covering Penrose tensor
  diagrams. The closest match is "The Tensor Product on Hilbert
  Spaces" (Unruh, 2024), which is functional analysis on
  tensor products in Hilbert spaces, NOT tensor-network theory or
  Penrose diagrams. Phase-5o dossier `Mathlib contribution strategy
  for categorical and number field infrastructure.md` does not
  reference an Isabelle/AFP "Tensor Networks" entry.
- **Expected:** Either the correct AFP entry name + URL, or removal.
  Given the round-1 / round-2 stage-10 cleanup logic, this clause
  appears to be hallucinated.
- **Fix:** Drop the Isabelle/AFP \emph{Tensor Networks} clause unless
  the author can supply the exact AFP entry name + URL.
- **Cache:** N/A

### 5.4 — 🟡 REQUIRED — Microsoft Quantum reference uncited (carried from round-1)

- **Gate:** NarrativeGrounding (attribution-claim)
- **Location:** `papers/I2/paper_draft.tex:780-782` ("the Microsoft
  Quantum program's published Majorana-fermion-based proposals")
- **Observed:** Paper attributes Majorana-fermion proposals to
  Microsoft Quantum without a bibliographic citation. This advisory
  was carried from the round-1 stage-10 review per
  `bundle_metadata.json` notes; round-2 marked it as still open.
- **Evidence:** No `\cite{...}` or bibitem accompanies the claim. The
  Microsoft Quantum program has indeed published Majorana-based
  topological-QC proposals (e.g., Karzig et al., PRB 2017;
  Hyart et al., Nature Comm 2013); a citation should accompany the
  attribution.
- **Expected:** A bibitem citation to the relevant Microsoft Quantum
  publication(s), e.g., the Karzig et al. 2017 review or a more
  recent Microsoft Quantum roadmap paper.
- **Fix:** Add a bibitem and `\cite{}` for at least one canonical
  Microsoft Quantum Majorana publication.
- **Cache:** N/A (no bibitem yet)

### 4.1 — 🟡 REQUIRED — Wrong test-file path in §2.2 line 232 (Tier-3 traceability)

- **Gate:** ComputationCorrectness (Tier-3 software-paper profile:
  each function must trace to a passing test)
- **Location:** `papers/I2/paper_draft.tex:232` (`The library
  includes such a numerical test for redundancy
  (\texttt{tests/test\_jackknife.py})`).
- **Observed:** Paper §2.2 paragraph for `jackknifeVariance_nonneg`
  cites the test file as `tests/test_jackknife.py`. This file does
  not exist.
- **Evidence:**
  ```
  $ ls SK_EFT_Hawking/tests/test_jackknife.py
  ls: ... No such file or directory
  $ ls SK_EFT_Hawking/tests/test_verified_statistics.py
  SK_EFT_Hawking/tests/test_verified_statistics.py
  ```
  Figure 4 caption (line 270) correctly references
  `tests/test_verified_statistics.py`. The test file does include
  the relevant tests (`test_nonnegative`, `test_lag_zero_nonneg`,
  `test_uncorrelated`, `test_ge_half`, etc.).
- **Expected:** `tests/test_verified_statistics.py`.
- **Fix:** Replace `tests/test\_jackknife.py` on line 232 with
  `tests/test\_verified\_statistics.py`. This is a Tier-3 software-
  paper integrity issue (per the I2 anchor list, "each function must
  trace to a passing test"); a wrong test-file path defeats the
  traceability requirement that distinguishes I2 from a non-software
  paper.
- **Cache:** N/A

### 7.1 — 🟡 REQUIRED — §5 declared count "161 declarations" sums to 164 in actual code

- **Gate:** NumericalFreshness (count-literal drift)
- **Location:** `papers/I2/paper_draft.tex:514-515`
  ("The cumulative count is 161 declarations across the nine
  algebraic-number-field modules.")
- **Observed:** Paper-by-module declared counts in §5 sum to 161 but
  three of the nine module counts are off-by-one vs the current
  Lean source:
  | Module | Paper says | Actual (counted with `^(theorem|lemma|def|noncomputable def|abbrev|class|structure|inductive) `) |
  |---|---|---|
  | QSqrt2 | 11 | 11 ✓ |
  | QSqrt3 | 20 | 20 ✓ |
  | QSqrt5 | 14 | 14 ✓ |
  | QCyc3 | 11 | **12 ✗** |
  | QCyc5 | 29 | 29 ✓ |
  | QCyc5Ext | 36 | 36 ✓ |
  | QCyc15 | 18 | **19 ✗** |
  | QCyc15SqrtPhi | 10 | **11 ✗** |
  | QCyc16 | 12 | 12 ✓ |
  | **§5 total** | **161** | **164** |

  This was flagged at round-2 as a count-convention drift advisory
  but the actual numerical impact is concrete: the cumulative
  number quoted in prose (161) does not equal the sum of the
  per-module numbers when those per-module numbers are recounted
  against current source.
- **Evidence:** Direct grep on each module file:
  ```
  $ for f in QSqrt2 QSqrt3 QSqrt5 QCyc3 QCyc5 QCyc5Ext QCyc15 \
            QCyc15SqrtPhi QCyc16; do
      grep -cE '^(theorem|lemma|def|noncomputable def|abbrev|class|structure|inductive) ' \
        lean/SKEFTHawking/${f}.lean
    done
  → 11 20 14 12 29 36 19 11 12 (total 164)
  ```
- **Expected:** Either (i) update §5 module-by-module counts and the
  cumulative "161" to the current 164 figure, OR (ii) document the
  count-convention used (e.g., excluding `private abbrev` /
  `local notation` constructs) and apply it consistently. Sub-wave
  7a.0 update_counts.py ran ExtractDeps for the bundle freshness
  pass; ParamProv human-verify is the only sub-task still pending.
- **Fix:** Replace the four numbers (`11` → `12` for QCyc3; `18` →
  `19` for QCyc15; `10` → `11` for QCyc15SqrtPhi; `161` → `164`
  for the cumulative). Best regenerated via
  `scripts/update_counts.py` against `counts.json` once the
  per-module counts are added to the canonical macro file.
- **Cache:** N/A (count freshness, not citation freshness)

### 5.5 — 🔵 RECOMMENDED — "the Fibonacci universality proof in `FibonacciBraiding.lean`" — no `fibonacci_universal` theorem in the file

- **Gate:** LeanProofSubstance (boundary case: a referenced "proof"
  that is actually only its supporting infrastructure)
- **Location:** `papers/I2/paper_draft.tex:783-787` ("the Fibonacci
  universality proof in \texttt{FibonacciBraiding.lean} is the formal
  counterpart of the universality result that motivates topological-QC
  research")
- **Observed:** Paper prose treats the universality result as if it
  were itself a shipped theorem in `FibonacciBraiding.lean`.
- **Evidence:** `grep '^theorem ' lean/SKEFTHawking/FibonacciBraiding.lean`
  returns 32 theorems on F-symbol identities, σ-matrix
  determinants/traces, braid commutators, σ-matrix orders (`R1_order_5`,
  `Rtau_order_divides_10`, etc.), and `s1s2_cu_not_identity` /
  `s1s2_6_diagonal` (cube non-identity). The file's docstring claims
  "FIRST verified universal quantum gates in any proof assistant" but
  there is no theorem named `fibonacci_universal` or equivalent stating
  the FLW-style density result. Comments at lines 150 and 167 refer to
  the "trace-based universality argument" — supporting infrastructure
  for an FLW universality argument, not a theorem stating that braid
  generators densely fill SU(2) or SU(N).
- **Expected:** Either (i) shipped universality theorem in the file,
  or (ii) reword paper to acknowledge that the file ships
  *infrastructure* for the Freedman–Larsen–Wang universality argument
  rather than the universality theorem itself, and explicitly defer
  the universality theorem to follow-up work.
- **Fix:** Soften §8 line 783 to read e.g. "the Fibonacci-anyon
  R-matrix and braid-generator infrastructure in
  \texttt{FibonacciBraiding.lean} is the formal substrate of the
  Freedman--Larsen--Wang universality argument; the universality
  theorem itself is targeted for follow-up work, with the σ_1·σ_2
  cube non-identity (`s1s2_cu_not_identity`) and 6-th-power
  non-trivial diagonal (`s1s2_6_diagonal`) shipped as load-bearing
  prerequisites." Recommended (not blocker) because the
  characterization is a soft overclaim — the supporting machinery
  IS shipped, the theorem statement is just not labeled as
  `fibonacci_universal`.

## Cross-bundle consistency

Cross-bundle anchor list per
`docs/agents/claims-reviewer-bundle-prompts.md` §I2 specifies:
"VerifiedJackknife test suite cross-references; lean-tensor-categories
Mathlib upstream cycle (when a PR is in review)." No PR is yet in
review (R1/R2/R3 relationship gates not yet cleared per §7), so the
upstream-cycle cross-bridge does not apply yet. The
VerifiedJackknife test-suite cross-reference is what drives finding
4.1 above — the test-file path discrepancy is exactly the kind of
cross-reference the I2 anchor list backstops.

`turaev2010` is shared with `paper16_wrt_tqft/paper_draft.tex`. The
two bibitems differ in surface form (`V.~G.~Turaev` vs `V.~Turaev`;
"de Gruyter, 2010" vs "de Gruyter Studies in Mathematics 18 (2010
ed.)") but both refer to the same canonical Turaev book; the registry
canonicalizes them to a single entry. Not a Gate-2 finding.

## QI Candidate

**qi-attribution-prior-art-cross-check.** Three of the six findings
(5.1 Steinberg, 5.2 unimath/Agda, 5.3 Isabelle/AFP Tensor Networks)
are attribution-claim hallucinations or misattributions in the §9
"Related work" prose where the paper enumerates parallel efforts in
other proof assistants. The Phase-5o prior-art dossier was correctly
pointed-at-by the paper, but specific named attributions in §9 were
not cross-checked against the dossier. **Pipeline gap:** Stage 10
claims-reviewer's bundle prompt does not explicitly require that
each named third-party library / submission attributed in §9
"Related work" sections trace to a specific Phase-N dossier
sentence + GitHub URL or AFP URL. A check of the form "for every
attribution `<Person>'s <library>` or `<system>/<repo> submission
<title>` in §9, require either a `\cite{}` to a primary source OR a
non-empty `grep` hit in the relevant `Lit-Search/Phase-N/*.md` AND a
working URL" would catch all three of 5.1/5.2/5.3 deterministically.
This is bundle-level only — the per-paper Stage-10 review couldn't
catch this without bundle-prompt context. Suggest adding to
`scripts/validate.py --check related_work_attribution` and to the
bundle-prompts §I2 / §I1 / §F anchor list.

