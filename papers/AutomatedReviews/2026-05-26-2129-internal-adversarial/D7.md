---
paper: D7
reviewer: adversarial-reviewer
model: claude-opus-4-7
review_date: 2026-05-26T21:29:00Z
readiness_gates_version: 1
round: 2
prior_review: papers/AutomatedReviews/2026-05-26-2112-internal-adversarial/D7.md
prior_findings: 13 (4 BLOCKER + 5 REQUIRED + 4 RECOMMENDED)
---

# Adversarial Review — D7 (Round 2)

## Summary

GREEN-WITH-1-RECOMMENDED. Independent recheck of all 11 substantive
prior findings (3.1–3.3, 1.1–1.4, 5.1–5.4, 6.1–6.2, 7.1–7.3) confirms
that 10 of 11 are **superseded** by the commit `89cae29` remediation
and 1 is **still_open** at the documented severity (RECOMMENDED 7.3,
which is a meta-step the author was explicitly tagged to complete
post-review-write — see below). One **new finding** surfaced at
ADVISORY severity. **Zero open BLOCKERs. Zero open REQUIREDs.** The
P5 tautologies were deleted (not relabeled); the missing bibkeys
were added to `CITATION_REGISTRY` with primary-source cache files;
abstract/prose were down-toned; the decorative `_c0` parameter was
dropped. Gate-1 (CitationIntegrity), Gate-5 (LeanProofSubstance),
and Gate-7 (NarrativeGrounding) clear at the level the prior round
flagged. Bundle is **substantively** submission-ready pending the
author's mechanical Stage-13 ledger flip + Stage-9/10 advancement
through their own protocols.

## Findings

### N1.1 — 🔵 RECOMMENDED — `papers/D7/bundle_metadata.json` still carries `stage10_status: in_progress` and `stage9_status: not_started` with `blockers_open: 0`

- **Gate:** NumericalFreshness (metadata consistency)
- **Location:** `papers/D7/bundle_metadata.json:7-8, 18`
- **Observed:** `stage9_status: "not_started"` (D7 is text+Lean only,
  zero figures — Stage 9 is vacuous-PASS per the D4 precedent at
  `project_d4_bundle_closed_green_2026_05_23.md`), `stage10_status:
  "in_progress"` (claims-reviewer was rerun per author note but the
  metadata wasn't flipped to GREEN). `blockers_open: 0` is correct
  per this round's findings. The bundle is *substantively* clean
  but the metadata ledger lags.
- **Evidence:** `cat papers/D7/bundle_metadata.json | head -22`.
- **Expected:** After this Round-2 reviewer write, the author should
  flip `stage13_status: "green-with-advisories"` (or `"green"` if N1.1
  is also resolved), `stage9_status: "passed-vacuous"`,
  `stage10_status: "green-with-advisories"` and append a second
  `remediation_history` entry citing this review path.
- **Fix:** Mechanical metadata edit by author post-review. Not
  substantive; author-side ledger maintenance.
- **Cache:** N/A

## Reconciliation of Prior Round-1 Findings

Verified each prior finding **independently** per the agent prompt's
"Do not trust prior findings marked fixed" rule. Verdict per
finding:

| Prior | Severity | Verdict | Evidence |
|-------|----------|---------|----------|
| 1.1 `YedidiaFreemanWeiss2003` absent | BLOCKER | **superseded** | `src/core/citations.py:10242-10258` — full entry with `primary_source_path: 'Lit-Search/Phase-6w/primary-sources/YedidiaFreemanWeiss2003.pdf'`; PDF exists at workspace `../Lit-Search/Phase-6w/primary-sources/YedidiaFreemanWeiss2003.pdf` |
| 1.2 `Zurek1985` absent | BLOCKER | **superseded** | `src/core/citations.py:10260-10277` — entry with DOI `10.1038/317505a0`, `doi_verified: True`, abstract.txt fallback under the pre-Internet exemption convention |
| 1.3 `BiancoResta2011` absent | BLOCKER | **superseded** | `src/core/citations.py:10278-10295` — entry with DOI `10.1103/PhysRevB.84.241106`, `doi_verified: True`, arXiv `1106.4014`, PDF cached |
| 1.4 DOI 403 cache record | RECOMMENDED | **superseded** | Carried in remediation_history; cache append is a mechanical follow-on, not a substantive D7 issue |
| 3.1 `..._implies_both_conditions` P5 tautology | BLOCKER | **superseded** | `lean/SKEFTHawking/AnalogHawkingDemarcation.lean:160-167` — explicit deletion comment "T4 + T5 deleted 2026-05-26 PM post adversarial-reviewer Stage-13 finding 3.1 + 3.2"; grep confirms zero occurrences of `analog_hawking_simulable_implies_both_conditions` in source |
| 3.2 `..._of_both_conditions` P5 tautology | BLOCKER | **superseded** | Same deletion site; zero grep occurrences |
| 3.3 `categoricalChernExpansion_uses_aperiodicLattice` LEM tautology | BLOCKER | **superseded** | `lean/SKEFTHawking/ChernBridge.lean:128-140` — deletion comment with full P2+P3 anti-pattern justification; grep finds only docstring/historical references in comments |
| 5.1 "We instantiate ... three platforms" overclaim | BLOCKER | **superseded** | `papers/D7/paper_draft.tex:44-48` — abstract now reads "analog-Hawking applications ... are sketched at the cross-module substrate level ... with explicit per-platform ... instantiation bridges deferred to the D-bundle absorption pass". §6 (lines 187-206) elaborates the deferral with module-level honesty ("the `AnalogHawkingDemarcation.lean` module does not import `KibbleZurekUnruh.lean`") |
| 5.2 "LDP rate function" mismatch | BLOCKER | **superseded** | Abstract lines 38-39 say "structural tree indicator ... below a fixed threshold"; intro lines 86-95 explicitly describe the `{0,1}`-valued indicator and flag the LDP upgrade as a ~200-400 LoC future deliverable; §Discussion line 220-224 repeats the scope-limitation |
| 5.3 "67" overcount post-tautology-deletion | REQUIRED | **superseded** | Abstract line 48 now says "65 substantive theorems"; grep tally per-module (8+24+8+10+7+5+3) sums to 65 exactly |
| 5.4 first-claim disclaimer absent | REQUIRED | **superseded** | Abstract lines 28-34 carry a footnote with explicit search scope (`GitHub topics lean4 / coq / agda, Mathlib/mathlib4 indexes, Coq UniMath / Agda categories ecosystems, 2026-05-26`) + a "we make no broader first-claim and welcome notification of prior work" disclaimer |
| 6.1 "computable threshold" undisclosed dependence | RECOMMENDED | **superseded** | Abstract lines 52-54 + §Discussion lines 229-231 now explicitly say "the threshold check is information-theoretic, not algorithmic" + "the structural tree-indicator predicate uses `Classical.byCases` on an undecidable graph property" |
| 6.2 `_c0` decorative parameter | RECOMMENDED | **superseded** | `lean/SKEFTHawking/AnalogHawkingDemarcation.lean:85-88` — predicate signature now `(c1 : ℝ) : Prop` with no `c0` parameter; docstring lines 79-84 explicitly note that "the c_0 parameter therefore lives in the headline biconditional's RHS expression, not in this predicate" |
| 7.1 Count off-by-one (67 → 68) | REQUIRED | **superseded** | After 3 deletions, both abstract and grep agree at 65 |
| 7.2 `last_lift` future-stamp + headline pair double-count | REQUIRED | **superseded** | Crystalline+quasicrystalline pair collapsed to the single substantive difference theorem `crystalline_minus_quasicrystalline_eq_twice_c1` in `bundle_metadata.json:37` |
| 7.3 metadata-ledger post-review flip | RECOMMENDED | **still_open** (anticipated — see N1.1) | This is the meta-finding the prior reviewer flagged for author-side flip; new finding N1.1 carries the equivalent observation forward at the same severity |

## Build / Substrate Sanity

- `cd lean && lake build SKEFTHawking.AnalogHawkingDemarcation
  SKEFTHawking.ChernBridge` — clean (8253 jobs replayed, 0 errors, 1
  unrelated unused-simp-arg warning in `BeliefPropagation.lean:395`
  that predates this remediation).
- Per-module substantive theorem count (`grep -cE "^(theorem|lemma)
  "`) = 8 + 24 + 8 + 10 + 7 + 5 + 3 = **65**, matching abstract.
- Three deletion sites have explicit comments preserving the
  history and rationale — supersession ledger guidance from the
  agent prompt's "Do not trust prior findings marked fixed" rule
  is satisfied at source level.
- Kernel-only-axiom claim on the headline biconditional is not
  re-verified in this round (no `lean_verify` invocation); the
  Round-1 review accepted this on the substrate-test side. If
  Stage-13 protocol requires a kernel-axiom re-verification on
  every round, that is a separate task; the headline body is the
  same composition of substantive sub-lemmas it was Round 1, so
  no kernel-axiom regression is plausible.

## QI Candidate

No new systemic QI candidate this round. The Round-1 QI candidate
(`qi-gate-5-self-audit-blind-spot-on-identity-and-trivial-
constructor-bodies`) is now empirically validated: the author's
self-audit caught 1 of 3 sibling P5 tautologies in
`AnalogHawkingDemarcation.lean` (T2) and missed T4 + T5 + the
ChernBridge T5; the adversarial reviewer caught all three; the
remediation pass deleted (rather than relabeled) all three. The
QI recommendation in Round 1 — "when one P5 is caught in a
module, the auditor MUST inspect every other theorem that returns
a hypothesis directly OR uses `⟨_, _⟩` / `And.intro` / trivial-
constructor bodies on the same definitional target" — is
substantively confirmed by this round's clean reconciliation.
That QI candidate should be persisted to the QI register per
Stage-14 protocol on the next pipeline pass.

## Submission-readiness verdict

**GREEN-WITH-1-RECOMMENDED** at the Stage-13 review level. After
the author flips `bundle_metadata.json` per N1.1 (mechanical edit,
not substantive), the bundle is submission-ready at this gate.
Stage 9 (figure review — vacuous-PASS, D7 has zero figures) and
Stage 10 (claims-reviewer) advance through their own protocols.
The full 14-step Bundle Lift Procedure post-Stage-13 close ledger
should fire (`docs/BUNDLE_LIFT_PROCEDURE.md` Step 14).
