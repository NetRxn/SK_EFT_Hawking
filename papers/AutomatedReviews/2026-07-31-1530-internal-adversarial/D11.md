---
paper: D11
bundle_target: D11
reviewer: adversarial-reviewer
model: claude-opus-5
review_date: 2026-07-31T15:30:00Z
readiness_gates_version: 1
artefact_commit: c60e83ad
round: 7
---

# Adversarial Review — D11 (Kernel-Verified Topological Band Theory and Metamaterial Substrate)

## Summary

Round-7 fresh-context sweep, classes 1–8 in order, against `c60e83ad`: **1 BLOCKER / 8 REQUIRED / 7 RECOMMENDED**, touching CitationIntegrity, CrossPaperConsistency, LeanProofSubstance, NarrativeGrounding, AssumptionDisclosure, NumericalFreshness and FixPropagation. `validate.py` is 53/53 green, so Stages 1–12 hold and this review is meaningful. Twelve of the twenty round-6 findings are genuinely fixed in the tree, four reproduce, three are fixed-but-differently and one is fixed in its primary layer only (per-finding verdicts in the reconciliation table below) — the PhysLib dependency is gone from the abstract and acknowledgments (and I confirmed the import multiset is Mathlib ×12 plus intra-bundle only), the `EffectiveModuli` docstring and module title are rewritten, the notebook's necessity print is corrected in source *and* stored output, the four evenness lemmas are real and kernel-pure, `sum_rawCurl_eq_zero` now genuinely calls `sum_forwardDiff_eq_zero`, the Python FHS port is faithful **by construction** (I audited it declaration-by-declaration against `bzPhase`/`structureFactor`/`haldaneD`/`lbVec`/`linkOfFrame`/`rawCurl`/`principal`/`branchIndex`, not by sampling), and the `voigt_sub_reuss_eq` history clause checks out against `git show 230b3528`. The single blocker is the same species round 6 named and the QI register predicted: a retraction applied to the `.tex` and to `visualizations.py` but not to the *executed companion notebook*, whose four stored figure outputs all predate the Stage-9 remediation arc and one of which still renders "Voigt–Reuss elastic **bounds**" and "**effective modulus** M". **Pass criterion (zero BLOCKER) is NOT met.**

## Round-6 reconciliation (`2026-07-31-1059-internal-adversarial/D11.md`)

Verdict on every round-6 finding, re-derived from the current tree rather than from the ledger.

| R6 | severity | verdict | evidence |
|---|---|---|---|
| 1.1 cache gate blind to Authors/Venue/Year/DOI | REQUIRED | **fixed** | `validate.py:4431-4480` now compares Authors (surname-set, initials-tolerant), DOI, arXiv and Title, with Year advisory. The round-2 author-only drift would now fail. |
| 1.2 `Phase-6C` vs `Phase-6c` silent no-op | REQUIRED | **fixed-but-differently** | The case-sensitivity half is fixed by explicit case-insensitive resolution (`:4394-4415`) rather than by normalizing the registry token — the paths still say `Phase-6C` and the real directory is `Phase-6c`. The "fail rather than skip" half is **not** fixed: `:4413-4414` still `continue`s when a declared `primary_source_path` cannot be resolved. Practical Linux-CI risk is gone; the silent-skip class is not. |
| 1.3 false `promote_primary_sources.py` claim | REQUIRED | **fixed** | Corrected in all three places: `validate.py:4378-4383`, and both cache files (`…/Phase-6c/primary-sources/{BoldoLaxMilgram2016,LeanLJ2025}.abstract.txt:34-44`) now say "THAT IS FALSE, and it was asserted without reading the script". |
| 1.4 Wiener/Voigt/Reuss uncited | RECOMMENDED | **fixed** | `Voigt1889`, `Reuss1929`, `Milton2002` bibitems + registry entries + cache records added; all three verdict `match` on independent fetch. The mechanism used to satisfy the gate is finding 1.1 above. |
| 1.5 `LiPaulson2018` / `LjungstromMortberg2024` venues | RECOMMENDED | **reproduces** | → this round's 1.3. Bibitem hashes unchanged; published records re-fetched and still absent from both bibitem and registry. |
| 1.6 `arxiv_verified` absent from the registry | RECOMMENDED | **reproduces** | → this round's 1.5. 8 of **652** entries carry the field (was 8 of 649). |
| 2.1 ParameterProvenance | n/a | **still n/a** | No experimental parameter in the bundle. |
| 3.1 `effectiveModuli_enclosure` VRH docstring | 🔴 BLOCKER | **fixed** | Module title `:4`, theorem docstring `:86-99`, and the `EffectiveMediumBounds.lean:32-36` cross-reference all rewritten and now mutually consistent. Residual one layer down: finding 3.2. |
| 3.2 enclosure bundle redundancy | RECOMMENDED | **reproduces** | → this round's 3.1. Strictness added; the conjunct-1 entailment the finding named is untouched and the proof term is unchanged. |
| 4.1 `Phase6CE_Roadmap.md` misnomers | RECOMMENDED | **fixed** | `:5`, `:12`, `:39` all carry dated retractions for both the Hashin–Shtrikman and the Voigt–Reuss–Hill names. |
| 5.1 PhysLib in abstract + acknowledgments | 🔴 BLOCKER | **fixed** | Verified by exhaustive import scan of all 22 modules: Mathlib ×12 + intra-bundle only, zero PhysLib. |
| 5.2 notebook necessity claim | 🔴 BLOCKER | **fixed in its primary layer only** | Cell 11 source **and** stored output corrected. The finding's named secondary (`LAB_NOTEBOOK_INDEX.md:32`) is unchanged → this round's 4.3. And the same artifact hosts a new instance of the same class → this round's **5.1** (a new BLOCKER). |
| 5.3 "six of these seven" | REQUIRED | **fixed-but-differently** | Clause [5] correctly split into Maxwell–Garnett and Wiener/HS (8 items now), and the two non-deliveries are correctly named for the first time — but the count was restated as "five" where the list gives six → this round's 5.2. |
| 5.4 `prior_art_novelty.md` §4 attribution | REQUIRED | **fixed** | `:166-180` fully rewritten with the correct Maxwell Garnett 1904 / HS 1962 EM / HS 1963 elastic / Voigt 1889 / Reuss 1929 attributions. Survives one layer over in `CITATION_REGISTRY` → this round's 1.2. |
| 5.5 Fig. 4 caption "bounds" | RECOMMENDED | **fixed** | `:274-276` now "the Voigt and Reuss elastic *averages* --- not bounds on a physical `M_eff`". The notebook's stored copy of that same figure is not → this round's 5.1. |
| 5.6 `±π` not formalized | RECOMMENDED | **fixed** | Six new kernel-pure lemmas; caption claims exactly what they state. |
| 5.7 `3.3177` had no computational source | RECOMMENDED | **fixed** | `haldane_lattice_chern` + `haldane_flip_location` + 5 tests + `visualizations.py:15250` call. Port audited faithful by construction. Residual precondition gap → this round's 6.1. |
| 5.8 `voigt_sub_reuss_eq` history clause | RECOMMENDED | **fixed** | Confirmed against `git show 230b3528:lean/SKEFTHawking/EffectiveModuli.lean:18`, which asserted the exact identity while only `reuss_le_voigt` existed. |
| 5.9 `sum_forwardDiff_eq_zero` orphan | RECOMMENDED | **fixed** | `FHSLatticeGauge.lean:97-104` now discharges both forward differences with it; the citation is backed by a call. |
| 7.1 counting rule not reproducible | REQUIRED | **fixed-but-differently** | The `abbrev` half is fixed and disclosed, and the figures moved to generated macros (the stronger remedy). The rule as stated still does not reproduce, now by 4 lines instead of 2 → this round's 7.1; and the file the fix text named, `PAPER_DRAFT_MAPPING.md:109`, was not updated → this round's 4.1. |
| 7.2 count literals hardcoded, no REPORTS edge | RECOMMENDED | **fixed** | `count_literals` → "D11 — no count literals found (macros in use)". REPORTS edges still absent, but generated macros make the drift half moot; not re-raised. |
| 8.1 ProductionRunHealth | n/a | **still n/a** | No simulation claim. |

Ledger state does not match this table — see finding 4.2.


## Findings

### 1.1 — 🟡 REQUIRED — the `primary_source_unreachable` exemption is takeable by any registry entry, including ones with a live cached arXiv abstract, and once taken removes the entry from every programmatic content check

- **Gate:** CitationIntegrity
- **Location:** `scripts/validate.py:4276-4297` (the exemption) and `:4386-4488` (the cache-content half it bypasses); `src/core/citations.py` `Voigt1889` / `Reuss1929` / `Milton2002`
- **Observed:** The guard is `entry.get("primary_source_unreachable") is True` plus `str(entry.get("notes") or "").strip()`. Nothing constrains *which* entries may take it, and the content-agreement half of the check iterates only over entries that have a `primary_source_path` — so an exempt entry is verified by nothing at all.
- **Evidence:** In-memory mutation test against the live check function (no files touched), each mutation reverted before the next:
  ```
  BASELINE                                                          passed=True  fails=[]
  MUT-A  arXiv-bearing, cacheable entry takes the exemption         passed=True  fails=[]
  MUT-B  exemption + one-character `notes`                          passed=True  fails=[]
  MUT-C  unreachable-exempt entry with a fabricated DOI             passed=True  fails=[]
  MUT-D  unreachable-exempt entry with a hallucinated title         passed=True  fails=[]
  MUT-E  unreachable-exempt entry with doi_verified=False           passed=True  fails=[]
  MUT-F  ALL 11 remaining D11 bibkeys drop their caches at once     passed=True  fails=[]
  ```
  MUT-A is `LeanLJ2025` — the bibkey whose *hallucinated title and three wrong author initials* were a round-2 BLOCKER. Setting `primary_source_unreachable: True` and `primary_source_path: None` on it deletes the only check that would ever have caught that again, and the gate stays green. MUT-D is the round-2 defect itself, re-injected into an exempt entry: green.
  Separately, the justification recorded for two of the three entries is **factually wrong**. `Voigt1889`'s note says the 1889 text "is not available through the whitelisted scholarly-primary egress". `api.semanticscholar.org` is on the whitelist (`harness_web_egress_guard.py:_WHITELIST` → `semanticscholar.org`), and I fetched, this round:
  ```
  DOI:10.1002/andp.18892741206 → {"title":"Ueber die Beziehung zwischen den beiden
      Elasticitätsconstanten isotroper Körper","authors":[{"name":"W. Voigt"}]}
  DOI:10.1002/zamm.19290090104 → {"title":"Berechnung der Fließgrenze von Mischkristallen auf
      Grund der Plastizitätsbedingung für Einkristalle .","authors":[{"name":"A. Reuß"}]}
  ```
  That is exactly the `Title:` / `Authors:` / `DOI:` header the cache-content gate consumes. For `Voigt1889` and `Reuss1929` a header-only cache file is constructible today from a whitelisted host, so the exemption is not merely dangerous, it is unnecessary. (`Milton2002` is the honest case: CUP elides the abstract, though a header-only cache is still constructible.)
  **The three citations themselves are correct** — verdict `match` for all three, records appended to `docs/citation_verifications.jsonl`. The finding is the mechanism, not the entries.
- **Expected:** An exemption written because three pre-digital works cannot be cached must not be takeable by an arXiv-bearing 2025 preprint, and must not move an entry outside every content check.
- **Fix:** (a) Refuse the exemption when `entry.get("arxiv")` is set (an arXiv abstract is always cacheable through the whitelist) — that alone kills MUT-A and MUT-F. (b) Require the exempt entry to carry `doi_verified: True` and a `verification_source` naming the whitelisted host used, and fail if absent. (c) Add a header-only cache class (`Title:`/`Authors:`/`Year:`/`DOI:` with no abstract body) so `Voigt1889`/`Reuss1929` leave the exemption entirely; keep the exemption for genuinely metadata-less works only. (d) Whatever survives, run the exempt entries through the Title/Authors/DOI comparison rather than skipping them.
- **Cache:** fresh-fetch (`semanticscholar-doi-lookup`), verdict `match` ×3

### 1.2 — 🟡 REQUIRED — `CITATION_REGISTRY['HashinShtrikman1963'].notes` still carries both halves of the attribution error that round 6 corrected in `prior_art_novelty.md`

- **Gate:** CitationIntegrity / NarrativeGrounding (attribution-claim)
- **Location:** `src/core/citations.py`, `HashinShtrikman1963` entry, `notes` field
- **Observed:** verbatim —
  ```
  'notes': 'CITE THIS ONE for the elastic Voigt/Reuss/HS bounds formalized in 6CE EffectiveModuli.',
  ```
- **Evidence:** Two independent errors in one line. (a) **Attribution:** it groups "Voigt/Reuss" under Hashin–Shtrikman 1963. The Voigt (1889, iso-strain) and Reuss (1929, iso-stress) averages are not Hashin & Shtrikman's; HS 1963's contribution is the strictly tighter elastic pair. This is precisely the sentence round-6 finding 5.4 flagged, and `prior_art_novelty.md:166-180` now says so explicitly ("**Voigt (arithmetic, iso-strain) average** → Voigt 1889; **Reuss (harmonic, iso-stress) average** → Reuss 1929. They are not Hashin & Shtrikman's"). (b) **Scope:** "**formalized** in 6CE EffectiveModuli" asserts the thing the whole round-6 3.1 remediation retracts. `EffectiveModuli.lean:17-21` now says "⚠️ **This is NOT the Voigt–Reuss–Hill theorem** … No effective modulus of a composite appears anywhere", and `paper_draft.tex:309-313` says "The classical Voigt–Reuss bracket … is a claim about a physical `M_eff` and is *not* formalized here … The Hashin–Shtrikman elastic bounds refine that physical bracket, not this ordering."
  Four layers carry this bundle's citation metadata (`.tex`, `CITATION_REGISTRY`, the `.abstract.txt` cache, `prior_art_novelty.md`). Three were fixed this round. The registry was not — the same one-layer-at-a-time pattern the round-6 QI candidate named, on its fourth consecutive round.
- **Expected:** The registry note a future lift will read must not contradict the Lean header, the paper and the novelty record simultaneously.
- **Fix:** Replace with: "Cite for the ELASTIC Hashin–Shtrikman variational bounds (the 1962 companion is the electromagnetic one). NOT formalized in this development — 6CE `EffectiveModuli` proves only the ordering of the Voigt (Voigt1889) and Reuss (Reuss1929) averages plus constituent bounds; see the module header." Then `git grep` the four layers before closing.
- **Cache:** cache-verified (bibitem hash `a2b7881ce2a7`, verdict `match`, 2026-07-31T05:28; corroborated this round by S2 DOI lookup for the 1962 sibling)

### 1.3 — 🔵 RECOMMENDED — `LiPaulson2018` and `LjungstromMortberg2024` still cite preprints whose published records the arXiv pages carry (round-6 1.5, unactioned)

- **Gate:** CitationIntegrity
- **Location:** `papers/D11/paper_draft.tex:618-619`, `:614-616`; `src/core/citations.py` both entries
- **Observed:** `LiPaulson2018` is a bare 2018 preprint with no venue and no DOI; `LjungstromMortberg2024` gives volume 35 but no article number and no DOI. Registry mirrors both (`journal: None, doi: None` and `page: None, doi: None`).
- **Evidence:** Independent fresh fetches this round (I did not rely on round 6): `https://arxiv.org/abs/1804.03922` → *Journal of Automated Reasoning (2019)*, DOI `10.1007/s10817-019-09521-3`; `https://arxiv.org/abs/2401.16336` → *Math. Struct. Comp. Sci. 35 (2025) e16*, DOI `10.1017/S0960129525000131`. Titles and author lists match the bibitems exactly — these are **incomplete, not wrong**. Both bibitem hashes are unchanged since the 2026-07-31T05:28 records, confirming no edit was attempted.
- **Expected:** Cite the published records; these are two of the four mandatory novelty carve-outs and a referee will check them.
- **Fix:** Two bibitem edits plus the matching `journal`/`page`/`doi` registry fields.
- **Cache:** fresh-fetch, verdict `match` ×2 (records appended)

### 1.4 — 🔵 RECOMMENDED — `citation_cache.is_stale()` raises on 6 of 83 records, and `citation_cache.stats()` is dead

- **Gate:** CitationIntegrity (tooling)
- **Location:** `scripts/citation_cache.py:97-104` (`is_stale`), `:120-138` (`stats`)
- **Observed:** `is_stale` catches only `KeyError` and `ValueError`. Six records carry a **date-only** `verified_date` (`"2026-05-31"`), which `datetime.fromisoformat` parses to a *naive* datetime; `now - verified` then raises `TypeError: can't subtract offset-naive and offset-aware datetimes`, which propagates.
- **Evidence:**
  ```
  records: 83  is_stale OK: 77  is_stale RAISES: 6
     ('AaronsonGottesman2004', '2026-05-31', 'TypeError')
     ('Mukhopadhyay2024', '2026-05-31', 'TypeError')
     ('Brylinski2002', '2026-05-31', 'TypeError')   … and 3 more
  ```
  `stats()` crashes outright on the first of them. None of the six is a D11 bibkey, so D11's own verification is unaffected — but this is the tool the Stage-13 protocol tells the reviewer to consult ("if the cache has a recent (`is_stale == False`) `match` entry → skip re-fetch"), and for those six the branch throws rather than failing safe toward re-fetch.
- **Fix:** Add `TypeError` to the except clause and default to `True` (fail safe = re-fetch), or normalize naive dates to UTC on read. Two lines.
- **Cache:** n/a

### 1.5 — 🔵 RECOMMENDED — `READINESS_GATES.md`'s stated Gate-1 criterion describes a check nothing performs (round-6 1.6, unchanged)

- **Gate:** CitationIntegrity
- **Location:** `docs/READINESS_GATES.md:34`; `src/core/citations.py` (schema); `scripts/readiness_gates.py` `_eval_citation_integrity`
- **Observed:** The canonical gate definition says Gate 1 "Passes iff … Every registry entry has `arxiv_verified == True` AND `doi_verified == True` (where applicable)". **8 of 652** registry entries carry an `arxiv_verified` key at all (was 8 of 649 at round 6 — the three new D11 entries did not add one). None of D11's five arXiv-bearing entries has it; `doi_verified` is `None` for `LjungstromMortberg2024`, `LiPaulson2018` and `BoldoLaxMilgram2016`.
- **Evidence:** Dict scan of `CITATION_REGISTRY`: `entries: 652, with arxiv_verified field: 8`. The implemented evaluator checks bibkey→registry coverage and defers the rest to Stage 13 by comment.
- **Expected:** Either the doc's criterion or the schema. As written, the arXiv/DOI half of the project's most-load-bearing gate rests entirely on the adversarial reviewer re-fetching by hand each round — which is what I did again this round, for eight bibkeys.
- **Fix:** Populate `arxiv_verified` from `docs/citation_verifications.jsonl` (every `verdict: match` record with an `arxiv_id` is exactly this evidence, and there are now 91 of them), or amend `READINESS_GATES.md:34` to state plainly that arXiv/DOI correspondence is a Stage-13 obligation with no programmatic gate.
- **Cache:** n/a

**Class-1 clean results.** All 14 bibkeys are present in `CITATION_REGISTRY`; zero uncited bibitems; zero `\cite` without a bibitem; `citation_primary_sources_present` PASS (529 bibkeys / 365 cached / 11 inprep / 150 textbook / 3 unreachable-exempt, all three **named** in the summary line / 0 missing). The three new bibitems are metadata-correct: `Voigt1889` (Ann. Phys. **274**, 573, 1889 — S2 returns the exact title and author), `Reuss1929` (ZAMM **9**, 49, 1929 — likewise), `Milton2002` (S2 returns "The Theory of Composites: Frontmatter", G. Milton, 2002; the suffix is a CUP book-DOI indexing artefact). Twelve bibitems were cache-verified with matching hashes; the two whose hashes had changed (`BoldoLaxMilgram2016`, `LeanLJ2025`) were fresh-fetched and both verdict `match` — Clément & Martin 2016 with the correct pen-and-paper/CPP-2017 parenthetical, and Ugwuanyi/Jones/Velkey/Josephson with the correct title, four authors and DOI. Eight new records appended to `docs/citation_verifications.jsonl` (83 → 91).

### 2.1 — (no findings) — ParameterProvenance

- **Gate:** ParameterProvenance
- **Observed:** D11 quotes no experimental or measured parameter. Every number is a dimensionless model parameter fixed by declaration: `t = t₂ = 1`, `φ = π/2`, `m ∈ {1, 5, 6}`, `(m₁,m₂,κ,a) = (1,2,1,1)`, `(ε_h,ε_i,f) = (1,4,½)`, `(M₁,M₂) = (1,4)`, `N₁ = N₂ = 4`, `g² ∈ [99/100, 1]`. `fermiVelocity` is left parametrized (`dispersion_slope_eq_hbar_fermiVelocity`), so no CODATA value is asserted; the carbon–carbon distance enters only symbolically via `chart_len_sq_of_neighbours`. `validate.py --check numerical_literals` → "D11 — no inline unit-bearing literals in body".
- **Verdict:** gate not applicable to this bundle. No finding.

### 3.1 — 🔵 RECOMMENDED — `effectiveMedium_hashinShtrikman_enclosure`'s bundle redundancy is not gone; the strictness rewrite answers a question nobody asked

- **Gate:** LeanProofSubstance
- **Location:** `lean/SKEFTHawking/EffectiveMediumBounds.lean:88-104`; cited at `papers/D11/paper_draft.tex:274`, `:291-293`
- **Observed:** The statement changed from `= 2 ∧ 1 ≤ … ∧ … ≤ 4` to `= 2 ∧ 1 < … ∧ … < 4`. The **proof term is unchanged in shape**:
  ```lean
  have hval : maxwellGarnett 1 4 (1 / 2) = 2 := by unfold maxwellGarnett; norm_num
  exact ⟨hval, by rw [hval]; norm_num, by rw [hval]; norm_num⟩
  ```
- **Evidence:** The round-6 finding was P2 bundle redundancy *with respect to conjunct 1*: conjuncts 2 and 3 are `norm_num` restatements of `= 2`. That is still exactly true — `rw [hval]; norm_num` is the proof, and dropping conjuncts 2 and 3 loses nothing, which is the project's own P2 test verbatim ("If I drop any conjunct, does the theorem still mean the same thing?"). The new docstring argues something different and *correct* — that strict interiority is not an instance of `effectiveMedium_constituent_bounds`, which delivers only the non-strict pair — but that was never the redundancy claimed. A conjunct that is not entailed by a *sibling theorem* can still be entailed by the *first conjunct of its own statement*, and here it is.
- **Expected:** Either the conjuncts carry independent content or the docstring says plainly that they are extraction conveniences over conjunct 1.
- **Fix:** State the substantive form `maxwellGarnett 1 4 (1/2) = 2` and, if downstream wants the bracket shape, ship a one-line extraction lemma; or make the enclosure genuinely independent by bracketing an *irrational-valued* parameter point where no exact value is available (that is what `band_gap_rational_enclosure` does correctly, and it is the model to copy). Amend the docstring paragraph to acknowledge the conjunct-1 entailment either way.
- **Cache:** n/a

### 3.2 — 🔵 RECOMMENDED — `voigtModulus` / `reussModulus` docstrings still call each "the … effective modulus", one screen above a header that says in bold that no effective modulus appears

- **Gate:** LeanProofSubstance / NarrativeGrounding
- **Location:** `lean/SKEFTHawking/EffectiveModuli.lean:43`, `:46`
- **Observed:**
  ```lean
  /-- The Voigt (arithmetic, iso-strain) effective modulus `(1−f)M₁ + f M₂`. -/
  /-- The Reuss (harmonic, iso-stress) effective modulus `M₁M₂ / ((1−f)M₂ + f M₁)`. -/
  ```
- **Evidence:** `:18-20` of the same file now reads "No effective modulus of a composite appears anywhere in `effectiveModuli_enclosure`'s statement — only the two closed-form **averages** and the constituents", and `paper_draft.tex:307-309` reads "This is an ordering of the two *averages* together with constituent bounds; no effective modulus of a composite appears in the statement." The two definitions the whole distinction is about are still named "effective modulus" on hover. The round-6 3.1 fix reached the module title and the theorem docstring; it did not reach the two `def` docstrings, which are what a consumer hovers first.
- **Expected:** The two names the paper is careful to call averages should not call themselves effective moduli in the artifact.
- **Fix:** "The Voigt (arithmetic, iso-strain) **average** of the constituent moduli" / "The Reuss (harmonic, iso-stress) **average** …", with a one-clause pointer to the header.
- **Cache:** n/a

**Class-3 clean results.** I extracted all 151 `\thm{}`/`\texttt{}` tokens from the draft body: **122 resolve to real declarations inside the 22 D11 modules** and the other 29 are Mathlib names (`Matrix.det_fin_three`, `toIocMod`, `toIocDiv`, `Chernoff`), Lean keywords, module names, revision hashes, or the two Python function names. **Zero phantom names.** Intersection of the cited set with the 576-node `PlaceholderMarker` set is **empty**. Every one of the 122 has `axiom_deps_core` **exactly** `{propext, Classical.choice, Quot.sound}` and `axiom_deps_project` **empty** — so the abstract's "axiom profile exactly {…}" holds for every cited name, not just headlines. Source scan of all 22 modules: 0 `sorry`, 0 `native_decide`, 0 `maxHeartbeats`, 0 `axiom` (the two `native_decide` string hits are docstring disclaimers). `lean_deps.json` over the 22 modules: 460 theorem / 109 def / 5 structure, zero project-axiom dependencies. All 22 confirmed root-imported from `lean/SKEFTHawking.lean`.
The **round-6 3.1 BLOCKER is genuinely closed**: `EffectiveModuli.lean:4` is now "Elastic effective moduli (constituent bounds on the Voigt/Reuss averages)", `:86-99` is a full rewrite carrying the ⚠️ scope warning, and `EffectiveMediumBounds.lean:32-36` no longer says the elastic sibling is "correctly named there as the Voigt–Reuss bracket" — it now says it "likewise does NOT prove the classical Voigt–Reuss bracket on a physical effective modulus", which is true.
The **four new evenness lemmas are real, substantive and kernel-pure**: `DiatomicChain.{normSqOff_neg, disc_neg, branchPlus_neg, branchMinus_neg}` (`AcousticBlochOperator.lean:99-115`, proved via `Real.cos_neg`) and `diatomicCrystal.{branchMinus_at_neg_pi, branchPlus_at_neg_pi}` (`PhononicBandGap.lean:102-110`, `rw [branchMinus_neg]; exact branchMinus_at_pi`). Fig. 1's caption claims exactly what they say and nothing more, and the PNG legend's `ω²∓(±π)` markers are now fully theorem-backed. **`sum_rawCurl_eq_zero` is now genuinely load-bearing on `sum_forwardDiff_eq_zero`** — `FHSLatticeGauge.lean:97-104` rewrites the raw curl into two forward differences and discharges each with `sum_forwardDiff_eq_zero`, so §2's "single load-bearing property" citation is backed by a call, not a docstring reference (round-6 5.9 closed correctly). Structural-tautology sweep of `exceptional_point_defective` (4-way conjunction over four independently proved lemmas), `band_gap_falsifier` (routes through `acousticBloch_eigenvalue_iff` — genuinely refutational), `pt_symmetric_real_spectrum_iff` (both directions carry real proof; the reverse constructs `√(1−g²)`), `bernal_spectrum_not_determine_model` (4-way, each conjunct with content), `voigt_sub_reuss_eq` (`field_simp; ring` on a real identity), `haldaneFrame_latticeChern_eq_neg_one` (kernel `decide` on an ℤ sum after rewriting all 16 plaquette branches — legitimate, and distinct from `native_decide` as the paper insists) — **no hypothesis-echo anywhere**. `blochPauli_gap_pos_iff : 0 < √(dNormSq d) ↔ d ≠ 0` is a genuine biconditional, so §2's claim is accurate.

### 4.1 — 🟡 REQUIRED — `docs/PAPER_DRAFT_MAPPING.md:109` reports 6144 / 394 / 83 against the paper's generated 6205 / 399 / 82 — three of four figures now wrong

- **Gate:** CrossPaperConsistency / NumericalFreshness
- **Location:** `docs/PAPER_DRAFT_MAPPING.md:109`; against `docs/counts.tex:23-26` and `papers/D11/paper_draft.tex:107-108`
- **Observed:** The mapping row reads "22 root-imported modules (**6144** lines, **394** theorems/lemmas, **83** defs/structures at source level)". `docs/counts.tex` emits `\dxiLines{6205}`, `\dxiTheorems{399}`, `\dxiDefs{82}`.
- **Evidence:** My independent recount over the 22 modules, applying the generator's rule (line-prefix, outside `/- … -/` and `--`), reproduces **22 / 6205 / 399 / 82 exactly**, and the `lean_deps.json` figure **460**. So the paper's macros are right and the mapping row is stale on lines, theorems *and* defs. Round-6 finding 7.1's fix text named this file explicitly ("plus the matching update to `docs/PAPER_DRAFT_MAPPING.md:109`, which repeats '83 defs/structures' with no rule at all"); the paper side was fixed by migrating to macros and the mapping side was not touched. The macro migration is the right remedy and it makes this row the *only* remaining hand-typed copy of the same five numbers — i.e. the exact failure mode the migration was performed to end, now concentrated in one place.
- **Expected:** `PAPER_DRAFT_MAPPING.md` is the append-only source of truth for bundle assignment. It must not disagree with the bundle's own generated figures.
- **Fix:** Update the row to 6205 / 399 / 82, and state the counting rule (or point at `scripts/update_counts.py`'s `_D11_MODULES` block) so the next drift is detectable. Better: have `update_counts.py` emit the row, since it already computes every number in it.
- **Cache:** n/a
- **Lane:** `prose`

### 4.2 — 🟡 REQUIRED — Gate 11 is blocked on bookkeeping: 20 of 23 round-6 findings are still `status: open` in the graph while `bundle_metadata.json` asserts `advisories_open: 0`

- **Gate:** FixPropagation
- **Location:** `docs/review_finding_supersessions.json` (3 D11 entries for the `2026-07-31-1059` review); `papers/D11/bundle_metadata.json`; graph `FLAGS` edges into `paper:D11`
- **Observed:** Graph query on `paper:D11` returns 23 incoming `FLAGS` edges from the round-6 review. Three carry `status: fixed` (3.1, 5.1, 5.2 — the BLOCKERs). **Twenty carry `status: open`**, including every REQUIRED and RECOMMENDED item. Simultaneously `bundle_metadata.json` records `"blockers_open": 0, "advisories_open": 0`.
- **Evidence:** `docs/review_finding_supersessions.json` contains exactly 3 entries matching `2026-07-31-1059`, all three the BLOCKERs. But I independently verified in the tree this round that at least **eight further round-6 findings are genuinely remediated**: 4.1 (`Phase6CE_Roadmap.md:5` now carries both dated retraction notes), 5.4 (`prior_art_novelty.md:166-180` fully rewritten with the correct Maxwell Garnett 1904 / HS 1962 EM / HS 1963 elastic / Voigt 1889 / Reuss 1929 attributions), 5.5 (Fig. 4 caption now says "*averages* --- not bounds on a physical `M_eff`"), 5.6 (six new lemmas), 5.7 (`haldane_lattice_chern`/`haldane_flip_location` + 5 tests + `visualizations.py:15250` now calls the function instead of hardcoding), 5.8 (history clause corrected — I confirmed against `git show 230b3528:lean/SKEFTHawking/EffectiveModuli.lean:18`, which asserted the exact identity while only `reuss_le_voigt` existed), 5.9 (routing), 7.2 (macros; `count_literals` now reports "D11 — no count literals found (macros in use)"). So the ledger **understates** the work, and Gate 11's stated pass criterion ("Every FLAGS incoming to this paper has `status != 'open'`") fails purely on unrecorded closures. `advisories_open: 0` is the third number in the same disagreement.
- **Expected:** §11 step 4 of `BUNDLE_LIFT_PROCEDURE.md` requires an append per closed finding *with deterministic-recheck evidence*, precisely so a fresh-context reviewer need not re-derive the closure. I had to re-derive eight of them.
- **Fix:** Append supersession entries with `file:line` + replacement text for each of the eight verified-fixed round-6 findings, mark the residuals (1.5 → this round's 1.3; 3.2 → 3.1; 5.3, 7.1) explicitly `open` with a pointer to this review, and reconcile `bundle_metadata.json`'s `advisories_open` against the graph rather than setting it by hand.
- **Cache:** n/a
- **Lane:** `infra`

### 4.3 — 🔵 RECOMMENDED — two project docs still call `haldane_massInversion_not_sufficient_at_N4` "the necessity theorem"

- **Gate:** CrossPaperConsistency
- **Location:** `docs/dev-loops/Phase6ED/LAB_NOTEBOOK_INDEX.md:32`; `docs/roadmaps/Phase6ED_Roadmap.md:174`
- **Observed:** Both read "… and **the necessity theorem** `haldane_massInversion_not_sufficient_at_N4`".
- **Evidence:** The theorem is `haldaneD … 5 diracK 2 * haldaneD … 5 diracK' 2 < 0 ∧ blochLatticeChern haldaneFrame5.toAdmissibleBandFrame = 0` — mass inversion holds *and* the invariant is zero. That is the **insufficiency** direction; necessity is exactly what the module header (`HaldaneWitness.lean:31-34`) says is "**NOT proved here or anywhere in this development**". Round-6 finding 5.2's fix list named `LAB_NOTEBOOK_INDEX.md:32` by line; it is unchanged, and the roadmap carries the same misnaming. The notebook half of that finding *is* fixed (verified below), so this is a partial propagation, not a missed finding.
- **Expected:** The two documents that seed the next lift should not name the theorem after the direction it disclaims.
- **Fix:** "the insufficiency theorem" in both. Then `git grep -i "necessity theorem"` must be empty.
- **Cache:** n/a

**Class-4 clean results.** `Berry1984` is the only bibkey D11 shares with another draft (`paper18_doublon_gate`); both give Proc. R. Soc. A **392**, 45 (1984) — consistent, and the registry entry is the merged one with both consumers. `validate.py --check bundle_consistency` PASS. D7 (`:35, :124, :163, :221, :226`) and E2 (`:418-423`) describe `ChernBridge.lean` as a "categorical Chern" / "real-space Chern marker", never as a Bloch-band invariant, so D11's carve-out at `:511-514` is accurate and no conflation exists in either direction. No other bundle mentions Haldane, Hashin–Shtrikman, Maxwell–Garnett, Voigt, exceptional points, honeycomb or lattice Chern (the D6 hits are "hashing bound"). D12's novelty framing uses the same hedged "we are aware of no" construction, so the two sibling bundles are consistent on the discipline. `Phase6CE_Roadmap.md:5, :12, :39` now carry dated retractions for both the Hashin–Shtrikman and the Voigt–Reuss–Hill misnomers (round-6 4.1 closed).

### 5.1 — 🔴 BLOCKER — all four stored figure outputs in the executed companion notebook predate the remediation arc, and one of them renders the two namings §5 retracts

- **Gate:** NarrativeGrounding / CrossPaperConsistency
- **Location:** `notebooks/D11_TopologicalBandTheory_Technical.ipynb` cells 6, 9, 12, 15 (stored outputs); mechanism at `scripts/validate.py` `notebook_exec`
- **Observed:** I re-rendered each `# viz-ref:`-tagged figure from the current `src/core/visualizations.py` and diffed its text strings against the notebook's stored output. **All four disagree.** The damaging ones:
  - **cell 15** (`fig_d11_effective_medium`) — stored output contains subplot title `Voigt–Reuss elastic bounds  (M₁ = 1, M₂ = 4)` and y-axis title `effective modulus  M`. The current function emits `(right) Voigt–Reuss, M₁ = 1, M₂ = 4` and `modulus M  (units of M₁)`.
  - **cell 12** (`fig_d11_haldane_chern`) — stored annotation is `4×4 invariant flips here ≈ 3.3177`. The current function emits `4×4 invariant flips ≈ 3.3177<br>(numerical, not certified)`, and a figure title `Haldane Chern witness — mass inversion is NOT sufficient at fixed N` that the stored output does not have at all.
- **Evidence:**
  - The retraction the stored cell-15 figure violates is the bundle's most-worked-over paragraph. `paper_draft.tex:307-313`: "This is an ordering of the two *averages* together with constituent bounds; **no effective modulus of a composite appears in the statement**. The classical Voigt–Reuss bracket … is *not* formalized here." `EffectiveModuli.lean:17-21` says the same in bold. Fig. 4's caption was rewritten **this round** (round-6 finding 5.5) from "Voigt and Reuss elastic bounds" to "the Voigt and Reuss elastic *averages* --- not bounds on a physical `M_eff`". The notebook renders the retracted wording *and* the retracted object ("effective modulus") in a figure a reader sees.
  - The stored cell-12 figure drops the "(numerical, not certified)" qualifier. `paper_draft.tex:342-344` states, as a claim about the artifact: "The flip location ≈ 3.3177 is computed by `haldane_flip_location` … and **is labelled in the figure as numerical rather than certified**." That is true of `papers/D11/figures/d11_fig3_haldane_chern.png` and **false of the notebook figure**. In a bundle whose entire thesis is the certified/numerical boundary, shipping an uncertified number with no qualifier is the specific failure the paper spends a paragraph forestalling.
  - **Why the gate cannot see it:** `validate.py --check notebook_exec` reports `✓ D11_TopologicalBandTheory_Technical.ipynb — SKIPPED — unchanged, previously vetted`. The check keys on the *notebook's* own change state, not on its dependencies, so an edit to `visualizations.py` silently invalidates every stored figure without invalidating the skip cache. This is why the notebook's cell-11 print *is* correctly updated (that cell's source changed, so it was touched) while none of the four figures is.
  - **This is a shipped artifact, not scratch.** `SK_EFT_Hawking_Inventory.md:929-932`: D11 ships "a `prior_art_novelty.md`, figures under `papers/D1x/figures/`, and an **executed companion notebook** (`notebooks/D11_TopologicalBandTheory_Technical.ipynb`)". Round-6 finding 5.2 was escalated to BLOCKER on exactly this reasoning, for exactly this file; the reasoning applies unchanged one cell over.
- **Expected:** Zero surviving instances of a retracted naming in any artifact the bundle ships. The notebook is the bundle's second-most-read surface after the PDF.
- **Fix:** (1) Re-execute the notebook end-to-end against the current `visualizations.py` and commit the regenerated outputs; verify by re-running the render-vs-stored text diff until all four cells report identical string sets. (2) Make `notebook_exec` invalidate its skip cache on the mtime/hash of `src/core/visualizations.py` and `src/core/formulas.py`, not only the notebook — otherwise this recurs on the next figure edit. (3) Re-invoke; per Stage 13 the re-run is the evidence.
- **Cache:** n/a

### 5.2 — 🟡 REQUIRED — the novelty enumeration says "five of these eight" and then lists six

- **Gate:** NarrativeGrounding (unification / first-claim)
- **Location:** `papers/D11/paper_draft.tex:499-511`
- **Observed:** The absence claim enumerates **eight** items: [1] a Chern number or Chern class *of a vector bundle*; [2] an acoustic or phononic band structure; [3] an exceptional point; [4] a `PT` transition; [5] a Maxwell–Garnett constituent enclosure; [6] a Wiener or Hashin–Shtrikman-type effective-medium bound; [7] a honeycomb band structure; [8] the Haldane model. The parenthetical then says: "the present paper formalizes **five of these eight** --- the acoustic band structure, the exceptional point, the `PT` transition, the Maxwell--Garnett enclosure, the honeycomb band structure and the Haldane model, of which the last two are one item apiece --- … **Two of the eight** are *not* delivered here …: the vector-bundle Chern class … and the Wiener/Hashin--Shtrikman families".
- **Evidence:** Count the list that follows "five": acoustic [2], exceptional point [3], `PT` [4], Maxwell–Garnett [5], honeycomb [7], Haldane [8] — **six**. The clause "of which the last two are one item apiece" says honeycomb and Haldane each count *separately*, so it does not merge them; it argues for six. And 6 delivered + 2 not delivered = 8, which closes exactly. The correct number is **six of these eight**. I verified each of the six is genuinely formalized (`AcousticBlochOperator`/`PhononicBandGap`; `ExceptionalPoint`; `pt_symmetric_real_spectrum_iff`; `effectiveMedium_constituent_bounds`; `GrapheneBand/Honeycomb`; `GrapheneBand/HaldaneWitness`) and that both non-deliveries are correctly identified.
  Round-6 finding 5.3 flagged "six of these seven" as unsupportable because clause [5] then bundled Maxwell–Garnett with Wiener/HS. The remediation split the clause — which is the right fix and makes the two-non-delivered accounting correct for the first time — but restated the count as "five" instead of "six". The paragraph is now self-contradictory in a different place.
- **Expected:** The enumeration and its arithmetic must survive being counted by a referee, which takes ten seconds.
- **Fix:** "the present paper formalizes **six of these eight**". Drop the "of which the last two are one item apiece" clause — with the count corrected it explains nothing. Mirror into `prior_art_novelty.md` N6, which still reads "No Maxwell–Garnett, Hashin–Shtrikman, or periodic-homogenization formalization in any prover" as one bundled claim and should be split the same way the paper now splits it.
- **Cache:** n/a

### 5.3 — 🟡 REQUIRED — the disclosed AFP coverage limit is scoped to two topics; the bundle's own novelty record scopes it to a third that the paper's absence claim depends on

- **Gate:** NarrativeGrounding (first-claim) / FirstClaimVerification
- **Location:** `papers/D11/paper_draft.tex:536-540`, against `papers/D11/prior_art_novelty.md:190-197`
- **Observed:** The paper: "The Isabelle Archive of Formal Proofs was not directly searchable during our survey, so our negative results **for index theory and for defective-eigenvalue infrastructure** should be read as probable rather than established."
- **Evidence:** `prior_art_novelty.md` §5.1 scopes the same gap differently and more widely: "Claims **N5** and the bulk–boundary/index-theorem negative should be read as **medium-high, not high**, until a direct AFP search is run". N5 is (`:35`) "**No exceptional points, PT symmetry, or non-Hermitian topology in any prover.**" Items **[3] and [4]** of the paper's own eight-item absence claim are the exceptional point and the `PT` transition — i.e. the backing document explicitly downgrades the confidence of two of the paper's eight headline absence claims on the AFP gap, and the paper's coverage-limit sentence does not say so. "Defective-eigenvalue infrastructure" names the Jordan-normal-form *substrate* D11 avoided, not the EP/PT novelty claims themselves; a referee reading only the paper concludes those two claims are AFP-independent.
  This matters here more than it would elsewhere: AFP is where Isabelle linear-algebra and analysis developments live (the survey's own §5.1 lists `Jordan_Normal_Form` and `Perron_Frobenius` as the unchecked targets), and §5.2 flags `Jordan_Normal_Form` as "believed to exist but could not be tied to a resolvable whitelisted source. **Do not cite it** … this is precisely the class of claim that becomes a review BLOCKER." I could not resolve the AFP through the whitelisted egress either — verdict `unknown`, not refuted — which is exactly why the disclosure has to cover every claim that rests on it.
  I did run independent prior-art searches for the harder claims and found **nothing refuting**: arXiv `"proof assistant" AND "Chern class"` → 0 results; `"formalization" AND "Haldane model"` → 18 results, none a proof-assistant formalization; `"two-scale convergence" AND "formal"` → 5 results, all conventional analysis. So the claims themselves stand; the *hedge* is under-scoped.
- **Expected:** "Two coverage limits bound these claims, and we state them rather than let a referee find them" must enumerate every claim the limit bounds.
- **Fix:** Extend the sentence to: "… so our negative results for index theory, for defective-eigenvalue infrastructure, **and for the exceptional-point / `PT` / non-Hermitian items of the list above** should be read as probable rather than established." One clause; it costs nothing and it is the difference between a disclosed limit and a partially-disclosed one.
- **Cache:** n/a

**Class-5 clean results.** The **round-6 5.1 BLOCKER is closed and correct**: the abstract now reads "against Mathlib" and the acknowledgments read "Formalization was carried out in Lean 4 against Mathlib … PhysLib is discussed in Sections 3 and 6 as adjacent prior art, but **no module in this bundle imports it**: the complete import set is Mathlib plus intra-bundle dependencies." I verified that claim exhaustively — the import multiset across all 22 modules is `Mathlib` ×11, `Mathlib.Analysis.Complex.Exponential` ×1, and 22 `SKEFTHawking.*` intra-bundle imports. Nothing else. The **round-6 5.2 BLOCKER is closed in both layers**: notebook cell 11 now prints, and stores as output, "Mass inversion is NOT sufficient at fixed grid size (necessity is sampled at m=6, not proved)"; a repo-wide sweep finds no surviving live necessity assertion for this result outside the two doc lines at finding 4.3. **Round-6 5.5** closed (Fig. 4 caption). **Round-6 5.8** closed and now *accurate*: `git show 230b3528:lean/SKEFTHawking/EffectiveModuli.lean:18` asserted "The Reuss ≤ Voigt gap is exactly `f(1−f)(M₁−M₂)²/((1−f)M₂+fM₁) ≥ 0`" in the module header while the file contained only `reuss_le_voigt` and no `voigt_sub_reuss_eq`, so the paper's "which was all that was formalized at the time the module header already asserted the exact identity" is the correct history. **Round-6 5.7** closed properly: `haldane_flip_location(4)` returns `3.317672196…`, `visualizations.py:15250` calls it instead of hardcoding, and `tests/test_bundle_formulas_d11_d12.py::TestHaldaneLatticeChern` pins five properties including a `ValueError` on a non-straddling bracket. **The Python port is faithful by construction, not by coincidence** — I checked it declaration-by-declaration against the Lean: `frameOverlap u v = Σ conj(uᵢ)vᵢ` ↔ `np.vdot(a,b)` (same conjugation side); `rawCurl`'s four oriented links ↔ the four `_link_arg` calls with the same `shift` directions (`shiftVec 0 = (1,0)`, `1 = (0,1)`); `lbVec d = ![d₀ − i d₁, −(d₂ + √‖d‖²)]` ↔ `v = [d[0] − 1j*d[1], −(d[2] + sqrt(d@d))]`; `haldaneD = ![t·Re f, −t·Im f, m − 2t₂ sin φ · haldaneNNN]` ↔ the same three components; `principal := toIocMod two_pi_pos (−π)` on `(−π, π]` ↔ `θ − 2π·ceil(θ/2π − ½)`, which I verified reproduces the half-open convention exactly at `θ = 2πm + π`; `latticeChern := −Σ branchIndex(rawCurl)` ↔ `return -total`. It reproduces `C(1) = −1`, `C(5) = 0`, `C(6) = 0`. Novelty carve-outs: all four present and correctly characterised (`:521-534`), both coverage limits stated (`:536-540`), no unhedged "first in any prover" anywhere. Kernel no-gos: both represented at `:446-465` with the theorem names that back them. The Chern sign (`−1`), the parameter point (`t = t₂ = 1`, `φ = π/2`, `m = 1`, `4×4`), the retracted classification, the `3 ∣ N` no-go and its *correct* Dirac-point-sampling mechanism, the `decide`/`native_decide` distinction, and the `ChernBridge` non-conflation all check out against the D11 anchor list. LaTeX compiles clean (two `pdflatex` passes, exit 0, zero undefined control sequences, zero Overfull hboxes, zero citation/reference warnings), and the `\input{../../docs/counts.tex}` macro chain resolves.

### 6.1 — 🟡 REQUIRED — `haldane_lattice_chern` has no admissibility guard, and returns an integer on precisely the grids the paper declares inadmissible

- **Gate:** AssumptionDisclosure
- **Location:** `src/core/formulas.py:11866-11927` (`haldane_lattice_chern`), `:11930-11968` (`haldane_flip_location`); cited by name at `papers/D11/paper_draft.tex:414-415` and in Fig. 3's caption `:342-344`
- **Observed:** Both functions take `n_grid` as their principal free parameter. Neither checks the north-pole condition `0 < ‖d‖ + d₃`, neither checks the nonzero-overlap condition, and neither mentions `3 ∤ N`. `haldane_lattice_chern`'s only validation is `if n_grid < 2: raise`.
- **Evidence:** I instrumented the port to report the two admissibility quantities the Lean side discharges:
  ```
  N=4  min(|d|+d₃)=1.623e-01   min|overlap|=4.057e-01   ok
  N=5  min(|d|+d₃)=5.818e-02   min|overlap|=4.891e-01   ok
  N=6  min(|d|+d₃)=0.000e+00   min|overlap|=4.887e-01   INADMISSIBLE
  N=8  min(|d|+d₃)=2.234e-02   min|overlap|=5.566e-01   ok
  N=9  min(|d|+d₃)=0.000e+00   min|overlap|=5.857e-01   INADMISSIBLE
  ```
  At `N = 6` and `N = 9` the north-pole condition fails **exactly** (`‖d‖ + d₃ = 0`) — and the function returns anyway: `haldane_lattice_chern(1.0, 3) == 0`, `(1.0, 6) == -1`, `(1.0, 9) == -1`, with no exception, no warning, and no docstring caveat. `haldane_flip_location(n_grid=6)` runs to completion and returns a bisection on that.
  On the Lean side the construction **cannot be formed**: `blochFrameOfD` (`BlochFrameOfD.lean:195-198`) takes `hpos : ∀ k, 0 < √(dNormSq (D k)) + (D k) 2` and `hov` as hypotheses, and `haldane1_pos` / `haldane1_link_ne` discharge them at all sixteen vertices and thirty-two links for `N = 4` only. `HaldaneWitness.lean:102-108` states the mechanism precisely: "whenever `3 ∣ N` the grid samples the Dirac points … the `lbVec` gauge is singular (`lbVec ![0,0,−c] = ![0,0]` for `c > 0`) … the frame is not admissible at all. This kills `N = 3`, `6`, `12`, `24`, …". The paper elevates it to a headline caveat (`:495-497`): "grids whose size is divisible by three are inadmissible … **This is a real constraint on which grids may be used, not a cosmetic one.**"
  So the bundle's canonical Python implementation (`formulas.py` is canonical per Pipeline Invariant #1), cited by name in the paper and newly added this round, silently violates a precondition the paper declares load-bearing. The paper's own quoted values are all at `N = 4` and are **correct** — this is not a wrong number, it is an undisclosed and unenforced hypothesis in the artifact a reader is directed to.
- **Expected:** A hypothesis the paper calls "a real constraint, not a cosmetic one" should be enforced, or at minimum stated, in the code that implements the same construction.
- **Fix:** Raise on `n_grid % 3 == 0` with a message naming the Dirac-point-sampling cause; additionally assert `min_k(√(dNormSq d) + d₃) > tol` and `min|overlap| > tol` and raise otherwise, mirroring `haldane*_pos` / `haldane*_link_ne`. Add the precondition to both docstrings. Add a test asserting `pytest.raises` at `n_grid ∈ {3, 6, 9}` — the class `TestHaldaneLatticeChern` already has the right shape for it (`test_bisection_rejects_a_bracket_with_no_flip`).
- **Cache:** n/a

### 6.2 — 🔵 RECOMMENDED — §5's elastic paragraph states no hypotheses, where the parallel electromagnetic paragraph spells them out

- **Gate:** AssumptionDisclosure
- **Location:** `papers/D11/paper_draft.tex:305-319` (including displayed equation (2)), against `:286-291`
- **Observed:** The electromagnetic half is explicit: "for a physical composite `0 < ε_h ≤ ε_i` at fill fraction `0 ≤ f ≤ 1` --- and the ordering hypothesis is load-bearing rather than a relabeling". The elastic half names no hypothesis at all: "we ship `voigtModulus` and `reussModulus` with the ordering chain `effectiveModuli_enclosure`, `M₁ ≤ M_R ≤ M_V ≤ M₂`", and equation (2) is displayed unconditionally.
- **Evidence:** `effectiveModuli_enclosure` takes `(h1 : 0 < M1) (h2 : 0 < M2) (ho : M1 ≤ M2) (hf0 : 0 ≤ f) (hf1 : f ≤ 1)`; `voigt_sub_reuss_eq` takes the same five. `ho` is load-bearing for the same reason the paper gives for `ε_h ≤ ε_i` (the chain reverses without it); `h1`/`h2`/`hf0`/`hf1` are what make the Reuss denominator nonzero, and under Lean's `x/0 = 0` convention equation (2) is false where it vanishes. The chain `M₁ ≤ … ≤ M₂` telegraphs `M₁ ≤ M₂`, but positivity and `0 ≤ f ≤ 1` are invisible.
- **Fix:** One clause: "for `0 < M₁ ≤ M₂` at `0 ≤ f ≤ 1`", attached to the `effectiveModuli_enclosure` sentence so it also governs equation (2).
- **Cache:** n/a

**Class-6 clean results.** Every structural hypothesis the D11 anchor list names is disclosed, and the three a referee would probe are disclosed emphatically: `AdmissibleBandFrame.overlap_ne` (`BlochFrame.lean:86-87`) at `:160-164`, with the explicit statement that it is *not* a consequence of a spectral gap; the north-pole condition `0 < √(dNormSq (D k)) + (D k) 2` (`BlochFrameOfD.lean:196`) at `:185-188`, correctly described as a genuine coordinate degeneracy on the negative-`d₃` axis; `IsHoneycombChart` chart-specificity at `:357-361` and `:371-374`; the `3 ∣ N` grid no-go at `:494-497` with the correct mechanism; `blochLatticeChern_eq_zero_of_narrow_D`'s `|arg| < π/4` at `:170-172`, and it is genuinely the route by which both `haldane_trivial_phase_chern_zero` and `haldane_massInversion_not_sufficient_at_N4` are discharged; `coneWinding` certified non-marginal but not alias-free at `:424-426`. Zero tracked-hypothesis Props exist in the bundle, so the abstract's "no tracked-hypothesis propositions" is exact.

### 7.1 — 🟡 REQUIRED — the counting rule is still not reproducible as stated, and this round's own remediation text added two new counterexamples

- **Gate:** NumericalFreshness
- **Location:** `papers/D11/paper_draft.tex:107-120`; generator at `scripts/update_counts.py:383-434`
- **Observed:** The paper states: "**The counting rule is stated so it is reproducible, on both sides:** a declaration is counted when a line begins with `theorem` or `lemma` (for the first figure), or with `def`, `noncomputable def`, `abbrev` or `structure` (for the second), optionally preceded by an attribute bracket or by `private`/`protected`."
- **Evidence:** I implemented that rule verbatim over the 22 modules and got **401 theorems and 84 definitions**, not the published 399 and 82. The generator applies an **additional, unstated** filter — `_decl_lines()` (`update_counts.py:388-421`) yields only lines *outside* `/- … -/` block comments (which includes every `/-- … -/` docstring) and outside `--` line comments. The four lines the paper's stated rule counts and the generator does not:
  | file:line | line text (truncated) |
  |---|---|
  | `AcousticBlochOperator.lean:93` | `structure on \`[-π/a, 0]\` is the mirror of the one on \`[0, π/a]\`. Added` |
  | `EffectiveMediumBounds.lean:92` | `theorem gives only the non-strict pair for every admissible parameter)` |
  | `GrapheneBand/BernalBilayer.lean:203` | `theorem below would be a statement about a polynomial that merely *res` |
  | `GrapheneBand/DiracExpansion.lean:289` | `structure factor **vanishes** (it is a Dirac point, Wave 1) while the ` |
  The first two are **new this round** — they are docstring prose written by the round-6/7 remediations themselves (the evenness-lemma section note and the `effectiveMedium_hashinShtrikman_enclosure` strictness justification). `update_counts.py`'s own docstring names only the other two and says "The instance of this bug was fixed once, by reflowing one docstring … That closed the instance and not the class — this closes the class." It closed the class *in the generator*. It did not close it in the **stated rule**, which is the artifact the paper offers a reader, and the discrepancy has grown from 2 to 4 in one round.
  Everything else reproduces exactly: 22 root-imported modules ✓, 6205 lines (`wc -l`) ✓, 399 theorems and 82 defs under the generator's real rule ✓, 460 `lean_deps.json` theorems ✓, and the two `abbrev`s are indeed `Torus` (`FiniteTorus.lean:19`) and `LinkField` (`FHSLatticeGauge.lean:44`) as the paper says.
- **Expected:** A sentence whose entire purpose is "so it is reproducible" must reproduce. This is the fourth consecutive round on the same paragraph.
- **Fix:** Add the missing clause: "…, and only on lines outside Lean block comments `/- … -/` (docstring prose occasionally wraps onto a line beginning with `theorem` or `structure`; four such lines exist)." That is the whole delta, it is one sentence, and it makes the figures reproducible by anyone with `grep` and the module list.
- **Cache:** n/a

**Class-7 clean results.** `counts_fresh` PASS (`staleness: fresh`). `count_literals` → "D11 — no count literals found (macros in use)" — round-6 7.2 is closed on the paper side; the five figures are now `\dxiModules`/`\dxiLines`/`\dxiTheorems`/`\dxiDefs`/`\dxiDepsTheorems` from `docs/counts.tex`, so they cannot drift again. `numerical_literals` → "D11 — no inline unit-bearing literals in body". `tables_fresh` PASS. `paper_provenance` → "D11/figures — 4 referenced figure(s) resolved". D11 still has **zero `REPORTS` edges** in the graph, so Gate 9's drift half remains vacuous for this bundle — but with generated macros the values are derived rather than typed, which is the stronger remedy, so I am not raising it again as a finding.

### 8.1 — (no findings) — ProductionRunHealth

- **Gate:** ProductionRunHealth
- **Observed:** Grep of the draft for "Monte Carlo" / "numerical evidence" / "simulation" / "simulated" returns **nothing**. The bundle is Lean-only; no `ProductionRun` node is linked to `paper:D11` and none is required. The single non-theorem numerical value, `≈ 3.3177`, is now produced by `haldane_flip_location`, pinned by `tests/test_bundle_formulas_d11_d12.py::TestHaldaneLatticeChern::test_flip_location_is_the_number_the_paper_quotes`, and labelled "numerical rather than certified" in both the body and the paper's Fig. 3 — that is a deterministic computation with a regression test, not a production run. (The notebook's *stored* copy of that figure drops the label; see finding 5.1.)
- **Verdict:** gate passes. No finding.

**Gate-4 (ComputationCorrectness) clean.** `tests/test_bundle_formulas_d11_d12.py` runs 77 tests in 0.08 s, all passing, with `golden`-shape assertions pinned to Lean witnesses. The five new `TestHaldaneLatticeChern` tests are the right shape: three certified masses against `haldaneFrameTopo`/`haldaneFrame5`/`haldaneFrameTrivial`, the flip location to `abs=5e-5` **plus** a genuine sign-change check either side of it, the ≈36 % window fraction against `HaldaneWitness.lean:1438-1439`, and a negative test on a non-straddling bracket. The one coverage gap is the admissibility precondition (finding 6.1).

## QI Candidate

**qi-derived-artifacts-invalidated-by-dependency-edits-are-invisible-to-their-own-gate.** Finding 5.1 is the second consecutive round in which a retraction landed in `paper_draft.tex` and in the Lean source but not in the executed companion notebook, and the mechanism is now identified precisely: `validate.py --check notebook_exec` skips a notebook when the *notebook* is unchanged ("SKIPPED — unchanged, previously vetted"), so an edit to `src/core/visualizations.py` or `src/core/formulas.py` silently invalidates every stored figure and every stored `print` output downstream of it while the gate keeps reporting green. The same shape almost certainly affects every bundle with a companion notebook, not just D11 — the check is global. Recommend: (a) fold the mtimes/hashes of `src/core/visualizations.py`, `src/core/formulas.py` and `src/core/constants.py` into `notebook_exec`'s skip key, so any canonical-module edit forces re-execution of every notebook that imports them; (b) add a cheap `notebook_output_freshness` check that, for each `# viz-ref:`-tagged cell, re-renders the named function and diffs the *text strings* of the stored output against the fresh one (the exact diff I ran this round is ~25 lines and caught all four stale cells in under a second, without needing to execute the notebook); (c) treat "the bundle's shipped surface" as `{.tex, figures/*.png, prior_art_novelty.md, notebook source AND stored outputs}` in the Stage-13 anchor list, since three of those four have now each hosted a surviving retracted claim in some round.

**qi-remediation-prose-is-itself-unreviewed-input.** Finding 7.1's two *new* counterexamples (`EffectiveMediumBounds.lean:92`, `AcousticBlochOperator.lean:93`) are docstring lines written by this round's remediations. The project's counting rule, its `find_phrase` sweeps and its `count_literals` gate all treat Lean docstrings as inert prose, but docstrings are simultaneously (a) counted by the published counting rule, (b) the text a referee reads on hover, and (c) where three of the last four rounds' surviving misnomers lived. Remediation text is added under time pressure at the end of a round and is the *least*-reviewed prose in the repo, yet it lands in the most-cited artifacts. Recommend a mechanical post-remediation pass: after any round that edits `.lean` docstrings, re-run the naive-vs-generator count diff (four lines of Python) and the retracted-string sweep over the diff itself, before closing the round. Cost is seconds; it would have caught 7.1's growth from 2 to 4 and, in earlier rounds, the `EffectiveModuli.lean` docstring reflow that inflated the theorem count by one.
