---
paper: D12
reviewer: adversarial-reviewer
model: claude-opus-5
review_date: 2026-07-31T15:24:00Z
readiness_gates_version: 1
head_commit: c60e83ad
round: 7
---

# Adversarial Review — D12

## Summary

1 BLOCKER / 7 REQUIRED / 10 RECOMMENDED, across gates CitationIntegrity, ParameterProvenance, ComputationCorrectness, LeanProofSubstance, CrossPaperConsistency, NarrativeGrounding, AssumptionDisclosure, NumericalFreshness and FixPropagation. `validate.py` is 53/53 green, so Stages
1–12 are clear and Stage 13 is meaningful. All five remediations the author
described are real and I verified each independently: the r\* = 1.19135
counterexample reproduces to 4 decimals, the `r^-(n+1)` identity is exact (I did
the algebra by hand and confirmed it numerically to 4e-16 against a genuinely
independent bath-referred implementation), and the composite-bite wording now
matches what `le_max_right` actually proves. What survives is one traceability
BLOCKER — round 6's review document was never written to disk, so an entire
round of findings never became graph nodes and its closures exist only in a
commit message — plus a gate guard that I mutation-tested into failing open on
two paths, two readiness surfaces that currently disagree with the live graph,
and residual prose arithmetic. D12 is **not** submission-ready.

## Findings

### 1.1 — 🔵 RECOMMENDED — `IrwinHilton2005` is disclosed as unread for the γ closed form but silently trusted for the loop-gain convention

- **Gate:** CitationIntegrity
- **Location:** `papers/D12/paper_draft.tex:349-350` vs `:420-423`; `Lit-Search/Phase-6E/primary-sources/IrwinHilton2005.abstract.txt`
- **Observed:** At `:420-423` the paper is exemplary: "We attach~\cite{IrwinHilton2005} as the standard review in which this layer is presented, but we have not inspected its text: we hold that source only as a resolved DOI record, so the attribution of this specific closed form to it should be read as provisional." At `:349-350`, the *same unread source* carries a different attribution with no such qualifier: "\thm{loopGain\_eq\_irwinHilton} proves it equals the instrument-literature convention~\cite{IrwinHilton2005,Irwin1995}."
- **Evidence:** The cache file says so in its own words: "This file is a verification record, not a reproduction of the source text. ... DOI resolved (302 -> link.springer.com/10.1007/10933596_3)." Its usage note reads "Source of the loop-gain convention L = P_J*alpha/(G*T) that 6EC proves equivalent to its own definition (loopGain_eq_irwinHilton)" — i.e. the registry asserts the loop-gain provenance from a source whose text was never read, and the paper repeats it unqualified. `Irwin1995` is co-cited and is a stronger record (DOI slug confirms journal/volume/page) but is likewise a resolution record, not a read text.
- **Expected:** Either extend the `:420-423` provisional-attribution qualifier to cover the loop-gain convention, or narrow `:349-350` to cite `Irwin1995` alone (the 1995 letter is where the ETF loop gain is introduced) and say the review is cited for context.
- **Fix:** One clause at `:350`, mirrored into the `IrwinHilton2005` registry `notes`.
- **Cache:** cache-verified (`IrwinHilton2005`, hash `925b58102cb8`, verdict `match`, 2026-07-31T05:27:44Z)

*(All 14 bibitems checked. 12 cache-verified at the current bibitem hash with verdict `match`. Two fresh-fetched this round: **`LuccioliDegenne2024`** — the round-6 title fix changed the bibitem hash, so the cache missed; github.com and remydegenne.github.io are both outside the egress whitelist, so I verified against the only reachable primary, pulled arXiv:2510.04070's PDF and read its bibliography directly: `[LD24] L. Luccioli and R. Degenne. TestingLowerBounds. 2024. URL: https://github.com/RemyDegenne/testing-lower-bounds.` Title, authors, year and URL now agree character-for-character with the bibitem, and the p.26 body sentence "The Kullback-Leibler divergence definition now in MATHLIB was developed in the TestingLowerBounds project [LD24]" independently supports the paper's `:586` claim. Verdict `match`. **`Degenne2025Kernels`** — abs page and PDF both fetched, title and sole author match. Verdict `match`. Both records appended to `docs/citation_verifications.jsonl`. Every bibkey is in `CITATION_REGISTRY`.)*

---

### 2.1 — 🟡 REQUIRED — "the two readings differ by 23 points" is wrong, and disagrees with the Lean docstring that round 6 harmonized it against

- **Gate:** ParameterProvenance
- **Location:** `papers/D12/paper_draft.tex:430`; cf. `lean/SKEFTHawking/Electrothermal/BolometricFloors.lean:219-220`
- **Observed:** "Mather's ``as much as $30\,\%$'' is quoted without a stated convention, and the two readings differ by $23$ points".
- **Evidence:** The two readings named in the immediately preceding sentence are the PSD reading $1-\gamma$ and the amplitude reading $1-\sqrt{\gamma}$ at $\gamma(2,4)$. Computed exactly:
  ```
  gamma(2,4)      = 0.47314814814814815
  1 - gamma       = 0.5268518518518519   -> 52.7 %  (paper rounds to 53 %)
  1 - sqrt(gamma) = 0.3121423489208337   -> 31.2 %  (paper rounds to 31 %)
  difference      = 21.47 points          (22 points using the paper's own rounded figures)
  ```
  There is no pair of quantities in the sentence that differ by 23. The nearest 23 is $53-30$ — the PSD reading against *Mather's* figure — which is not "the two readings". The Lean docstring for `phononPSDGamma` says **22**: "Mather's \"as much as 30 %\" is quoted with no stated convention, and the two readings differ by 22 points". So the paper and its source-of-truth artifact now disagree by one point, in the one paragraph whose round-6 remediation was described as making all three artifacts say the same thing. `src/core/formulas.py:11538-11543` states both readings but quotes no difference, so it is silent rather than concordant.
- **Expected:** `22` at `:430`, matching the Lean; or drop the figure and say "differ by more than twenty points".
- **Fix:** One numeral at `paper_draft.tex:430`.
- **Cache:** n/a

### 2.2 — 🔵 RECOMMENDED — the paper says the closed form "attains every PSD reduction between 0 and 56 %"; 55.56 % is a supremum the form never reaches, and its own test asserts strict inequality

- **Gate:** ParameterProvenance
- **Location:** `papers/D12/paper_draft.tex:433-436`; `src/core/formulas.py:11542-11543`; `lean/SKEFTHawking/Electrothermal/BolometricFloors.lean:222-223`; `tests/test_bundle_formulas_d11_d12.py:495-501`
- **Observed:** Two over-precise phrasings in one sentence pair: "the largest amplitude reduction the closed form **admits** is $1-\sqrt{n/(2n+1)} = 33\,\%$ at $n=4$" and "the closed form attains \emph{every} PSD reduction **between $0$ and $56\,\%$**".
- **Evidence:** $\gamma(r,n)$ is strictly decreasing in $r$ with $\lim_{r\to\infty}\gamma = n/(2n+1)$, so $1-n/(2n+1) = 55.56\%$ at $n=4$ is a supremum, approached and never attained. The project's own test says exactly this and is the only artifact that gets it right:
  ```python
  # tests/test_bundle_formulas_d11_d12.py:497-501
  # and the maxima, which are SUPREMA -- approached as r -> infinity, never attained.
  assert 1.0 - F.phonon_psd_gradient_factor(10.0, n) < sup_psd
  ```
  The four prose artifacts disagree on the endpoint: Lean says `[0, 56 %)` (half-open, correct); `formulas.py` says "EVERY PSD reduction in [0, 56%]" (closed); the Mather primary-source cache says "[0, 56%]" (closed); the paper says "between 0 and 56 %" (ambiguous). Additionally 55.56 % is being reported as "56 %", so the interval as written claims reductions in (55.56 %, 56 %] that are unattainable at any $r$.
- **Expected:** "attains every PSD reduction strictly below $1-n/(2n+1) = 55.6\,\%$", and "the supremum of the amplitude reduction is 33 %".
- **Fix:** One clause at `:435`, mirrored into `formulas.py:11542` and the Mather cache so all four match the Lean's half-open form.
- **Cache:** n/a

### 2.3 — 🔵 RECOMMENDED — the second reading of the `r^-(n+1)` factor is labelled "re-referencing of $G$", but $G$ supplies only $r^{-(n-1)}$; the $r^{-2}$ comes from $T^2$ and is never attributed

- **Gate:** ParameterProvenance
- **Location:** `papers/D12/paper_draft.tex:412-419`; `src/core/formulas.py:11525-11530`; `lean/SKEFTHawking/Electrothermal/BolometricFloors.lean:210-213`
- **Observed:** "it is simultaneously the change of variable $r = 1/t$ ... and the physical re-referencing of $G$ via $G \propto T^{n-1}$ --- the two differ only in bookkeeping and yield the same expression, since $r^{-(n-1)}\cdot r^{-2} = r^{-(n+1)}$."
- **Evidence:** I verified the algebra independently. Change of variable: with $F(t,n) = \frac{n}{2n+1}\frac{t^{2n+1}-1}{t^{n}-1}$ and $t = 1/r$,
  $$\frac{r^{-(2n+1)}-1}{r^{-n}-1} = \frac{1-r^{2n+1}}{1-r^{n}}\cdot\frac{r^{n}}{r^{2n+1}} = \frac{r^{2n+1}-1}{r^{n}-1}\,r^{-(n+1)},$$
  exact. The second reading, spelled out correctly only in `Lit-Search/Phase-6E/primary-sources/Mather1982.abstract.txt`, needs **two** factors: "Using G ∝ T^(n-1), G(T_s) = G(T_b) * r^-(n-1) **and T_s^2 = T_b^2 * r^-2**". The paper and `formulas.py` name only the $G$ half and then produce $r^{-2}$ out of nowhere in the justification; a referee cannot reconstruct where it comes from. So the "one account across three artifacts" harmonization is not quite complete — there is a fourth artifact (the primary-source cache) that states it correctly and two that state it partially.
  Separately, `f_link_bath_referred`'s docstring says its $t = T_{\rm bath}/T_{\rm bolo}$ form "is the shape the transition-edge-sensor literature usually writes", while the Mather cache writes the bath-referred bracket with the *reciprocal* argument $r = T_b/T_s$. Both are algebraically fine and both route to the same $\gamma$, but they are reciprocal conventions differing by $r^{n+1}$ — in a paper whose thesis is that convention is load-bearing.
- **Expected:** "the physical re-referencing of $T^2$ and $G$ from the bath to the bolometer, via $T_s^2 = T_b^2 r^{-2}$ and $G \propto T^{n-1}$".
- **Fix:** One clause at `:415-416`, mirrored into the `formulas.py` docstring and the Lean; align the cache's bath-referred convention statement with `f_link_bath_referred`'s.
- **Cache:** n/a

### 2.4 — 🟡 REQUIRED — Mather's "as much as 30 %" is the numeral the whole §3.2 convention argument turns on and it has no `PARAMETER_PROVENANCE` entry; Gate 3 passes vacuously for D12

- **Gate:** ParameterProvenance
- **Location:** `papers/D12/paper_draft.tex:429-436`; `src/core/provenance.py`
- **Observed:** `PARAMETER_PROVENANCE` has 205 entries and not one is a D12 parameter (`grep` for Mather / F_link / gradient factor / 6E* returns nothing). The graph reports `ParameterProvenance p1 passed — no parameter dependencies declared`.
- **Evidence:** The gate passes because D12 declares no dependent parameters, not because its numerals were checked. The paper's §3.2 spends a full paragraph reasoning about a literature figure ("as much as 30 %") and derives a convention conclusion from it; that figure is a quoted primary-source result, and the mechanism the project built to make such figures auditable does not see it. The same holds for the whole numeric surface: `--check numerical_literals` reports "✓ D12 — no inline unit-bearing literals in body" and `--check count_literals` reports "✓ D12 — no count literals found", while the draft carries 0.4731, 53 %, 31 %, 33 %, 56 %, 22/25, 3/25, 0.15866, 148, 51.40, 3491.96, 76 %, 95 %, 50 %, 80 %, $z \lesssim 0.37$ and $r = 1.19$. **Not one D12 numeral is under any freshness or provenance gate.**
  I did not escalate to BLOCKER because the substance of Gate 3 — traceability to a primary — *is* met, by a different and arguably stronger artifact: `Lit-Search/Phase-6E/primary-sources/Mather1982.abstract.txt` carries the verbatim abstract sentence ("Phonon noise in the thermal link is reduced by as much as 30% relative to the usual equilibrium formula") with the retrieval date and an explicit "we hold this source in ABSTRACT ONLY" caveat. The defect is that no gate can see that, so nothing prevents the next such figure from arriving unbacked.
- **Expected:** A `PARAMETER_PROVENANCE` entry (tier `EXTRACTED`, source Mather 1982, detail "abstract only, no stated convention") for the 30 % figure, and a `DEPENDS_ON` edge so Gate 3 evaluates non-vacuously.
- **Fix:** One provenance record; optionally register $\gamma(2,4)$ as a `DERIVED` entry pointing at `phonon_psd_gradient_factor` + its golden test.
- **Cache:** n/a

### 2.5 — 🔵 RECOMMENDED — `phonon_psd_gradient_factor` and `f_link_bath_referred` carry no `Lean:` reference, and `formula_grounding` is structurally blind to a function with none

- **Gate:** ComputationCorrectness
- **Location:** `src/core/formulas.py:11497-11554`; `scripts/validate.py::check_formula_grounding`
- **Observed:** Pipeline Invariant #4 / Stage-2 gate: "Every function must reference a Lean theorem — no unformalized formulas." Neither of the two γ functions has a `Lean:` line. `phonon_psd_gradient_factor` mentions `phononPSD_eq_phononPSDGamma_one` inside a prose bullet; `f_link_bath_referred` names no Lean declaration at all, and none exists — the γ closed form is the one piece of D12 physics with no Lean theorem.
- **Evidence:** `check_formula_grounding` iterates `_parse_formula_lean_refs(...)`, i.e. it validates *the references that exist*. A function with zero references contributes zero entries and is silently clean. The check therefore cannot enforce the invariant it names.
- **Expected:** Either a `Lean:` line on both (pointing at `phononPSDGamma` / `phononPSD_eq_phononPSDGamma_one` for the first, and an honest `Lean: none — closed form is literature-quoted, not formalized` for the second), or a Lean theorem for the identity. The paper does **not** overclaim here — it says the identity is verified "in `tests/test_bundle_formulas_d11_d12.py`", never that it is kernel-checked — so this is a guard gap, not a false claim.
- **Fix:** Two docstring lines, plus a `formula_grounding` sub-check that every public function in `formulas.py` carries ≥1 `Lean:` reference. See QI Candidate.
- **Cache:** n/a

---

### 3.1 — 🔵 RECOMMENDED — `correlated_pair_sigma_eq_sqrt_two_mul` REPRODUCES unchanged from round 6: bare-real √ algebra described as quantifying a witness pair

- **Gate:** LeanProofSubstance
- **Location:** `papers/D12/paper_draft.tex:305-307`; `lean/SKEFTHawking/Detection/NEPAlgebra.lean:471-472`
- **Observed:** Paper text is byte-identical to round 6: "exhibits a fully-correlated pair whose total power is twice what quadrature predicts, with the discrepancy quantified exactly as $\sqrt{2}$ by \thm{correlated\_pair\_sigma\_eq\_sqrt\_two\_mul}." The theorem is likewise unchanged: `theorem correlated_pair_sigma_eq_sqrt_two_mul (S : ℝ) : √(4 * S) = √2 * √(S + S)`.
- **Evidence:** The statement mentions neither `IsUncorrelatedAt`, nor the witness functionals `V`/`Vtot`, nor `sigma_eq_responsivity_nep_sqrt_enbw`. It is an identity about reals that survives any edit to the witness — the project's own P3 anti-pattern. The module docstring at `:463-470` is honest about the gap ("Since the output noise goes as the square root of the PSD at fixed bandwidth ..., the statement below is exactly the ratio of the two output noises"); the paper elides that step and says the theorem "quantifies" the witness's discrepancy.
- **Expected:** Reword to "the corresponding output-noise ratio is $\sqrt2$, recorded arithmetically as \thm{...}", or ship a five-line corollary instantiating the identity at the witness's own `V`/`Vtot`.
- **Fix:** One clause at `:306-307`.
- **Cache:** n/a

*(No other class-3 findings. All 164 `\thm{}` names in the draft were checked against `lean_deps.json`: 160 resolve to project declarations and the four that do not — `HasSubgaussianMGF.measure_ge_le`, `bayesRisk_le_bayesRisk_comp`, `cdf`, `gaussianReal` — are Mathlib names, correctly. Zero intersection with the 576-node `PlaceholderMarker` set. Substrate: 582 declarations across the 13 modules (up from 576), `axiom_deps_project` empty and `axiom_deps_core ⊆ {propext, Classical.choice, Quot.sound}` for **every** one — zero violations, checked from the extract, not by sampling. I read the bodies of every theorem the round-6/7 work touched: `gaussian_ceiling_bites_nondegenerate`, `relaxation_filtered_ceiling_bites`, `relaxation_detector_chain_ceiling_bites`, `gammaOne_phononFloor_overstates_strict`, `matchedBudget_gamma_ge`, `phononPSDGamma_lt_phononPSD`, `speedupWitness_johnsonNEP_double`. None is `rfl`/`trivial`/`Equiv.refl`; none returns a hypothesis; the two composite bites are genuine `le_trans` compositions through their own composite theorem with the full binder list inherited.)*

---

### 4.1 — 🔵 RECOMMENDED — the duplicate-registry-key finding REPRODUCES, and has grown from four keys across two bundles to five keys across four

- **Gate:** CrossPaperConsistency
- **Location:** `src/core/citations.py`
- **Observed:** Round 6's 4.1 is unfixed, and a systematic sweep for duplicate normalised titles surfaces the same two sources now carried under more keys:
  ```
  "A Formalization of the Generalized Quantum Stein's Lemma in Lean"
     Meiburg2025PhysLib        arxiv=2510.08672  used_in=[D8]
     MeiburgLessaSoldati2025   arxiv=2510.08672  used_in=[D12]
     leanQI2025                arxiv=None        used_in=[D10]     <- never fetched
  "Complex Bounded Operators in Isabelle/HOL"
     UnruhCBO2025              arxiv=2512.05878  used_in=[D12]
     isabelleCBO               arxiv=None        used_in=[D10]     <- never fetched
  "Markov kernels in Mathlib's probability library"
     Degenne2025MarkovKernels  arxiv=2510.04070  used_in=[I3]
     Degenne2025Kernels        arxiv=2510.04070  used_in=[D12]
  ```
  30 normalised titles in the registry are carried under ≥2 keys.
- **Evidence:** Gate 2's literal blocking condition ("a bibkey whose `arxiv_id` differs across papers") is still not triggered, because the keys differ rather than the IDs. But D10 and D12 make the same prior-art carve-out about the same two works, and D10's copies carry `arxiv: None, doi: None` while D12's are arXiv-verified — the citation cache keys on `(bibkey, bibitem_hash)`, so verification cannot amortize across a duplicated source by construction.
- **Expected:** One registry entry per primary source, aliased.
- **Fix:** Alias `leanQI2025 → MeiburgLessaSoldati2025` and `isabelleCBO → UnruhCBO2025`; add a duplicate-normalised-title/DOI/arXiv detector to `validate.py`. See QI Candidate.
- **Cache:** cache-verified (both D12 keys `match`)

*(No cross-paper contradictions found. I checked the three seams the anchor list names. D9↔D12: D9's `readoutDecayProb` enclosure $(t/T_1)/(1+t/T_1) \le p_{\rm decay}$ is exactly the relaxation branch D12's composites consume, halved by `avgAssignmentError`; consumption-only, no re-proof, and D9 claims no detector model — its `fdt_noise_floor_detector` is a Johnson operating point, not an electrothermal device. D1↔D12: `GrapheneNoiseFormula.johnsonNyquistPSD kB_T sigma_Q = 4·kB_T·σ_Q` is consumed by D12 at $\sigma_Q = 1/R$; D1's draft does not itself cite it, so the seam the anchor list records is one-sided but not contradictory. D9↔D12 Helstrom/POVM: project-side, not PhysLib, exactly as both drafts say.)*

---

### 5.1 — 🟡 REQUIRED — "$\mathrm{ENBW} = 1/4T$ for a single-pole lock-in filter" reuses $T$ for a time constant thirty lines after proving $\mathrm{ENBW}\cdot T \ge 1/2$, and reads as a counterexample to the paper's own headline theorem

- **Gate:** NarrativeGrounding
- **Location:** `papers/D12/paper_draft.tex:285-289`
- **Observed:** "The $\mathrm{ENBW}\cdot T$ relation itself circulates in the instrument literature as a convention attached to particular filters --- $t_{\rm int} = 1/(2\,\mathrm{ENBW})$, and $\mathrm{ENBW} = 1/4T$ for a single-pole lock-in filter --- rather than as a proved inequality with a sharpness characterization."
- **Evidence:** `enbw_mul_window_ge_half` (`Detection/FilterFloors.lean:197-201`) fixes $T$ as the **observation-window length**: the binders are `IntervalIntegrable h volume 0 T` and `(∫ x in (0:ℝ)..T, h x) ≠ 0`, and the conclusion is `1 / 2 ≤ enbw h T * T`. In "$\mathrm{ENBW} = 1/4T$" the $T$ is the single-pole filter's RC time constant, and the filter has infinite support so it is not in the admissible class at all. As typeset the sentence multiplies out to $\mathrm{ENBW}\cdot T = 1/4 < 1/2$, i.e. a printed violation of the section's central result, with nothing in the sentence marking the change of meaning. Both quoted relations are also attributed only to "the instrument literature" with no citation, in a paper that cites Gabor for the far weaker antecedent one sentence earlier.
  (Both relations are themselves correct: a single-pole RC filter of time constant $\tau$ has one-sided ENBW $1/(4\tau)$, and $t_{\rm int} = 1/(2\,\mathrm{ENBW})$ is consistent with the boxcar's $\mathrm{ENBW} = 1/(2T)$. The defect is the symbol, not the physics.)
- **Expected:** "$\mathrm{ENBW} = 1/(4\tau)$ for a single-pole lock-in filter of time constant $\tau$ — a filter outside the window-supported admissible class of \thm{enbw\_mul\_window\_ge\_half}, so the two numbers are not comparable", with a citation attached.
- **Fix:** Rename the symbol at `:287` and add one parenthetical; attach a source or say plainly that none was found (as the paper already does for the inequality itself at `:288`).
- **Cache:** n/a

### 5.2 — 🟡 REQUIRED — "the relaxation branch is inert and the $\max$ selects the detection term" misdescribes the round-6 remediation's own proofs; both proofs *discard* the relaxation branch, and for $t/T_1 > 0.465$ the $\max$ selects it

- **Gate:** NarrativeGrounding
- **Location:** `papers/D12/paper_draft.tex:553-556`; mirrored in `lean/SKEFTHawking/Control/CompositeReadoutCeilings.lean:668-675`
- **Observed:** "It is not a sharper bound: at this point the relaxation branch is inert and the $\max$ selects the detection term, so the value is the single-mechanism one."
- **Evidence:** Both proofs close by *bounding the max from below by its right branch*, not by evaluating it:
  ```lean
  -- CompositeReadoutCeilings.lean:694-697 (and :720-723)
  have hmax : Detection.gaussianQ ((2:ℝ) / 2)
      ≤ max (t / T1 / (2 * (1 + t / T1))) (Detection.gaussianQ ((2:ℝ) / 2)) := le_max_right _ _
  linarith
  ```
  The theorems quantify over all `t ≥ 0`, `T1 > 0` with no constraint tying them to the budget. Solving $x/(2(1+x)) = Q(1) = 0.158655$ gives $x = 0.4651$: for $t/T_1 > 0.4651$ the relaxation term **is** the larger of the two and the max selects *it*, not the detection term. The conclusion still holds (the bound is a `≤`), but the stated reason is false over a large part of the theorem's own domain. "Inert" is the wrong word for "discarded by the proof".
  The load-bearing half of the remediation is correct and I confirmed it: the composite bound is not sharper, the composite *statement* (full binder list, attributed pair $(e_0,e_1)$ rather than the threshold pair) is genuinely non-vacuous at matched budget 2, and $1-Q(1) \le 22/25$ follows from $3/25 \le \frac{1}{1+1^2}\varphi(1) = 0.120985$ — which I verified arithmetically. This finding is about the one sentence that explains *why*.
- **Expected:** "It is not a sharper bound: each proof lower-bounds the maximum by its detection branch and discards the relaxation term, so the numerical value $22/25$ is the single-mechanism one and holds uniformly in $t$ and $T_1$."
- **Fix:** One sentence at `:555-556`; mirror into `CompositeReadoutCeilings.lean:673-675`, which currently states the proof mechanism correctly ("Each proof closes on the right-hand branch of the maximum") and then contradicts itself with "the relaxation branch is inert".
- **Cache:** n/a

### 5.3 — 🔵 RECOMMENDED — the $f$-divergence generator/complement factor-2 REPRODUCES, now in three artifacts

- **Gate:** NarrativeGrounding
- **Location:** `papers/D12/paper_draft.tex:235-236`; `Lit-Search/Phase-6E/primary-sources/LuccioliDegenne2024.abstract.txt`
- **Observed:** Unchanged from round 6: "The squared Hellinger distance is an $f$-divergence with generator $f(t) = (\sqrt{t}-1)^2$, and the affinity is its complement".
- **Evidence:** With $f(t)=(\sqrt t-1)^2$, $D_f(P\|Q) = \sum_i q_i(\sqrt{p_i/q_i}-1)^2 = 2-2\,\mathrm{BC} = 2(1-\mathrm{BC})$. The complement relation $H^2 = 1-\mathrm{BC}$ holds only for the $\tfrac12$-normalised squared Hellinger, generator $\tfrac12(\sqrt t-1)^2$. The primary-source cache repeats it with a hedge ("H^2(p,q) = 1 - BC(p,q) in the normalized case") without saying the generator must change too. The **subsumption argument is unaffected**: BC is an affine decreasing function of $D_f$ under either normalisation, so an $f$-divergence DPI does imply the affinity DPI, which is what the paragraph claims.
- **Expected:** $f(t)=\tfrac12(\sqrt t-1)^2$, or "an affine function of" in place of "the complement of".
- **Fix:** One symbol at `:235`; mirror into the cache file.
- **Cache:** n/a

### 5.4 — 🔵 RECOMMENDED — the blocking-checks paragraph still describes TestingLowerBounds anonymously (round-6 5.9, partially reproduced)

- **Gate:** NarrativeGrounding
- **Location:** `papers/D12/paper_draft.tex:614-616`
- **Observed:** "First, an out-of-Mathlib Lean project on information theory and hypothesis testing is under active development; a Bayes-binary-risk--divergence lower bound there would be substantially our Le Cam floor, and it is the highest prior-art risk in this work." No `\cite`.
- **Evidence:** §5 names the project by name and citation two paragraphs earlier (`:586`, "itself developed in the \textsc{TestingLowerBounds} project~\cite{LuccioliDegenne2024,Degenne2025Kernels}") and the conclusion names it again (`:672`). Only the paragraph that declares it blocking withholds the name, so a reader of that paragraph alone cannot tell which project is meant.
- **Expected:** Name it and cite it where the obligation is declared.
- **Fix:** Insert `\textsc{TestingLowerBounds}~\cite{LuccioliDegenne2024}` at `:614`.
- **Cache:** n/a

*(All other class-5 checks pass and I re-derived each rather than inheriting it. No unhedged first-claim exists: every "first" in the draft is either a refutation of one ("a factor-two sharpening ... not a first kernel-checked Chernoff bound", `:88-89`; "Our Gaussian Chernoff bound is a *sharpening*, not a first", `:574`) or the phrase "first-class deliverable". Every Mathlib absence claim in §5 holds at pin `81a5d257`, verified by direct grep of the checkout: no `erf`/`erfc` token anywhere; no `fDiv` token; `Hellinger` occurs only as Hellinger–Toeplitz; `Bhattacharyya` only as an author name in `MeasureTheory/Measure/Tight.lean`; no `Neyman`, no hypothesis test, no Le Cam, no binary Bayes risk. Every Mathlib *presence* claim also holds: `Probability/CDF.lean:57 def cdf`, `Probability/Decision/Risk/Basic.lean:236 bayesRisk_le_bayesRisk_comp`, and `Moments/SubGaussian.lean:334 measure_ge_le` docstringed "Chernoff bound on the right tail of a sub-Gaussian random variable" giving `exp (-ε ^ 2 / (2 * c))` — exactly a factor 2 weaker at $c=1$ than the project's `gaussianQ z ≤ (1/2) * Real.exp (-z^2/2)`. The PhysLib claim is verbatim true at pin `c4843367`: `QuantumInfo/States/Mixed/Fidelity.lean` docstrings `fidelity` as "the quantum version of the Bhattacharyya coefficient" and its trailing `--TODO:` block's second line reads "Matches with classical (squared) Bhattacharyya coefficient". The §5 grep claim — "for [the electrothermal device model] a grep across the entire package tree returns no related identifier of any kind" — I re-ran over `.lake/packages` for `bolometer`, `electrothermal`, `loopGain`, `loop gain`, `thermistor`, `transition-edge`, `noise equivalent power`, `ENBW`, `matched filter`: all zero. The prior-art hedging is now internally consistent — abstract `:55`, intro `:91`, §5 `:614` and conclusion `:672` all say **two** open checks, and §5 `:604-606` now narrows "assessed" to the Coq and Isabelle *published literature* with the libraries explicitly carved out.)*

---

### 6.1 — 🔵 RECOMMENDED — `avgError_ge_gaussianQ_sharp` carries an undisclosed ordering binder $\mu_0 \le \mu_1$

- **Gate:** AssumptionDisclosure
- **Location:** `papers/D12/paper_draft.tex:194-198`; `lean/SKEFTHawking/Detection/GaussianThreshold.lean:785-787`
- **Observed:** "The headline is the sharp separation-budget floor \thm{avgError\_ge\_gaussianQ\_sharp}: no threshold placement achieves an average error below $Q(\Delta\mu/2\sigma)$."
- **Evidence:** The signature is `(hσ : 0 < σ) (hμ : μ₀ ≤ μ₁) (hz : (μ₁ - μ₀) / (2 * σ) ≤ z₀)`. `hσ` is a positivity condition a reader supplies; `hμ` is a **sign convention** on the separation, and without it the statement is false (a signed $\Delta\mu$ with $\mu_1 < \mu_0$ makes $Q(\Delta\mu/2\sigma) > 1/2$). §2.3 nowhere says the branches are labelled so that the signal mean is the larger. Low severity because the convention is the obvious one and no claim is weakened — but the paper's §5 assumption paragraph enumerates twelve modelling Props to four-decimal precision while this binder of the section's own headline theorem goes unnamed.
- **Expected:** "for a signal branch of the larger mean ($\mu_0 \le \mu_1$, an explicit binder)".
- **Fix:** One parenthetical at `:195`.
- **Cache:** n/a

*(The round-6 6.1 defect is genuinely fixed and I re-read the signature rather than the fix note: `bolometer_error_floor` (`Electrothermal/BolometricFloors.lean`) binds `IsUncorrelatedAt`, `IsThermalFluctuationLimited`, `IsWhiteFilteredVariance` (Johnson channel) and `IsAdmissibleFilter`, which is exactly the four the paper now names at `:637-642`; `IsResponsivityChain` is gone from that sentence and appears only in the 6EB NEP-chain discussion where it actually binds. The twelve-Prop enumeration at `:623-631` is **exactly right** — I enumerated every `Prop`-valued declaration across the 13 modules and got 11 `def`s plus one `structure ... : Prop` (`IsAdmissibleFilter`, `Detection/MatchedFilter.lean:136`) = 12, with no thirteenth. The 10 / 1 / 1 split is also right: `IsThermalFluctuationLimitedGamma` has zero binder uses tree-wide, and `PerturbationsDecay` is the conclusion of `etf_stable_iff`. `poisson_avgError_floor`'s sole hypothesis really is `hδ : IsCountRule δ`, so the `:132` claim is exact. `gammaOne_phononFloor_overstates_strict`'s extra binders are exactly `hγ1 : γ < 1` and `hE : 0 < ∫ s²`, exactly as `:452-453` says.)*

---

### 7.1 — 🔵 RECOMMENDED — spelled-out tree counts REPRODUCE outside the macro chain; both correct today, both invisible to `count_literals`

- **Gate:** NumericalFreshness
- **Location:** `papers/D12/paper_draft.tex:93` ("Thirteen root-imported modules"), `:623` ("Twelve propositions carry modelling or admissibility content")
- **Observed:** Both are hand-written counts of tree contents, not `\input{counts.tex}` macros. The second was "Eleven" in round 6 and is now "Twelve" — it drifted and was hand-corrected, which is exactly the failure mode the macro chain exists to prevent.
- **Evidence:** Both verified correct at HEAD: `lean/SKEFTHawking.lean` root-imports exactly 13 `Detection.*`/`Electrothermal.*`/`Control.*` modules (lines 5082–5197); exactly 12 `Prop`-valued declarations exist across them. `validate.py --check count_literals` reports "✓ D12 — no count literals found" — the detector does not match spelled-out numerals, so this class is unguarded across every bundle.
- **Expected:** `d12modules` / `d12modelprops` macros in `docs/counts.tex`, or extend `count_literals` to catch spelled-out numerals before "modules"/"propositions"/"theorems".
- **Fix:** Two macros, or one regex.
- **Cache:** n/a

### 7.2 — 🟡 REQUIRED — `bundle_metadata.json` still reports "all 565 declarations kernel-pure"; the live count is 582, a second consecutive round of drift in the same field

- **Gate:** NumericalFreshness
- **Location:** `papers/D12/bundle_metadata.json` (`notes`)
- **Observed:** The notes field says "all **565** declarations kernel-pure from axiom closures".
- **Evidence:** `lean_deps.json` carries **582** declarations across the 13 D12 modules (`Detection` 214, `Electrothermal` 168, `Control` 200). Round 6 flagged this at 576; it has since drifted a further six and was not touched. The kernel-purity claim itself is true — I re-verified all 582 have empty `axiom_deps_project` and `axiom_deps_core ⊆ {propext, Classical.choice, Quot.sound}` — but the count attached to it is 17 low, and this field is quoted downstream as a substrate summary.
- **Expected:** Derive the number from `lean_deps.json` at write time, or drop it.
- **Fix:** One field; better, have `bundle_lift`/`bundle_readiness` recompute it.
- **Cache:** n/a

---

### 8.1 — 🔴 BLOCKER — round 6's D12 review document was never written to disk, so an entire round of findings never became ReviewFinding nodes and its closures exist only in a commit message

- **Gate:** FixPropagation
- **Location:** `papers/AutomatedReviews/` (no round-6 D12 file); `docs/review_finding_supersessions.json`; `scripts/validate.py:3374-3418`
- **Observed:** The findings the author describes as remediated this round — 2.1 (test-file false universal), 2.2 (`r^-(n+1)`), 4.1 (ledger re-key), 5.1 (composites bite), 8.1 (gate severity), 8.2 (orphan-scan failing open) — do not correspond to any finding numbers in the only D12 review on disk. `papers/AutomatedReviews/2026-07-31-0829-internal-adversarial/D12.md` numbers 2.1 as "at 95 % of truth", 2.2 as the γ attribution paragraph, 4.1 as the duplicate registry keys and 5.1 as the one-vs-two prior-art count, and has no 8.x section at all. There is no other D12 review file anywhere in the tree.
- **Evidence:**
  ```
  $ ls papers/AutomatedReviews/*/D12*.md
  2026-07-31-0829-internal-adversarial/D12.md      (the round-4/5 review, 299 lines)
  2026-07-31-bundle-stage13/D12.md                 (bundle_readiness aggregation summary; mints no findings)
  ```
  I searched all 781 supersession records for any mention of a round-6 remediation (`r^-(n+1)`, `bite in their own right`, `severity-aware`, `readiness_verdicts_agree`, `_eval_fix_propagation`, `round-6 BLOCKER 8.1/5.1/2.1`, `round-6 finding 2.2`): **zero hits**. The 15 D12 ledger records all close 0829-numbered findings. So round 6's findings have no node, no FLAGS edge, no ledger entry, and no written statement of the defect — only the HEAD commit message. Gate 11's pass condition ("every FLAGS incoming has `status != 'open'`") is satisfied *vacuously* for a whole round of BLOCKERs.
  This is not hypothetical damage: it has already corrupted the ledger once. `validate.py:3374-3378` says so in its own words — "Findings raised in rounds whose review document was never written to disk were filed under an EARLIER review's IDs. That both collides with live findings (a still-open finding rendered `fixed`) and mints dangling IDs naming no node." The 4.1 re-key the author reports fixing is that collision. The **mechanism is unchanged**: nothing in the pipeline requires a Stage-13 round to leave a document, and nothing detects a round that didn't.
  The gate is simultaneously wrong in the other direction. Six 0829 findings that I verified are **demonstrably fixed in the tree** still carry `status: open` and are counted as blocking: 2.2 (γ attribution — now cites `IrwinHilton2005`, states the asymptote as $n/(2n+1)$, and discloses the attribution as provisional), 5.1 (one-vs-two — abstract, intro, §5 and conclusion all say two), 5.3 (Coq/Isabelle — now carved out explicitly), 5.4 (fail-open label — now "mirror image of the fail-open failure ... not an instance of it"), 5.6 (propagator norm — now names the $\ell^\infty$ operator norm and says the spectral norm is 1), 5.7 (ceiling recount — now distinguishes by floor provenance). So D12's `FixPropagation blocked — 6 open review findings at severity blocker/critical/major` is composed *entirely* of findings that are fixed, while the round that produced actual BLOCKERs contributes nothing.
- **Expected:** Every Stage-13 round writes its report to `papers/AutomatedReviews/{YYYY-MM-DD-HHMM}-internal-adversarial/{key}.md` before any remediation begins, per `WAVE_EXECUTION_PIPELINE.md` Stage 13 action 3. Where a round's report is genuinely lost, its findings are reconstructed into a dated file so they can be superseded by ID.
- **Fix:** (a) Reconstruct round 6's D12 findings into a dated review file from the commit message, or accept them as closed against *this* review's IDs; (b) file supersession records for 0829 D12 2.2 / 5.1 / 5.3 / 5.4 / 5.6 / 5.7 citing this round as the re-invocation evidence; (c) add a `validate.py` check that every bundle whose `bundle_metadata.last_stage13_review` is set has a matching review file on disk minting ≥1 finding node for that bundle.
- **Cache:** n/a

### 8.2 — 🟡 REQUIRED — `readiness_verdicts_agree` fails open on two paths, and cannot reach the failure mode it was written for

- **Gate:** FixPropagation
- **Location:** `scripts/validate.py:3818-3907`
- **Observed:** I mutation-tested the guard against the live graph. It catches the mutation the author reported and misses two others.
- **Evidence:**
  ```
  BASELINE                                     passed = True   (15 heatmap-RED bundles, 0 disagreements)
  MUT1  D12 ReadinessGate nodes deleted        passed = True   <- FAIL-OPEN
        detail: "D12  heatmap RED (6 blockers) but no ReadinessGate nodes exist for paper:D12"
  MUT2  D12 blocked gate -> needs-recheck      passed = False  <- caught (the author's mutation)
  MUT3  bundle_readiness raises                passed = True   <- FAIL-OPEN
        detail: "heatmap  heatmap unavailable (simulated heatmap failure); skipping"
  ```
  * **MUT1** — `:3879-3884` emits `Detail(bundle, True, ...)` with `warning=True` and `continue`s without incrementing `disagreements`. A heatmap-RED bundle with no gate nodes is *exactly* the state in which `readiness_submission_gate` silently omits the bundle from its report altogether, i.e. the most dangerous state available, and the guard rates it a warning. Trigger: any change to the Paper-node glob or key that drops `paper:D12`.
  * **MUT3** — both the `ImportError` handler (`:3844-3846`) and the bare `except Exception` (`:3854-3857`) return `passed=True`. Compare `:3369-3373` in the same file, which was corrected this round for precisely this reason: "FAIL, not pass: this guard exists to make invisible findings visible, so a build error in the scan must not be indistinguishable from a clean scan (D12 round-6 finding 8.2 — the original `warning=True` failed open)." The same lesson was not applied 450 lines later.
  * **Structural.** The docstring says "Two subsystems compute a per-bundle readiness verdict **from different inputs**". They do not. `bundle_readiness.load_findings_by_paper` calls `build_graph.extract_review_finding_nodes()` — the same function the graph uses — and both sides attribute findings through the same `meta['inferred_bundle']`, itself derived from the review **filename stem** via `_infer_bundle_from_text`. A review filed as `D12-round7.md` yields `inferred_bundle = None`, so `load_findings_by_paper` drops it *and* `extract_flags_edges` emits no edge: heatmap GREEN **and** gate passed, together. The guard then does nothing at all, because its first act is `if str(agg.get('readiness','')).upper() != 'RED': continue`. The check can only fire when one of the two subsystems is already correct — which makes it blind by construction to the attribution failure that the four `⚠️ ADDED 2026-07-31` comments in `build_graph.py` and `bundle_readiness.py` say has now bitten four separate layers.
  * `BLOCKING_SEVERITIES` **is** complete against the minter for blocking purposes: `_SEV_GLYPHS` mints `critical`/`major`/`minor` and the default is `advisory`, and `{critical, blocker, major}` covers the first two. `blocker` is dead — never minted — which is harmless but indicates the set was written without consulting `_SEV_GLYPHS`. One brittleness worth recording, which this very report demonstrates: `_BLOCKER_RE` requires an asterisk immediately preceding the token BLOCKER, so the mandated heading form `### 1.1 — 🔴 BLOCKER — …` does **not** match it and severity comes from the glyph alone — but a bold-emphasised occurrence of that token anywhere in the file escalates *every* finding in that file to `critical` through the `elif file_has_critical_marker` branch, regardless of its own glyph. Severity is a function of incidental markdown formatting. My first draft of this section quoted the regex literally and thereby minted all 19 of this report's findings as `critical`, including the ProductionRunHealth PASS section; the quotation had to be paraphrased to restore honest severities.
- **Expected:** MUT1 → `disagreements += 1` (heatmap-RED with no gate nodes is strictly worse than a disagreement). MUT3 → `passed=False` on both exception paths, matching the sibling guard. Structural → assert the converse direction too (any bundle with ≥1 open blocking ReviewFinding must be non-GREEN at *both* surfaces) and add an independent floor: assert that every review file named after a roster bundle mints ≥1 finding resolving to that bundle, which is a check on the shared attribution layer rather than on its two consumers.
- **Fix:** Three lines for the two fail-open paths; one new sub-check for the shared-input blindness.
- **Cache:** n/a
- **Lane:** `infra`

### 8.3 — 🟡 REQUIRED — two further readiness surfaces disagree with the live graph right now, and neither is guarded

- **Gate:** FixPropagation
- **Location:** `papers/D12/bundle_metadata.json`; `docs/BUNDLE_READINESS_HEATMAP.md:35`
- **Observed:** The operator asked whether a bundle with open blockers can reach green by another route. Two such routes are currently occupied.
- **Evidence:**
  ```
  live graph        FixPropagation blocked — 6 open blocking findings, 15 open in total
  live aggregate    D12: 19 total / 15 open / 6 blockers / {minor 8, major 6, advisory 1} / RED
  bundle_metadata   "blockers_open": 0, "advisories_open": 0, "freshness_stale": false
  heatmap doc :35   | D12 | 1 | 1 | 32 | 13 | 2 advisory, 2 critical, 11 major, 17 minor | RED |
  ```
  * `papers/D12/bundle_metadata.json` asserts **zero** open blockers and **zero** open advisories against a live 6 and 15. That zero is load-bearing: `LATE_PHASE6_ABSORPTION_PROTOCOL.md` makes `freshness_stale` clearable only once `blockers_open == 0`, and `freshness_stale` is set `false` here. Nothing recomputes or cross-checks these fields — the same file's `stage13_review_doc` is `null` while `last_stage13_review_source` names a backfilled path, so the record is self-inconsistent as well.
  * `docs/BUNDLE_READINESS_HEATMAP.md` is one commit stale: it was written by `ad844e42` (round 6) and not regenerated after `c60e83ad` closed findings, so its D12 row reports 32 open / 13 blockers / "2 critical, 11 major" against a live 15 / 6 / "0 critical, 6 major" — off by better than 2×, and it reports two `critical` findings where none now exists. `readiness_verdicts_agree` recomputes the aggregate live, so it cannot see the stale document; no check compares the rendered heatmap against a fresh run. Today the staleness errs pessimistic, which is safe; the same mechanism errs optimistic for any round that *adds* findings after the last regeneration.
- **Expected:** `bundle_metadata.{blockers_open, advisories_open}` derived from the graph at write time, not hand-maintained; a `validate.py` check that regenerating `BUNDLE_READINESS_HEATMAP.md` is a no-op (the `tables_fresh` pattern applied to the heatmap).
- **Fix:** Have `bundle_readiness.py` write both fields back, and add a heatmap-freshness check.
- **Cache:** n/a

### 8.4 — Class 8 ProductionRunHealth: PASS, no findings

D12 makes no simulation-evidence claim. I scanned the draft for "Monte Carlo", "numerical evidence", "simulation" and "simulated" — none present; every quantitative statement is a kernel-checked rational enclosure or a hand/Python evaluation of a closed form. The graph carries 18 `ProductionRun` nodes and zero links touching D12, which is correct for a pure-formalization bundle.

---

## Prior-round disposition (2026-07-31-0829 review, and the five round-6 items)

Every item re-verified against the tree at `c60e83ad`; none taken on the author's word.

| Prior finding | Status | Evidence |
|---|---|---|
| 0829 1.1 BLOCKER — LuccioliDegenne2024 title | **fixed** | Bibitem now `\emph{TestingLowerBounds} (2024). \url{...}` with the secondhand caveat; registry title `TestingLowerBounds`; the only surviving occurrence of the bad subtitle is inside the `citations.py` correction note. I re-read `[LD24]` out of the 2510.04070 PDF myself. |
| 0829 1.2 RECOMMENDED — Mather DOI-only | **fixed-but-differently** | Still DOI-resolution-only, but now cache-verified at the current hash (verdict `match`, 2026-07-31T08:25:56Z) and the bibitem itself discloses the scope. Superseded in substance by new 1.1, which is the same defect on `IrwinHilton2005`. |
| 0829 2.1 BLOCKER — "95 % of truth" at z = 1 | **fixed** | `:548` now reads "$76\,\%$ ... against $95\,\%$ at $z=2$". I recomputed: BF/Q = 0.7626 at z=1, 0.9493 at z=2. The added Mills comparison is also right: BF/Mills = $z^2/(1+z^2)$ = 0.50 and 0.80. |
| 0829 2.2 REQUIRED — γ attribution | **fixed** | `IrwinHilton2005` now co-cited for the closed form, asymptote stated as $n/(2n+1) = 4/9$, the $[1/2,1]$ band explicitly said not to contain 0.4731, and the attribution disclosed as provisional. Residue split into new 2.1/2.2/2.3. |
| 0829 3.1 RECOMMENDED — `correlated_pair_sigma_...` | **reproduces** | Paper text and theorem both byte-identical. Re-filed as 3.1. |
| 0829 4.1 RECOMMENDED — duplicate registry keys | **reproduces, worse** | `leanQI2025`/`isabelleCBO` unchanged; two further same-source key pairs surfaced. Re-filed as 4.1. |
| 0829 5.1 REQUIRED — one vs two open checks | **fixed** | Abstract `:55`, intro `:91`, §5 `:614`, conclusion `:672` all say two, with the Isabelle-AFP/Coq-`infotheo` gap named. |
| 0829 5.2 BLOCKER — "Mathlib carries no Gaussian CDF" | **fixed** | `:191-193` and `:589-591` now state that `cdf` applied to `gaussianReal` **is** available and explain what is actually missing (closed form, tail bound). Verified `Probability/CDF.lean:57` at the pin. `prior_art_novelty.md` carries no residual absence claim. |
| 0829 5.3 REQUIRED — Coq/Isabelle assessed vs not | **fixed** | `:604-606` now narrows "assessed" to the Coq and Isabelle *published literature*, "(not their libraries, which are treated in the next paragraph)". |
| 0829 5.4 REQUIRED — "fail-open" mislabel | **fixed** | `:454-457` now: "the mirror image of the fail-open failure of Section 2, **not an instance of it**: a floor stated too *high* yields false *refutations*". Mirrored in `BolometricFloors.lean:267-278` and the Mather cache. |
| 0829 5.5 REQUIRED — composites bite | **fixed by building** | `relaxation_filtered_ceiling_bites` and `relaxation_detector_chain_ceiling_bites` now exist (`CompositeReadoutCeilings.lean:680`, `:702`), derived by calling their composites so they inherit the full binder list. Residue is new 5.2 (the explanation, not the claim). |
| 0829 5.6 REQUIRED — propagator norm unnamed | **fixed** | `:498-502` now names the $\ell^\infty$ operator norm and states the spectral norm is 1. |
| 0829 5.7 RECOMMENDED — three vs two ceilings | **fixed** | `:519-521` now gives the real criterion: the two anchors are "a one-line re-casting of a pre-existing quantum-network floor into fidelity form". |
| 0829 5.8 RECOMMENDED — $f$-divergence factor 2 | **reproduces** | Unchanged at `:235-236`. Re-filed as 5.3. |
| 0829 5.9 RECOMMENDED — TLB unnamed in §5 | **partially reproduces** | Now named at `:586`, still anonymous in the blocking-checks paragraph `:614`. Re-filed as 5.4. |
| 0829 6.1 REQUIRED — `IsResponsivityChain` binder | **fixed** | `:637-642` names the four real binders; verified against the signature, not the fix note. |
| 0829 6.2 RECOMMENDED — γ ≤ 1 as hypothesis | **fixed-but-differently** | Still phrased "Because $\gamma \le 1$", but the Mather bibitem now carries the claim explicitly ("i.e.\ $\gamma \le 1$") and `:452-453` discloses that the strict form additionally requires $\gamma < 1$ as a binder. Adequately grounded. |
| 0829 7.1 RECOMMENDED — spelled-out counts | **reproduces** | Both literals still present (one having drifted 11→12 in the interim). Re-filed as 7.1. |
| **Round-6 2.1** — test file's false universal | **fixed, verified** | `test_a_thirty_percent_psd_reduction_IS_attainable` is real and the counterexample is real: I bisected $1-\gamma(r,4) = 0.30$ and got $r^\* = 1.1913538$; at the pinned 1.19135 the reduction is 0.29999643. The old name and comment are gone; the surrounding prose no longer asserts the universal — `:438-440` records the retraction. `pytest tests/test_bundle_formulas_d11_d12.py` → 77 passed. Residue: new 2.1 (the "23 points" numeral) and 2.2 (attained vs supremum). |
| **Round-6 2.2** — `r^-(n+1)` two accounts | **fixed, verified** | I did the algebra by hand: $F(1/r,n) = \frac{n}{2n+1}\frac{r^{2n+1}-1}{r^{n}-1}r^{-(n+1)}$, exact. `f_link_bath_referred` is a **genuinely independent** implementation — it omits the $r^{-(n+1)}$ factor entirely and takes the reciprocal argument — so `test_bolometer_referred_gamma_equals_bath_referred_F_link` is a real cross-check, not a restatement; max relative error over the 28-point grid is 3.7e-16. Two residues in new 2.3: the $r^{-2}$ is attributed to $G$ when it comes from $T^2$, and the paper says the identity is verified "not by spot-checking values" when the artifact is a 28-point float grid with no symbolic or Lean backing. |
| **Round-6 5.1** — composites "bite in their own right" | **fixed, then over-corrected in the explanation** | The new wording gets the load-bearing part exactly right ("It is not a sharper bound"; the composite *statement* is non-vacuous) and I confirmed the 22/25 arithmetic. But the reason given — "the relaxation branch is inert and the $\max$ selects the detection term" — is false for $t/T_1 > 0.4651$, where the max selects the relaxation branch; both proofs *discard* the branch via `le_max_right` rather than evaluating the max. Re-filed as 5.2. |
| **Round-6 8.1** — gate severity awareness | **fixed, but the guard has holes** | `_eval_fix_propagation` is severity-aware and D12 now reads `FixPropagation blocked`; `paper_aggregate_state` treats any blocked gate as red; `BLOCKING_SEVERITIES` is complete against `_SEV_GLYPHS`. MUT2 (reverting the severity fix) is caught. MUT1 (no gate nodes) and MUT3 (heatmap raises) both fail open, the guard is blind by construction to the shared-attribution failure mode, and two further verdict surfaces are unguarded and currently wrong. Re-filed as 8.2 and 8.3. |
| **Round-6 4.1** — supersession re-key | **fixed, and the re-key is correct** | `review:2026-07-31-0829-internal-adversarial:D12:1.1` resolves to a live ReviewFinding node; `review:2026-07-31-bundle-stage13:D12:1.1` does not (that directory holds `bundle_readiness.py`'s aggregation summary, which mints no finding nodes). The pinned dangling baseline of 67 is **honest**: I recomputed it from the live graph and got exactly 67, so there is no headroom hiding a new dangling record. The 29 dangling IDs dated 2026-07-2x/3x are all legacy `stage9`/`stage10` slug-scheme keys that never minted nodes, consistent with the comment. The underlying cause — a round with no review document — is unfixed and is finding 8.1. |

## QI Candidate

**Three systemic gaps, all instances of one pattern: a guard that validates its consumers instead of its shared input.**

1. **A Stage-13 round can complete without leaving a review document.** Round 6's D12 findings never became nodes, and their closures live only in a commit message. `WAVE_EXECUTION_PIPELINE.md` Stage 13 specifies the output path but nothing enforces it, and Gate 11's "no open FLAGS" is trivially satisfied by a round that filed nothing. The consequence already materialized once (the 4.1 mis-key) and the ledger-integrity check added in response detects the *symptom* (dangling IDs) rather than the *cause* (a round with no document). Proposed: a check that every bundle with a non-null `last_stage13_review` has a review file minting ≥1 finding node for that bundle at or after that date, plus a pre-commit refusal to record remediation of a finding ID that resolves to no node.

2. **Cross-validation between two consumers of one input is not independence.** `readiness_verdicts_agree` compares `bundle_readiness` against the ReadinessGate graph, but both call `extract_review_finding_nodes()` and both attribute through `meta['inferred_bundle']`, derived from the review *filename*. Every failure in that shared layer moves both verdicts the same way, and the guard's `if readiness != 'RED': continue` means it never even runs in that case. Four separate `⚠️ ADDED 2026-07-31` comments in `build_graph.py` / `bundle_readiness.py` document that this shared layer has now failed at four distinct points. The guard that should catch the fifth cannot. Proposed: guard the *input* — assert that every review file named after a roster bundle mints ≥1 finding resolving to that bundle (`findings_reach_the_graph` already does half of this; make it total) — and add the converse direction (≥1 open blocking finding ⇒ non-GREEN at every surface).

3. **Readiness has four verdict surfaces and two of them are hand-maintained.** The graph gates, `bundle_readiness`'s live aggregate, `docs/BUNDLE_READINESS_HEATMAP.md`, and `papers/<X>/bundle_metadata.json`. The last two currently disagree with the first two (0 vs 6 blockers; 13 vs 6 blockers), one because it is hand-written and one because it is a committed artifact with no freshness check. `bundle_metadata.blockers_open == 0` is load-bearing for `freshness_stale` under the late-absorption protocol, so a hand-written zero can clear a re-review obligation. Proposed: derive both from the graph (the `tables_fresh` pattern — regeneration must be a no-op), and never let a hand-editable field gate a protocol.

**Also, smaller and cross-cutting:** (a) `formula_grounding` validates the `Lean:` references that exist and is blind to a function with none, so Invariant #4 is unenforced for any new formula that simply omits the tag (finding 2.5); (b) `count_literals` does not match spelled-out numerals, so hand-written tree counts in prose are unguarded across every bundle (finding 7.1, now on its second drift in D12); (c) ReviewFinding severity depends on incidental markdown — `_BLOCKER_RE` requires an asterisk immediately preceding the token BLOCKER, so the mandated `### N.N — 🔴 BLOCKER —` heading form does not match it, while one bold-emphasised occurrence of that token anywhere in a file escalates *every* finding in that file to `critical`, so a report that merely *discusses* the regex mis-severities itself (this one did, until the quotation was paraphrased); (d) D12 has 205 registry parameters available to it and declares zero, so Gate 3 and Gate 9 both pass vacuously while sixteen distinct numerals sit in the prose (finding 2.4).
