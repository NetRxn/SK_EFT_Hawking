---
paper: D12
bundle_target: D12
reviewer: adversarial-reviewer
model: claude-opus-5
review_date: 2026-07-31T20:22:26Z
readiness_gates_version: 1
head_commit: 9d631fad
round: 10
---

# Adversarial Review — D12

## Summary

3 BLOCKER / 12 REQUIRED / 14 RECOMMENDED. Stages 1–12 are green on the tree I
reviewed (`validate.py` 57/57, archived `validation_20260731T195858Z.json`), so
this review is meaningful. Two of the three repairs I was asked to attack
hardest do not hold: the round-9 8.1 closure guard is **inert** — the line
immediately after it, `meta['status'] = ledger.get('status', status)`,
unconditionally overwrites the guard's verdict, so a two-key ledger record
closes a 🔴 BLOCKER and 264 of 287 blocking findings currently `fixed` are
closed by records that fail the guard's own stated bar; and
`review_docs_mint_findings`'s scoping exempts the exact directory
`BUNDLE_DIRECTORY_SCHEMA.md` names as the canonical home for a bundle Stage-13
review. A third BLOCKER is new and is about the paper, not the machinery: the
capstone `bolometer_error_floor` carries two load-bearing equational
identifications that appear nowhere in the draft, while §5 asserts the capstone
"is only as strong as those four identifications". The `prxquantum` correction,
the `extract_verifies_edges` alias fix and the `update_counts.py` idempotency
fix are all genuine and I re-derived each. **Your round-8 `freshness_stale`
reading was right and mine was wrong — I withdraw round-8/9 8.6 explicitly
below.** D12 is not submission-ready.

## Environment

`uv run python scripts/validate.py` → **57/57, 1114 warnings**, exit 0, 340 s,
archived to `docs/validation/reports/validation_20260731T195858Z.json`. Stage
13's precondition is met. The working tree at `9d631fad` carries 133 uncommitted
tracked modifications (132 regenerated `figures/*.png` at 09:54 plus
`lean/lean_deps.json.hash`); none was produced by anything I ran, and none is a
D12 artifact.

Compile gate (`uv run python scripts/compile_bundle_pdf.py D12`, never `pdflatex`
inside `papers/`): `OK pages=10 overfull=0 tex_errors=0 unresolved_refs_in_pdf=0
dropped_class_opts=0`. I judged it from the rendered PDF, not the log — page 1
renders the title, author, abstract and §I correctly, cross-references resolve,
the `\thm{}` macro breaks long Lean names at underscores as intended.

## Findings

### 1.1 — 🟡 REQUIRED — `IrwinHilton2005` is still trusted unqualified for the loop-gain convention while the paper *and* the source's own verification record disclose it as unread (round-9 1.1, fourth consecutive round)

- **Gate:** CitationIntegrity
- **Location:** `papers/D12/paper_draft.tex:364-365` vs `:435-437`; `Lit-Search/Phase-6E/primary-sources/IrwinHilton2005.abstract.txt`
- **Observed:** `:364-365` — "The loop gain \thm{loopGain} is signed, and \thm{loopGain\_eq\_irwinHilton} proves it equals the instrument-literature convention~\cite{IrwinHilton2005,Irwin1995}", no qualifier. `:435-437` — "We attach~\cite{IrwinHilton2005} as the standard review in which this layer is presented, but we have not inspected its text: we hold that source only as a resolved DOI record, so the attribution of this specific closed form to it should be read as provisional."
- **Evidence:** `git diff ac66444f..HEAD -- papers/D12/paper_draft.tex` touches **one** hunk, the class-option comment at `:1-10`. Neither line moved. The primary-source cache is unambiguous about the strength of the check: "DOI resolved (302 -> link.springer.com/10.1007/10933596_3). This file is a verification record, not a reproduction of the source text… Where the publisher landing page could not be read, that is said plainly" — and then, in its *Caveat / usage note*: "Source of the loop-gain convention L = P_J*alpha/(G*T) that 6EC proves equivalent to its own definition (`loopGain_eq_irwinHilton`)." So the project's own record asserts the loop-gain attribution from a source it records as unread. I re-resolved the DOI this round: `https://doi.org/10.1007/10933596_3` → 302 → `link.springer.com`, which confirms the DOI is live and Springer-hosted but nothing else; `link.springer.com` is outside this repo's egress whitelist, so the chapter title, author list, series volume and page remain unconfirmed against the publisher. `Irwin1995` is the stronger citation — its redirect slug (`pubs.aip.org/apl/article/66/15/1998/…/An-application-of-electrothermal-feedback-for-high`) independently confirms title, volume, issue and page.
- **Expected:** Extend the `:435` provisional qualifier to the loop-gain sentence, or narrow `:365` to `Irwin1995`.
- **Fix:** One clause at `:365`, mirrored into `CITATION_REGISTRY['IrwinHilton2005'].notes` and the primary-source caveat line.
- **Severity note:** raised from round-9's RECOMMENDED on the basis of `READINESS_GATES.md:44` — "All CitationIntegrity findings are `blocked` at submission time." A Gate-1 finding cannot be advisory at submission.
- **Cache:** fresh-fetch (DOI redirect re-resolved this round; record appended, `method: doi-redirect-partial`)
- **Lane:** `prose`

### 1.2 — 🔵 RECOMMENDED — `scripts/citation_cache.stats()` still raises on the cache it summarizes (round-9 1.2 REPRODUCES)

- **Gate:** CitationIntegrity
- **Location:** `scripts/citation_cache.py` (`is_stale` / `stats`)
- **Observed:** `from scripts.citation_cache import stats; stats()` → `TypeError: can't subtract offset-naive and offset-aware datetimes`. Re-run this round; unchanged.
- **Evidence:** `is_stale` catches `KeyError` and `ValueError`; the failure is a `TypeError` at `now - verified`, from records whose `verified_date` is a bare `"YYYY-MM-DD"` string. No D12 bibkey is among them — all 14 D12 lookups resolved cleanly — but the summary entry point the CLI and dashboard call is dead for every caller, which is also why nobody has noticed the `arxiv_verified` gap in 1.3.
- **Expected:** `except (KeyError, ValueError, TypeError)`, or normalise naive timestamps to UTC on read.
- **Fix:** One `except` clause.
- **Cache:** n/a

### 1.3 — 🟡 REQUIRED — none of D12's six arXiv-bearing registry entries carries `arxiv_verified`, so Gate 1's documented pass criterion cannot be met (round-9 1.3, fourth round)

- **Gate:** CitationIntegrity
- **Location:** `src/core/citations.py`; `docs/READINESS_GATES.md:33,40`
- **Observed:** Gate 1 "Passes iff … Every registry entry has `arxiv_verified == True` AND `doi_verified == True` (where applicable)". Measured this round: `arxiv_verified` is set on **8 of 652** entries; **369** entries carry an arXiv ID and **8** of those are verified. All six D12 arXiv entries — `MeiburgLessaSoldati2025`, `coqq2022`, `UnruhCBO2025`, `EchenimMhalla2021`, `RashidHasan2017`, `Degenne2025Kernels` — have no such key at all.
- **Evidence:** `_eval_citation_integrity` implements a different, weaker predicate and D12's gate reads `passed — all bibkeys registered`. The consequence is concrete: the five fresh verification records I appended this round land in `docs/citation_verifications.jsonl` and in nothing the gate reads. Two canonical documents disagree about what Gate 1 is.
- **Expected:** Either set `arxiv_verified` on the `promote_primary_sources.py` write path (and have the Stage-13 cache append feed it), or amend `READINESS_GATES.md` Gate 1 to state the deferral the evaluator actually implements.
- **Fix:** One writer change or one doc paragraph.
- **Severity note:** raised from RECOMMENDED per `READINESS_GATES.md:44`, same basis as 1.1.
- **Cache:** fresh-fetch
- **Lane:** `infra`

*(All 14 bibitems resolved. All 14 are in `CITATION_REGISTRY`, all 14 are cited, none is uncited, and all 14 hit `docs/citation_verifications.jsonl` at the **recomputed current** `bibitem_hash` with verdict `match` and `is_stale == False` — I recomputed every hash rather than trusting the key. I did not stop there. Fresh independent fetches this round, chosen as the two arXiv entries no prior round had fetched plus the two DOIs the physics turns on: `2103.08535` → "Quantum projective measurements and the CHSH inequality in Isabelle/HOL", Echenim & Mhalla, 15 Mar 2021 — **match**; `1705.10050` → "Formalization of Transform Methods using HOL Light", Rashid & Hasan, 29 May 2017 — **match**; `10.1364/AO.21.001125` → 302 → `opg.optica.org/abstract.cfm?URI=ao-21-6-1125`, whose slug confirms Applied Optics vol. 21, issue 6, page 1125 exactly as the bibitem states — **match, partial** (landing page not readable under the egress policy); `10.1007/10933596_3` → 302 → `link.springer.com` — **match, partial** (see 1.1). Plus `2512.05878` re-fetched for the author-order adjudication in 4.1. Five records appended under reviewer `adversarial-reviewer-round10`. Unchanged nit, still not raised as a finding: `coqq2022` pairs year 2022 (arXiv) with DOI `10.1145/3571222` (POPL'23).)*

---

### 2.1 — 🟡 REQUIRED — `MATHER_1982_GRADIENT_REDUCTION` still has `human_verified_date: None`, and here is the precise answer to whether the gate should treat it differently: **no**

- **Gate:** ParameterProvenance
- **Location:** `src/core/provenance.py` (`PARAMETER_PROVENANCE['MATHER_1982_GRADIENT_REDUCTION']`); graph node `paper:D12`
- **Observed:** The edge half is done and committed, and I verified it end-to-end rather than reading the diff: `PAPER_DEPENDENCIES['D12']['parameters'] == ['MATHER_1982_GRADIENT_REDUCTION']`, the live graph carries `paper:D12 --DEPENDS_ON--> param:MATHER_1982_GRADIENT_REDUCTION` (`D12 OUT: {'CLAIMS': 1, 'DEPENDS_ON': 1, 'HAS_FIGURE': 3, 'CITES_SOURCE': 14}`), and Gate 3 reads `blocked — 1 parameters lack human_verified_date`. `llm_verified_date = '2026-07-31'`; `human_verified_date = None`.
- **Evidence:** You asked whether the gate should treat this differently, since you will not forge a human sign-off. **It should not, and the current behaviour is exactly right.** `READINESS_GATES.md:69` makes `human_verified_date` a pass condition and `CLAUDE.md` invariant 8 states the division: "LLM verification unblocks computation; human verification unblocks paper submission." The gate is doing its job — it is reporting an outstanding *operator* action, not a code defect, and a gate that reports an outstanding operator action as `blocked` is a correct gate. The only changes I would accept are cosmetic and neither weakens it: (a) `GateResult.notes` could distinguish `blocked-pending-human-signoff` from `blocked-on-artifact-defect` so the dashboard can sort operator work from author work; (b) `PARAMETER_PROVENANCE` could carry `human_verification_requested_date` so the age of the request is visible. What must **not** happen is an auto-verify path, a `tier == 'LITERATURE'` exemption, or a reviewer-set `human_verified_date` — any of those turns the one gate that requires a human into a gate that requires nobody. This finding therefore stays open until you sign it off through the provenance dashboard, and I will keep re-filing it; that is the intended cost.
- **Expected:** One dashboard sign-off against `Lit-Search/Phase-6E/primary-sources/Mather1982.abstract.txt` (which records the abstract verbatim, the "as much as 30 %" wording, and the explicit note that the *body* has not been read).
- **Fix:** Operator action, not an author action.
- **Cache:** n/a
- **Lane:** `prose`

### 2.2 — 🟡 REQUIRED — the `extract_verifies_edges` fix is real, but Gate 4 now passes D12 on a hand-curated **4 of 27** formula list, and the 23 it excludes contain the one formula that would flip the gate to `blocked`

- **Gate:** ComputationCorrectness
- **Location:** `scripts/readiness_gates.py:257-300` (`_eval_computation_correctness`); `src/core/provenance.py` `PAPER_DEPENDENCIES['D12']['formulas']`; `src/core/formulas.py:11283`; `tests/test_bundle_formulas_d11_d12.py:310-316`
- **Observed:** Gate 4 reads `passed — all 4 grounded formulas have substantive tests`. That statement is true and I verified the four independently — `phonon_psd_gradient_factor`, `phonon_psd_gamma`, `f_link_bath_referred`, `assignment_fidelity` all carry `golden`/`identity` edges backed by real assertions (the γ(2,4) = 0.4731 pin at `abs=5e-4`, the isothermal `== 1.0`, the bath-referred identity checked against an **independent implementation** over a 28-point grid at `rel=1e-12`, the fidelity complement at `abs=1e-12`). The alias allow-list manufactures no false coverage today: every `F` and `formulas` binding under `tests/` is `from src.core import formulas [as F]`, in exactly four modules, verified by grep.
- **Evidence:** The problem is upstream of the fix. `_eval_computation_correctness` derives its formula set from `CLAIMS→GROUNDED_IN` plus `paper.meta['formulas']`. **D12 has zero `GROUNDED_IN` edges** (`D12 OUT` above), so the entire gate runs on the hand-written 4-element `PAPER_DEPENDENCIES['D12']['formulas']` list. The D12 region of `formulas.py` (`poisson_avg_error_floor` … `bloch_siegert_scale`) contains **27** functions. Measured coverage across all 27 from the live graph:
  ```
  strong (golden/identity/roundtrip) : 26
  bounds/unknown only                :  1   gaussian_tail_birnbaum_lower  ['bounds']
  no coverage                        :  0
  ```
  `gaussian_tail_birnbaum_lower` is the single exception, and it is not an obscure one — it is the Birnbaum–Feller lower bound the paper calls its own contribution ("has no Mathlib counterpart, and it is what makes the ceilings of Section~\ref{sec:control} bite", `:596-599`), and every one of the paper's `76\,\%`, `95\,\%`, `50\,\%`, `80\,\%` figures at `:563-566` is a ratio computed from it. Its only test is `assert F.gaussian_tail_birnbaum_lower(z) <= q + 1e-15` — an inequality against the true tail, with no golden value pinned. A constant that made the bound *smaller* would still pass. That is precisely the failure mode `READINESS_GATES.md:80` says Gate 4 exists to catch. Under the gate's own blocking rule ("a grounded formula whose VERIFIES edges are all `test_kind ∈ {bounds, unknown}`"), a complete formula list flips D12's Gate 4 to `blocked`.
- **Expected:** Either derive Gate 4's formula set from the paper (the `Sentence`/`ProseClaim` layer already parses the draft) rather than from a hand-written list the author curates, or — cheaper and sufficient now — extend `PAPER_DEPENDENCIES['D12']['formulas']` to the full 27 and pin a golden value for `gaussian_tail_birnbaum_lower` (e.g. `φ(1)·1/2 = 0.12098536225957168`, `φ(2)·2/5 = 0.021596386605275228`). Both numbers are already implicit in the paper's own 76 %/95 % sentence.
- **Fix:** One list extension plus two `pytest.approx` assertions; or one evaluator change.
- **Cache:** n/a
- **Lane:** `infra`

### 2.3 — 🔵 RECOMMENDED — "attains every PSD reduction between 0 and 56 %" states an unattained supremum as attained, in three artifacts (round-9 2.3 REPRODUCES)

- **Gate:** ParameterProvenance
- **Location:** `papers/D12/paper_draft.tex:448-451`; `src/core/formulas.py:11543-11545`; `Lit-Search/Phase-6E/primary-sources/Mather1982.abstract.txt`
- **Observed:** Unchanged: "the largest amplitude reduction the closed form **admits** is $1 - \sqrt{n/(2n+1)} = 33\,\%$" and "the closed form **attains** \emph{every} PSD reduction between $0$ and $56\,\%$".
- **Evidence:** I recomputed rather than inheriting. $\gamma(r,n)$ decreases strictly in $r$ with $\lim_{r\to\infty}\gamma = n/(2n+1) = 4/9$, so $1-4/9 = 55.5556\,\%$ is a supremum approached and never attained; likewise $1-\sqrt{4/9} = 33.333\,\%$. The project's own test is the only artifact that says so, in terms: "the maxima, which are SUPREMA -- approached as r -> infinity, never attained" (`tests/test_bundle_formulas_d11_d12.py:493-494`), and it asserts strictness at $r=10$ for $n \in \{3,4,5\}$. Reporting $55.56$ as $56$ additionally claims attainment on $(55.56\,\%,\,56\,\%]$, which is empty.
- **Expected:** "attains every PSD reduction strictly below $1-n/(2n+1) = 55.6\,\%$"; "the supremum of the amplitude reduction is $33\,\%$".
- **Fix:** One clause at `:449`, mirrored into `formulas.py` and the primary-source note.
- **Cache:** n/a

### 2.4 — 🔵 RECOMMENDED — the $r^{-2}$ in $r^{-(n+1)}$ is still attributed to $G$, which supplies only $r^{-(n-1)}$ (round-9 2.4 REPRODUCES)

- **Gate:** ParameterProvenance
- **Location:** `papers/D12/paper_draft.tex:428-431`; `src/core/formulas.py:11525-11529`
- **Observed:** Unchanged: "the physical re-referencing of $G$ via $G \propto T^{n-1}$ --- the two differ only in bookkeeping and yield the same expression, since $r^{-(n-1)}\cdot r^{-2} = r^{-(n+1)}$."
- **Evidence:** With $F(t,n)=\frac{n}{2n+1}\frac{t^{2n+1}-1}{t^{n}-1}$ and $t=1/r$, $F(1/r,n)=\frac{n}{2n+1}\frac{r^{2n+1}-1}{r^{n}-1}\,r^{-(n+1)}$ exactly. The second reading needs **two** re-referencing factors, and the project's own primary-source cache names both: "Using G ∝ T^(n-1), G(T_s) = G(T_b) · r^-(n-1) **and T_s² = T_b² · r^-2**". The paper and `formulas.py` name only the $G$ half and then produce $r^{-2}$ from nowhere.
- **Expected:** "the re-referencing of $T^2$ and $G$ from the bath to the bolometer, via $T_s^2 = T_b^2 r^{-2}$ and $G \propto T^{n-1}$".
- **Fix:** One clause at `:430`, mirrored into `formulas.py`.
- **Cache:** n/a

### 2.5 — 🔵 RECOMMENDED — neither γ function carries a `Lean:` reference and `formula_grounding` is structurally blind to a function with none (round-9 2.5 REPRODUCES, fourth round)

- **Gate:** ComputationCorrectness
- **Location:** `src/core/formulas.py:11497-11556`; `scripts/validate.py::check_formula_grounding`
- **Observed:** `f_link_bath_referred` (`:11497`) carries no reference line at all; `phonon_psd_gradient_factor` (`:11518`) offers `Provenance: Mather (1982)…` and no `Lean:`. Pipeline Invariant #4 and the Stage-2 gate both require every function to reference a Lean theorem.
- **Evidence:** `check_formula_grounding` iterates the references that *exist*, so a function with zero references contributes zero entries and is silently clean. This is the third mechanism in a row that measures only what is declared: 2.2's Gate 4 measures a declared formula list, this measures declared references. The paper does **not** overclaim here — `:433-434` says the identity is verified "in `tests/test\_bundle\_formulas\_d11\_d12.py`" and never that it is kernel-checked.
- **Expected:** `Lean: phononPSDGamma, phononPSD_eq_phononPSDGamma_one` on the first; an explicit `Lean: none — literature-quoted closed form, not formalized` on the second; plus a `formula_grounding` sub-check that every public function carries ≥1 reference line.
- **Fix:** Two docstring lines plus one sub-check.
- **Cache:** n/a

### 2.6 — 🔵 RECOMMENDED — the `_FORMULA_MODULE_ALIASES` allow-list is global and never validated against the importing module

- **Gate:** ComputationCorrectness
- **Location:** `scripts/build_graph.py:3509` and `:3526-3531`
- **Observed:** `_FORMULA_MODULE_ALIASES = {"F", "formulas", "src.core.formulas"}` is consulted for every `PythonTest` node regardless of what that module actually bound the name to.
- **Evidence:** The allow-list is the right call over a blanket tail fallback and I verified it manufactures no false coverage **today**: `tests/test_bundle_formulas_d11_d12.py` and `tests/test_quantum_network.py` bind `F` and only to `src.core.formulas`; `tests/test_nonlinear_efe.py` and `tests/test_cosmological_perturbations.py` bind `formulas` and only to `src.core.formulas`. Four modules, four correct bindings. But the guard is a name convention, not a resolution: a future `import numpy.fft as F` or `from mypkg import formulas` in any test file silently manufactures coverage for every formula whose bare name collides, and Gate 4 is the consumer. The extractor already reads each test module's source to build `referenced_names`; resolving the alias from that module's own `import` lines costs one pass and removes the assumption.
- **Expected:** Per-module alias resolution; fail closed (no edge) when the alias does not resolve to `src.core.formulas`.
- **Fix:** One import-scan in `extract_python_test_nodes`, one dict lookup here.
- **Cache:** n/a

---

### 3.1 — 🔵 RECOMMENDED — `correlated_pair_sigma_eq_sqrt_two_mul` REPRODUCES a fifth time: bare real-√ algebra described as quantifying a witness pair

- **Gate:** LeanProofSubstance
- **Location:** `papers/D12/paper_draft.tex:320-322`; `lean/SKEFTHawking/Detection/NEPAlgebra.lean:470-474`
- **Observed:** Both artifacts byte-identical to rounds 6–9. Paper: "…exhibits a fully-correlated pair whose total power is twice what quadrature predicts, with the discrepancy quantified exactly as $\sqrt{2}$ by \thm{correlated\_pair\_sigma\_eq\_sqrt\_two\_mul}." Theorem: `theorem correlated_pair_sigma_eq_sqrt_two_mul (S : ℝ) : √(4 * S) = √2 * √(S + S)`, discharged by two `show … by ring` rewrites plus `Real.sqrt_mul`.
- **Evidence:** The statement mentions neither `IsUncorrelatedAt`, nor the witness functionals of `quadrature_uncorrelated_hypothesis_load_bearing`, nor `sigma_eq_responsivity_nep_sqrt_enbw`. It is unconditional in `S` — including `S < 0`, where both sides are Lean's junk `0` — so it survives any edit to the witness. The witness itself is substantive (I read it: it instantiates `h ≡ 1` and derives a contradiction by `linarith`); what is missing is the link, and the verb "quantified" asserts the link exists. This is the project's own P3/P5 anti-pattern applied to prose rather than to a statement.
- **Expected:** "the corresponding output-noise ratio is $\sqrt2$, recorded arithmetically as \thm{…}", or a one-line corollary instantiating the identity at the witness's own `V`/`Vtot`.
- **Fix:** One clause at `:321-322`.
- **Cache:** n/a

### 3.2 — 🔵 RECOMMENDED — the Lean now discloses that `enbw_dcGain_hypothesis_load_bearing` runs through the `x/0 = 0` junk value; the paper still does not (round-9 3.2 REPRODUCES)

- **Gate:** LeanProofSubstance
- **Location:** `papers/D12/paper_draft.tex:285-286`; `lean/SKEFTHawking/Detection/FilterFloors.lean:471-497`
- **Observed:** Paper: "The hypothesis that DC gain is nonvanishing is non-droppable, witnessed by \thm{enbw\_dcGain\_hypothesis\_load\_bearing}."
- **Evidence:** I read the proof. The witness is $h(y)=y-1$ on $[0,2]$; the proof establishes $\int_0^2 (y-1) = 0$ and closes through `enbw_of_dcGain_eq_zero`, i.e. `enbw h 2 = 0` and `0 * 2 < 1/2`. As a claim about the *formal* theorem this is correct and I am not calling it unsound — dropping `hDC` does make the Lean statement false. The Lean file now discloses the reading explicitly ("Physically this is not a Lean artefact: a zero-DC-gain filter rejects the signal entirely, so there is no 'bandwidth per unit gain' to bound", `:482-483`) and the `enbw` docstring flags the degenerate branch at `:121-123`. The paper carries neither sentence, so the one witness in the draft whose refutation runs through a division convention is also the one the draft says least about.
- **Expected:** One parenthetical at `:286` importing the Lean's own sentence.
- **Fix:** One parenthetical.
- **Cache:** n/a

*(No other class-3 findings, and I re-derived the substrate rather than inheriting. All **181** `\thm{}`/`\texttt{}` identifiers in the draft were resolved against the live graph's Lean index: **160 resolve to project declarations**; the 21 that do not are Mathlib names (`HasSubgaussianMGF.measure_ge_le`, `bayesRisk_le_bayesRisk_comp`, `cdf`, `gaussianReal`, `tsum`), the three core axioms, literal words (`sorry`, `native_decide`, `maxHeartbeats`, `TODO`, `infotheo`), paths and version strings. **Zero** intersection with the 576-name `PlaceholderMarker` set. **Zero** occurrences of any of the 13 stale names the anchor list enumerates. Substrate re-verified from a fresh `lean_deps.json` read (the hash has moved since round 9), not by sampling: **582** declarations across the 13 root-imported modules; `axiom_deps_project` **empty for every one**; `axiom_deps_core` is `{Classical.choice, Quot.sound, propext}` for 578 and `{propext}` for 4; **zero** `sorry`. A body sweep of all **427** `theorem`/`lemma` declarations in those modules found **zero** with a ≤3-token proof. The abstract's PhysLib claim is exactly true and I checked it independently: **zero** of the 582 declaration records mention `Physlib` or `QuantumInfo`, while **47** consume `SKEFTHawking.QuantumNetwork.*`. I also verified the paper's PhysLib claim at `:229-233` against the pinned checkout and it is **correct in every particular** — `lean/.lake/packages/Physlib/QuantumInfo/States/Mixed/Fidelity.lean:27` does say "the quantum version of the Bhattacharyya coefficient", and `:136-139` is an open TODO list containing "Matches with classical (squared) Bhattacharyya coefficient". The anchor list's pointer to `Distance/Fidelity.lean` is the stale one, not the paper's.)*

---

### 4.1 — 🟡 REQUIRED — the same primary source is rendered with **reversed author order** in D10 and correctly in D12, at source, not just in the `.bbl`; and the duplicate-key population has grown to 32

- **Gate:** CrossPaperConsistency
- **Location:** `papers/D10/bibliography.bib:49-56` and `papers/D10/paper_draft.bbl:188-192`; `papers/D12/paper_draft.tex:741-742`; `src/core/citations.py`
- **Observed:**
  ```
  papers/D10/bibliography.bib:50   author = {Caballero, Jose Manuel Rodriguez and Unruh, Dominique}
  papers/D12/paper_draft.tex:741   D.~Unruh and J.~M.~Rodr\'iguez~Caballero
  arXiv:2512.05878 (fetched this round)  1. Dominique Unruh  2. José Manuel Rodríguez Caballero
  ```
  D12 is right; D10 is wrong, and it is wrong in the `.bib` source, so re-rendering the `.bbl` will not fix it.
- **Evidence:** I re-fetched `https://arxiv.org/abs/2512.05878` this round specifically to adjudicate the order rather than inherit round 9's fetch: "Authors (in order given): 1. Dominique Unruh 2. José Manuel Rodríguez Caballero", submitted 5 Dec 2025. Under this review's own class-1 taxonomy that is a `wrong_author` verdict on `isabelleCBO`. Gate 2's literal blocking condition ("a bibkey whose `arxiv_id` differs across papers") still does not fire, because the *keys* differ rather than the IDs — I confirmed no bibkey carries two different arXiv IDs across any bundle draft. The duplicate-key population has grown since round 9: **32** normalized titles are now carried under ≥2 registry keys (30 last round). The three that touch D12:
  ```
  "A Formalization of the Generalized Quantum Stein's Lemma in Lean"
     Meiburg2025PhysLib       arxiv=2510.08672  used_in=[D8]
     MeiburgLessaSoldati2025  arxiv=2510.08672  used_in=[D12]
     leanQI2025               arxiv=None        used_in=[D10]
  "Complex Bounded Operators in Isabelle/HOL"
     UnruhCBO2025             arxiv=2512.05878  used_in=[D12]
     isabelleCBO              arxiv=None        used_in=[D10]
  "Markov kernels in Mathlib's probability library"
     Degenne2025MarkovKernels arxiv=2510.04070  used_in=[I3]
     Degenne2025Kernels       arxiv=2510.04070  used_in=[D12]
  ```
  Both D10 keys carry `arxiv: None` in the registry while `papers/D10/bibliography.bib` carries the eprint IDs — the registry is strictly less informative than the artifact it governs.
- **Expected:** Correct `papers/D10/bibliography.bib:50` to `{Unruh, Dominique and Rodriguez Caballero, Jose Manuel}`; alias `leanQI2025 → MeiburgLessaSoldati2025` and `isabelleCBO → UnruhCBO2025`; add a duplicate-normalised-title/DOI/arXiv detector to `validate.py` so the population cannot keep growing silently.
- **Fix:** One `.bib` line, two registry aliases, one detector.
- **Cache:** fresh-fetch (`UnruhCBO2025`, record appended)
- **Lane:** `prose`

*(No cross-paper contradictions. I re-walked the seams the anchor list names: D9↔D12 — `quarter_sqrtFidelity_sq_le_povmAvgError` and `IsBinaryPOVM` appear in no draft but D12's and live under `SKEFTHawking/QuantumNetwork/`, i.e. project-side, exactly as `:238-241` says, and the 47 consuming declarations confirm it; D1↔D12 — `johnsonNyquistPSD` is D12-side only. The paper's "two open prior-art checks" count is consistent across the abstract `:65`, §5 `:633` and the conclusion `:691`, and matches `prior_art_novelty.md §3`.)*

---

### 5.1 — 🟡 REQUIRED — the ENBW numerical contradiction still stands: the paper's parenthetical disambiguates the **symbols** and the reader's problem is the **functionals** (round-9 5.1 residual, RESTATED PRECISELY as requested)

- **Gate:** NarrativeGrounding
- **Location:** `papers/D12/paper_draft.tex:292-304`
- **Observed:** The constant is right at all four sites — `:50` (abstract), `:268` (§3.1 headline), `:300` (this parenthetical), `:675` (conclusion) all say $1/2$, matching `FilterFloors.lean:197-201` (`1 / 2 ≤ enbw h T * T`); the draft's only `1/4` is the Le Cam prefactor at `:139`, which is correct. What remains is the sentence's content. The paper quotes the instrument convention "$\mathrm{ENBW} = 1/(4\tau)$ for a single-pole lock-in filter of time constant $\tau$", then resolves the apparent conflict entirely by distinguishing the *letters*: "(The time constant is written $\tau$, not $T$: thirty lines above, $T$ is the observation window … and the two are different quantities. Reusing the symbol would read as though the theorem were the convention…)".
- **Evidence:** Distinguishing $\tau$ from $T$ does not remove the contradiction, because $T$ is a free parameter and a reader may legitimately set $T = \tau$. Doing so gives instrument-$\mathrm{ENBW}\cdot T = 1/4 < 1/2$ and the theorem reads false. The actual resolution is that these are two different functionals, not one functional at two arguments. `FilterFloors.lean:124-125` defines
  ```lean
  noncomputable def enbw (h : ℝ → ℝ) (T : ℝ) : ℝ :=
    (∫ x in (0:ℝ)..T, h x ^ 2) / (2 * (∫ x in (0:ℝ)..T, h x) ^ 2)
  ```
  — a **time-domain ratio of the impulse response over the same finite window `T` that multiplies it**. The instrument's $1/(4\tau)$ is a **frequency-domain** noise bandwidth of an **infinite-support** single-pole response over the whole line. I evaluated the paper's own `enbw` on the single-pole response $h(x)=e^{-x/\tau}$ over $[0,T]$ and it closes in one line: with $u = T/\tau$,
  $$\mathrm{enbw}(h,T)\cdot T \;=\; \frac{u}{4}\cdot\frac{1+e^{-u}}{1-e^{-u}},$$
  which is monotone increasing with limit $1/2$ as $u\to 0^+$ — so the single-pole filter satisfies the theorem at **every** window and never approaches $1/4$:
  ```
  T/tau =  0.01   enbw(h,T)*T = 0.50000
  T/tau =  1.00   enbw(h,T)*T = 0.54099
  T/tau =  5.00   enbw(h,T)*T = 1.26696
  T/tau = 20.00   enbw(h,T)*T = 5.00000
  ```
  That computation is the disambiguation, and it is one sentence long.
- **Expected:** Replace the symbol paragraph with the functional one, e.g. "…and the two are different *functionals*, not one functional at two arguments: \thm{enbw} is a time-domain ratio over the same finite window that multiplies it, whereas $1/(4\tau)$ is a frequency-domain bandwidth of an infinite-support response. Evaluating \thm{enbw} on a single-pole response over $[0,T]$ gives $\tfrac{u}{4}\tfrac{1+e^{-u}}{1-e^{-u}} \ge \tfrac12$ at every $T$, with $u = T/\tau$."
- **Fix:** One sentence at `:298-303`, replacing the current parenthetical rather than adding to it.
- **Cache:** n/a

### 5.2 — 🟡 REQUIRED — the paragraph that declares the highest prior-art risk still withholds the name and the `\cite`, **and** describes the project using the exact characterization this project retracted as unverified

- **Gate:** NarrativeGrounding
- **Location:** `papers/D12/paper_draft.tex:633-636`; cf. `:605`, `:691`; `papers/D12/prior_art_novelty.md §3`; `Lit-Search/Phase-6E/primary-sources/LuccioliDegenne2024.abstract.txt`
- **Observed:** "First, an out-of-Mathlib Lean project **on information theory and hypothesis testing** is under active development; a Bayes-binary-risk--divergence lower bound there would be substantially our Le Cam floor, and it is the highest prior-art risk in this work." No name, no `\cite`.
- **Evidence:** Two defects in one sentence.
  1. **The withheld citation (round-9 5.5 reproduces).** §5 names and cites the project two paragraphs earlier (`:605`, `\cite{LuccioliDegenne2024,Degenne2025Kernels}`) and the conclusion names it again (`:691`). Only the paragraph that *declares the obligation* withholds it, so a reader acting on that paragraph alone cannot discharge it.
  2. **New, and worse.** "on information theory and hypothesis testing" is the retracted subtitle. `prior_art_novelty.md §3` records the retraction in terms: "⚠️ This entry previously gave the title as 'Information theory and hypothesis testing, in Lean'. That subtitle came from a web-search snippet, not from any source this project read, and this line was the origin from which it propagated into the registry, the bibitem and the cache. Corrected 2026-07-31 after a D12 Stage-13 BLOCKER; the only verified title is the bare token." The primary-source cache repeats the warning: "Do not reintroduce a subtitle without a verified primary." The retraction was applied to the registry title, the bibitem and the cache — and **not** to the prose that seeded them, where the same words now stand as a factual description of a third party's work. The only statement this project has verified about the project is the `Degenne2025Kernels` quotation, which supports "developed Mathlib's KL divergence", "data-processing inequality for f-divergences" and "the chain rule for KL", and says nothing about hypothesis testing.
- **Expected:** "First, \textsc{TestingLowerBounds}~\cite{LuccioliDegenne2024} — the out-of-Mathlib Lean project in which Mathlib's Kullback--Leibler divergence and a general $f$-divergence data-processing inequality were developed~\cite{Degenne2025Kernels} — is under active development; …". That names it, cites it, and asserts only what a whitelisted primary states.
- **Fix:** One sentence at `:633`.
- **Cache:** n/a
- **Lane:** `prose`

### 5.3 — 🔵 RECOMMENDED — the conclusion's composite-bite sentence still never received the §4 qualification (round-9 5.3 REPRODUCES, fourth round)

- **Gate:** NarrativeGrounding
- **Location:** `papers/D12/paper_draft.tex:682-686`; cf. `:569-575`
- **Observed:** "Two of those composites are shown to bite at a \emph{non-degenerate} operating point --- matched budget $2$, ceiling $22/25$ … --- and not merely at the signal-free zero-budget point, which is what the Birnbaum--Feller lower bound buys."
- **Evidence:** Byte-identical across `b6830c7d`, `ad844e42`, `c60e83ad`, `837f335e`, `ac66444f` and HEAD. It is not false — I read both proofs and they do conclude `assignmentFidelity e0 e1 ≤ 22/25` at a signal-carrying point with the full composite binder list. What it omits is what §4 `:569-575` and the Lean's own header note (`CompositeReadoutCeilings.lean:669-683`) both spend several lines saying: each proof `lower-bounds the max by its detection branch and discards the relaxation term`, so `22/25` is the single-mechanism value and holds uniformly in `t` and `T1`. A referee reading only the conclusion takes "the composites bite" to mean the composite *bound* is what bites, which §4 explicitly disclaims.
- **Expected:** One clause: "…, though the numerical value is the single-mechanism one (§\ref{sec:control})".
- **Fix:** One clause at `:684`.
- **Cache:** n/a

### 5.4 — 🔵 RECOMMENDED — the $f$-divergence generator and "the affinity is its complement" still disagree by a factor 2, in three artifacts (round-9 5.4 REPRODUCES)

- **Gate:** NarrativeGrounding
- **Location:** `papers/D12/paper_draft.tex:245-247`; `Lit-Search/Phase-6E/primary-sources/LuccioliDegenne2024.abstract.txt`; `papers/D12/prior_art_novelty.md §3`
- **Observed:** Unchanged: "The squared Hellinger distance is an $f$-divergence with generator $f(t) = (\sqrt{t}-1)^2$, and the affinity is its complement".
- **Evidence:** With $f(t)=(\sqrt t-1)^2$, $D_f(P\|Q)=\sum_i q_i(\sqrt{p_i/q_i}-1)^2 = \sum_i(\sqrt{p_i}-\sqrt{q_i})^2 = 2-2\,\mathrm{BC}$, so the complement relation $H^2 = 1-\mathrm{BC}$ holds only for the $\tfrac12$-normalised squared Hellinger, generator $\tfrac12(\sqrt t-1)^2$. Both the primary-source cache and `prior_art_novelty.md` repeat the unnormalised generator alongside the complement relation. The paragraph's operative claim — that a general $f$-divergence DPI plausibly subsumes the affinity DPI — is **unaffected**, since BC is an affine decreasing function of $D_f$ under either normalisation, which is why this is advisory and not a substance defect.
- **Expected:** $f(t)=\tfrac12(\sqrt t-1)^2$, or "an affine function of" in place of "the complement of".
- **Fix:** One symbol at `:245`, mirrored into two artifacts.
- **Cache:** n/a

### 5.5 — 🔵 RECOMMENDED — the round-9 `prxquantum` correction is factually right, but it put eight lines of first-person internal-review narration at the top of the file a referee receives

- **Gate:** NarrativeGrounding
- **Location:** `papers/D12/paper_draft.tex:1-10`
- **Observed:** The submitted source now opens with: "⚠️ CORRECTION 2026-07-31 (D12 round-9): the first version of this note claimed the drop meant 'every page count and Overfull reading was against the wrong configuration'. **That was wrong, and I asserted it without compiling both.**"
- **Evidence:** I verified the substance and it is correct — `compile_bundle_pdf.py D12` returns `pages=10 overfull=0 dropped_class_opts=0`, and the same content already lives in `papers/D12/bundle_metadata.json` `notes` and in `scripts/compile_bundle_pdf.py`, i.e. in two places designed to hold it. The correction was the right call; the placement is not. `git diff ac66444f..HEAD -- papers/D12/paper_draft.tex` shows this comment block is the **only** change to the draft since round 8, so the file a journal receives grew a first-person account of the authors' review errors while every other round-9 paper-level finding went untouched. Journals receive the `.tex`.
- **Expected:** One factual line in the source — "% Class option `prx`: revtex4-2 has no `prxquantum` substyle and would drop it; `\rtx@apsprx` is a no-op so this is a correctness fix, not a layout change. Re-measure against the journal's own class before submission." — and the provenance narrative left in `bundle_metadata.json`, which already carries it verbatim.
- **Fix:** Delete seven lines from `:4-10`.
- **Cache:** n/a

### 5.6 — 🔵 RECOMMENDED — Fig. 1's caption quotes the crossover $N_a = 51.40$ without the $N_b$ it depends on, while the neighbouring panel establishes $N_b = 5$, where the crossover is $6.48$

- **Gate:** NarrativeGrounding
- **Location:** `papers/D12/paper_draft.tex:159-167`
- **Observed:** "(A) … certified at $148\times$ for $(N_b,N_a) = (5,10)$. **(B)** Fail-open as an average-error screen: beyond the crossover at $N_a = 51.40$ …"
- **Evidence:** The crossover solves $2\sqrt{N_b}(\sqrt{N_a}-\sqrt{N_b}) = \log 4$ and is therefore a function of $N_b$. At the panel-B value $N_b = 50$ it is $((\log 4)/(2\sqrt{50}) + \sqrt{50})^2 = 51.3959\ldots \to 51.40$ ✓; at the $N_b = 5$ the caption has just established for panel A it is $6.482$. The figure itself does state the parameter — `visualizations.py:14858-14860` sets the subplot titles "(A) false-strict miss bound, N_b = 5" / "(B) fail-open screen, N_b = 50" — so a reader looking at the plot can recover it, which is why this is advisory rather than required. The caption alone cannot be read correctly.
- **Expected:** "beyond the crossover at $N_a = 51.40$ (at $N_b = 50$)".
- **Fix:** Three words at `:162`.
- **Cache:** n/a

*(Every other class-5 check passes and I re-derived each numeral rather than inheriting. $Q(1)=0.15865525393145705$; $3/25 = 0.12$, i.e. $75.6\,\%$ of $Q(1)$ — and the paper's $76\,\%$ is the ratio of the **Birnbaum–Feller bound** $0.12098536$ to $Q(1)$, $= 76.26\,\%$ ✓, with $95\,\%$ at $z=2$ ($0.02159639/0.02275013 = 94.93\,\%$) ✓; the Mills fractions are exactly $z^2/(1+z^2)$, verified numerically as $0.5000$ and $0.8000$ ✓. $e^5 = 148.4131591$ ✓; the true ratio at $(50,60)$ is $3491.9554$ ✓; the Mills vacuity root is $0.37223890$ ✓; $x = 2Q(1)/(1-2Q(1)) = 0.4647947734915441$ and $x/(2(1+x))$ returns $Q(1)$ to 16 digits ✓; $\gamma(2,4)=0.4731481481$, $1-\gamma = 52.685\,\%$, $1-\sqrt\gamma = 31.214\,\%$, exact difference $21.471 \to 21.5$ ✓; $r^\* = 1.1913538$ for a 30 % PSD reduction ✓. **No unhedged first-claim exists**: every "first" in the draft either refutes one or is "first-class deliverable"; `:622-631` is scoped to "the ecosystems we actually assessed" and `:618` explicitly disclaims absence in HOL Light. I independently verified the paper's Mathlib claims against the pinned checkout rather than trusting the draft: `HasSubgaussianMGF.measure_ge_le` exists at `Mathlib/Probability/Moments/SubGaussian.lean:704` with docstring "Chernoff bound on the right tail of a sub-Gaussian random variable" and statement `μ.real {ω | ε ≤ X ω} ≤ exp (-ε ^ 2 / (2 * c))`, giving $\exp(-z^2/2)$ at $c=1$ against the paper's $\tfrac12\exp(-z^2/2)$ — the factor-two claim is exact ✓; `bayesRisk_le_bayesRisk_comp` exists at `Probability/Decision/Risk/Basic.lean:236` ✓; `cdf` at `Probability/CDF.lean:57` and `gaussianReal` at `Probability/Distributions/Gaussian/Real.lean:222` ✓. The **absence** claims also survive a hostile grep: `bayesBinaryRisk` 0 files, `Neyman` 0, `Le Cam` 0, `erf` 0; the "Hellinger" hits are Hellinger–Toeplitz in `InnerProductSpace`, the "Bhattacharyya" hit is an author name in `MeasureTheory/Measure/Tight.lean`, and the "f-divergence" hits are prose in the KL docstrings noting `klDiv` is an f-divergence for `klFun` — no general `fDiv` development exists at the pin.)*

---

### 6.1 — 🔴 BLOCKER — the capstone `bolometer_error_floor` carries two load-bearing **equational identifications** that appear nowhere in the draft, while §5 asserts it "is only as strong as those four identifications" and §1 asserts identifications "never enter our statements"

- **Gate:** AssumptionDisclosure
- **Location:** `papers/D12/paper_draft.tex:656-661`, `:86-88`, `:487-491`; `lean/SKEFTHawking/Electrothermal/BolometricFloors.lean:793-806`
- **Observed:** §5 names four modelling Props as the capstone's binders and then makes an explicit completeness claim: "Four deserve naming because they are the modelling binders of the capstone \thm{bolometer\_error\_floor} itself: \thm{IsThermalFluctuationLimited} …; \thm{IsWhiteFilteredVariance} …; \thm{IsUncorrelatedAt} …; and \thm{IsAdmissibleFilter}. **The capstone is only as strong as those four identifications.**" §1 states the discipline: "The \emph{identification} of any symbol with a physical instrument, of what counts as an absorbed photon or which plane a power is referred to, is the consuming application's declared hypothesis and **never enters our statements**."
- **Evidence:** The signature contradicts both sentences.
  ```lean
  theorem bolometer_error_floor (m : ETFModel) {kB T Tw : ℝ} {Vtot : (ℝ → ℝ) → ℝ}
      {V : Fin 2 → (ℝ → ℝ) → ℝ} {s hf : ℝ → ℝ} {μ₀ μ₁ σ t : ℝ}
      (hkB : 0 < kB) (hT : 0 < T) (hG : 0 < m.G)
      (hindep : IsUncorrelatedAt Finset.univ Vtot V)
      (hphonon : IsThermalFluctuationLimited (V 0) m kB T Tw)
      (hjohnson : IsWhiteFilteredVariance (V 1) (m.johnsonNEP kB T ^ 2) Tw)
      (hTw : 0 ≤ Tw) (hadm : IsAdmissibleFilter Tw s hf)
      (hs : IntervalIntegrable (fun x => s x ^ 2) volume 0 Tw)
      (hσ : 0 < σ) (hμle : μ₀ ≤ μ₁)
      (hμ : μ₁ - μ₀ = ∫ x in (0:ℝ)..Tw, hf x * s x)      -- ← undisclosed
      (hσV : σ = Real.sqrt (Vtot hf)) :                   -- ← undisclosed
      gaussianQ (matchedBudget (m.phononNEP kB T ^ 2 + m.johnsonNEP kB T ^ 2) Tw s / 2)
        ≤ avgAssignmentError (thrErr0 μ₀ σ t) (thrErr1 μ₁ σ t)
  ```
  `hμ` identifies the Gaussian mean separation with the matched-filter correlation of the template against the filter; `hσV` identifies the Gaussian σ with the square root of the total filtered variance. These are exactly "the identification of any symbol with a physical instrument" in the paper's own sense, and they are exactly what the paper elsewhere calls "three phases in one inequality" (`:489-491`) — they are the glue, not boilerplate. Both are load-bearing, which I checked by constructing falsifiers rather than assuming: drop `hμ` and take a zero template (so `matchedBudget = 0`, `gaussianQ(0) = 1/2`) with $\mu_1 \gg \mu_0$ and small σ (so the average error → 0), and the conclusion is false; drop `hσV` and take σ arbitrarily small while the true filtered variance stays large, and the conclusion is false again. I then grepped the entire draft for any statement of either identification — `identif|separation|\\mu` returns `:86` (the §1 discipline sentence), `:204-206` (the sharp floor's own $\Delta\mu/2\sigma$), `:211` (`thrErr*_eq_measure`), `:610` (`gaussianQ_eq_measure`) and `:661` (the completeness claim). Neither `hμ` nor `hσV` appears anywhere, under any paraphrase.
  This is `READINESS_GATES.md:122` verbatim — "an assumed hypothesis that is load-bearing for a cited result but does not appear in the paper" — with the aggravating factor that the paper does not merely omit them, it affirmatively claims the list is complete. A referee who opens `BolometricFloors.lean:793` sees six load-bearing hypotheses where §5 says four, and sees §1's discipline claim falsified by the paper's own capstone. Gate 6 currently reads `passed — no hypothesis dependencies`, which is vacuous: **zero** `ASSUMES` edges touch `paper:D12` (66 exist project-wide), so nothing mechanical can see this.
- **Expected:** Name both in §5 and drop the completeness claim, e.g.: "…and \thm{IsAdmissibleFilter}. Two further binders are identifications in the sense of §\ref{sec:intro} and are equally load-bearing: the capstone assumes the mean separation *is* the matched-filter correlation, $\mu_1-\mu_0 = \int_0^{T_w} h\,s$, and that the threshold noise scale *is* the total filtered standard deviation, $\sigma = \sqrt{V_{\rm tot}(h)}$. These are what carry the electrothermal PSD into the Gaussian floor; without them the two halves of the inequality are unrelated. §\ref{sec:intro}'s claim that identifications never enter our statements is therefore accurate for every theorem here except the capstone, where the composition requires them explicitly." Then §1 needs the matching carve-out.
- **Fix:** Two sentences in §5 plus one qualifying clause in §1. No Lean change.
- **Cache:** n/a

### 6.2 — 🔵 RECOMMENDED — `avgError_ge_gaussianQ_sharp` carries an undisclosed ordering binder $\mu_0 \le \mu_1$ (round-9 6.1 REPRODUCES)

- **Gate:** AssumptionDisclosure
- **Location:** `papers/D12/paper_draft.tex:204-208`; `lean/SKEFTHawking/Detection/GaussianThreshold.lean:785-788`
- **Observed:** "The headline is the sharp separation-budget floor \thm{avgError\_ge\_gaussianQ\_sharp}: no threshold placement achieves an average error below $Q\!\left(\Delta\mu/2\sigma\right)$." §2.3 nowhere states the branch-ordering convention.
- **Evidence:** `(hσ : 0 < σ) (hμ : μ₀ ≤ μ₁) (hz : (μ₁ - μ₀) / (2 * σ) ≤ z₀)`. `hσ` is a positivity condition a reader supplies; `hμ` is a sign convention on the separation, used directly (`hhnn : 0 ≤ (μ₁ - μ₀) / (2 * σ) := div_nonneg (by linarith) …`). Without it a signed $\Delta\mu$ with $\mu_1 < \mu_0$ makes $Q(\Delta\mu/2\sigma) > 1/2$ and the statement fails. Low severity because the convention is the obvious one and no claim weakens — but §5 enumerates twelve modelling Props by name while this binder of the section's own headline theorem goes unnamed.
- **Expected:** "for a signal branch of the larger mean ($\mu_0 \le \mu_1$, an explicit binder)".
- **Fix:** One parenthetical at `:205`.
- **Cache:** n/a

### 6.3 — 🔵 RECOMMENDED — "for every admissible filter" describes a theorem that does not take `IsAdmissibleFilter`, in a paper where that identifier is one of the twelve disclosed Props

- **Gate:** AssumptionDisclosure
- **Location:** `papers/D12/paper_draft.tex:267-269`; `lean/SKEFTHawking/Detection/FilterFloors.lean:197-201`; cf. `:646`
- **Observed:** "the central result is \thm{enbw\_mul\_window\_ge\_half}: for every **admissible filter** with nonvanishing DC gain, $\mathrm{ENBW}\cdot T \ge 1/2$."
- **Evidence:** The theorem's binders are `(h : ℝ → ℝ) (T : ℝ) (hT : 0 < T) (hint : IntervalIntegrable h volume 0 T) (hsq : IntervalIntegrable (fun x => h x ^ 2) volume 0 T) (hDC : (∫ x in (0:ℝ)..T, h x) ≠ 0)`. `IsAdmissibleFilter` is **not** among them — it is a `Prop` declared in a different module (`MatchedFilter.lean:136`), it is `\thm{}`-cited in this very paper at `:646` as one of the twelve disclosed modelling hypotheses, and the floor does not consume it. The direction of the error is conservative (the theorem is *more* general than the prose says), so nothing is overclaimed; the defect is that a reader mapping prose onto the disclosed-Prop list will attach the wrong hypothesis to the section's central result, and the two integrability conditions that *are* load-bearing are never mentioned.
- **Expected:** "for every filter integrable with its square on the window and with nonvanishing DC gain" — and note that `IsAdmissibleFilter` enters only at the matched-filter and capstone layers.
- **Fix:** One clause at `:268`.
- **Cache:** n/a

*(The rest of class 6 re-verified from signatures. The "Twelve propositions" enumeration at `:642-650` is exactly right — I enumerated every `Prop`-valued `def`/`abbrev`/`structure` across the 13 modules and got exactly the twelve the paper lists, with no thirteenth. The anchor list's "Tracked-Prop disclosure (11 of them)" is the stale artifact, not the paper: it omits `IsThermalFluctuationLimitedGamma`. `poisson_avgError_floor`'s sole hypothesis really is `hδ : IsCountRule δ`, so `:142` is exact. The composite-bite theorems inherit their composites' full binder lists including the attribution hypotheses `hattr0`/`hattr1`, which `:545` discloses.)*

---

### 7.1 — 🟡 REQUIRED — D12's entire numeric surface still sits outside every freshness and provenance gate (round-9 7.1 REPRODUCES)

- **Gate:** NumericalFreshness
- **Location:** `papers/D12/paper_draft.tex` throughout; `scripts/validate.py --check count_literals`, `--check numerical_literals`, `--check counts_fresh`
- **Observed:**
  ```
  count_literals      ✓ D12 — no count literals found
  numerical_literals  ✓ D12 — no inline unit-bearing literals in body
  counts_fresh        ✓ 1/1 checks passed
  ```
  while the draft carries at least `0.4731`, `53 %`, `31 %`, `52.7`, `31.2`, `21.5`, `22`, `33 %`, `56 %`, `4/9`, `0.444`, `1.19`, `30 %`, `22/25`, `3/25`, `0.15866`, `148`, `148.41`, `51.40`, `3491.96`, `76 %`, `95 %`, `50 %`, `80 %`, `0.37`, `0.4648`, `0.46479`, `9.5445`.
- **Evidence:** No `REPORTS` edge exists from `paper:D12` to any `CountMetric` (14 exist project-wide, none D12), `papers/D12/tables/` feeds no prose, and Gate 9 therefore passes on an empty set. The `DEPENDS_ON` edge added at round 9 reaches exactly one parameter and no derived value. Every numeral above is correct today — I recomputed all of them (see the class-5 note) — but rounds 6, 7 and 8 each found a wrong one in this unguarded surface (`23`, then the 21.5 derivation, then `0.4651`), and the only thing that caught each was a reviewer. Note 2.2 above compounds this: the one Python function whose value feeds four of these numerals is also the one with bounds-only test coverage.
- **Expected:** A `\pipelinevalue{}` chain or a `papers/D12/tables.py` spec for the derived quantities, so a wrong numeral fails a check; or at minimum register the derived values in `PARAMETER_PROVENANCE` as `DERIVED` with `DEPENDS_ON` edges, extending the mechanism round 9 built for one parameter.
- **Fix:** One tables spec plus a macro retrofit.
- **Cache:** n/a
- **Lane:** `prose`

### 7.2 — 🔵 RECOMMENDED — spelled-out tree counts REPRODUCE outside the macro chain; both correct today, both invisible to `count_literals`

- **Gate:** NumericalFreshness
- **Location:** `papers/D12/paper_draft.tex:103` ("Thirteen root-imported modules"), `:642` ("Twelve propositions carry modelling or admissibility content")
- **Observed:** Both unchanged; both hand-written counts of tree contents rather than `\input{counts.tex}` macros.
- **Evidence:** Both verified correct at review time — `lean/SKEFTHawking.lean` root-imports exactly 13 `Detection.*`/`Electrothermal.*`/`Control.*` modules (6 + 3 + 4), and exactly 12 `Prop`-valued declarations exist across them. `count_literals` reports "no count literals found" because the detector does not match spelled-out numerals, so the class is unguarded across every bundle. `bundle_metadata.json` correctly no longer freezes a declaration count; the live figure has held at 582 since round 9.
- **Expected:** `d12modules` / `d12modelprops` macros in `docs/counts.tex`, or extend `count_literals` to catch spelled-out numerals before "modules"/"propositions"/"theorems".
- **Fix:** Two macros, or one regex.
- **Cache:** n/a

---

### 8.1 — 🔴 BLOCKER — the round-9 closure guard is **inert**: the line immediately after it overwrites its verdict, so a two-key ledger record closes a 🔴 BLOCKER, and 264 of 287 blocking findings currently `fixed` are closed by records that fail the guard's own stated bar

- **Gate:** FixPropagation
- **Location:** `scripts/build_graph.py:1670-1690` (the hardened guard) and `:1701-1706` (the override)
- **Observed:** The guard you added is correct in isolation:
  ```python
  if status == 'fixed' and severity in ('critical', 'major', 'blocker'):
      _rec = supersessions.get(finding_id) or {}
      _why = str(_rec.get('evidence') or _rec.get('note') or _rec.get('rationale') or '').strip()
      _closes = (_rec.get('status') == 'fixed' and len(_why) >= 40
                 and bool(str(_rec.get('commit') or '').strip()))
      if not _closes:
          status = 'open'
  ```
  Twelve lines later, unconditionally:
  ```python
  ledger = supersessions.get(finding_id)
  if ledger:
      meta['status'] = ledger.get('status', status)
  ```
  `_closes` only ever writes the local `status`, which is then discarded for any record carrying a `status` key. The guard's three new conditions have **no effect on the emitted node**.
- **Evidence:** Mutation-tested in-process against a monkeypatched `PROJECT_ROOT` in a `tempfile.mkdtemp()` tree — nothing in the repo was touched, and the tree was removed:
  ```
  finding   ledger record                                                  emitted meta.status
  9.1  🔴   {"finding_id": …, "status": "accepted"}                        'accepted'  ← BYPASS
  9.2  🔴   {"finding_id": …, "status": "accepted", "rationale": <60ch>}   'accepted'  ← BYPASS
  9.3  🔴   {"finding_id": …, "status": "fixed", "evidence": <80ch>}       'fixed'     ← BYPASS (no commit)
  9.4  🔴   (no ledger record — control)                                   'open'      ← guard works
  9.5  🔴   {"finding_id": …, "status": "fixed"}                           'fixed'     ← MINIMAL BYPASS, two keys
  ```
  **9.5 is the whole finding.** Two keys. No evidence, no commit, no note, no date, no `superseded_by`. Round 9's one-key bypass `{"finding_id": X}` *is* closed — `ledger.get('status', status)` then falls back to the guard-corrected `'open'` — and adding the single token `"status": "fixed"` restores it completely. On the **live** ledger this means the guard fires exactly never: all **805** records carry a `status` key, so `ledger.get('status', status)` never reaches its default. Measured against the guard's own bar:
  ```
  ledger records                                                         805
  records with status=fixed that FAIL the guard's bar (no commit)        629
  blocking-severity ReviewFinding nodes currently 'fixed'                287
    …closed by a record that fails the guard's own bar                   264   (92%)
  ```
  D12's own 24 ledger records split 13 PASS / 11 FAIL, the eleven failures being the Stage-9/10 figure-review annotated IDs (`D12:MAJOR:fig1-plots-only-one-of-two-failure-directions` etc.), all `status: fixed` with `commit: None`. Those eleven are annotated-scheme IDs that resolve to no `ReviewFinding` node, so D12's graph-level exposure through this path is nil — but the mechanism is project-wide and D12's gate reads from it.
- **Expected:** Three changes, and the third is the answer to your question about point (b).
  1. **Make the guard authoritative.** Compute the ledger override *inside* the guard, not after it: `meta['status']` for a blocking finding must be `'open'` unless `_closes`, full stop. One-line reorder.
  2. **Apply the bar to `accepted` too.** `accepted` is now the cheapest closure in the system (see 8.5) and it bypasses this guard by construction. A blocking-severity `accepted` should require the same `commit`-or-`decision_ref` plus rationale, and should not remove the finding from `r.blockers` — it should move it to a separate `r.accepted_blockers` that the gate still surfaces.
  3. **Severity must not come from the closing document — here is the precise specification.** The defect is not that a document can *close* a finding by wording; it is that **the same document mints the finding, sets its severity, and sets its status**, so a document that omits the glyph is born `advisory` + `fixed` and no guard ever looks at it. Three concrete requirements:
     - **(3a) Birth-status invariant.** A `ReviewFinding` minted from a review document is born `status: 'open'`, at every severity. Heading-parse closure is removed entirely, not just for blocking severities. The supersession ledger becomes the sole status-transition channel, which is what makes 8.4 (the ledger scan failing open) load-bearing rather than incidental.
     - **(3b) Severity is a declared field, not an inferred glyph.** Parse severity from a required body key — `- **Severity:** BLOCKER|REQUIRED|RECOMMENDED` — and add a `validate.py` leg that FAILS any review document containing a `### N.N —` heading whose body lacks that key. Today, omitting the glyph silently yields `advisory`; under this rule, omitting the declaration is a suite failure. That converts the exploit from "silent downgrade" into "red build". Keep the glyph as a rendering convention; stop deriving semantics from it.
     - **(3c) Recurrence reopens.** When a finding body declares that a prior finding reproduces (this document does so eleven times), the named prior `finding_id` must be reopened, overriding any ledger record. A finding that a later fresh-context round re-derives is by definition not fixed, and the closing party should not get to keep the ledger entry. Implement as: parse `round-N X.Y REPRODUCES` from the body, resolve to the prior id, force `status='open'`, and record the reopening id in `meta['reopened_by']`.
  Together, (1)–(3) mean no artifact authored by the party seeking closure can set severity or status; only the ledger sets status, only the finding sets severity, and only a fresh review can clear a recurrence.
- **Fix:** One reorder (≈4 lines), one severity-declaration parser plus one validator leg, one recurrence parser.
- **Cache:** n/a

### 8.2 — 🔴 BLOCKER — `review_docs_mint_findings` exempts the exact directory `BUNDLE_DIRECTORY_SCHEMA.md` names as the canonical home for a bundle Stage-13 review; five findings-bearing reviews mint zero inside it, 120 zero-minting documents sit outside the check's scope entirely, and I found seven heading forms that still mint nothing — three of them already used on disk

- **Gate:** FixPropagation
- **Location:** `scripts/validate.py:3862-3872` (the two `continue`s); `scripts/build_graph.py:1421-1424` (`_REVIEW_SECTION_RE`); `docs/BUNDLE_DIRECTORY_SCHEMA.md:86`; `docs/BUNDLE_LIFT_PROCEDURE.md:243`
- **Observed:** The new check is real and it does close the two documents I reported at round 9 (D7 `### N1.1` now mints 1, I3 `#### Anchor 1` now mints 5). You asked me to verify that the scoping does not hide a real defect. **It does, three ways.**
- **Evidence:**
  1. **The directory exclusion exempts the schema's own canonical location.** The check skips any document whose parent directory name contains `bundle-stage13`, on the stated grounds that those hold `bundle_readiness.py`'s generated aggregations, which mint zero by design. But `docs/BUNDLE_DIRECTORY_SCHEMA.md:86` defines `stage13_review_doc` as "path | reviewer-agent points to latest Stage-13 doc under `papers/AutomatedReviews/<DATE>-bundle-stage13/<X>.md`", and `BUNDLE_LIFT_PROCEDURE.md:243` repeats it. **A reviewer who follows the schema writes into the one directory the guard does not check.** These directories are mixed, not generated — measured, with severity-marked headings counted against minted nodes:
     ```
     papers/AutomatedReviews/2026-05-04-bundle-stage13/F.md        8 severity headings   0 minted   EXEMPT
     papers/AutomatedReviews/2026-05-06-bundle-stage13/D2-r2.md    8                     0 minted   EXEMPT
     papers/AutomatedReviews/2026-05-06-bundle-stage13/D5-r2.md    6                     0 minted   EXEMPT
     papers/AutomatedReviews/2026-05-04-bundle-stage13/D4.md       4                     0 minted   EXEMPT
     papers/AutomatedReviews/2026-05-01-T1830-bundle-stage13/L1.md 1                     0 minted   EXEMPT
     papers/AutomatedReviews/2026-05-04-bundle-stage13/D1.md      10                     1 minted   EXEMPT
     ```
     These are not statistics documents. `2026-05-04-bundle-stage13/F.md` opens with `bundle: F / reviewer: adversarial-reviewer / model: claude-opus-4-7-1m` and its first finding heading is `### BLOCKER 1.1 — LaTeX undefined control sequences  →  CLOSED`. A declared BLOCKER, in a genuine fresh-context review, minting nothing, permanently exempt.
  2. **The roster filter drops the entire per-paper population.** `if md.stem not in roster: continue` restricts the check to the 21 bundle codes. **120** review documents currently mint zero nodes and **all 120** are out of scope — including `2026-04-26-1955-lean-substance-audit/lean_project_audit.md` (17 severity headings, 0 minted), `2026-04-10/CitationReview-01.md` (15, 0), and `2026-04-29-0200-notebook-adversarial/paper32_strong_cp_de.md` (10, 0). The class you asked me to check is not closed; one sub-case is.
  3. **Seven heading forms still mint nothing, and three are already on disk.** Tested against the live regex:
     ```
     MATCH   '### 1.1 — 🔴 BLOCKER — x'          NOMATCH '##### 1.1 — 🔴 BLOCKER — x'
     MATCH   '#### 1.1 — 🔴 BLOCKER — x'         NOMATCH '### 1.1.2 — 🔴 BLOCKER — x'
     MATCH   '### N1.1 — 🔵 RECOMMENDED — x'     NOMATCH '### Finding 1.1 — 🔴 BLOCKER — x'
     MATCH   '#### Anchor 1 — x'                 NOMATCH '### 1.1: 🔴 BLOCKER — x'
     MATCH   '### 8.10 — 🔴 BLOCKER — x'         NOMATCH '### BLOCKER 1.1 — …'      ← used in F.md
                                                 NOMATCH '### V.1 — BLOCKER #1.1 …'  ← used in D2-r2.md
                                                 NOMATCH '### R-1 — RECOMMENDED — …' ← used in D4.md
                                                 NOMATCH '### AB1.1 — …'  '### [1.1] — …'
     ```
     `### 1.1.2 —` is the natural class·index·subindex form and is one keystroke from the sanctioned one. The widening fixed the two forms I named at round 9 and left the three the project had already shipped.
  4. **The residual predicate is still too weak even in scope.** The check tests `minted ≥ 1`, so a document losing 19 of 20 headings passes. One in-scope document already exhibits partial minting (`2026-06-08-2314-internal-adversarial/L2.md`, 3 severity headings, 2 minted). And `papers/D12/bundle_metadata.json:19` still reads `"stage13_review_doc": null` beside `"last_stage13_review": "2026-07-31"`; grepping `scripts/` for `stage13_review_doc` returns exactly one hit — `bundle_source_manifest.py:141`, which *writes* `None` at creation. No reader anywhere, in either document that mandates it.
- **Expected:** (a) Replace the directory-name exclusion with a provenance test — `bundle_readiness.py` should stamp its generated aggregations (`"generated_by": "bundle_readiness.py"` in the front matter) and the check should exempt on that key, never on a path convention that the schema itself points reviewers at. (b) Drop the roster filter; scope by "has YAML front matter containing `reviewer:`" instead, which covers per-paper reviews and is what actually distinguishes a review from a summary. (c) Strengthen the predicate from `minted ≥ 1` to `minted == (count of headings carrying a severity marker)`, which is computable from the same two inputs and is the predicate that catches partial loss. (d) Either make `stage13_review_doc` mandatory-and-read, or delete it from both documents; a field two canonical docs require and no code reads is worse than no field.
- **Fix:** One front-matter stamp, one scope predicate, one equality instead of an inequality.
- **Cache:** n/a

### 8.3 — 🟡 REQUIRED — `readiness_submission_gate`'s `ImportError` handler still returns `passed=True`, and the AST sweep still finds twelve such handlers (round-9 8.3 REPRODUCES)

- **Gate:** FixPropagation
- **Location:** `scripts/validate.py:4412-4419` vs `:4420-4432`
- **Observed:** Unchanged:
  ```python
  try:
      from build_graph import build_graph_json
  except ImportError as exc:
      return CheckResult(passed=True, details=[
          Detail("import", True, f"build_graph not available ({exc}); skipping", warning=True)])
  ```
  Ten lines below, the `if not gates:` branch that round 8 corrected now reads "FAIL, not warn (D12 round-8 BLOCKER 8.2). Zero gate nodes is not 'no problems found'". The same function holds both postures.
- **Evidence:** I re-ran round-9's own AST sweep of `scripts/validate.py` for `CheckResult(passed=True)` returns inside `except` handlers: **12 hits**, at lines `623, 1956, 1959, 1971, 2112, 2381, 4085, 4416, 5367, 6993, 7001, 7277` — the identical set round 9 reported. This is the fifth consecutive round in which "not evaluated is spelled passed" has been fixed at the site a finding named and nowhere else. Reported at REQUIRED rather than BLOCKER for the same honest reason as round 9: on this mutation three sibling checks still fail, so the suite goes red.
- **Expected:** `passed=False` here; then run the sweep as a test so the class closes instead of being fixed one site per round. Round-9 QI item 2 proposed exactly this and it was not implemented.
- **Fix:** Three lines plus one test.
- **Cache:** n/a
- **Lane:** `infra`

### 8.4 — 🟡 REQUIRED — `ledger_ids_resolve` still fails open on its own exception, and after 8.1 the ledger is the only thing that can close a blocker (round-9 8.4 REPRODUCES verbatim)

- **Gate:** FixPropagation
- **Location:** `scripts/validate.py:3420-3423`
- **Observed:** Unchanged:
  ```python
  except Exception as exc:  # pragma: no cover
      roster_details.append(Detail(
          "ledger_ids_resolve", True,
          f"ledger integrity scan skipped ({type(exc).__name__}: {exc})", warning=True))
  ```
- **Evidence:** Forty lines earlier the sibling handler reads "FAIL, not pass: this guard exists to make invisible findings visible, so a build error in the scan must not be indistinguishable from a clean scan (D12 round-6 finding 8.2 — the original `warning=True` failed open)." Both were shipped in the same commit and only one was corrected. The path is live: the scan reads `_g`, bound inside the *previous* `try` block, so any failure of the graph build raises `UnboundLocalError` here and this handler swallows it. The severity argument is structural: `ledger_ids_resolve` is the only referential guard on the ledger, and under my recommended fix to 8.1 the ledger becomes the sole status-transition channel for every severity. A guard on the closure channel that reports green when it did not run is the wrong shape for that job.
- **Expected:** `passed=False` on this handler, and `_g` computed once outside both blocks so the dependency is explicit.
- **Fix:** Two lines.
- **Cache:** n/a

### 8.5 — 🟡 REQUIRED — 27 blocking-severity `accepted` findings remain invisible to Gate 11, and `accepted` is now the *cheapest* closure in the system because it bypasses the 8.1 guard entirely (round-9 8.5, RESTATED as requested)

- **Gate:** FixPropagation
- **Location:** `scripts/readiness_gates.py::_eval_fix_propagation`; `scripts/validate.py::check_accepted_findings_carry_rationale`
- **Observed:** Measured on the live graph this round: **1190** `ReviewFinding` nodes, **122** at `status: accepted`, of which **27** are at blocking severity — **8 critical, 19 major** — the same 27 rounds 8 and 9 counted. `_eval_fix_propagation` partitions `accepted` out of `open_findings` before the severity filter runs, so none of the 27 enters `r.blockers`, sets `state = 'blocked'`, or drives `needs-recheck`.
- **Evidence:** The rationale floor works and I re-ran it: 140 accepted records, **0** with under 40 characters of written rationale. That closes the "unenforced discipline" half. What round 9 could not yet see is the interaction with the new guard, and it inverts the incentive. After 8.1, closing a blocking finding as `fixed` nominally requires `status == 'fixed'` + ≥40 characters + a `commit`; closing it as `accepted` requires `status == 'accepted'` + ≥40 characters and **no commit at all** — and only the second removes the finding from the gate's blocking set. My mutation test above confirms both paths emit through the same unguarded override, so in practice neither bar binds; but the moment 8.1 is fixed correctly, `accepted` becomes the documented low-friction escape and the 27 become 28. For **D12 specifically the exposure is nil** — all 24 D12 ledger records are `status: fixed` and none is `accepted` — so this neither helps nor hurts this bundle's gate today. It is a project-wide surface on the gate D12 is currently blocked at.
- **Expected:** Surface accepted-at-blocking-severity in `GateResult.notes` and in a dedicated `r.accepted_blockers` list rather than only in an evidence line, so the dashboard shows "0 open, 27 accepted at blocking severity" instead of "0 open"; and require a blocking-severity `accepted` record to name a decision-maker (`accepted_by`) and a review date, not only a rationale. An acceptance is a decision; a decision has an owner.
- **Fix:** One notes string, one list field, one validator condition.
- **Cache:** n/a
- **Lane:** `infra`

### 8.6 — 🟡 REQUIRED — **I withdraw round-8/9 finding 8.6.** You were right about `freshness_stale`; my reading was wrong and the fix I endorsed would have corrupted the field. The real residual is that the protocol states a clearing rule no code enforces and two writers contradict

- **Gate:** FixPropagation
- **Location:** `docs/LATE_PHASE6_ABSORPTION_PROTOCOL.md:86` and `:184`; `docs/READINESS_GATES.md:258`; `scripts/check_bundle_source_freshness.py:184-206`; `scripts/bundle_append.py:318`; `scripts/validate.py:4028-4034`
- **Observed (withdrawal first):** Round 8 and round 9 both reported that seven bundles including D12 "assert the forbidden pair" `freshness_stale=false` with open blockers, citing `LATE_PHASE6_ABSORPTION_PROTOCOL.md:184`. **That was wrong and I re-derived why from the code rather than from the doc.** The field is owned by `check_bundle_source_freshness.py`, which sets it `True` when a source paper's mtime post-dates `last_lift` (protocol `:86`) and `False` otherwise; `bundle_append.py:318` clears it on every lift with the comment "we just lifted; no longer stale". Its meaning is **"a source paper is newer than the last lift"**, which is independent of blocker count. Line `:184` is a *workflow step inside a Stage-F re-review cycle*, not the field's definition, and D12 has never entered such a cycle. `freshness_stale: false` with 34 open blockers is therefore correct data. Your backed-out derivation (`freshness_stale = not (n_blockers == 0 and stages_green)`) would have set the flag on bundles whose sources had not moved, made two writers fight, and — per the comment now at `validate.py:4028` — broken idempotency. Backing it out was right. I verified D12's blob is internally consistent: `blockers_open: 34`, `advisories_open: 50`, `readiness: "RED"`, `blocked_p1_gates: ["FixPropagation", "ParameterProvenance"]`, all matching the gate evaluation I ran independently, and `bundle_metadata_matches_graph` passes.
- **Evidence (the residual that is real):** The doc line survives, and it states an invariant nothing holds. `LATE_PHASE6_ABSORPTION_PROTOCOL.md:184` — "`freshness_stale` cleared once `blockers_open == 0` and all three statuses GREEN" — is quoted verbatim into `READINESS_GATES.md:258`, a document this review is required to treat as canonical. Meanwhile `check_bundle_source_freshness.py:201-206` clears the flag on mtime grounds alone, unconditionally, with no reference to blockers or stage statuses, and `bundle_append.py:318` clears it on every lift. So the canonical documents assert a gate that two writers actively violate on every run. That contradiction is what produced two rounds of wrong findings from me and one wrong remediation from you; it will produce a third from the next fresh-context reviewer, who has no way to know the doc is the unreliable artifact.
- **Expected:** Pick one. Either (a) delete the clearing condition from `:184` and `READINESS_GATES.md:258` and state the field's actual definition — "set when a source paper is newer than `last_lift`; cleared on the next lift" — and move the Stage-F gate onto `stage13_redo_required`, which `bundle_append.py` already sets on lift and which no other writer clears; or (b) implement `:184` and delete the mtime clearer. (a) is right: the field is an input signal, and overloading an input signal with an exit condition is what made this ambiguous.
- **Fix:** Two doc lines, or one field rename.
- **Cache:** n/a

### 8.7 — Class 8 ProductionRunHealth: PASS, no finding for D12

D12 makes no simulation-evidence claim. I grepped the draft case-insensitively for "Monte Carlo", "numerical evidence", "simulation", "simulated" and "we simulate" — **zero** occurrences; every quantitative statement is a kernel-checked rational enclosure or a closed-form evaluation. The graph carries 18 `ProductionRun` nodes and **zero** edges touching `paper:D12`, which is correct for a pure-formalization bundle. Aside, unchanged since round 8 and not a D12 finding: 17 of the 18 `ProductionRun` nodes carry `status: unknown`, which is a blocking status under Gate 8 for any paper that ever links one.

---

## Verification of the repairs you asked me to attack

| item | verdict | how I checked it |
|---|---|---|
| **8.1 closure guard** | **INERT — worse than round 9 believed** | Mutation-tested five ledger shapes in a temp tree. `{"finding_id": X, "status": "fixed"}` — two keys — closes a 🔴 BLOCKER, because `meta['status'] = ledger.get('status', status)` at `:1703` overwrites the guard. All 805 live records carry a `status` key, so the guard fires **never**; 264 of 287 blocking `fixed` findings are closed by records failing its own bar. Finding 8.1, with the precise (b) specification you asked for. |
| **8.2 `review_docs_mint_findings`** | **real, but the scoping hides a real defect** | The directory exclusion exempts `papers/AutomatedReviews/<DATE>-bundle-stage13/<X>.md` — the exact path `BUNDLE_DIRECTORY_SCHEMA.md:86` and `BUNDLE_LIFT_PROCEDURE.md:243` name as canonical for a Stage-13 review. Five findings-bearing reviews mint zero inside it, one of them declaring a BLOCKER. 120 zero-minting documents are out of scope entirely. New zero-minting heading forms found: `### BLOCKER 1.1 —`, `### V.1 —`, `### R-1 —` (all three already on disk), plus `### 1.1.2 —`, `##### 1.1 —`, `### Finding 1.1 —`, `### 1.1: `. Finding 8.2. |
| **`extract_verifies_edges`** | **fix genuine; the 4 are substantively tested; no false coverage today** | All four D12 formulas carry `golden`/`identity` edges backed by real assertions, including a 28-point identity grid at `rel=1e-12` against an independent implementation. Every `F`/`formulas` binding under `tests/` resolves to `src.core.formulas` — 4 modules, verified by grep — so the allow-list manufactures nothing. **But** Gate 4 evaluates a hand-curated 4 of the 27 D12-region formulas, and the 23 it excludes contain `gaussian_tail_birnbaum_lower`, whose only test is an inequality and which would flip the gate to `blocked`. Findings 2.2, 2.6. |
| **`prxquantum`** | **correction is right; placement is not** | `compile_bundle_pdf.py D12` → `pages=10 overfull=0 tex_errors=0 dropped_class_opts=0`, judged from the rendered PDF. All three artifacts now carry the corrected account. The only change to the draft since round 8 is this comment, which now puts eight lines of first-person internal-review narration at the top of the submitted source. Finding 5.5. |
| **idempotency** | **genuine, verified by execution** | `validate.py` 57/57 in 340 s with **zero** tracked artifacts mutated (`git status --porcelain` byte-identical before and after; the 132 dirty figures pre-date the run at 09:54). `update_counts.py` run twice consecutively: `docs/counts.tex` md5 `fa1c8354c15cb669d29401791d432803` and `docs/counts.json` md5 `ef3c99af64e8ba80a3ad95d604400ec8`, unchanged across both runs, both printing "left byte-identical". |
| **`freshness_stale`** | **your reading is right; mine was wrong — withdrawn** | Traced the field to `check_bundle_source_freshness.py:184-206` and `bundle_append.py:318`. It means "a source paper is newer than `last_lift`" and is independent of blocker count. Round-8/9 finding 8.6 is withdrawn in full. The residual is a doc-vs-code contradiction that will re-produce my error in the next reviewer. Finding 8.6. |
| **round-9 2.1 (`DEPENDS_ON`)** | **committed and live** | `paper:D12 --DEPENDS_ON--> param:MATHER_1982_GRADIENT_REDUCTION` present in the live graph; Gate 3 evaluates non-vacuously at `blocked — 1 parameters lack human_verified_date`. Only the human sign-off remains, and the gate should keep blocking on it (finding 2.1). |

## Compliance note

I ran no git-mutating command — only `git log`, `git status`, `git diff`,
`git ls-files`, `git check-ignore`, `git rev-parse`. I ran no `pdflatex` inside
`papers/`; the only compile was `uv run python scripts/compile_bundle_pdf.py D12`,
which rewrote the gitignored `papers/D12/paper_draft.pdf`. All mutation testing
was in-process against a monkeypatched `PROJECT_ROOT` in `tempfile.mkdtemp()`
trees, every one of which was removed; **no repository file was mutated by it.**
Before running `update_counts.py` twice I copied `docs/counts.tex` and
`docs/counts.json` to the scratch directory as backups; both were left
byte-identical, so no restore was needed. Files I wrote: this report, and five
verification records appended to `docs/citation_verifications.jsonl`
(`EchenimMhalla2021`, `RashidHasan2017`, `Mather1982`, `IrwinHilton2005`,
`UnruhCBO2025`).

## QI Candidate

**Systemic: five of D12's eleven gates pass on empty evidence sets, and nothing distinguishes "checked and clean" from "nothing to check".**

Measured on the live graph, `paper:D12` has `OUT = {CLAIMS: 1, DEPENDS_ON: 1,
HAS_FIGURE: 3, CITES_SOURCE: 14}` and `IN = {FLAGS: 97}` — one `PaperClaim`,
zero `GROUNDED_IN`, zero `ASSUMES`, zero `REPORTS`, zero `ProseClaim` links,
for a 755-line paper citing 160 project declarations. The gates report:

```
CrossPaperConsistency  passed  "no contradictions detected"          (0 CONTRADICTS edges exist project-wide)
ComputationCorrectness passed  "all 4 grounded formulas ... tested"  (4 of 27, from a hand-written list)
AssumptionDisclosure   passed  "no hypothesis dependencies"          (0 ASSUMES edges; §5 is a hypothesis-disclosure section)
NarrativeGrounding     passed  "no interesting prose claims flagged" (0 ProseClaim links; the abstract is 26 lines of claims)
NumericalFreshness     passed  "no numerical literals detected"      (28 derived numerals in the prose)
```

Every one of those five sentences is literally true and none of them means what
a reader of the heatmap takes it to mean. Finding 6.1 is the concrete cost: a
load-bearing undisclosed assumption in the bundle's capstone, sitting under a
gate whose evaluator has nothing to evaluate. `READINESS_GATES.md:19` says
"every gate exists because a real failure class has passed through the internal
pipeline in the past" — but a gate with an empty input set is not guarding that
failure class, it is reporting the absence of its own evidence.

Proposed: `GateResult` gains an `evidence_count` field; any gate evaluating on
an empty set reports `open` ("insufficient data to evaluate", already in the
status model at `:12`) rather than `passed`, and the bundle aggregate treats
`open` as YELLOW rather than GREEN. That is a one-field change to
`readiness_gates.py` and it converts five silent vacuous passes into five
visible obligations. It also subsumes the round-9 QI item about the numeric
surface: `NumericalFreshness` would immediately read `open` for D12, which is
the true state.

**Also, cross-cutting, each already measured above:**

1. **The closure machinery still has exactly one channel and it is unguarded at
   both ends** (8.1, 8.2, 8.4, 8.5). Round 8 narrowed closure to the ledger;
   round 9 tried to raise the ledger's bar; the bar is overwritten twelve lines
   later, the only referential guard on the ledger fails open, `accepted`
   bypasses the bar entirely, and a review document written at the schema's own
   canonical path mints nothing for any of it to guard. Narrowing a path without
   hardening it concentrates risk. The (3a)/(3b)/(3c) specification in 8.1 is
   the minimum that makes the closing party unable to set severity or status.
2. **"Not evaluated" is still spelled "passed", fifth round running** (8.3). The
   AST sweep finds 12 `CheckResult(passed=True)` returns inside exception
   handlers — the identical set round 9 reported. Each round fixes the handler a
   finding names and no others. Make the sweep a test.
3. **Canonical documents are now a source of reviewer error, not just of
   reviewer guidance** (8.6, 8.2, 1.3). Three separate canonical documents
   currently state things the code does not do: `LATE_PHASE6_ABSORPTION_PROTOCOL.md:184`
   (a clearing rule two writers violate), `BUNDLE_DIRECTORY_SCHEMA.md:86` (a
   field no code reads, pointing at a directory the new guard exempts), and
   `READINESS_GATES.md:33` (an `arxiv_verified` criterion 8 of 369 entries
   meet). A fresh-context reviewer is instructed to treat these as defining what
   "correct" means, so each divergence manufactures a wrong finding — I produced
   one from the first, in two consecutive rounds. Doc-vs-implementation
   divergence should itself be a validate leg wherever the doc names a field or
   a path.
