---
paper: E2
reviewer: adversarial-reviewer
model: claude-opus-4-7
review_date: 2026-05-11T15:50:00Z
readiness_gates_version: 1
bundle_target: E2
---

# Adversarial Review — E2 (Dean-Kim-Lucas Graphene PRL)

## Summary

Fresh-context Stage-13 sweep of `papers/E2/paper_draft.tex` (443 lines, "Detecting Analog Hawking Radiation in the Bilayer-Graphene Dirac Fluid"). The bundle was last touched at 2026-05-07 Session-3 cross-bundle propagation; Wave 6 closures on the sibling E1 + I2 bundles did NOT propagate to E2. Result: E2 is **stale relative to the current pipeline state** on multiple fronts. **Six findings emerge with the headline a BLOCKER:** (1) **Zhao2023 bibitem carries a paraphrased/wrong title** — same defect that was fixed in D1 in the 2026-05-11 Stage-13 fix-pass; the registry already has the correct metadata ("Observation of hydrodynamic plasmons and energy waves in graphene", Nature 614:688, DOI 10.1038/s41586-022-05619-8) but E2's bibitem text was never updated; (2) **stale theorem-count literal 5229/243** at line 356 — canonical is 5858/322 (12% off), and E2 has no `\input{counts.tex}` retrofit; (3) **κ = 2.2e12/s versus D = 0.23 is internally inconsistent** within the paper (the two values imply different κ — paper-internal drift); (4) E2 quotes `l_mr ~ 15 µm` at line 188 while canonical `GRAPHENE_PLATFORMS['Dean_bilayer_nozzle']['l_mr_um'] = 5.0`; (5) six dangling bibitems (Unruh1981, Visser1998, Falque2025, CGL2017a, CGL2017b, Romatschke2010) — never `\cite{}`d in body; PRL renders them but the bibliography appears bloated; (6) `hawking_temp_from_surface_gravity` is a `rfl`-discharge on its own definitional unfolding (same P5 anti-pattern flagged in sibling E1); citation at line 83 reads as if substantive. The earlier Session-3 review's three RECOMMENDED items (Falque2025 dangling, Zhao2023 missing coordinates, η/s device-equivalence) remain — Zhao2023 is now escalated to BLOCKER since the registry-side fix landed.

**Counts:** 🔴 2 BLOCKER · 🟡 5 REQUIRED · 🔵 4 RECOMMENDED · 11 total findings.
**Gates affected:** CitationIntegrity (Gate 1), CrossPaperConsistency (Gate 2), ParameterProvenance (Gate 3), NumericalFreshness (Gate 9), NarrativeGrounding (Gate 7), LeanProofSubstance (Gate 5 — advisory only), AssumptionDisclosure (Gate 6).
**Submission-ready:** No — Gate 1 + Gate 9 BLOCKERS must be cleared.

## Findings

### 1.1 — 🔴 BLOCKER — Zhao2023 bibitem has paraphrased / wrong title, no journal, no DOI, no arXiv ID

- **Gate:** CitationIntegrity
- **Location:** `papers/E2/paper_draft.tex:413-416`
- **Observed (E2 bibitem):**
  ```
  \bibitem{Zhao2023}
  Y.~Zhao \emph{et al.},
  Direct measurement of the Dirac-fluid sound speed
  $c_s = v_F/\sqrt{2}$ via hydrodynamic plasmon spectroscopy (2023).
  ```
- **Evidence (Crossref cache `Lit-Search/Phase-5w/primary-sources/Zhao2023.json`, plus registry `src/core/citations.py:6579-6595`):**
  - **Actual title:** "Observation of hydrodynamic plasmons and energy waves in graphene"
  - **First author:** Wenyu Zhao (initial **W.**, not "Y.")
  - **Venue:** Nature 614, 688-693 (2023), DOI 10.1038/s41586-022-05619-8
  - The registry note explicitly records the historical defect class: *"D1 used_in added 2026-05-11 (Stage-13 fix-pass: prior D1 bibitem had wrong first-author initial and wrong paraphrased title `Direct measurement of plasmon-mediated viscosity in graphene`)."*
  - D1's bibitem was fixed; E2's was missed in the propagation.
- **Verdict:** `wrong_title` AND `wrong_author` AND `missing_venue` AND `missing_doi` AND `missing_arxiv`. The paraphrased title is an editorial restatement of what the paper *implies* about c_s = v_F/√2 (per provenance.py:716-722, the Phase 5w deep-research interpretation), but it is NOT the actual published title. A reader who follows this bibitem hits a non-resolvable cite.
- **Expected:** The bibitem should read (replicating the fixed D1 form):
  ```
  \bibitem{Zhao2023}
  W.~Zhao, S.~Wang, S.~Chen, Z.~Zhang, K.~Watanabe, T.~Taniguchi, A.~Zettl, and F.~Wang,
  ``Observation of hydrodynamic plasmons and energy waves in graphene,''
  Nature \textbf{614}, 688 (2023),
  doi:10.1038/s41586-022-05619-8.
  ```
  (Optionally include a one-line note that the paper's hydrodynamic-plasmon dispersion implies c_s = v_F/√2 — this is the load-bearing physical content for E2 line 70-71 but it is interpretive, not the title.)
- **Cross-bundle propagation:** D1's `Zhao2023` bibitem and the registry are already correct; E2 is the sole holdout. The 2026-05-11 D1 fix-pass should be propagated.
- **Cache:** fresh-fetch (verified against Crossref JSON cached at `Lit-Search/Phase-5w/primary-sources/Zhao2023.json` — see `title`/`author`/`DOI`/`volume`/`page` fields).

### 1.2 — 🟡 REQUIRED — Six bibitems with no `\cite{}` invocation in the body (dangling bibliography entries)

- **Gate:** CitationIntegrity
- **Location:** `papers/E2/paper_draft.tex:369-441`
- **Observed:** The following bibitems are declared in the bibliography but never cited in the body of the paper. Grep `\cite{key}` in lines 1-367 confirms zero occurrences:
  - `Unruh1981` (line 369-371)
  - `Visser1998` (line 373-375)
  - `Falque2025` (line 385-388) — already flagged 2026-05-06 r1 as RECOMMENDED; still unresolved
  - `CGL2017a` (line 390-392)
  - `CGL2017b` (line 394-396)
  - `Romatschke2010` (line 422-424)
- **Evidence:** `grep -on "\\cite{[^}]*}" papers/E2/paper_draft.tex | cut -d: -f2- | sort -u` shows only AndersonBFP2013, AnantramDatta1996, Bilic1999, Blanter2000, CallenWelton1951, Geurs2025, KSS2005, Majumdar2025, Steinhauer2016, Zhao2023, deNova2019 used in body.
- **Expected:** A PRL-style bibliography should contain only cited references. Either (a) add appropriate `\cite{}` invocations in the body (e.g., Unruh1981 / Visser1998 belong in the §1 prior-art chain alongside Bilic1999; CGL2017a/b belong wherever SK-EFT framework provenance is mentioned — and given that this E2 paper IS a SK-EFT prediction, the absence is striking; Romatschke2010 belongs wherever the η/s context is invoked at line 67-68), or (b) remove the dangling bibitems.
- **Why this is REQUIRED not BLOCKER:** A dangling bibitem is not a wrong-target citation but it inflates the bibliography and signals editorial sloppiness; PRL editors flag this in screening. Falque2025 has been flagged at this severity since 2026-05-06 r1.
- **Cache:** N/A.

### 1.3 — 🔵 RECOMMENDED — Citation cache cold for all 11 used bibkeys (process gap, not correctness gap)

- **Gate:** CitationIntegrity
- **Location:** `docs/citation_verifications.jsonl` (44 entries; zero matching E2's bibkeys)
- **Observed:** None of E2's 11 body-used bibkeys (Geurs2025, Majumdar2025, KSS2005, Zhao2023, Steinhauer2016, deNova2019, Bilic1999, CallenWelton1951, AndersonBFP2013, Blanter2000, AnantramDatta1996) has a record in `docs/citation_verifications.jsonl`. The agent spec requires per-bibitem fresh-fetch when cache is cold.
- **Spot-fetch evidence (this review):**
  - `Zhao2023` → DOI 10.1038/s41586-022-05619-8 (Crossref JSON cached): wrong-title verdict — see Finding 1.1.
  - `Geurs2025` → arXiv:2509.16321: title + first-author + Dean-team affiliations confirmed match (per 2026-05-06 cross-bundle r1 review, which performed the WebFetch).
  - `Majumdar2025` → arXiv:2501.03193 / Nature Physics 21:1374: title + author + η/s and σ_Q universalities confirmed (per same prior r1).
- **Verdict:** Spot sample shows registry coherence for 10 of 11 bibkeys; Zhao2023 mismatch handled in 1.1. No new BLOCKER from this finding alone; cache hygiene is the remaining gap.
- **Fix:** Append `match` records for the 10 verified bibkeys via `scripts/citation_cache.append_record`; fresh-fetch the remaining (AnantramDatta1996, Bilic1999, Blanter2000, CallenWelton1951) within 7 days of submission. The prior 2026-05-06 r1 review's WebFetch of Geurs2025 and Majumdar2025 can be recorded as `cache-verified` from this round.
- **Cache:** fresh-fetch (spot sample); not-yet-cached (8 remaining).

### 2.1 — 🔴 BLOCKER — Stale theorem-count literal (5229/243 vs canonical 5858/322)

- **Gate:** NumericalFreshness (Gate 9) + CrossPaperConsistency (Gate 2)
- **Location:** `papers/E2/paper_draft.tex:354-358`
- **Observed (E2 conclusion):**
  ```
  the SK-EFT framework supplies the prediction; the
  Lean~4 verification chain (5229 theorems / 243 modules / 1 axiom /
  0 sorry; \texttt{lake build} clean, v4.29.0, Mathlib commit
  \texttt{8850ed93}) supplies the structural guarantee.
  ```
- **Evidence:** `docs/counts.tex` (auto-generated 2026-05-11T11:50:18) reports:
  - `\totaltheorems{5858}` / `\substantivetheorems{5833}` (E2 quotes 5229 — drift = (5858 − 5229)/5858 = **10.7%**, ≫ Gate-9's 0.5% threshold)
  - `\leanmodules{322}` (E2 quotes 243 — drift = (322 − 243)/322 = **24.5%**)
  - `\axiomcount{1}` (matches)
  - `\sorrycount{0}` (matches)
- **Cross-paper consistency:** Sibling E1 was just flagged in the 2026-05-11-1550 review for the same defect at the 5812-vs-5855 level (0.73% drift); D1 already uses the `\substantivetheorems{}` macro. E2 carries a much larger drift and has not been retrofitted at all (no `\input{counts.tex}` anywhere — grep confirms zero occurrences).
- **Expected:** Either (a) replace the inline values with current canonical (5833 substantive / 322 modules / 1 axiom / 0 sorry) AND adopt `\input{../../docs/counts.tex}` plus `\substantivetheorems{}` / `\leanmodules{}` / `\axiomcount{}` / `\sorrycount{}` macros, OR (b) the macro retrofit alone — the latter is the Wave-6 standard (D1, E1, I2 all converged on this pattern).
- **Fix:** Add `\input{../../docs/counts.tex}` after the preamble; replace `5229 theorems / 243 modules / 1 axiom / 0 sorry` with `\substantivetheorems{} substantive theorems / \leanmodules{} modules / \axiomcount{} axiom / \sorrycount{} sorry`. The Mathlib commit hash `8850ed93` is unchanged and remains correct per CLAUDE.md "Lean Mathlib: Pinned in lean/lakefile.toml (currently 8850ed93)."
- **Severity rationale:** 10–24% drift on a prominently-displayed count in the paper's *closing claim of structural guarantee* is gate-blocking; a reader hits this number first when looking for evidence of formal-verification depth.
- **Cache:** N/A.

### 3.1 — 🟡 REQUIRED — Internal κ vs. D self-inconsistency (paper says κ ≈ 2.2e12/s and D ≈ 0.23 but the canonical pipeline κ that produces D = 0.23 is 2.0e12/s)

- **Gate:** ParameterProvenance (Gate 3) — paper-internal drift
- **Location:** `papers/E2/paper_draft.tex:87` (κ ≈ 2.2e12/s) vs. `:154-161` (D ≈ 0.23)
- **Observed:**
  - Line 87: `the surface gravity $\kappa \approx c_s/L \approx \SI{2.2e12}{\per\second}$` (derived: c_s=4.4e5/L=200e-9 = 2.2e12).
  - Line 161: `$l_{\rm ee} \approx \SI{51}{\nano\meter}$ gives $D \approx 0.23$`.
- **Internal consistency check:** D = κ · l_ee / c_s. Using paper values: 2.2e12 × 51e-9 / 4.4e5 = **0.255**, not 0.23.
- **Pipeline-canonical reconciliation:** `src/core/constants.py:228` defines `gradient_s1: 2.0e12` for the Dean nozzle. `src/core/provenance.py:664-670` quotes "D = κ·l_ee/c_s = 0.232" — internally consistent at κ = 2.0e12, NOT κ = c_s/L = 2.2e12. The c_s/L formula assumes |dv/dx| = c_s/L at the horizon, but the deep-research-derived gradient at the throat is actually estimated as 2.0e12 (consistent with the velocity profile shape, not just c_s/L).
- **Expected:** Pick one consistent set. Either:
  - **Option A (recommended):** Use canonical κ = 2.0e12/s at line 87 (drop the "≈ c_s/L" identification or qualify it as an order-of-magnitude estimate), keep D = 0.23. Hawking temperature recomputes: T_H = ℏ × 2.0e12 / (2π k_B) ≈ 2.43 K → 2.4 K rounded. Consistent.
  - **Option B:** Use κ = 2.2e12/s, recompute D = 0.255 ≈ 0.26, δ_disp = −(π/6)(0.26)² ≈ −3.5%. Then T_H = ℏ × 2.2e12 / (2π k_B) ≈ 2.67 K, requiring all "2.4 K" claims in the paper to be re-rounded to 2.7 K — a paper-wide ripple.
  - **Option A is the lower-cost fix and is consistent with `constants.py`.**
- **Cache:** N/A.

### 3.2 — 🟡 REQUIRED — `l_mr ≈ 15 µm` quoted at line 188 disagrees with canonical Dean parameter `l_mr_um = 5.0`

- **Gate:** ParameterProvenance (Gate 3)
- **Location:** `papers/E2/paper_draft.tex:187-189`
- **Observed:** "The Dean bilayer nozzle has $\omega_H/\Gamma \approx 1.6$, marginal but improving with cleaner samples (mean-free path $l_{\rm mr} \sim \SI{15}{\micro\meter}$)."
- **Evidence:** `src/core/constants.py:223` for `GRAPHENE_PLATFORMS['Dean_bilayer_nozzle']` defines `'l_mr_um': 5.0`. `src/core/provenance.py` has no entry for the prospective-clean-sample 15 µm value. Paper-internal arithmetic check on the ω_H/Γ ratio:
  - Γ_mr = v_F / l_mr = 1e6 / 5e-6 = 2e11/s (canonical) → ω_H/Γ_mr = 3.14e11 / 2e11 = 1.57 ≈ 1.6 ✓ (canonical 5 µm gives the 1.6 number quoted).
  - With l_mr = 15 µm: Γ_mr = 1e6 / 15e-6 = 6.7e10 → ω_H/Γ_mr = 4.7, not 1.6.
- **Verdict:** The text is internally OK in that "1.6" matches l_mr = 5 µm (canonical), but the prose-quoted "l_mr ~ 15 µm" is a forward-looking projection for "cleaner samples" — there is no provenance entry justifying 15 µm specifically. This is `unverified` per Gate-3 taxonomy.
- **Expected:** Either (a) add a `PARAMETER_PROVENANCE` entry tagged `tier: 'PROJECTED'` for the prospective 15-µm-cleaner-sample value with a deep-research source citation, OR (b) drop the specific "~ 15 µm" number and use "ultra-clean samples with longer momentum-relaxation l_mr" qualitatively, OR (c) replace with the canonical 5.0-µm value and rephrase the cleaner-sample sentence.
- **Cache:** N/A.

### 5.1 — 🔵 RECOMMENDED — `hawking_temp_from_surface_gravity` is `rfl` on its own definitional unfolding (P5 anti-pattern)

- **Gate:** LeanProofSubstance (Gate 5)
- **Location:** `papers/E2/paper_draft.tex:82-83`; Lean source at `lean/SKEFTHawking/AcousticMetric.lean:282-283`.
- **Observed:** Theorem `AcousticMetric.hawking_temp_from_surface_gravity` is cited at line 83 as the formal artifact for Eq. (1) T_H = ℏκ / (2π k_B). The Lean body is:
  ```
  theorem hawking_temp_from_surface_gravity (bg : FluidBackground) (h : SonicHorizon bg) :
      hawkingTemp h.surfaceGravity = h.surfaceGravity / (2 * Real.pi) := rfl
  ```
  where `hawkingTemp` is defined in `Basic.lean:185` as `noncomputable def hawkingTemp (kappa : ℝ) : ℝ := kappa / (2 * Real.pi)`. The theorem is a `rfl`-discharge on its own definitional unfolding — CLAUDE.md's strengthening-discipline P5 "definitional-unfolding-as-physics" anti-pattern.
- **Cross-bundle observation:** Same finding was flagged in `2026-05-11-1550-bundle-stage13/E1.md` §5.2 — sibling letter has the identical issue. This is a project-wide bridge-citation pattern, not an E2-specific defect.
- **Expected:** Either (a) document inline that this Lean theorem is the formal home / name-binding for the formula and that the physical-equivalence content lives upstream (e.g., in the κ definition via the Kruskal-like coordinate map), OR (b) cite a more load-bearing Lean theorem alongside (e.g., `diracFluid_hawkingTemp_eq_BEC` at `DiracFluidMetric.lean:177` — also `rfl`, same issue — or a κ-equivalence theorem if one exists).
- **Severity:** RECOMMENDED — not a submission blocker; the citation is technically valid. Same severity as E1's instance.
- **Cache:** N/A.

### 5.2 — 🔵 RECOMMENDED — `fdt_consistency` is `unfold; ring` on a definition (not a substantive cross-derivation theorem)

- **Gate:** LeanProofSubstance (Gate 5)
- **Location:** `papers/E2/paper_draft.tex:200-228` (Keldysh + Landauer-Büttiker dual derivation); related D1 §7.5 prose claiming "consistency-of-derivation closure `GrapheneNoiseFormula.fdt_consistency` certifying internal agreement at the Lean level."
- **Observed (Lean body, `GrapheneNoiseFormula.lean:175-178`):**
  ```
  theorem fdt_consistency (hbar_omega sigma_Q n_H : ℝ) :
      hawkingNoisePSD hbar_omega sigma_Q 1 n_H =
      2 * hbar_omega * sigma_Q * n_H := by
    unfold hawkingNoisePSD; ring
  ```
  where `hawkingNoisePSD` is the literal definition `2 * hbar_omega * sigma_Q * greybody * n_H`. The theorem reduces to "if Γ=1 then 2·ℏω·σ_Q·1·n_H = 2·ℏω·σ_Q·n_H" — pure arithmetic.
- **Issue:** E2 does NOT cite `fdt_consistency` directly in body text — it only cites the module file `GrapheneNoiseFormula.lean` indirectly (line 364). But the paper's central claim "both routes give the same closed form" (line 36, 213) is *not* discharged at the Lean level by anything more substantive than this arithmetic identity. The two-route equivalence is established in prose only.
- **Expected:** Either (a) introduce a substantive Lean theorem that formally bridges the Keldysh FDT formula and the Landauer-Büttiker B = 2ℏωσ_QΓn_H formula at the structural level (e.g., a coefficient-matching theorem with hypotheses on Bogoliubov-mixing channels), OR (b) flag in prose that the formal-Lean verification covers only the closed-form expression's algebraic properties and that the *equivalence-of-derivations* is a paper-level claim.
- **Severity:** RECOMMENDED — E2 does not directly assert `fdt_consistency` covers the cross-derivation claim, but the surrounding architecture (D1 §7.5 + the GrapheneNoiseFormula module-mention) implies it. A reader who follows the trail will find arithmetic, not a derivation-equivalence theorem.
- **Cache:** N/A.

### 6.1 — 🟡 REQUIRED — Undisclosed assumption: η/s bound transported across devices (Majumdar's η/s within-factor-of-four is for *cleanest devices, thermal regime*, not necessarily the Dean nozzle)

- **Gate:** AssumptionDisclosure (Gate 6) — carry-forward from 2026-05-06 r1 (was flagged RECOMMENDED there)
- **Location:** `papers/E2/paper_draft.tex:168-170` and `:64-69`
- **Observed:**
  - Line 168-169: "With Majumdar et al.'s [Majumdar2025] $\eta/s \approx 4 \times \hbar/(4\pi k_B)$ and $T \approx \SI{150}{\kelvin}$..."
  - Line 67-68: "...with $\eta/s$ within a factor of four of the Kovtun-Son-Starinets bound..."
- **Evidence:** Per the 2026-05-06 cross-bundle r1 review (line 64): *"Majumdar's η/s within-factor-of-four claim is for the **thermal-regime effective dynamic viscosity** (η_th) in the **cleanest devices**, not necessarily the Dean-nozzle device used by Geurs2025. E2 implicitly transports the bound across devices."* Verified in this round: the Majumdar abstract states `η_th/s_th → ℏ/(4πk_B)` for the *thermal-regime effective viscosity* in the cleanest hBN-encapsulated samples. The Dean device is a Dirac-fluid hBN-encapsulated bilayer sample (Geurs2025) and is plausibly within the universality class, but the transport is a modeling assumption that should be explicit.
- **Status escalation:** 2026-05-06 r1 marked this RECOMMENDED. Promoted to REQUIRED here because (a) the η/s value is *load-bearing* for the δ_diss ~ 10⁻¹³ claim at line 175, (b) the resulting "dissipation negligible" narrative is the paper's central technical-feasibility argument, and (c) any reviewer who knows Majumdar2025 will catch the regime-transport assumption immediately if it isn't disclosed.
- **Expected:** Add a one-sentence disclosure at line 168 (suggested wording from r1): "Adopting Majumdar et al.'s thermal-regime $\eta/s$ bound for the Dean-equivalent ultra-clean bilayer-Dirac-fluid regime..."
- **Cache:** N/A.

### 7.1 — 🟡 REQUIRED — Inline `\SI{}` literals throughout (no `tables.py` retrofit, no `\pipelinevalue{}` macro chain)

- **Gate:** NumericalFreshness (Gate 9)
- **Location:** Throughout `papers/E2/paper_draft.tex`; sample lines: 29 (T_H ≈ 2.4 K), 33 (δ_diss ~ 1e-13), 39 (ΔS_I ≈ 1e-26 A²/Hz peak), 40 (peak 51 GHz), 45 (≤ 1.8% quasi-1D bound), 63 (c_s ≈ 440 km/s), 87 (κ ≈ 2.2e12/s — also under Finding 3.1), 88 (T_H ≈ 2.4 K duplicate), 124 (W = 1 µm), 126 (ω_⊥(0) ≈ 1.38e12/s), 145 (T_H ≈ 17 K at L = 50 nm), 161 (l_ee ≈ 51 nm, D ≈ 0.23), 174 (Γ_H ≈ 0.3/s), 263 (S_thermal ≈ 1.3e-24 A²/Hz).
- **Observed:** Every device-parameter and predicted-observable literal is hard-coded `\SI{...}` rather than sourced through `\input{tables/<id>.tex}`. The bundle has a `tables/` directory (per `ls`) but it is not populated. Cross-bundle reference: D1 + I2 are partway through the `tables.py` retrofit; E1 was flagged for the same omission in 2026-05-11-1550 §7.2.
- **Expected:** Move the device-parameter literals + the predicted-Hawking-spectrum table to `papers/E2/tables.py` spec → `render_paper_tables.py` autogen → `\input{tables/dean_device_params.tex}` + `\input{tables/predicted_observables.tex}`. Tag prose-embedded literals via `\pipelinevalue{...}` macros.
- **Status:** Per Gate-9 remediation guidance, WARN-acceptable at draft but **FAIL at submission** once retrofit is complete bundle-wide. Currently the E1+I2+D1 retrofit pattern is the project standard; E2 has not started.
- **Cache:** N/A.

### 7.2 — 🔵 RECOMMENDED — "First electronic sonic horizon" first-claim (Geurs2025-attributed but not softened per project policy)

- **Gate:** NarrativeGrounding (Gate 7)
- **Location:** `papers/E2/paper_draft.tex:60-62`
- **Observed:** "The Geurs et al.~\cite{Geurs2025} demonstration of supersonic electron flow through a de~Laval nozzle in bilayer graphene created the **first electronic sonic horizon**..."
- **Evidence:** Per the task brief's project-policy adjustment §2: *"first-claim removal policy: 'first machine-checked X', 'first formally verified X', 'first computed', 'to our knowledge ... the first' — flag every instance for full removal (not softening). Program-history attributions are acceptable."* The 2026-05-06 r1 review flagged this as RECOMMENDED with the rationale "Geurs2025-attributed first-claim, not E2-author-original" — i.e., it falls under the "program-history attribution" exemption since E2 is *reporting* Geurs2025's own framing, not making an E2-author first-claim.
- **Verdict:** Hold severity at RECOMMENDED per the exemption. Author should still consider rephrasing for citation-hygiene reasons (e.g., "the Geurs et al. demonstration of supersonic electron flow ... in a Laval nozzle geometry" — drop the redundant first-claim chain). The factual content is undisputed (Geurs2025 is the headline first electron-fluid horizon paper); only the rhetorical framing is at issue.
- **Fix:** Soften or remove "first electronic sonic horizon" without losing content. Suggested: "The Geurs et al.~\cite{Geurs2025} demonstration of supersonic electron flow through a de~Laval nozzle in bilayer graphene establishes an electronic sonic horizon: drift velocity..."
- **Cache:** N/A.

### 7.3 — 🔵 RECOMMENDED — Self-history annotation about prior wrong-prefactor leaks into paper text

- **Gate:** NarrativeGrounding (Gate 7)
- **Location:** `papers/E2/paper_draft.tex:225-228`
- **Observed:** "(An earlier version of this analysis used the prefactor $2 e^2/\pi$, off by a factor of $2 e^2/h \approx \SI{77.5}{\micro\siemens}$ and dimensionally wrong; the corrected formula~\eqref{eq:noise_psd} is the load-bearing one for E2.)"
- **Evidence:** This is a transparency annotation about a prior error in the project's own derivation. While project-internal honesty is admirable, this kind of self-history annotation does NOT belong in a PRL submission — it (a) signals editorial sloppiness to the reviewer, (b) draws unnecessary attention to a now-resolved error, and (c) the corrected formula stands on its own dimensional grounds without needing the contrast.
- **Expected:** Drop the parenthetical entirely from the submission version. Keep it in a project-internal change-log / commentary file if desired for institutional memory.
- **Fix:** Delete lines 225-228 (or move them to `change_log.md`).
- **Cache:** N/A.

### 7.4 — 🔵 RECOMMENDED — Second boxed equation (line 304) lacks `\label{}`; mixed `\ref` / `\eqref` usage

- **Gate:** NarrativeGrounding (Gate 7)
- **Location:** `papers/E2/paper_draft.tex:303-305` (second boxed) and `:256` (mixed `\ref`/`\eqref`)
- **Observed:**
  - Line 304: `\boxed{\;\omega/\omega_H \in [0.5, 4]\;}` — no `\label{}`. Referenced loosely in prose (line 282 "across the $[0, 4\omega_H]$ band", line 311 "inside this band") but never via `\eqref`.
  - Line 256: `(Eq.~\ref{eq:noise_psd})` — uses `\ref` (yields "Eq. 4" instead of "Eq. (4)") whereas line 228 correctly uses `\eqref{eq:noise_psd}`.
- **Evidence:**
  ```
  $ grep -nE "label\\{|eqref\\{|ref\\{" papers/E2/paper_draft.tex
  80:\label{eq:T_H}        # never \eqref'd
  106:\label{eq:metric}    # never \eqref'd
  217:\label{eq:noise_psd}
  228:formula~\eqref{eq:noise_psd}
  256:(Eq.~\ref{eq:noise_psd})  # should be \eqref
  ```
- **Expected:** (a) Add `\label{eq:falsifiable_window}` to the line-304 boxed equation; (b) reference it via `\eqref{eq:falsifiable_window}` wherever the [0.5, 4] band is invoked; (c) replace line 256 `\ref{eq:noise_psd}` with `\eqref{eq:noise_psd}` for PRL house-style consistency; (d) consider whether `eq:T_H` and `eq:metric` should be referenced explicitly in prose — currently both labels are inert.
- **Severity:** RECOMMENDED. Cosmetic; not gate-blocking.
- **Cache:** N/A.

## QI Candidates

**QI-1 (cross-bundle propagation gap detector).** The Zhao2023 wrong-title defect (Finding 1.1) is a textbook case of partial cross-bundle propagation — D1's registry-side fix from the 2026-05-11 Stage-13 fix-pass should have triggered a corresponding bibitem update in E2. The 2026-05-06 cross-bundle propagation r1 review already flagged Zhao2023 as RECOMMENDED ("missing arXiv/DOI") but didn't escalate to "wrong title" — only the recent D1 registry update made the wrong-title nature explicit. **Proposal:** add a `validate.py --check bibitem_registry_drift` that for every `\bibitem{key}` in every paper, parses the bibitem text and asserts the bibitem title (case-/whitespace-insensitive) appears as a substring of `CITATION_REGISTRY[key]['title']` (or matches a known alias). This catches every E2-Zhao2023-class drift in seconds.

**QI-2 (`tables.py` retrofit completion gating).** Sibling reviews of E1, I2, and now E2 all flag the inline-`\SI{}` literals as REQUIRED Gate-9 findings; the retrofit pattern is well-established (D1 partially done, E1 partway, I2 partway) but no submission-gating mechanism enforces completion before submission. **Proposal:** extend `validate.py --check numerical_literals` to operate per-bundle with a deadline-aware severity: WARN if bundle is in DRAFT, FAIL if bundle has `bundle_metadata.json.stage13_status == "green"` AND `submission_target` set.

**QI-3 (paper-internal arithmetic-consistency check).** Finding 3.1 is the second instance this round of a paper claiming two physically-coupled values that don't reconcile (E2's κ = 2.2e12 vs D = 0.23 implying κ = 2.0e12). The pipeline's `paper_provenance` check verifies paper-vs-canonical drift but does NOT verify paper-internal consistency (e.g., "if D = κ·l_ee/c_s and the paper quotes all four values, do they reconcile to ±tolerance?"). **Proposal:** add a `validate.py --check paper_internal_arithmetic_consistency` that parses well-known compound parameters (D, ω_H, ω_max, δ_disp, etc.) from each paper and checks them against the elementary values also quoted in the same paper. This is a small, deterministic regex-and-formula audit; high catch-rate for the κ-vs-D drift class.
