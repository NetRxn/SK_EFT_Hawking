---
paper: L1
bundle: L1
bundle_target: L1
tier: 2
reviewer: adversarial-reviewer
model: claude-opus-4-7
review_date: 2026-05-11T15:59:00Z
readiness_gates_version: 1
prior_review_round3: papers/AutomatedReviews/2026-05-01-T1845-bundle-stage13/L1.md
prior_review_bundle_agg: papers/AutomatedReviews/2026-05-06-bundle-stage13/L1.md
counts_snapshot: 5855 thm / 5830 substantive / 322 mods / 0 sorry / 1 axiom
validate_status: 27/27 PASS
lake_build: clean (8586 jobs)
---

# Adversarial Review — L1 (GW170817 / vestigial-graviton PRL splash)

## Summary

Fresh-context Stage-13 sweep of L1 paper draft (after Wave 4 D1 fix-pass).
Findings: **0 BLOCKER, 1 REQUIRED, 3 RECOMMENDED**. The Wave 4 carryover
state (GREEN with 1 open advisory) is preserved; no new BLOCKERs surfaced
and no submission-blocking regressions. The single REQUIRED finding is
the long-standing user-action parameter-provenance human-verify for
`GW.CHI_VEST_NATURAL_LOWER` / `_UPPER` (carryover 2.1 from rounds 1-3 on
2026-05-01). The amsthm + `\newtheorem{theorem}` fix from Session-2
(2026-05-06) is still in place: `\usepackage{amsmath, amssymb, amsthm}`
at L5 + `\newtheorem{theorem}{Theorem}` at L9 of `paper_draft.tex`; the
latest LaTeX log records "Output written on paper_draft.pdf (3 pages,
391983 bytes)" with no `! LaTeX Error:` lines (vs. round-3 round's
prior three errors). All 9 distinct primary-source citations in the L1
bibitem list verify clean against Crossref / arXiv on a fresh-fetch
2026-05-11 pass; cross-bundle anchors L1↔D3 §6 remain
character-for-character consistent (Δc/c ≤ 3×10⁻¹⁵; falsification
factor 7.21×10¹⁴ at χ=10; natural range [0.1,10] → Δc/c ∈ [-0.68, +2.16]).

---

## Findings

### 1. Wrong-target citations → Gate 1 CitationIntegrity

**No findings.**

Cache-state notes (fresh-fetched 2026-05-11 because every cache entry
for `Volovik2024Vestigial` and `Volovik2022Counting` from 2026-05-01 has
verdict=`wrong_venue` on a stale bibitem hash; the L1 bibitem has since
been corrected and the current hashes do not match any cache record):

- **Volovik2024Vestigial** (L320-323): Bibitem "Fermionic Quartet and
  Vestigial Gravity, JETP Lett. 119, 330 (2024); DOI:
  10.1134/S002136402460006X". Crossref WebFetch 2026-05-11 returns
  title "Fermionic Quartet and Vestigial Gravity", G. E. Volovik,
  JETP Letters 119, 330-334, 2024. **Verdict: match (fresh-fetch).**
- **Volovik2022Counting** (L325-328): "Gravity from Symmetry Breaking
  Phase Transition, J. Low Temp. Phys. 207, 127 (2022); DOI:
  10.1007/s10909-022-02694-z". Crossref returns title "Gravity from
  Symmetry Breaking Phase Transition", G. E. Volovik, JLTP 207,
  127-137, 2022. **Verdict: match (fresh-fetch).**
- **Akama1978** (L330-332): "K.~Akama, Prog. Theor. Phys. 60, 1900
  (1978); DOI: 10.1143/PTP.60.1900". Crossref returns "An Attempt at
  Pregeometry: -- Gravity with Composite Metric --", K. Akama, PTP 60,
  1900-1909, 1978. Bibitem carries no title; venue + author + page
  match. **Verdict: match (fresh-fetch).**
- **Diakonov2011** (L334-336): "D.~Diakonov, arXiv:1109.0091 (2011)".
  arXiv fetch returns "Towards lattice-regularized Quantum Gravity",
  Dmitri Diakonov, 2011. **Verdict: match (fresh-fetch).**
- **VladimirovDiakonov2012** (L338-341): "Phys. Rev. D 86, 104019
  (2012); DOI: 10.1103/PhysRevD.86.104019". Crossref returns "Phase
  transitions in spinor quantum gravity on a lattice", Vladimirov +
  Diakonov, PRD 86, 104019, 2012. **Verdict: match (fresh-fetch).**
- **HebeckerWetterich2003** (L343-346): "Phys. Lett. B 574, 269
  (2003); DOI: 10.1016/j.physletb.2003.09.010". Crossref returns
  "Spinor gravity", Hebecker + Wetterich, PLB 574, 269-275, 2003.
  **Verdict: match (fresh-fetch).**
- **Wetterich2022** (L348-350): "JHEP 06, 069 (2022); DOI:
  10.1007/JHEP06(2022)069". Crossref returns title "Pregeometry and
  spontaneous time-space asymmetry", C. Wetterich, JHEP 2022, article
  69, 2022. Bibitem carries no title; DOI + venue + author + page
  match. **Verdict: match (fresh-fetch).**
- **Vergeles2025** (L352-354): "Phys. Rev. D 112, 054509 (2025); DOI:
  10.1103/PhysRevD.112.054509". Existing cache entry from 2026-04-26
  records "match" against arXiv 2506.00036, title "Unitarity of 4D
  Lattice Theory of Gravity". **Verdict: match (cache-verified).**
- **Abbott2017GW170817** (L356-359): "ApJL 848, L13 (2017); DOI:
  10.3847/2041-8213/aa920c". Crossref returns "Gravitational Waves
  and Gamma-Rays from a Binary Neutron Star Merger: GW170817 and GRB
  170817A", LIGO+Virgo+Fermi+INTEGRAL, ApJL 848, L13, 2017. PDF tail
  confirms Δc/c bound "between [-3×10⁻¹⁵, +7×10⁻¹⁶] times the speed
  of light". **Verdict: match (cache-verified + PDF-confirmed
  numerical bound).**
- **Roehm2026LinearizedEFE** (L361-365): in-prep companion paper23.
  Registry has `inprep=True`, title verified against paper23
  `\title{...}`. **Verdict: match (Pipeline Invariant #11 exempt).**

All 9 ⊞ in-prep bibitems pass. Note however that **registry hygiene
issue (out of scope for L1, surfaced for QI register):**
`src/core/citations.py` resolves `Wetterich2022` to title "Spinor
gravity and gauge symmetry" and `arxiv: '2110.11138'` — both wrong:
DOI 10.1007/JHEP06(2022)069 resolves to the actual title "Pregeometry
and spontaneous time-space asymmetry", and arXiv 2110.11138 is a
different paper entirely ("The emergence of gapless quantum spin
liquid..." by Liu et al, 2021). L1's bibitem itself is title-less so
the wrong registry title does not appear in the rendered PDF, but the
registry record should be corrected. Logging as QI Candidate below.

### 2. Parameter drift from primary sources → Gate 3 ParameterProvenance

#### 2.1 — 🟡 REQUIRED — `chi_vest_natural_lower / _upper` human-verify outstanding (CARRYOVER)

- **Gate:** ParameterProvenance
- **Location:** `src/core/provenance.py:2512-2543` (GW.CHI_VEST_NATURAL_LOWER) +
  `provenance.py:2545-2569` (GW.CHI_VEST_NATURAL_UPPER); used in L1
  `paper_draft.tex:41` (abstract), L111 (intro), L128 (§Result),
  L132, L162, L194, L222-225 (falsifiers).
- **Observed:** Both parameter entries have `llm_verified_date:
  '2026-04-25'` but `human_verified_date: None`. Detailed
  `human_verified_notes` block is pre-filled with the proposed-note
  rationale (RPA bubble-integral half-decade convention, cross-link
  to GRAV.ALPHA_ADW_LOWER/UPPER), but the human-verify hand-shake via
  `scripts/provenance_dashboard.py` has not been completed.
- **Evidence:** Direct read of `provenance.py:2523` (`'human_verified_date':
  None`) and `:2554` (`'human_verified_date': None`). Per Gate 3 spec:
  "every non-`PROJECTED` parameter has a primary-source citation that
  resolves" — these are tier `PROJECTED` so the relaxed requirement
  is that they be clearly labeled as projections in the paper prose;
  L1 satisfies this ("natural range" is consistently framed as a
  half-decade prior).
- **Expected:** `human_verified_date` populated via interactive
  dashboard approval.
- **Fix:** Run `scripts/provenance_dashboard.py` → navigate to
  `GW.CHI_VEST_NATURAL_LOWER` / `_UPPER` → review the pre-filled
  proposed note → approve or edit-and-approve.
- **Cache:** N/A (provenance-side check, not citation fetch).
- **Severity rationale:** REQUIRED, not BLOCKER. PROJECTED-tier
  parameters can advance in the draft stage (Gate 3 escalation note);
  human-verify is a *submission-gate* user-action carryover that has
  been documented as the only open advisory at rounds 1-3 + Wave 3
  + Wave 4 in `bundle_metadata.json:advisories_open=1`. Re-flagging
  to maintain visibility, not as new finding.

No other Gate 3 drift surfaced. The two-sided GW170817 cap
(`tolGW170817 := 3e-15` in Lean L137; matches abstract claim
`τ = 3 × 10⁻¹⁵`) is `human_verified_date: '2026-04-28'` (confirmed at
`provenance.py:2493-2509`). The Abbott 2017 ApJL primary-source PDF
quotes "between [-3×10⁻¹⁵, +7×10⁻¹⁶] times the speed of light", which
matches the L1 abstract claim verbatim (`paper_draft.tex:104`).

### 3. Placeholder theorems cited as verified → Gate 5 LeanProofSubstance

**No findings.**

All 12 distinct Lean-theorem references in L1 (`c_GW_pos`,
`c_GW_at_chi_one`, `c_GW_deviation`, `c_GW_deviation_strict_mono`,
`LigoSatisfied`, `c_GW_match_iff_chi_close_to_one`,
`natural_lower_violates_ligo`, `natural_upper_violates_ligo`,
`vestigial_natural_range_violates_ligo`,
`natural_range_disjoint_from_ligo_window`,
`second_sound_graviton_natural_range_universally_falsified`,
`H_VestigialModeIsGraviton` + 4 `_fails_at_*` falsifiers) resolve to
live declarations in `lean/SKEFTHawking/GravitationalWaves.lean`.
Spot-check of bodies:

- `c_GW_match_iff_chi_close_to_one` (L153-180): substantive nlinarith
  proof with sq_nonneg hints; not a placeholder.
- `natural_range_disjoint_from_ligo_window` (L284-291): substantive
  rcases proof reducing to two endpoint falsifiers (not tautological).
- `H_VestigialModeIsGraviton_fails_at_quarter` (L363-376): substantive
  arithmetic proof: rewrites `Real.sqrt (1/4) = 1/2`, then
  `|1/2 - 1| = 1/2 ≥ 1/2` contradicts the bundle's strict `< 1/2`.
  This is the load-bearing P3' independence witness.
- `vestigial_dispersion_below_ligo_at_inspiral_peak` (L442-450):
  substantive `mul_le_mul_of_nonneg_right` + linarith on placeholder
  `γ ≤ 1e-30 → |γ·100| ≤ 1e-28 ≤ 3e-15`. Conditional on the
  placeholder, not vacuous.

No theorem in this module is on the `PlaceholderMarker` set (verified
via tail-of-the-graph: no `rfl`/`trivial`/`Equiv.refl` body on a
non-trivial statement). The pre-existing project rule (per
`feedback_post_wave_strengthening_audit.md`) that retired
`second_sound_graviton_not_derived_DOF` (∃-absorption tautology) is
correctly honoured — the retirement is noted in the paper prose
(L238-243) and the theorem is genuinely absent from the Lean source.

### 4. Cross-paper contradictions → Gate 2 CrossPaperConsistency

**No findings.**

Cross-bundle anchors L1↔D3 §6 are character-for-character consistent
on the five load-bearing claims (per bundle anchor list §L1 +
session-2 round-1 stage10 verification):

- GW170817 cap: L1 abstract L40 `|Δc/c| ≤ 3×10⁻¹⁵` ↔ D3 L612
  `|Δ c/c| \le 3\times 10^{-15}`. ✓
- Natural range: L1 abstract L41 + body L128, L162 `χ_vest ∈ [0.1,
  10]` ↔ D3 L593 `\chi_{\mathrm{vest}} \in [0.1, 10]`. ✓
- Falsification factor at χ=10: L1 body L177 `≈ 7.21×10¹⁴` ↔ D3 L473
  `\approx 7.21\times 10^{14}`. ✓
- Falsification factor at χ=0.1: L1 body L170 `≈ 2.28×10¹⁴` ↔ D3 L474
  `\approx 2.28\times 10^{14}`. ✓
- Δc/c range: L1 abstract L43 `[-0.68, +2.16]` ↔ D3 L614
  `[-0.68, +2.16]`. ✓
- Disjointness theorem name: L1 L54 + L245
  `natural_range_disjoint_from_ligo_window` ↔ D3 L621 same. ✓
- Frame-independent theorem name: L1 L248
  `second_sound_graviton_natural_range_universally_falsified` ↔ D3
  L631 same. ✓

Shared bibkeys L1↔D3 (Volovik2024Vestigial, Abbott2017GW170817,
Vergeles2025, VladimirovDiakonov2012) all resolve to same registry
entry (verified above).

α_ADW SM range L1 L296 `α_ADW^* ∈ [0.40, 1.27]` ↔ paper23 L53
`\aADW \in [0.40, 1.27]`. ✓

L1's reference to the placeholder Γ_H/c² ~ 10⁻³⁰ at L285 cross-links
to `SecondOrderSK.GammaH` (which exists at
`SecondOrderSK.lean:401`); the Lean source `GravitationalWaves.lean`
docstring at L437 also names it as a "Phase 6a Wave 2 placeholder",
matching the paper prose verbatim.

D3 §8 "honest smooth-vs-discrete framing" (added Wave 2) is not
referenced by L1 — L1 is exclusively about the vestigial-second-sound
identification and does not make claims about discrete-spectrum
gravitons; no cross-bridge consistency check is triggered.

### 5. Narrative overclaims → Gate 7 NarrativeGrounding

#### 5.1 — 🔵 RECOMMENDED — Forensic reference to retired theorem in published prose

- **Gate:** NarrativeGrounding
- **Location:** `paper_draft.tex:234-243`
- **Observed:** Paragraph "Phase~5y H1 caveat as a Lean disjointness
  theorem" contains a forensic explanation of the retirement of the
  prior theorem `second_sound_graviton_not_derived_DOF`. Verbatim:
  "The original `\texttt{second\_sound\_graviton\_not\_derived\_DOF}`
  theorem (a ∀ c, ∃ χ_vest existential discharged trivially by
  χ_vest = 1) was retired during the 2026-04-25 audit as an
  ∃-absorption tautology — the existence of one consistent value does
  not encode underdetermination, which is the substantive Phase 5y H1
  finding."
- **Evidence:** L1 contains 1 occurrence of `second_sound_graviton_not_derived_DOF`
  (counted via `grep -c "second.sound.graviton"`); a `grep` of the
  Lean source confirms the theorem is absent. The replacement
  `natural_range_disjoint_from_ligo_window` is correctly cited.
- **Expected:** Published-prose papers rarely include audit forensics
  for retired-internal-theorems. The replacement theorem stands on
  its own; the prior-art history of internal retirements does not
  add reader value and slightly invites scrutiny of the project's
  internal audit cadence.
- **Fix (RECOMMENDED, not gating):** Compress L238-243 to a single
  sentence: "We formalize this caveat directly as the disjointness
  theorem `natural_range_disjoint_from_ligo_window`: for χ_vest at
  either natural-range endpoint {0.1, 10}, no c-rescaling gives a
  GW170817-compatible Δc/c." The Phase 5y H1 deep-research framing in
  L235-237 is the substantive content; the retirement-history is
  inside-baseball.
- **Cache:** N/A.
- **Severity rationale:** RECOMMENDED — the retirement was the
  audit-disclosure that this paper now honestly encodes. Some
  reviewers will value the explicit retirement-history; others will
  see it as project-internal noise that the reader doesn't need.
  Trade-off is editorial.

#### 5.2 — 🔵 RECOMMENDED — Placeholder Γ_H value disclosed honestly but conditional claim could be sharpened

- **Gate:** NarrativeGrounding (project anti-placeholder discipline)
- **Location:** `paper_draft.tex:281-287` (§Discussion item 3
  "Frequency-dependent χ_vest")
- **Observed:** Verbatim: "The SK--EFT dispersion correction
  (formalized in `\texttt{SecondOrderSK.lean}`) is the leading
  frequency dependence in the present framework; at the placeholder
  Γ_H/c_GW² ~ 10⁻³⁰ value it does not lift the falsification, but a
  derivation of Γ_H from a microscopic matching is open."
- **Evidence:** The placeholder is also explicitly named in the Lean
  source: `GravitationalWaves.lean:436-441` docstring "At the Phase
  6a Wave 2 vestigial-regime placeholder γ ≤ 1e-30 and probe
  frequency ω = 100 Hz (GW170817 inspiral peak), the dispersion
  correction |γ·ω| ≤ 1e-28, well below tolGW170817 = 3e-15." The
  Lean theorem `vestigial_dispersion_below_ligo_at_inspiral_peak`
  has a substantive proof conditional on the `|γ| ≤ 1e-30`
  hypothesis (mul_le_mul + linarith). The paper's load-bearing
  leading-order claim (the natural-range falsification at
  Δc/c = √χ - 1) is *independent* of Γ_H so the placeholder is not
  papering over a missing artifact for the main result.
- **Expected:** Per the project policy override ("if you see prose
  like... 'provisional module name, not yet shipped' or any
  placeholder/footnote that papers over a missing artifact, flag it
  as a BLOCKER"), the question is whether this prose is
  load-bearing for L1's claims. Verdict: the main result does NOT
  depend on Γ_H; only the *Discussion §recovery-path 3* depends on
  Γ_H, and there the placeholder is explicitly named and the
  discussion is about why even subleading corrections cannot rescue
  the identification. This is honest hedging, not papering-over.
  Below-BLOCKER threshold.
- **Fix (RECOMMENDED, not gating):** Optional: replace "at the
  placeholder Γ_H/c² ~ 10⁻³⁰ value" with "at the project's current
  conservative upper bound |γ| ≤ 10⁻³⁰ (derivation of Γ_H from
  microscopic matching is open)". The word "placeholder" appearing
  in a published PRL is unusual and may invite reviewer
  pushback even when honestly disclosed; the upper-bound framing
  is mathematically equivalent and less semantically loaded. The
  Lean theorem name `vestigial_dispersion_below_ligo_at_inspiral_peak`
  also uses "placeholder" in its docstring; a coordinated rename
  to "conservative bound" would be the broader fix.
- **Cache:** N/A.
- **Severity rationale:** RECOMMENDED — the artifact exists and is
  substantive (per Gate 5 spot-check above); the "placeholder"
  framing is honest. Project-policy override targets prose that
  *papers over* a missing artifact; this prose acknowledges the
  derivation gap explicitly.

#### 5.3 — 🔵 RECOMMENDED — "21 theorems and lemmas" language imprecise (0 lemmas)

- **Gate:** NarrativeGrounding
- **Location:** `paper_draft.tex:308-309` (§Reproducibility)
- **Observed:** "The Lean layer is `GravitationalWaves.lean` (21
  theorems and lemmas, zero `sorry`, zero new axioms, zero
  `maxHeartbeats` overrides)".
- **Evidence:** `grep -c "^theorem"` returns 21; `grep -c "^lemma"`
  returns 0. The L49 abstract says "21 theorems" without "and
  lemmas". Internal language inconsistency (abstract → "21
  theorems"; §Reproducibility → "21 theorems and lemmas") plus
  imprecision (no lemmas exist).
- **Expected:** Either "21 theorems" (matches abstract + actual
  count) or "21 theorems and definitions" (= 21 + 8 = would change
  the headline number).
- **Fix:** §Reproducibility L308 → "21 theorems" (drop "and
  lemmas").
- **Cache:** N/A.
- **Severity rationale:** RECOMMENDED — minor language drift; does
  not affect the headline count or reader's understanding. A
  reviewer who fact-checks via `grep -c "^lemma"` would notice.

No `first-claim` patterns surface in L1 (`grep -n "first.*formal\|first.*proof\|
first.*Lean\|first.*machine\|first.*compute\|first.*verif"` returns
no matches; the only `first` occurrences are "first principles"
L94/L236, both interpretive prose, not first-formalization claims).

No `unification-claim`, `feasibility-claim`, or
`simulation-evidence-claim` overclaims surface.

### 6. Undisclosed assumptions → Gate 6 AssumptionDisclosure

**No findings.**

The two non-trivial hypothesis structures in L1 are:

- **`H_VestigialModeIsGraviton` 3-conjunct bundle (P1 ∧ P2 ∧ P3'):**
  Disclosed in §Tracked-hypothesis bundle (L206-232) with verbatim
  conjunct breakdown (positivity, GW170817 compatibility,
  sub-half-deviation) and the 4 falsifier theorems naming each
  failure mode.
- **`hχ : 0 ≤ χ_vest` precondition on `c_GW_match_iff_chi_close_to_one`:**
  Disclosed implicitly via "for χ_vest ≥ 0" at L150 (theorem
  statement preamble).

Both are explicit. The natural-range premise χ_vest ∈ [0.1, 10] is
introduced as a prior (L123-134) and acknowledged as projection-tier
(not derived); this is the load-bearing hypothesis and it is
prominently disclosed.

### 7. Count literal drift → Gate 9 NumericalFreshness

**No findings (carryover 7.1 remains accepted-with-rationale).**

L1 has 2 inline count literals — L49 "21 theorems" + L308 "21
theorems and lemmas". Both correspond to a verifiable per-module
count (`grep -c "^theorem" GravitationalWaves.lean = 21`). The
canonical macros `\totaltheorems{5855}` /
`\substantivetheorems{5830}` reflect *project-wide* counts; L1's "21
theorems" refers specifically to `GravitationalWaves.lean` module
count, not project-wide. No project-wide macro exists for the
per-module count of `GravitationalWaves.lean`, which is why the
literal persists.

`validate.py --check count_literals` (run 2026-05-11): PASS with
WARN-class drift on 17 papers but **does not scan L1** — bundle
papers are scoped separately. Carryover 7.1 acceptance-with-rationale
(ledger 2026-05-01) is unchanged: the literals are verifiable via
the documented grep recipe.

`validate.py --check paper_provenance` (run 2026-05-11): PASS.

No Gate 9 BLOCKER or REQUIRED triggered. Sub-finding 5.3 above
flags a language imprecision in the same L308 inline literal
(RECOMMENDED only).

### 8. Production run health → Gate 8 ProductionRunHealth

**No findings.**

L1 makes no Monte Carlo / RHMC / production-run-based numerical
claims. The dispersion correction's "10⁻³⁰" is a conservative bound,
not a measured/computed value from a production run. The Lean
theorem `vestigial_dispersion_below_ligo_at_inspiral_peak`
discharges its conclusion *conditionally* on `|γ| ≤ 10⁻³⁰`, not by
simulation evidence. No `ProductionRun` graph node is required to
back any L1 claim.

The headline figure
`figures/fig_c_GW_vs_ligo_constraint.png` (2400×1600 PNG, 8-bit
RGBA) is regenerated from
`src/core/visualizations.py:fig_c_GW_vs_ligo_constraint` (existing,
verified at line 8501); the underlying numerical values are
analytic (`sqrt(χ) - 1` over χ ∈ [0.1, 10]), not Monte Carlo. Gate 8
does not apply.

---

## QI Candidate

**Pattern (registry-hygiene observation, surfaced during fresh-fetch
Gate 1 sweep):** The `Wetterich2022` registry entry in
`src/core/citations.py` has both wrong title ("Spinor gravity and
gauge symmetry" registry truth vs. "Pregeometry and spontaneous
time-space asymmetry" primary source) and wrong arXiv ID
(`'2110.11138'` registry truth vs. that ID resolving to a different
paper "The emergence of gapless quantum spin liquid from deconfined
quantum critical point" by Liu et al, 2021). The L1 bibitem is
title-less so the wrong registry title does not appear in the
rendered PDF, and L1's reader-facing artifact is therefore not
affected — but the registry record is incorrect and would propagate
silently to any future paper that includes the title field in its
own bibitem text.

**Recommended QI register entry:** Add a `validate.py --check
registry_primary_source_match` that, for every CITATION_REGISTRY
entry with a populated `doi` AND `doi_verified=True/None`,
fresh-fetches Crossref and asserts registry-title matches primary
source title (within a known-aliases tolerance). The check would
have caught Wetterich2022 + similar drift cases. Severity:
REQUIRED at submission; WARN at draft. The companion check
`registry_arxiv_id_resolves` would catch the wrong-arXiv issue —
fresh-fetch `arxiv.org/abs/<id>` for every registry entry with
populated `arxiv` field; assert returned author list matches
registry author list (or a structural-similarity threshold).

Also surfaced: the existence of duplicate `Akama1978` and
`Volovik2024Vestigial` keys in `src/core/citations.py` (lines
790/3011 + 632/3080). Python's `dict` literal takes the LAST value,
which is fortunate (the later entry is the corrected one for
Akama1978), but the duplication itself is a maintenance risk. A
`validate.py --check registry_no_duplicate_keys` would catch this
class of issue. Both QI candidates are project-wide infrastructure
work, not L1-specific.

**Carryover QI from round-2 (still applicable):** Extend
`paper_lean_refs_live` validate.py check to enumerate every
`\verb|*|` / `\texttt{*}` Lean-name reference in the entire paper
file. (Round-2's regression `3.1.b` was an abstract-only stale
reference that the existing check missed.) A round-2-style
single-section grep would catch the same pattern; the broader audit
ensures abstract/body parity.

**Carryover QI from round-3 (still applicable):** Add
`paper_latex_compile_clean` validate.py check fail-on-any-LaTeX-error.
Round-3 reported pre-existing `\begin{theorem}` env undefined errors;
Session-2 (2026-05-06) fixed L1 specifically (added amsthm +
\newtheorem), but a project-wide check would catch the same pattern
in the other 12 bundles before submission.

---

## Status of carryover items

- **2.1 (chi_vest provenance human-verify):** REQUIRED, OPEN.
  Re-flagged as finding 2.1 above. User-action item.
- **7.1 (inline count literals):** REQUIRED, ACCEPTED-WITH-RATIONALE
  per ledger entry `review:2026-05-01-bundle-stage13:L1:7.1`. Round-5
  does not re-open. Finding 5.3 surfaces a related language
  imprecision in the SAME literal occurrence, but only at
  RECOMMENDED severity.
- **Round-3 QI on `paper_latex_compile_clean`:** Carryover, re-noted
  in QI section above. L1's local fix (Session-2: amsthm +
  \newtheorem) is verified still in place.

---

## Verdict

**GREEN** (zero open BLOCKERs from the Stage 13 review chain).

The bundle remains submission-ready at Stage-13 level, modulo the
known user-action 2.1 ParameterProvenance human-verification gate.

**Submission-readiness blockers after this round:** none from the
adversarial review pipeline.

**Submission-gate carryovers (non-bundle-state):**
1. (2.1) ParameterProvenance human-verification of
   `chi_vest_natural_lower` / `chi_vest_natural_upper` via
   `scripts/provenance_dashboard.py` (user action).

**Recommended in-place edits (all RECOMMENDED, none gating):**
- §Reproducibility L308: drop "and lemmas" from "21 theorems and
  lemmas" (matches abstract + actual count) — finding 5.3.
- §Discussion item 3 L284-285: optionally re-frame "placeholder
  Γ_H/c² ~ 10⁻³⁰" as "conservative bound |γ| ≤ 10⁻³⁰" — finding 5.2.
- §Phase 5y H1 caveat L238-243: optionally compress retired-theorem
  forensics into a single sentence — finding 5.1.

**QI candidates raised (project-wide infrastructure):**
- `registry_primary_source_match` validate.py check (Wetterich2022
  drift case).
- `registry_arxiv_id_resolves` validate.py check (Wetterich2022 wrong
  arXiv ID case).
- `registry_no_duplicate_keys` validate.py check (Akama1978,
  Volovik2024Vestigial duplicate keys).
- (Carryover from rounds 2/3) `paper_lean_refs_live` whole-paper
  enumeration + `paper_latex_compile_clean` LaTeX-error-fail.
