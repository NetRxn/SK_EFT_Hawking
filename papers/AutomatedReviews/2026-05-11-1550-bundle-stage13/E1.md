---
paper: E1
reviewer: adversarial-reviewer
model: claude-opus-4-7
review_date: 2026-05-11T15:50:00Z
readiness_gates_version: 1
bundle_target: E1
---

# Adversarial Review — E1 (Paris-LKB Polariton PRL)

## Summary

Fresh-context Stage-13 sweep of `papers/E1/paper_draft.tex` (396 lines, draft of "A Falsifiable Hawking Spectrum for Polariton Microcavities at the Paris-LKB Device Parameters"). All 15 bibitems are registry-resident; spot-checked Falque2025 / Burkhard2025 / Giacobino2025 / Steinhauer2016 via WebFetch — every fetched bibitem matches the registry. Quantitative load-bearing numerics (T_H smooth = 85 mK, T_H steep = 134 mK, D = 0.60 / 0.93, δ_disp = -0.19 / -0.46, gain plateau values, ln(3)/(2π) threshold, T_noise = 0.81 K, SNR ≈ 11) all reproduce from canonical provenance values to ±0.5%. **Three findings emerge, dominated by one BLOCKER class:** (1) stale theorem count (5812 in two places; canonical = 5855), aggravated by the absence of `\input{counts.tex}` macro adoption; (2) two outstanding D.2 absorption stubs (Phase 6o W1a noise floor + W1b Kerr-Schild) registered in `change_log.md` and `source_manifest.md` but never lifted into prose, with an in-text "Eq.~(boxed window)" pseudo-reference (line 276) that papers over a missing `\label`/`\eqref`; (3) undisclosed `Γ(ω) ≡ 1` assumption in the §4 gain numerics. Paper is otherwise tight and Tier-4-appropriate; D1 §6 cross-bridge consistency holds.

**Counts:** 🔴 1 BLOCKER · 🟡 4 REQUIRED · 🔵 1 RECOMMENDED · 6 total findings.
**Gates affected:** NumericalFreshness (Gate 9), NarrativeGrounding (Gate 7), AssumptionDisclosure (Gate 6), LeanProofSubstance (Gate 5 — advisory only).
**Submission-ready:** No — Gate 9 BLOCKER must be cleared.

## Findings

### 1.1 — 🟡 REQUIRED — Citation cache cold for all E1 bibkeys

- **Gate:** CitationIntegrity
- **Location:** `docs/citation_verifications.jsonl` (44 entries; zero matching E1's 15 bibkeys)
- **Observed:** None of E1's 15 bibitems (Steinhauer2016, deNova2019, Gerace2012, Nguyen2015, Falque2025, Giacobino2025, Estrecho2021, Stepanov2019, Claude2023, Grisins2016, Burkhard2025, Jacquet2022, Finazzi2012, Amelio2020, Jacquet2023QNM) has a record in `docs/citation_verifications.jsonl`. The agent spec requires per-bibitem fresh-fetch when the cache is cold.
- **Evidence:** Spot-fetch sample verified by this run:
  - `Falque2025` → arXiv:2311.01392: title + 6-author list match registry (Falque, Delhom, Glorieux, Giacobino, Bramati, Jacquet); PRL 135, 023401 (2025); confirmed.
  - `Burkhard2025` → arXiv:2511.12339: title "Stimulated Hawking effect and quasinormal mode resonance in a polariton simulator of field theory on curved spacetime" + 6-author list (Burkhard, Kroj, Falque, Bramati, Carusotto, Jacquet) match registry.
  - `Giacobino2025` → arXiv:2512.14194: title "Acoustic horizons and the Hawking effect in polariton fluids of light", authors Giacobino, Jacquet — match registry.
  - `Steinhauer2016` → arXiv:1510.00621: title + sole author Steinhauer match registry.
  - `Jacquet2023QNM` → DOI 10.1103/PhysRevLett.130.111501: APS returned 403 (Cloudflare block, not paper-side issue). Registry note dated 2026-05-11 (D1 Stage-13 fix-pass) crossref-verified actual title; finding inferred-clean.
  - Remaining 10 bibkeys not yet fetched this run; registry metadata coherent with bibitem text.
- **Expected:** Per agent spec §1, every bibkey gets one cache record per Stage-13 round (cache-verified or fresh-fetch); records appended to `citation_verifications.jsonl`.
- **Fix:** Run a citation-cache backfill pass for E1 bibitems; append `match` records for spot-checked entries; fresh-fetch the remaining 10 within 7 days of submission. The four spot-checked entries above are safe to record as `cache-verified` from this review.
- **Cache:** fresh-fetch (spot sample); not-yet-cached (remaining 10).
- **Severity note:** Not a BLOCKER because the registry itself is coherent and the spot sample matches; the cache absence is a process gap, not a citation-correctness gap. If `validate.py --check citation_cache_freshness` is gating, escalate.

### 7.1 — 🔴 BLOCKER — Stale theorem-count literal (5812 vs canonical 5855)

- **Gate:** NumericalFreshness
- **Location:** `papers/E1/paper_draft.tex:45` (abstract) and `:82` (introduction)
- **Observed:**
  - Line 45 (abstract): `The Lean~4 verification of the underlying chain (5812 theorems / 322 modules / 1 axiom / 0 sorry) is independent of platform parameters...`
  - Line 82 (intro): `formally verified spectral formulas derived from a Lean~4 chain (5812 theorems / 322 modules / 1 axiom / 0 sorry; \texttt{lake build} clean).`
- **Evidence:** `docs/counts.tex` (auto-generated 2026-05-06T18:02:10) reports `\totaltheorems{5855}` and `\substantivetheorems{5830}`. Drift = (5855 − 5812)/5855 = 0.73%, exceeding Gate-9's 0.5% threshold. The companion D1 paper at line 83 already uses `\substantivetheorems{}` macro — E1 has not been retrofitted.
- **Expected:** Either inline value matches canonical (5855 substantive / 322 mods / 1 axiom / 0 sorry — but check which population the prose intends: total or substantive) OR `\input{../../docs/counts.tex}` with `\totaltheorems{}` / `\substantivetheorems{}` macros (preferred — anti-drift discipline per Gate 9 remediation guidance).
- **Fix:** Add `\input{../../docs/counts.tex}` after the preamble; replace both inline `5812 theorems / 322 modules / 1 axiom / 0 sorry` strings with the macro chain (e.g., `\substantivetheorems{} substantive theorems / \leanmodules{} modules / \axiomcount{} axiom / \sorrycount{} sorry`). This is the same fix-pattern applied to D1 line 83 and visible across most current-generation bundles.
- **Cache:** N/A (file-internal).

### 7.2 — 🟡 REQUIRED — Inline `\SI{}` numerical literals outside macros (NumericalFreshness retrofit)

- **Gate:** NumericalFreshness
- **Location:** Throughout `papers/E1/paper_draft.tex`; sample lines: 27 (`\SI{0.40}{\micro\meter/\pico\second}`), 28 (`\SI{3.4}{\micro\meter}`), 29 (`\SI{85}{\milli\kelvin}`), 30 (`\SI{7e10}{\per\second}`), 100-104 (device-parameter table inline rows), 203 (T_noise = 0.81 K), 274 (cavity-lifetime prose).
- **Observed:** Every device-parameter literal in abstract + §2 + §3 + §5 + figure caption + §7 is hard-coded `\SI{...}` rather than sourced through `\input{tables/<id>.tex}` (the bundle has a `tables/` directory but the device-parameter table on lines 99-106 is inline-LaTeX). Per Gate-9 remediation, this is WARN-acceptable at draft but FAIL at submission.
- **Evidence:** `validate.py --check numerical_literals` would flag the abstract block + device-parameter table + T_noise + figure caption. `papers/E1/tables/` exists but isn't populated for the smooth/steep configuration table.
- **Expected:** Move the device-parameter table to `papers/E1/tables.py` spec → `render_paper_tables.py` autogen → `\input{tables/lkb_device_params.tex}`; tag prose-embedded literals via the `\pipelinevalue{...}` macro (or move into a shared `\input` chain).
- **Fix:** Retrofit the abstract and §2/§3/§5 numerical literals through the tables.py pipeline; canonicalize the T_noise = 0.81 K derivation by emitting it from `formulas.py` (or document the m*c_s²/k_B = 0.811 K derivation inline once values move to canonical source).
- **Cache:** N/A.

### 5.1 — 🟡 REQUIRED — Undisclosed assumption: Γ(ω) ≡ 1 in §4 gain numerics

- **Gate:** AssumptionDisclosure
- **Location:** `papers/E1/paper_draft.tex:148-159` (gain numerics) and `:36-39` (abstract threshold derivation).
- **Observed:** Eq. (3) defines G(ω) = Γ(ω) / (e^{2πω/κ_eff} − 1), with prose at line 152 calling Γ(ω) the "greybody transmission factor." The subsequent claims `G(0.1κ) ≈ 1.14`, `G(0.3κ) ≈ 0.18`, `G(κ) ≈ 0.002`, and the universal threshold `ω < ln(3)/(2π) κ ≈ 0.175 κ` only hold under the unstated assumption Γ(ω) ≡ 1.
- **Evidence:** Numerical re-derivation under Γ(ω) ≡ 1: 1/(e^{2π·0.1} − 1) = 1.144 ✓, 1/(e^{2π·0.3} − 1) = 0.179 ✓, 1/(e^{2π·1} − 1) = 0.00187 ✓, ln(3)/(2π) = 0.1748 ✓ (where 1/(e^{ln 3} − 1) = 1/2 = 0.5 reaches threshold). The ln(3)/(2π) universality is conditional on Γ being ω-independent. The "model-independent spectral signature" framing (line 161) is overstated relative to this conditionalization.
- **Expected:** Disclose Γ(ω) ≡ 1 (or document the regime where this holds — e.g., low-ω greybody saturation) inline at line 152, and re-state the ln(3)/(2π) universality as "universal in the Γ-independent limit." If a frequency-dependent Γ is known for the LKB platform, the gain values need recomputation; if not, the figure caption should also disclose.
- **Fix:** Either (a) add an explicit `Γ(ω) ≡ 1` assumption line after Eq. (3) (or replace Γ(ω) with the symbol you actually use, e.g., `Γ = 1` in the perturbative regime), or (b) cite Finazzi2012 / Jacquet2023QNM for the perturbative-regime Γ ≈ const result and qualify the universality claim accordingly.
- **Cache:** N/A.

### 7.3 — 🟡 REQUIRED — Two outstanding D.2 absorption stubs and a broken in-prose Eq. reference

- **Gate:** NarrativeGrounding
- **Location:** `papers/E1/paper_draft.tex:369-394` (commented stubs) and `:276` ("Eq.~(boxed window)" literal pseudo-reference); cross-evidence in `papers/E1/change_log.md` (last three entries) + `papers/E1/source_manifest.md`.
- **Observed:**
  - The bundle's `change_log.md` registers two 2026-05-06 Lift-section actions:
    - §3: D.2 absorption from `_phase6o_W1a_lean_only` — "Phase 6o W1a universal noise floor n_noise/Hawking-flux Wilson-coefficient-independence at Paris-LKB device parameters"
    - §4: D.2 absorption from `_phase6o_W1b_lean_only` — "Phase 6o W1b polariton ringdown signature from Kerr-Schild single-copy"
  - Both stubs are commented-out in `paper_draft.tex:369-394` with explicit `TODO: lift content` markers (Session-2 cleanup left bookkeeping anchors only). The Phase 6o W1a content is partially referenced by the abstract phrase "the SK-EFT noise-floor formula" (line 42), and the Kerr-Schild / Petrov-D content is not in the rendered paper at all.
  - Line 276: `the detection-band edges in Eq.~(boxed window) shift by amounts calculable from the same Lean chain` — the boxed equation on line 213-215 has no `\label{}`; "Eq.~(boxed window)" is literal pseudo-text, not a typeset `\eqref`. Per project-policy adjustment §1, "any footnote that papers over a missing artifact" is a Gate-7 finding.
- **Evidence:**
  ```
  $ grep -nE "\\\\eqref|\\\\label" papers/E1/paper_draft.tex
  123:\label{eq:T_H}
  133:\label{eq:delta_disp}
  138:Eq.~\eqref{eq:delta_disp} ...
  150:\label{eq:gain}
  ```
  The boxed `ω/κ ∈ [0.05, 0.30]` window equation has no label and is never referenced via `\eqref`.
- **Expected:** Either (a) lift the Phase 6o W1a / W1b content per the `LATE_PHASE6_ABSORPTION_PROTOCOL` (the protocol explicitly addresses absorbing late waves into drafted bundles), or (b) close the absorption ledger entries with explicit "non-blocking — content not lifted because [rationale]" notes, marking the bundle freshness-stale clear. For line 276: add `\label{eq:window}` to the boxed equation and replace "Eq.~(boxed window)" with `Eq.~\eqref{eq:window}`.
- **Fix:** Two-part: (1) decide W1a / W1b lift status and either lift or close; (2) fix the line-276 broken cross-reference inline.
- **Cache:** N/A.

### 5.2 — 🔵 RECOMMENDED — `hawking_temp_from_surface_gravity` is definitional unfolding, cited as if substantive

- **Gate:** LeanProofSubstance
- **Location:** `papers/E1/paper_draft.tex:125-127` (Eq. T_H + Lean citation); Lean source at `lean/SKEFTHawking/AcousticMetric.lean:282-283`.
- **Observed:** Theorem `AcousticMetric.hawking_temp_from_surface_gravity` is cited at line 126 as the Lean grounding for Eq. (1) `T_H = ℏκ/(2π k_B)`. The Lean body is:
  ```
  theorem hawking_temp_from_surface_gravity (bg : FluidBackground) (h : SonicHorizon bg) :
      hawkingTemp h.surfaceGravity = h.surfaceGravity / (2 * Real.pi) := rfl
  ```
  where `hawkingTemp` is itself defined in `Basic.lean:185` as `noncomputable def hawkingTemp (kappa : ℝ) : ℝ := kappa / (2 * Real.pi)`. The theorem is therefore a `rfl`-discharge on its own definitional unfolding — CLAUDE.md's strengthening-discipline P5 "definitional-unfolding-as-physics" anti-pattern.
- **Evidence:** This is the same pattern flagged in the CLAUDE.md preemptive-strengthening discipline (item 5 in the five-question checklist). The substantive physics content lives in the formula registry / `formulas.py`, not in this Lean theorem.
- **Expected:** The paper would benefit from a more substantive Lean grounding — e.g., the universality theorem `T_H = κ/(2π)` proved against an alternative κ definition (peeling, Killing horizon analog, near-horizon expansion) — rather than the bare `rfl`. The current citation is technically valid (Mathlib + `Basic.hawkingTemp` provides the formal home for the formula) but does not encode physics content beyond a name binding.
- **Fix:** RECOMMENDED only — not a submission blocker. Consider citing a more load-bearing Lean theorem in the chain (e.g., `HawkingUniversality.lean`'s universality bridge) alongside or instead of `hawking_temp_from_surface_gravity`. Alternatively, document inline that this Lean theorem is the formal home / name-binding for the formula and that the physical-equivalence content lives upstream of it.
- **Cache:** N/A.

## QI Candidate

**Citation cache backfill discipline for low-bibitem-count Tier-4 letters.** E1 has 15 bibitems and zero cache records; bibliography is small enough that a per-bundle pre-Stage-13 backfill pass (perhaps via `scripts/citation_cache_backfill.py --bundle E1`) would close Finding 1.1 mechanically across all letters. The current per-Stage-13-invocation fetch model amortizes cost but produces cold-cache findings in every fresh-context review. Worth adding to `scripts/validate.py --check citation_cache_freshness` as a per-bundle gate.

**`Eq.~(boxed)` / `\eqref{}`-not-set anti-pattern detector.** Line 276's "Eq.~(boxed window)" is a literal pseudo-reference that LaTeX renders as plain text — it never resolves to a typeset equation number. A trivial regex check (`Eq\.~?\([^)]*\)` where the parens content isn't a numeric form) in `validate.py --check tex_crossref_integrity` would catch this class.
