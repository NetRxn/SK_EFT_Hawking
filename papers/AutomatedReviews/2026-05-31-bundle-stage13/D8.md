---
paper: D8
bundle_target: D8
reviewer: adversarial-reviewer
model: claude-opus-4-8
review_date: 2026-05-31T17:10:00Z
readiness_gates_version: 1
---

# Adversarial Review — D8 (Kernel-Verified Universal Quantum Gate Compilation)

## Summary

Findings: **0 BLOCKER, 2 REQUIRED, 4 RECOMMENDED.** Gates affected:
NarrativeGrounding/FirstClaimVerification (Gate 7/10, REQUIRED) and
AssumptionDisclosure (Gate 6, RECOMMENDED). Citations (Gate 1) are clean —
all 10 keys are in `CITATION_REGISTRY` with cached primary sources; four
load-bearing arXiv IDs were fresh-fetched and matched exactly, including the
documented `AaronsonGottesman2004 = quant-ph/0406196` correction. The three
scrutinized Lean theorems (`channelSde2_le_toffoliCost`, `cliffordCCZLiteral_dense`,
`cliffordOnly_not_dense`) are all substantive — the Toffoli bound is genuinely
unconditional (hC/hCCZ discharged, not smuggled), and every one of the 13
D8-cited declarations verifies with empty `axiom_deps_project` and core axioms
⊆ {propext, Classical.choice, Quot.sound}. No project-local axioms, no sorries,
no private-repo references. **D8 is NOT yet submission-ready**: the two REQUIRED
findings (unhedged "first … theory" claim with zero prior-art positioning
against VOQC/Qbricks; and first-claims lacking a ledger entry) block submission
until remediated, but neither is a BLOCKER and the bundle is in strong shape.

## Findings

### 1.1 — 🔵 RECOMMENDED — `AharonovArad2017` bibkey year suffix is misleading

- **Gate:** CitationIntegrity
- **Location:** `papers/D8/bibliography.bib:27` and `papers/D8/paper_draft.tex:71`
- **Observed:** Bibkey is `AharonovArad2017` but the entry's `year = {2011}`,
  title "The BQP-hardness of approximating the Jones polynomial", NJP 13,
  035019, arXiv:quant-ph/0605181. The `2017` in the key matches nothing in the
  record.
- **Evidence:** `CITATION_REGISTRY` note (`citations.py:6032`): "Bibkey carried
  over from earlier phase (year suffix 2017 inherited); actual publication year
  is 2011 … arXiv ID verified via arxiv.org 2026-05-23." Bib `year` field and
  registry agree on 2011; only the key string disagrees.
- **Expected:** Internally consistent metadata. The bib record itself is
  correct and resolves; this is a cosmetic key-name artifact, not a wrong-target
  citation. Not a Gate-1 blocker (bibitem→primary correspondence holds).
- **Fix:** Optionally rename the key to `AharonovArad2011` across draft + registry
  + bib for clarity. Low priority; the current state is internally documented.
- **Cache:** cache-verified (registry note 2026-05-23) + not re-fetched (not load-bearing for a D8 claim; cited only for the polylog length bound alongside Kitaev/Dawson-Nielsen).

### 1.2 — 🔵 RECOMMENDED — `doi_verified` is `None` for all D8 registry entries

- **Gate:** CitationIntegrity
- **Location:** `src/core/citations.py:6018–6193` (all 10 D8 keys)
- **Observed:** Every D8 entry carries `doi_verified: None`. Gate 1 "passes iff
  every registry entry has `arxiv_verified == True` AND `doi_verified == True`
  (where applicable)."
- **Evidence:** arXiv IDs are notes-documented as web-verified 2026-05-31 and I
  fresh-fetched four (1.x evidence below); but there is no `arxiv_verified`
  boolean field on these entries at all — verification is recorded only in the
  free-text `notes`, and `doi_verified` is `None` even for the four entries that
  carry a DOI (`AharonovArad2017`, `ReadRezayi1999`, `AaronsonGottesman2004` —
  PRA 70.052328). Textbook `KitaevShenVyalyi2002` is correctly DOI-exempt.
- **Expected:** For submission, the three DOI-bearing journal refs should have
  `doi_verified: True` set (or the verification booleans populated), so the
  Gate-1 evaluator can pass them mechanically rather than relying on prose notes.
- **Fix:** Run the DOI redirect check on the three DOIs and set the flags; or
  document the "arXiv-only verification sufficient at draft stage" posture
  explicitly as is done for I3's arXiv-only refs. Advisory at draft stage.
- **Cache:** four arXiv fetches appended to `docs/citation_verifications.jsonl`
  this run (AaronsonGottesman2004, Mukhopadhyay2024, Brylinski2002, BMPRV1999),
  all verdict `match`.

### 3.1 — (no finding) — Scrutinized theorems are substantive

- **Gate:** LeanProofSubstance — **PASS, no finding.** Recorded for the audit trail.
- `channelSde2_le_toffoliCost` (`MukhopadhyayToffoliUnconditional.lean:161`):
  takes only `hU : IsExactlyCliffordCCZ U`; the bound is over `csInf` of all gate
  words (`le_csInf`), with the per-word bound `toffoliCount_ge_channelSde2`
  proven by induction. The Clifford-preservation (`hC`) is discharged via
  `channelSde2_clifford_le` (signed-permutation structure, `matrixSde2_signedPerm_mul_le`),
  and the CCZ-increment rationality (`hCCZ`) via `channelRep_interp_isRat` (a real
  induction over gate words). **The hC/hCCZ hypotheses are genuinely discharged,
  not re-introduced as theorem hypotheses.** Unconditional claim is real.
- `cliffordCCZLiteral_dense` (`CliffordCCZSU8Density.lean:130`): discharges via
  `densityFromWitness` applied to a constructed witness (Clifford-orbit spanning
  set, lines 100–113). Not `rfl`/tautology.
- `cliffordOnly_not_dense` (`MukhopadhyayCliffordNotDense.lean:142`): genuine
  finiteness-vs-infinite-order contradiction (channel rep maps Clifford words
  into a finite signed-permutation set; seed has infinite order). Not a tautology.
- All 13 cited headlines verified against `lean/lean_deps.json`: each exists,
  `axiom_deps_project = []`, `axiom_deps_core ⊆ {propext, Classical.choice,
  Quot.sound}`. `counts.json` (2026-05-30): axioms 0, sorry 0.

### 5.1 — 🟡 REQUIRED — Unhedged "first … as a theory" claim with no prior-art positioning against verified-QC frameworks

- **Gate:** NarrativeGrounding (Gate 7) / FirstClaimVerification (Gate 10)
- **Location:** `papers/D8/paper_draft.tex:27` (abstract), echoed at intro `:81–83`
- **Observed:** "We report the **first** machine-checked development of universal
  quantum gate compilation **as a theory** rather than a single algorithm."
  Stated flatly ("We report the first …"), unlike the SU(d) and Toffoli claims,
  which are softened with "to our knowledge." The draft cites **zero** prior
  verified-quantum-computation-in-proof-assistants work.
- **Evidence:** Prior-art search this run:
  - **VOQC / SQIR (inQWIRE, Coq), POPL 2021** — described in its own materials
    as "the **first fully verified compiler for quantum circuits**" (verified
    optimizer; arXiv:1912.02250; github.com/inQWIRE/SQIR). I fetched the SQIR
    repo: it formalizes *exact* gate decompositions and circuit-optimization
    correctness, with **no** Solovay-Kitaev / approximation / density / synthesis
    to precision. So it does **not** negate the D8 claim's precise content.
  - **Qbricks (Why3/Coq/Isabelle)** — path-sum verified quantum *programs*;
    supports Toffoli but no SK approximation and no Toffoli lower-bound proof
    found.
  - No formalization of the (quantitative) Solovay-Kitaev theorem in any proof
    assistant was found (Lean/Mathlib, Coq, Isabelle/AFP, Agda).
  - **Conclusion:** the claim is *true under its precise reading* (quantitative
    SK / approximation-and-length theory, vs. VOQC's circuit-optimization), and
    no prior work negates it → **not a BLOCKER.** But a verified-QC referee will
    immediately ask "how does this relate to VOQC?" and the draft is silent on
    the entire prior-art line. An unhedged "the first … as a theory" against
    that backdrop is an overclaim risk.
- **Expected:** Either (a) soften line 27 to match the "to our knowledge"
  hedging used elsewhere AND add a sentence positioning the work against
  VOQC/SQIR/Qbricks (verified circuit *optimization/construction*) vs. this
  work's verified *approximation/compilation theory*; or (b) cite them and draw
  the distinction explicitly. The distinction is real and easy to state.
- **Fix:** Add VOQC (arXiv:1912.02250) and Qbricks (arXiv:2110.01320) to the
  bibliography + a 2–3 sentence "related verified-QC work" positioning paragraph
  (intro or scope section), and reword line 27 to "the first machine-checked
  development of quantitative/approximation gate-compilation *theory* … ; prior
  verified-QC efforts (VOQC, Qbricks) target exact circuit optimization and
  construction, not approximation to arbitrary precision."
- **Cache:** prior-art searches this run (web); no ledger node exists (see 5.2).

### 5.2 — 🟡 REQUIRED — First-claims have no FirstClaimLedger entry

- **Gate:** FirstClaimVerification (Gate 10)
- **Location:** `paper_draft.tex:27` ("first machine-checked … theory"), `:38`
  + `:92` + `:180` ("first kernel-verified quantitative Solovay–Kitaev at
  arbitrary dimension"), `:343` ("unconditional resource lower bound … the
  first of its kind").
- **Observed:** Three distinct first-claims; per READINESS_GATES Gate 10, each
  `first-claim`-tagged ProseClaim requires a ledger entry confirming a prior-work
  search. The ledger node type "is not yet implemented … this gate is
  `needs-recheck` for every paper with a first-claim."
- **Evidence:** No `FirstClaimLedger` artifact found for D8. My own searches
  (5.1) found no negating prior work for any of the three, so all three are
  *defensible*, but the gate obligation is procedural and unmet.
- **Expected:** A ledger/record (even a committed `papers/D8/first_claims.md`)
  documenting the prior-art search for each of the three claims, with the
  search terms and the negative result, dated.
- **Fix:** Record this review's three searches (Solovay-Kitaev formalization;
  VOQC/SQIR/Qbricks; Toffoli-lower-bound formalization) as the ledger evidence
  for the three claims; cite the closest prior art per 5.1.
- **Cache:** n/a (narrative).

### 6.1 — 🔵 RECOMMENDED — Generic SU(d) headline is a conditional substrate; abstract phrasing reads unconditional

- **Gate:** AssumptionDisclosure (Gate 6)
- **Location:** `paper_draft.tex:176–181` (abstract/intro phrasing of the SU(d)
  headline)
- **Observed:** The abstract says the SU(d) lift yields, "for every $d\ge2$,
  every $U\in\su d$, and every $\varepsilon>0$, a compilation exists with error
  $\le\varepsilon$ and poly-logarithmic length." The actual theorem
  `solovayKitaev_dawson_nielsen_quantitative_generic_sud_strict_constructive_tight`
  takes two hypotheses: `IsDenseInSUd_gs gs` (density) and
  `WordLengthFreeGroupLike` (length structure). It is a *conditional substrate*,
  not an unconditional universal statement.
- **Evidence:** Type from `lean_deps.json`: `… (gs : GeneratingSet (n+2)) (h_eq …)
  → IsDenseInSUd_gs gs → WordLengthFreeGroupLike … → SolovayKitaevHeadline_FreeGroup_SUd gs h_eq`.
  The §2 prose *does* disclose this honestly ("reduces to the density witness and
  the length bound alone"; "adding a new alphabet is an instantiation problem"),
  and the concrete instances (`trappedIonSU4_…`, `cliffordCCZSU8_…`) discharge
  density and are genuinely unconditional. So this is a disclosed assumption, not
  a hidden one → RECOMMENDED, not BLOCKER.
- **Expected:** Abstract/intro should make the substrate-conditionality explicit
  at first mention (the body already does), so the abstract isn't read as an
  unconditional "for every d, U, ε" claim independent of a density witness.
- **Fix:** In the abstract sentence, append "given a dense generating set" or
  "for any alphabet whose density witness is discharged" to the SU(d) clause.
- **Cache:** n/a.

### 6.2 — 🔵 RECOMMENDED — Lemma 3.10 cited as `channelRep_interp_isRat`; verify the dyadic (ℤ[1/2]) claim, not merely rational

- **Gate:** AssumptionDisclosure (Gate 6)
- **Location:** `paper_draft.tex:332–334`
- **Observed:** Draft: "the channel-representation entries of any Clifford+CCZ
  word are dyadic, lying in $\mathbb Z[1/2]$ (Lemma 3.10,
  `channelRep_interp_isRat`)." The Lean theorem `channelRep_interp_isRat`
  proves `IsRatℂ` (rational), and its docstring says "each entry lies in
  `ℤ[1/2] ⊂ ℚ`" but the *statement* is rationality; the dyadic refinement is
  argued in the docstring/induction structure (signed-perm = {0,±1}, CCZ =
  half-integer), not in the proposition's type.
- **Evidence:** `MukhopadhyayToffoliUnconditional.lean:117` —
  `theorem channelRep_interp_isRat … : IsRatℂ (channelRep (interp gs) r s)`. The
  half-integer facts (`channelRep_CCZ_isRat` via `…isHalfInt`) feed the bound but
  the headline lemma's *type* is `IsRatℂ`, weaker than "∈ ℤ[1/2]".
- **Expected:** The prose "lying in ℤ[1/2]" should either match a theorem whose
  type asserts dyadic membership, or be reworded to "rational (the dyadic
  structure is used in the increment bound)." The unconditional Toffoli bound
  itself does not depend on the stronger dyadic *statement* (it threads through
  `channelSde2_ccz_le`), so this is a precision-of-prose issue, not a correctness
  gap.
- **Fix:** Reword the parenthetical to match the `IsRatℂ` statement, or add an
  explicit ℤ[1/2]-membership corollary if the dyadic claim is to be cited verbatim.
- **Cache:** n/a.

### 7/8 — (light, as scoped) — No volatile counts, no production runs

- **Gates:** NumericalFreshness (Gate 9) / ProductionRunHealth (Gate 8) — **no findings.**
- The draft is a sourceless (D.4) theory bundle: it carries no count-literal
  macros (`\totaltheorems`/`\leanmodules` etc. are absent), no `tables/*.tex`
  numeric inputs (the `tables/` dir is empty), and no ProductionRun-backed
  numerical-evidence claims (no "Monte Carlo evidence" / simulation prose). The
  only numerics are mathematical constants (length exponent `log5/log(3/2)≈3.97`
  — verified honest per anchor; the Chebyshev identity `T_7(x)+1 =
  (x+1)(8x^3-4x^2-4x+1)^2` — I checked numerically to machine precision; trace
  `1/√2`). Per the task scope, these gates are correctly light for D8.

## Anchor-list specific verifications (all PASS)

- **SU(8) duality honesty (load-bearing):** Draft `:276–307` keeps Route A
  ($T$-based, "CCZ … *not* load-bearing", explicitly flagged) distinct from
  Route B ($T$-free, CCZ-essential). CCZ-essentiality is attributed ONLY to the
  6z literal route, backed by `cliffordOnly_not_dense`. No conflation. **PASS.**
- **Unconditional vs parametric Toffoli bound:** Stated unconditional
  (`channelSde2_le_toffoliCost`), with the parametric→discharged transition
  named (`:340–342`). Conjecture 4.8 (full minimality / upper bound) explicitly
  OUT of scope (`:350–352`). No overclaim. **PASS.**
- **Length-exponent honesty:** `log5/log(3/2)≈3.97` stated, with explicit
  rejection of the `log5/log2≈2.32` form as requiring an unproven quadratic
  contraction (`:216–227`). Matches anchor `skLengthExponent_sud`. **PASS.**
- **Public/private seam:** Scope section keeps runtime compiler, vendor tuning,
  certificate formats, length-optimality OUT (`:367–385`). No reference to any
  private repo / commercial product (grep clean). **PASS.**
- **No project-local axioms:** counts.json axioms 0; all 13 cited headlines
  `axiom_deps_project = []`, core ⊆ kernel set. **PASS.**
- **Citations:** all 10 keys in registry with cached primaries; 4 fresh-fetched
  match exactly; AaronsonGottesman = quant-ph/0406196 correction confirmed (not
  0403025 Bravyi–Kitaev). **PASS.**

## QI Candidate

Two systemic, multi-bundle gaps surfaced, suitable for the Stage-14 QI register:

1. **No `arxiv_verified`/`doi_verified` booleans on the D8 registry block** —
   verification is recorded only in free-text `notes`. The Gate-1 evaluator keys
   off the boolean fields, so a prose-only verification cannot pass the gate
   mechanically. This likely affects every recently-added registry block, not
   just D8. Recommend a registry-hygiene pass that backfills the booleans
   wherever `notes` claims a verification date.
2. **FirstClaimLedger node type still unimplemented** (READINESS_GATES Gate 10
   explicitly flags this). Every bundle carrying a "first … in any proof
   assistant" claim is structurally stuck at `needs-recheck`. D8 has three such
   claims. A lightweight committed `first_claims.md` convention per bundle would
   discharge the obligation uniformly until the node type lands.
