# Bundle L2 — Stage-13 fresh-context claims review (TARGETED RE-REVIEW)

**Date:** 2026-06-08
**Reviewer:** `physics-qa:claims-reviewer` (v2, bundle-aware mode, `bundle_target=L2`)
**Reviewer model:** claude-opus-4-8
**Tier profile:** Tier 2 (PRL splash) — stand-alone single-paper depth; absent broader scope NOT penalized.
**Source paper:** `papers/paper10_modular_generation/paper_draft.tex` (387 lines)
**Anchor list:** `docs/agents/claims-reviewer-bundle-prompts.md` §L2
**Trigger:** Lean milestone re-review — `16 ∣ σ(M)` for closed smooth spin 4-manifolds is now an UNCONDITIONAL kernel-pure theorem (`SKEFTHawking.SpinRokhlinInterface.SmoothSpinManifold4.rokhlin`); `HYPOTHESIS_REGISTRY['rokhlin_sigma_mod_16']['status'] = 'discharged'`.

---

## Verdict: **FAIL** (1 critical HD blocker + 2 TP blockers + 2 IA blockers)

The 16∣σ milestone is verified independently and is real. The L2 draft predates it and now contains **stale prose that frames the Rokhlin / 16∣σ result as a tracked hypothesis / external input** — directly contradicting the now-discharged registry state. This is the Class HD special-focus finding the re-review was triggered for, and it is a Stage-13 BLOCKER (the central "16 convergence" narrative understates a result the project now proves outright). Two pre-existing toolchain-pin drifts (Class TP) and two count drifts (Class IA) are also blocking.

No Class TN miss against the new decls: the draft does not yet cite any of the new theorems (`SmoothSpinManifold4.rokhlin`, `sixteen_dvd_latticeSig`, `eight_dvd_latticeSig`, `hasIsotropicVector`, `hasWeakIsotropicVector`, `weakIsotropic_rank_four`) — the finding is the inverse: the prose should now reference them to replace the stale hypothesis framing.

---

## Change-context independent verification (all confirmed)

| Claim in change context | Verification | Result |
|---|---|---|
| `SmoothSpinManifold4.rokhlin` is unconditional kernel-pure theorem | `SpinRokhlinInterface.lean:76-77` — `theorem SmoothSpinManifold4.rokhlin (M) : 16 ∣ M.sig := sixteen_dvd_latticeSig M.form M.even_unimod M.topo` | CONFIRMED |
| `SmoothSpinManifold4` dropped `eight_dvd` field | `SpinRokhlinInterface.lean:55-63` — fields are `rank, form, even_unimod, topo` only; `eight_dvd` now a derived theorem `eight_dvd_sig` (line 70), not a field. (Header docstring lines 15-32 still mention `eight_dvd` as stale text but the structure is unconditional.) | CONFIRMED |
| `.rokhlin` follows from `even_unimod` + `topo` alone | proof term uses exactly `M.even_unimod` + `M.topo` (line 77) | CONFIRMED |
| `HYPOTHESIS_REGISTRY['rokhlin_sigma_mod_16']` status `'discharged'` | `constants.py:2208` — `'status': 'discharged'` | CONFIRMED |
| New decls `hasWeakIsotropicVector`, `hasIsotropicVector`, `weakIsotropic_rank_four`, `eight_dvd_latticeSig`, `sixteen_dvd_latticeSig` | `RokhlinHMRankFour.lean:461,577,579,595,599,604` — all present | CONFIRMED |

`lean_deps.json`/`counts.json` lag (generated 2026-06-04, before the milestone modules landed); cross-checked against `.lean` source directly per the change-context instruction. No Class-TN false-miss resulted.

---

## BLOCKING FINDINGS

### BLOCK-1 (Class HD, CRITICAL) — Rokhlin/16∣σ framed as tracked hypothesis / external input; now stale

The milestone discharged `rokhlin_sigma_mod_16`. The draft still presents 16∣σ as an assumed hypothesis in three places:

- **§"The 16 Convergence", lines 224–228** (`sentence:paper10_modular_generation:the-16-convergence:31e382b1`):
  > "…(iii) divisibility by 16 of spin-4-manifold signatures holds *as a hypothesis* (Rokhlin's theorem). It does *not* prove the mathematical equivalence of the four phenomena; that equivalence is the content of Wang's cobordism argument, which we use as an external input rather than formalize."
  STALE. The 16∣σ conjunct is now `SmoothSpinManifold4.rokhlin`, a kernel-pure theorem, not a hypothesis. (The Wang-equivalence-of-four-phenomena scope statement remains independently honest, but "as a hypothesis" for 16∣σ is now false.)

- **§"With vs. without ν_R", lines 256–257** (`sentence:paper10_modular_generation:the-16-convergence:198db886`):
  > "Rokhlin's theorem enters as a hypothesis (not an axiom) in the key theorems."
  STALE. Rokhlin's theorem no longer enters as a hypothesis in the unconditional theorems; `SmoothSpinManifold4.rokhlin` proves it from `even_unimod` + `topo`.

- **§"Conclusions", lines 318–319** (`sentence:paper10_modular_generation:conclusions:237406ce`):
  > "The complete generation constraint chain rests on machine-checked algebra plus three standard textbook topology results (ko cohomology, ASS convergence, ABP splitting)."
  PARTIALLY STALE / under-claim. The chain now also has 16∣σ as a kernel-pure theorem (van der Blij 8∣σ + Hasse–Minkowski `hasIsotropicVector` + theta-modularity + topological factor-of-two `topo`). The remaining irreducible input is the single topological `topo : 2 ∣ σ/8` factor, not the three textbook results as the only residuals. The prose should be updated to claim the strengthened result.

The L2-specific lift also references the OLD conditional theorem `RokhlinBridge.sixteen_convergence_full` (lines 220–221, 292), which STILL takes `h_rokhlin : ∀ M : SpinManifold4, 16 ∣ M.signature` as a hypothesis (`RokhlinBridge.lean:112-122`). The unconditional replacement `SpinRokhlinInterface.sixteen_convergence_unconditional` (`SpinRokhlinInterface.lean:82-88`, `16∣σ` conjunct = `fun M => M.rokhlin`) now exists and should be the cited theorem. This is not a Class-TN miss (the cited theorem still exists) but is a strength under-claim coupled to BLOCK-1.

**Remediation:** rewrite the three sentences above to state 16∣σ is now an unconditional kernel-pure theorem (`SmoothSpinManifold4.rokhlin`, via `eight_dvd_latticeSig` + `sixteen_dvd_latticeSig` over the `SmoothSpinManifold4` interface), and re-point the `sixteen_convergence_full` citations to `sixteen_convergence_unconditional`. Remove "as a hypothesis" / "external input" framing for the 16∣σ divisibility specifically. **Maps to Gate 6 AssumptionDisclosure + Gate 5 LeanProofSubstance.**

### BLOCK-2 (Class TP, MAJOR) — Lean toolchain pin drift

- **§Acknowledgments, line 327** (`sentence:paper10_modular_generation:conclusions:bed35798`):
  > "Lean~4 proofs verified by `lake build` (v4.29.0, Mathlib `8850ed93`)."
  `lean/lean-toolchain` = `leanprover/lean4:v4.29.1`; paper says `v4.29.0`. FAIL. **Maps to Gate 9 NumericalFreshness.**

### BLOCK-3 (Class TP, MAJOR) — Mathlib pin drift

- **Same sentence, line 327.** `lean/lakefile.toml:27` rev = `5e932f97` (v4.29.1 tag); paper says Mathlib `8850ed93`. The lakefile comment (line 21) explicitly identifies `8850ed93` as the *previous* v4.29.0 pin. FAIL. **Maps to Gate 9.**

### BLOCK-4 (Class IA, MAJOR) — Module count drift

- **Abstract lines 31–32** and **Conclusions lines 284–285** state "170 Lean~4 modules". `docs/counts.json` (2026-06-04) reports 853 modules; current is higher still after the milestone modules landed. 170 ≠ 853, not hedged. FAIL. **Maps to Gate 9.** (Note: the F-tier policy requires `\input{counts.tex}` macros, not inline literals — this paper uses inline literals, an independent `count_literals`/`numerical_literals` violation. The literal route is what made these drift.)

### BLOCK-5 (Class IA, MAJOR) — Axiom count drift

- **Conclusions line 285:** "(1~axiom, zero in the generation constraint chain)". `docs/counts.json` reports `axioms: 0, axiom_names: []`; a strict source grep finds zero top-level `axiom` declarations (the two `^axiom ` grep hits in `LaplaceMethod.lean` / `BHEntropyMicroscopic.lean` are comment-line wraps, not declarations). The project is now axiom-free. "1 axiom" is stale. FAIL. **Maps to Gate 9.** (The "zero in the generation constraint chain" qualifier remains true and is in fact now true globally.)

---

## NON-BLOCKING / PASS confirmations (anchor checks)

- **L2 anchor — `N_f ≡ 0 mod 3` from generation-anomaly discriminant:** derivation chain `c_- = 8 N_f` (lines 64, 104–105, 160) + `24 ∣ c_-` (line 158) ⟹ `3 ∣ N_f` (line 162). `framing_anomaly_constraint` resolves (`ModularInvarianceConstraint.lean:103`); `qParam_shift` resolves (same module); `GenerationConstraint.lean` exists; Aristotle `a1dfcbde` = `generation_mod3_constraint` in registry. PASS.
- **L2 anchor — `24 | c₋` chain load-bearing:** present and central (lines 158–164, 176–179). Cross-bridge to D2 §2 verified consistent: `papers/D2/paper_draft.tex:40,242,254,257,489` all use the same `24 ∣ c_-` chain (Schellekens holomorphic-VOA reframing). PASS — no `BundleConsistencyMismatch`.
- **L2 anchor — Ext over Steenrod A(1):** modules `A1Ring.lean`, `A1Resolution.lean`, `A1Ext.lean`, `ChangeOfRings.lean` all exist (lines 304–312). `dim Ext^n = 1,2,2,2,3,4` claim present (line 307). Current abstract (lines 34–36) wisely does NOT carry a bare "first such computation in any proof assistant" first-claim (the 2026-04-25 prior abstract did; it has been softened to "machine-checked Ext computation"). The conclusion (line 313–314) says "machine-checked end-to-end" without an overreaching first-claim. PASS — first-claim spot-check not triggered by current wording (was an open advisory per the L2 anchor; current draft avoids it).
- **Cited theorem-name refs (Class TN sweep):** `SMFermionData.total_components_with_nu_R` (`SMFermionData.lean:176`), `fermion_count_gives_central_charge` (`WangBridge.lean`), `central_charge_fractional_without_nu_R` (`WangBridge.lean`), `framing_anomaly_constraint`, `qParam_shift`, `sixteen_convergence_full` (`RokhlinBridge.lean:112`), `z16_anomaly_always_cancels_with_nu_R`, `z16_anomaly_without_nu_R`, `constraints_without_nu_R`, `rokhlin_strictly_stronger` (all `RokhlinBridge.lean`) — ALL RESOLVE. None reference the removed `eight_dvd` field. Aristotle `b54f9611` = `z16_anomaly_without_nu_R` in registry. PASS.
- **All cited module files exist:** WangBridge, ModularInvarianceConstraint, RokhlinBridge, SMFermionData, GenerationConstraint, AlgebraicRokhlin, E8Lattice, SpinBordism, A1Ring, A1Resolution, A1Ext, ChangeOfRings — all present. PASS.

---

## Reconciliation with prior review (`paper10_modular_generation/claims_review.json`, 2026-04-25)

The prior review marked the three Rokhlin-hypothesis sentences (`526c8eca`, `60a4458b`, and the enumeration sentence) as **PASS with `finding_class: [HD]`** — i.e., the HD disclosure was *correctly made* when 16∣σ genuinely was a hypothesis. The milestone **inverts** these: the disclosure is now stale (it discloses a hypothesis that no longer exists). These are NOT auto-closed supersessions; they are NEW findings (BLOCK-1) arising from the discharge.

`non_reproducing_prior_findings` (candidate_for_supersession, human-ratify path — these are LLM-judgment HD calls, not deterministic rechecks):
- `sentence:paper10_modular_generation:the-16-convergence:526c8eca` — prior HD-PASS "Rokhlin's theorem σ≡0 mod 16 … cite Rokhlin1952". Now: the underlying result is a theorem; the citation-as-external-fact reading is superseded by the in-project proof. Candidate for supersession; re-issued as part of BLOCK-1.
- `sentence:paper10_modular_generation:the-16-convergence:60a4458b` — prior HD-PASS "Rokhlin's theorem enters as a hypothesis (not an axiom)". Now FALSE → re-issued as BLOCK-1.

---

## Summary line

`L2 (paper10) | Tier 2 PRL | FAIL | 1 CRITICAL HD blocker (16∣σ now a theorem, draft still says "hypothesis"/"external input" at tex 224–228, 256–257, 318–319) + 2 TP blockers (Lean v4.29.0→v4.29.1, Mathlib 8850ed93→5e932f97 @ tex 327) + 2 IA blockers (170→853 modules, "1 axiom"→0 axioms @ tex 31–32, 284–285) | 24|c₋ chain + D2 cross-bridge + Ext-A(1) + all theorem-name refs PASS | milestone independently verified (SmoothSpinManifold4.rokhlin kernel-pure, eight_dvd field dropped, registry discharged).`
