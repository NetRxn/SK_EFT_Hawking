# D2 — Stage-13 Bundle Claims Review (TARGETED RE-REVIEW: 16∣σ Rokhlin milestone)

- **Bundle:** D2 — *Anomaly constraints on Standard-Model particle content*
- **Tier:** 1 (deep paper) — intra-bundle consistency + cross-bundle cross-bridge checks
- **Sources walked:** consolidated `papers/D2/paper_draft.tex` (lifts paper7, paper8, paper9 anomaly, paper10 full §2, + Phase 6n W1b / 6o W2a handles)
- **Review date:** 2026-06-08
- **Reviewer:** claims-reviewer-v2 (bundle-aware), model `claude-opus-4-8`
- **Trigger:** Lean milestone — `16 ∣ σ(M)` for closed smooth spin 4-manifolds is now UNCONDITIONAL kernel-pure (`SmoothSpinManifold4.rokhlin`); `HYPOTHESIS_REGISTRY['rokhlin_sigma_mod_16']['status']` flipped `decomposed → discharged`.
- **Scope:** This is a *targeted* re-review. Focus = Class HD (stale/over-disclosure of the Rokhlin/16∣σ result), Class TN (Lean theorem-reference resolution incl. the removed `eight_dvd` field), and Class IA/SD (cardinality counts that shifted). The full sentence-walk of unaffected sections (§3 Z16, §4 chirality wall, Ext computation, change-of-rings) was carried forward from the 2026-05-06 review with no new regressions found in the affected neighborhood.

---

## Change-context verification (independent, not taken on faith)

All four change-context claims were verified at source + kernel level:

| Claim | Verification | Result |
|---|---|---|
| `SmoothSpinManifold4.rokhlin : 16 ∣ σ` is unconditional kernel-pure | `lean_verify` → axioms `{propext, Classical.choice, Quot.sound}`; proof = `sixteen_dvd_latticeSig M.form M.even_unimod M.topo` (SpinRokhlinInterface.lean:76-77) | **CONFIRMED** |
| `SmoothSpinManifold4` dropped `eight_dvd` field | structure (lines 55-63) has fields `rank, form, even_unimod, topo` ONLY; no `eight_dvd` | **CONFIRMED** |
| `.rokhlin` derives from `even_unimod` + `topo` alone | proof consumes `M.even_unimod` + `M.topo`; `eight_dvd_sig` now a derived theorem (line 70-71), not a field | **CONFIRMED** |
| `HYPOTHESIS_REGISTRY['rokhlin_sigma_mod_16']['status'] == 'discharged'` | `constants.py` literal `'status': 'discharged'` | **CONFIRMED** |
| New citable theorems exist | `hasWeakIsotropicVector`, `hasIsotropicVector`, `weakIsotropic_rank_four`, `eight_dvd_latticeSig`, `sixteen_dvd_latticeSig` all present in `RokhlinHMRankFour.lean` (lines 461, 579, 595, 599, 604) | **CONFIRMED in source** (NOTE: `lean_deps.json` is STALE for the three `*Isotropic*` names — `MISS` on lookup — but they exist in source per change-context warning; not TN failures) |

**Additional kernel-level findings beyond the stated change context:**
- `eight_dvd_latticeSig` (the van der Blij `8 ∣ σ` algebraic half) is ALSO unconditional and kernel-pure (`{propext, Classical.choice, Quot.sound}`). Its proof discharges the Hasse–Minkowski input via `hasIsotropicVector`, which is itself a *theorem* (`hasWeakIsotropicVector` proven by full rank case-split with p-adic local-global machinery, RokhlinHMRankFour.lean:579-595) — kernel-pure, verified.
- **Consequence:** not only is `16 ∣ σ` discharged; the entire algebraic floor `8 ∣ σ` (HM + theta-modularity) is now discharged. This makes the paper's §2 "what remains pending" prose stale in a *larger* way than the trigger note anticipated (see HD-2 below).

---

## FINDINGS

### Class HD (hypothesis-disclosure / over-disclosure) — PRIMARY

#### HD-1 (FAIL) — Outlook lists `rokhlin_sigma_mod_16` among "five active tracked Props"
- **Sentence ID:** `sentence:D2:tracked-hypotheses:cab9b774`
- **TeX lines:** 1026–1034
- **Quote:** "The five active tracked Props underlying the chirality-wall + modular chain are registered in `src/core/constants.py::HYPOTHESIS_REGISTRY` under the keys `rokhlin_sigma_mod_16`, `modular_invariance_framing`, `c_minus_equals_8Nf`, `characteristic_square_mod_8`, and `spin_bordism_iso_Z` …"
- **Verdict:** FAIL (Class HD + IA).
- **Why:** This sentence is now **doubly-to-triply stale** w.r.t. the registry it cites:
  - `rokhlin_sigma_mod_16` → `discharged` (the 16∣σ milestone). Listing it as an *active tracked Prop* directly mis-describes the artifact state.
  - `characteristic_square_mod_8` → `superseded_on_wiring_path` (the 2026-06-04 interface rewiring replaced `charSq` with the `eight_dvd` residual, which is itself now discharged). Not active.
  - `spin_bordism_iso_Z` → `proposed` (not `active`).
  - Only `modular_invariance_framing` and `c_minus_equals_8Nf` remain genuinely `active` (verified: 2 of the 5 keys).
- **Fix:** Rewrite to "Two active tracked Props … `modular_invariance_framing` and `c_minus_equals_8Nf`," and move `rokhlin_sigma_mod_16` to a *discharged-Props* sentence (it is a positive result now — `SmoothSpinManifold4.rokhlin` — and should be cited as such, not buried as an undisclosed dependency).

#### HD-2 (FAIL) — §2 over-discloses the `8 ∣ σ` algebraic half as "pending / no Mathlib substrate"
- **Sentence IDs:** `sentence:D2:from-assumed-hypothesis-to-a-wired-theorem-spectra-free-route:3b459ddd` (lines 368–377) and `sentence:D2:...:a85db7b2` (lines 378–385)
- **TeX lines:** 368–385
- **Quote (368-377):** "The remaining input is solely the classification's existence statement … together with the two classical inputs with no current Mathlib substrate --- Hasse--Minkowski (an indefinite form represents zero) and the level-one theta-modularity giving 8∣rank=σ in the definite case …"
- **Quote (378-385):** "… for which the `LatticeTheta.lean` substrate (… ) is assembled **pending multivariate Poisson summation**."
- **Verdict:** FAIL (Class HD — over-disclosure; the artifact is now *stronger* than the prose admits).
- **Why:** `eight_dvd_latticeSig` is now unconditional and kernel-pure; `hasIsotropicVector` (the HM input) is a discharged theorem (rank case-split + p-adic local-global, RokhlinHMRankFour.lean:579-595). The prose framing "two classical inputs with no current Mathlib substrate" and theta-modularity "pending multivariate Poisson summation" no longer describes the build state — `8∣σ` no longer rests on pending inputs. This is the inverse of a normal HD gap (the paper *under*-claims its own result), but it still mis-describes the artifact state and must be corrected for Stage-13.
- **Note on direction:** Per the trigger note, the result genuinely IS unconditional, so the correct remedy is to *upgrade* the prose to claim `8∣σ` (and hence `16∣σ`) as discharged kernel-pure theorems, not to retain the hedge.

#### HD-3 (WARN, leaning FAIL) — §2 "carried as the precise field `eight_dvd`" describes a removed field
- **Sentence ID:** `sentence:D2:from-assumed-hypothesis-to-a-wired-theorem-spectra-free-route:343eea3f`
- **TeX lines:** 333–335 (within the 324–335 sentence)
- **Quote:** "… the algebraic residual is carried as the precise field `eight_dvd` : 8 | latticeSig(form), the isolated van der Blij bound."
- **Verdict:** FAIL (Class HD + TN-adjacent).
- **Why:** `SmoothSpinManifold4` **no longer has an `eight_dvd` field** (verified: structure fields are `rank, form, even_unimod, topo`). The `8∣σ` bound is now a *derived theorem* `SmoothSpinManifold4.eight_dvd_sig` (line 70-71), proven from `even_unimod` via the discharged HM input — not a carried structure field. The prose describing it as "the precise field `eight_dvd` … carried" mis-states both the structure shape and the dependency posture.
- **Fix:** "… the algebraic residual `8 ∣ latticeSig(form)` is itself a derived theorem `SmoothSpinManifold4.eight_dvd_sig` (van der Blij, via the discharged Hasse–Minkowski input), not a carried field."

#### HD-4 (PASS) — `SmoothSpinManifold4.rokhlin` correctly described as kernel-pure unconditional
- **Sentence ID:** `sentence:D2:from-assumed-hypothesis-to-a-wired-theorem-spectra-free-route:9981b3e4`
- **TeX lines:** 336–344
- **Quote:** "From these, `SmoothSpinManifold4.rokhlin` : 16 | σ is a kernel-pure theorem (standard axioms only; no global Rokhlin hypothesis, no new `axiom`) …"
- **Verdict:** PASS. This sentence is now exactly correct (kernel-pure verified `{propext, Classical.choice, Quot.sound}`; derives from `even_unimod` + `topo`). The companion `sixteen_convergence_unconditional` reference (line 343) resolves and is accurate.

### Class TN (theorem-name reference drift)

All `\texttt{...}` Lean references in the affected §2 Rokhlin block were checked against source (lean_deps.json is stale for the brand-new decls, so source was used as the authority per the trigger note):

| Reference | TeX line | Resolves? |
|---|---|---|
| `SmoothSpinManifold4.rokhlin` | 336 | YES (verified kernel-pure) |
| `sixteen_convergence_unconditional` | 343 | YES (SpinRokhlinInterface.lean:82) |
| `eight_dvd_sig` (implied) | — | YES (`SmoothSpinManifold4.eight_dvd_sig`, line 70) |
| `RokhlinClassification.sixteen_dvd_latticeSig_of_eight_dvd_of_topo` | 339 | YES |
| `rokhlin_from_serre_plus_topology` | 342 | YES |
| `RokhlinBridge.sixteen_convergence_full` | 309 | YES (RokhlinBridge.lean:112 — STILL the OLD conditional form on abstract `SpinManifold4`; correctly described as the four-16 *enumeration*, not unconditional) |
| `RokhlinBridge.rokhlin_strictly_stronger` | 322 | YES (RokhlinBridge.lean:160) |
| `RokhlinBridge.constraints_without_nu_R` | 317 | YES (RokhlinBridge.lean:265) |
| `e8_sigma_not_div_16`, `E8Lattice.rokhlin_gap`, `AlgebraicRokhlin.char_sq_valid_e8` | 300-306 | YES |
| `exists_hyperbolic_pair`, `even_form_dvd` (LatticePrimitive) | 371-372 | YES |
| `LatticeSignature.lean`, `E8Signature.lean`, `LatticeSignatureCongr.lean`, `BlockSignature.lean`, `GeneratorNondeg.lean`, `LatticeSigBlock.lean`, `RokhlinClassification.lean` | 357-368 | YES (module refs) |
| **`eight_dvd` (as a struct field)** | 334 | **NO — field removed** (see HD-3; classified under HD, not pure TN, since it's a structural-shape claim) |

- **TN verdict:** No naked theorem-name TN failures. The single stale identifier (`eight_dvd` field, line 334) is captured as HD-3.
- **Important non-finding:** the OLD `RokhlinBridge.sixteen_convergence_full` (line 309) still carries `h_rokhlin` as a hypothesis BY DESIGN — it operates on the abstract free-integer `SpinManifold4`. The paper correctly distinguishes this from the new unconditional `sixteen_convergence_unconditional` on the genuine `latticeSig`. No regression; the prose is accurate here.

### Class IA / SD (count drift adjacent to the change)

- **IA-1 (FAIL, = HD-1):** "five active tracked Props" (line 1026) — true active count is 2 (`modular_invariance_framing`, `c_minus_equals_8Nf`). The 16∣σ discharge is the proximate cause of the drift but the count was already loose (charSq superseded, spin_bordism proposed). Cross-listed with HD-1.
- **IA-2 (INFO, adjacent, NOT in 16∣σ scope but surfaced):** Abstract (line 74) + axiom-budget prose state "one axiom (`gapped_interface_axiom`)". A `^axiom <ident>` grep over `lean/SKEFTHawking/*.lean` returns **zero** genuine axiom declarations (the two grep hits are docstring continuation lines); SPTClassification.lean docstrings note `gapped_interface_axiom` was converted to a Prop field "pre-2026-05-19". This may be a separate stale count, but it is OUTSIDE this targeted re-review's 16∣σ scope and may already be tracked by `AXIOM_METADATA`. Flagged INFO for a full-walk follow-up; not asserting FAIL without the full axiom-budget audit.
- **SD:** "three Mathlib infrastructure items whose upstream completion would discharge every remaining tracked hypothesis" (lines 167-170) cross-checks consistently with the outlook §"Mathlib infrastructure" (H1/H3/H4, line 1065). This is NOT about Rokhlin (a separate, now-discharged Prop) and remains accurate. PASS.

### Cross-bundle (D2 ↔ L2, D2 §3 ↔ D4 §4)

- **L2 ↔ D2 §2 (`24 | c₋` / `N_f ≡ 0 mod 3` chain):** The 16∣σ discharge strengthens the Rokhlin leg of D2 §2 but does not change the `24 | c₋` Ext chain or the `N_f ≡ 0 mod 3` conclusion. No cross-bundle numeric mismatch introduced. The L2 splash should eventually carry a one-line "Rokhlin 16∣σ now discharged unconditionally" note for parity, but L2 does not currently over-disclose it as a hypothesis (out of scope to re-verify here; flagged for L2's own re-review).
- **D2 §3 ↔ D4 §4 (Drinfeld center):** unaffected by the 16∣σ change. Not re-verified in this targeted pass.

---

## Reconciliation with prior review (2026-05-06)

The prior `claims_review.json` (2026-05-06T18:30:00Z, claims-reviewer-v2) had **zero blocking issues** and predates BOTH the 2026-06-04 interface rewiring AND the 2026-06-08 discharge. Its Rokhlin sentences (6 found) were assessed against the older tex and registry where `rokhlin_sigma_mod_16` was a genuine tracked hypothesis — so listing it as such was CORRECT at that time. The findings HD-1/HD-2/HD-3 here are NEW regressions introduced by the artifact moving ahead of the prose, not contradictions of the prior review. No prior finding is auto-closed or superseded by this targeted pass.

---

## SUMMARY

- **HD-1 (FAIL):** line 1026 — "five active tracked Props" lists discharged `rokhlin_sigma_mod_16` (and stale `characteristic_square_mod_8`, `spin_bordism_iso_Z`); true active count = 2.
- **HD-2 (FAIL):** lines 368–385 — §2 over-discloses `8∣σ` (HM + theta-modularity) as "pending / no Mathlib substrate"; both are now discharged kernel-pure theorems.
- **HD-3 (FAIL):** lines 333–335 — prose references a removed `eight_dvd` struct field; `8∣σ` is now the derived theorem `SmoothSpinManifold4.eight_dvd_sig`.
- **HD-4 (PASS):** lines 336–344 — `SmoothSpinManifold4.rokhlin` correctly stated as kernel-pure unconditional.
- **TN:** no naked theorem-name failures; all `\texttt{}` refs resolve in source (lean_deps.json stale for 3 brand-new `*Isotropic*` names — source-confirmed). Single stale identifier `eight_dvd` field captured under HD-3.
- **IA-2 (INFO):** abstract "one axiom" count may itself be stale (zero `axiom` decls found) — outside 16∣σ scope, flagged for full-walk follow-up.

**Verdict counts (targeted neighborhood):** 3 FAIL · 1 PASS · 1 INFO (+ TN table all-resolve).

**BLOCKER for Stage-13:** YES. Three HD FAILs (HD-1, HD-2, HD-3) must be remediated before D2 ships. All three are *good-news* corrections — the artifact is unconditional where the prose still hedges/mis-describes. Recommended single-pass fix: rewrite the §2 "From assumed hypothesis to a wired theorem" paragraph (324–385) and the outlook "Tracked hypotheses" paragraph (1026–1034) to (a) state `8∣σ` and `16∣σ` as discharged kernel-pure theorems, (b) drop the `eight_dvd`-as-field description, (c) reduce the active-tracked-Props list to the 2 genuinely-active keys and relocate `rokhlin_sigma_mod_16` to a discharged-results sentence.
