---
paper: L2
reviewer: adversarial-reviewer
model: claude-opus-4-8
review_date: 2026-06-08T22:39:00Z
readiness_gates_version: 1
---

# Adversarial Review — L2

## Summary

7 findings: **3 BLOCKER, 3 REQUIRED, 1 RECOMMENDED**. Gates affected:
NarrativeGrounding (Gate 7), AssumptionDisclosure (Gate 6),
CrossPaperConsistency (Gate 2), NumericalFreshness (Gate 9),
CitationIntegrity (Gate 1). The paper is **NOT submission-ready**. The
dominant problem is staleness against the now-discharged Rokhlin
infrastructure: `16 ∣ σ(M)` is an unconditional kernel-pure theorem
(`SmoothSpinManifold4.rokhlin`, verified clean below), yet L2 still
presents it as an unformalized "external Wang input," cites the OLD
conditional `RokhlinBridge.sixteen_convergence_full`, and the deep
companion D2 already cites the unconditional route — a direct
cross-paper contradiction. Independently, L2 hardcodes "1 axiom
`gapped_interface_axiom`" in two places when the canonical axiom count
is now **0** (the axiom was converted to a Tracked-Prop on 2026-05-19;
`\axiomcount{}` = 0). Citations and Lean proof substance are otherwise
sound: all 16 bibkeys are in the registry, the three highest-load arXiv
citations fetch-verified clean, and the new HM theorems are genuinely
substantive (not placeholders).

## Findings

### 5.1 — 🔴 BLOCKER — `16 ∣ σ` presented as unformalized external input; the unconditional theorem is omitted

- **Gate:** NarrativeGrounding
- **Location:** `papers/L2/paper_draft.tex:273-274` (also abstract :56-60, :199-203, :244-252)
- **Observed:** Line 273-274: "We record the four occurrences as `RokhlinBridge.sixteen_convergence_full` without claiming a formal equivalence; the equivalence remains an external Wang input." The abstract (:56-60) and Sec.~Ext (:244-252) frame the entire Rokhlin story through the `Ext_{A(1)}` route plus "three textbook topology results" bridging "to the bordism group," with `16 ∣ σ` never stated to be a theorem.
- **Evidence:** `lean/SKEFTHawking/SpinRokhlinInterface.lean:76-77` —
  `theorem SmoothSpinManifold4.rokhlin (M : SmoothSpinManifold4) : 16 ∣ M.sig := sixteen_dvd_latticeSig M.form M.even_unimod M.topo`.
  `lean_verify` on `SKEFTHawking.SmoothSpinManifold4.rokhlin` returns axioms `[propext, Classical.choice, Quot.sound]` only — kernel-pure, no project axiom, no sorry. `M.sig := latticeSig M.form` is the GENUINE signature of the intersection form, not a free parameter. The algebraic half `8 ∣ σ` (`eight_dvd_latticeSig`, `RokhlinHMRankFour.lean:599`) is also `lean_verify`-clean (same three axioms). The only remaining input is the topological factor `2 ∣ σ/8` (`topo` field).
- **Expected:** L2 should state that `16 ∣ σ` for an even-unimodular intersection form with the topological factor `2 ∣ σ/8` is a kernel-pure Lean theorem (`SmoothSpinManifold4.rokhlin`), and that the only residual topological input is the factor of two `2 ∣ σ/8` (Â-genus even / Arf(q̄)=0). The "external Wang input" framing is now a stale under-claim: only the *bordism-group identification* `Ω^{Spin^{Z4}}_5 ≅ ℤ_16` and the four-way physical *equivalence* remain external — not `16 ∣ σ` itself.
- **Fix:** Rewrite :273-275 to cite `SKEFTHawking.sixteen_convergence_unconditional` (see 5.2) and `SmoothSpinManifold4.rokhlin`; add a sentence stating `16 ∣ σ` is now machine-checked unconditionally with the topological factor `2 ∣ σ/8` as the sole topological input. Adjust the abstract (:56-60) and conclusion to match.
- **Cache:** n/a (Lean artifact)

### 5.2 — 🔴 BLOCKER — Cites the OLD conditional convergence theorem; unconditional companion exists

- **Gate:** NarrativeGrounding
- **Location:** `papers/L2/paper_draft.tex:273`
- **Observed:** Paper cites `\texttt{RokhlinBridge.sixteen\_convergence\_full}`.
- **Evidence:** `lean/SKEFTHawking/RokhlinBridge.lean:112-122` — `sixteen_convergence_full` takes `(h_rokhlin : ∀ M : SpinManifold4, (16 : ℤ) ∣ M.signature)` as a HYPOTHESIS and operates on the abstract free-integer type `SpinManifold4` (`signature : ℤ` is a free field, `RokhlinBridge.lean:54-56`). Its own docstring (`RokhlinBridge.lean:95-96`) labels conjunct 3 an "(echo): verbatim restatement of the hypothesis `h_rokhlin`" and (:108-110) mandates "Papers citing this theorem MUST describe it as a formal enumeration, not a formal unification." The unconditional replacement is `SpinRokhlinInterface.lean:82-88` `sixteen_convergence_unconditional`, whose `16 ∣ σ` conjunct is `fun M => M.rokhlin` (a theorem) over the genuine `latticeSig`-based `SmoothSpinManifold4` type.
- **Expected:** Citation of `SKEFTHawking.sixteen_convergence_unconditional`.
- **Fix:** Replace the `sixteen_convergence_full` citation at :273 with `sixteen_convergence_unconditional`. If L2 deliberately retains the conditional theorem to describe the abstract-type enumeration, it must say so explicitly and additionally cite the unconditional companion — but the current bare cite of only the conditional version, in a PRL splash on this exact result, understates the formalization.
- **Cache:** n/a (Lean artifact)

### 6.1 — 🔴 BLOCKER — Stale axiom disclosure: "1 axiom `gapped_interface_axiom`" but the axiom was eliminated (count = 0)

- **Gate:** AssumptionDisclosure
- **Location:** `papers/L2/paper_draft.tex:63-64` (abstract) and `:337-338` (conclusion)
- **Observed:** Abstract: "with one axiom (`gapped\_interface\_axiom`) and zero `sorry`." Conclusion: "1 axiom `gapped\_interface\_axiom` unused in this chain, zero `sorry`."
- **Evidence:** `grep -rnE "^axiom " lean/SKEFTHawking/*.lean` returns **zero** axiom declarations (the two matches are the literal word "axiom" inside docstring prose in `BHEntropyMicroscopic.lean:182` and `LaplaceMethod.lean:122`). `lean/SKEFTHawking/SPTClassification.lean:16` and `:217-224`: "**Posture change (2026-05-19):** This was previously asserted as a project-local `axiom gapped_interface_axiom` ... the substantive content is shipped here as a `def`-Prop. Consumers take `(H : TPFConjecture)` as an explicit hypothesis parameter." Canonical `docs/counts.tex`: `\newcommand{\axiomcount}{0}`. The change context also confirms `HYPOTHESIS_REGISTRY['rokhlin_sigma_mod_16']` is `discharged` and the new Rokhlin theorems carry no axiom.
- **Expected:** "zero axioms" (or use the `\axiomcount{}` macro = 0). `gapped_interface_axiom` no longer exists as an axiom; it is a Tracked-Prop `TPFConjecture`. Disclosing a non-existent axiom is a factual mis-disclosure that misrepresents the library's trust posture.
- **Fix:** Replace both literals with `\axiomcount{}` axioms (renders "0 axioms"); drop the `gapped_interface_axiom` parenthetical or recast it as "the prior `gapped_interface_axiom` is now a tracked Prop, not an axiom." Note the abstract already concedes it is "unused in this chain," reinforcing that the mention is gratuitous.
- **Cache:** n/a

### 4.1 — 🔴 BLOCKER — Cross-paper contradiction with D2 on the Rokhlin `16 ∣ σ` formalization status

- **Gate:** CrossPaperConsistency
- **Location:** `papers/L2/paper_draft.tex:273-274` vs `papers/D2/paper_draft.tex:324-344`
- **Observed:** L2 (:273-274): `16 ∣ σ` "remains an external Wang input," cites only the conditional `sixteen_convergence_full`. D2 (:324-344), the deep companion to the SAME result, has a paragraph titled "**From assumed hypothesis to a wired theorem (spectra-free route)**": "Rather than carry `16 ∣ σ` as an opaque hypothesis, we expose its content... `SmoothSpinManifold4.rokhlin` : `16 ∣ σ` is a kernel-pure theorem (standard axioms only; no global Rokhlin hypothesis, no new axiom)... the companion `sixteen_convergence_unconditional` reruns the four-16 enumeration with the `16 ∣ σ` conjunct now a theorem rather than an assumed input."
- **Evidence:** Two companion papers on the identical mechanism describe its formalization status oppositely: D2 says it is a wired unconditional theorem; L2 says it is an unformalized external hypothesis. `Roehm2026D2` is explicitly cited by L2 at :329 as "the deep companion to L2."
- **Expected:** L2 and D2 must agree. Since D2 is correct against the current Lean source (verified above), L2 must be updated to the wired-theorem framing.
- **Fix:** Apply 5.1 and 5.2; the cross-paper inconsistency closes once L2 adopts D2's accurate framing.
- **Cache:** n/a

### 9.1 — 🟡 REQUIRED — Hardcoded axiom-count literal drifts from canonical `\axiomcount{}` = 0

- **Gate:** NumericalFreshness
- **Location:** `papers/L2/paper_draft.tex:64` ("one axiom"), `:337` ("1 axiom")
- **Observed:** Two inline count literals for the axiom count ("one axiom", "1 axiom") instead of the `\axiomcount{}` macro. `\totaltheorems{}` and `\leanmodules{}` are correctly macro-driven (:8-9, :62, :336), but the axiom count is not.
- **Evidence:** `docs/counts.tex`: `\axiomcount{}` = 0. The paper's literal "1" disagrees with the canonical value 0 (this is the numerical-freshness face of finding 6.1; it is also a correctness error, but the gate-9 view is the count-literal-outside-macro violation).
- **Expected:** `\axiomcount{}` macro, which renders the current canonical 0.
- **Fix:** Replace both literals with `\axiomcount{}`. After fix, re-run `validate.py --check count_literals` to confirm no axiom-count literal remains.
- **Cache:** n/a

### 6.2 — 🟡 REQUIRED — Abstract over-attributes the "three textbook topology results" as bridging `16 ∣ σ` when only the bordism-group identification remains external

- **Gate:** AssumptionDisclosure
- **Location:** `papers/L2/paper_draft.tex:57-60` (abstract), `:244-252` (Sec. Ext)
- **Observed:** Abstract: "Three textbook topology results (ko cohomology, ASS convergence, ABP splitting) enter as commentary documenting the bridge from the algebraic kernel to the bordism group `Ω^Spin_4 ≅ ℤ`." Sec. Ext (:244-252): "Three textbook topology results bridge from `Ext_{A(1)}` to the bordism group `Ω^{Spin^{Z4}}_5 ≅ ℤ_16`... Each enters as standard textbook content cited at the bibliographic level."
- **Evidence:** With `SmoothSpinManifold4.rokhlin` now proving `16 ∣ σ` directly from `even_unimod` + `topo` (kernel-pure, verified), the "three textbook results" are NOT what establishes `16 ∣ σ` — they establish the *separate* bordism-group identification `Ω^{Spin^{Z4}}_5 ≅ ℤ_16`. Conflating these in the abstract leaves the reader believing `16 ∣ σ` is only as strong as the cited textbook results, when it is in fact a machine-checked theorem with a single primitive topological input (`2 ∣ σ/8`).
- **Expected:** Distinguish: (a) `16 ∣ σ` — now a Lean theorem modulo `2 ∣ σ/8`; (b) the bordism-group identification and the four-way physical equivalence — still external textbook/Wang inputs. (The abstract also says `Ω^Spin_4 ≅ ℤ` at :60 while the body says `Ω^{Spin^{Z4}}_5 ≅ ℤ_16` at :245 — these are different groups; reconcile.)
- **Fix:** Split the disclosure as in (a)/(b) and reconcile the `Ω^Spin_4 ≅ ℤ` (abstract :60) vs `Ω^{Spin^{Z4}}_5 ≅ ℤ_16` (body :245) bordism-group statements.
- **Cache:** n/a

### 1.1 — 🔵 RECOMMENDED — All L2 bibkeys absent from the citation-verification cache (no prior fetch record)

- **Gate:** CitationIntegrity
- **Location:** `papers/L2/paper_draft.tex:349-422` (all 16 bibitems)
- **Observed:** None of L2's 16 bibkeys (`Wang2024`, `GarciaEtxebarria2019`, `MollerScheithauer2024`, `Schellekens1993`, `Kitaev2009`, `FidkowskiKitaev2010`, `BeaudryCampbell`, `Niemeier1973`, `Adams1974`, `ABP1967`, `Rokhlin1952`, `AlvarezGaumeWitten1984`, `Rademacher1973`, `diFrancesco1997`, `GrossJackiw1972`, `Roehm2026D2`) has a record in `docs/citation_verifications.jsonl`. All 16 ARE present in `src/core/citations.py` CITATION_REGISTRY (registry check passed), so this is not a registry-absence blocker.
- **Evidence:** Fresh-fetched this run, all `match`: `Wang2024` (arXiv:2312.14928 — title "Family Puzzle, Framing Topology, $c_-=24$ and 3(E8)$_1$..." and author Juven Wang both match; PRD 110 125028 (2024) is the published venue, not shown on the arXiv abstract page but consistent); `GarciaEtxebarria2019` (arXiv:1808.00009 — "Dai-Freed anomalies in particle physics," García-Etxebarria & Montero, JHEP 08, 003 (2019) — full match); `MollerScheithauer2024` (arXiv:2112.12291 — title, Möller & Scheithauer, Algebra & Number Theory 18, 1891-1922 (2024) — full match). The remaining arXiv-bearing bibitems (`Kitaev2009`/0901.2686, `FidkowskiKitaev2010`/0904.2197, `BeaudryCampbell`/1801.07530, `Schellekens1993`/hep-th/9205072) were NOT re-fetched this run.
- **Expected:** Cache entries for all L2 bibkeys so future runs amortize. Pre-submission, the four un-fetched arXiv bibitems should be fetch-verified and recorded.
- **Fix:** Append `verdict: match` records for the three fetched-clean bibkeys via `scripts/citation_cache.append_record`; fetch-verify the four remaining arXiv-bearing bibitems before submission.
- **Cache:** fresh-fetch (Wang2024, GarciaEtxebarria2019, MollerScheithauer2024); not-fetched (Kitaev2009, FidkowskiKitaev2010, BeaudryCampbell, Schellekens1993)

## QI Candidate

**Systemic late-absorption staleness across companion papers.** The
discharge of `16 ∣ σ` to an unconditional theorem and the elimination
of `gapped_interface_axiom` (axiom → Tracked-Prop, 2026-05-19)
propagated to the D2 deep companion (D2:324-344 cites the unconditional
route) but NOT to the L2 PRL splash on the same result. This is exactly
the failure mode the Late-Phase-6 Absorption Protocol exists to
prevent: an infrastructure upgrade lands, one bundle absorbs it, a
paired bundle does not, and the two then contradict each other. Two
independent stale artifacts (Rokhlin framing + axiom count) in one
paper suggests L2 was not re-swept after the discharge. Recommend a
`validate.py --check bundle_source_freshness` pass keyed on
axiom-count-literal and `sixteen_convergence_full`-citation across ALL
13 bundles, not just the bundle that triggered the absorption. Note D2
itself still describes `eight_dvd` as a carried interface field
(D2:333-334) although `8 ∣ σ` is now the unconditional theorem
`eight_dvd_latticeSig` — a second-order staleness worth a D2 re-sweep.
