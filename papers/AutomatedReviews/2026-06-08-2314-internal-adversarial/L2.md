---
paper: L2
reviewer: adversarial-reviewer
model: claude-opus-4-8
review_date: 2026-06-08T23:14:00Z
readiness_gates_version: 1
re_review_of: 2026-06-08-2239-internal-adversarial/L2.md
---

# Adversarial Review — L2 (re-review after remediation)

## Summary

Re-run after author remediation. **All 4 prior BLOCKERs and both prior
REQUIREDs are RESOLVED**, verified against the current Lean source,
`counts.tex`, and the D2 companion. `16 ∣ σ` is now correctly framed as
the kernel-pure theorem `SmoothSpinManifold4.rokhlin` (lean_verify:
`[propext, Classical.choice, Quot.sound]` only) with `2 ∣ σ/8` as the
sole topological input; the convergence cite is repointed to
`sixteen_convergence_unconditional` (also kernel-pure); the axiom
disclosure is now `\axiomcount{}` (=0) with `TPFConjecture` named as the
tracked Prop; the bordism group is consistently
`Ω_5^{Spin^{Z4}} ≅ ℤ_16` in all four occurrences (the inconsistent
`Ω^Spin_4 ≅ ℤ` is gone); and L2/D2 now agree on the wired-theorem
framing. One prior RECOMMENDED (cache absence) persists — advisory only.
**One NEW finding (REQUIRED): a cross-paper toolchain-pin contradiction
where D2, not L2, is stale** (D2 pins old Mathlib `8850ed93`; L2
correctly pins canonical `5e932f97`). Because the defect is on the D2
side, it does not block L2. **No new BLOCKERs. Final: 0 BLOCKER, 1
REQUIRED (D2-side), 1 RECOMMENDED.**

## Disposition of prior findings

### Prior 5.1 — `16 ∣ σ` as unformalized external input — ✅ RESOLVED

- Abstract :58-62 now reads: "The Rokhlin divisibility `16 ∣ σ(M)` is
  itself a kernel-pure Lean theorem (`SmoothSpinManifold4.rokhlin`):
  for an even-unimodular intersection form it follows from the
  machine-checked algebraic half `8 ∣ σ` together with the single
  topological input `2 ∣ σ/8`."
- Body §Ext :263-270 and §16-convergence :288-303 both state `16 ∣ σ`
  is "not deferred to these citations: it is a kernel-pure Lean
  theorem" with "no global Rokhlin hypothesis and no project axiom
  (standard axioms only)."
- The stale "external Wang input" framing for `16 ∣ σ` is gone; only
  the bordism-group identification and the four-way physical
  equivalence are now labeled external (:300-304). Independently
  verified: `lean_verify SKEFTHawking.SmoothSpinManifold4.rokhlin` →
  `[propext, Classical.choice, Quot.sound]`, no project axiom, no sorry.

### Prior 5.2 — cites OLD conditional `sixteen_convergence_full` — ✅ RESOLVED

- The string `sixteen_convergence_full` no longer appears anywhere in
  L2. §16-convergence :294-296 now cites
  `SpinRokhlinInterface.sixteen_convergence_unconditional`, "whose
  `16 ∣ σ` conjunct is now this theorem rather than an assumed input."
- Verified: `lean_verify
  SKEFTHawking.sixteen_convergence_unconditional` →
  `[propext, Classical.choice, Quot.sound]`; its third conjunct is
  `fun M => M.rokhlin` over the genuine `latticeSig`-based
  `SmoothSpinManifold4` type (`SpinRokhlinInterface.lean:82-88`).

### Prior 6.1 — stale "1 axiom `gapped_interface_axiom`" disclosure — ✅ RESOLVED

- Abstract :68 now reads "with `\axiomcount{}` axioms and zero
  `sorry`" (renders "0 axioms"). Conclusion :369-371 reads
  "`\axiomcount{}` axioms, zero `sorry`; the formerly asserted
  `gapped_interface_axiom` is now a tracked Prop `TPFConjecture`, not
  used in this chain."
- Verified: `grep -rnE "^axiom " lean/SKEFTHawking/*.lean` → zero axiom
  declarations (two hits are the word "axiom" inside docstring prose).
  `docs/counts.tex`: `\axiomcount{}` = 0.
  `SPTClassification.lean:16` confirms `TPFConjecture` is the
  Tracked-Prop replacement for the pre-2026-05-19 axiom.

### Prior 4.1 — cross-paper contradiction with D2 on `16 ∣ σ` status — ✅ RESOLVED

- L2 (:288-303) and D2 (:338-352) now describe the formalization status
  identically: both state `16 ∣ σ` is a kernel-pure wired theorem
  `SmoothSpinManifold4.rokhlin` derived from the `8 ∣ σ` theorem plus
  the `2 ∣ σ/8` topological input, with no global Rokhlin hypothesis
  and no new axiom; both cite `sixteen_convergence_unconditional`.
- The prior "second-order staleness" note (D2 treating `eight_dvd` as
  a carried interface field) is also resolved: D2 :338-340 now calls
  `8 ∣ latticeSig` "not a carried interface field — it is itself the
  derived kernel-pure theorem `SmoothSpinManifold4.eight_dvd_sig`."

### Prior 9.1 — hardcoded axiom-count literal — ✅ RESOLVED

- Both former literals ("one axiom" :64, "1 axiom" :337) are replaced
  by the `\axiomcount{}` macro (:68, :369). No axiom-count literal
  remains outside the macro.

### Prior 6.2 — abstract conflates "three textbook results" with `16 ∣ σ`; `Ω` mismatch — ✅ RESOLVED

- Abstract :62-65 now cleanly separates: `16 ∣ σ` is "a kernel-pure
  Lean theorem"; the "Three textbook topology results (ko cohomology,
  ASS convergence, ABP splitting) enter only as commentary on the
  *separate* bordism-group identification
  `Ω_5^{Spin^{Z4}} ≅ ℤ_16`."
- The `Ω` mismatch is reconciled: all four bordism-group occurrences
  (:65, :254, :278, :301) now read `Ω_5^{Spin^{Z4}} ≅ ℤ_16`; the prior
  `Ω^Spin_4 ≅ ℤ` is gone. (Line 353's `Ω_5^{Spin}(BG_{SM})` is the
  distinct spin-bordism chain, correctly different.)

### Prior 1.1 — L2 bibkeys absent from citation cache — 🔵 STILL-OPEN (RECOMMENDED, advisory)

- `docs/citation_verifications.jsonl` (50 records) still has zero
  entries for any L2 bibkey. All 16 bibkeys remain present in
  `src/core/citations.py` CITATION_REGISTRY (re-verified this run), so
  this is not a registry-absence blocker. Advisory only; restated as
  finding 1.1 below.

## New findings

### 4.1 — 🟡 REQUIRED — Cross-paper toolchain-pin contradiction: D2 pins stale Mathlib commit (L2 is correct)

- **Gate:** CrossPaperConsistency
- **Location:** `papers/L2/paper_draft.tex:12` vs `papers/D2/paper_draft.tex:15`
- **Observed:** L2 sets `\newcommand{\mathlibcommit}{5e932f97}`. D2
  sets `\newcommand{\mathlibcommit}{8850ed93}`. Both papers describe the
  same single Lean library; companion papers on the same result must
  cite the same toolchain.
- **Evidence:** `lean/lakefile.toml:21,27` — canonical Mathlib pin is
  `rev = "5e932f97dd25535344f80f9dd8da3aab83df0fe6"` (v4.29.1 tag); the
  same comment records `8850ed93` as the *previous* v4.29.0 pin.
  `lean/lean-toolchain` = `leanprover/lean4:v4.29.1`. CLAUDE.md
  likewise gives `5e932f97`. L2's pin matches canonical; **D2's pin is
  the stale v4.29.0 commit.**
- **Expected:** D2 should be updated to `5e932f97`. L2 needs no change —
  its pin is correct. Flagged here per the re-review charge to verify
  cross-paper (vs D2) consistency on the toolchain.
- **Fix:** Update `papers/D2/paper_draft.tex:15` to
  `\newcommand{\mathlibcommit}{8850ed93}` → `{5e932f97}`. No L2 edit
  required. Because the defect is on the D2 side, this does not block
  the L2 gate; it is recorded as a D2 CrossPaperConsistency action.
- **Cache:** n/a

### 1.1 — 🔵 RECOMMENDED — L2 bibkeys absent from citation-verification cache (carried over)

- **Gate:** CitationIntegrity
- **Location:** `papers/L2/paper_draft.tex:381-457` (all 16 bibitems)
- **Observed:** None of L2's 16 bibkeys has a record in
  `docs/citation_verifications.jsonl` (50 records total; 0 for any L2
  key). All 16 ARE in CITATION_REGISTRY — not a registry-absence
  blocker. The three highest-load arXiv citations (`Wang2024`
  2312.14928, `GarciaEtxebarria2019` 1808.00009, `MollerScheithauer2024`
  2112.12291) were fetch-verified `match` in the prior run; the four
  remaining arXiv-bearing bibitems (`Kitaev2009`, `FidkowskiKitaev2010`,
  `BeaudryCampbell`, `Schellekens1993`) remain un-fetched.
- **Evidence:** `grep -c "<key>" docs/citation_verifications.jsonl` = 0
  for all L2 keys.
- **Expected:** Cache entries so future runs amortize; fetch-verify the
  four remaining arXiv bibitems before submission.
- **Fix:** Append `verdict: match` records for the fetched-clean keys
  via `scripts/citation_cache.append_record`; fetch the four remaining.
- **Cache:** not-fetched this run (advisory; no metadata change since
  prior fetch).

## Re-verified clean (normal pass)

- **Class 1 (CitationIntegrity):** all 16 bibkeys in CITATION_REGISTRY;
  no wrong-target/wrong-author issues introduced; no new bibitems.
- **Class 3 (LeanProofSubstance):** the two load-bearing new theorems
  (`SmoothSpinManifold4.rokhlin`, `sixteen_convergence_unconditional`)
  lean_verify kernel-pure; not placeholders. All eight other cited
  theorem names (`fermion_count_gives_central_charge`,
  `central_charge_fractional_without_nu_R`, `qParam_shift`,
  `framing_anomaly_constraint`, `eight_dvd_latticeSig`,
  `z16_anomaly_always_cancels_with_nu_R`, `constraints_without_nu_R`,
  `rokhlin_strictly_stronger`) resolve to real declarations in source.
- **Class 5 (NarrativeGrounding):** the `16 ∣ σ` / `8 ∣ σ` framing is
  now backed by kernel-pure theorems; no overclaim. The "first" /
  unification language is appropriately scoped (Wang's four-way
  equivalence explicitly retained as external).
- **Class 6 (AssumptionDisclosure):** axiom posture correct (0 axioms,
  `TPFConjecture` tracked Prop disclosed); the sole topological input
  `2 ∣ σ/8` is explicitly stated as the residual assumption.
- **Class 9 (CountFreshness):** axiom count now macro-driven; all
  count literals route through `counts.tex` macros.

## QI Candidate

**Toolchain-pin staleness is the residual face of the same
late-absorption asymmetry flagged last run.** The prior review's QI
candidate (an infrastructure upgrade absorbed by one bundle but not its
companion) was remediated on the `16 ∣ σ` / axiom axes for both L2 and
D2 — but the Mathlib-commit pin moved `8850ed93 → 5e932f97` (v4.29.0 →
v4.29.1) and only L2 picked it up; D2 still carries `8850ed93`.
Recommend a `validate.py` cross-bundle check that asserts every bundle's
`\mathlibcommit` literal equals the canonical `lakefile.toml` rev, run
across all 13 bundles, not just the pair that triggered an absorption.
This would have caught the D2 pin drift mechanically.
