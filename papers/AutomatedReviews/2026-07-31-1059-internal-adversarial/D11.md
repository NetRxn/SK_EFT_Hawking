---
paper: D11
bundle_target: D11
reviewer: adversarial-reviewer
model: claude-opus-5
review_date: 2026-07-31T10:59:00Z
readiness_gates_version: 1
artefact_commit: c10188ac
round: 4
---

# Adversarial Review — D11 (Kernel-Verified Topological Band Theory and Metamaterial Substrate)

## Summary

Round-4 fresh-context sweep, classes 1–8 in order: **3 BLOCKER / 6 REQUIRED / 11 RECOMMENDED**, touching CitationIntegrity, LeanProofSubstance, CrossPaperConsistency, NarrativeGrounding and NumericalFreshness. The three blockers are all the same species round 3 named — remediations that landed in one layer and not the others: the abstract and acknowledgments assert a PhysLib dependency that **zero of the 22 D11 modules actually have**; `effectiveModuli_enclosure`'s own docstring still headlines the **Voigt–Reuss–Hill enclosure** and speaks of "the effective modulus", i.e. exactly the claim its module header and §5 spent a paragraph retracting; and the executed companion notebook still prints "Mass inversion is necessary but NOT sufficient at fixed grid size". Everything the round-3 brief said was fixed *in the paper* is fixed and verified — the two citations now match a fresh arXiv fetch, the necessity claims in `HaldaneWitness.lean` are scoped, the PhysLib pin scope and the past-tense novelty wording landed, `PAPER_STRATEGY.md` :180/:396 are clean, and my independent recount reproduces 22 / 6133 / 394 / 454 **exactly**. **Pass criterion (zero BLOCKER) is NOT met.**

**Freeze compliance:** artifacts were frozen for this review, so I did **not** append to `docs/citation_verifications.jsonl`. The five fresh-fetch records are transcribed inline under class 1 for the author to append.

## Findings

### 1.1 — 🟡 REQUIRED — the new cache-content gate checks Title and arXiv only; it is blind to Authors, Venue, Year and DOI — the fields that carried half of both round-3 blockers

- **Gate:** CitationIntegrity
- **Location:** `scripts/validate.py:4087-4138`
- **Observed:** The gate added this round compares each `.abstract.txt` header's `Title:` and `arXiv:` against `CITATION_REGISTRY`. The cache header format also carries `Authors:`, `Venue:`, `DOI:` and `Year:` — none is compared.
- **Evidence:** In-memory mutation test against the live check (no files touched), mutating one registry field at a time for `LeanLJ2025` and re-running the check function:
  ```
  BASELINE                                      passed=True  fail_details=[]
  MUT-A registry title drift                    passed=False fail_details=['cache_title_mismatch:LeanLJ2025']
  MUT-B registry AUTHOR drift (round-2 error)   passed=True  fail_details=[]
  MUT-C registry arxiv drift                    passed=False fail_details=['cache_arxiv_mismatch:LeanLJ2025']
  MUT-D registry journal drift                  passed=True  fail_details=[]
  MUT-E registry year drift                     passed=True  fail_details=[]
  MUT-F registry doi drift                      passed=True  fail_details=[]
  ```
  MUT-B is literally the round-2 defect (`Ugwuanyi, S., Jones, M., Velkey, K., Josephson, T.`) and it passes. `BoldoLaxMilgram2016`'s round-2 defect was a **five-author list naming a non-author**; if that had shipped with the correct title, this gate would not have seen it either.
- **Expected:** A gate written to close "the refuted metadata survived in the cache" must compare every field the cache header carries and the registry stores.
- **Fix:** Extend the loop to `Authors:` (normalized surname-set comparison, initials-tolerant), `DOI:` and `Venue:`/`Year:`. Keep Title/arXiv as-is; they are sound.
- **Cache:** n/a
- **Lane:** `infra`

### 1.2 — 🟡 REQUIRED — `PAPER_TO_PHASE['D11']` is `Phase-6C` but the directory on disk is `Phase-6c`; the new gate silently no-ops for all 11 D11 citations on any case-sensitive filesystem

- **Gate:** CitationIntegrity
- **Location:** `src/core/citations.py` `PAPER_TO_PHASE` (D11 row) and every D11 `primary_source_path`; consumed at `scripts/validate.py:4107-4109`
- **Observed:** `PAPER_TO_PHASE['D11'] == 'Phase-6C'`; the workspace directory is `Lit-Search/Phase-6c/` (lowercase). `find_phrase.py` reports the real path as `.../Phase-6c/primary-sources/BoldoLaxMilgram2016.abstract.txt`. Five other papers map to `Phase-6c`; D11 is the lone `Phase-6C`.
- **Evidence:** `ls -1 Lit-Search | grep -i phase-6c` → `Phase-6c`, `Phase-6ca` — no `Phase-6C`. The content-agreement half of the gate does `cache_file = find_workspace() / ps; if not cache_file.exists(): continue` — a **silent skip**, not a failure. On macOS (case-insensitive APFS) both spellings resolve, which is the only reason the gate covers D11 today. On a case-sensitive filesystem (Linux CI) all 11 D11 entries would fall through the `continue` and the gate would report `cache_content_agreement PASS` having checked nothing.
- **Expected:** One canonical spelling, and a hard failure rather than a silent `continue` when a declared `primary_source_path` does not resolve.
- **Fix:** Normalize the D11 phase token to `Phase-6c` in `PAPER_TO_PHASE` and in the 11 `primary_source_path` values; change the `continue` to a failing `Detail` when `ps` is declared but missing.
- **Cache:** n/a
- **Lane:** `infra`

### 1.3 — 🟡 REQUIRED — the rationale written into the new gate and copied verbatim into both remediated cache files is factually wrong about `promote_primary_sources.py`

- **Gate:** CitationIntegrity
- **Location:** `scripts/validate.py:4096-4098`; `Lit-Search/Phase-6c/primary-sources/BoldoLaxMilgram2016.abstract.txt` (§"This was not merely stale bookkeeping"); same paragraph in `LeanLJ2025.abstract.txt`
- **Observed:** All three assert: "scripts/promote_primary_sources.py writes cache-file contents BACK INTO CITATION_REGISTRY, so a stale cache is not inert: the next promote run would have re-injected the hallucinated title and author list."
- **Evidence:** `promote_primary_sources.py` never reads a `.abstract.txt`. Its module docstring (`:2-26`) states its two inputs: `docs/primary_sources_state.json` (fetch *verdicts* + paths) and `docs/missing_bibkey_stubs.json`. Operation (a) (`:246-264`) inserts exactly two lines into an existing entry — `'inprep': False,` and `'primary_source_path': <path>,`; it writes **no** metadata. Operation (b) (`render_stub_entry`, `:160-196`) renders `authors`/`title`/`journal`/`year`/`doi`/`arxiv` from the **stub** dict, which `extract_missing_bibkeys.py` derives from the `\bibitem` block, and applies only to bibkeys *absent* from the registry. Neither path can re-inject cache contents into an existing entry.
- **Expected:** The two `.abstract.txt` files are the pipeline's designated strongest evidence class and exist so "a later reviewer can see EXACTLY how strong that check was". Putting a false mechanism claim in them, twice, is the same defect class they document.
- **Fix:** Replace the paragraph in both cache files and the comment at `validate.py:4096-4098` with the true justification, which is sufficient on its own: a stale cache defeats the *only* content-layer check the pipeline has, and any later reviewer reading it for corroboration is misled. If a write-back path is genuinely wanted, build it; do not assert one that does not exist.
- **Cache:** n/a

### 1.4 — 🔵 RECOMMENDED — Wiener (1912) carries a date and a specific numeric bracket with no bibitem; Voigt and Reuss are named with no citation at all

- **Gate:** CitationIntegrity
- **Location:** `papers/D11/paper_draft.tex:280-282` ("The Wiener~(1912) bounds are the harmonic and arithmetic means of the phases… the Wiener bracket is $[1.6, 2.5]$"); `:288` ("we ship \thm{voigtModulus} and \thm{reussModulus}")
- **Observed:** Three attributed historical results — Wiener 1912, Voigt, Reuss — appear in the argument, one with a year and a quantitative claim, none with a `\bibitem`. The bibliography has 11 entries; none is Wiener, Voigt or Reuss.
- **Evidence:** I verified the Wiener numbers: at `(ε_h, ε_i, f) = (1, 4, ½)`, harmonic mean `1/(0.5/1 + 0.5/4) = 1.6`, arithmetic mean `0.5·1 + 0.5·4 = 2.5`. `[1.6, 2.5]` is correct, and the paper's inference (constituent `[1,4]` strictly wider, entailing neither family) is correct. The attribution is simply uncited. Round-3 finding 5.2 explicitly recommended adding Voigt 1889 / Reuss 1929 bibitems; the remediation instead *added* the Wiener attribution without adding any of the three.
- **Expected:** A dated attribution supporting a quantitative comparison gets a bibitem, or the date is dropped and a standard composites text is cited.
- **Fix:** Add Wiener (Abh. Math.-Phys. Kl. Königl. Sächs. Ges. Wiss. **32**, 509, 1912), Voigt (1889) and Reuss (ZAMM **9**, 49, 1929), or cite a single composites monograph for all three; register each in `CITATION_REGISTRY` with a primary-source cache.
- **Cache:** n/a (no bibitem to verify)

### 1.5 — 🔵 RECOMMENDED — two bibitems remain less complete than their arXiv records support

- **Gate:** CitationIntegrity
- **Location:** `papers/D11/paper_draft.tex:577-578` (`LiPaulson2018`), `:573-575` (`LjungstromMortberg2024`)
- **Observed:** `LiPaulson2018` is cited as a bare 2018 preprint with no venue. `LjungstromMortberg2024` gives volume 35 but no article number and no DOI.
- **Evidence:** Fresh fetch `https://arxiv.org/abs/1804.03922` → journal-ref **Journal of Automated Reasoning, 2019**, DOI `10.1007/s10817-019-09521-3`. Fresh fetch `https://arxiv.org/abs/2401.16336` → journal-ref **Math. Struct. Comp. Sci. 35 (2025) e16**, DOI `10.1017/S0960129525000131`. Both titles and author lists match the bibitems exactly. Round-3 finding 1.5 raised the `LiPaulson2018` half; unactioned.
- **Expected:** Cite the published records.
- **Fix:** Two one-line bibitem edits plus the corresponding `journal`/`doi` fields in `CITATION_REGISTRY` (both currently `doi: None`).
- **Cache:** fresh-fetch, verdict `match` for both.

### 1.6 — 🔵 RECOMMENDED — `arxiv_verified` is absent from 641 of 649 registry entries, so Gate 1's stated pass criterion is unevaluable

- **Gate:** CitationIntegrity
- **Location:** `src/core/citations.py` (registry schema); `docs/READINESS_GATES.md:34`; `scripts/readiness_gates.py:133-171`
- **Observed:** `READINESS_GATES.md` says Gate 1 "Passes iff … Every registry entry has `arxiv_verified == True` AND `doi_verified == True`". Only 8 of 649 entries carry an `arxiv_verified` key at all, and none of D11's four arXiv-bearing entries (`FukuiHatsugaiSuzuki2005`, `LjungstromMortberg2024`, `LiPaulson2018`, `BoldoLaxMilgram2016`, `LeanLJ2025`) does. The implemented evaluator `_eval_citation_integrity` checks only bibkey→registry coverage and defers everything else to Stage 13 by comment.
- **Evidence:** AST/dict scan of `CITATION_REGISTRY`: `entries: 649, with arxiv_verified field: 8`. `doi_verified` is `None` for the four arXiv-only entries.
- **Expected:** Either the doc's criterion or the schema. As it stands the canonical gate definition describes a check nothing performs, and the whole arXiv half of Gate 1 rests on the adversarial reviewer re-fetching by hand each round.
- **Fix:** Add `arxiv_verified` alongside `doi_verified` for every entry with an `arxiv` field and set it from the citation cache; or amend `READINESS_GATES.md` to state that arXiv/DOI correspondence is a Stage-13 obligation with no programmatic gate.
- **Cache:** n/a

**Class-1 clean results (record so a re-review need not repeat them).** All 11 bibkeys are in `CITATION_REGISTRY`; `used_in` is now exact (the round-3 `HasanKane2010` phantom D11 consumer is gone, and the duplicate `Berry1984` key is merged with both consumers and a duplicate-key guard now in `citation_primary_sources_present`); zero bibitems uncited, zero cites without a bibitem. `citation_primary_sources_present` PASS (526 bibkeys / 365 cached / 0 missing / `cache_content_agreement` PASS).

**Both round-3 BLOCKERs are genuinely closed, verified by independent fresh fetch this round:**
- `BoldoLaxMilgram2016` — `https://arxiv.org/abs/1607.03618` → *"The Lax-Milgram Theorem. A detailed proof to be formalized in Coq"*, **François Clément (SERENA), Vincent Martin (LMAC)**, 2016, no journal-ref. Bibitem, registry and cache all now agree. Verdict `match`. The parenthetical distinguishing the pen-and-paper preprint from the CPP-2017 Coq development is correct and the §5 prose ("a detailed proof prepared for Coq formalization is available… and a Coq development followed") is now the accurate framing. Residual cosmetic: the bibkey still names Boldo, who is not an author of the cited work; the cache file flags this itself.
- `LeanLJ2025` — `https://arxiv.org/abs/2505.09095` → *"Benchmarking Energy Calculations Using Formal Proofs"*, **Ejike D. Ugwuanyi, Colin T. Jones, John Velkey, Tyler R. Josephson**, Molecular Physics (2025), DOI `10.1080/00268976.2025.2539421`. Bibitem matches field-for-field. Verdict `match`. `prior_art_novelty.md` §C4 and §C5 both propagated correctly.

Also fresh-fetched this round, all verdict `match`: `cond-mat/0503172` (Fukui, Hatsugai, Suzuki; JPSJ **74**, 1674–1677, 2005 — title, all three authors and journal-ref confirmed), `2401.16336`, `1804.03922`. The six DOI-only bibitems (`TKNN1982`, `Berry1984`, `Haldane1988`, `MaxwellGarnett1904`, `HashinShtrikman1962`, `HashinShtrikman1963`) are byte-identical to the `edd9878d` text that round 3 fresh-fetched today with verdict `match`, so they are **cache-verified**; I additionally resolved two DOIs to corroborate venue: `10.1063/1.1728579` → `pubs.aip.org/jap/article/33/10/3125/…/A-Variational-Approach-to-the-Theory-of-the` (J. Appl. Phys. **33**, 3125 ✓), `10.1016/0022-5096(63)90060-7` → `linkinghub.elsevier.com/retrieve/pii/0022509663900607` (J. Mech. Phys. Solids ✓). Publisher landing pages themselves are outside the egress whitelist; that limit is already disclosed in `prior_art_novelty.md` §5.

**Cache records not appended (freeze).** For the author to append to `docs/citation_verifications.jsonl`:
`BoldoLaxMilgram2016` hash `1b1527f3c723…` verdict `match`; `LeanLJ2025` hash `21e91d1d0f29…` verdict `match`; `FukuiHatsugaiSuzuki2005` hash `f223d3b08785…` verdict `match`; `LjungstromMortberg2024` hash `1eeef904b5d2…` verdict `match` (venue incomplete, see 1.5); `LiPaulson2018` hash `0b929ae023eb…` verdict `match` (venue absent, see 1.5). Hashes are `scripts.citation_cache.bibitem_hash` over the current bibitem text.

### 2.1 — (no findings) — ParameterProvenance

- **Gate:** ParameterProvenance
- **Observed:** D11 quotes no experimental or measured parameter. Every number is a dimensionless model parameter fixed by declaration: `t = t₂ = 1`, `φ = π/2`, `m ∈ {1,5,6}`, `(m₁,m₂,κ,a) = (1,2,1,1)`, `(ε_h,ε_i,f) = (1,4,½)`, `(M₁,M₂) = (1,4)`, `N₁ = N₂ = 4`, `g² ∈ [99/100, 1]`. `fermiVelocity` is left parametrized (`dispersion_slope_eq_hbar_fermiVelocity`), so no CODATA value is asserted.
- **Verdict:** gate not applicable to this bundle. No finding.

### 3.1 — 🔴 BLOCKER — `effectiveModuli_enclosure`'s own docstring still claims the Voigt–Reuss–Hill theorem and still speaks of "the effective modulus"

- **Gate:** LeanProofSubstance / NarrativeGrounding
- **Location:** `lean/SKEFTHawking/EffectiveModuli.lean:86-88`, and the module title at `:4`; cross-referenced falsely from `lean/SKEFTHawking/EffectiveMediumBounds.lean:32-33`; the paper cites the declaration by name at `papers/D11/paper_draft.tex:289`
- **Observed:** The declaration's docstring reads verbatim:
  ```
  /-- **Voigt–Reuss–Hill enclosure (Phase 6CE W3).** For a physical two-phase elastic composite
  (`0 < M₁ ≤ M₂`, `0 ≤ f ≤ 1`), the effective modulus is bracketed `M₁ ≤ M_Reuss ≤ M_Voigt ≤ M₂` —
  the elastic analog of the Maxwell–Garnett constituent enclosure. -/
  theorem effectiveModuli_enclosure …
  ```
  It is byte-identical to the version shipped in `230b3528`, before any remediation.
- **Evidence:**
  - The module header **13 lines above it** (`:17-27`) now says: "⚠️ **This is NOT the Voigt–Reuss–Hill theorem** … No effective modulus of a composite appears anywhere in `effectiveModuli_enclosure`'s statement — only the two closed-form averages and the constituents."
  - The paper (`:288-295`) says: "This is an ordering of the two *averages* together with constituent bounds; no effective modulus of a composite appears in the statement. The classical Voigt--Reuss bracket $M_{\rm R} \le M_{\rm eff} \le M_{\rm V}$ is a claim about a physical $M_{\rm eff}$ and is *not* formalized here."
  - The statement itself confirms the header and the paper: `M1 ≤ reussModulus M1 M2 f ∧ reussModulus M1 M2 f ≤ voigtModulus M1 M2 f ∧ voigtModulus M1 M2 f ≤ M2`. No `M_eff` appears.
  - The sibling file compounds it: `EffectiveMediumBounds.lean:32-33` now tells readers "The elastic sibling `EffectiveModuli.lean` genuinely does prove the arithmetic/harmonic pair, **correctly named there as the Voigt–Reuss bracket**." It is not named that there; it is named the Voigt–Reuss–**Hill** enclosure, in both the module title and the theorem docstring.
  - This is not a bystander string. `effectiveModuli_enclosure` is one of the two names §5 cites by hand, and the docstring is what a referee reads on hover or on jump-to-definition. Round-3 BLOCKER 5.1 was escalated on exactly this reasoning ("a referee following those citations lands on that header"); the same reasoning applies unchanged.
- **Expected:** The declaration a paper cites must not, in its own docstring, assert the theorem the paper says is not formalized.
- **Fix:** (1) Rewrite `:86-88` as "**Average ordering plus constituent bounds (Phase 6CE W3)**… the two closed-form averages are ordered and bracketed by the constituents; no physical effective modulus appears — see the module header, this is *not* the Voigt–Reuss–Hill theorem." (2) Change the module title at `:4` from "(Voigt–Reuss–Hill bounds)" to "(Voigt/Reuss averages and their ordering)". (3) Correct `EffectiveMediumBounds.lean:32-33`. (4) Re-invoke; per Stage 13 the re-run is the evidence.
- **Cache:** n/a

### 3.2 — 🔵 RECOMMENDED — `effectiveMedium_hashinShtrikman_enclosure` still bundles two conjuncts entailed by its first (round-3 3.1, unactioned)

- **Gate:** LeanProofSubstance
- **Location:** `lean/SKEFTHawking/EffectiveMediumBounds.lean:84-88`; quoted at `papers/D11/paper_draft.tex:275-277`
- **Observed:** `maxwellGarnett 1 4 (1/2) = 2 ∧ 1 ≤ … ∧ … ≤ 4`, proved `⟨hval, by rw [hval]; norm_num, by rw [hval]; norm_num⟩`. Conjuncts 2 and 3 are `norm_num` restatements of conjunct 1 — a P2 bundle-redundancy artefact by the project's own preemptive-strengthening checklist.
- **Evidence:** Value hand-recomputed: numerator `1·(4 + 2 + 2·½·3) = 9`, denominator `4 + 2 − ½·3 = 4.5`, `9/4.5 = 2` exactly. The `= 2` claim is correct; the enclosure adds nothing.
- **Expected:** Not a placeholder and not a tautology — do not escalate. But the paper calls it "the worked instance" of `effectiveMedium_constituent_bounds`, which reads as if the enclosure carries independent content.
- **Fix:** Prose-only, or strengthen to a genuinely independent rational bracket at an irrational-valued parameter point. The declaration name also still says `hashinShtrikman`; the paper handles that explicitly at `:283-286`, which is acceptable, but a rename would remove the last carrier of the misnomer.
- **Cache:** n/a

**Class-3 clean results.** 117 of the 142 `\thm{}`/`\texttt{}` tokens in the draft resolve to real declarations inside the 22 D11 modules; the other 25 are prose, revision hashes, Mathlib names (`Matrix.det_fin_three`, `toIocMod`, `toIocDiv` — all confirmed present at the pin), module names, or Lean keywords. **Zero phantom names.** Intersection of the cited set with the 576-node `PlaceholderMarker` set is **empty**. Kernel purity, extracted from `lean/lean_deps.json` over the 22 modules: **568 declarations** (454 theorem, 109 def, 5 structure), `axiom_deps_project` **empty for every one**; of the 117 cited names, 116 have `axiom_deps_core` **exactly** `{propext, Classical.choice, Quot.sound}` and the 117th (`Torus`) is an `abbrev` with an empty closure — so the abstract's "axiom profile exactly {…}" for every headline theorem holds. Source scan of all 22 files: **0 `sorry`, 0 `native_decide`, 0 `maxHeartbeats`, 0 `axiom`** (the two `native_decide` string hits are prose disclaimers in docstrings). **0 tracked-hypothesis Props.** Structural-tautology sweep of `exceptional_point_defective` (4-way conjunction over four independently proved lemmas), `bernal_spectrum_not_determine_model` (4-way, each conjunct with real content including the `det_fin_four` collapse), `band_gap_falsifier` (routes through spectral completeness, genuinely refutational), `band_gap_rational_enclosure`, `phononic_band_gap_exists`, `pt_symmetric_real_spectrum_iff` (both directions carry proof), `sum_plaquetteArg_eq_two_pi_mul_latticeChern` and `latticeChern_gaugeInvariant` — all packaging constructors over substantive lemmas, **no hypothesis-echo**. The two theorems added since round 3 (`blochPauli_gap_zero_of_eq_zero`, `blochPauli_gap_pos_iff`) are real and account exactly for the 392→394 delta; `blochPauli_gap_pos_iff` is a genuine biconditional, so the paper's `:166-170` is now accurate where the round-3 draft could only claim the forward direction. `haldaneFrame_latticeChern_eq_neg_one` closes with kernel `decide` on a finite sum after rewriting all 16 plaquette branches — legitimate, and distinct from `native_decide` as the paper insists.

### 4.1 — 🔵 RECOMMENDED — `Phase6CE_Roadmap.md` still carries both retracted misnomers

- **Gate:** CrossPaperConsistency
- **Location:** `docs/roadmaps/Phase6CE_Roadmap.md:5`
- **Observed:** "W2 … (Hashin–Shtrikman/Wiener enclosure) + concrete `norm_num` rational instance `effectiveMedium_hashinShtrikman_enclosure`; W3 `effectiveModuli_enclosure` (the elastic **Voigt–Reuss–Hill bracket** `M₁ ≤ M_Reuss ≤ M_Voigt ≤ M₂`)".
- **Evidence:** `find_phrase.py "Voigt–Reuss–Hill"` returns five hits: three in `EffectiveModuli.lean` (two of them finding 3.1), this roadmap line, and the round-3 review. `find_phrase.py "hashinShtrikman"` shows the roadmap is the only *doc* still asserting the module proves an HS/Wiener enclosure, now that `PAPER_STRATEGY.md:180` is corrected.
- **Expected:** A roadmap is a status ledger; leaving the retracted naming here is how it re-enters the next lift.
- **Fix:** Two phrase edits, with a dated correction note matching the ones in the two Lean files.
- **Cache:** n/a

**Class-4 clean results.** `Berry1984` is the only bibkey D11 shares with another draft (`paper18_doublon_gate:597-599`); both give Proc. R. Soc. A **392**, 45 (1984) — consistent, and the registry duplicate that shadowed paper18's consumer is merged with a duplicate-key guard now in place. `validate.py --check bundle_consistency` PASS (2 exact-match clusters, 0 flagged). D7 (`:35, :124, :163, :221`) and E2 (`:418-423`) use "categorical Chern" / "real-space Chern marker" language for `ChernBridge.lean`; I read that module and confirm D11's carve-out is accurate — `categoricalChernExpansion : ℝ → ℝ → ChebyshevExpansion` and `realSpaceChernAt : … → ℝ` are real-valued Chebyshev evaluations, not integer bundle invariants — and the carve-out is mirrored into `prior_art_novelty.md` N1 as round-3 4.1 required. `docs/PAPER_STRATEGY.md:180` is fully corrected: the "Hashin–Shtrikman enclosure" claim now reads "constituent-bracket enclosure (NOT Hashin–Shtrikman…)", the "via the PhysLib Schur substrate" claim now reads "(NOT via PhysLib — the non-Hermitian modules import only Mathlib and each other)" — which I independently confirm — and the acoustic novelty claim is hedged to "we are aware of no prior". `:396` now reads "finite-lattice FHS Chern — bulk-boundary DEFERRED". `docs/PAPER_DRAFT_MAPPING.md:109` reports 6133 / 394 / 83, matching the paper exactly (round-3 4.2 closed).

### 5.1 — 🔴 BLOCKER — the abstract and acknowledgments assert a PhysLib dependency that none of the 22 D11 modules has

- **Gate:** NarrativeGrounding
- **Location:** `papers/D11/paper_draft.tex:32-33` (abstract, first sentence) and `:538-541` (acknowledgments)
- **Observed:**
  - Abstract: "We report a machine-checked formalization, in the \Lean{} proof assistant **with Mathlib and the PhysLib physics library**, of five layers of band theory and effective-medium physics…"
  - Acknowledgments: "Formalization was carried out in \Lean{}~4 **against Mathlib and PhysLib**. We thank the Mathlib and PhysLib communities for the underlying libraries."
- **Evidence:** Import scan of all 22 D11 modules: **zero `import Physlib.*`**. The complete import multiset is `Mathlib` ×11, `Mathlib.Analysis.Complex.Exponential` ×1, and 22 intra-bundle `SKEFTHawking.*` imports — nothing else. Project-wide only four modules import PhysLib (`MolecularHamiltonian`, `CoulombRelativeBound`, `QuantumNetwork/QuantumFDTFloor`, `QuantumNetwork/ThermalAssignmentFloor`), none of them D11 and none transitively reachable from D11. PhysLib appears in the paper **only as a negative comparison** — §3 (`:190-193`) argues PhysLib's `TightBindingChain` is single-band and gapless "so a two-band object had to be built"; §5 (`:305-306`) that PhysLib's optics directory is a placeholder; §Scope (`:493-497`) that it has "no multi-band Bloch matrix, no gap, no Berry quantity and no topological invariant". So the paper simultaneously claims to have built on PhysLib and argues at length that PhysLib had nothing usable.
  This is the identical defect the author already fixed one layer away: `docs/PAPER_STRATEGY.md:180` was corrected this round to "(NOT via PhysLib — the non-Hermitian modules import only Mathlib and each other; corrected 2026-07-31)". The correction was not propagated to the paper's own abstract.
- **Expected:** A referee who opens any of the 22 files sees `import Mathlib` and nothing else. The abstract is the single most-read sentence in the bundle; it must not assert a library dependency the artifact does not have.
- **Fix:** Abstract → "in the \Lean{}~4 proof assistant against Mathlib". Acknowledgments → thank Mathlib for the underlying library and PhysLib separately for the physics-library context the scope claims are checked against, stating plainly that no D11 module imports PhysLib. Keep the PhysLib revision pin at `:489-491` — that claim ("Every PhysLib claim in this paper is likewise pinned, to revision `c4843367`, and verified against that checkout") is true and I re-verified it.
- **Cache:** n/a

### 5.2 — 🔴 BLOCKER — the executed companion notebook still asserts the retracted necessity claim, in source and in stored output

- **Gate:** NarrativeGrounding / CrossPaperConsistency
- **Location:** `notebooks/D11_TopologicalBandTheory_Technical.ipynb` cell 11 (source line and the stored stdout); supporting: `docs/dev-loops/Phase6ED/LAB_NOTEBOOK_INDEX.md:32`
- **Observed:** The notebook prints, and its stored output contains:
  ```
  NOTE m = 5 is INSIDE the window yet certifies C = 0.
  Mass inversion is necessary but NOT sufficient at fixed grid size.
  ```
- **Evidence:**
  - `find_phrase.py "necessary but not sufficient" --ignore-case` returns 15 hits; after excluding unrelated projects and the *corrective/history* mentions, the only live D11 assertion is this notebook (twice: `:2168` source, `:2183` output).
  - It is directly negated by three artifacts remediated this round: `HaldaneWitness.lean:31-34` ("The converse direction — necessity … is **NOT proved here or anywhere in this development**; it is sampled at the single point `m = 6`"), `HaldaneWitness.lean:1428-1433` (the theorem's own headline, changed from "NECESSARY BUT NOT SUFFICIENT" to "NOT SUFFICIENT"), and the paper's Fig. 3 caption (`:322-325`: "Necessity is *not* certified — it rests on the single sample $m = 6$ — so the figure makes no necessity claim at all").
  - The notebook is not an internal scratch file: `SK_EFT_Hawking_Inventory.md:929-932` lists it as part of what D11 "ships" — "Both ship with a fresh-authored `paper_draft.tex` (compiled to PDF), a `prior_art_novelty.md`, figures under `papers/D1x/figures/`, and an **executed companion notebook** (`notebooks/D11_TopologicalBandTheory_Technical.ipynb`…)". `validate.py --check notebook_exec` executes it, so the stored output is re-blessed on every run.
  - Secondary: `docs/dev-loops/Phase6ED/LAB_NOTEBOOK_INDEX.md:32` calls `haldane_massInversion_not_sufficient_at_N4` "**the necessity theorem**". It is the insufficiency theorem; the necessity direction is precisely what it does not prove.
- **Expected:** Zero remaining necessity assertions. The project's own standing rule (`HaldaneWitness.lean:55-58`, paper `:450-455`) is that no statement in this corpus may claim the invariant is nonzero "exactly where the masses invert" at fixed grid size; asserting necessity is half of exactly that claim.
- **Fix:** Change the notebook print to "Mass inversion is NOT sufficient at fixed grid size; the necessity direction is sampled (m = 6), not proved." Re-execute the notebook so the stored output changes too — the output cell is the artifact a reader sees. Fix `LAB_NOTEBOOK_INDEX.md:32`. Then re-invoke.
- **Cache:** n/a

### 5.3 — 🟡 REQUIRED — "the present paper formalizes six of these seven" is true only if the Wiener/Hashin–Shtrikman clause counts as formalized, which §5 explicitly denies

- **Gate:** NarrativeGrounding (unification/first-claim)
- **Location:** `papers/D11/paper_draft.tex:477-483`
- **Observed:** The §Novelty enumeration has seven clauses: [1] a Chern number or Chern class *of a vector bundle*; [2] an acoustic or phononic band structure; [3] an exceptional point; [4] a $\mathcal{PT}$ transition; [5] **a Maxwell--Garnett or Wiener/Hashin--Shtrikman-type effective-medium bound**; [6] a honeycomb band structure; [7] the Haldane model. The parenthetical then says "the present paper formalizes **six of these seven**, so a present-tense universal negative would be self-refuting."
- **Evidence:** Clause [1] is the acknowledged non-delivery — `FHSLatticeGauge.lean:14-16` and the paper `:470-471` both disclaim equality to any continuum first Chern class. So the "six" must include clause [5]. But §5 (`:279-286`) and `EffectiveMediumBounds.lean:31-33` state the opposite in as many words: "Neither the Wiener nor the Hashin–Shtrikman bound is formalized in this development. Do not cite this module as having proved one." The paper therefore either miscounts (it is five of seven, plus a partial), or asserts it formalized a Wiener/HS-type bound that it did not. Round-3 finding 5.2 flagged precisely this ("asserting novelty over work not done"); the remediation **added** "Wiener/" to the clause rather than removing the unformalized families.
- **Expected:** The novelty enumeration and the arithmetic must survive being read against §5.
- **Fix:** Split clause [5] into "a Maxwell–Garnett effective-medium bound" (formalized) and drop or separately scope the Wiener/Hashin–Shtrikman families, restating the count accordingly; or say "six of these seven — the exception being the vector-bundle Chern class — with the Wiener/Hashin–Shtrikman half of clause five likewise not formalized here, as §5 states." Mirror into `prior_art_novelty.md` N6.
- **Cache:** n/a

### 5.4 — 🟡 REQUIRED — `prior_art_novelty.md` §4 still attributes Voigt/Reuss to Hashin–Shtrikman 1963 and the Maxwell–Garnett formula to Hashin–Shtrikman 1962

- **Gate:** NarrativeGrounding (attribution-claim)
- **Location:** `papers/D11/prior_art_novelty.md:166-168`
- **Observed:** "⚠ **For the Maxwell–Garnett *permittivity* formula cite the 1962 paper, not the 1963 elastic one** — a common bibliography error. The 1963 paper is the correct citation for the **Voigt/Reuss/Hashin–Shtrikman *elastic* bounds**."
- **Evidence:** Both halves are wrong. (a) The Maxwell–Garnett permittivity formula's source is Maxwell Garnett 1904 (`10.1098/rsta.1904.0024`), which the paper correctly cites at `:264`; Hashin–Shtrikman 1962 is a variational-bounds paper, not the mixing rule. (b) The Voigt (1889, iso-strain) and Reuss (1929, iso-stress) averages are not Hashin & Shtrikman's; HS 1963's contribution is the strictly tighter elastic pair, which this bundle does not formalize. Round-3 finding 5.2 named this line explicitly — "`prior_art_novelty.md` §4's note … carries the same error and must change" — and it is unchanged. The paper's own §5 is now correct, so this document, which the draft's header comment (`:11`) names as its novelty backing, is the only place the misattribution still lives at bundle level.
- **Expected:** The novelty-backing document must not misattribute the results the paper is careful about.
- **Fix:** Rewrite as: "Cite Maxwell Garnett 1904 for the mixing formula. Cite HS 1962 for the electromagnetic variational bounds and HS 1963 for the elastic ones — neither of which is formalized here. The Voigt and Reuss averages are Voigt 1889 and Reuss 1929." Same edit resolves finding 1.4's registry side.
- **Cache:** n/a

### 5.5 — 🔵 RECOMMENDED — Fig. 4's caption calls the Voigt and Reuss averages "bounds", re-importing the misnomer §5 removes

- **Gate:** NarrativeGrounding
- **Location:** `papers/D11/paper_draft.tex:257-260`
- **Observed:** "Right: Voigt and Reuss elastic **bounds** with their *exact* arithmetic-minus-harmonic gap (\thm{voigt\_sub\_reuss\_eq})."
- **Evidence:** §5 (`:288-295`) argues at length that what is formalized is an ordering of two *averages* and that the bracket-on-`M_eff` reading is not formalized. Calling the same two curves "bounds" thirty lines earlier undoes the distinction for any reader who reads the figure first — which, in a two-column PRB layout with `figure*` at the top of the page, is most of them. The figure's own legend is careful ("Voigt (iso-strain, arithmetic)", "Reuss (iso-stress, harmonic)", "exact Voigt − Reuss gap"); only the caption slips.
- **Verified correct in the same figure:** at `f = 2/3`, `M₁ = 1`, `M₂ = 4` — Voigt `= 3`, Reuss `= 2`, gap `= 1`, and the exact formula `f(1−f)(M₁−M₂)²/((1−f)M₂+fM₁) = (2/9)(9)/2 = 1` ✓. Left panel: MG at `f = ½` is exactly 2 ✓, inside `[1,4]` ✓.
- **Fix:** "Right: the Voigt and Reuss elastic *averages* with their exact arithmetic-minus-harmonic gap".
- **Cache:** n/a

### 5.6 — 🔵 RECOMMENDED — Fig. 1's `±π` and the caption's evenness justification rest on a step that is not formalized; the PNG was not regenerated

- **Gate:** NarrativeGrounding
- **Location:** `papers/D11/figures/d11_fig1_phononic_band_gap.png` (legend); caption at `papers/D11/paper_draft.tex:182-186`
- **Observed:** The caption was corrected to "Both edges are *attained* at $k = \pi$ (\thm{branchMinus\_at\_pi}, \thm{branchPlus\_at\_pi}), and by evenness of the branches in $k$ also at $-\pi$". The PNG legend still reads `ω²₋(±π) = 1 (lower edge attained)` and `ω²₊(±π) = 2 (upper edge attained)`, with filled/open "certified" markers drawn at both `−π` and `+π`.
- **Evidence:** `PhononicBandGap.lean:85, :92` state `diatomicCrystal.branchMinus Real.pi = 1` and `… branchPlus Real.pi = 2` — at `Real.pi` only. Grep of `PhononicBandGap.lean` + `AcousticBlochOperator.lean` finds no `_at_neg_pi`, no evenness lemma, and no `Real.cos_neg` rewrite outside the Hermiticity proof. The evenness is mathematically immediate (`disc` depends on `k` only through `Real.cos (k*a)`), but the paper's whole thesis is that every step is kernel-checked, and this one is not.
- **Expected:** Either a one-line lemma or a caption/legend that stops marking `−π` as certified.
- **Fix:** Add `branchMinus_neg_pi` / `branchPlus_neg_pi` via `Real.cos_neg` (two lines each) and cite them; then the figure needs no change. Otherwise regenerate the PNG with `π`-only markers.
- **Cache:** n/a

### 5.7 — 🔵 RECOMMENDED — `|m| ≈ 3.3177` still has no reproducible computational source

- **Gate:** NarrativeGrounding
- **Location:** `papers/D11/paper_draft.tex:393-396`; `src/core/visualizations.py:15246`
- **Observed:** The prose half of round-3 finding 5.4 is fixed — the body now reads "the invariant flips *numerically* near $|m| \approx 3.3177$ --- a bisection, with no Lean backing". The reproducibility half is not: the value exists only as `fig.add_vline(x=3.3177, …)` and as prose in `formulas.py:11799` / `HaldaneWitness.lean`. No function computes it and no test pins it.
- **Evidence:** Grep across `src/` for `3.3177` / `haldane_flip` returns the one plot literal and two docstring mentions. The dependent arithmetic checks out: `3√3 = 5.196152`, `(5.196152 − 3.3177)/5.196152 = 36.1%`, so "roughly a third" is fair, and `HaldaneWitness.lean:1443-1444`'s "about 36 %" agrees.
- **Fix:** Add a `haldane_flip_location(N)` bisection helper to `formulas.py` with a golden test, and have `visualizations.py` call it rather than hardcode.
- **Cache:** n/a

### 5.8 — 🔵 RECOMMENDED — §5's history clause about `voigt_sub_reuss_eq` inverts the actual history

- **Gate:** NarrativeGrounding
- **Location:** `papers/D11/paper_draft.tex:296-301`
- **Observed:** "The sharpest statement here is \thm{voigt\_sub\_reuss\_eq}, which gives the arithmetic-minus-harmonic gap *exactly*, … rather than as the inequality $\ge 0$ that a docstring had previously asserted while only the weaker form was formalized."
- **Evidence:** The clause is self-contradictory as written (if the docstring asserted `≥ 0` and only the weaker form was formalized, then docstring and formalization agreed). The real history is the reverse: `git show 230b3528:lean/SKEFTHawking/EffectiveModuli.lean:18` asserted the *exact identity* in the module header — "The Reuss ≤ Voigt gap is exactly `f(1−f)(M₁−M₂)²/((1−f)M₂+fM₁) ≥ 0`" — while only `reuss_le_voigt` (the inequality) was formalized, until `f3e8f12f` shipped `voigt_sub_reuss_eq`. So the docstring over-claimed with the *stronger* statement, not the weaker one.
- **Fix:** "…rather than only as the inequality `M_V ≥ M_R`, which was all that was formalized when the module header already asserted the exact identity."
- **Cache:** n/a

### 5.9 — 🔵 RECOMMENDED — `sum_forwardDiff_eq_zero` is called the torus's "single load-bearing property" but has zero consumers

- **Gate:** NarrativeGrounding
- **Location:** `papers/D11/paper_draft.tex:128-131`
- **Observed:** "Its single load-bearing property is telescoping: \thm{sum\_forwardDiff\_eq\_zero} states that a discrete forward difference sums to zero around the torus, which is what makes the raw curl sum vanish."
- **Evidence:** `sum_forwardDiff_eq_zero` (`FiniteTorus.lean:49-51`) has **zero consumers project-wide** — grep across `lean/` finds no reference outside its own file, and a `name_deps_project` scan over all 40 236 `lean_deps.json` entries returns an empty consumer list. `sum_rawCurl_eq_zero` (`FHSLatticeGauge.lean:88-93`) is proved from `sum_shift` directly. The mathematical statement in the prose is true; the named lemma is an orphan and is not the one doing the work.
- **Fix:** Cite `sum_shift` as the load-bearing lemma (with `sum_forwardDiff_eq_zero` as its packaged corollary), or route `sum_rawCurl_eq_zero` through `sum_forwardDiff_eq_zero` so the citation is backed by a call — the project's own preemptive-strengthening rule #3 applied at paper level.
- **Cache:** n/a

**Class-5 clean results.** Every round-3 item the brief listed as remediated *in the paper* is verified applied and correct:
- **Necessity (round-3 5.1):** `HaldaneWitness.lean:31-34` (module header), `:302-311` (`haldane_mass_inversion_iff` docstring), `:1398-1405` (headline theorem docstring), `:1426-1433` (anti-witness headline) and `:55-61` (the dangling reference to the deleted sentence) are all scoped; only insufficiency is claimed and the `m = 6` sampling limit is named at each site. The paper's own prose and Fig. 3 make no necessity claim. `haldane_chern_iff_mass_inversion` has zero occurrences anywhere.
- **PhysLib pin scope (round-3 5.2/5.3):** `:489-491` now reads "Every PhysLib claim **in this paper** is likewise pinned, to revision `c4843367`" — the two substantive claims above it (§3 `TightBindingChain`, §5 optics placeholder) are now inside the scope word. All three claims independently re-verified at `c48433678e8f…`: `Physlib/CondensedMatter/TightBindingChain/Basic.lean:283` `BrillouinZone := Set.Ico (-π/a) (π/a)` (a real interval), single-band `energyEigenvalue k = E0 − 2t cos(ka)` at `:486`; `Physlib/Optics/Basic.lean` "This directory is currently a place holder."; **zero** `Chern`/`berryCurvature`/`berryConnection` strings anywhere in the package.
- **Novelty tense:** now "no *prior* formalization" (see 5.3 for the residual arithmetic issue).
- **Mathlib negatives, re-verified at `81a5d257`:** all 14 `Chern` occurrences are `Chernoff`/`ChernoffBound`; zero `ChernClass`/`PontryaginClass`/`StiefelWhitney`/`EulerClass`; no de Rham cohomology (`BDeRham.lean` is perfectoid ring theory); `Analysis/Calculus/DifferentialForm/Basic.lean:34-40` TODO reads "same for manifolds (**not defined yet**)"; `MeasureTheory/Integral/CurveIntegral/Poincare.lean:19-22` "Poincaré lemma for 1-forms … for convex sets"; `Matrix.det_fin_three` exists at `LinearAlgebra/Matrix/Determinant/Basic.lean:816` and `det_fin_four` does not; no Jordan normal form; Sobolev present, zero two-scale convergence. Pins confirmed against `lake-manifest.json`: Mathlib `81a5d257c8e410db227a6665ed08f64fea08e997`, PhysLib `c48433678e8fb6306ebcd48453300c8e16058a62`, toolchain `leanprover/lean4:v4.32.0`.
- **Carve-outs and coverage limits:** all four mandatory carve-outs present and correctly characterised (`:493-506`); both coverage limits stated (`:508-512`); no unhedged "first in any prover" survives anywhere in the draft — Gate 10's obligation is met by hedging.
- **Kernel no-gos:** both registered in `KERNEL_NOGO_REGISTRY` with exactly the backing theorems the paper names (`honeycomb_phase_chart_gl2z_invariant` ← `structureFactor_zero_set_not_shear_invariant` + `isHoneycombChart_of_neighbours`; `spectrum_determines_multiband_model` ← `bernal_spectrum_not_determine_model` + `bernal_chirality_two` + `bernalSwapped_chirality_zero`); `nogo_substrate_integrity` PASS.
- **Standing-methodology claim** (`:49`, `:445-448`) is grounded: the eigenvector-level-invariant rule is recorded in `KernelNoGos.lean:846`, `BernalBilayer.lean:216` and `Phase6ED/LAB_NOTEBOOK_INDEX.md:81`.
- **Spot-checked physics, all correct:** `ω²± = 3/2 ± √(5/4 + cos k)` for `(1,2,1,1)` (mid `= 3/2`, disc `= 1/4 + 1 + cos k`), giving exactly `(1, 2)` at `k = π` and matching `disc_at_pi = 1/4`; `sqrt_two_enclosure : 141/100 ≤ √2 ≤ 142/100`; `ep_proximity_enclosure` is tight at `g² = 99/100` (`Δ = 2·√(1/100) = 1/5`), matching Fig. 2's annotation; `norm_exp_mul_I_sub_one_sub_id_le : ‖e^{ix} − 1 − ix‖ ≤ x²` — constant 1, every real `x`, no validity ball, as claimed; `structureFactor_linear_expansion_global` bounds the difference of *norms*, exactly as the prose says; `latticeChern := −∑ plaquetteBranch` so the `−1` sign convention is real; `haldaneFrameTopo` is `blochFrameOfD (haldaneD 1 1 (π/2) 1 ∘ bzPoint)` on `Torus 4 4`, i.e. the stated parameter point; 16 vertices and 32 links on a `4×4` torus ✓; `3 ∣ N` caveat matches `HaldaneWitness.lean:102-108` including the corrected Dirac-point-sampling cause; `principal := toIocMod two_pi_pos (-π)` and `branchIndex := toIocDiv two_pi_pos (-π)` as stated; all 22 modules confirmed root-imported from `lean/SKEFTHawking.lean`.

### 6.1 — (no findings) — AssumptionDisclosure

- **Gate:** AssumptionDisclosure
- **Observed:** Every load-bearing structural hypothesis is disclosed, and the ones a referee would probe are disclosed emphatically:
  - `AdmissibleBandFrame.overlap_ne` (`BlochFrame.lean:86-87`) → `:147-151`, with the explicit warning that it is not a consequence of a spectral gap.
  - north-pole positivity `0 < √(dNormSq (D k)) + (D k) 2` (`BlochFrameOfD.lean:195-197`) → `:173-175`.
  - `blochLatticeChern_eq_zero_of_narrow_D`'s `|arg| < π/4` (`BlochFrameOfD.lean:255-262`) → `:157-159`, and it is genuinely the route by which both `haldane_trivial_phase_chern_zero` and `haldane_massInversion_not_sufficient_at_N4` are discharged.
  - `IsHoneycombChart` chart-specificity → `:338-342` and `:352-355`.
  - `3 ∣ N` grid inadmissibility → `:472-475`, with the correct mechanism.
  - `coneWinding` certified non-marginal but not alias-free → `:402-404`, disclosed rather than elided.
  - `effectiveMedium_constituent_bounds`' `εh ≤ εi` ordering hypothesis → `:273-275`, correctly identified as load-bearing rather than WLOG.
- **Evidence:** Zero tracked-hypothesis Props exist in the bundle. The one hypothesis the round-3 draft had to hedge (`blochPauli_gap_pos` forward-only) is no longer a hypothesis at all: the converse shipped this round.
- **Verdict:** gate passes. No finding.

### 7.1 — 🟡 REQUIRED — "83 definitions and structures" is not reproducible under the paper's stated rule, and is not what "definitions and structures" counts

- **Gate:** NumericalFreshness
- **Location:** `papers/D11/paper_draft.tex:101-107`
- **Observed:** "22 root-imported modules --- 6133 lines, 394 theorems and lemmas, 83 definitions and structures, counted at source level. The counting rule is stated so it is reproducible: a declaration is counted when a line begins with `theorem` or `lemma`, optionally preceded by an attribute bracket or by `private`/`protected`."
- **Evidence:** My independent recount over the 22 files:
  | quantity | rule applied | value |
  |---|---|---|
  | modules | root-imported from `SKEFTHawking.lean` | **22** ✓ |
  | lines | `wc -l` | **6133** ✓ |
  | theorems+lemmas | the paper's stated rule verbatim | **394** ✓ |
  | `def` + `structure` | literal reading of "definitions and structures" | **81** ✗ |
  | `def` + `structure` + `abbrev` | undisclosed | **83** |
  | `lean_deps.json` theorems | `kind == theorem` over the 22 modules | **454** ✓ |
  The two `abbrev`s are `Torus` (`FiniteTorus.lean:19`) and `LinkField` (`FHSLatticeGauge.lean:44`). Four of the five literals reproduce exactly; the fifth reproduces only if a reader guesses that `abbrev` is silently folded in — and the sentence immediately preceding it promises reproducibility. Zero `instance` declarations, so that is not the gap. This was raised in round 3 and the brief confirms it is unresolved.
- **Expected:** Either state the second rule ("a definition is counted when a line begins with `def`, `structure` or `abbrev`, with the same optional prefixes") or report 81 and mention the two abbreviations separately.
- **Fix:** One clause added to `:104-105`, plus the matching update to `docs/PAPER_DRAFT_MAPPING.md:109`, which repeats "83 defs/structures" with no rule at all.
- **Cache:** n/a

### 7.2 — 🔵 RECOMMENDED — D11's five count literals are still hardcoded, and no REPORTS edge exists, so the Gate-9 drift evaluator is vacuous for this bundle

- **Gate:** NumericalFreshness
- **Location:** `papers/D11/paper_draft.tex:101-107`
- **Observed:** No `\input{counts.tex}` anywhere in the file. `validate.py --check count_literals` now *does* see D11 (round-3's QI candidate is closed — both globs were widened to `papers/*/paper_draft.tex` at `scripts/validate.py:3193` and `:3583`, with the fix documented in-source) and WARNs: `D11 — 2 count-literal matches: L101 "394 theorems"; L107 "454 theorems"`.
- **Evidence:** Graph query on `paper:D11`: outgoing edges are `CITES_SOURCE ×11`, `HAS_FIGURE ×4`, `CLAIMS ×2` — **zero `REPORTS` edges**, so the "no REPORTS edge has `stale == True`" half of Gate 9 passes vacuously. `counts_fresh` PASS; `numerical_literals` PASS for D11 (no inline unit-bearing literals). All five values are correct today (see 7.1), so nothing is presently wrong — but if a module lands tomorrow, the paper rots silently and only the WARN fires.
- **Fix:** Retrofit the bundle onto the `\input{../../docs/counts.tex}` macro chain, or add a D11-specific counts block generated by the same pipeline, so the REPORTS edges exist and the values are derived rather than typed.
- **Cache:** n/a

### 8.1 — (no findings) — ProductionRunHealth

- **Gate:** ProductionRunHealth
- **Observed:** D11 makes **no** Monte-Carlo, RHMC or simulation-evidence claim — grep of the draft for "Monte Carlo" / "numerical evidence" / "simulation" returns nothing. The bundle is Lean-only; every headline is kernel-checked. No `ProductionRun` node is linked to `paper:D11` and none is required. The single non-theorem numerical value in the paper is `3.3177`, handled under 5.7 and correctly labelled numerical in both body and figure.
- **Verdict:** gate passes. No finding.

**Gate-4 (ComputationCorrectness) clean — round-3 finding 8.1 is closed.** `tests/test_bundle_formulas_d11_d12.py` now exists with 68 tests, all passing in 0.06 s, and all 15 D11 `formulas.py` functions are referenced. The assertions are `golden`-shape, pinned to the Lean witnesses (58 `pytest.approx`/`isclose` calls), e.g. `F.maxwell_garnett(1.0, 4.0, 0.5) == pytest.approx(2.0, abs=1e-12)` against `effectiveMedium_hashinShtrikman_enclosure`, and the diatomic branches against `branchMinus_at_pi`/`branchPlus_at_pi`. This was the round-3 REQUIRED and it is genuinely fixed.

## QI Candidate

**qi-citationintegrity-three-layer-gate-asymmetry.** The bundle's citation metadata lives in four layers — `\bibitem` in the `.tex`, `CITATION_REGISTRY`, `Lit-Search/Phase-*/primary-sources/<key>.abstract.txt`, and `prior_art_novelty.md` — and after this round's fix exactly **one** of the six pairwise correspondences is machine-checked: cache↔registry, on two fields (Title, arXiv). Nothing checks bibitem↔registry, which is the pair a reader is actually exposed to; nothing checks any layer against `prior_art_novelty.md`, which is where round-3 5.2's misattribution survived and is *still* surviving (finding 5.4); and the one gate that exists is blind to Authors/Venue/Year/DOI (1.1) and silently no-ops if the declared cache path does not resolve on a case-sensitive filesystem (1.2). `bibitem_title_primary_source` is WARN-only and returned 21 advisories this run without failing. The pattern across rounds 2, 3 and 4 is identical: a citation defect is found, fixed in one or two layers, and re-found in a third. Recommend a single `citation_layer_agreement` check that (a) parses each `\bibitem` body into `{authors, title, venue, year, arxiv, doi}` and compares field-by-field against the registry, (b) extends the cache-header comparison to Authors/Venue/Year/DOI, (c) fails rather than skips on an unresolvable `primary_source_path`, and (d) greps each bundle's `prior_art_novelty.md` for every registry title/author string it cites. Four layers, one gate, one failure mode instead of three rounds of whack-a-mole.

**qi-narrativegrounding-companion-artifacts-outside-the-review-surface.** Finding 5.2 is a claim the project formally retracted, still asserted in an *executed* notebook that the inventory lists as part of what the bundle ships, and re-blessed on every `validate.py --check notebook_exec` run. Stage 13's anchor list, `bundle_consistency`, and `count_literals` all scan `paper_draft.tex`; none scans `notebooks/`, `figures/*.png` text, or a bundle's `prior_art_novelty.md` for retracted-claim strings. A bundle's shipped surface is larger than its `.tex`. Recommend a per-bundle **retracted-claim denylist** (seeded from `KERNEL_NOGO_REGISTRY.false_statement` and each wave's recorded retractions) enforced by `find_phrase.py` across the bundle's whole artifact set — `.tex`, `prior_art_novelty.md`, the companion notebook source *and* stored outputs, and the Lean modules it cites — so a retraction has one place to be checked instead of five places to be missed.
