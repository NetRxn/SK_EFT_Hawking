---
paper: D11
bundle_target: D11
reviewer: adversarial-reviewer
model: claude-opus-5
review_date: 2026-07-31T19:51:00Z
readiness_gates_version: 1
artefact_commit: 9d631fad
artefact_state: working-tree (docs/citation_verifications.jsonl +4 protocol records by me; lean/lean_deps.json.hash and figures/*.png modified by a concurrent process, not by me)
round: 10
---

# Adversarial Review — D11 (Kernel-Verified Topological Band Theory and Metamaterial Substrate)

## Summary

Round-10 fresh-context sweep, classes 1–8 in order: **0 BLOCKER / 8 REQUIRED / 10 RECOMMENDED**.
Round 9's BLOCKER is genuinely closed and the replacement text is accurate about what the cited
development contains — I re-fetched the abstract and text-extracted the 40-page PDF, and the
enumeration matches word for word. Two residues survive the fix: the primary-source cache file's
*second* note block still asserts `H*(CP-inf)` twenty lines below the correction that retracts it,
and the corrected sentence's load-bearing clause now over-reaches in the opposite direction
(§4.4 of that source is titled "The Thom isomorphism and the Gysin sequence", and its Gysin map is
cup product with a constructed class `e` — the Euler class in substance). Of the five remediations I
was asked to attack, **one holds** (the notebook digest defeats every mutation round 9 proposed) and
**four do not**: the ledger records landed but 7 of 8 cite a commit that does not contain the change
they describe; one of them closes a finding whose subject is stale in the tree *right now*; and the
carve-out hardening is dead code at `9d631fad` — `meta['status'] = ledger.get('status', status)`
overrides it unconditionally two lines later, so a two-key ledger record still closes a critical
finding. (A concurrent D12 round-10 remediation found and fixed that one mid-review; I re-measured,
and three of the four carve-out exploits — including a new one this document tripped on itself —
still reproduce against the current working tree.)

**Zero BLOCKER is a statement about *this round's* findings.** D11 still carries **two** `critical`
`ReviewFinding` nodes with `status: open` in the live graph —
`…:1530-…:D11:5.1` (round-7 stored-figure blocker) and `…:1801-…:D11:1.1` (round-9's CP^∞ blocker).
I independently verified that the *substance* of both is fixed: the notebook's four stored figure
outputs re-execute clean under the round-9 digest rule, and the paper's carve-out now matches the
source abstract verbatim. Neither has a supersession record, so Gate 11 stays blocked on
bookkeeping, not on substance — the same shape as round-9 finding 4.3, one round later. Filing those
two records (with the correct commits — see 4.1) is the shortest path to a GREEN aggregate.

**Stage-12 precondition IS green:** `validate.py` 57/57 (1114 warnings), 342.8 s, archived to
`docs/validation/reports/validation_20260731T195827Z.json`. `compile_bundle_pdf.py D11` →
`OK pages=9 overfull=0 tex_errors=0 unresolved_refs_in_pdf=0 dropped_class_opts=0`.

**Files I touched (all restored, SHA-256 verified):**
`notebooks/D11_TopologicalBandTheory_Technical.ipynb`
(pre `b604edc7f4a67ac3b5d1e84f5b36e6a2940299559af0eb6961693664fac91b91`, post identical).
All `build_graph` / ledger / review-document exploits were run against a **copied project root** in
`/tmp` with `bg.PROJECT_ROOT` repointed — zero writes under `papers/AutomatedReviews/` or
`docs/review_finding_supersessions.json`. All `CITATION_REGISTRY` mutations were in-memory and
reverted in a `finally`. **Intentionally left modified:** `docs/citation_verifications.jsonl`
(+4 protocol-mandated records). **Not mine:** `lean/lean_deps.json.hash` (mtime 13:40:08, written by
`extract_lean_deps.py`/`update_counts.py` — neither of which I ran; a concurrent round-10 reviewer is
live in this tree, appending cache records at 19:56 UTC) and the pre-existing `figures/*.png` churn.

## Round-9 reconciliation

Re-derived from the current tree, not from round 9's report.

| R9 | severity | verdict | evidence |
|---|---|---|---|
| 1.1 CP^∞ carve-out | BLOCKER | **fixed in the paper; two residues** | Paper text now matches the abstract verbatim. Cache file's `Caveat / usage note` still says `H*(CP-inf)` → 1.1. New over-reach in the replacement clause → 5.1. |
| 1.2 page-convention split | RECOMMENDED | **unchanged; answered** | Still 2 range-form vs 12 first-page. Direct answer with code evidence → 1.3. |
| 1.3 `LiPaulson2018` preprint-only | RECOMMENDED | **reproduces** | Fresh arXiv fetch returns JAR (2019) + DOI `10.1007/s10817-019-09521-3`; bibitem + registry still preprint-only → 1.4. |
| 1.4 `arxiv_verified` absent | RECOMMENDED | **reproduces** | 0 of 14 → 1.6. |
| 1.5 declared-but-unresolvable path | RECOMMENDED | **reproduces; restated precisely** | Two independent silent-skip paths, not one → 1.5. |
| 1.6 `is_stale()` TypeError | RECOMMENDED | **reproduces** | 6 of 123 records → 1.6. |
| 3.1 `exceptional_point_defective` conj-4 | RECOMMENDED | **reproduces** (left open by design) | Body unchanged at `NonHermitianBloch.lean:79-84` → 3.2. |
| 4.1 `PAPER_DRAFT_MAPPING.md:109` | REQUIRED | **reproduces** | Row still `6248`; `counts.tex` is `6258` → 4.2. |
| 4.2 "necessity theorem" | RECOMMENDED | **worse than reported** | Round 9 called `LAB_NOTEBOOK_INDEX.md:32` clean. It is not — and it is **untracked**, so `git grep` cannot see it → 4.4. |
| 4.3 nine findings wrongly open | REQUIRED | **records landed; attribution wrong** | 8 filed, graph reflects them (18 → 17). 7 of 8 cite the wrong commit → 4.1. |
| 4.4 14→18 misattribution | REQUIRED | **accepted** | Arithmetic re-derived: 1059→5, 1530→2 (after closures), 1652→4, 1801→6 = 17. Consistent with round 9's account. |
| 4.5 carve-out exploitable | REQUIRED | **NOT fixed; a third exploit added** | E1 and E2 reproduce verbatim end-to-end; the new guard is unreachable → 4.3. |
| 5.1 notebook bulk-array rule | REQUIRED | **fixed as described** | The rounded digest defeats all four round-9 mutations. Two *other* surfaces still hide → 5.2. |
| 6.1 elastic hypotheses | RECOMMENDED | **reproduces** | `:316-330` unchanged → 6.1. |
| 7.1 metadata notes stale | RECOMMENDED | **reproduces** | Still "566 declarations" / "all 8 pages" vs live 578 / 9 → 7.1. |

## Findings

### 1.1 — 🟡 REQUIRED — the round-9 correction reached three of the four artifacts it names; the primary-source cache still asserts `H*(CP-inf)` twenty lines below the note that retracts it

- **Gate:** CitationIntegrity
- **Location:** `Lit-Search/Phase-6C/primary-sources/LjungstromMortberg2024.abstract.txt`, final `Caveat / usage note:` block
- **Observed:** the file now carries a correct `⚠️ CORRECTION 2026-07-31` block and a corrected
  `Relevance to this project:` block. Its last block, verbatim, still reads:
  > `Caveat / usage note:`
  > `MANDATORY CARVE-OUT for D11 novelty: Gysin + H*(CP-inf) is the substrate from which Chern classes are DEFINED. D11 may claim no prover defines Chern classes, but must acknowledge this.`
- **Evidence:** the same file, ~20 lines earlier, says *"there is no CP^inf in this development"*. So
  one artifact makes both claims. Round 9's fix instruction named this file explicitly and called it
  "the worst of them, because it records 'cohomology rings including CP-infinity' as a *verification
  record*". The other three sites verify as corrected: `papers/D11/paper_draft.tex:534-543`
  (renders on PDF p. 8), `papers/D11/prior_art_novelty.md:70-94`, and
  `CITATION_REGISTRY['LjungstromMortberg2024']` `provides` + `notes`.
- **Why this is REQUIRED and not BLOCKER.** The bibitem, the registry entry and the paper prose are
  all now correct, so none of Gate 1's four blocking conditions is met. But this file is the artifact
  `validate.py --check citation_primary_sources_present` designates as the pipeline's strongest
  evidence class, and it is the file a future fresh-context reviewer will read first. A verification
  record that contradicts itself is worse than one that is simply wrong: the reader cannot tell which
  half is current.
- **Expected:** one internally consistent record.
- **Fix:** rewrite the `Caveat / usage note` block to the same content as the corrected `Relevance`
  block. Then `grep -ri "CP-inf\|CP\^inf\|ℂP\^∞" Lit-Search/ src/ papers/D11/` must return only the
  retraction sentences.
- **Cache:** fresh-fetch (arXiv abs + full v2 PDF text extraction); record appended, verdict `match`.

### 1.2 — 🟡 REQUIRED — the corrected carve-out's supporting verification is scoped to the abstract, and its conclusion is quoted downstream as a fact about the development; the development formalizes the Thom isomorphism

- **Gate:** CitationIntegrity (SourceFidelity, folded in per `READINESS_GATES.md:238`)
- **Location:** `papers/D11/prior_art_novelty.md:92-94`; mirrored in
  `Lit-Search/Phase-6C/primary-sources/LjungstromMortberg2024.abstract.txt`
- **Observed:** the novelty record's C2 entry closes with
  > "Verified by direct fetch — the abstract does not mention Chern classes, characteristic classes,
  > Euler class, or Thom class."
  and the cache file carries the same sentence. Read as scoped to the abstract it is true. It is
  positioned as, and consumed as, the evidential basis for the carve-out's *scope*.
- **Evidence:** I downloaded `arxiv.org/pdf/2401.16336v2` and text-extracted all 40 pages (`pypdf`,
  127 644 chars). Token counts: `Euler` **0**, `Chern` **0**, `characteristic class` **0**,
  `Thom` **6**, `Gysin` **16**. Section **4.4 is titled "The Thom isomorphism and the Gysin
  sequence"** and reads:
  > "Brunerie [2016] also introduced, for the first time in HoTT, the Thom isomorphism and the Gysin
  > sequence. While Brunerie's original constructions concerned integral cohomology, we may easily
  > generalise them to cohomology with coefficients in an arbitrary commutative ring R."
  with the isomorphism stated as `φ_i : H^i(B,R) ≅ H̃^{i+n}(Ẽ,R)`.
  So the development **does** formalize the Thom isomorphism. The abstract omits it; the record's
  sentence therefore certifies an absence that the source contradicts, in the direction that makes
  D11's novelty look stronger.
- **Expected:** a verification record whose stated scope matches the evidence actually gathered, or
  evidence gathered at the scope the conclusion is stated at.
- **Fix:** two edits. (a) Replace the sentence with: *"Verified against the full v2 PDF: the
  development contains no occurrence of `Euler`, `Chern` or `characteristic class`; it DOES formalize
  the Thom isomorphism (§4.4, generalizing Brunerie 2016) and states the Gysin sequence as cup
  product with a constructed class `e`."* (b) Add the Thom isomorphism to the paper's enumeration at
  `paper_draft.tex:534-537`, since the sentence's job is to concede the surrounding machinery and
  the Thom isomorphism is the nearest precursor to an Euler class (Milnor–Stasheff §12: the Euler
  class is the restriction of the Thom class). Conceding it costs nothing and closes the hole.
- **Cache:** fresh-fetch (PDF full text).
- **Lane:** `prose`

### 1.3 — 🔵 RECOMMENDED — the page-convention question, answered: the registry field should hold the FIRST PAGE, and the bibitem must match it exactly

- **Gate:** CitationIntegrity
- **Location:** `src/core/citations.py` `Voigt1889.page` (`'573-587'`), `Reuss1929.page` (`'49-58'`), against the other twelve D11 entries
- **Observed (direct answer to the round-10 brief's question).** **First-page.** Not on aesthetics —
  the registry has two programmatic consumers and both are built for first-page semantics.
- **Evidence:**
  1. `src/core/citations.py:12085-12089` — `format_citation` composes
     `f"{journal} {volume}, {page} ({year})"`, i.e. it emits a *bibitem-shaped string*. Measured:
     ```
     Voigt1889            -> "Voigt et al., Annalen der Physik 274, 573-587 (1889)"
     bibitem :612-614     -> "Ann. Phys. \textbf{274}, 573 (1889)"
     TKNN1982             -> "Thouless et al., Physical Review Letters 49, 405 (1982)"
     bibitem :592-594     -> "Phys. Rev. Lett. \textbf{49}, 405 (1982)"
     ```
     Under first-page the two agree; under range form they cannot.
  2. `src/core/citations.py:12123` — `find_unregistered_citations` matches a reference to a registry
     entry by `str(entry.get('page','')) in ref_text`, i.e. **substring containment against a
     rendered reference**. Measured against a first-page-style rendering:
     ```
     Voigt1889 '573-587'          in ref -> False   ← entry becomes unmatchable
     Reuss1929 '49-58'            in ref -> False   ← entry becomes unmatchable
     HashinShtrikman1963 '127'    in ref -> True
     MaxwellGarnett1904 '385'     in ref -> True
     TKNN1982 '405'               in ref -> True
     ```
     A range-form `page` silently disables orphan-citation detection for that entry.
  3. `READINESS_GATES.md:36` requires "Journal/volume/page/year in bibitem matches registry". Under
     exact comparison the only two D11 entries that fail are the two most carefully verified ones.
  Both range values are themselves *correct* (Semantic Scholar returns `pages: 573-587` for Voigt);
  this is a schema question, not a factual one.
- **Expected:** one convention, stated in the module docstring's per-entry schema block
  (`src/core/citations.py:14-22`), which currently documents `page` with no semantics at all.
- **Fix:** set `Voigt1889.page = '573'` and `Reuss1929.page = '49'`; if the ranges are worth keeping,
  add an optional `page_range` field and document both. Then amend the schema block to read
  `page: str | None — FIRST page only; must equal the page printed in every bibitem`. An exact
  `page` comparison in the venue check round 8 proposed then needs no tolerance.
- **Cache:** cache-verified (`Voigt1889`, `Reuss1929`, `MaxwellGarnett1904`, `HashinShtrikman1963`
  all carry same-day `verdict: match` records whose `bibitem_hash` reproduces from the current tree).

### 1.4 — 🔵 RECOMMENDED — `LiPaulson2018` still cites a preprint whose published record exists, and `LjungstromMortberg2024` still omits its article number and DOI

- **Gate:** CitationIntegrity
- **Location:** `papers/D11/paper_draft.tex:635-636` and `:631-633`; `src/core/citations.py` `LiPaulson2018`, `LjungstromMortberg2024`
- **Observed:** `LiPaulson2018` bibitem ends `… Isabelle/HOL} (2018). arXiv:1804.03922.` — no journal,
  no DOI. Registry: `journal: None, volume: None, page: None, doi: None, doi_verified: None`.
  `LjungstromMortberg2024` prints `Math. Struct. Comp. Sci. \textbf{35} (2025).` — no article number,
  no DOI.
- **Evidence:** fresh arXiv fetches this round. `1804.03922` → `Journal reference: Journal of
  Automated Reasoning (2019)`, `DOI: 10.1007/s10817-019-09521-3`, authors Wenda Li and Lawrence C.
  Paulson — the bibitem's authors and title exactly. `2401.16336` → `Math. Struct. Comp. Sci. 35
  (2025) **e16**`, `https://doi.org/10.1017/S0960129525000131`. Both entries are **incomplete, not
  wrong**.
- **Fix:** four bibitem tokens and the matching registry fields. `LiPaulson2018` is the carve-out
  that concedes Isabelle's index-theoretic machinery; citing the arXiv preprint of a seven-year-old
  published JAR paper reads as a survey that stopped in 2018.
- **Cache:** fresh-fetch ×2, verdict `match`, records appended.

### 1.5 — 🔵 RECOMMENDED — `cache_path_unresolvable` is guarded on a property of the BIBKEY, not of the DECLARED PATH, and there are two silent-skip routes, not one — precise restatement

- **Gate:** CitationIntegrity (tooling)
- **Location:** `scripts/validate.py:4890-4905` (`check_citation_primary_sources_present`, the cache-content loop) and `:4930-4938` (the case-insensitive retry)
- **Observed.** The comment at `:4884-4886` states "a declared-but-unresolvable path is a FAIL rather
  than a silent skip". Measured: the Detail fires **only** when *both* (a) the declared path does
  **not** end in `.abstract.txt` **and** (b) `_discover_cache_for(bibkey)` finds nothing under any
  extension in any phase directory. For all fourteen D11 bibkeys — every one of which declares an
  `.abstract.txt` path and has a conventionally-named cache on disk — the FAIL is **structurally
  unreachable no matter what the field is set to**.
- **Evidence — the two routes, each demonstrated by an in-memory `CITATION_REGISTRY` mutation
  reverted in a `finally`, calling the live check:**
  ```
  BASELINE                                                              passed=True   fails=[]
  MUT-A  Voigt1889 path -> …/NoSuchFile1889.abstract.txt (nonexistent)  passed=True   fails=[]   ✗
  MUT-B  Voigt1889 path -> …/Voigt1889.pdf (nonexistent)                passed=True   fails=[]   ✗
  MUT-C  Voigt1889 path -> 'totally/bogus/path.abstract.txt'            passed=True   fails=[]   ✗
  MUT-D  Ljungstrom path -> a .pdf that does not exist                  passed=True   fails=[]   ✗
  MUT-E  Voigt1889 path -> Reuss1929's REAL cache                       passed=False  title+authors+doi  ✓
  MUT-F  Voigt1889 path -> None                                         passed=True   fails=[]
  MUT-G  Voigt1889 path None + hallucinated title                       passed=False  cache_title_mismatch  ✓ (round-8 fix holds)
  ```
  And, constructing the only reachable case with a synthetic registry entry:
  ```
  declared path NOT .abstract.txt + no cache anywhere -> FIRES  ['cache_path_unresolvable:ZZFake…']
  declared path IS  .abstract.txt + no cache anywhere -> silent []
  ```
  **Route 1 (MUT-A, MUT-C).** `if cache_file is None or not str(ps).endswith(".abstract.txt")` is
  **False** for an `.abstract.txt` path, so the discovery branch — the only place the Detail lives —
  is never entered and `_discover_cache_for` is never called. Non-existence then falls to the
  case-insensitive retry at `:4930`, whose failure branch is
  `if resolved is None: continue  # existence is the other half of this check`.
  **Route 2 (MUT-B, MUT-D).** The branch *is* entered, but the guard is `found is None` where
  `found = _discover_cache_for(bibkey)` — a property of the bibkey. Discovery succeeds, the loop
  silently re-points `cache_file` at the real cache, the content checks run against the right file,
  and the rotted declaration is never surfaced.
  Net: the Detail can only add signal for a bibkey that is *cited nowhere* (so the existence half
  never examined it), declares a non-`.abstract.txt` path, and has no cache — a near-empty set.
- **Expected:** the declared path validated on its own terms, independently of discovery.
- **Fix — mechanical, no guessing required.** Factor the existing case-insensitive resolution at
  `:4930-4941` into a helper `_resolve_ci(p: Path) -> Path | None`, then replace `:4890-4905` with:
  ```python
  ps       = entry.get("primary_source_path")
  declared = bool(ps)
  declared_file = _resolve_ci(find_workspace() / ps) if ps else None   # None == unresolvable
  found    = _discover_cache_for(bibkey)
  if declared and declared_file is None:
      title_details.append(Detail(f"cache_path_unresolvable:{bibkey}", False, <existing message>))
  cache_file = declared_file or found
  if cache_file is None:
      continue
  if cache_file.suffix not in (".txt", ".md"):
      continue
  ```
  Three properties this has and the current code does not: the FAIL depends on `declared and
  declared_file is None` **only** — never on `found`; the `.abstract.txt` suffix no longer gates
  whether the path is checked at all; and when the declaration resolves, content checks still run
  against it (so MUT-E keeps failing). Optionally add a second Detail when `declared_file` and
  `found` are both non-None and different — MUT-E only fails today because the wrong file happens to
  have a parseable header.

### 1.6 — 🔵 RECOMMENDED — `citation_cache.is_stale()` raises on 6 of 123 records, `stats()` is still dead, and no canonical bibitem extractor ships

- **Gate:** CitationIntegrity (tooling)
- **Location:** `scripts/citation_cache.py`
- **Observed / Evidence:** unchanged from rounds 7, 8 and 9 — same six bibkeys
  (`AaronsonGottesman2004`, `BMPRV1999`, `Brylinski2002`, `Hietala2020VOQC`, `Lewis2021VerifQC`,
  `Mukhopadhyay2024`), same `TypeError: can't subtract offset-naive and offset-aware datetimes`,
  `stats()` raises the same way, `hasattr(citation_cache, 'bibitems_from_tex') == False`. None is a
  D11 bibkey. Separately, **0 of D11's 14 registry entries carry an `arxiv_verified` key**, while
  `READINESS_GATES.md:34` states Gate 1 passes iff "Every registry entry has `arxiv_verified == True`
  AND `doi_verified == True`" — a canonical criterion no code evaluates.
- **Fix:** add `TypeError` to the `except` clause and return `True` (fail safe = re-fetch); ship
  `bibitems_from_tex(path) -> dict[key, text]` so hashes are comparable across rounds; and either
  populate `arxiv_verified` from the 123-record cache or amend `READINESS_GATES.md:34` to say plainly
  that arXiv correspondence is a Stage-13 obligation with no programmatic gate.

**Class-1 clean results.** 14 bibitems, 14 in `CITATION_REGISTRY`, 14 cited, zero uncited bibitems,
zero `\cite` without a bibitem, zero bibkeys missing from the registry. All 14 declare a
`primary_source_path`; all 14 resolve on disk; `citation_primary_sources_present` PASS. All 14
`bibitem_hash` values reproduce from the current tree under body-only hashing and all 14 carry a
non-stale `verdict: match` record. I fresh-fetched four anyway (`LjungstromMortberg2024` — abstract
**and** full PDF, `LiPaulson2018`, `BoldoLaxMilgram2016`, `LeanLJ2025`): every bibliographic field
matches. `LeanLJ2025`'s abstract confirms the paper's "nearest periodic-lattice-in-Lean precedent"
framing (Lennard-Jones energies under periodic boundaries, NIST-matched).
`BoldoLaxMilgram2016`'s authors are Clément (SERENA) and Martin (LMAC) — matching both the bibitem
and the registry, and confirming the pen-and-paper-not-a-formalization framing. The four records I
appended are the only change I left in the cache file.

### 2.1 — (no findings) — ParameterProvenance

D11 has **zero** `DEPENDS_ON` edges to `Parameter` nodes in the live graph. Every number in the paper
is a dimensionless model parameter fixed by declaration ($t=t_2=1$, $\varphi=\pi/2$,
$m\in\{1,5,6\}$, $(m_1,m_2,\kappa,a)=(1,2,1,1)$, $(\varepsilon_h,\varepsilon_i,f)=(1,4,\tfrac12)$), a
kernel-certified value, or a `counts.tex` macro. `numerical_literals` → "D11 — no inline unit-bearing
literals in body". I independently re-derived the two quantitative side-claims: Maxwell–Garnett at
$(1,4,\tfrac12)$ is exactly $2$, and the Wiener bracket the paper quotes as $[1.6, 2.5]$ is
$1/(0.5/1+0.5/4) = 1.6$ and $0.5\cdot1+0.5\cdot4 = 2.5$ — both correct, and the paper correctly
labels the second as computed from the definition rather than quoted. Gate not applicable.

### 3.1 — 🔵 RECOMMENDED — `bernal_spectrum_not_determine_model`'s fourth conjunct is a parameter-free arithmetic fact, and round 9 cleared it as "mutually independent"

- **Gate:** LeanProofSubstance
- **Location:** `lean/SKEFTHawking/GrapheneBand/BernalBilayer.lean:268-303`; `papers/D11/paper_draft.tex:471-476`
- **Observed:** the bundle's self-declared "most transferable result" is a four-way conjunction whose
  fourth component is
  ```lean
  (Complex.I : ℂ) ^ 2 ≠ Complex.I * (starRingEnd ℂ) Complex.I
  ```
  discharged by `simp [Complex.ext_iff]; norm_num`. It mentions no Hamiltonian, no parameter, and no
  bilayer object: it is `-1 ≠ 1`.
- **Evidence:** conjuncts 1–3 are the refutation in full — `bernalBlochSwapped` is Hermitian for all
  `(u, γ, f)`; its secular determinant equals `bernalBloch`'s for all `(u, γ, E, f)` (a genuine
  `linear_combination` argument over `det_fin_four`); and the two matrices differ at
  `(u,γ,f) = (0,1,i)`. Dropping conjunct 4 leaves "the spectrum does not determine the model"
  entirely intact. This is the project's own preemptive-strengthening question 1 ("If I drop any
  conjunct, does the theorem still mean the same thing?") and question 4 ("Could this statement be
  discharged by … `norm_num`? If yes, is the substantive content somewhere else?") —
  `CLAUDE.md`, Preemptive-strengthening discipline. The paper spends a clause presenting it as a
  fourth certified fact: "and that $i^2 \neq i\,\bar{\imath}$ --- the last being the arithmetic fact
  that separates the chirality coefficients". It does separate them, in the same sense that
  $-1 \neq 1$ separates anything; the *model* content is in `bernal_chirality_two` /
  `bernalSwapped_chirality_zero`, which the paper already cites one sentence earlier.
  **Round 9's class-3 clean block asserted these "four conjuncts are mutually independent"** — true
  but not the relevant property; independence does not imply content.
- **Why RECOMMENDED and not BLOCKER.** The theorem is true, the refutation is real and the physical
  conclusion is carried by conjuncts 1–3. This is P2/P3 bundle redundancy, not a tautology and not a
  placeholder. Stated so a later round can re-evaluate severity rather than re-derive the analysis.
- **Second, smaller point in the same sentence.** The paper says the two Hamiltonians are "distinct
  at $f = i$". The Lean witness is `bernalBlochSwapped 0 1 Complex.I ≠ bernalBloch 0 1 Complex.I` —
  distinctness at the *point* $(u,\gamma,f) = (0,1,i)$, not at $f=i$ for all $(u,\gamma)$.
- **Fix:** drop conjunct 4 and delete the trailing clause at `:475-476`; or keep it and say plainly
  that it is the arithmetic identity underlying the chirality separation rather than a fourth
  certificate. State the witness point as $(u,\gamma,f)=(0,1,i)$.

### 3.2 — 🔵 RECOMMENDED — `exceptional_point_defective`'s fourth conjunct is still entailed by its third

- **Gate:** LeanProofSubstance
- **Location:** `lean/SKEFTHawking/NonHermitianBloch.lean:79-84`; `papers/D11/paper_draft.tex:256-262`
- **Observed:** reproduces round-9 3.1 verbatim; the body is unchanged
  `⟨ptBlochEP_nilpotent, ptBlochEP_eigenvector, ptBlochEP_kernel_line, ptBlochEP_not_eigenvector⟩`.
  Conjunct 4 (`ptBlochEP.mulVec ![1,0] ≠ 0`) follows from conjunct 3 by instantiating at `v = ![1,0]`.
- **Evidence:** round 9 kernel-checked the entailment against the live toolchain. I re-read the
  declaration rather than trusting that; it is byte-identical. Deliberately left open per the
  round-10 brief.
- **Fix:** as round 9 — drop conjunct 4, keep `ptBlochEP_not_eigenvector` standalone, and reword
  `:259` to say the second candidate's exclusion *follows from* the kernel-line lemma.

**Class-3 clean results.** **Zero placeholder intersection.** The paper cites 159 `\thm{}`/`\texttt{}`
tokens; 124 resolve to declarations in `lean_deps.json`; the 35 that do not are all legitimately
non-declarations (Lean keywords, `toIocMod`/`toIocDiv`/`Matrix.det_fin_three`/`Chernoff` from
Mathlib, the module names `ChernBridge` and `NonHermitianWinding`, PhysLib's `TightBindingChain`, two
revision hashes, two Python function names, four file/format strings). Intersection with the
**576**-node `PlaceholderMarker` set is **empty**.
**I swept all 399 theorems/lemmas in the 22 D11 modules for trivial and tautological bodies** with my
own extractor (statement/proof split at `:=`, docstrings and line comments stripped): `rfl` /
`trivial` / bare `decide` / bare `simp` / bare `norm_num` bodies = **0**; identity-wrapper bodies
(`:= h…`) = **0**; anonymous-constructor bodies = **1**, and that one
(`effectiveMedium_constituent_bounds_strict`) composes two substantive lemmas. Spot-checked at
source: `pt_symmetric_real_spectrum_iff` proves both directions with real content (forward via
`nlinarith` on the secular polynomial; reverse by exhibiting `√(1-g²)`); `band_gap_falsifier` routes
through spectral completeness and the two global branch bounds; `acousticBloch_eigenvalue_iff` is a
genuine iff off `acousticBloch_charpoly_factor`; `blochPauli_gap_pos_iff` proves both directions;
`maxwellGarnett_clausius_mossotti` carries exactly the one denominator hypothesis the paper claims;
`norm_exp_mul_I_sub_one_sub_id_le` is a real MVT argument with constant 1 and no validity ball, as
claimed; `structureFactor_linear_expansion_global` is global as claimed;
`structureFactor_zero_set_not_shear_invariant` exhibits a concrete shear counterexample;
`haldaneFrame_latticeChern_eq_neg_one` is `decide` over a finite sum *after* sixteen individually
proved `plaquetteBranch` lemmas, not `decide` on the claim. Substrate claims verified directly:
**578 declarations** (464 theorem / 109 def / 5 structure) across the 22 modules,
`axiom_deps_project` **empty for every one**, `sorry` = 0, `maxHeartbeats` = 0, project-local
`axiom` = 0, and the two `native_decide` string hits (`HaldaneWitness.lean:94`,
`FHSExamples.lean:17`) are both prose *disclaiming* its use. The import closure of all 22 modules is
exactly `Mathlib` + the same 22 modules — **zero** `SKEFTHawking` imports outside the bundle and no
PhysLib import, so the acknowledgment at `:585-587` is exactly true.

### 4.1 — 🟡 REQUIRED — the eight ledger records landed, but seven of them cite a commit that does not contain the change they describe

- **Gate:** FixPropagation
- **Location:** `docs/review_finding_supersessions.json`, the eight `…:1530-internal-adversarial:D11:*` entries
- **Observed (direct answer to the round-10 brief's question).** All eight records **are** present
  and the graph reflects them (D11 open-blocking 18 → 17; `bundle_metadata.blockers_open: 17`
  agrees). Each cites a round whose report **does** support the closure. But seven of the eight carry
  `"commit": "ac66444f"`, and `ac66444f` does not touch the file each record names.
- **Evidence:** full file list of `ac66444f` ("Round 8: D11 reaches 0 BLOCKER"), 23 files, contains
  `lean/SKEFTHawking/EffectiveModuli.lean`, `scripts/validate.py`, `scripts/build_graph.py`,
  `docs/roadmaps/Phase6CE_Roadmap.md`, `papers/D12/paper_draft.tex` — and **not**
  `papers/D11/paper_draft.tex`, **not** `src/core/citations.py`, **not** `src/core/formulas.py`,
  **not** `docs/PAPER_DRAFT_MAPPING.md`, **not** `tests/test_bundle_formulas_d11_d12.py`.
  Pickaxe search locates each fix:
  ```
  record        claimed fix                                   file that must change      in ac66444f?  actual commit
  1530:1.1  primary_source_unreachable deleted            scripts/validate.py            (present)   bb0c7e34   ✗
  1530:1.2  HashinShtrikman1963.notes rewritten           src/core/citations.py          NO          837f335e   ✗
  1530:3.2  voigt/reuss docstrings -> "average"           EffectiveModuli.lean           YES         ac66444f   ✓
  1530:4.1  PAPER_DRAFT_MAPPING.md:109 regenerated        docs/PAPER_DRAFT_MAPPING.md    NO          bb0c7e34   ✗
  1530:5.2  novelty enumeration "six of these eight"      papers/D11/paper_draft.tex     NO          bb0c7e34   ✗
  1530:5.3  AFP caveat extended                           papers/D11/paper_draft.tex     NO          bb0c7e34   ✗
  1530:6.1  haldane_lattice_chern 3|N guard + 5 tests     src/core/formulas.py, tests/   NO          bb0c7e34   ✗
  1530:7.1  counting rule block-comment clause            papers/D11/paper_draft.tex     NO          bb0c7e34   ✗
  ```
  (`git log -S` confirms `bb0c7e34` — "D11/D12 Stage-13 round 7: close both BLOCKERs" — introduced
  "divisible by 3", "not inside a block comment", "six of these", the `6248` row, and removed
  `primary_source_unreachable`.)
- **Why it matters.** `READINESS_GATES.md:217` blocks Gate 11 on "a `fixed` finding without a
  SUPERSEDES / commit reference". A commit reference that does not contain the change is not a
  reference — a future auditor runs `git show ac66444f -- docs/PAPER_DRAFT_MAPPING.md`, gets an empty
  diff, and cannot distinguish "wrong SHA" from "never fixed". The pattern is systematic: the author
  recorded the *round* whose report confirmed the fix rather than the *commit* that carried it, and
  the two are one commit apart throughout.
- **Fix:** rewrite the `commit` field on the seven — `bb0c7e34` for 1.1 / 4.1 / 5.2 / 5.3 / 6.1 /
  7.1, `837f335e` for 1.2. Then add a ledger guard: for each record, assert the named commit's
  `git show --stat` touches at least one path named in the record's `evidence`. That is the same
  class of assertion `ledger_ids_resolve` already makes on `finding_id`.

### 4.2 — 🟡 REQUIRED — `1530:4.1` was closed as `fixed` today against a file that is stale in the tree today

- **Gate:** FixPropagation / NumericalFreshness
- **Location:** `docs/review_finding_supersessions.json` entry `review:2026-07-31-1530-internal-adversarial:D11:4.1`; `docs/PAPER_DRAFT_MAPPING.md:109`
- **Observed:** the record reads "PAPER_DRAFT_MAPPING.md:109 regenerated from docs/counts.tex.
  Closed on INDEPENDENT verification by round 8 (1652) re-derived the counts and reported the row
  current." The row currently reads **6248 lines**. `docs/counts.tex` currently reads
  `\newcommand{\dxiLines}{6258}`.
- **Evidence:** round 8's confirmation was correct *at 16:52*; round 9 at 18:01 reported the row
  stale again after the `EffectiveModuli.lean` docstring edits added ten lines. The record was filed
  **after** round 9 and cites round 8 anyway, with no mention of the re-drift. So the ledger asserts
  `fixed` for a condition the author had already been told was false. I independently re-derived the
  counts with my own comment-stripping algorithm (whole-file character scan blanking block-comment
  bodies, versus `update_counts.py`'s per-line depth tracker):
  ```
  modules=22  lines=6258  theorems=403  defs=82        (counts.tex: 22 / 6258 / 403 / 82)  ✓
  naive, without the block-comment clause: theorems=405  defs=84  -> overcount 2+2 = 4     ✓
  ```
  So `counts.tex` is right and the mapping row is wrong — this is the seventh consecutive round in
  which that row has been hand-synced and drifted.
- **Fix:** re-open `1530:4.1` (or supersede it with a `reopened` record), and stop hand-maintaining
  the row: emit it from `counts.json` the way `counts.tex` is emitted, or delete the four figures
  from it and point at the macros. The paper itself is immune — it uses `\dxiLines{}` — but
  `PAPER_DRAFT_MAPPING.md` is the seed document for the next lift.

### 4.3 — 🟡 REQUIRED — the closure hardening is unreachable: the ledger's own `status` overrides it two lines later, so a two-key record still closes a submission-blocking finding; both round-9 exploits reproduce verbatim; and a fourth exploit is in this very document

- **Gate:** FixPropagation
- **Location:** `scripts/build_graph.py:1676-1699` (the guard) and `:1701-1706` (the override); `_BLOCKER_RE` at `:1425-1428`; `file_has_critical_marker` at `:1587`
- **Observed.** The guard added at `9d631fad` requires, for blocking severities, a ledger record with
  `status == 'fixed'` **and** ≥40 characters of evidence **and** a non-empty `commit`. The very next
  block is:
  ```python
  meta = {'severity': severity, 'status': status, …}
  ledger = supersessions.get(finding_id)
  if ledger:
      meta['status'] = ledger.get('status', status)     # ← unconditional
  ```
  So whenever a record exists, the record's `status` wins and the evidence/commit requirements are
  discarded. The guard can only ever act when **no** record exists — the one case where it was
  already redundant with `status = 'open'`. And on the ordinary closure path (heading does not say
  "fixed"; the ledger does the closing) the guard's `if status == 'fixed'` precondition is never even
  entered, so the ≥40-char and commit requirements are **never evaluated for any of D11's 18
  ledger-closed findings**.
- **Evidence — all three exploits run end-to-end through the real `extract_review_finding_nodes`,
  against a copy of `papers/AutomatedReviews/` + `docs/review_finding_supersessions.json` in a
  temporary directory with `build_graph.PROJECT_ROOT` repointed. Nothing in the repo was written.**
  ```
  BASELINE D11 open-blocking = 17

  E1  glyph demotion (round-9 4.5 exploit 1) — one heading line of the REAL round-7 file:
      before: ('critical', 'open',  '<red> BLOCKER - all four stored figure outputs ...')
      after : ('minor',    'fixed', '🔵 RECOMMENDED — all four stored figure outputs … (fixed)')
      D11 open-blocking 17 -> 16, no ledger record, no signal anywhere.
      UNCHANGED: the new guard keys on `severity`, which the same edit lowers.

  E2  typeset escalation (round-9 4.5 exploit 2) — round 8's own zero-blocker summary,
      "**0 BLOCKER / 4 REQUIRED / 6 RECOMMENDED**", retypeset so the asterisks bracket the
      WORD rather than the count:
      as written : file_has_critical_marker=False  severities {minor:6, major:4, advisory:3}
      retypeset  : file_has_critical_marker=True   severities {critical: 13}
      D11 open-blocking 17 -> 26.   UNCHANGED.

  E3  NEW - the guard is dead code.  Append to the ledger, against the real critical finding
      review:2026-07-31-1530-…:D11:5.1 :
        {"finding_id": "…:D11:5.1", "status": "fixed"}      -> ('critical','fixed')  ✗ CLOSED
        {"finding_id": "…:D11:5.1"}                          -> ('critical','open')   ✓ caught
      Two keys. No evidence, no commit, no re-review. Open-blocking 17 -> 16.
      Only the one-key form — the exact case the code comment at :1687-1693 names — is caught.
  ```
- **Evidence — E4, NEW, and this review document is the specimen.** `_SEV_GLYPHS`
  (`build_graph.py:1403-1407`) is ordered `critical → major → minor`, and the severity loop at
  `:1616-1619` breaks on the **first glyph in dict order found anywhere in `heading + body[:600]`** —
  not on the heading's leading glyph. So a finding that merely *quotes* a higher-severity glyph in
  its opening lines is silently escalated. My first draft of this very section carried the
  red-circle glyph inside its own 🟡 REQUIRED heading, while quoting round-7's finding; the extractor
  minted it `critical`, taking D11 from 24 open-blocking to 25 and putting a phantom submission
  blocker on the bundle:
  ```
  draft-1 heading: "### 4.3 — 🟡 REQUIRED — … still closes a <red-glyph> BLOCKER; …"
      minted severity = critical        ← from the quoted glyph, not the declared one
  draft-2, glyph replaced by the word "critical", nothing else changed:
      minted severity = major           ← correct
  ```
  This is the same root cause as E1 and E2 — severity is read off free text with no notion of
  position or scope — and it is the *unintentional* direction of the same defect: a reviewer cannot
  discuss a blocker without risking escalating their own finding to one. It also means the
  established convention of writing severity glyphs in headings and quoting prior findings in bodies
  is structurally unsafe.

- **Re-measurement against the moving working tree (mandatory disclosure).** All four exploits above
  were measured against the head I was asked to review, `9d631fad`. While this review was in flight a
  **concurrent D12 round-10 remediation** rewrote `scripts/build_graph.py` (uncommitted; mtime
  15:33:18, then 15:35:07 local) and independently found the same E3 defect from the D12 side. I
  re-ran the identical harness against the current file:
  ```
  E1 glyph demotion            STILL REPRODUCES   D11 open-blocking 25 -> 24
  E2 typeset escalation        STILL REPRODUCES   D11 open-blocking 25 -> 34
  E3 two-key ledger record     FIXED              two-key record no longer closes 1530:5.1
  E4 glyph-order escalation    STILL REPRODUCES   `_SEV_GLYPHS` loop unchanged at :1616-1619
  ```
  So **item 1 of the fix list below is already done** and items 2–4 stand. One process note that
  belongs in the record: the first version of that concurrent fix (15:33:18) re-indented three
  `meta['superseded_by'] = ledger.get(...)` lines into the new guard block, where `ledger` can be
  `None`; `scripts/build_graph.py --json` exited 1 with
  `AttributeError: 'NoneType' object has no attribute 'get'` at `:1718` on the live corpus — the
  graph could not be built at all — until it was repaired two minutes later at 15:35:07. I verified
  both states directly (`exit=1`, then `exit=0` with `node_count: 46044`). It is repaired, so it is
  not a finding; it is recorded because it is the third data point for this review's QI candidate.

- **Fix, in dependency order.**
  1. ~~**E3 first, it is one line.**~~ **DONE** by the concurrent remediation described above; the
     shape it landed on is the right one (apply the bar to `meta['status']`, after the override).
     For the record, the fix I would have proposed: make the override respect the guard:
     `meta['status'] = ledger.get('status', status) if _closes or severity not in BLOCKING else 'open'`
     — or, cleaner, compute the ledger status *before* the guard and let the guard have the last
     word. As written, the guard is provably unreachable and should not be described as protection.
  2. **E1:** record `severity` in the ledger at first extraction and FAIL when a later extraction of
     the same `finding_id` reads a *lower* severity than the ledger holds. The ledger is already
     append-only; this needs no new infrastructure and it closes post-hoc demotion completely.
  3. **E2:** delete `file_has_critical_marker`. It cannot distinguish "this report found a blocker"
     from "this report says there are none", and `_BLOCKER_RE` requires an asterisk *adjacent to* the
     word — so the red-circle-glyph form, which is what every reviewer here actually writes in headings, does not match
     it at all, while a sentence declaring **zero** blockers does. Derive severity from the glyph
     only. (This finding deliberately never types the literal token, for the reason round 9 gave.)
  4. **E4:** read the severity glyph **positionally** — the first glyph in the heading string,
     anchored to the heading — and drop `body[:600]` from the severity scan entirely. Two lines:
     `m = re.match(r'\s*([🔴🟡🔵])', heading)` and default to `advisory` when absent. That makes a
     finding's declared severity independent of what it quotes, which is a precondition for any of
     the other three fixes to be auditable.

### 4.4 — 🔵 RECOMMENDED — both "necessity theorem" sites survive, and one of them is untracked, so `git grep` cannot see it

- **Gate:** CrossPaperConsistency
- **Location:** `docs/roadmaps/Phase6ED_Roadmap.md:174`; `docs/dev-loops/Phase6ED/LAB_NOTEBOOK_INDEX.md:32`
- **Observed:** both read "**the necessity theorem** `haldane_massInversion_not_sufficient_at_N4`".
  Round 9 reported `LAB_NOTEBOOK_INDEX.md:32` clean and only the roadmap surviving.
- **Evidence:** `git ls-files --error-unmatch docs/dev-loops/Phase6ED/LAB_NOTEBOOK_INDEX.md` →
  `did not match any file(s) known to git`. The file is **untracked** (the `docs/dev-loops/` tree is
  local/auto-written per `CLAUDE.md`), so the all-occurrence `git grep` discipline of
  `BUNDLE_LIFT_PROCEDURE.md §11 step 3` is structurally blind to it, and a reviewer using `git grep`
  will keep reporting it clean while a reader of the file sees the wrong claim. The theorem asserts
  the masses invert **and** the invariant is zero — the *insufficiency* direction; its own docstring
  (`HaldaneWitness.lean:1429-1433`) says in bold "Necessity is **not** formalized anywhere in this
  development", and the paper agrees at `:351-353`.
- **Fix:** "the insufficiency theorem" at both sites; and change the §11-step-3 all-occurrence rule
  from `git grep` to plain `grep -r` (or `git grep --untracked`), since the project deliberately
  keeps claim-bearing dev-loop documents out of git.

### 4.5 — 🟡 REQUIRED — E2 attributes a *band-structure* Chern coefficient to `ChernBridge.lean`, which is exactly the conflation D11's §Scope carve-out exists to prevent

- **Gate:** CrossPaperConsistency
- **Location:** `papers/E2/paper_draft.tex:418-424`, against `papers/D11/paper_draft.tex:522-526` and `papers/D7/paper_draft.tex:124`
- **Observed:** E2 states the demarcation condition as
  > "(ii) the topological Chern coefficient $c_1$ of the graphene moir\'e **band-structure** vanishes"
  and two sentences later attributes the Lean-level formalization to
  `\texttt{AperiodicLattice.lean} and \texttt{ChernBridge.lean}`. D7, describing the same object,
  says "categorical Chern coefficient $c_1$" (`:124`). D11 says the `ChernBridge` object is
  "a categorical Chebyshev *marker* … a different object from the Bloch-band invariant reported here
  --- an algebraic marker, not an integral of curvature over a bundle".
- **Evidence:** I traced E2's $c_1$ into Lean. `analog_hawking_fourCycleFree_demarcation`
  (`AnalogHawkingDemarcation.lean:143`) states condition (ii) as
  ```lean
  realSpaceChernAt (categoricalChernExpansion c0 c1) 1
    = realSpaceChernAt (categoricalChernExpansion c0 c1) (-1)
  ```
  and `ChernBridge.lean:68,74` gives `categoricalChernExpansion c0 c1 := ⟨[c0, c1]⟩` (a two-term
  Chebyshev expansion) and `realSpaceChernAt e x := evalChebyshev e x` (a **real-valued** evaluation).
  There is no band, no Brillouin zone and no integer. So E2's "$c_1$ of the graphene moiré
  band-structure" names D11's object and points at D7's. The D11 anchor list
  (`docs/agents/claims-reviewer-bundle-prompts.md:1094`) designates exactly this axis: *"Chern marker
  vs Chern number | D7 (`ChernBridge`) | D11 | **Different objects.** … Conflation is a finding."*
- **Scope note.** **D11's own prose is correct and needs no edit**; the defect is in
  `papers/E2/paper_draft.tex`. I am reporting it here because class 4 is cross-paper by construction
  and this is the named D11↔D7/E2 cross-bridge, but D11's Gate 2 should not be held blocked on
  another bundle's sentence. The two papers cannot both ship as written.
- **Fix:** in E2, "the **categorical** Chern coefficient $c_1$ (a Chebyshev marker — see D7 §; not a
  Bloch-band Chern number)". Then `grep -rn "Chern" papers/*/paper_draft.tex` should show every
  non-D11 occurrence carrying the "categorical"/"marker" qualifier.

**Class-4 clean results.** `Berry1984` remains the only bibkey D11 shares with another draft
(`paper18_doublon_gate`); across all 64 `papers/*/paper_draft.tex` the two bibitems differ only in
LaTeX formatting (`\,` vs `~`, `\textit` vs `\emph`, `Proc. R. Soc. A` vs `Proc. R. Soc. Lond. A`) —
same authors, title, volume 392, page 45, year 1984, same DOI target. Not a Gate-2 violation.
`bundle_consistency` PASS; `bundle_source_freshness` PASS (D11 fresh; one WARN for
`stage13_redo_required=true`, which is correct). No other paper mentions `latticeChern`, a lattice
Chern number, or the Haldane model. Both kernel no-gos the D11 anchor list requires are present in
`KERNEL_NOGO_REGISTRY` with backing refutation theorems, and both are represented in §7 rather than
elided; I re-read both refutations at source and they are concrete counterexample exhibitions, not
prose. The retracted `haldane_chern_iff_mass_inversion` appears in the draft only inside its own
retraction paragraph at `:483-488`. The 18 → 17 blocker arithmetic reconciles exactly: 1059
contributes 5 open blocking, 1530 contributes 2 (4.2, 5.1 — deliberately left open), 1652
contributes 4, 1801 contributes 6.

### 5.1 — 🟡 REQUIRED — the replacement carve-out is accurate about what the source contains, but its load-bearing clause now over-reaches on Euler classes — and the cited source's own Gysin sequence carries one

- **Gate:** NarrativeGrounding (`first-claim`) / FirstClaimVerification
- **Location:** `papers/D11/paper_draft.tex:534-543` (rendered: `paper_draft.pdf` p. 8); mirrored at `papers/D11/prior_art_novelty.md:89-92`
- **Observed (direct answer to the round-10 brief's question).** The **enumeration is correct** — I
  re-fetched `arxiv.org/abs/2401.16336` and the paper's list is the abstract's list, word for word:
  ```
  abstract : "the spheres, torus, Klein bottle, real/complex projective planes, and infinite real projective space"
  paper    : "the spheres, the torus, the Klein bottle, the real and complex projective planes, and infinite real projective space"
  ```
  The retraction parenthetical is also accurate: §5.4 computes $H^*(\mathbb{CP}^2,\mathbb{Z}) \cong
  \mathbb{Z}[x]/(x^3)$ and §5.5 computes $H^*(\mathbb{RP}^\infty,\mathbb{Z}/2)$; there is no
  $\mathbb{CP}^\infty$ anywhere. **The correction is neither over- nor under-shot on the facts.**
  The over-reach is in the clause that follows:
  > "The Gysin sequence is the machinery from which Euler and Chern classes are built, so our claim
  > is that no assistant \emph{defines} the classes, not that the surrounding machinery is absent."
  Read literally, "the classes" is the antecedent just named — **Euler and Chern** classes. The
  paper's actual headline claim (`:510-512`) is scoped correctly and only to Chern:
  "no *prior* formalization, in any proof assistant, of a Chern number or a Chern class *of a vector
  bundle*".
- **Evidence:** LM2024 §4.4 "The Thom isomorphism and the Gysin sequence", **Theorem 43**, states the
  Gysin sequence as
  ```
  … H^{i-1}(E,R)  →  H^{i-n}(B,R)  --(−) ⌣ e-->  H^i(B,R)  →  H^i(E,R) …
  ```
  with `e := |b ↦ c_b(south)|` constructed for every fibration satisfying condition (∗), and §5.5
  computes that class explicitly for $\mathbb{RP}^\infty$ ("it is easy to show that `e = |id|`").
  Under the standard dictionary (Milnor–Stasheff §12) that class *is* the Euler class of the oriented
  sphere bundle. The word "Euler" never appears in the 40-page PDF (token count 0), so this is not a
  competing novelty claim — it is a referee-visible ambiguity in the one sentence whose entire job is
  to hedge. I checked independently that no such competing claim exists: arXiv API sweeps for
  `"Euler class" AND "Cubical Agda"`, `"homotopy type theory" AND "Euler class"`,
  `"proof assistant" AND "characteristic class"`, `"Chern number" AND "Lean 4"`,
  `"Haldane model" AND "proof assistant"`, `"exceptional point" AND "proof assistant"`,
  `"phononic" AND "formal verification"`, `"formalized" AND "effective medium" AND "theorem prover"`
  all return **0 results**; `"Chern class" AND "formalization"` returns 8, all pure algebraic
  geometry with no prover content. So N1–N7 hold up against an independent sweep.
- **Why REQUIRED and not BLOCKER.** A charitable reading takes "the classes" to refer back to "our
  claim", which is about Chern classes only, and under that reading the sentence is true. The paper
  is not making a false statement; it is making an ambiguous one at the exact point a hostile referee
  will read most carefully, in carve-out two of four that the paper itself calls mandatory.
- **Fix:** three words. "The Gysin sequence is the machinery from which Euler and Chern classes are
  built — that development constructs the Gysin class and the Thom isomorphism but not the
  characteristic classes themselves, so our claim is that no assistant \emph{defines} Chern classes,
  not that the surrounding machinery is absent." That is strictly *stronger* for D11 and removes the
  ambiguity, and it discharges finding 1.2 in the same edit.

### 5.2 — 🟡 REQUIRED — the digest fix works; a claim still hides, in a payload type the collector does not descend, and I demonstrated it end-to-end on the shipped notebook

- **Gate:** NarrativeGrounding (tooling)
- **Location:** `scripts/validate.py:4213-4227` (`_texts`), against `_strings_in`/`_array_digest` at `:4114-4190`
- **Observed (direct answer to the round-10 brief's question).** The rounded-value digest is a real
  fix — I re-read it and it defeats every mutation round 9 proposed, in both the `bdata` and the
  plain-list branch, hashing values in document order at 9 significant figures. **The hole moved one
  level out.** `_texts` collects a payload only when the MIME key is `text/plain`, `text/html`, or
  contains the substring `json`. Everything else is dropped: `text/markdown`, `text/latex`,
  `image/svg+xml`, `image/png` — and markdown *cells* are skipped entirely by
  `if cell.cell_type != "code": continue`.
- **Evidence — MUT-N1, end-to-end against the real check, on the real shipped notebook.** Backup
  taken (`b604edc7f4a6…`), restored afterwards, hash verified identical. I appended to code cell 15
  a `display(Markdown(...))` producing the **correct** sign, and stored an output asserting the
  **opposite**:
  ```
  stored  text/markdown : "**Haldane witness: C = +1 (certified by haldaneFrame_latticeChern_eq_neg_one)**"
  fresh   text/markdown : "**Haldane witness: C = -1 (certified by haldaneFrame_latticeChern_eq_neg_one)**"
  stored/fresh text/plain: "<IPython.core.display.Markdown object>"   (identical, as Jupyter always emits)

  ✓ PASS  notebook_stored_outputs_current
    ✓ D11_TopologicalBandTheory_Technical.ipynb — stored output matches a fresh run (1864 text payloads)
  ```
  The payload count rose by exactly 1 — the `text/plain` sibling — confirming the markdown half was
  seen and discarded. The reader of the shipped artifact sees `C = +1` next to the name of the
  theorem that certifies `C = −1`, the sign the D11 anchor list singles out
  (`claims-reviewer-bundle-prompts.md:686-689`: "The certified value is **`−1`**, not `+1`").
- **Evidence — the control, proving the harness genuinely re-executes.** Same injection with the
  `text/plain` sibling corrupted instead:
  ```
  ✗ FAIL  D11_… STORED OUTPUT IS STALE …
          First divergence: stored='<<CONTROL-SENTINEL>>' fresh='<IPython.core.display.Markdown object>'
  ```
  So the fresh run *does* produce the markdown payload; the check simply never looks at it.
- **Evidence — MUT-N2, the markdown-cell surface.** I rewrote markdown cell 13 to read
  "`effectiveModuli_enclosure` gives the **Voigt-Reuss bounds on the effective modulus M_eff**" —
  verbatim the naming that §5 and `EffectiveModuli.lean`'s header retract, and the subject of round-7
  BLOCKER 5.1. `notebook_stored_outputs_current` PASS, `notebook_exec` PASS, `notebooks` PASS,
  `viz_consistency` PASS. Grepping the four notebook-touching checks in `validate.py`
  (`:1671`, `:2266`, `:2403`, `:4089`) confirms **not one reads a markdown cell's source**.
- **Why the current design cannot close this.** The collector's discriminator is the MIME key, and
  claim-bearing-ness is not a function of the MIME key any more than it was a function of array
  length. `text/markdown` and `text/latex` are *more* reader-visible than `text/plain`, not less.
- **Fix:** (a) in `_texts`, replace the `key in ("text/plain","text/html")` test with an explicit
- **Lane:** `infra`
  **deny**-list — collect every `text/*` and `application/*json*` key, skip only `image/png`,
  `image/jpeg` and `image/gif`, and collect `image/svg+xml` for its string leaves. That is a
  one-line inversion and it covers `text/markdown`, `text/latex`, `text/csv` and anything future.
  (b) Separately, add a companion check that greps *markdown cell sources* of the two bundle
  notebooks for the bundle's retracted-phrase list — the same list `find_phrase.py` already carries;
  a markdown cell has no code to compare against, so it needs a phrase gate, not a re-execution gate.
  (c) Record in the docstring the surface each version does **not** cover, so the next round starts
  from the stated boundary rather than rediscovering it.

### 5.3 — 🔵 RECOMMENDED — the abstract states the grid but omits the parameter point for the Haldane witness

- **Gate:** NarrativeGrounding
- **Location:** `papers/D11/paper_draft.tex:47-48` (abstract), against `:405-408` (body)
- **Observed:** the abstract reads "culminating in a concrete Haldane-model Chern witness certifying
  $C = -1$ on a $4\times4$ torus." The body is fully compliant: "at parameters $t = t_2 = 1$,
  $\varphi = \pi/2$, $m = 1$ … we state the parameter point explicitly because the result is a
  certificate at that point, not a general classification."
- **Evidence:** the D11 anchor list (`claims-reviewer-bundle-prompts.md:686-689`) reads: "Any prose
  quoting `+1`, **omitting the parameter point**, or implying generality beyond that grid is a
  finding." The abstract omits it. I verified the parameter point is baked into the Lean witness and
  not a free parameter: `haldaneFrameTopo := blochFrameOfD (haldaneD44 1) …` and
  `haldaneD44 m := haldaneD 1 1 (Real.pi/2) m (bzPoint k)` — so `t = 1`, `t₂ = 1`, `φ = π/2`, and the
  `1` in `haldaneD44 1` is `m`. The paper's account is exactly right; only the abstract is silent.
- **Why RECOMMENDED.** "concrete … witness" plus the explicit grid signals a point certificate rather
  than a classification, so the abstract does not *imply generality*; it just does not state the
  point. But the anchor's text is unconditional, and the fix is four tokens.
- **Fix:** "… certifying $C = -1$ at $t = t_2 = 1$, $\varphi = \pi/2$, $m = 1$ on a $4\times4$ torus."

**Class-5 clean results.** Every externally checkable claim in the paper verifies against the pinned
checkouts, which I inspected directly rather than trusting round 9. `lean/lakefile.toml` pins Mathlib
at `81a5d257c8e410db227a6665ed08f64fea08e997` and PhysLib at `c48433678e8fb6306ebcd48453300c8e16058a62`,
`lean-toolchain` is `leanprover/lean4:v4.32.0` — matching `:78-79` and `:526-529` exactly. Every
occurrence of the string `Chern` in the pinned Mathlib is `Chernoff` (10) or `ChernoffBound` (4), so
`:527-528` is exactly true; `EulerClass` / `StiefelWhitney` / `PontryaginClass` return zero files;
`det_fin_*` enumerates to `zero`, `one`, `two`, `three` only, so `:441-442` is true. The Haldane
numerics reproduce independently through the Python port: `haldane_flip_location(4) =
3.317672196172225`, `C(m=1) = C(3.0) = −1`, `C(3.4) = C(5) = C(6) = 0`, and the fraction of the
analytic window above the flip is **36.15 %** — "roughly a third", as `:427-429` says. The `3 ∣ N`
guard raises `ValueError` on N ∈ {3,6,9,12} and returns on {4,5,8}. Maxwell–Garnett at
$(1,4,\tfrac12)$ is exactly 2 and the Wiener bracket is exactly $[1.6, 2.5]$. Novelty hedging is
present and correctly scoped throughout ("We are aware of no *prior* formalization"; the past-tense
clause with its six-plus-two arithmetic, which I recounted and which closes; the AFP coverage limit
extended to the EP / PT / non-Hermitian items; the explicit HOL4-and-PVS weakness statement). The
`ChernBridge` self-carve-out at `:522-526` and the SymTFT bulk–boundary self-carve-out at `:496-500`
are both present and correctly worded, and §Scope's Berry-curvature and bulk–boundary deferrals match
the tree (zero Berry connection/curvature declarations in the 22 modules). **The compile gate is
green and judged from the rendered PDF, not the log:** `compile_bundle_pdf.py D11` →
`OK pages=9 overfull=0 tex_errors=0 unresolved_refs_in_pdf=0 dropped_class_opts=0`; `pdftotext` on
the output confirms p. 8 renders all four mandatory carve-outs including the corrected Cubical-Agda
sentence and its retraction parenthetical, and p. 1 renders the fresh macro values 6258 / 403 / 82.

### 6.1 — 🔵 RECOMMENDED — §5's elastic paragraph still states no hypotheses, and equation (2) is still displayed unconditionally

- **Gate:** AssumptionDisclosure
- **Location:** `papers/D11/paper_draft.tex:316-330` and equation (2), against `:291-296`
- **Observed:** reproduces round-9 6.1 verbatim, unchanged. The electromagnetic half spells its
  hypotheses out — "for a physical composite $0 < \varepsilon_h \le \varepsilon_i$ at fill fraction
  $0 \le f \le 1$ --- and the ordering hypothesis is load-bearing rather than a relabeling". The
  elastic half names none.
- **Evidence:** `effectiveModuli_enclosure` and `voigt_sub_reuss_eq` both take
  `(h1 : 0 < M1) (h2 : 0 < M2) (ho : M1 ≤ M2) (hf0 : 0 ≤ f) (hf1 : f ≤ 1)`
  (`EffectiveModuli.lean:83`, `:107`). The displayed chain $M_1 \le M_R \le M_V \le M_2$ telegraphs
  `ho`, but positivity and $0 \le f \le 1$ are invisible, and under Lean's `x/0 = 0` convention
  equation (2) is false wherever the Reuss denominator vanishes — which `reuss_denom_pos` excludes
  using exactly the hypotheses the paper omits.
- **Fix:** one clause — "for $0 < M_1 \le M_2$ at $0 \le f \le 1$" — attached to the
  `effectiveModuli_enclosure` sentence so it also governs equation (2).

**Class-6 clean results.** Every structural hypothesis the D11 anchor list names is disclosed:
`AdmissibleBandFrame.overlap_ne` at `:164-169`, with the explicit and correct statement that a
spectral gap does *not* imply it; the north-pole condition of `blochFrameOfD` at `:190-193`; the
`IsHoneycombChart` chart-specificity at `:368-372` and `:382-383`; and the `3 ∣ N` grid no-go at
`:506-508` with the correct Dirac-point mechanism. I checked five further paper-cited theorems
against their declarations this round and found no undisclosed load-bearing hypothesis:
`maxwellGarnett_clausius_mossotti` carries exactly `hD : εi + 2εh − f(εi−εh) ≠ 0`, which is what the
paper says ("without a side-condition beyond nonvanishing of the denominator");
`norm_exp_mul_I_sub_one_sub_id_le (x : ℝ) : ‖exp(xI) − 1 − xI‖ ≤ x²` is unconditional with constant
1, as claimed; `structureFactor_linear_expansion_global` is bounded by `q₁² + q₂²` for all `q`, as
claimed; `bernal_mexicanHat` declares `(hγ : 0 < γ) (hu : u ≠ 0)` and the paper's claim is an
existence statement that does not need them stated; `ep_proximity_enclosure` is in fact *stronger*
than the paper implies — its `_h2 : g² ≤ 1` is unused. `dispersion_slope_eq_hbar_fermiVelocity`
leaves `fermiVelocity` genuinely parametrized (`ħ` cancels against
`fermiVelocity t a ħ := 3ta/(2ħ)`, with `hbar_ne` doing real work), so `:384-386` is accurate. The
bundle carries zero tracked-hypothesis Props, as claimed.

### 7.1 — 🔵 RECOMMENDED — `bundle_metadata.json`'s Stage-9 notes still quote a declaration count and a page count the artifact no longer has

- **Gate:** NumericalFreshness
- **Location:** `papers/D11/bundle_metadata.json` `notes`
- **Observed:** "verified all **566** declarations kernel-pure" and "a same-line word-overlap scan
  across all **8** pages returning 0 collisions". Reproduces round-9 7.1, unchanged.
- **Evidence:** the live tree has **578** declarations across D11's 22 modules (`lean_deps.json`,
  464 theorem + 109 def + 5 structure), and the current compile renders **9** pages. Both figures
  moved with the round-7/8/9 remediations. The notes are the human-facing record of what was
  verified, so a stale page count silently rescopes a completed scan.
- **Fix:** restate the two figures, or drop them and reference the gate that recomputes them.

**Class-7 clean results.** `counts_fresh` PASS. `count_literals` → "D11 — no count literals found
(macros in use)". `numerical_literals` → "D11 — no inline unit-bearing literals in body".
`tables_fresh` PASS. `paper_provenance` PASS ("D11/figures — 4 referenced figure(s) resolved").
**I re-implemented the paper's stated counting rule from scratch**, with a different
comment-stripping algorithm from `update_counts.py`'s, over the 22 modules: `modules=22 lines=6258
theorems=403 defs=82`, matching `counts.tex` exactly, and the naive count without the block-comment
clause gives 405 / 84 — an overcount of exactly **four**, as `:116` claims.
`\dxiDepsTheorems{464}` matches my independent count of `kind == "theorem"` entries in
`lean_deps.json` for the 22 modules. The one stale numerical claim in the bundle is the
`PAPER_DRAFT_MAPPING.md` row (finding 4.2), which the paper itself does not consume.

### 8.1 — (no findings) — ProductionRunHealth

D11 makes no simulation-evidence claim: a case-insensitive scan of `paper_draft.tex` for
`monte carlo | numerical evidence | simulation | simulated | we simulate | stochastic` returns **zero**
hits. The graph holds 18 `ProductionRun` nodes; **zero** edges touch any of them, and none is D11's.
The bundle is Lean-only. The single non-theorem numerical value, $|m| \approx 3.3177$, is produced by
`haldane_flip_location`, is regression-pinned against the Lean-certified masses
(`tests/test_bundle_formulas_d11_d12.py:596-604`, which I re-executed:
`m_star = 3.317672196172225`, `pytest.approx(3.3177, abs=5e-5)` holds, and the
window-fraction assertion reproduces at 36.15 %), and is labelled "numerical rather than certified"
in the body (`:424`), in the figure caption (`:353-355`) and in the stored figure annotation. Gate
passes.

## QI Candidate

**qi-a-hardening-must-be-tested-against-the-code-path-that-follows-it.** The round-9 closure
hardening (`build_graph.py:1687-1699`) is well-designed, well-commented, correctly motivated by a
real D12 blocker — and unreachable, because the next four lines overwrite the field it sets. Three
rounds in a row have now shipped a guard whose *intent* was right and whose *reachability* was never
measured: round 7's `primary_source_unreachable` (takeable by any entry), round 8's cache-content
gate (opt-out by blanking one field), round 9's closure guard (overridden one statement later). Each
time the author verified the guard's logic in isolation and each time the enclosing control flow
defeated it. **Recommendation:** every new guard ships with a *negative* test in `tests/` that
constructs the state the guard exists to reject and asserts the check FAILs — not a review-time
mutation the next reviewer has to re-invent, a committed test. A guard with no committed negative
test should be treated as unverified, exactly the way `READINESS_GATES.md` treats a `fixed` finding
with no re-review.

**qi-a-closure-record-must-name-the-commit-that-carries-the-fix-not-the-round-that-confirmed-it.**
Seven of eight new D11 ledger records cite `ac66444f` (the round that *confirmed* the fixes) rather
than `bb0c7e34` / `837f335e` (the commits that *contain* them), and one closes a finding whose file
is stale in the tree at the moment of filing. `READINESS_GATES.md:217` accepts "a documented commit
reference" as a substitute for a SUPERSEDES edge; a reference that resolves to an empty diff is
indistinguishable, to a future auditor, from no fix at all. This is the mirror image of round 9's QI
("a reviewer assertion about a ledger must be produced by reading the ledger"): an *author* assertion
about a commit must be produced by reading the commit. **Recommendation:** extend the existing
`ledger_ids_resolve` guard to assert that each record's `commit` resolves in git **and** that its
`--stat` touches at least one path named in the record's `evidence` string. Both are one `git show`
per record, and the check already walks every record.
