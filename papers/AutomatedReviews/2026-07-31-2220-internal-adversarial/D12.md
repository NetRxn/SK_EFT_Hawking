---
paper: D12
bundle_target: D12
reviewer: adversarial-reviewer
model: claude-opus-5
review_date: 2026-07-31T22:20:10Z
readiness_gates_version: 1
head_commit: fc624fc5
round: 12
---

# Adversarial Review — D12

## Summary

2 blocker / 13 required / 17 recommended (plus one class-8 pass note). Stages 1–12 are
green (`validate.py` 59/59, 1114 warnings, archived
`validation_20260731T222617Z.json`), so this review is meaningful. **The paper itself is
clean of blockers again**, and three of the four round-11 repairs you asked me to verify
hold up: the seventh capstone binder is disclosed and the completeness claim is genuinely
dropped rather than re-pitched; the ENBW functional account is arithmetically exact (I
integrated it numerically, it agrees to eight digits); and the `LATE_PHASE6:184`
contradiction is resolved correctly in both documents. Both blockers are in the 8.1(b)
machinery you asked me to attack, and both are the failure mode you warned me about:
**(3a) was not built — the heading-parse closure it says it deletes is still executing at
`build_graph.py:1698-1704`, and all 73 findings the commit message says "now read open"
still read `fixed`** — and **(3b) opened a new blocking-closure bypass rather than closing
one**, because a declared severity is a downgrade lever that (3a)'s surviving hole then
converts into self-closure. D12 is not submission-ready.

## Environment

`uv run python scripts/validate.py` → **59/59, 1114 warnings**, exit 0, archived to
`docs/validation/reports/validation_20260731T222617Z.json`. Stage 13's precondition is met.

No LaTeX was compiled: the two paper hunks since round 11 (`:86`, `:300-312`, `:673-686`)
are prose-only inside existing paragraphs, adding no float, no macro and no reference, and
round 11 already judged the rendered PDF at `OK pages=10 overfull=0 unresolved_refs=0`.
I ran no `pdflatex`, inside `papers/` or anywhere.

Working tree at `fc624fc5`: 134 dirty tracked paths on arrival (132 `figures/*.png`,
`lean/lean_deps.json.hash`, `.claude/plugins/skeft-qa/agents/adversarial-reviewer.md`).
None is a D12 artifact. Files I wrote are disclosed in the compliance note.

## Findings

### 1.1 — 🟡 REQUIRED — `IrwinHilton2005` is still trusted unqualified for the loop-gain convention while the paper discloses that source as unread, sixth consecutive round

- **Severity:** required
- **Gate:** CitationIntegrity
- **Location:** `papers/D12/paper_draft.tex:372-374` vs `:443-446`
- **Observed:** `:372-374` — "The loop gain \thm{loopGain} is signed, and \thm{loopGain\_eq\_irwinHilton} proves it equals the instrument-literature convention~\cite{IrwinHilton2005,Irwin1995}", no qualifier. `:443-446` — "We attach~\cite{IrwinHilton2005} as the standard review in which this layer is presented, but we have not inspected its text: we hold that source only as a resolved DOI record, so the attribution of this specific closed form to it should be read as provisional."
- **Evidence:** `git diff 89d10728 fc624fc5 -- papers/D12/paper_draft.tex` touches exactly three hunks (`:86`, `:300-312`, `:673-686`); neither cited line moved. The registry note for `IrwinHilton2005` still asserts the loop-gain attribution from a source the same project records as unread. `Irwin1995` is the stronger citation and carries the same claim.
- **Expected:** Extend the `:443` provisional qualifier to the loop-gain sentence, or narrow `:373` to `Irwin1995` alone.
- **Fix:** One clause at `:373`, mirrored into `CITATION_REGISTRY['IrwinHilton2005'].notes`.
- **Severity note:** required on the basis of `READINESS_GATES.md:44` — "All CitationIntegrity findings are `blocked` at submission time." A Gate-1 finding cannot be advisory at submission.
- **Cache:** cache-verified (`verdict: match`, `is_stale: False`, hash `925b58102c` recomputed this round)
- **Lane:** `prose`

### 1.2 — 🟡 REQUIRED — `RashidHasan2017` is cited for a completed formalization; the source describes itself as an ongoing project with a roadmap

- **Severity:** required
- **Gate:** CitationIntegrity
- **Location:** `papers/D12/paper_draft.tex:625-627`; `Lit-Search/Phase-6E/primary-sources/RashidHasan2017.abstract.txt`
- **Observed:** "Transform methods and transfer-function algebra **are formalized** in HOL Light~\cite{RashidHasan2017}; we are aware of no stochastic noise-metrology layer above them…"
- **Evidence:** Fresh-fetched `https://arxiv.org/abs/1705.10050` this round. Title and authors match the bibitem exactly ("Formalization of Transform Methods using HOL Light"; Adnan Rashid, Osman Hasan) — the citation is not wrong-target. What does not match is what the paper says it establishes. The abstract, verbatim: "In this paper, we present **an ongoing project**, which focuses on the higher-order logic formalization of transform methods using HOL Light theorem prover. … Next, we present the **task completed so far** while highlighting some of the challenges faced during the formalization. Finally, we present **a roadmap to achieve our objectives, the current status and the future goals** for this project." That is a project-status report. It does not establish that transfer-function algebra *is* formalized, and it says nothing about how much of transform methods is. The project's own cache file repeats the overstatement in its `Caveat / usage note` — "transform methods and transfer-function algebra ARE formalized" — while its `VERIFICATION RECORD` section quotes only the negative half ("the abstract mentions no noise, bandwidth, matched filter or detection"). So the claim is wrong in three artifacts: the cache caveat, the prose, and whatever the registry note inherited from them.
- **Expected:** Either qualify — "a HOL Light formalization of transform methods is under way~\cite{RashidHasan2017}" — or cite a completed Rashid–Hasan formalization paper instead. The direction of the error is conservative for D12's own novelty (it overstates prior art, not D12), which is why this is required rather than blocking; but this is the paragraph the anchor list ranks HIGHEST priority, and the paper elsewhere holds itself to exactly this standard ("we hold that source only as a resolved DOI record", "We have read \cite{Mather1982} in abstract only"). It does not apply that standard here.
- **Fix:** One clause at `:625`, mirrored into the cache caveat.
- **Cache:** fresh-fetch (record appended, reviewer `adversarial-reviewer-round12`, verdict `match` with the content note attached)
- **Lane:** `prose`

### 1.3 — 🟡 REQUIRED — none of D12's six arXiv-bearing registry entries carries `arxiv_verified`, so Gate 1's documented pass criterion still cannot be met

- **Severity:** required
- **Gate:** CitationIntegrity
- **Location:** `src/core/citations.py`; `docs/READINESS_GATES.md:33,40`
- **Observed:** Gate 1 "Passes iff … Every registry entry has `arxiv_verified == True` AND `doi_verified == True` (where applicable)". I dumped every registry field for all 14 D12 keys this round: `doi_verified` is present on the eight DOI-bearing entries; `arxiv_verified` is **absent on all six** arXiv-bearing entries (`MeiburgLessaSoldati2025`, `coqq2022`, `UnruhCBO2025`, `EchenimMhalla2021`, `RashidHasan2017`, `Degenne2025Kernels`).
- **Evidence:** `_eval_citation_integrity` implements a weaker predicate and D12's gate reads `passed — 14 bibitems, 14 registered`. The two verification records I appended this round land in `docs/citation_verifications.jsonl` and in nothing the gate reads. Two canonical artifacts disagree about what Gate 1 is, and the doc is the one a fresh reviewer is instructed to treat as defining "correct".
- **Expected:** Either set `arxiv_verified` on the `promote_primary_sources.py` write path and have the Stage-13 cache append feed it, or amend `READINESS_GATES.md` Gate 1 to state the deferral the evaluator implements.
- **Fix:** One writer change or one doc paragraph.
- **Cache:** n/a
- **Lane:** `infra`

### 1.4 — 🔵 RECOMMENDED — `scripts/citation_cache.stats()` still raises on the cache it summarizes

- **Severity:** recommended
- **Gate:** CitationIntegrity
- **Location:** `scripts/citation_cache.py` (`is_stale`)
- **Observed:** `from citation_cache import stats; stats()` → `TypeError: can't subtract offset-naive and offset-aware datetimes`. Re-run this round; unchanged from rounds 9, 10 and 11.
- **Evidence:** `is_stale` catches `(KeyError, ValueError)`; the failure is a `TypeError` at `now - verified`, raised by records whose `verified_date` is a bare `"YYYY-MM-DD"` string — including the two I appended this round, which follow the same convention every prior round used. No D12 bibkey is affected individually (I computed `is_stale` for all 14; every one returned `False`), but the aggregate entry point the CLI and dashboard call is dead for every caller.
- **Expected:** `except (KeyError, ValueError, TypeError)`, or normalise naive timestamps to UTC on read.
- **Fix:** One `except` clause.
- **Cache:** n/a

*(All 14 bibitems resolve, all 14 are in `CITATION_REGISTRY`, all 14 are cited, none is uncited, and all 14 hit `docs/citation_verifications.jsonl` at the **recomputed current** `bibitem_hash` with `verdict: match` and `is_stale == False` — I recomputed every hash from the live `.tex` rather than trusting the key. I fresh-fetched the two entries carrying prior-art load that no prior round had fetched: `arXiv:1705.10050` (finding 1.2 above) and `arXiv:2103.08535` → "Quantum projective measurements and the CHSH inequality in Isabelle/HOL", Mnacho Echenim / Mehdi Mhalla, 15 Mar 2021 — **match**, and its abstract does support `:591-592`'s Isabelle/HOL entry in the quantum-stack list. Unchanged nit, still not raised as a finding: `coqq2022` pairs year 2022 (arXiv) with DOI `10.1145/3571222` (POPL'23).)*

---

### 2.1 — 🟡 REQUIRED — `MATHER_1982_GRADIENT_REDUCTION` still has `human_verified_date: None`; the gate is right to keep blocking

- **Severity:** required
- **Gate:** ParameterProvenance
- **Location:** `src/core/provenance.py`; graph node `paper:D12`
- **Observed:** Re-measured end-to-end: `PAPER_DEPENDENCIES['D12']['parameters'] == ['MATHER_1982_GRADIENT_REDUCTION']`; Gate 3 reads `blocked — 1 parameters depended on; 0 human-verified`; `value: 0.3`, `tier: LITERATURE`, `source: 'Mather 1982, Applied Optics 21, 1125'`, `doi: 10.1364/AO.21.001125`, `llm_verified_date: '2026-07-31'`, `human_verified_date: None`, `human_verified_notes: None`.
- **Evidence:** This is an outstanding **operator** action, not an author defect, and a gate that reports an outstanding operator action as `blocked` is a correct gate (`READINESS_GATES.md:69`; `CLAUDE.md` invariant 8). What must not happen is an auto-verify path, a `tier == 'LITERATURE'` exemption, or a reviewer-set date. The correct consequence is that this finding stays open.
- **Expected:** One dashboard sign-off against `Lit-Search/Phase-6E/primary-sources/Mather1982.abstract.txt`.
- **Fix:** Operator action.
- **Cache:** n/a
- **Lane:** `prose`

### 2.2 — 🟡 REQUIRED — the Gate-4 list is 23 of 27, not "D12's full domain surface", and the five omitted are the functions behind every headline numeral in §2

- **Severity:** required
- **Gate:** ComputationCorrectness
- **Location:** `src/core/provenance.py` `PAPER_DEPENDENCIES['D12']['formulas']` (and its new comment); `src/core/formulas.py`
- **Observed:** The list was extended from 4 to 23 and the new comment says it now covers "D12's **full domain surface**". Gate 4 reads `passed — 23 formulas grounding paper claims (23 with golden/identity/roundtrip, 0 bounds-only, 0 no tests)`.
- **Evidence:** The D12 region of `formulas.py` (`poisson_avg_error_floor` … `bloch_siegert_scale`) contains **27** public functions — the same 27 round 11 measured. The list contains 23. Five are omitted:
  ```
  folklore_miss_floor              -> the paper's 148x factor and Fig. 1(A)
  folklore_gap_exponent            -> the log-4 threshold, the 51.40 crossover, the 3491.96 ratio
  gaussian_q                       -> Q(1) = 0.15866, the 76 %/95 % ratios, the 22/25 ceiling
  gaussian_threshold_error_floor   -> the sharp separation-budget floor of Sec. II C
  snr_chain_window_ceiling         -> the end-to-end ceiling of Sec. III B
  ```
  I corrected myself mid-measurement and want that on record, because my first pass produced a much louder finding that was wrong: reading the `test_kind` off `edge['meta']` instead of `edge['test_kind']` made all five look like they had `unknown`-only coverage, which would have flipped Gate 4 to `blocked`. Re-measured correctly, **all 27 have strong (golden/identity/roundtrip) coverage** and the complete list would still pass. So the gate's verdict is not wrong. What is wrong is that the verdict is again a property of the list rather than of the bundle, and the comment asserting otherwise is the reassuring number that could not have been produced — nobody counted the region before writing "full domain surface".
- **Expected:** Add the five, or derive Gate 4's formula set from the draft (the `Sentence`/`ProseClaim` layer already parses it) so the list cannot be short again. Round 11's structural point stands unrepaired: `paper:D12` still has **zero** `GROUNDED_IN` edges, so `_eval_computation_correctness` degenerates to a hand-written list authored by the party the gate audits.
- **Fix:** Five list entries; or one evaluator change.
- **Cache:** n/a
- **Lane:** `infra`

### 2.3 — 🔵 RECOMMENDED — "attains every PSD reduction between 0 and 56 %" states an unattained supremum as attained, in two artifacts

- **Severity:** recommended
- **Gate:** ParameterProvenance
- **Location:** `papers/D12/paper_draft.tex:456-460`; `src/core/formulas.py` (γ docstring)
- **Observed:** Unchanged: "the largest amplitude reduction the closed form **admits** is $1 - \sqrt{n/(2n+1)} = 33\,\%$" and "the closed form **attains** \emph{every} PSD reduction between $0$ and $56\,\%$".
- **Evidence:** Recomputed this round, not inherited. $\gamma(r,n)$ decreases strictly in $r$ with $\lim_{r\to\infty}\gamma = n/(2n+1) = 4/9$, so $1-4/9 = 55.5556\,\%$ is a supremum approached and never attained; likewise $1-\sqrt{4/9} = 33.3333\,\%$. Rounding $55.56$ to $56$ additionally claims attainment on the empty interval $(55.56\,\%,\,56\,\%]$. The project's own test says so in terms — "the maxima, which are SUPREMA -- approached as r -> infinity, never attained".
- **Expected:** "attains every PSD reduction strictly below $1-n/(2n+1) = 55.6\,\%$"; "the supremum of the amplitude reduction is $33\,\%$".
- **Fix:** One clause at `:457`, mirrored into `formulas.py`.
- **Cache:** n/a

### 2.4 — 🔵 RECOMMENDED — the $r^{-2}$ in $r^{-(n+1)}$ is still attributed to $G$, which supplies only $r^{-(n-1)}$

- **Severity:** recommended
- **Gate:** ParameterProvenance
- **Location:** `papers/D12/paper_draft.tex:436-439`
- **Observed:** Unchanged: "the physical re-referencing of $G$ via $G \propto T^{n-1}$ --- the two differ only in bookkeeping and yield the same expression, since $r^{-(n-1)}\cdot r^{-2} = r^{-(n+1)}$."
- **Evidence:** With $F(t,n)=\frac{n}{2n+1}\frac{t^{2n+1}-1}{t^{n}-1}$ and $t=1/r$, $F(1/r,n)=\frac{n}{2n+1}\frac{r^{2n+1}-1}{r^{n}-1}\,r^{-(n+1)}$ exactly. The second reading needs **two** re-referencing factors and the project's own primary-source cache names both — "Using G ∝ T^(n-1), G(T_s) = G(T_b) · r^-(n-1) **and T_s² = T_b² · r^-2**". The paper names only the $G$ half and then produces $r^{-2}$ from nowhere.
- **Expected:** "the re-referencing of $T^2$ and $G$ from the bath to the bolometer, via $T_s^2 = T_b^2 r^{-2}$ and $G \propto T^{n-1}$".
- **Fix:** One clause at `:438`.
- **Cache:** n/a

### 2.5 — 🔵 RECOMMENDED — neither γ function carries a `Lean:` reference and `formula_grounding` is structurally blind to a function with none

- **Severity:** recommended
- **Gate:** ComputationCorrectness
- **Location:** `src/core/formulas.py` (`f_link_bath_referred`, `phonon_psd_gradient_factor`); `scripts/validate.py::check_formula_grounding`
- **Observed:** Re-grepped this round: neither docstring contains a `Lean:` line; `f_link_bath_referred` carries no reference line of any kind. Pipeline Invariant #4 and the Stage-2 gate both require every function to reference a Lean theorem, and Gate 4's own evidence line now surfaces this as a WARN ("2 grounding formula(s) have no resolving Lean theorem (unverified): f_link_bath_referred, phonon_psd_gradient_factor") — a WARN that does not block.
- **Evidence:** `check_formula_grounding` iterates the references that *exist*, so a function with zero references contributes zero entries and is silently clean. Same shape as 2.2: the check measures what was declared. The paper does not overclaim here — `:441-443` says the identity is verified in `tests/test\_bundle\_formulas\_d11\_d12.py` and never that it is kernel-checked.
- **Expected:** `Lean: phononPSDGamma, phononPSD_eq_phononPSDGamma_one` on the first; an explicit `Lean: none — literature-quoted closed form, not formalized` on the second; plus a `formula_grounding` sub-check that every public function carries ≥1 reference line.
- **Fix:** Two docstring lines plus one sub-check.
- **Cache:** n/a

### 2.6 — 🔵 RECOMMENDED — the `_FORMULA_MODULE_ALIASES` allow-list is global and still never validated against the importing module

- **Severity:** recommended
- **Gate:** ComputationCorrectness
- **Location:** `scripts/build_graph.py:3607` and `:3627`
- **Observed:** `_FORMULA_MODULE_ALIASES = {"F", "formulas", "src.core.formulas"}` is consulted for every `PythonTest` node regardless of what that module bound the name to. Unchanged from rounds 10 and 11.
- **Evidence:** It manufactures no false coverage today — every `F` / `formulas` binding under `tests/` resolves to `src.core.formulas`. But the guard is a name convention, not a resolution: a future `import numpy.fft as F` in any test file silently manufactures coverage for every formula whose bare name collides, and Gate 4 is the consumer — now over 23 formulas instead of 4, so the blast radius grew with 2.2's repair. The extractor already reads each test module's source to build `referenced_names`; resolving the alias from that module's own `import` lines costs one pass.
- **Expected:** Per-module alias resolution; fail closed (no edge) when the alias does not resolve to `src.core.formulas`.
- **Fix:** One import-scan plus one dict lookup.
- **Cache:** n/a

---

### 3.1 — 🔵 RECOMMENDED — `correlated_pair_sigma_eq_sqrt_two_mul` reproduces a seventh time: bare real-√ algebra described as quantifying a witness pair

- **Severity:** recommended
- **Gate:** LeanProofSubstance
- **Location:** `papers/D12/paper_draft.tex:328-330`; `lean/SKEFTHawking/Detection/NEPAlgebra.lean:471-472`
- **Observed:** Both artifacts byte-identical to rounds 6–11. Paper: "…exhibits a fully-correlated pair whose total power is twice what quadrature predicts, with the discrepancy **quantified** exactly as $\sqrt{2}$ by \thm{correlated\_pair\_sigma\_eq\_sqrt\_two\_mul}." Theorem, read from source this round: `theorem correlated_pair_sigma_eq_sqrt_two_mul (S : ℝ) : √(4 * S) = √2 * √(S + S)`.
- **Evidence:** The statement mentions neither `IsUncorrelatedAt`, nor the witness functionals of `quadrature_uncorrelated_hypothesis_load_bearing`, nor `sigma_eq_responsivity_nep_sqrt_enbw`. It is unconditional in `S` including `S < 0`, where both sides are Lean's junk `0`, so it survives any edit to the witness. The witness itself is substantive; what is missing is the link, and the verb "quantified" asserts the link exists.
- **Expected:** "the corresponding output-noise ratio is $\sqrt2$, recorded arithmetically as \thm{…}", or a one-line corollary instantiating the identity at the witness's own `V`/`Vtot`.
- **Fix:** One clause at `:329-330`.
- **Cache:** n/a

### 3.2 — 🔵 RECOMMENDED — the Lean discloses that `enbw_dcGain_hypothesis_load_bearing` runs through the `x/0 = 0` junk value; the paper still does not

- **Severity:** recommended
- **Gate:** LeanProofSubstance
- **Location:** `papers/D12/paper_draft.tex:290-291`; `lean/SKEFTHawking/Detection/FilterFloors.lean:471-497`
- **Observed:** Paper: "The hypothesis that DC gain is nonvanishing is non-droppable, witnessed by \thm{enbw\_dcGain\_hypothesis\_load\_bearing}." Unchanged.
- **Evidence:** The witness is $h(y)=y-1$ on $[0,2]$; the proof establishes $\int_0^2 (y-1) = 0$ and closes through `enbw_of_dcGain_eq_zero`, i.e. `enbw h 2 = 0` and `0 * 2 < 1/2`. As a claim about the formal theorem this is correct and I am not calling it unsound. The Lean file discloses the reading explicitly ("Physically this is not a Lean artefact: a zero-DC-gain filter rejects the signal entirely…"). The paper carries no such sentence, so the one witness in the draft whose refutation runs through a division convention is the one the draft says least about — in a paper that elsewhere makes a virtue of disclosing exactly these branches (`filteredSNR_of_variance_eq_zero`, and now the `c = 0` branch at `:350-351`).
- **Expected:** One parenthetical at `:291` importing the Lean's own sentence.
- **Fix:** One parenthetical.
- **Cache:** n/a

*(No other class-3 findings, and I re-derived the substrate rather than inheriting it. All **175** `\thm{}`/`\texttt{}` identifiers in the draft were resolved against a fresh `lean_deps.json` read: **160 resolve to project declarations**; the 15 that do not are Mathlib names (`HasSubgaussianMGF.measure_ge_le`, `bayesRisk_le_bayesRisk_comp`, `cdf`, `gaussianReal`, `tsum`), the three core axioms, literal words (`sorry`, `native_decide`, `maxHeartbeats`, `TODO`, `infotheo`) and version strings. **Zero** intersection with the 576-name `PlaceholderMarker` set. **Zero** occurrences of any of the 13 stale names the anchor list enumerates. Over all **582** declarations in the thirteen root-imported modules: `axiom_deps_project` empty for every one, `axiom_deps_core` exactly `{propext, Classical.choice, Quot.sound}` (578/578/582 — the two smaller counts are declarations that do not need choice), zero `sorry`. That is the scope `:108-112` claims, and it is true at that scope; I did not re-derive round 11's wider 779-declaration cross-module closure and do not repeat its number. The capstone's thirteen binders were read from `BolometricFloors.lean:793-806` and match §5's account exactly. `IsAdmissibleFilter` was read from `MatchedFilter.lean:136-141`: two fields, `sq_integrable` and `mul_integrable`, which makes §5's description of `hs` as "the template-side counterpart" accurate, and makes the new $s = x^{-1/2}$, $h_f = x^{1/2}$ counterexample check out — $\int_0^1 h_f^2 = 1/2$ and $\int_0^1 h_f s = 1$ both exist, $\int_0^1 s^2$ does not.)*

---

### 4.1 — 🟡 REQUIRED — the same primary source is still rendered with reversed author order in D10 and correctly in D12, at source

- **Severity:** required
- **Gate:** CrossPaperConsistency
- **Location:** `papers/D10/bibliography.bib:49`; `papers/D12/paper_draft.tex:766-767`
- **Observed:**
  ```
  papers/D10/bibliography.bib:49   author = {Caballero, Jose Manuel Rodriguez and Unruh, Dominique}
  papers/D12/paper_draft.tex:766   D.~Unruh and J.~M.~Rodr\'iguez~Caballero
  arXiv:2512.05878                 1. Dominique Unruh   2. Jose Manuel Rodriguez Caballero
  ```
  D12 is right; D10 is wrong, and it is wrong in the `.bib` source, so re-rendering the `.bbl` will not correct it. Unchanged since round 10. Re-read from both files this round.
- **Evidence:** Gate 2's literal blocking condition ("a bibkey whose `arxiv_id` differs across papers") does not fire, because the *keys* differ rather than the IDs — `isabelleCBO` in D10 versus `UnruhCBO2025` in D12, with `isabelleCBO` carrying `arxiv: None` in the registry while `papers/D10/bibliography.bib` carries the eprint ID. The registry is strictly less informative than the artifact it governs, and the guard is keyed on the field the registry does not populate.
- **Expected:** Correct `papers/D10/bibliography.bib:49` to `{Unruh, Dominique and Rodriguez Caballero, Jose Manuel}`; alias `isabelleCBO → UnruhCBO2025` and `leanQI2025 → MeiburgLessaSoldati2025`; add a duplicate-normalised-title/DOI/arXiv detector to `validate.py`.
- **Fix:** One `.bib` line, two registry aliases, one detector.
- **Cache:** cache-verified (`UnruhCBO2025`, hash `e238304306`, `is_stale: False`, `verdict: match`)
- **Lane:** `prose`

*(No cross-paper contradictions. Gate 2 evaluates `passed` on an empty evidence set — zero CONTRADICTS edges exist project-wide — which is the standing structural caveat, not a new finding. I re-walked the seams the anchor list names: D9↔D12, the project's own `QuantumNetwork/HelstromDiscrimination.lean` POVM layer, which `:243-246` describes correctly; D1↔D12, `johnsonNyquistPSD` through `GrapheneNoiseFormula`. The paper's "two open prior-art checks" count is consistent across the abstract `:65`, §5 `:641` and the conclusion `:716-718`.)*

---

### 5.1 — 🟡 REQUIRED — the paragraph that declares the highest prior-art risk still withholds the name and the `\cite`, and still describes the project with the characterization this project retracted as unverified

- **Severity:** required
- **Gate:** NarrativeGrounding
- **Location:** `papers/D12/paper_draft.tex:641-644`; cf. `:612-613`, `:716-718`; `papers/D12/prior_art_novelty.md §3`
- **Observed:** "First, an out-of-Mathlib Lean project **on information theory and hypothesis testing** is under active development; a Bayes-binary-risk--divergence lower bound there would be substantially our Le Cam floor, and it is the highest prior-art risk in this work." No name, no `\cite`. Unchanged.
- **Evidence:** Two defects in one sentence. (1) **The withheld citation.** §5 names and cites the project two paragraphs earlier (`:612-613`, `\cite{LuccioliDegenne2024,Degenne2025Kernels}`) and the conclusion names it again (`:716-717`). Only the paragraph that *declares the obligation* withholds it, so a reader acting on that paragraph alone cannot discharge it. (2) **The retracted characterization.** `prior_art_novelty.md §3` records the retraction verbatim — the "Information theory and hypothesis testing, in Lean" subtitle "came from a web-search snippet, not from any source this project read … the only verified title is the bare token". The retraction was applied to the registry title, the bibitem and the cache, and **not** to the prose that seeded them, where the same words stand as a factual description of a third party's work. The `LuccioliDegenne2024` bibitem itself says the metadata rests solely on the `[LD24]` citation on p. 26 of `Degenne2025Kernels`, which supports "Mathlib's KL divergence" and "f-divergence data-processing inequality" and not "hypothesis testing".
- **Expected:** "First, \textsc{TestingLowerBounds}~\cite{LuccioliDegenne2024} — the out-of-Mathlib Lean project in which Mathlib's Kullback--Leibler divergence and a general $f$-divergence data-processing inequality were developed~\cite{Degenne2025Kernels} — is under active development; …"
- **Fix:** One sentence at `:641`.
- **Cache:** n/a
- **Lane:** `prose`

### 5.2 — 🔵 RECOMMENDED — the conclusion's composite-bite sentence still never received the §4 qualification, sixth round

- **Severity:** recommended
- **Gate:** NarrativeGrounding
- **Location:** `papers/D12/paper_draft.tex:707-711`; cf. `:577-583`
- **Observed:** "Two of those composites are shown to bite at a \emph{non-degenerate} operating point --- matched budget $2$, ceiling $22/25$ … --- and not merely at the signal-free zero-budget point, which is what the Birnbaum--Feller lower bound buys." Byte-identical across seven commits.
- **Evidence:** It is not false — both proofs do conclude `assignmentFidelity e0 e1 ≤ 22/25` at a signal-carrying point with the full composite binder list. What it omits is what §4 `:577-583` spends several lines saying: each proof lower-bounds the max by its detection branch and discards the relaxation term, so `22/25` is the single-mechanism value and holds uniformly in `t` and `T1`. A referee reading only the conclusion takes "the composites bite" to mean the composite *bound* is what bites, which §4 explicitly disclaims.
- **Expected:** One clause: "…, though the numerical value is the single-mechanism one (§\ref{sec:control})".
- **Fix:** One clause at `:709`.
- **Cache:** n/a

### 5.3 — 🔵 RECOMMENDED — the $f$-divergence generator and "the affinity is its complement" still disagree by a factor 2, in three artifacts

- **Severity:** recommended
- **Gate:** NarrativeGrounding
- **Location:** `papers/D12/paper_draft.tex:250-251`; `Lit-Search/Phase-6E/primary-sources/LuccioliDegenne2024.abstract.txt`; `papers/D12/prior_art_novelty.md §3`
- **Observed:** Unchanged: "The squared Hellinger distance is an $f$-divergence with generator $f(t) = (\sqrt{t}-1)^2$, and the affinity is its complement".
- **Evidence:** With $f(t)=(\sqrt t-1)^2$, $D_f(P\|Q)=\sum_i(\sqrt{p_i}-\sqrt{q_i})^2 = 2-2\,\mathrm{BC}$, so the complement relation $H^2 = 1-\mathrm{BC}$ holds only for the $\tfrac12$-normalised squared Hellinger, generator $\tfrac12(\sqrt t-1)^2$. Both the primary-source cache and `prior_art_novelty.md` repeat the unnormalised generator alongside the complement relation. The paragraph's operative claim — that a general $f$-divergence DPI plausibly subsumes the affinity DPI — is unaffected, since BC is an affine decreasing function of $D_f$ under either normalisation, which is why this is advisory.
- **Expected:** $f(t)=\tfrac12(\sqrt t-1)^2$, or "an affine function of" in place of "the complement of".
- **Fix:** One symbol at `:250`, mirrored into two artifacts.
- **Cache:** n/a

### 5.4 — 🔵 RECOMMENDED — eight lines of first-person internal-review narration still sit at the top of the file a referee receives

- **Severity:** recommended
- **Gate:** NarrativeGrounding
- **Location:** `papers/D12/paper_draft.tex:1-10`
- **Observed:** The submitted source still opens with: "⚠️ CORRECTION 2026-07-31 (D12 round-9): the first version of this note claimed the drop meant 'every page count and Overfull reading was against the wrong configuration'. That was wrong, and I asserted it without compiling both."
- **Evidence:** The substance is correct and I am not disputing it. The same account already lives verbatim in `papers/D12/bundle_metadata.json` `notes`, i.e. in a place designed to hold it. Journals receive the `.tex`; the provenance narrative belongs in the metadata, and one factual line belongs in the source.
- **Expected:** One line — "% Class option `prx`: revtex4-2 has no `prxquantum` substyle and would drop it; `\rtx@apsprx` is a no-op so this is a correctness fix, not a layout change. Re-measure against the journal's own class before submission." — and the narrative left in `bundle_metadata.json`.
- **Fix:** Delete seven lines from `:4-10`.
- **Cache:** n/a

### 5.5 — 🔵 RECOMMENDED — Fig. 1's caption quotes the crossover $N_a = 51.40$ without the $N_b$ it depends on, while the neighbouring panel establishes $N_b = 5$, where the crossover is $6.48$

- **Severity:** recommended
- **Gate:** NarrativeGrounding
- **Location:** `papers/D12/paper_draft.tex:164-172`
- **Observed:** "(A) … certified at $148\times$ for $(N_b,N_a) = (5,10)$. **(B)** Fail-open as an average-error screen: beyond the crossover at $N_a = 51.40$ …" Unchanged.
- **Evidence:** The crossover solves $2\sqrt{N_b}(\sqrt{N_a}-\sqrt{N_b}) = \log 4$ and is a function of $N_b$. Recomputed this round by root-finding: at $N_b = 50$ it is $51.395903\ldots \to 51.40$ ✓; at the $N_b = 5$ the caption has just established for panel A it is $6.482385$. The figure itself states the parameter in its subplot titles, so a reader looking at the plot can recover it — which is why this is advisory. The caption alone cannot be read correctly.
- **Expected:** "beyond the crossover at $N_a = 51.40$ (at $N_b = 50$)".
- **Fix:** Three words at `:167`.
- **Cache:** n/a

*(Every other class-5 check passes and I recomputed each numeral independently. **The new ENBW parenthetical at `:303-311` is correct and I verified it two ways**, which is the item you asked me to check: analytically, $\int_0^T e^{-2x/\tau} = \tfrac{\tau}{2}(1-e^{-2u})$ and $\int_0^T e^{-x/\tau} = \tau(1-e^{-u})$ give $\mathrm{enbw}\cdot T = \tfrac{u}{4}\tfrac{1+e^{-u}}{1-e^{-u}}$ exactly; and numerically against a 2-million-point trapezoid integration of the paper's own `enbw` definition, agreeing to 8 significant figures at $u \in \{0.01, 0.1, 1, 5, 20\}$ (0.50000417, 0.50041660, 0.54098835, 1.26695914, 5.00000002). A 200k-point scan over $u \in (0, 50]$ gives a minimum of 0.5000000000, and the $u \to 0^+$ limit is $1/2$ — so "$\ge \tfrac12$ for every $u > 0$", "$\to \tfrac12$ as $u \to 0$" and "it never approaches $1/4$" are all exactly right. Round-11 finding 5.1 is genuinely and correctly discharged. Other numerals, all recomputed: $Q(1)=0.15865525393145705$, Birnbaum–Feller $0.12098536225957168 = 76.257\,\%$ of it, $0.021596386605275\ldots = 94.929\,\%$ of $Q(2)$ ✓; Mills fractions exactly $1/2$ and $4/5$ ✓; Mills vacuity root $0.37223890$ ✓; $x = 2Q(1)/(1-2Q(1)) = 0.4647947734915441$ and $x/(2(1+x))$ returns $Q(1)$ to 17 digits ✓; $e^5 = 148.4131591$ ✓; ratio at $(50,60)$ is $3491.955383$ ✓; $\gamma(2,4)=0.4731481481$, $1-\gamma = 52.6852\,\%$, $1-\sqrt\gamma = 31.2142\,\%$, difference $21.4710 \to 21.5$ ✓; $r^{*} = 1.1913538$ for a 30 % PSD reduction ✓; $22/25 = 0.88$ ✓. **No unhedged first-claim exists**: `:630-639` is scoped to "the ecosystems we actually assessed", `:626` explicitly disclaims absence in HOL Light, `:644-647` says of Isabelle AFP and Coq `infotheo` "That is a gap in our evidence, not a finding of absence, and we do not assert absence in those ecosystems" — exactly what the anchor list requires. The abstract's factor-two-sharpening disclosure at `:63-65` is present and matches §5.)*

---

### 6.1 — 🔵 RECOMMENDED — §1 promises an enumeration that §5 now explicitly declines to give, and only two of the seven are identifications in §1's sense

- **Severity:** recommended
- **Gate:** AssumptionDisclosure
- **Location:** `papers/D12/paper_draft.tex:86-93` vs `:664-686`
- **Observed:** §1 now reads "the capstone \thm{bolometer\_error\_floor} carries **several, enumerated in** \S\ref{sec:scope}", in a sentence whose subject is *identifications*: "The \emph{identification} of any symbol with a physical instrument … Where such an identification is needed it appears as an \emph{explicit binder}…". §5 lists seven and then says "We deliberately do not claim this list is complete… the theorem statement in \texttt{Electrothermal/BolometricFloors.lean} is the authority on the full list."
- **Evidence:** Two residues of round-11 6.1/6.2. (a) "enumerated in §5" promises an enumeration; §5 delivers a deliberately partial list and says so. The words fight. (b) Of the seven, §5 introduces four as "the modelling binders of the capstone" (`IsThermalFluctuationLimited`, `IsWhiteFilteredVariance`, `IsUncorrelatedAt`, `IsAdmissibleFilter`), two as identifications ($\mu_1-\mu_0 = \int h_f s$, $\sigma = \sqrt{V_{\rm tot}}$) and one as a regularity condition ($\int s^2 < \infty$). A reader following §1's pointer and counting *identifications* finds two. `IsAdmissibleFilter` in particular is an admissibility condition on the filter — I read the structure, two integrability fields — not an identification of a symbol with an instrument.
- **Expected:** "…carries several modelling binders, two of them identifications in this sense; §\ref{sec:scope} names seven, and the Lean statement is the authority on the full list."
- **Fix:** One clause at `:89`.
- **Cache:** n/a
- **Note on the item you asked me to verify:** the round-11 6.1 repair is **honest**. The theorem's binder list at `BolometricFloors.lean:793-806` is thirteen; `hs : IntervalIntegrable (fun x => s x ^ 2) volume 0 Tw` is present at exactly the position §5 describes; the counterexample §5 gives is correct; and the completeness claim was dropped rather than restated at a new number, which is the outcome that cannot go stale. I am not filing 6.1-of-round-11 again.

### 6.2 — 🔵 RECOMMENDED — `avgError_ge_gaussianQ_sharp` carries an undisclosed ordering binder $\mu_0 \le \mu_1$

- **Severity:** recommended
- **Gate:** AssumptionDisclosure
- **Location:** `papers/D12/paper_draft.tex:209-213`; `lean/SKEFTHawking/Detection/GaussianThreshold.lean:785-787`
- **Observed:** "The headline is the sharp separation-budget floor \thm{avgError\_ge\_gaussianQ\_sharp}: no threshold placement achieves an average error below $Q\!\left(\Delta\mu/2\sigma\right)$." §2.3 nowhere states the branch-ordering convention.
- **Evidence:** Read from source this round: `(hσ : 0 < σ) (hμ : μ₀ ≤ μ₁) (hz : (μ₁ - μ₀) / (2 * σ) ≤ z₀)`. Here the ordering binder **is** load-bearing: with a signed $\Delta\mu$ and $\mu_1 < \mu_0$, $Q(\Delta\mu/2\sigma) > 1/2$ while the achievable average error approaches $1/2$ from above, so the statement fails. Low severity because the convention is the obvious one — but §5 enumerates twelve modelling Props by name while this binder of the section's own headline theorem goes unnamed.
- **Expected:** "for a signal branch of the larger mean ($\mu_0 \le \mu_1$, an explicit binder)".
- **Fix:** One parenthetical at `:210`.
- **Cache:** n/a

### 6.3 — 🔵 RECOMMENDED — "for every admissible filter" describes a theorem that does not take `IsAdmissibleFilter`, in a paper where that identifier is one of the twelve disclosed Props

- **Severity:** recommended
- **Gate:** AssumptionDisclosure
- **Location:** `papers/D12/paper_draft.tex:272-274`; `lean/SKEFTHawking/Detection/FilterFloors.lean:197-201`; cf. `:654`
- **Observed:** "the central result is \thm{enbw\_mul\_window\_ge\_half}: for every **admissible filter** with nonvanishing DC gain, $\mathrm{ENBW}\cdot T \ge 1/2$."
- **Evidence:** Read from source this round: `(h : ℝ → ℝ) (T : ℝ) (hT : 0 < T) (hint : IntervalIntegrable h volume 0 T) (hsq : IntervalIntegrable (fun x => h x ^ 2) volume 0 T) (hDC : (∫ x in (0:ℝ)..T, h x) ≠ 0)`. `IsAdmissibleFilter` is not among them — it is declared in a different module (`MatchedFilter.lean:136`), is `\thm{}`-cited in this paper at `:654` as one of the twelve disclosed hypotheses, and the floor does not consume it. The direction is conservative (the theorem is more general than the prose says), so nothing is overclaimed; the defect is that a reader mapping prose onto the disclosed-Prop list attaches the wrong hypothesis to the section's central result, and the two integrability conditions that *are* load-bearing are never mentioned.
- **Expected:** "for every filter integrable with its square on the window and with nonvanishing DC gain" — noting that `IsAdmissibleFilter` enters only at the matched-filter and capstone layers.
- **Fix:** One clause at `:273`.
- **Cache:** n/a

### 6.4 — 🔵 RECOMMENDED — "independent of responsivity" omits the $R \neq 0$ binder that makes it true

- **Severity:** recommended
- **Gate:** AssumptionDisclosure
- **Location:** `papers/D12/paper_draft.tex:335-337`; `lean/SKEFTHawking/Detection/NEPAlgebra.lean:584-585`
- **Observed:** "The end-to-end ceiling is \thm{snrChain\_le\_window\_ceiling}, attained by the matched boxcar … and independent of responsivity (\thm{snrChain\_independent\_of\_responsivity})."
- **Evidence:** Read from source this round: `theorem snrChain_independent_of_responsivity {R P_sig nepVal enbwVal : ℝ} (hR : R ≠ 0) : snrChain R P_sig nepVal enbwVal = P_sig / (nepVal * √enbwVal)`. At `R = 0` the chain returns Lean's junk `0` and the independence fails. Same class and same low severity as 6.2/6.3 — no physical claim moves, since a zero-responsivity readout is not a readout — but this paper's whole discipline is that degenerate branches are stated, and `NEPAlgebra.lean` itself ships `nepOfOutput_of_responsivity_eq_zero` precisely to disclose this branch.
- **Expected:** "independent of responsivity (\thm{snrChain\_independent\_of\_responsivity}, for any nonzero $R$)".
- **Fix:** Four words at `:337`.
- **Cache:** n/a

*(The rest of class 6 re-verified from signatures rather than inherited. The "Twelve propositions" enumeration at `:650-658` is exactly right — I enumerated every `Prop`-valued `def`/`abbrev`/`structure` across the thirteen modules and got exactly the twelve the paper lists, with no thirteenth: `IsCountRule`, `IsWhiteFilteredVariance`, `IsShotFilteredMoments`, `IsResponsivityChain`, `IsUncorrelatedAt`, `IsAdmissibleFilter`, `SolvesHeatBalance`, `PerturbationsDecay`, `SolvesDrivenBalance`, `SolvesSinusoidalBalance`, `IsThermalFluctuationLimited`, `IsThermalFluctuationLimitedGamma`. The anchor list's "Tracked-Prop disclosure (11 of them)" is the stale artifact, not the paper. "Thirteen root-imported modules" at `:108` is exactly right: `lean/SKEFTHawking.lean` root-imports 6 `Detection.*` + 3 `Electrothermal.*` + 4 `Control.*`. Gate 6 evaluates `passed` on "0 hypotheses assumed by cited theorems; 0 referenced in paper, 0 undisclosed" — an empty evidence set for a paper with a hypothesis-disclosure section, which is the standing structural caveat carried in the QI section.)*

---

### 7.1 — 🟡 REQUIRED — D12's entire numeric surface still sits outside every freshness and provenance gate

- **Severity:** required
- **Gate:** NumericalFreshness
- **Location:** `papers/D12/paper_draft.tex` throughout; `validate.py --check count_literals` / `numerical_literals` / `counts_fresh` / `tables_fresh` / `paper_provenance`
- **Observed:** Re-run this round, all five green and all five vacuous for this bundle:
  ```
  counts_fresh        1/1 checks passed
  count_literals      D12 — no count literals found
  numerical_literals  D12 — no inline unit-bearing literals in body
  tables_fresh        1/1 checks passed
  paper_provenance    D12/figures — 3 referenced figure(s) resolved
  ```
  while the draft carries at least `0.4731`, `53 %`, `31 %`, `52.7`, `31.2`, `21.5`, `22`, `33 %`, `56 %`, `4/9`, `0.444`, `1.19`, `30 %`, `22/25`, `3/25`, `0.15866`, `148`, `148.41`, `51.40`, `3491.96`, `76 %`, `95 %`, `50 %`, `80 %`, `0.37`, `0.4648`, `0.46479`, and now the ENBW expression at `:308-310`.
- **Evidence:** No `REPORTS` edge exists from `paper:D12` to any `CountMetric`, `papers/D12/tables/` feeds no prose, and Gate 9 therefore passes on an empty set. Every numeral above is correct today — I recomputed all of them this round — but rounds 6, 7 and 8 each found a wrong one in this unguarded surface (`23`, then the 21.5 derivation, then `0.4651`), and the only thing that caught each was a reviewer. 2.2 compounds it: `gaussian_q`, `folklore_miss_floor` and `folklore_gap_exponent` — the functions behind eight of those numerals — are the ones Gate 4's list still omits.
- **Expected:** A `\pipelinevalue{}` chain or a `papers/D12/tables.py` spec for the derived quantities; or at minimum register the derived values in `PARAMETER_PROVENANCE` as `DERIVED` with `DEPENDS_ON` edges, extending the mechanism round 9 built for one parameter.
- **Fix:** One tables spec plus a macro retrofit.
- **Cache:** n/a
- **Lane:** `prose`

### 7.2 — 🔵 RECOMMENDED — spelled-out tree counts reproduce outside the macro chain; both correct today, both invisible to `count_literals`

- **Severity:** recommended
- **Gate:** NumericalFreshness
- **Location:** `papers/D12/paper_draft.tex:108` ("Thirteen root-imported modules"), `:650` ("Twelve propositions carry modelling or admissibility content")
- **Observed:** Both unchanged; both hand-written counts of tree contents rather than `\input{counts.tex}` macros.
- **Evidence:** Both verified correct at review time by direct enumeration (13 root imports; 12 `Prop`-valued declarations — enumerations above). `count_literals` reports "no count literals found" because the detector does not match spelled-out numerals, so the class is unguarded across every bundle. Note that `:650`'s twelve is now the *only* count in the paper that a wave can silently invalidate, since the capstone-binder count was correctly replaced with "several"/"seven, not claimed complete".
- **Expected:** `d12modules` / `d12modelprops` macros in `docs/counts.tex`, or extend `count_literals` to catch spelled-out numerals before "modules"/"propositions"/"theorems".
- **Fix:** Two macros, or one regex.
- **Cache:** n/a

---

### 8.1 — 🔴 BLOCKER — (3a) was not built. Heading-parse closure is still executing at `build_graph.py:1698-1704`, and the 73 findings the commit says "now read open" all still read `fixed`

- **Severity:** blocker
- **Gate:** FixPropagation
- **Location:** `scripts/build_graph.py:1682-1704`
- **Observed:** You asked me to find a path that still closes a finding without a conforming record. There are three, and they are all the same path: the mechanism (3a) says it deleted is still there. Lines 1682–1697 are a sixteen-line comment asserting the deletion —

  > "EVERY finding is born `open`. The supersession ledger is the SOLE channel that can transition it, at every severity. This **replaces** heading-parse closure… Making the ledger the only transition channel removes the class instead of narrowing it."

  Immediately below it, unchanged and still executing:
  ```python
  status = 'open'
  if re.search(r'[✅✓]|\bfixed\b|\bresolved\b|\bdone\b|now\s+closed', heading, re.IGNORECASE):
      status = 'fixed'
  if re.search(r'(now\s+closed|✅\s*\(?(?:now\s+)?closed)', body[:600], re.IGNORECASE):
      status = 'fixed'
  ```
  The commit message's own remediation text said "delete the two `status = 'fixed'` assignments at `build_graph.py:1651-1656`". The comment was added; the assignments were not deleted. Nothing downstream compensates: the blocking-closure bar at `:1792` is gated on `severity in ('critical', 'major', 'blocker')`, so it never touches a `minor` or `advisory` finding, and `meta['status']` ships whatever heading-parse set.
- **Evidence:** Two independent measurements.

  **(a) The live corpus.** Findings whose emitted `meta.status` is not `open` and which have **no ledger record at all**, computed by joining `extract_review_finding_nodes()` against `_load_supersession_ledger()`:
  ```
  73 findings closed with no ledger record   (72 advisory, 1 minor)
  e.g. review:2026-05-01-T1900-L3-bundle-stage13:L3:1.1
       label "1.1 ✅ FIXED — Reall2024ThirdLawBPS bibitem"    status=fixed
       review:2026-05-01-1507-bundle-stage13:I1:3.1
       label "3.1 VERIFIED FIXED — §5 cross-registry attribution now"  status=fixed
  ```
  **73, 72 advisory and 1 minor** — character-for-character the blast-radius figure the commit message reports as the set that "now read open until a record closes them". It is the set that *would* have changed had the change been made, reported as though it had been. This is the seventh instance of the pattern you flagged, and it is the one that matters most, because the number is not merely unproducible — it is the exact diff of the unshipped edit.

  **(b) Mutation test, isolated temp tree, empty ledger.** Eight synthetic findings in a `tempfile.mkdtemp()` tree with `build_graph.PROJECT_ROOT` monkeypatched and a ledger containing zero records. No repository file was read or written:
  ```
  case                                sev declared  minted     status
  A  🔴 heading, decl blocker             blocker    critical   open     <- guard works
  B  🔴 heading + "✅ FIXED", decl blocker blocker    critical   open     <- guard works
  C  🔴 heading + "✅ FIXED", decl recommended        minor      fixed    <- CLOSED, NO RECORD
  D  🟡 heading + "resolved", decl required required  major      open     <- guard works
  E  🔵 heading + "✅ FIXED", decl recommended        minor      fixed    <- CLOSED, NO RECORD
  F  no glyph, heading contains "done"    (none)     advisory   fixed    <- CLOSED, NO RECORD
  ```
  E and F are (3a) plainly not holding at minor and advisory. C is finding 8.2.
- **Expected:** Delete lines `1698-1704` in full and leave `status = 'open'`. Then re-measure: the join in (a) must return **0**, and that number — not a predicted blast radius — is the evidence the change landed. The 73 re-opening is the correct state; none of them has a closure record.
- **Fix:** Delete seven lines. The comment describing the deletion is already written.
- **Impact note:** not cosmetic. `bundle_readiness.py` counts open advisories, and the GREEN verdict in `READINESS_GATES.md:251` requires "≤5 open advisories". A bundle can move toward GREEN by wording its own review headings.
- **Cache:** n/a

### 8.2 — 🔴 BLOCKER — (3b) opened a new blocking-closure bypass: a declared severity is a downgrade lever, and combined with 8.1 it self-closes a document's own 🔴 finding with no ledger record

- **Severity:** blocker
- **Gate:** FixPropagation
- **Location:** `scripts/build_graph.py:1645-1672` (declared severity) interacting with `:1698-1704` (8.1) and `:1791-1804` (the bar)
- **Observed:** You asked whether a declared field can be gamed. It can, and the specific game is one the previous design structurally prevented. Before `fc624fc5`, severity came from the heading glyph, so a finding rendering as `### 1.1 — 🔴 BLOCKER — ✅ FIXED: …` minted `critical`, hit the blocking-closure bar, found no conforming record, and was forced back to `open`. That is mutation case B above, and it still works. After `fc624fc5`, adding **one line to the body** — `- **Severity:** recommended` — demotes the same finding to `minor`, which drops it below the bar's `severity in ('critical', 'major', 'blocker')` guard, and 8.1's surviving heading-parse then closes it. Mutation case C:
  ```
  heading:  ### 3.1 — 🔴 BLOCKER — ✅ FIXED: the widget is broken in D12
  body:     - **Gate:** FixPropagation
            - **Severity:** recommended
  ledger:   {"supersessions": []}          (empty)
  minted:   severity=minor   status=fixed
  ```
  A finding that renders to a human as a red blocker ships as a closed minor. The document set both its severity and its status, which is exactly the property (3a) and (3b) were jointly specified to remove — "no artifact authored by the party seeking closure can set severity or status".
- **Evidence:** The two mutation runs above, same temp tree, empty ledger, `PROJECT_ROOT` monkeypatched, tree removed. Case B (no declared line) → `critical`/`open`. Case C (identical but for the declared line) → `minor`/`fixed`. The only difference between them is one body line. Two further shapes from the same run:
  ```
  G  🔵 heading, "- **Severity:** blocker" placed at body char >1200   -> minted minor
  H  🔵 heading, "- **Severity:** P1-blocker"                          -> minted minor
  ```
  G is a window bug: `_decl` searches `body[:1200]` only, while `review_severity_declared` searches the whole file, so a long finding can declare `blocker` where the validator sees it and the extractor does not. H is a value bug: `([A-Za-z]+)` captures `P`, `_MAP.get('p')` returns `None`, and the code falls silently to glyph inference — no warning, no failure, and the validator (8.4) counts the line as a compliant declaration.
- **Expected:** Three changes, and the first alone closes the demonstrated exploit.
  1. Land 8.1 (delete heading-parse). With no self-closure channel, a demotion cannot close anything.
  2. Apply the bar at **every** severity, not `critical/major/blocker`. Severity should determine how loudly a finding is reported, never whether the closure bar applies. This is the same generalisation `_CLOSING_STATUSES` already made in the status dimension, applied to the severity dimension.
  3. Make an unrecognised declared value a hard error rather than a silent fallback, and widen `_decl`'s window to the whole body or raise it well past the longest observed finding body (this document has several over 4000 characters).
- **Fix:** One deletion (8.1), one predicate widening, one `else: raise`/warn branch.
- **Cache:** n/a
- **Lane:** `infra`

### 8.3 — 🟡 REQUIRED — the declared severity field is not authoritative: the file-level `_BLOCKER_RE` escalation overrides it, and round-11 8.8 is unfixed

- **Severity:** required
- **Gate:** FixPropagation
- **Location:** `scripts/build_graph.py:1614`, `:1673-1680`
- **Observed:** The commit headline is "severity is **declared not inferred**". Twelve lines after the declared-severity block, unchanged from round 11:
  ```python
  if _BLOCKER_RE.search(heading) or _BLOCKER_RE.search(body[:1000]):
      severity = 'critical'
  elif file_has_critical_marker:
      severity = 'critical'
  ```
  with `file_has_critical_marker = bool(_BLOCKER_RE.search(source))` over the whole file. Both branches run *after* the declared field is read and overwrite it.
- **Evidence:** Mutation-tested in the same temp tree. Two identical three-finding documents, differing only in one appended sentence in a QI section that contains an asterisk-bolded blocker token:
  ```
                     control      poisoned
  1.1 declared recommended   minor        critical
  2.1 declared blocker       critical     critical
  8.9 declared advisory      advisory     critical
  ```
  Every declared severity in the file is discarded. So the authoritative-declaration claim holds only for documents that never quote the word in bold — including quoting the anchor list's own sentence "An unhedged 'first' is a [bolded blocker token]". I omitted both literal marker forms from this entire document on purpose, which is itself the finding: a reviewer must currently write around the extractor.
- **Expected:** Delete the `file_has_critical_marker` fallback and restrict `_BLOCKER_RE` escalation to documents with no declared field, matching the stated precedence. Escalation and declaration cannot both be authoritative.
- **Fix:** Four lines.
- **Cache:** n/a

### 8.4 — 🟡 REQUIRED — `review_severity_declared` has examined zero documents, and its counting predicate passes padded documents, fails compliant ones, accepts bogus values, and is evaded by a directory name

- **Severity:** required
- **Gate:** FixPropagation
- **Location:** `scripts/validate.py::check_review_severity_declared`
- **Observed:** You asked whether the cutoff is the right boundary. The boundary is defensible; the predicate under it is not, and the check's live output is
  ```
  0 review document(s) dated >= 2026-08-01 checked; 0 with findings that do not declare severity
  ```
  — it has never looked at a document, and it shipped green on that.
- **Evidence:** Six mutation cases, each a synthetic review document in an isolated temp tree with `validate.PROJECT_ROOT` monkeypatched:
  ```
  compliant: 3 findings, each declares once                      PASS   correct
  non-compliant: 3 findings, none declares                       FAIL   correct
  PADDING: 1 finding declares 3x, other 2 declare nothing        PASS   <- bypass
  PADDING: 3 stray Severity lines in the Summary, 0 in findings  PASS   <- bypass
  FALSE POS: 3 compliant findings + 2 non-finding ### sections   FAIL   <- false positive
  CUTOFF EVASION: same non-compliant doc in a 2026-07-31 dir     PASS   <- bypass
  BOGUS VALUE: "- **Severity:** yes" on all 3 findings           PASS   <- bypass
  ```
  Four defects, all from the same root — the predicate is `n_sev < n_head`, a **document-level count comparison**, so severity declarations are fungible across findings and can even come from outside any finding:
  1. **Padding.** Any surplus of declarations anywhere covers any deficit anywhere. Two undeclared findings pass if a third declares three times, or if the Summary carries three stray lines.
  2. **False positive.** `_HEADING = r'^#{3,5}\s+\S'` counts *every* h3–h5 heading, not finding headings. A fully compliant report with an `### Environment` section is a red build. This document uses `##` for its non-finding sections to avoid that, which is a structural constraint the check imposes without documenting.
  3. **Cutoff evasion.** `date = md.parent.name[:10]` — the directory name, chosen by the author. Nothing cross-checks it against the front-matter `review_date:`, the file mtime, or the commit date. A non-compliant document in a `2026-07-31-…` directory is exempt permanently, in 2027 as much as today.
  4. **Value not validated.** `_SEV_LINE` matches the prefix only. `- **Severity:** yes` passes here and maps to `None` in `build_graph`, which then falls back to glyph inference (8.2 case H). The check certifies the presence of a field whose content it never reads.
- **Expected:** Per-finding, not per-document: split the file on the same `_REVIEW_SECTION_RE` the extractor uses, and require each *section body* to contain exactly one `- **Severity:**` line whose value is in `_MAP`. Take the date from the front-matter `review_date:` (falling back to the directory name only when absent) so the cutoff cannot be renamed away. Assert `extractor_severity == declared_severity` for every finding — that single assertion catches padding, bogus values, the `body[:1200]` window bug of 8.2, and 8.3's escalation override, all four at once.
- **Fix:** One section-split plus one equality assertion.
- **Cache:** n/a
- **Lane:** `infra`

### 8.5 — 🟡 REQUIRED — `recurrence_reopens_closures` catches 7 of the 26 recurrences this corpus itself documents; the thresholds are measured but the *matcher* is not, and rewording defeats it

- **Severity:** required
- **Gate:** FixPropagation
- **Location:** `scripts/validate.py::check_recurrence_reopens_closures`
- **Observed:** You asked whether a real recurrence can evade. Most of them do. The check requires a common **prefix** of ≥30 characters and ≥75 % of the shorter normalized label — and the normalized label is derived from `label = f'{section_num} {heading[:50]}'`, i.e. **the heading truncated at 50 characters**, from which `_norm` then strips the leading section number and severity word. The comparable text is therefore ~35 characters, of which 30 must agree from the first character.
- **Evidence:** Measured against this corpus's own ground truth. Round 11's verification table states that every round-10 D12 paper-side finding reproduces, and enumerates them: 1.1, 1.2, 1.3, 2.1–2.6, 3.1, 3.2, 4.1, 5.1–5.6, 6.2, 6.3, 7.1, 7.2, 8.3, 8.4, 8.5, 8.6 — **26 documented recurrences out of 30**. I simulated closure of every round-10 D12 finding and ran the check's own predicate against round 11:
  ```
  round-10 findings eligible for the check                    15
  of those, FIRED against a round-11 finding                   7
  skipped, severity minor/advisory (csev filter)              11
  skipped, normalized label shorter than _MIN_TITLE = 30       5
  checked but missed (reworded heading)                        8
  ```
  Detection rate on documented ground truth: **7 / 26 ≈ 27 %**. The misses are not marginal — the two most important findings in the corpus score near zero:
  ```
  round-10 8.1 (blocker, continued verbatim as round-11 8.1)   best common prefix = 4
  round-10 8.2 (blocker, continued verbatim as round-11 8.2)   best common prefix = 2
  round-10 2.2 (required, restated in round-11 3.2's slot)     best common prefix = 4
  round-10 8.5, 8.6 (required, both explicitly restated)       best common prefix = 0, 1
  ```
  Corpus-wide, **223 of 1465 findings (15.2 %)** have a normalized label below `_MIN_TITLE` and are excluded from both sides of the comparison before any matching happens.

  The structural point: prefix matching rewards **verbatim heading reuse**, and a fresh-context reviewer rewords by construction — this agent's own instructions say "Scan the paper freshly… Do not pattern-match on known-past failures." The check is therefore weakest exactly where the process is strongest. The three corrections in the commit message all tuned the *thresholds* against measured label lengths; none tested the *matcher* against a known recurrence pair. The planted probe that finally fired was a copy of its own source heading, which is the one input shape a real recurrence never has.
- **Expected:** Replace prefix agreement with an order-insensitive token measure (Jaccard or containment over the normalized token set, calibrated on the 26 known pairs above, which give you a labelled dataset for free). Widen the label beyond `heading[:50]` — the truncation, not the threshold, is what makes 15 % of findings unmatchable. Drop the `csev in (critical, major)` filter, or state in the summary that minor/advisory closures are unchecked. And validate against the 26 pairs: a matcher that scores under 20/26 on them has not been tested.
- **Fix:** One similarity function plus one fixture of known pairs.
- **Cache:** n/a

### 8.6 — 🟡 REQUIRED — `recurrence_reopens_closures`'s summary line reports a denominator 3.7× larger than what it compared, and two innocent-pair classes are one severity away from firing

- **Severity:** required
- **Gate:** FixPropagation
- **Location:** `scripts/validate.py::check_recurrence_reopens_closures` (the `details.insert(0, …)` summary)
- **Observed:** The live check prints
  ```
  552 blocking-severity closure(s) checked against 690 open finding(s)
  from later reviews; 0 contradicted by a recurrence
  ```
  Both figures misdescribe what happened. `len(closed)` is **every** closed finding with a label ≥30 characters, at any severity; the `critical/major` filter is applied inside the loop and is not reflected in the count. And `len(open_)` is the entire open set; the `odate > cdate` filter is also applied inside the loop, so "from later reviews" describes a filter the number does not carry.
- **Evidence:** Re-derived with the check's own code, on the live graph:
  ```
  552  what the summary calls "blocking-severity closures checked"  (= all closures)
  308  actually at critical/major severity
  148  of those with >=1 same-bundle LATER open finding to compare  (26.8% of 552)
    3  closures with inferred_bundle/paper == None -> can never match
   85  of the 690 "open findings" have no attribution -> can never match
  ```
  So "0 contradicted over 552 closures" is reassurance sized 3.7× larger than the comparison performed. Same species as the 60-character prefix the commit message correctly diagnoses; the constant was fixed and the summary that made the constant look harmless was not.

  On the other direction you asked about — an innocent pair tripping it — I swept every same-bundle cross-date pair in the corpus for firing pairs with non-identical labels. **Seven** fire; five are true recurrences (I read the headings: round-9 5.5 and round-10 5.2 are both the withheld-TestingLowerBounds-citation finding, correctly matched). Two are not, and both are currently masked only by severity:
  ```
  D12  "class 8 productionrunhealth pass no findings"     (round 8, advisory)
       "class 8 productionrunhealth pass no finding for"  (round 10, advisory)   common=43
  D5   "lucianopaliathanasissaridakis2506 CLOSED with c"  (advisory closure NOTE)
       "lucianopaliathanasissaridakis2506 re"             (later 🔴)             common=34
  ```
  The first pair is the **class-8 PASS note**, repeated near-verbatim by design in every round; it carries no defect at all, and if any round's copy is ever closed at major severity, every later round's copy fires as a "recurrence". The second is a **closure note matched against the finding it closes** — a shape guaranteed to share a long prefix. Both sit at `advisory` today purely by accident of glyph, and 8.3 shows one bolded token anywhere in a file promotes every finding in it to `critical`. The false-positive floor is therefore zero-by-luck, not zero-by-construction.
- **Expected:** Report the number actually compared (`148`), and separately the number skipped and why. Exclude non-finding sections (pass notes, closure notes) from the `open_` side — the extractor already has the section heading and can be told that a section whose label begins "class N … pass" is not a finding.
- **Fix:** Two summary strings and one exclusion predicate.
- **Cache:** n/a

### 8.7 — 🟡 REQUIRED — no supersession record exists for any D12 finding from rounds 7–11, so 65 blocking findings flag D12 and the repairs you did land stay open

- **Severity:** required
- **Gate:** FixPropagation
- **Location:** `docs/review_finding_supersessions.json`; graph node `paper:D12`
- **Observed:** Gate 11 reads `blocked — 161 findings flag this paper (13 fixed, 0 accepted, 148 open, 65 of them submission-blocking)`. That is up from round 11's 127/49, because round 11's own 34 findings landed with no closure records. All 24 D12 ledger records still target the two oldest review documents; **zero** records exist for rounds 7 through 11.
- **Evidence:** This is not a machinery defect — it is the machinery working — but it is a live obstruction, and it is now the dominant one. Concretely: round-11's 6.1 (the capstone binder disclosure) and round-11's 5.1 (the ENBW functional) are both genuinely and correctly repaired at `fc624fc5`, I verified both independently above, and both `ReviewFinding` nodes are still `open` and still counted among the 65 — because nothing wrote a record. `READINESS_GATES.md:213` requires "every finding marked `fixed` [to have] a SUPERSEDES edge … or a documented commit reference"; a fix with neither is invisible to the gate in exactly the way an unfixed one is. `BUNDLE_LIFT_PROCEDURE.md` §11's loop is "Author fixes → supersession ledger append → re-invoke same reviewer fresh context", and the middle step has been skipped for five consecutive rounds. D12 cannot leave RED on this gate by fixing findings alone — and after 8.1 is properly landed, the ledger will be the *only* channel, so the backlog compounds.
- **Expected:** Append one ledger record per closed finding for rounds 7–11, each carrying `status`, ≥40 characters of evidence and the commit containing the fix. I re-checked the 13 existing D12 records that name a commit: every one resolves to a real object with a plausible subject, so the existing practice is sound; it is simply not being applied.
- **Fix:** Ledger appends; no code change.
- **Cache:** n/a
- **Lane:** `infra`

### 8.8 — 🔵 RECOMMENDED — three residuals, each measured this round: `stage13_review_doc` is still write-only, `minted >= 1` is still the mint predicate, and twelve `except` handlers still spell "not evaluated" as "passed"

- **Severity:** recommended
- **Gate:** FixPropagation
- **Location:** `papers/D12/bundle_metadata.json:19`; `scripts/validate.py::check_review_docs_mint_findings`; `scripts/validate.py` (12 sites)
- **Observed:**
  1. `papers/D12/bundle_metadata.json` still reads `"stage13_review_doc": null` beside `"last_stage13_review": "2026-07-31"`. `BUNDLE_DIRECTORY_SCHEMA.md:53` and `BUNDLE_LIFT_PROCEDURE.md` both mandate the field; the only code that touches it is `bundle_source_manifest.py:132`, which *writes* `False`/`None` at creation. No code reads it.
  2. `review_docs_mint_findings` still passes on `minted >= 1`, so a document losing all but one of its findings passes.
  3. AST sweep of `scripts/validate.py` for `CheckResult(passed=True)` inside an `except` handler: **12 hits**, at lines `623, 1956, 1959, 1971, 2112, 2381, 4275, 4613, 5564, 7190, 7198, 7474` — the same twelve, line numbers shifted by this commit's insertions. Seventh consecutive round.
- **Evidence:** Credit where due on (3): `check_recurrence_reopens_closures`'s own `ImportError` handler returns `passed=False` with the message "unavailable — unverified, not passing", which is the correct posture and shows the lesson landed at the new site. It did not land at the twelve old ones, which is precisely the one-site-per-round cadence round 11 flagged. Making the AST sweep a test converts this from a recurring finding into a build failure.
- **Expected:** Make the sweep a `pytest` case; strengthen `minted >= 1` to equality against the count of severity-marked headings; either read `stage13_review_doc` or delete it from all four documents.
- **Fix:** One test, one equality, one field decision.
- **Cache:** n/a

### 8.9 — Class 8 ProductionRunHealth: PASS, no finding for D12

- **Severity:** advisory
- **Gate:** ProductionRunHealth
- **Location:** `papers/D12/paper_draft.tex`; graph node `paper:D12`
- **Evidence:** D12 makes no simulation-evidence claim. I grepped the draft case-insensitively for "Monte Carlo", "numerical evidence", "simulation", "simulated" and "we simulate" — **zero** occurrences; every quantitative statement is a kernel-checked rational enclosure or a closed-form evaluation, and I recomputed all of them (class-5 note). Gate 8 evaluates `passed — 0 production runs linked, 0 successful, 0 failed`, which is correct for a pure-formalization bundle. Aside, unchanged and not a D12 finding: most `ProductionRun` nodes project-wide carry `status: unknown`, a blocking status under Gate 8 for any paper that ever links one.

---

## Verification of the repairs you asked me to attack

| item | verdict | how I checked it |
|---|---|---|
| **(3a) birth-status invariant — "heading-parse closure is GONE at every severity"** | **not built** | The comment asserting the deletion is at `build_graph.py:1682-1697`; the code it says it deleted is at `:1698-1704`, unchanged and executing. Two measurements: 73 findings (72 advisory, 1 minor) still ship `fixed` with no ledger record — the identical figure the commit reports as the set that now reads open; and a six-case mutation run in a temp tree with an empty ledger closes three findings by heading text alone. Finding 8.1. |
| **(3b) declared severity — "is the cutoff right; can the field be gamed?"** | **cutoff is defensible; the field is gameable and the check is vacuous** | The field is a downgrade lever: one body line demotes a 🔴-rendering, "FIXED"-worded finding to `minor`, which drops it below the blocking-closure bar, and 8.1's surviving heading-parse then closes it — mutation case C, empty ledger, no record. The field is also *not* authoritative in the other direction: a single bolded blocker token anywhere in a file rewrites every declared severity in it to critical (measured: 3/3 findings, including one declared `advisory`). The validator has examined **0** documents, passes padded documents, fails compliant ones with non-finding `###` sections, accepts `- **Severity:** yes`, and is evaded by naming the directory `2026-07-31-…`. Findings 8.2, 8.3, 8.4. |
| **(3c) recurrence-reopens — "can a real recurrence evade; can an innocent pair trip it?"** | **yes and yes** | Against this corpus's own documented ground truth (round 11's table: 26 of 30 round-10 findings reproduce), the check fires on **7**. 11 are excluded by the critical/major filter, 5 by `_MIN_TITLE` against a label truncated at `heading[:50]`, 8 by rewording — including both blockers, at common prefixes of 4 and 2 characters. Corpus-wide, 15.2 % of findings are below the title floor. On the innocent side, 7 same-bundle pairs fire; 2 are not recurrences (the class-8 PASS note repeated verbatim each round; a closure note matched against the finding it closes), and both are masked only by an advisory severity that 8.3 can promote. Findings 8.5, 8.6. |
| **the seventh capstone binder + dropped completeness claim** | **honest** | Binder list read from `BolometricFloors.lean:793-806`: thirteen binders, `hs : IntervalIntegrable (fun x => s x ^ 2) volume 0 Tw` present exactly where §5 says. The $s = x^{-1/2}$, $h_f = x^{1/2}$ counterexample checks out against `IsAdmissibleFilter`'s two actual fields. §5 now says "We deliberately do not claim this list is complete" and names the Lean statement as the authority — the one form of the claim that cannot go stale. Residual is cosmetic (§1 still says "enumerated in §5"): finding 6.1. |
| **the ENBW functional account** | **verified two ways** | Analytically exact, and matched to 8 significant figures against a 2-million-point numeric integration of the paper's own `enbw` definition at five values of $u$. Minimum over $u \in (0,50]$ is 0.5000000000; limit at $u \to 0^+$ is $1/2$. "Never approaches $1/4$" is right. Round-11 5.1 discharged. |
| **Gate 4 extended 4 → 23 with the Birnbaum golden values** | **the golden values are right; "full domain surface" is not** | `phi(1)/2 = 0.12098536225957168` and `phi(2)*2/5 = 0.021596386605275228` both recomputed and both correct, and the new test's own comment records catching a $z=3$-for-$z=2$ slip, which is the right kind of test. But the region has **27** functions, the list has 23, and the five omitted (`gaussian_q`, `folklore_miss_floor`, `folklore_gap_exponent`, `gaussian_threshold_error_floor`, `snr_chain_window_ceiling`) are the ones behind §2's headline numerals. All 27 do have strong coverage — I corrected an accessor error that had briefly made the omitted five look uncovered — so the gate's verdict is sound; the claim about the list is not. Finding 2.2. |
| **`LATE_PHASE6:184` in both docs** | **correct, and the new claim checks out** | Both documents now state the mtime meaning and name `stage13_redo_required` as the Stage-F exit gate. I verified the new claim rather than accepting it: `stage13_redo_required` is set `True` by `bundle_append.py:301,317`, initialised `False` by `bundle_source_manifest.py:132`, set `False` only on the explicit bookkeeping path (`bundle_append.py:371`), and read by `check_bundle_source_freshness.py:131`. No mtime writer clears it. Round-11 8.6 discharged. |
| **round-11 paper-side findings 1.1, 1.3, 1.4, 2.1, 2.3–2.6, 3.1, 3.2, 4.1, 5.2–5.6, 6.3–6.5, 7.1, 7.2** | **all reproduce** | The paper diff since `89d10728` touches three hunks only (`:86`, `:300-312`, `:673-686`). Every other cited line is byte-identical. Re-filed above with this round's own measurements and re-read Lean signatures, not inherited text. |

## Compliance note

I ran no git-mutating command — only `git log`, `git show`, `git diff`, `git status`. I ran
no `pdflatex`, inside `papers/` or anywhere; no compile was needed for a three-hunk
prose-only diff (see Environment).

All mutation testing was in-process against a monkeypatched `PROJECT_ROOT` in
`tempfile.mkdtemp()` trees, each removed at the end of its run; **no repository file was
mutated by it.** Files I wrote: this report, and two verification records appended to
`docs/citation_verifications.jsonl` (`RashidHasan2017`, `EchenimMhalla2021`) — that file
was backed up to the scratch directory before the append. `lean/lean_deps.json.hash`, the
132 `figures/*.png` and `.claude/plugins/skeft-qa/agents/adversarial-reviewer.md` were
already dirty on arrival and I did not touch them.

Three disclosures about this document itself. **(1) I tripped finding 8.3 on my own
report and am recording it rather than quietly fixing it.** My first draft quoted the
second alternative of `_BLOCKER_RE` — the `severity`-colon-`critical` literal — once, inside
finding 8.4's remediation text. Extracting that draft minted **33 findings, all 33
`critical`**, against 2 / 13 / 17 / 1 declared in the bodies: a 31-finding false escalation
on D12's already-blocked Gate 11, produced by nothing but naming a regex alternative in
prose. I paraphrased the literal and re-extracted; severity now tracks the declared fields
exactly. That is the second consecutive round in which the reviewer's own report is the
demonstration case, and it is why 8.3 is filed rather than left as a note. **(2)** This
document therefore contains neither of `_BLOCKER_RE`'s two literal alternatives anywhere —
a reviewer currently has to write around the extractor, and that constraint is
undocumented. **(3)** Its
non-finding sections use `##`, not `###`, because finding 8.4 case 5 shows an `###
Environment` heading would make this document fail `review_severity_declared` despite every
finding declaring its severity. Both are workarounds, not preferences.

Per rule 10, I have closed nothing. Every finding above is `open`, including the ones whose
underlying defect I verified as repaired (round-11 5.1, 6.1, 8.6, and the Birnbaum golden
values) — those are reported as verified in the table above and require a ledger record
with status, evidence and a commit before any of them transitions.

## QI Candidate

**Systemic: a guard shipped in response to a finding is being verified against the cases
its author imagined, and the summary line it prints is chosen at the same time and from the
same imagination. The summary is the part that has to be measured.**

This is the third consecutive round in which the same shape appears, and this round it
appears three times in one commit:

1. **(3a)** — the comment describing the deletion was written, the deletion was not made,
   and the blast-radius figure (73 findings, 72 advisory, 1 minor) was reported as an
   accomplished state change when it was a prediction of an unshipped edit. Nothing in the
   commit re-measured after the change, because there was no change to re-measure.
2. **(3c)** — the commit message correctly diagnoses the 60-character prefix as "a constant
   chosen from what I imagined the data looked like", fixes the constant against measured
   label lengths, and then prints `552 blocking-severity closures checked` where 148 were
   compared. The threshold was measured; the denominator that makes the threshold look
   harmless was not.
3. **(3b)** — `review_severity_declared` reports `0 review document(s) checked` and passes.

The register already carries "a guard's scope predicate must not share a blind spot with
the thing it guards" from round 11. This is its sibling and it is cheaper to enforce:
**every numeric a check prints must be the cardinality of the set it actually iterated,
and that must be asserted, not narrated.** Concretely, for a check of the form "N things
checked, M bad": add one assertion that `N` equals the length of the loop's own filtered
input after every `continue` in the body, and one test that runs the check against a
fixture where the answer is known by construction. For (3c) that fixture already exists for
free: the 26 recurrence pairs round 11 enumerated in its own verification table are a
labelled dataset, and a matcher scoring 7/26 on them cannot be shipped as passing.

**Also carried forward, each measured this round:**

1. **Five of D12's eleven gates still pass on empty or curated evidence sets** —
   `CrossPaperConsistency` (0 CONTRADICTS edges project-wide), `ComputationCorrectness`
   (23 of 27, from a hand-written list; `paper:D12` still has zero `GROUNDED_IN` edges),
   `AssumptionDisclosure` (0 ASSUMES edges, for a paper with a hypothesis-disclosure
   section), `NarrativeGrounding` (5 abstract sentences, 0 flagged interesting, 0 SUPPORTS
   edges, for a 26-line abstract of claims), `NumericalFreshness` (28 derived numerals in
   the prose, 0 REPORTS edges). `GateResult` should gain an `evidence_count`, and a gate
   evaluating on an empty set should report `open` — already in the status model — rather
   than `passed`.
2. **"Not evaluated" is still spelled "passed", seventh round running** (8.8). The AST
   sweep finds the identical 12 handlers; the new check got it right at its own site and
   nowhere else.
3. **Canonical documents remain a source of reviewer error** (1.3, 8.8). `READINESS_GATES.md:33`
   states an `arxiv_verified` criterion no D12 entry meets, and `BUNDLE_DIRECTORY_SCHEMA.md:53`
   mandates a field no code reads. `LATE_PHASE6_ABSORPTION_PROTOCOL.md:184` was fixed this
   round and is the model for the other two: state what the code does, date the correction,
   and name the finding that forced it.
