---
paper: L3
bundle: L3
bundle_target: L3
tier: 2
reviewer: lead
model: claude-opus-5
review_date: 2026-08-15T00:00:00Z
readiness_gates_version: 1
kind: stage10-redraft
prior_reviews:
  - papers/AutomatedReviews/2026-05-01-L3-bundle-stage13/L3.md
  - papers/AutomatedReviews/2026-05-11-1251-bundle-stage13/L3.md
  - docs/audits/stage13_attribution_sweep_2026-06-10.md#l3
counts_snapshot: 22693 thm / 22667 substantive / 2040 modules / 0 sorry
lean_axioms_BHThermodynamicsFourLaws: propext, Classical.choice, Quot.sound (0 project axioms)
compile_L3: OK pages=4 tex_errors=0 unresolved_refs_in_pdf=0 words=2679 (+300 fig) = 2979 vs ceiling 3750
---

# L3 — findings surfaced by the Stage-10 full redraft

## Summary

**2 BLOCKER, 5 REQUIRED, 8 RECOMMENDED.** All were found while drafting, by
reading the Lean module and the primary sources rather than by reviewing the
prior manuscript. The redraft itself states only what the substrate supports,
so none of these is a live defect *in the new draft*; they are defects in the
substrate, the figure, the registry and the process that the new draft had to
route around.

The two blockers are primary-source misreadings that the project has carried
since 2026-04-26 and that propagate beyond L3: a contrast case that is not one
(§1.1), and a result attributed to a Bose--Einstein condensate that its authors
explicitly computed for a constant-sound-speed fluid and explicitly deferred for
a condensate (§1.2). Both are recorded in `BHThermodynamicsFourLaws.lean`'s
docstring, in `CITATION_REGISTRY`, in the Python mirror, in the shipped figure
and in `bundle_metadata.json`, so remediation is not a prose edit.

The required findings are all one shape: **the formal content is thinner than
the names suggest.** `FourLaws_*` encodes neither the zeroth nor the third law;
its "first law" field is `0 < G_N^emerg`; its second-law field is a recorded
sign constraint on a supplied parameter; the two branch signs are hypotheses
rather than consequences of the monotonicity theorems that sit beside them; and
one of the four falsifiers falsifies nothing it is advertised to falsify.

**Mechanical state after the redraft.** L3 is clean on every gate it can be
scored against: `bundle_prose_em_dash_free`, `bundle_reader_facing_voice`,
`bundle_manuscript_length` (2979 of 3750 word equivalents),
`bundle_sentence_length` (the corpus ratchet is unchanged and the advisory
over-60 count fell), `numerical_literals`, `count_literals`,
`prose_theorem_reference_coverage` (every one of the draft's Lean identifiers
resolves in `lean_deps.json`), `bundle_cross_references_resolve`,
`bundle_figure_adequacy`, `bundle_structural_coherence`,
`bundle_native_decide_debt`, `paper_provenance`,
`citation_primary_sources_present`. The three corpus-level failures
(`em_dash_free`, `reader_facing_voice`, `sentence_length`) are all in other
bundles.

---

## Findings

### 1.1 — 🔴 BLOCKER — Jacobson--Volovik 1998 is recorded as a heating contrast case whose own prose contradicts its equations; both halves are false

- **Severity:** blocker
- **Lane:** `research`
- **Verify:** `cd "$REPO" && ! grep -q "inconsistent with the same paper" lean/SKEFTHawking/BHThermodynamicsFourLaws.lean`
  *What it asserts:* asserts the retracted characterisation is gone from the module that is the substrate for L3, D3 and paper 27. Exits 1 at HEAD.
- **Gate:** CitationIntegrity
- **Location:** `lean/SKEFTHawking/BHThermodynamicsFourLaws.lean:131-134`;
  `src/core/citations.py` (`CITATION_REGISTRY['JacobsonVolovik1998']['provides']`);
  `papers/AutomatedReviews/2026-04-26-2230-wave5-process/deep_research_analog_conflation.md:34`;
  propagated to `papers/paper27_bh_thermodynamics_four_laws/paper_draft.tex` and the
  superseded L3 draft.
- **Observed:** The module docstring records `§VIII prose "cools as it evaporates" is **inconsistent with the same paper's own equations** (back-reaction slows v, so T_H rises)`, and lists the paper among the "explicit contrast cases (different analog system, opposite behavior)". The registry entry repeats it verbatim.
- **Evidence:** Read directly from the preserved TeX source,
  `Lit-Search/Phase-6a/primary-sources/JacobsonVolovik1998.source/EventHorIn3He13Unix.tex`.
  - Line 453: "An ergoregion exists for this soliton only if $v>c_\perp$, so there is an ergoregion if and only if there is an event horizon." The horizon exists **above** $c_\perp$, not below it.
  - Eq. (5.7), line 525: $\kappa = (v_F/d)(1-v^2/v_F^2)\sqrt{(1-c_\perp^2/v^2)/(1-c_\perp^2/v_F^2)}$. As $v$ falls toward $c_\perp$ the radicand vanishes, so $\kappa \to 0$.
  - §Discussion, lines 707-713: "the back reaction will be to slow the soliton. The Hawking temperature is fairly constant until, as $v$ approaches $c_\perp$, $T_H$ goes to zero. This is unlike the evaporation of a neutral black hole which gets hotter as it shrinks. It is rather like the Hawking radiation from a black hole with a large magnetic charge which cannot be discharged and so cools as it evaporates and approaches an extremal black hole."
  The prose follows from the equations. The "back-reaction slows $v$, so $T_H$ rises" reasoning is the **thin-film domain-wall** law of Jacobson--Koike, where the horizon exists for $v < c_\perp$ and $T_H$ stays finite at $v = 0$. The two systems were swapped. This is the mirror image of the 2026-04-26 conflation the note was written to fix.
- **Expected:** The bulk moving-soliton horizon of Jacobson--Volovik 1998 sits on the **cooling** side, with Balbinot et al., and only the Jacobson--Koike thin film is a heating contrast case.
- **Fix:** Rewrite the module docstring's JV-1998 bullet and the registry `provides` field to state the cooling behaviour and its Eq. (5.7) mechanism. Propagate to paper 27 and D3. The new L3 draft already states it correctly, and the correction strengthens the Letter: one substrate class hosts both branches, which is the sharpest available argument that a criterion is needed rather than an analogy.
- **Cache:** local primary source (TeX), read in full.

### 1.2 — 🔴 BLOCKER — Balbinot et al. 2005 is labelled a Bose--Einstein-condensate result throughout, and it is not one

- **Severity:** blocker
- **Lane:** `research`
- **Verify:** `cd "$REPO" && ! grep -rq "BEC-acoustic" lean/SKEFTHawking/BHThermodynamicsFourLaws.lean src/bh_thermodynamics src/core/citations.py`
  *What it asserts:* asserts the misattributing label is gone from the module, the Python mirror and the citation registry. Exits 1 at HEAD.
- **Gate:** CitationIntegrity
- **Location:** `lean/SKEFTHawking/BHThermodynamicsFourLaws.lean` (docstring throughout, and the `Regime` constructor gloss at :158-166); `src/bh_thermodynamics/{__init__,acoustic_evolution,four_laws_data,falsifier_checks}.py`; `src/core/visualizations.py:8906` (`fig_T_H_evolution_regime_partition`, including the rendered legend); `src/core/citations.py`; `papers/L3/bundle_metadata.json` (`apex_theorems[*].claims`).
- **Observed:** The regime is named "BEC-acoustic" in every artifact, and the prior draft opened with "the BEC-acoustic backreaction analysis of Balbinot, Fagnocchi, Fabbri ... backreaction of thermally radiated phonons on the fluid background".
- **Evidence:** `Lit-Search/Phase-6a/primary-sources/Balbinot2005PRD.source/acusticpap.tex`, §"The fate of the acoustic black hole", lines 1020-1023, verbatim: "In a similar way one can treat sonic black holes formed by a Bose-Einstein condensate [garayvisser]. The major difference comes from the fact that the sound velocity $c$ is not constant, but proportional to $\rho^{1/2}$. This will be discussed elsewhere." The computation shipped in that paper is a Laval-nozzle sonic hole at constant $c$; the condensate case is explicitly future work. Eq. (Tsonic) itself, at lines 991-997, is derived from the constant-$c$ velocity profile.
- **Expected:** The cooling branch is anchored to a **sonic** hole in a constant-sound-speed flow. A condensate inherits the branch by the sign of $d\Thawk/dt$, not by Eq. (Tsonic).
- **Fix:** Rename the regime and its glosses to "sonic" or "acoustic" across the module docstring, the Python mirror, the figure and the metadata `claims` strings. The `ADWExtremality` constructor name is already carried for bibliographic continuity and need not change; its docstring must. The new L3 draft states the constant-$c$ scope explicitly in Sec. III.
- **Cache:** local primary source (TeX), read in full.

### 1.3 — 🟡 REQUIRED — Balbinot's own contrast reference is a different Jacobson--Volovik paper from the one the project cites

- **Severity:** required
- **Lane:** `research`
- **Verify:** `cd "$REPO" && uv run python -c "from src.core.citations import CITATION_REGISTRY as C; assert any(str(v.get('journal','')).startswith('JETP') and v.get('year')==1998 and 'Jacobson' in str(v.get('authors','')) for v in C.values()), 'JETP Lett. 68, 874 (1998) absent'"`
  *What it asserts:* asserts the registry holds the paper Balbinot actually cites for the contrast. Exits 1 at HEAD.
- **Gate:** CitationIntegrity
- **Location:** `lean/SKEFTHawking/BHThermodynamicsFourLaws.lean:65-68` and the superseded `papers/L3/paper_draft.tex` intro, both of which attach Balbinot's contrast sentence to `JacobsonVolovik1998` = PRD **58**, 064021.
- **Observed:** The module records that "Balbinot 2005 §'Fate of the acoustic black hole' **explicitly contrasts** the two", and the project resolves the citation to PRD 58, 064021.
- **Evidence:** `acusticpap.tex`, `\bibitem{jacobson}`: "T. Jacobson and G.E. Volovik, *JEPT Letters* **68**, 874 (1998); T. Jacobson and T. Koike, 'Black hole and baby universe in a thin film of ${}^3He$-A', cond-mat/0205174". Both are thin-film domain-wall works. PRD 58, 064021 is not in Balbinot's reference list.
- **Expected:** The contrast attribution resolves to JETP Lett. **68**, 874 (1998) plus Jacobson--Koike.
- **Fix:** Add the JETP Lett. entry to `CITATION_REGISTRY` with its primary source, and re-anchor the module docstring. The new L3 draft attributes the contrast to the thin film only, so it is already correct.
- **Cache:** local primary source (TeX), read in full.

---

### 2.1 — 🟡 REQUIRED — `FourLaws_*` is named for four laws and encodes two, one of them only as a side condition

- **Severity:** required
- **Lane:** `lean`
- **Verify:** `cd "$REPO" && uv run python -c "import json; d=json.load(open('lean/lean_deps.json')); n={e['name'] for e in d}; P='SKEFTHawking.BHThermodynamicsFourLaws.FourLaws_Schwarzschild.'; assert any(k.startswith(P) and ('zeroth' in k or 'thirdLaw' in k) for k in n), 'no zeroth/third-law field'"`
  *What it asserts:* asserts the structure gained a zeroth- or third-law field (or, equivalently, fails until the naming and the content are reconciled). Exits 1 at HEAD.
- **Gate:** LeanProofSubstance
- **Location:** `lean/SKEFTHawking/BHThermodynamicsFourLaws.lean:537-543` (`FourLaws_Schwarzschild`), `:569-577` (`FourLaws_ADWExtremality`), `:420-422` (`ADWSecondLaw`)
- **Observed:** Each structure carries a first-law field, a second-law field and an evaporation-sign field. `firstLaw_smarr` is `0 < p.G_N_emerg_eval`, a positivity side condition on the coupling, not the differential first law. `ADWSecondLaw` is `0 ≤ entropy_div ∧ 0 < b.A`, where `entropy_div` is a supplied real parameter of the structure, so the Glorioso--Liu result is recorded as a hypothesis on that parameter rather than derived. The zeroth and third laws have no field at all; the docstrings say they are deferred pending Killing-vector, horizon-generator and affine-time infrastructure.
- **Evidence:** Statements read from `lean_deps.json` and the source; the deferral is stated by the module itself at `:509-520` and `:585-595`.
- **Expected:** Either the structures acquire zeroth- and third-law content, or their names stop asserting it. A structure whose name is the paper's headline and whose content is two of four laws will be read as the former by anyone who does not open it.
- **Fix:** Preferred: build the deferred content. Minimum: rename to something that names what is inside (the first-law coupling condition, the entropy-production sign, the evaporation sign), and keep the four-laws framing in prose where the deferral can be stated alongside it. The new L3 draft states the scope explicitly in Sec. IV and takes no credit for the missing laws.
- **Cache:** N/A.

### 2.2 — 🟡 REQUIRED — The partition criterion transports its branch signs rather than deriving them, while the theorems that would derive them sit beside it unused

- **Severity:** required
- **Lane:** `lean`
- **Verify:** `cd "$REPO" && uv run python -c "import json; d=json.load(open('lean/lean_deps.json')); e=[x for x in d if x['name'].endswith('BHThermodynamicsFourLaws.regime_partition_criterion')][0]; deps=set(e['name_deps_project']+e['value_deps_project']); assert any('T_H_schwarzschild_strict_decreasing' in x for x in deps), 'criterion does not consume the monotonicity theorem'"`
  *What it asserts:* asserts the criterion's proof actually consumes the Schwarzschild monotonicity result rather than reading a hypothesis field. Exits 1 at HEAD.
- **Gate:** LeanProofSubstance
- **Location:** `lean/SKEFTHawking/BHThermodynamicsFourLaws.lean:441-450` (`H_RegimePartition`), `:468-479` (`regime_partition_criterion`)
- **Observed:** `evap_dT_dt_above : b.M > M_c p → 0 < dT_dt_evap` and `evap_dT_dt_below : b.M < M_c p → dT_dt_evap < 0` are fields of the hypothesis structure. `regime_partition_criterion` proves its conclusion by rewriting with `classify_Schwarzschild_iff` and applying the field. `T_H_schwarzschild_strict_decreasing` and `T_H_acoustic_evolution_strict_decreasing` are proved in the same module and are not used by it.
- **Evidence:** Proof body read at `:474-479`; both branches are `exact H.evap_dT_dt_*`.
- **Expected:** A criterion that earns its name derives the sign on at least the Schwarzschild branch, where the derivation is short: `T_H_schwarzschild_strict_decreasing` plus a mass-decrease hypothesis `dM/dt < 0` gives $d\Thawk/dt > 0$ without assuming it. The cooling branch can be derived the same way from `T_H_acoustic_evolution_strict_decreasing` and a time-parameterised profile.
- **Fix:** Add `evap_sign_above_of_mass_decreasing` and its cooling counterpart, deriving the two signs, and restate `H_RegimePartition` over the weaker inputs. This converts the headline theorem from a transport of assumptions into a derivation, which is the single largest available strengthening of this bundle. The new draft says plainly that the signs are hypotheses, which is honest and is also the weakest form of the claim the paper can make.
- **Cache:** N/A.

### 2.3 — 🟡 REQUIRED — `falsifier_alpha_ADW_dependence` does not falsify what the metadata says it falsifies

- **Severity:** required
- **Lane:** `lean`
- **Verify:** `cd "$REPO" && uv run python -c "import json; d=json.load(open('lean/lean_deps.json')); e=[x for x in d if x['name'].endswith('falsifier_alpha_ADW_dependence')][0]; assert 'H_RegimePartition' in e['type'], 'falsifier does not reference the hypothesis bundle it claims to constrain'"`
  *What it asserts:* asserts the theorem actually constrains the tracked bundle's $\delta$ rather than proving a standalone positivity. Exits 1 at HEAD.
- **Gate:** LeanProofSubstance
- **Location:** `lean/SKEFTHawking/BHThermodynamicsFourLaws.lean:736-741`; claim string in `papers/L3/bundle_metadata.json`, `apex_theorems[12].claims`
- **Observed:** The metadata says the theorem establishes that "δ_ADW must depend on α_ADW via the Wave-9 ansatz, not be a free parameter". The theorem is `(p : ADWParams) → 1 < p.α_ADW → 0 < (p.α_ADW - 1) * p.Λ_UV`. It mentions neither `H_RegimePartition` nor `delta`, and follows from `p.Λ_pos` and one `linarith`.
- **Evidence:** Type and two-line proof body read from the source and `lean_deps.json`. The theorem that does carry the advertised content is `delta_ADW_nonzero_iff_alpha_ADW_ne_one` (`:495-509`), which is stated over the bundle and proves $\delta \neq 0 \iff \aADW \neq 1$.
- **Expected:** Either the falsifier is restated over the bundle's `delta`, or the metadata claim is corrected to the sign statement the theorem actually makes.
- **Fix:** Restate as `(H : H_RegimePartition b p T_H0 dT_dt_evap delta) → 1 < p.α_ADW → 0 < delta`, which is one rewrite away and is the claim the paper wants. Correct the metadata string either way. The new L3 draft describes both theorems by what they prove and claims no independence result.
- **Cache:** N/A.

### 2.4 — 🔵 RECOMMENDED — The Letter's central physical reading of `M_c` is an exact identity between two shipped definitions and is not a theorem

- **Severity:** recommended
- **Lane:** `lean`
- **Verify:** `cd "$REPO" && uv run python -c "import json; d=json.load(open('lean/lean_deps.json')); assert any('M_c' in x['name'] and 'G_N_emerg' in x['type'] and x['kind']=='theorem' for x in d if x['module']=='SKEFTHawking.BHThermodynamicsFourLaws'), 'no theorem links M_c to G_N_emerg'"`
  *What it asserts:* asserts the identity is machine-checked rather than left to the reader. Exits 1 at HEAD.
- **Gate:** LeanProofSubstance
- **Location:** `lean/SKEFTHawking/BHThermodynamicsFourLaws.lean:246-249` (`M_c`), `:214-217` (`ADWParams.G_N_emerg_eval`), `lean/SKEFTHawking/LinearizedEFE.lean:227-228` (`G_N_sakharov`), `:274-275` (`G_N_emerg`)
- **Observed:** `M_c p = N_f·Λ_UV/(12π·α_ADW)` and `G_N_emerg = α_ADW·12π/(N_f·Λ_UV²)`, so `G_N_emerg_eval p * M_c p = 1/Λ_UV` exactly. Nothing states this.
- **Evidence:** Verified symbolically and numerically against the Python mirror at three parameter points, agreement to machine precision (`src/bh_thermodynamics/regime_classifier.py::M_c_default`).
- **Expected:** `M_c_mul_G_N_emerg_eq_inv_Lambda : p.G_N_emerg_eval * M_c p = 1 / p.Λ_UV`.
- **Fix:** One `field_simp; ring` after unfolding, with `Λ_pos`, `N_pos`, `α_pos` in scope. This is the sentence the redraft leans on to convert `M_c` from "a project-original dimensional ansatz with no derivation" (the prior framing, and the prior draft's own words) into "the mass whose emergent gravitational length reaches the cutoff length". Making it a theorem lets the paper cite it instead of asserting it.
- **Cache:** N/A.

### 2.5 — 🔵 RECOMMENDED — "Without invoking pointwise NEC" is attached to Glorioso--Liu, who neither assume nor target the null energy condition

- **Severity:** recommended
- **Lane:** `research`
- **Verify:** `cd "$REPO" && ! grep -q "without invoking pointwise NEC" lean/SKEFTHawking/BHThermodynamicsFourLaws.lean`
  *What it asserts:* asserts the vacuous attribution is gone from the module. Exits 1 at HEAD.
- **Gate:** CitationIntegrity
- **Location:** `lean/SKEFTHawking/BHThermodynamicsFourLaws.lean:84`, `:398`; the superseded L3 abstract and §"Four laws by regime"
- **Observed:** The claim is presented as a property of the Glorioso--Liu derivation and as "the load-bearing substrate-class generalization compared to the standard Bekenstein--Hawking-on-area-law treatment".
- **Evidence:** `Lit-Search/Phase-1-and-Background/primary-sources/GloriosoLiu2018.pdf`, abstract and §I read in full. The derivation runs from a $Z_2$ dynamical KMS symmetry and the non-negative imaginary part of the effective action; the null energy condition appears nowhere as premise or as target. The statement is true and vacuous as an attribution.
- **Expected:** If the contrast with the area-theorem route is wanted, it is this project's observation about that route and needs its own grounding, not a property of Glorioso--Liu.
- **Fix:** Drop the phrase from the docstring, or restate it as our own comparison with a citation for the area-theorem side. The new draft omits it.
- **Cache:** local primary source (PDF), abstract and §I read in full.

---

### 3.1 — 🟡 REQUIRED — The shipped figure prints internal taxonomy into the published PDF and its legend mislabels the right panel

- **Severity:** required
- **Lane:** `figure`
- **Verify:** `cd "$REPO" && uv run python -c "import src.core.visualizations as v, inspect; s=inspect.getsource(v.fig_T_H_evolution_regime_partition); assert 'Phase 6a Wave 5' not in s, 'figure title still carries internal taxonomy'"`
  *What it asserts:* asserts the reader-visible figure title no longer names an internal work unit. Exits 1 at HEAD.
- **Gate:** NarrativeGrounding
- **Location:** `src/core/visualizations.py:8906` (`fig_T_H_evolution_regime_partition`); rendered at `papers/L3/figures/fig_T_H_evolution_regime_partition.png`
- **Observed:** The rendered PNG carries the title "Phase 6a Wave 5 — BCH four-laws regime partition (Schwarzschild ↔ BEC-acoustic)". Two defects in one string: an internal work-unit label that a referee will read as a repository artifact, and an em-dash, in a bundle whose `.tex` is em-dash clean. Separately, the two-panel figure uses a shared legend, so the right panel's single straight trace, which is $\dADW/\Ladw$ against $\aADW$, is labelled "BEC-acoustic (Balbinot 2005)". It is neither.
- **Evidence:** Direct read of the rendered PNG. The `bundle_prose_em_dash_free` check reads `.tex` only, so a dash inside a figure is invisible to it.
- **Expected:** A title naming the physics, and per-panel legends.
- **Fix:** Retitle to the physics, give the right panel its own legend or drop the legend there entirely, and re-render. See 3.2 before re-rendering.
- **Cache:** N/A.

### 3.2 — 🔵 RECOMMENDED — The figure's right panel plots a straight line and should plot the crossing that defines `M_c`

- **Severity:** recommended
- **Lane:** `figure`
- **Verify:** `cd "$REPO" && uv run python -c "import src.core.visualizations as v, inspect; s=inspect.getsource(v.fig_T_H_evolution_regime_partition); assert 'M_c' in s, 'right panel does not show the critical-mass crossing'"`
  *What it asserts:* asserts the second panel carries the paper's defining scale rather than the identity $y = x - 1$. Exits 1 at HEAD.
- **Gate:** NarrativeGrounding
- **Location:** same as 3.1
- **Observed:** The right panel is $\dADW/\Ladw = \aADW - 1$, a straight line through zero at $\aADW = 1$. In a four-page Letter with one figure, half of that figure spends 300 word equivalents restating a definition.
- **Evidence:** Direct read of the rendered PNG.
- **Expected:** The panel that earns its space here is the one behind finding 2.4: the emergent gravitational length $\GNem M$ against the cutoff length $\Ladw^{-1}$ as a function of $M$, crossing at $\Mc$, with the two branches shaded. That panel makes the paper's central claim visible instead of restating an ansatz.
- **Fix:** Replace the right panel and re-render through `src/core/visualizations.py` per Pipeline Invariant #3, together with 3.1.
- **Cache:** N/A.

---

### 4.1 — 🔵 RECOMMENDED — The Python mirror's `Regime` docstring describes a smooth crossover that neither the Lean module nor the manuscript asserts

- **Severity:** recommended
- **Lane:** `python`
- **Verify:** `cd "$REPO" && ! grep -q "T_H attains its maximum" src/bh_thermodynamics/regime_classifier.py`
  *What it asserts:* asserts the mirror no longer describes a $\Thawk(M)$ with an interior maximum. Exits 1 at HEAD.
- **Gate:** CrossPaperConsistency
- **Location:** `src/bh_thermodynamics/regime_classifier.py:16-30`
- **Observed:** The docstring characterises the regimes by the sign of $d\Thawk/dM$ and states "`BOUNDARY`: `M = M_c` — `dT_H/dM = 0`, T_H attains its maximum" and "`ADW_EXTREMALITY`: `M < M_c` — cooling-toward-extremality (`dT_H/dM > 0`, `C(M) > 0`)".
- **Evidence:** No $\Thawk(M)$ spanning both regimes exists in the Lean module or in this package; `T_H_schwarzschild` is defined on the heating branch only, and the cooling branch is parameterised by $t$, not $M$. The manuscript states explicitly that $M = \Mc$ is a classification boundary and not a demonstrated smooth crossover.
- **Expected:** The mirror's docstring describes the same object the Lean does: a comparison of $M$ against $\Mc$ selecting between two separately-anchored time evolutions.
- **Fix:** Rewrite the three constructor glosses in terms of the sign of $d\Thawk/dt$ under evaporation.
- **Cache:** N/A.

---

### 5.1 — 🔵 RECOMMENDED — Nothing gates PRL's abstract limit, and the prior L3 abstract was over it by a factor of three

- **Severity:** recommended
- **Lane:** `infra`
- **Verify:** `cd "$REPO" && uv run python scripts/validate.py --check bundle_abstract_length`
  *What it asserts:* asserts a check exists that sizes a letter's abstract against its venue's limit. Exits nonzero at HEAD because no such check is registered.
- **Gate:** none yet
- **Location:** `scripts/validation/checks/bundles_readiness.py` (`bundle_manuscript_length` sizes the whole manuscript only); `papers/L3/bundle_metadata.json` (`length_target` has no abstract field)
- **Observed:** PRL limits an abstract to roughly 600 characters. The superseded L3 abstract was about 1900, and would have been desk-returned. `bundle_manuscript_length` passed it, because 4 pages is within a 3750 word-equivalent ceiling however the words are distributed.
- **Evidence:** Measured from the superseded draft. The redrafted abstract is 594 characters.
- **Expected:** A venue-scoped abstract ceiling declared alongside `length_target` and checked the same way, at least for the letter-format bundles (L1, L2, L3, E1, E2).
- **Fix:** Extend `length_target` with an optional `abstract_ceiling` and size the `abstract` environment from the `.tex`. This is a small check and it catches a hard rejection. Route through the `architecture-change` skill.
- **Cache:** N/A.

### 5.2 — 🔵 RECOMMENDED — `bundle_lean_module_coverage` makes a redraft pay for dropping a dependency it no longer has

- **Severity:** recommended
- **Lane:** `infra`
- **Verify:** `cd "$REPO" && uv run python -c "import json; a=json.load(open('papers/L3/append_log.json')); m={x for e in a['events'] for x in e.get('lean_modules_referenced',[])}; assert not ({'GloriosoLiu.Axioms','GloriosoLiu.SecondOrderProjection','SKDoubling','EntropicGravityDarkEnergy'} & m), 'stale registrations still present'"`
  *What it asserts:* asserts L3's registration ledger no longer claims modules the manuscript does not use. Exits 1 at HEAD.
- **Gate:** none
- **Location:** `papers/L3/append_log.json`; `scripts/validation/checks/` (`bundle_lean_module_coverage`)
- **Observed:** L3 registers seven Lean modules. Four (`GloriosoLiu.Axioms`, `GloriosoLiu.SecondOrderProjection`, `SKDoubling`, `EntropicGravityDarkEnergy`) are reached by neither name nor theorem, and were registered by lift events whose own notes say "cross-ref upgrade only; no prose change". The check is at its frozen ceiling of 149 corpus-wide, so any redraft that legitimately stops depending on a module raises the count and fails the corpus.
- **Evidence:** The check reports L3 at 4 unreached both before and after this redraft; it stayed at 4 only because the redraft deliberately retained theorem citations for the two modules it would otherwise have dropped.
- **Expected:** A drafting pass that removes a dependency should be able to record that, rather than being pushed to name a module in a Letter to keep a ratchet green. The check's own guidance already says "repay by removing the registration, not by writing prose about them" for unbuilt modules; the same remedy needs to be available for built ones.
- **Fix:** Add a `--deregister` path to `bundle_append.py` that records a removal event with a rationale, and let the check read the net registration set. Route through the `architecture-change` skill.
- **Cache:** N/A.

### 5.3 — 🔵 RECOMMENDED — `CITATION_REGISTRY.used_in` does not list L3 for the references L3 cites

- **Severity:** recommended
- **Lane:** `infra`
- **Verify:** `cd "$REPO" && uv run python -c "from src.core.citations import CITATION_REGISTRY as C; import re; keys=set(re.findall(r'\\\\bibitem\{([^}]+)\}', open('papers/L3/paper_draft.tex').read())); bad=[k for k in keys if 'papers/L3/' not in ' '.join(C.get(k,{}).get('used_in') or [])]; assert not bad, bad"`
  *What it asserts:* asserts every bibkey L3 ships is recorded as used by L3. Exits 1 at HEAD, naming the keys.
- **Gate:** CitationIntegrity
- **Location:** `src/core/citations.py`
- **Observed:** Only `Roehm2026D3` lists `papers/L3/paper_draft.tex`. The other seventeen keys the draft ships list paper 27, D3 or F instead. Four keys are newly cited by this redraft (`Sakharov1968`, `Adler1982`, `Visser2002`, `FinazziLiberatiSindoni2012PRL`); the rest predate it.
- **Evidence:** Registry read at HEAD. No check enforces `used_in`, so the field silently decays; `theorem_name_embedded_citations` reads it and would mis-resolve if it ever mattered for these keys.
- **Expected:** `used_in` names every draft that ships the bibitem.
- **Fix:** Append `papers/L3/paper_draft.tex` to the eighteen entries. Deliberately not done in this pass: several agents are editing this repository concurrently and an eighteen-site edit to a canonical registry is the wrong thing to land beside a manuscript rewrite.
- **Cache:** N/A.

### 5.4 — 🔵 RECOMMENDED — The charter still calls this bundle "BCH four laws by regime" and the manuscript no longer argues that

- **Severity:** recommended
- **Lane:** `prose`
- **Verify:** `cd "$REPO" && uv run python -c "import json; t=json.load(open('papers/L3/bundle_metadata.json'))['title']; assert 'four laws' not in t.lower(), t"`
  *What it asserts:* asserts the charter title names the result the manuscript delivers. Exits 1 at HEAD.
- **Gate:** NarrativeGrounding
- **Location:** `papers/L3/bundle_metadata.json` (`title`); `docs/PAPER_STRATEGY.md` §2.3 ("Paper L3")
- **Observed:** The charter titles the work "BCH four laws by regime" and its lede promises the four laws partitioned by regime. Two of the four are in the development, one of those only as a coupling positivity, and the manuscript says so. The delivered result is the partition itself: a mass threshold that decides the sign of $d\Thawk/dt$, with the four-laws status reported as scope.
- **Evidence:** Findings 2.1 and 2.2 above; the redrafted manuscript's title, abstract and section headings all name the partition.
- **Expected:** The charter and the manuscript agree on what the paper claims. The charter is the operator's document, so the direction of the fix is a portfolio call rather than a drafting one.
- **Fix:** Either retitle the charter entry to the partition result, or record that the manuscript has superseded the charter's framing and why. Note the knock-on: `docs/PAPER_STRATEGY.md` §3 makes D3 "the deep companion that absorbs L3's content", so whichever way this resolves, D3's scope statement moves with it.
- **Cache:** N/A.

---

## Not filed, and why

- **`ADWSecondLaw`'s `horizon_area_link_consistent : 0 < b.A`** duplicates `BHData.A_pos`. The module's docstring says so and calls it a consistency witness for use sites. That is a defensible design and not a finding.
- **The `ADWExtremality` constructor name** for the cooling branch is bibliographic continuity, disclosed in the module. Renaming it would break citations for no gain; only the docstrings need the correction in 1.2.
- **`Israel1986`, `Hawking1975CMP` and `Adler1982` are held as Crossref DOI records, not full text.** The redraft was written around this: it attributes the strong form of the third law through Kehle--Unger, who state the conjecture and whose full text is held, and it uses Hawking and Adler only for results that are canonical-reference material. This is adjacent to the open infra finding at `papers/AutomatedReviews/2026-08-15-textbook-exemption-rewards-absent-metadata/`, and is not re-filed here.
