---
paper: D12
bundle_target: D12
reviewer: adversarial-reviewer
model: claude-opus-5
review_date: 2026-08-01T02:02:00Z
readiness_gates_version: 1
head_commit: 0a8e4832
round: 14
kind: adjudication
---

# Adversarial Review — D12 (round 14, adjudication round)

## Summary

I adjudicated all 52 open blocking findings at `0a8e4832`: **13 REPAIRED, 9 PARTIALLY
REPAIRED, 28 NOT REPAIRED, 2 WITHDRAWN.** Of the three author-filed closures I was asked
to audit, **one is rejected and two are confirmed**: `2220:2.2` claims "all 27" over a
population of **28** — `snr_chain_window_ceiling` is a D12-region formula whose Lean
theorems the paper cites at `:335-337`, and it is not in the list. That is the third
generation of the same sentence (4 → 23 → 27), and the answer to your own question is yes,
you repeated yourself. A second, unflagged instance sits four lines below it in the same
registry entry. `2220:8.5` and `2220:8.6` both hold and I reproduced each against the
commit. D12 is not submission-ready.

## Environment

`uv run python scripts/validate.py` → **59/59 checks passed, 1114 warnings**, exit 0,
archived to `docs/validation/reports/validation_20260801T014843Z.json`. Stage 13's
precondition is met.

`papers/D12/paper_draft.tex` is byte-identical to what rounds 12 and 13 reviewed
(`md5 fab8068f34953371d63161317c24dc18`; last touched at `8b9e2376`, three commits back).
I re-read and re-derived every paper-side item rather than inheriting a prior reading, but
I ran **no LaTeX compile**: no LaTeX input changed since the last compile-verified state,
so a compile would measure nothing. I ran no `pdflatex`, inside `papers/` or anywhere.

Working tree on arrival: 134 dirty tracked paths (132 `figures/*.png`,
`lean/lean_deps.json.hash`, and one more figure). None is a D12 artifact and I touched
none of them. Files I wrote are in the compliance note.

⚠️ The suite goes RED **because of this document**, and that is the guard working, not a
regression. After I wrote this file, `recurrence_reopens_closures` fails with
`1 contradicted by a recurrence`:
`review:2026-07-31-2220-internal-adversarial:D12:2.2` is closed, and finding 14.1 below
raises the same finding at a later date in the same bundle. That is a **true positive** —
it is mechanical corroboration of AUDIT 1's rejection, arrived at by a guard I did not
consult while writing the finding. Nothing else in the suite changed: the other 58 checks
are as measured above, and `review_severity_declared` (4 documents in scope) and
`review_docs_mint_findings` (134 documents) both pass on this file. Extracting this
document mints exactly **6** findings at exactly the declared severities
(`14.1 critical`, `14.2 major`, `14.3 major`, `14.4 major`, `14.5 minor`, `14.6 minor`),
all `open`.

## Part A0 — The three author-filed closures you asked me to audit

**AUDIT 1 — `2220:D12:2.2` — REJECTED. The population is 28, not 27.**
The record's reproducible check runs and returns what it says: Gate 4 reads
`passed — all 27 grounded formulas have substantive tests`, over
`27 formulas grounding paper claims (27 with golden/identity/roundtrip, 0 bounds-only,
0 no tests)`, and I confirmed the four newly-added formulas each carry non-bounds coverage
(`gaussian_q` → `test_gaussian_q_known_values` golden; `gaussian_threshold_error_floor` →
`test_threshold_floor_is_midpoint_q` golden; `folklore_miss_floor` →
`test_direction_A_certified_148_fold` golden; `folklore_gap_exponent` →
`test_folklore_gap_split_identity` golden). **The check is not the claim.** I enumerated the
D12 region of `src/core/formulas.py` — the block between the `D12 —` banner at `:11127` and
the `D11 —` banner at `:11626` — and it contains **28** public top-level `def`s, not 27.
The omitted one is `snr_chain_window_ceiling` (`formulas.py:11357`). It is not omitted for
cause: its graph node is `verification: verified` with `lean_refs_resolved =
['snrChain_le_window_ceiling', 'snrChain_window_ceiling_attained', ...]`, it carries a
`golden` VERIFIES edge from `test_snr_ceiling_closed_form`, and `paper_draft.tex:335-337`
cites all three of its theorems by name. It would have passed the gate. Round 13's A21 said
"27 public functions" and then named **five** omissions from a list of 23 — an arithmetic
that only closes at 28 — and the repair took the wrong half of that sentence and shipped
four. Detail in finding 14.1.

**AUDIT 2 — `2220:D12:8.5` — CONFIRMED. Every clause of the evidence reproduces.**
I re-measured with the check's own `_norm`, same-bundle rule and date rule, driving
`build_graph._load_supersession_ledger` off historical ledger snapshots via `git show`
(in-process, restored after; no repository file written):

```
ledger snapshot   blocking closures   compared   MAX Jaccard   hits @ 0.40
03a4592e (pre-fix)        ~318            162       0.429            3
8d0a3e53 (the fix)         357            201       0.375            0
0a8e4832 (HEAD)            360            204       0.375            0
```

"previously max achievable Jaccard 0.429 < threshold 0.50 meant zero hits were possible" —
correct. "three at the fix" — correct, and I identified them: `2026-05-01-1500…:I1:5.1`
(j=0.417), `2026-06-08-2242…:paper10_modular_generation:6.1` (j=0.400), and
`2026-07-31-1530…:D11:4.1` (j=0.429); all three appear in the ledger diff as `status: open`
records, so they were reopened, not quietly dropped. "a fourth rejected at filing" —
correct: planting `1524:D12:8.2` as `fixed` fires the guard at j=0.400 against
`1823:D12:8.4`, and the record is filed `open`. The guard is live, not vacuous: planting
`1823:D12:7.1` and `2022:D12:8.4` fires at j=1.000 (controls). On my own independently
built labelled set of **29** consecutive-round pairs drawn from twelve self-declared D12
recurrence chains, detection is **21/29 at 0.50 → 22/29 at 0.40**; the pair the change buys
is precisely round-13 13.3's table row 1 (`1823:8.3` ImportError chain, j=0.400 exactly),
which I re-ran and which now FIRES. **Do not read HEAD's `MAX 0.375` as incapacity** — it is
a property of which closures happen to be filed right now, and I verified the guard fires on
planted candidates at 0.40. One caveat carried to finding 14.4: the first live effect of
0.40 on D12 is a **false positive**.

**AUDIT 3 — `2220:D12:8.6` — CONFIRMED, with a stale literal.**
The semantic defect is genuinely fixed. `validate.py:3976-3984` now increments `compared`
inside the candidate loop, gated on a `_had_candidate` set, so it counts closures with
something to compare against rather than closures that reached the loop. I recomputed the
quantity independently — blocking-severity closures with at least one same-bundle,
later-dated open finding — and got **204**, exactly what the check prints, and my recount
matches at all three ledger snapshots above. The record's `REPRODUCE: it reports 162` was
true of the **`03a4592e`** graph (round 13's measurement) and is **not** true at the commit
the record cites: at `8d0a3e53` it reports **201**, at HEAD **204**. Finding 14.3.

**Your question, answered directly.** Yes, one other count claim shipped this session is a
curated subset described as a population, and it is four lines below the one you asked
about: `PAPER_DEPENDENCIES['D12']['lean_modules']` names **3** modules while D12's own
formula docstrings cite **9** and its prose cites 2 more. Finding 14.2. I checked the other
completeness claims in the same blast radius and they are **sound**: the paper's "Twelve
propositions carry modelling or admissibility content" at `:650` is 12 of 12 — I swept every
`Prop`-valued `structure`/`def`/`abbrev` across `lean/SKEFTHawking/{Detection,Electrothermal,Control}/`
and got exactly the twelve the paragraph names, no more; `bundle_metadata.json`'s
`blockers_open: 52 / advisories_open: 101 / open_findings: 153` all match the live graph;
`review_docs_mint_findings`' 132 and `recurrence_reopens_closures`' 204 are both the
cardinality of the set actually iterated.

## Part B — Adjudication of the 52 open blocking findings

Verdict counts: **13 REPAIRED / 9 PARTIALLY REPAIRED / 28 NOT REPAIRED / 2 WITHDRAWN.**
Each block is written to be quoted verbatim into a ledger record.

**B1 — REPAIRED — `1730:8.1` (a self-authored review document can close its own findings).**
Heading-parse closure is gone from the artifact, not just from the commit message.
`scripts/build_graph.py` sets `status = 'open'` unconditionally with nothing after it.
Mutation, isolated `tempfile.mkdtemp()` tree with `PROJECT_ROOT` monkeypatched and an empty
ledger: a heading reading `🔴 BLOCKER — ✅ FIXED: the widget is broken` mints
`critical`/**`open`**, and `🔵 RECOMMENDED — this is now closed and resolved` mints
`advisory`/**`open`**. Both wordings that used to close a finding now close nothing.

**B2 — REPAIRED — `1730:8.2` (a broken `readiness_gates` import silently reported agreement).**
`check_readiness_verdicts_agree`'s `ImportError` handler at `validate.py:4591-4598` returns
`CheckResult(passed=False, …)`. Live output on the real graph: `17 heatmap-RED bundles
cross-checked, 0 disagreement(s)`, with `D12 — agree: heatmap RED (52 blockers), gate
blocked on FixPropagation, ParameterProvenance`. The rename route this finding demonstrated
no longer yields a green check.

**B3 — REPAIRED — `1730:8.3` (the lost round-6 document).**
`papers/AutomatedReviews/2026-07-31-1210-reconstructed-round6/D12.md` exists (9 781 bytes)
and its provenance block states the coverage gap in the terms the finding demanded: "This
file recovers 7 findings; round 6 raised 14", against commit `ad844e42`'s recorded
"5 BLOCKER / 9 REQUIRED".

**B4 — REPAIRED — `1730:8.4`, `1823:8.4`, `2022:8.4`, `2103:8.4` (`ledger_ids_resolve` fails open).**
`validate.py:3443-3453`: the handler now emits `Detail("ledger_ids_resolve", False, "ledger
integrity scan FAILED TO RUN (…) — the dangling-closure guard did not execute, so its
silence is not evidence")`. The headroom defect is closed too: I counted the live dangling
population and it is **66**, and `_LEDGER_DANGLING_BASELINE = 66`, so growth by a single
record fails on the first record. This is the finding whose repair matters most, because
after B1 the ledger is the sole channel that can close anything.

**B5 — REPAIRED — `1524:8.2` (`readiness_verdicts_agree` fails open).**
Same artifact as B2, adjudicated separately because it is filed separately. The author
filed this one `open` rather than `fixed`, on the ground that `recurrence_reopens_closures`
fires on it. I reproduced that firing and it is a **false positive** — see finding 14.4.
The underlying defect is repaired; the finding is being held open by a matcher collision,
not by a live defect.

**B6 — REPAIRED — `2022:5.1` (the ENBW functional disambiguation).**
Re-derived rather than inherited. `paper_draft.tex:274` states the constant as
`ENBW·T ≥ 1/2`, matching `FilterFloors.lean:197-201`; `:302` writes the lock-in convention
as `1/(4τ)` with "time constant τ", so the symbol collision with the window `T` is gone;
`:303-310` carries the functional disambiguation. With `enbw h T := (∫₀^T h²)/(2(∫₀^T h)²)`
a single-pole `h = e^{-x/τ}` gives `enbw·T = (u/4)·coth(u/2)`, which is `1/2` in the limit
`u → 0` and increases monotonically — so "≥ ½ for every u>0" and "never approaches 1/4" are
both right. This finding is repaired and carries **no ledger record**: the record that
verified it was re-keyed to `1730:5.1` by 13.1's remediation, correctly, and this ID was
left behind. File it against B6.

**B7 — REPAIRED — `0001:13.1` (three ledger records closing a finding their evidence did not describe).**
The Expected is met exactly. Three re-keyed records now exist at `1730:D12:5.1`,
`1730:D12:5.2` and `1730:D12:2.1`, each carrying an explicit `⚠️ RE-KEYED 2026-08-01`
annotation naming the finding it was wrongly filed against and quoting round 13's
adjudication; and `2022:D12:5.1`, `2022:D12:5.2`, `2022:D12:2.1` are all `open` at HEAD.
The proposed guard leg was **not** shipped, and the non-shipping is documented and measured
at `validate.py:3848-3865`: the author built the evidence-to-label overlap check, measured
that it flags 40 records, sampled them, found the flags wrong, and recorded why the premise
fails ("well-written evidence routinely shares no vocabulary with the finding it closes"),
plus what would work instead. That is the correct disposition of a proposed fix that
measurement refutes, and I am not re-filing it.

**B8 — PARTIALLY REPAIRED — `2022:2.2`, `2103:2.2` (Gate 4's formula list).**
The list went 23 → 27 and all 27 carry golden/identity coverage — real progress, and the
four added are the right four in the sense that they back §2's headline numerals. It is
still not the population: 28 public functions live in the D12 region and
`snr_chain_window_ceiling` is outside the gate. `paper:D12` also still has **zero**
`GROUNDED_IN` edges, so the gate's scope remains a hand-written list authored by the party
it audits, and Gate 4's WARN still names two unresolved groundings
(`f_link_bath_referred`, `phonon_psd_gradient_factor`).

**B9 — PARTIALLY REPAIRED — `1823:8.5`, `2022:8.5`, `2103:8.5` (blocking-severity `accepted` findings invisible to Gate 11).**
Half repaired since the finding was written: `_eval_fix_propagation` now counts and names
the accepted population in its evidence line — D3 reads "29 findings flag this paper
(15 fixed, 3 accepted, 11 open, 3 of them submission-blocking)". What is still invisible is
the accepted population's **severity**: `GateResult` has no `accepted_blockers` field
(`hasattr(r, 'accepted_blockers') == False` on every bundle), and "0 of them
submission-blocking" refers to the open set only. Measured project-wide at HEAD: **27**
findings are `accepted` at critical/major severity (paper17 ×6, paper20 ×5, paper37 ×3,
paper21 ×2, one each in eight more, L1, D4, D3, E2, one unattributed). E2's gate reads
"2 accepted … 0 of them submission-blocking" while one of those two is a major. D12 itself
carries 0 accepted, so this remains a system exposure rather than a D12-local one.

**B10 — PARTIALLY REPAIRED — `2103:8.7`, `2220:8.7` (the ledger backlog).**
This moved further than any other item this session. D12 went from 76 blocking findings at
`03a4592e` to **52** at HEAD, on 44 newly-filed `fixed` records — and the three most
recent are explicitly self-labelled `author-filed-reproducible-evidence` with "round 14
should confirm or reject", which is the right way to file an unadjudicated closure. Not
closed: 52 blocking findings still flag D12, and one of those three author-filed records
is wrong (14.1).

**B11 — PARTIALLY REPAIRED — `0001:13.3` (the recurrence threshold's calibration).**
0.50 → 0.40 converts exactly one of the three evaders 13.3 tabulated. Re-run at HEAD by
planting each closure against a live ledger copy, in-process, loader restored:
```
planted closure                                   best J   guard @ 0.40
1823:8.3  ImportError chain (2022:8.3 open)        0.400   FIRES   <- newly caught
1823:2.1  MATHER chain      (2022:2.1 open)        0.333   EVADES
2022:8.5  accepted chain    (2103:8.5 open)        0.111   EVADES
2022:2.2  Gate-4 list chain (2103:2.2 open)        0.300   EVADES
1823:7.1  numeric surface   (2022:7.1 open)        1.000   FIRES   <- control
2022:8.4  ledger_ids        (2103:8.4 open)        1.000   FIRES   <- control
```
Three chains you are likely to file records against still evade, and they are three I
adjudicate NOT REPAIRED below (B24, B15, B9). The `pytest` fixture of labelled pairs 13.3
asked for does not exist — no test file references `recurrence_reopens_closures` — so the
next change to the matcher will again be measured against nothing.

**B12 — PARTIALLY REPAIRED — `1823:8.1`, `2022:8.1`, `2103:8.1`, `2220:8.2` (the blocking-closure bypass chain).**
The bar itself works and I confirmed it: a `🔵` heading declaring `blocker` mints `critical`
and a two-key `{"finding_id": X, "status": "fixed"}` record is rejected, with `status`
forced back to `open`. The scope defects survive intact — see 14.x cross-reference to
round-13 13.2 in B26.

**B13 — WITHDRAWN — `1730:8.5` and `1823:8.6` (`freshness_stale` with `blockers_open > 0`).**
Round 10 withdrew the premise: the field is an mtime signal, not a readiness verdict.
`docs/LATE_PHASE6_ABSORPTION_PROTOCOL.md:184` states the definition and dates the
correction; `papers/D12/bundle_metadata.json` carries `stage13_redo_required: true` beside
`freshness_stale: false`, which is the documented-correct combination. File as `accepted`
with a withdrawal rationale, not `fixed`.

**C-group — NOT REPAIRED. Each verified at `0a8e4832` from the artifact.**

**B14 — `2022:1.1`, `2103:1.1`, `2220:1.1` — `IrwinHilton2005` unqualified for the loop-gain convention.**
`paper_draft.tex:372-373`: "proves it equals the instrument-literature
convention~\cite{IrwinHilton2005,Irwin1995}", no qualifier — while `:443-446` says of the
same source "we have not inspected its text: we hold that source only as a resolved DOI
record, so the attribution of this specific closed form to it should be read as
provisional." Eighth consecutive round. Cache-verified at the recomputed current bibitem
hash, `verdict match`, `is_stale False`.

**B15 — `1730:2.2`, `1823:2.1`, `2022:2.1`, `2103:2.1`, `2220:2.1` — `MATHER_1982_GRADIENT_REDUCTION` is not human-verified.**
Read from `src/core/provenance.py` at HEAD: `value 0.3`, `tier LITERATURE`,
`llm_verified_date '2026-07-31'`, `human_verified_date: None`, `human_verified_notes: None`.
Gate 3 reads `blocked — 1 parameters lack human_verified_date`, which is the correct verdict
for an outstanding operator action. The registration and `DEPENDS_ON` halves are repaired;
the remaining half is one dashboard sign-off and must not be discharged by a reviewer-set
date.

**B16 — `2220:1.2` — `RashidHasan2017` cited for a completed formalization.**
`:625-626` still reads "Transform methods and transfer-function algebra **are formalized**
in HOL Light~\cite{RashidHasan2017}". Cache-verified rather than re-fetched: the record set
now carries five independent fetches of `arxiv.org/abs/1705.10050` across rounds 8–13, all
`verdict: match` on title and authors, and the round-12 and round-13 records both quote the
abstract's "we present **an ongoing project**" and "a roadmap … current status and future
goals". A project-status report does not establish that transfer-function algebra *is*
formalized. Not a wrong-target citation; a wrong tense.

**B17 — `2022:1.3`, `2103:1.3`, `2220:1.3` — `arxiv_verified` absent.**
`docs/READINESS_GATES.md:33` states the Gate-1 criterion as "Every registry entry has
`arxiv_verified == True` AND `doi_verified == True` (where applicable)". I dumped every D12
bibitem's registry entry: on all **six** arXiv-bearing keys (`MeiburgLessaSoldati2025`,
`coqq2022`, `UnruhCBO2025`, `EchenimMhalla2021`, `RashidHasan2017`, `Degenne2025Kernels`)
the `arxiv_verified` key is **absent**, not `False` — so the documented criterion cannot be
met while `_eval_citation_integrity` passes on the weaker "all bibkeys registered". All 14
bibitems do resolve, all 14 are registered, and all 14 hit
`docs/citation_verifications.jsonl` at their recomputed current hash with `verdict: match`
and `is_stale: False`; I recomputed every hash from the live `.tex` this round.

**B18 — `2022:4.1`, `2103:4.1`, `2220:4.1` — reversed author order in D10.**
`papers/D10/bibliography.bib:49` reads `author = {Caballero, Jose Manuel Rodriguez and
Unruh, Dominique}`. Cache-verified: three independent fetches of `arxiv.org/abs/2512.05878`
(rounds 9, 10, 13) all record the canonical order as 1. Dominique Unruh, 2. José Manuel
Rodríguez Caballero. `papers/D12/paper_draft.tex:766` is right; D10 is wrong at source, so
re-rendering a `.bbl` will not fix it. Gate 2 still does not fire because the two papers use
different keys (`isabelleCBO` vs `UnruhCBO2025`) and the D10 registry entry carries
`arxiv: None`.

**B19 — `2022:5.2`, `2103:5.2`, `2220:5.1` — the highest-prior-art-risk paragraph withholds the name and the cite.**
`:641-643` verbatim: "First, an out-of-Mathlib Lean project on information theory and
hypothesis testing is under active development; a Bayes-binary-risk--divergence lower bound
there would be substantially our Le Cam floor, and it is the highest prior-art risk in this
work." No name, no `\cite` — while `:612-613` names and cites `\textsc{TestingLowerBounds}`
two paragraphs earlier and `:716-717` names it again. The characterization "on information
theory and hypothesis testing" is the one `prior_art_novelty.md §3` records as retracted;
the retraction reached the registry title, the bibitem and the cache, and not the prose that
seeded them.

**B20 — `1730:7.1`, `1823:7.1`, `2022:7.1`, `2103:7.1`, `2220:7.1` — D12's numeric surface is outside every freshness gate.**
Gate 9 evidence at HEAD, verbatim: `0 REPORTS edges (0 stale); 0 inline numerical literals
in body; 0 stale tables/*.tex files` — and it evaluates `passed` on that empty set. I
recomputed the derived numerals myself at 30 dps this round and every one I checked is
correct: `e^5 = 148.4132` (paper: 148.41); the (50,60) Le Cam/folklore ratio 3491.9554
(paper: 3491.96); the direction-B crossover at `N_b = 50`, `N_a = 51.3959` (paper: 51.40);
`γ(2,4) = 0.4731481`, PSD reduction 52.6852 %, amplitude reduction 31.2142 %, difference
21.4710 (paper: 0.4731 / 53 % / 31 % / 21.5 = 52.7 − 31.2, rounded 22); `Q(1) = 0.1586553`;
`2Q(1)/(1−2Q(1)) = 0.4647948`; Birnbaum/Q = 0.7626 at z=1 and 0.9493 at z=2 (paper: 76 %,
95 %); `z²/(1+z²)` = 0.5 and 0.8 (paper: 50 %, 80 %); Mills vacuous below z = 0.37224
(paper: "z ≲ 0.37"); max amplitude reduction `1−√(4/9) = 33.33 %` and max PSD reduction
`1−4/9 = 55.56 %` (paper: 33 %, 56 %). **One is off** — see finding 14.5 — which is the
fourth round running in which a reviewer, not a gate, is the only thing between this surface
and a wrong number.

**B21 — `1823:8.3`, `2022:8.3`, `2103:8.3` — "not evaluated" is still spelled "passed".**
AST sweep of `scripts/validate.py` for `CheckResult(passed=True)` inside an `except`
handler: **12 hits**, at lines `623, 1956, 1959, 1971, 2112, 2381, 4370, 4721, 5672, 7298,
7306, 7582`. Line 4721 is inside `check_readiness_submission_gate` (which begins at 4704) —
the exact site all three findings name, and it is its `ImportError` handler returning
`passed=True` with `warning=True`. Ninth consecutive round; the line numbers have shifted
with the file and the count has not moved from 12.

**B22 — `2220:8.4` — `review_severity_declared`'s predicate is unsound.**
The code at `validate.py:4027-4063` is unchanged. All four defects stand: the predicate is
the document-level count comparison `n_sev < n_head`, so declarations are fungible across
findings and one may come from outside any finding; `_HEADING = r'^#{3,5}\s+\S'` counts
every h3–h5 heading, so a fully compliant report with an `### Environment` section is a red
build; the date comes from `md.parent.name[:10]`, a directory name the author chooses, with
nothing cross-checking the front-matter `review_date`; and `_SEV_LINE` matches the prefix
only, so a declaration whose value is `yes` passes here and maps to `None` in `build_graph`
(demonstrated in B26 row 4). The check now has documents in scope — live output
`2 review document(s) dated >= 2026-08-01 checked; 0 with findings that do not declare
severity` — so its core function works; the predicate around it does not. I had to write
this document around the second defect, using `##` for every non-finding section, which is
itself the finding.

**B23 — `1524:8.1`(b), carried as part of `2103:8.7` — ledger records for the round-1 (`0829`) findings.**
Measured: 19 D12 findings from the `0829` document, 11 now carrying a ledger record.
**Zero** blocking-severity `0829` findings remain without one, so the blocking half of this
is discharged; the advisory half is not.

**B24 — `1823:8.2`(b) — `stage13_review_doc`.**
`papers/D12/bundle_metadata.json:19` still reads `null` beside
`"last_stage13_review": "2026-07-31"`, while `BUNDLE_DIRECTORY_SCHEMA.md:53` mandates the
field. No code reads it, which is why nothing catches it.

**B25 — `0001:13.2` — the declared-severity downgrade lever into the two-key ledger bypass.**
All three remediations are unshipped and I reproduced the exploit table at HEAD rather than
inheriting it. Mutation in a `tempfile.mkdtemp()` tree with `build_graph.PROJECT_ROOT`
monkeypatched and a ledger containing nothing but a two-key record
`{"finding_id": X, "status": "fixed"}` — no evidence, no commit, no date:
```
case                                                    minted     status   bar
red heading, declares "recommended"                     minor      fixed    NOT applied
blue heading, declares "blocker"                        critical   open     applied  <- control
blue heading, declares "blocker" past body[:1200]       advisory   fixed    NOT applied
blue heading, declares "yes"                            advisory   fixed    NOT applied
```
The bar is still gated on `severity in ('critical','major','blocker')` at
`build_graph.py:1796-1797`; `_decl` still searches `body[:1200]` at `:1645`; and
`_MAP.get(_v)` still returns `None` on an unrecognised value and falls through to glyph
inference with no warning and no failure. The control row proves the bar works, so this
remains a scope defect, not a broken bar.

**B26 — the remaining C-group members carry forward unchanged**: `2022:8.1`, `2103:8.1`
and `2220:8.2` are the same bypass chain as B12/B25 and are PARTIALLY REPAIRED on the same
evidence; `1823:8.1` likewise.

## Part C — New findings

### 14.1 — 🔴 BLOCKER — Gate 4's "completed" list is 27 of 28, the third generation of the same sentence, and it closed a finding on that basis

- **Severity:** blocker
- **Gate:** ComputationCorrectness
- **Location:** `src/core/provenance.py:3737-3761` (`PAPER_DEPENDENCIES['D12']['formulas']`); `docs/review_finding_supersessions.json` (record for `review:2026-07-31-2220-internal-adversarial:D12:2.2`)
- **Observed:** The record closes `2220:2.2` on "Gate 4's formula list completed 23 → 27 in
  `PAPER_DEPENDENCIES['D12']`. REPRODUCE: the gate now reports 'all 27 grounded formulas
  have substantive tests'." The gate does report that. The D12 region of
  `src/core/formulas.py` contains **28** public top-level functions, not 27.
- **Evidence:** Enumerated between the two section banners — `# D12 — Detector & readout
  metrology` at `formulas.py:11127` and `# D11 — Topological band theory` at `:11626` —
  28 non-underscore top-level `def`s. The list holds 27. The omitted function is
  `snr_chain_window_ceiling` at `formulas.py:11357`. It is not omitted for cause:
  ```
  formula:snr_chain_window_ceiling | verification = verified
      lean_refs_resolved = ['snrChain_le_window_ceiling', 'snrChain_window_ceiling_attained', ...]
      VERIFIES = 1, kinds = ['golden']  <- test_bundle_formulas_d11_d12::test_snr_ceiling_closed_form
  ```
  and `papers/D12/paper_draft.tex:335-337` cites all three of its theorems by name: "The
  end-to-end ceiling is \thm{snrChain\_le\_window\_ceiling}, attained by the matched boxcar
  (\thm{snrChain\_window\_ceiling\_attained}) and independent of responsivity
  (\thm{snrChain\_independent\_of\_responsivity})." Including it would have left the gate
  `passed` at 28. The in-code comment introducing the four additions states "The list was 23
  of 27" and the commit subject at HEAD reads "Gate 4 was 23 of 27" — both inherit round
  13's A21, whose own text named **five** omissions against a list of 23, an arithmetic that
  closes only at 28. The lineage is: "all 4 grounded formulas" (wrong), "the full 23-formula
  domain surface" (wrong), "all 27" (wrong). Each fix moved the number and preserved the
  sentence shape.
- **Expected:** `PAPER_DEPENDENCIES['D12']['formulas']` should be the 28-element population,
  and the record for `2220:2.2` should be reopened until it is. A closure whose reproducible
  check confirms the artifact but not the claim is the failure mode this round exists to
  catch.
- **Fix:** Add `'snr_chain_window_ceiling'`. Then replace the hand-curated list with a
  derived one, or add a `pytest` case asserting that the D12 slice of `formulas.py` and
  `PAPER_DEPENDENCIES['D12']['formulas']` are the same set — that test is four lines and
  ends this lineage permanently, where a fourth manual recount does not.
- **Cache:** n/a

### 14.2 — 🟡 REQUIRED — the same registry entry lists 3 Lean modules where D12's own formulas cite 9 and its prose cites 2 more, and no round has flagged it

- **Severity:** required
- **Gate:** ComputationCorrectness
- **Location:** `src/core/provenance.py:3762-3763`
- **Observed:** `PAPER_DEPENDENCIES['D12']['lean_modules'] = ['Electrothermal.BolometricFloors',
  'Detection.FilterFloors', 'Control.CompositeReadoutCeilings']`. This is the same
  curated-subset-as-population shape as 14.1, four lines below it in the same dict, written
  this session (`git blame` → `686a2a4e`, 2026-07-31 14:13) and never examined by rounds
  8–13.
- **Evidence:** Parsing the `Lean:` lines of every docstring in the D12 region of
  `formulas.py` yields nine distinct modules: `Control.CompositeReadoutCeilings`,
  `Control.RotatingWave`, `Detection.FilterFloors`, `Detection.GaussianThreshold`,
  `Detection.MatchedFilter`, `Detection.NEPAlgebra`, `Detection.PoissonDiscrimination`,
  `Electrothermal.BolometricFloors`, `Electrothermal.ETFModel`. Six are missing from the
  field. Two more are reached by the paper's own disclosed-hypotheses paragraph:
  `Detection.ShotNoise` (home of `IsShotFilteredMoments`, `ShotNoise.lean:629`) and
  `Electrothermal.ETFResponsivity` (home of `SolvesDrivenBalance` `:163` and
  `SolvesSinusoidalBalance` `:561`) — both named at `paper_draft.tex:654-658`. The field is
  read by `scripts/aristotle_usage_by_bundle.py:208`, `scripts/provenance_dashboard.py:558`
  and `:740`, and lands on the `paper:D12` node at `build_graph.py:839`, so every consumer
  of "which Lean does D12 rest on" currently sees a third of it.
- **Expected:** Eleven modules, or a derivation from the formula list rather than a second
  hand-curated list beside it.
- **Fix:** One list, or one derivation. The same `pytest` fixture proposed in 14.1 covers
  both fields.
- **Cache:** n/a

### 14.3 — 🟡 REQUIRED — the `2220:8.6` closure's REPRODUCE constant is not what the check prints at the commit the record cites

- **Severity:** required
- **Gate:** FixPropagation
- **Location:** `docs/review_finding_supersessions.json` (record for `review:2026-07-31-2220-internal-adversarial:D12:8.6`)
- **Observed:** The record reads `commit: 8d0a3e53` and "REPRODUCE: it reports 162, matching
  your independent count, where it previously said 318." The semantic repair is real and I
  confirmed it independently. The number is not reproducible at that commit.
- **Evidence:** Driving `build_graph._load_supersession_ledger` off `git show <rev>:` ledger
  snapshots, in-process, loader restored afterwards:
  ```
  ledger snapshot        `compared` printed / independently recounted
  03a4592e  (round 13)                      162
  8d0a3e53  (the record's own commit)       201
  0a8e4832  (HEAD)                          204
  ```
  The live check at HEAD prints "204 blocking-severity closure(s) compared against 904 open
  finding(s)"; my independent recount of blocking-severity closures with at least one
  same-bundle later-dated open finding is also 204, so the arithmetic is correct and the
  fix is genuine. 162 was the value against the graph round 13 measured, before the 44
  closures filed since. A record whose reproduction instruction returns a different number
  at its own commit trains the next reader to stop reproducing.
- **Expected:** Either quote no constant, or quote the one the cited commit produces, or say
  which graph state the constant belongs to. This is the same lesson as 13.1 applied to a
  number instead of an ID: an assertion about an artifact must be checkable against that
  artifact.
- **Fix:** One string.
- **Cache:** n/a

### 14.4 — 🟡 REQUIRED — the first live effect of the 0.40 recurrence threshold on D12 is a false positive, and it is holding a repaired finding open

- **Severity:** required
- **Gate:** FixPropagation
- **Location:** `scripts/validate.py::check_recurrence_reopens_closures` (`_MIN_OVERLAP = 0.40`)
- **Observed:** `1524:D12:8.2` is filed `open` rather than `fixed`, with the rationale
  "reopened by `recurrence_reopens_closures` in the same pass that filed it. A later
  same-bundle review raises this finding and it is open there." I reproduced the firing —
  it is real — and the two findings are not the same defect.
- **Evidence:** Planting `1524:D12:8.2` as a conforming closure against a live ledger copy
  (in-process, loader restored, no repository file written) fires the guard at exactly
  j=0.400 against `1823:D12:8.4`. The two labels:
  ```
  closure:   '8.2 🟡 REQUIRED — `readiness_verdicts_agree` fails open'
  "recurrence": '8.4 🟡 REQUIRED — `ledger_ids_resolve` still fails open'
  ```
  Normalised token sets `{readiness_verdicts_agree, fails, open}` and
  `{ledger_ids_resolve, still, fails, open}` share `{fails, open}` over a union of 5 —
  j = 2/5 = 0.400, at the threshold to the digit. These are two different guards in two
  different functions; `readiness_verdicts_agree`'s fail-open is repaired (B5/B2) and
  `ledger_ids_resolve`'s is also repaired (B4). The match is carried entirely by the
  English phrase "fails open", which this corpus uses as a term of art across every guard it
  audits — so the shared vocabulary that makes Jaccard work on true recurrences is the same
  vocabulary that manufactures this one. The threshold buys one true detection (B11) and
  costs this false one, on the same 29-pair labelled set.
- **Expected:** Fire on same-*subject* recurrence, not same-vocabulary. The cheapest
  discriminator available is the section number: a true recurrence in this corpus overwhelmingly
  reappears at the same or adjacent section number in a later round (all twelve chains I
  labelled do), and `_norm` deliberately strips it. Re-admitting it as a tie-breaker at
  threshold — require either j ≥ 0.5, or j ≥ 0.4 with matching section number — keeps
  `1823:8.3` (8.3→8.3) and drops this pair (8.2→8.4).
- **Fix:** One conjunct, plus the `pytest` fixture 13.3 asked for so the precision cost is
  measured next time instead of discovered by a false hold.
- **Cache:** n/a

### 14.5 — 🔵 RECOMMENDED — "$30\,\%$ exactly at $r = 1.19$" is 29.87 %, in the paragraph whose subject is that a rounded number cannot settle the question

- **Severity:** recommended
- **Gate:** NumericalFreshness
- **Location:** `papers/D12/paper_draft.tex:459`
- **Observed:** "the closed form attains \emph{every} PSD reduction between $0$ and
  $56\,\%$ --- $30\,\%$ exactly at $r = 1.19$ --- so no argument from the available range can
  settle the convention either way."
- **Evidence:** With `γ(r,n) = [n/(2n+1)]·(r^{2n+1}−1)/(r^n−1)·r^{−(n+1)}` read from
  `formulas.py:11518`, at 25 dps: `γ(1.19, 4) = 0.70127346`, i.e. a PSD reduction of
  **29.8727 %**, not 30 %. Solving `γ(r,4) = 0.70` gives `r = 1.1913538`. The claim is true
  in substance — 30 % is attained, near r = 1.19 — but the word "exactly" is attached to the
  30, and a reader who evaluates the closed form at the r the paper supplies does not get
  30. This paragraph is where the paper corrects two predecessors for exactly this kind of
  slip, and where it argues that a rounded figure cannot settle a convention.
- **Expected:** "$30\,\%$ at $r \approx 1.19$", or "$29.9\,\%$ at $r = 1.19$", or
  "$30\,\%$ at $r = 1.1914$". Any of the three is checkable; the current sentence is not.
- **Fix:** One word or two digits. Note this numeral is inside the surface B20 shows is
  gated by nothing — Gate 9 reports `0 inline numerical literals in body` for a draft
  carrying about thirty derived ones.
- **Cache:** n/a

### 14.6 — 🔵 RECOMMENDED — `citation_cache.stats()` still raises on the cache it summarises, fifth round running

- **Severity:** recommended
- **Gate:** CitationIntegrity
- **Location:** `scripts/citation_cache.py::stats`
- **Observed:** `stats()` → `TypeError: can't subtract offset-naive and offset-aware
  datetimes`, on the live `docs/citation_verifications.jsonl`, including on records written
  today.
- **Evidence:** `is_stale` catches `(KeyError, ValueError)` only, so the naive/aware
  mismatch propagates. No individual D12 bibkey is affected — I computed `is_stale` for all
  14 this round and every one returned `False` — but the aggregate entry point the CLI and
  the dashboard call is dead for every caller.
- **Expected:** Normalise to aware datetimes at parse, or widen the `except` to `TypeError`.
- **Fix:** One `except` clause.
- **Cache:** cache-verified (all 14 D12 bibitems, recomputed hashes, `verdict: match`,
  `is_stale: False`)

## Compliance note

I ran no git-mutating command — only `git log`, `git show`, `git blame`, `git status`,
`git rev-parse`. I ran no `pdflatex`, inside `papers/` or anywhere; no LaTeX input changed
since the last compile-verified state.

All mutation testing was in-process: either against `build_graph.PROJECT_ROOT` monkeypatched
to a `tempfile.mkdtemp()` tree removed at the end of the run, or against
`build_graph._load_supersession_ledger` monkeypatched to return a historical or synthetic
ledger dict, with the real loader restored in a `finally`. **No repository file was mutated
by any of it**, and I appended nothing to `docs/citation_verifications.jsonl` this round —
every D12 bibitem had a non-stale `match` record at its recomputed current hash, so the
cache protocol says skip the fetch, and I did.

Files I wrote: this report, and nothing else. The 132 `figures/*.png` and
`lean/lean_deps.json.hash` were already dirty on arrival and I did not touch them.

Two notes about this document. Every non-finding section uses `##`, not `###`, because B22
shows an `### Environment` heading would fail `review_severity_declared` despite every
finding declaring its severity — a workaround for an undocumented constraint, not a
preference. And I quoted the bolded blocking-marker literal nowhere in prose, because unlike
round 13 I have no claim resting on it; B25 row 2 is the control that shows the declared
field is authoritative in the escalating direction.

Per rule 10, I have closed nothing. Every finding above remains `open`, including the 13 I
adjudicate REPAIRED. The adjudications are written to be quoted verbatim into ledger
records; filing them is yours.

## QI Candidate

**Systemic: a completeness claim in this project is repaired by moving its number, and the
number is recomputed by the same hand that got it wrong.** Gate 4's D12 formula list has
now been "completed" three times — at 4, at 23, at 27 — and is wrong at 28. Each repair was
filed with a reproducible check that ran and returned exactly what the record said, because
each check verified *the artifact against the claim* and never *the claim against the
population*. The companion field four lines below it has been wrong at 3-of-11 for four
rounds and no round has looked at it, because no reviewer was told to.

The existing register carries "a guard's scope predicate must not share a blind spot with
the thing it guards" and "every numeric a check prints must be the cardinality of the set it
actually iterated". This is the third member and it is about *registry data* rather than
guard code: **a hand-curated list that a gate evaluates over is a claim about a population,
and it must be derived from that population or tested against it — recounting it is not a
repair, it is the next iteration of the same defect.** The mechanisation is cheap and exists
for exactly one of the two fields already: a `pytest` case asserting
`set(PAPER_DEPENDENCIES[B]['formulas']) == {public defs in the B region of formulas.py}` is
four lines, would have failed at 4, at 23 and at 27, and converts a recurring Stage-13
finding into a build failure. The same shape applies to `lean_modules` against the `Lean:`
lines of those same docstrings.

**Also carried forward, each measured this round:**

1. **Five of D12's eleven gates still pass on empty or curated evidence sets** —
   `CrossPaperConsistency` (no evidence line at all), `ComputationCorrectness` (27 of 28,
   hand-written, 0 `GROUNDED_IN` edges on `paper:D12`), `AssumptionDisclosure`
   (`0 hypotheses assumed by cited theorems` for a paper with a twelve-proposition
   hypothesis-disclosure paragraph), `NarrativeGrounding` (`5 abstract sentences; 0 flagged
   interesting; 0 SUPPORTS edges`), `NumericalFreshness` (`0 REPORTS edges` for ~30 derived
   numerals, one of which is wrong — 14.5). `GateResult` should gain an `evidence_count`,
   and a gate evaluating on an empty set should report `open`, not `passed`.
2. **"Not evaluated" is still spelled "passed", ninth round running** (B21) — the identical
   12 handlers, line numbers shifted only. Making the AST sweep a `pytest` case converts a
   recurring finding into a build failure and is a one-line test.
3. **A threshold calibrated on one exemplar is calibrated on nothing** (B11, 14.4). 0.40 was
   derived from a single true pair at 0.429; both ends of that pair are now closed, and the
   threshold's first live effect on D12 is a false positive at exactly 0.400. The labelled
   dataset the corpus gives away for free — its own self-declared recurrence chains — has
   now been built independently twice, by two rounds, and committed neither time.
