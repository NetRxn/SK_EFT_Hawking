---
paper: I1
bundle_target: I1
reviewer: adversarial-reviewer
model: claude-opus-4-7
review_date: 2026-05-01T13:45:12Z
readiness_gates_version: 1
review_mode: bundle
tier: 3
profile: software_methodology
---

# Adversarial Review — Bundle I1 (Verification methodology with worked cases)

## Summary

Stage-13 review of `papers/I1/paper_draft.tex` (1338 lines, authored
2026-05-01T15:30Z, Phase 7a sub-wave 7a.2 stub-completion). The
draft is substantively coherent and the three worked cases trace to
real Lean modules and existing Aristotle run IDs, but the bundle is
**not submission-ready**: 4 BLOCKER findings on Gate 1
(CitationIntegrity), 2 BLOCKER findings on Gate 5 / Gate 7 mixed
(narrative-vs-artifact misalignment for AXIOM_METADATA and Aristotle
run attribution), 1 BLOCKER on Gate 9 (missing `tables/table1_stages.tex`),
and 5 REQUIRED / RECOMMENDED findings on inline counts and minor line
references. Affected gates: CitationIntegrity (Gate 1),
LeanProofSubstance (Gate 5), CrossPaperConsistency (Gate 2),
NarrativeGrounding (Gate 7), NumericalFreshness (Gate 9). Verdict:
**RED — not submission-ready.** Author must remediate the BLOCKERs
and re-invoke Stage 13 before submission can be considered.

## Findings

### 1.1 — 🔴 BLOCKER — Bibkey `paper15methodology` not in CITATION_REGISTRY

- **Gate:** CitationIntegrity
- **Location:** `papers/I1/paper_draft.tex:1330`
- **Observed:** `\bibitem{paper15methodology} J.~Roehm, ``A Twelve-Stage Verification Pipeline for Computational Physics Research'' (in prep, project internal draft).` The bibkey `paper15methodology` is not registered in `src/core/citations.py` `CITATION_REGISTRY`. Confirmed by `grep -n "^    'paper15methodology'" src/core/citations.py` (no result). Per Gate 1: "A bibkey that is not in the registry" is a blocker.
- **Evidence:** `src/core/citations.py` CITATION_REGISTRY does contain a `paper15_methodology` Phase entry on line 48, and bibkeys for `compcert`, `sel4`, `physlean`, `alphaproof`, `leancipilot` (which are reused from paper15_methodology) — but no `paper15methodology` bibkey. The closest bibkey would presumably be a new in-prep entry.
- **Expected:** A registry entry of the form `'paper15methodology': { ..., 'inprep': True, 'used_in': ['papers/I1/paper_draft.tex'], ... }` or — if the cite is replaced by a self-supersession note — removal of the `\cite{}`/`\bibitem{}` pair.
- **Fix:** Either (a) add `'paper15methodology'` and `'paper44riemannian'` registry stub entries with `inprep=True`, or (b) since I1 supersedes paper15, replace `\cite{paper15methodology}` with prose ("an earlier internal draft of this pipeline (April 2026)") and drop the bibitem.
- **Cache:** fresh-fetch (no cache record exists; bibkey not registered).

### 1.2 — 🔴 BLOCKER — Bibkey `paper44riemannian` not in CITATION_REGISTRY

- **Gate:** CitationIntegrity
- **Location:** `papers/I1/paper_draft.tex:1094`, `papers/I1/paper_draft.tex:1150`, `papers/I1/paper_draft.tex:1333`
- **Observed:** Two `\cite{paper44riemannian}` calls in §12 (lines 1094, 1150) and the matching `\bibitem{paper44riemannian}` (line 1333). The bibkey `paper44riemannian` is not in CITATION_REGISTRY. The companion paper directory exists (`papers/paper44_riemannian_connection/`) but no registry entry binds the cite key to the paper.
- **Evidence:** `grep -n "^    'paper44riemannian'" src/core/citations.py` returns no results. Registry has paper44-derived bibkeys (e.g., a block at line 6878 ff. for paper44_riemannian_connection-sourced refs) but no key matching the bibitem name.
- **Expected:** Registry entry pinning `paper44riemannian` to an in-prep companion paper, with `used_in` including `papers/I1/paper_draft.tex` and `papers/paper44_riemannian_connection/paper_draft.tex`.
- **Fix:** Add an `'paper44riemannian'` stub with `inprep=True, internal_draft=True, used_in=['papers/I1/...','papers/paper44_riemannian_connection/...']`. Same fix as 1.1.
- **Cache:** fresh-fetch (no cache record exists).

### 1.3 — 🔴 BLOCKER — Dangling `\cite{}` references with no `\bibitem{}`

- **Gate:** CitationIntegrity
- **Location:** `papers/I1/paper_draft.tex:415`, `papers/I1/paper_draft.tex:416`, `papers/I1/paper_draft.tex:417`
- **Observed:** §5 (chirality-wall worked case) cites three bibkeys that have no matching `\bibitem` in the bibliography: `\cite{paper10_ewbg}` (L415), `\cite{paper09_phaseI}` (L416), `\cite{paper08_topology}` (L417). LaTeX will render these as undefined references (`[?]`). None of these bibkeys are in CITATION_REGISTRY. The paper directories `paper10_ewbg`, `paper09_phaseI`, `paper08_topology` do not exist (`ls papers/` shows `paper10_modular_generation` but no `paper10_ewbg`).
- **Evidence:** `grep -n "\\\\bibitem" papers/I1/paper_draft.tex` returns 7 bibitems (physlean, compcert, sel4, leancipilot, alphaproof, paper15methodology, paper44riemannian) — none of these match the three citations in §5. `grep -nR "paper10_ewbg\|paper09_phaseI\|paper08_topology" src/core/citations.py` returns no results.
- **Expected:** Either add bibitem entries for the three referenced papers (real or in-prep companions) with corresponding registry entries, or replace the prose with non-cite phrasing (e.g., "the Z₁₆ anomaly chain (companion paper, in prep)").
- **Fix:** This is a stub-state artifact of synthesis-driven authoring (Phase 7a 7a.2). Either bind the cites to existing source papers (e.g., `paper33_ewbg_chirality_wall`, which exists per `ls papers/`) and rename the keys, or remove the cites and rephrase. Three undefined references in a published paper is a credibility-destroying class of error.
- **Cache:** fresh-fetch (no cache record exists).

### 1.4 — 🟡 REQUIRED — Five external bibkeys lack arXiv/DOI verification

- **Gate:** CitationIntegrity
- **Location:** `papers/I1/paper_draft.tex:1319-1329`
- **Observed:** The five external bibitems `physlean`, `compcert`, `sel4`, `leancipilot`, `alphaproof` are present in CITATION_REGISTRY but each has `doi_verified=False` and lacks an `arxiv_verified` field entirely. Each entry's `notes` field reads "Auto-generated stub from \bibitem block in `papers/paper14_braided_mtc/paper_draft.tex`" or `paper15_methodology/paper_draft.tex` — i.e., these were stubbed during a prior import sweep and never rounded out with verification metadata or primary-source paths. None have `primary_source_path` populated (except `leancipilot`, which has `Lit-Search/Phase-5w/primary-sources/leancipilot.pdf`). None of the five are present in the citation cache (`docs/citation_verifications.jsonl`) — confirmed by enumerating all 33 cached bibkeys.
- **Evidence:**
  - `physlean`: registry says `doi_verified=False`, no arxiv field; bibitem says "https://github.com/HEPLean/PhysLean (2024)" — verifiable via GitHub repo existence, not DOI/arXiv.
  - `compcert`: registry says `volume=52`, `year=2009`, no DOI; bibitem says "Comm. ACM 52(7), 107--115 (2009)". Verifiable via DOI 10.1145/1538788.1538814.
  - `sel4`: registry says `journal=Proc. ACM SOSP`, `year=2009`; verifiable via DOI 10.1145/1629575.1629596.
  - `leancipilot`: registry says `arxiv` field None; bibitem says "arXiv:2404.12534 (2024)". The arXiv ID can be cache-verified.
  - `alphaproof`: registry says no DOI, no arXiv; bibitem cites a 2024 DeepMind blog post URL, with no canonical archival ID.
- **Expected:** For each bibitem, populate `doi_verified=True` (or `arxiv_verified=True`) after WebFetch confirmation and append a `citation_verifications.jsonl` record per the cache schema. `alphaproof` may stay unverified pending a primary-source archival, but should carry a `primary_source_path` to a screenshot or webarchive snapshot.
- **Fix:** Run a verification pass — fetch each via arXiv/DOI, append cache records, set `doi_verified=True` / `arxiv_verified=True` in the registry. Then update `used_in` lists to include `papers/I1/paper_draft.tex` for each of the five.
- **Cache:** fresh-fetch needed (no records exist).

### 1.5 — 🟡 REQUIRED — `used_in` field for I1 bibitems does not list the I1 bundle

- **Gate:** CitationIntegrity
- **Location:** `src/core/citations.py:6280-6553` (the five bibkey blocks)
- **Observed:** All five external bibkeys (`physlean`, `compcert`, `sel4`, `leancipilot`, `alphaproof`) have `used_in` lists containing only `papers/paper15_methodology/paper_draft.tex` (and for `physlean`, `papers/paper14_braided_mtc/paper_draft.tex`). None list `papers/I1/paper_draft.tex`. The registry's `used_in` field is the data source for downstream propagation checks; if it is wrong, the bundle's citation graph is structurally inconsistent.
- **Evidence:** Lines 6292, 6344, 6446, 6498, 6549 in `src/core/citations.py` list only the paper15/paper14 source.
- **Expected:** Each of the five entries' `used_in` field updated to include `papers/I1/paper_draft.tex`.
- **Fix:** Mechanical edit appending `'papers/I1/paper_draft.tex'` to each `used_in` list.
- **Cache:** N/A (registry edit, not a fetch).

### 3.1 — 🔴 BLOCKER — `chirality_wall_three_pillars` is NOT in `AXIOM_METADATA`; paper claims it is

- **Gate:** LeanProofSubstance / NarrativeGrounding (Gate 5 + Gate 7)
- **Location:** `papers/I1/paper_draft.tex:407-419`
- **Observed:** §5 (worked case 3) says "The fix was a nine-fold structural decomposition driven by the `AXIOM_METADATA` table in `src/core/constants.py`. ... The chirality-wall theorem's load-bearing axioms group as `chirality_wall_three_pillars` (three axioms at tier `hard`...". The string `chirality_wall_three_pillars` does **not** appear in `AXIOM_METADATA` (which begins at `src/core/constants.py:1340`). It does appear once in `src/core/constants.py:3132` as `'chirality_wall_three_pillars': 3` — but that line is in a wave-boundary integer-mapping table, not a structural-axiom-grouping field. The paper's narrative claim that this string names a tier-`hard` axiom group of three axioms (Z₁₆ anomaly chain + 4+1D gapped interface + topological pivoting frame no-go) is unsupported by the artifact.
- **Evidence:** `grep -n "'chirality_wall_three_pillars'\|chirality_wall_three" src/core/constants.py` returns the single line 3132 (integer 3, not an axiom-metadata entry). `grep -nR "chirality_wall_three_pillars" lean/SKEFTHawking/` returns no results — the term is not in any Lean file. `grep -n "chirality_wall_three_pillars\|three_pillars" lean/SKEFTHawking/EWBaryogenesisChiralityWall.lean` returns no results. The dispatch instructions explicitly told the reviewer to verify `AXIOM_METADATA chirality_wall_three_pillars (in EWBaryogenesisChiralityWall.lean)`; neither side of that trace exists.
- **Expected:** Either (a) the paper's narrative cites a real AXIOM_METADATA entry that exists (e.g., the gapped_interface_axiom group at `constants.py:1351` is the closest real match), or (b) the AXIOM_METADATA table gains a `chirality_wall_three_pillars` entry that the prose then describes. Currently the prose describes a nonexistent artifact.
- **Fix:** Add a `chirality_wall_three_pillars` AXIOM_METADATA entry pointing at the three real axioms in EWBaryogenesisChiralityWall.lean, OR rewrite §5 to cite real existing AXIOM_METADATA keys (gapped_interface_axiom, plus the Z₁₆-anomaly axiom in `Z16AnomalyForcesThetaBar.lean`, plus the relevant TPF axiom). Until one of these is done, the paragraph is referring to a structure that does not exist.
- **Cache:** N/A.

### 4.1 — 🔴 BLOCKER — Aristotle run `78dcc5f4` mis-attributed as "Phase 5d gap-equation batch"

- **Gate:** CrossPaperConsistency / NarrativeGrounding (Gate 2 + Gate 7)
- **Location:** `papers/I1/paper_draft.tex:838-840`
- **Observed:** §9 (Aristotle integration) says "The single-batch record is twenty-five theorems closed in run `78dcc5f4` (Phase~5d gap-equation batch)". Run 78dcc5f4 is **not** the gap-equation batch — it is the Phase 5c Wave 1–7 batch covering S-matrix unitarity (SU2kSMatrix.lean), E8 lattice (E8Lattice.lean), ribbon category modularity (RibbonCategory.lean), spin bordism (SpinBordism.lean), restricted quantum group (RestrictedUq.lean), VerifiedJackknife, and Hopf-algebra structure (Uqsl2Hopf.lean) — 25 theorems total. The Phase 5d gap-equation batch is run `79e07d55` (`gapIntegral_pos`, `gapOperator_self_map`, `gap_solution_monotone`, `gap_nontrivial_exists`, etc., per `src/core/constants.py:1303-1312`). The companion source paper `papers/paper15_methodology/paper_draft.tex:121` correctly identifies 78dcc5f4 as covering "S-matrix unitarity, restricted quantum groups, ribbon categories, spin bordism, E8 lattice verification, and verified statistics" — I1 directly contradicts paper15 on this fact.
- **Evidence:**
  - I1 prose (line 838): "twenty-five theorems closed in run `78dcc5f4` (Phase~5d gap-equation batch)".
  - paper15 prose (line 121): "The most impactful single run (`78dcc5f4`) proved 25 theorems spanning S-matrix unitarity, restricted quantum groups, ribbon categories, spin bordism, E8 lattice verification, and verified statistics".
  - `src/core/constants.py:1271-1300` ARISTOTLE_THEOREMS: 78dcc5f4 → SU2kSMatrix, E8Lattice, RibbonCategory, SpinBordism, RestrictedUq, VerifiedJackknife, Uqsl2Hopf (25 theorems by row count).
  - `src/core/constants.py:1303-1312` ARISTOTLE_THEOREMS: 79e07d55 → TetradGapEquation gap-* theorems.
- **Expected:** §9 corrected to "twenty-five theorems closed in run `78dcc5f4` (Phase~5c Wave~1–7 batch covering S-matrix unitarity, ribbon-category modularity, spin bordism, E8 lattice, restricted quantum group, jackknife, and Hopf algebra)". Or, if I1 wants to feature the gap-equation batch, replace 78dcc5f4 with 79e07d55 — but then the paper's claim that 79e07d55 also produced the gap-solution-bounded counterexample (which is true, per §4 line 350) means the §9 text was probably meant to describe a *different* high-volume batch and the author confused two runs.
- **Fix:** Re-author §9 paragraph at lines 830-843 against the actual ARISTOTLE_THEOREMS table.
- **Cache:** N/A.

### 9.1 — 🔴 BLOCKER — `tables/table1_stages.tex` does not exist in the I1 bundle directory

- **Gate:** NumericalFreshness (Gate 9)
- **Location:** `papers/I1/paper_draft.tex:455`
- **Observed:** The paper ships `\input{tables/table1_stages.tex}` (line 455). The file `papers/I1/tables/table1_stages.tex` does not exist (the `papers/I1/tables/` directory is empty). The paper15_methodology directory does contain `tables/table1_stages.tex` (per `find papers -name "table1_stages*"`), but lift was not performed — table file should have been copied or regenerated as part of Phase 7a 7a.2 stub-completion.
- **Evidence:** `ls papers/I1/tables/` returns empty. `find papers -name "table1_stages*"` returns only `papers/paper15_methodology/tables/table1_stages.tex`.
- **Expected:** Either (a) lift the table from `papers/paper15_methodology/tables/table1_stages.tex` into `papers/I1/tables/table1_stages.tex` (and confirm it covers all 14 stages, since I1 describes 14 stages while paper15 may only describe 12), or (b) write a fresh `tables.py` spec for the I1 bundle and run `render_paper_tables.py`.
- **Fix:** Mechanical lift if table is current; new `tables.py` if I1's 14-stage version diverges from paper15's 12-stage version.
- **Cache:** N/A.

### 7.1 — 🟡 REQUIRED — Inline spelled-out count literals not macro-backed

- **Gate:** NumericalFreshness (Gate 9)
- **Location:** `papers/I1/paper_draft.tex:991, 1003, 1012, 1020, 1025, 1034, 1040, 1046, 1107, 1121`
- **Observed:** §11 (Phase 6f W1–W6 substrate sidebar) and §12 (Riemannian connection sidebar) use spelled-out numeric counts that are not macro-backed: "sixty-four substantive theorems" (L991, L1046), "sixteen theorems" (L1003), "nine theorems" (L1012), "eight theorems" (L1020), "sixteen theorems" (L1025), "ten theorems" (L1034), "five theorems" (L1040), "thirteen retroactive cuts" (L1047), "thirteen substantive theorems" (L1107), "forty-four substantive theorems" (L1121). These counts are not detected by `validate.py --check count_literals` (regex matches numeric digits, not spelled words). Reconciled against module file counts:
  - Wave 1 Curvature.lean: paper says 16, file has 18 (raw `^theorem|^lemma`).
  - Wave 2 EinsteinTensor.lean: paper says 9, file has 11.
  - Wave 3 EnergyConditions.lean: paper says 8, file has 9.
  - Wave 4 ExactSolutions.lean: paper says 16, file has 17.
  - Wave 5 ADMFormalism.lean: paper says 10, file has 11.
  - Wave 6 TetradFormalism.lean: paper says 5, file has 7.
  - W1-W6 total: paper says 64; file count (raw) = 73. Reasonable approximation given placeholder/aux exclusion, but not reproducible without explicit "substantive" extractor invocation.
  - W7 (LorentzianMetric+RiemannianConnection): paper says 13; file count = 14.
  - W8 (RiemannCoordinate+RiemannDifferentialBianchi+BundleRiemannAux+BundleRiemann+LeviCivita): paper says 44; file count = 39. Off by 5.
- **Evidence:** Per-module theorem-line counts via `grep -cE "^theorem|^lemma" lean/SKEFTHawking/<Module>.lean`. The Wave 8 mismatch (claim 44 vs file 39) is the largest absolute drift and should be reconciled.
- **Expected:** Either (a) replace each spelled-out count with a `\input`-backed macro or per-module count macro (e.g., `\waveOnetheorems` defined in counts.tex), or (b) reconcile the prose counts against the actual extractor output (counts.json per-module breakdown). Until either lands, prose drift is uncaptured.
- **Fix:** Per-wave macro definitions in counts.tex regen, or prose updates to match file counts.
- **Cache:** N/A.

### 7.2 — 🟡 REQUIRED — Line-range references in Lean source diverge from current source

- **Gate:** NarrativeGrounding (Gate 7) (and indirectly Gate 9 / 5)
- **Location:** `papers/I1/paper_draft.tex:271-272, 333-335, 353, 434`
- **Observed:** Several line-range citations into the Lean source are slightly off vs. current source state:
  - L271-272: "(in `lean/SKEFTHawking/SKDoubling.lean`, lines~351--439)". `firstOrder_uniqueness` is at line 412–439 in current source; the FirstOrderKMS structure starts at line 367. The 351 starting boundary picks up the docstring of `FirstOrderKMS` at line 351, which is plausible context, so this line range is roughly defensible.
  - L335: "(`lean/SKEFTHawking/TetradGapEquation.lean`, lines~304--340)". The commented-out `gap_solution_bounded` block is at lines 307–321 (per current source); the `gap_solution_monotone` runs lines 329–345. Range "304--340" partially overlaps both but is off at both ends.
  - L353: "commented-out stub at lines 316--325 of `TetradGapEquation.lean`". Actual commented-out block runs lines 307–321.
  - L434: "lines~94--120 of `EWBaryogenesisChiralityWall.lean`" for `sphaleronSuppression_le_one`. Actual theorem lines 106–114; lines 94–120 covers the surrounding `def sphaleronSuppression` at line 94 plus the `pos` and `le_one` theorems.
- **Evidence:** Line-by-line read of the three Lean files shows the discrepancies. None of the cited line ranges are catastrophically wrong, but each is loose enough that a reader following the citation will land on adjacent code rather than the exact theorem.
- **Expected:** Tighten line ranges to encompass the cited theorem only (e.g., `L412--439` for `firstOrder_uniqueness`; `L307--321` for the commented `gap_solution_bounded`; `L106--114` for `sphaleronSuppression_le_one`). Better: replace literal line ranges with permalinks (commit-pinned) or fully-qualified theorem names that the reader can grep. Hard-coded line ranges drift on every refactor and turn into wrong-target references silently.
- **Fix:** Either tighten ranges (mechanical re-grep) or remove specific ranges in favor of fully-qualified theorem names and module paths.
- **Cache:** N/A.

### 5.1 — 🟡 REQUIRED — "First in any proof assistant" claim in §11 and §12 lacks ledger backing

- **Gate:** NarrativeGrounding / FirstClaimVerification (Gate 7 + Gate 10)
- **Location:** `papers/I1/paper_draft.tex:986-987, 1009-1010, 1101-1102, 1138-1145`
- **Observed:** §11 line 986-987 says "the verification pipeline has produced as a side product a body of classical-general-relativity formalization at a substrate level that is, to the authors' knowledge after exhaustive prior-art search, new to any proof assistant". §11 line 1008-1010 specifically claims "Prior-art search across Mathlib4, Coq's `Math-Components`, Isabelle/AFP, HOL Light, HOL4, Mizar, and Agda `unimath` found no formalization of the Riemann tensor or the algebraic first Bianchi at this scope". §12 line 1101-1102 says "an indefinite-signature analogue did not exist in any proof assistant prior to Wave 7". §12 line 1138-1145 says "The Phase 6f W7–W8 substrate collectively addresses two Mathlib gaps that no other proof assistant project we surveyed had filled". §11 line 1048 says "The exhaustive prior-art audit is on file at `Lit-Search/Phase-6f/audit`". I have not verified the existence of `Lit-Search/Phase-6f/audit` (no Read tool invocation issued for it during this review). Per Gate 10, "every `first-claim`-tagged ProseClaim has a corresponding ledger entry confirming prior-work search" is required for submission.
- **Evidence:** Three first-formalization-in-any-proof-assistant claims (algebraic Riemann at Wave 1; indefinite-signature metric at Wave 7; Riemann/Ricci/scalar curvature on a connection at Wave 8). The §11 line 1048 cites `Lit-Search/Phase-6f/audit` as the supporting ledger; this should be inspected (and ideally committed to the repo with verifiable timestamps) before submission.
- **Expected:** A discoverable ledger entry — `Lit-Search/Phase-6f/audit/` directory present in the repo; per-claim audit document listing what was searched (Mathlib4 specific commit, Coq Math-Components release, etc.) and what was found.
- **Fix:** Verify `Lit-Search/Phase-6f/audit` exists and is current; if it does not, this becomes a BLOCKER. Also consider a similar ledger for Wave 8 W7–W8 sidebar claims.
- **Cache:** N/A.

### 5.2 — 🔵 RECOMMENDED — "Roughly a thousand times faster" speedup claim is qualitative

- **Gate:** NarrativeGrounding (Gate 7)
- **Location:** `papers/I1/paper_draft.tex:471-472, 805-808`
- **Observed:** §6 line 471-472 says "Empirically, the interactive loop is roughly a thousand times faster for proof construction than the build-iterate cycle." §9 line 806-807 repeats: "The empirical speedup over the build-iterate cycle is roughly three orders of magnitude." This is a feasibility-claim-shaped quantitative statement. There is no reference to a benchmark, no per-cycle time measurement, no methodology for the comparison. The CLAUDE.md project-instruction file does say "interactive proof of a single theorem now takes seconds, not hours" qualitatively (and the MCP integration document claims "milliseconds vs ~15s" for tactic-test cycles, which is ~15,000× not ~1,000×). The numerical claim should either be derived from a measurement or softened to "two-to-three orders of magnitude".
- **Evidence:** Project-internal MCP integration doc (per CLAUDE.md): per-cycle tactic test = milliseconds (LSP/REPL), per-cycle build-iterate = ~15s. That is 15,000× per cycle — but a typical proof requires many cycles, and end-to-end proof construction time is the more relevant metric. No benchmark numbers in the paper.
- **Expected:** Either cite a benchmark (e.g., "measured on N representative theorems, mean speedup factor M with stddev σ"), or soften the claim to "by two-to-three orders of magnitude" with a methodological footnote.
- **Fix:** Add benchmark or soften prose. RECOMMENDED rather than REQUIRED because the claim is plausibly correct, just not quantitatively grounded.
- **Cache:** N/A.

### 6.1 — 🟡 REQUIRED — Aristotle Lean toolchain version mismatch not disclosed in §9

- **Gate:** AssumptionDisclosure (Gate 6)
- **Location:** `papers/I1/paper_draft.tex:810-828`
- **Observed:** §9 (Aristotle integration) describes the Aristotle integration protocol in detail (priority batches, manual diff review, axiom-hygiene verification, Invariant 10 heartbeats) but does not disclose the load-bearing constraint that Aristotle runs on Lean 4.28.0 / Mathlib pinned at the Aristotle service version, while the project's Lean toolchain is at 4.29.0 (per CLAUDE.md MCP rules: "Aristotle runs on fixed versions of Lean 4 and Mathlib (4.28.0). (We use 4.29.0), results may be affected by compatibility differences"). This is a load-bearing assumption: any Aristotle run that exercises a 4.29.0-only feature in the proof body will fail. The paper says "no heartbeat overrides have been introduced (Invariant 10)" but says nothing about toolchain compatibility. Submission readers reproducing the worked cases will hit this drift silently.
- **Evidence:** CLAUDE.md instructs: "Aristotle runs on fixed versions of Lean 4 and Mathlib (4.28.0). (We use 4.29.0), results may be affected by compatibility differences. Therefore we should use sparingly". Paper §9 makes no mention of this.
- **Expected:** Add a disclosure paragraph in §9 (or in §7 invariants) noting the Aristotle/project toolchain skew and listing which worked-case theorems were verified to compile on the project's local 4.29.0 toolchain after Aristotle integration.
- **Fix:** One paragraph in §9.
- **Cache:** N/A.

### 8.1 — 🔵 RECOMMENDED — No production-run claims; class 8 is moot

- **Gate:** ProductionRunHealth (Gate 8)
- **Location:** N/A
- **Observed:** The paper makes no Monte Carlo / RHMC / numerical-evidence claims that require ProductionRun graph backing. The "twenty-five theorems closed" and "322 theorems closed by Aristotle" counts are formal-verification counts, not simulation-evidence counts. Gate 8 has no contended findings for I1.
- **Evidence:** `grep -nE "Monte Carlo|numerical evidence|simulation|RHMC" papers/I1/paper_draft.tex` returns one match (line 919) which is a methodological reference describing the agent's eight finding classes, not a substantive evidence claim.
- **Expected:** None.
- **Fix:** None.
- **Cache:** N/A.

## Process notes for re-invocation

The four BLOCKER findings cluster in two themes:

1. **Citation cleanup (1.1, 1.2, 1.3):** Three blockers because the
   bundle bibliography references seven bibitems and three additional
   `\cite{}` keys, of which two have no registry entry and three are
   undefined references. The fix is straightforward: bind every cite
   to a registered, used_in-correct registry entry, and remove
   undefined references from the body prose.

2. **Narrative-vs-artifact alignment (3.1, 4.1):** Two blockers because
   the prose describes structural artifacts (`AXIOM_METADATA
   chirality_wall_three_pillars`; "Phase 5d gap-equation batch
   78dcc5f4") that do not exist in their cited locations. These
   require either prose revision against the real artifacts, or
   artifact additions (e.g., adding a real
   `chirality_wall_three_pillars` AXIOM_METADATA entry) that match
   the prose.

3. **Bundle deliverable completeness (9.1):** One blocker because the
   table file referenced by the prose was never lifted into the
   bundle directory. Mechanical fix.

The four REQUIRED findings (1.4, 1.5, 7.1, 7.2, 5.1, 6.1) are
precision-tightening at the registry/metadata layer (citation
verification status; used_in lists; line-range tightening; first-claim
ledger; Aristotle toolchain disclosure). Each is mechanical and
non-blocking at the draft-state level.

After fixes are applied, re-invoke this review. The next pass should
flip Gate 1, Gate 2, Gate 5, Gate 7, Gate 9 to `passed` provided the
mechanical fixes propagate clean.

## QI Candidate

**Pattern: synthesis-driven new-bundle prose carries cite-key drift relative to the registry that source-paper-derived bundles do not.**

I1 is the project's first synthesis-driven new bundle composition (Phase
7a sub-wave 7a.2). It draws on paper15_methodology and
paper44_riemannian_connection as substrate sources but the prose is
authored fresh, not lifted. Three citation-class artifacts that a lift
workflow would carry mechanically — registry presence of every cite key,
`used_in` propagation, undefined-reference avoidance — have all drifted in
this bundle: two bibkeys absent from registry (1.1, 1.2), three
undefined-reference cite keys present in §5 prose (1.3), and `used_in`
fields not updated for any of the five external bibitems (1.5).

**Pipeline gap:** the `BUNDLE_LIFT_PROCEDURE.md` 14-step workflow is
oriented around lifts (mechanical copy-with-reconcile). For
synthesis-driven new bundles, the steps that catch citation hygiene
(`citations.py` registry update; `used_in` propagation; bibliography
synthesis) are not enforced as a `bundle_metadata.json` field or
`validate.py` check.

**Suggested QI register entry:** Add `validate.py CHECK 23
bundle_citation_propagation` that for each bundle's `paper_draft.tex`,
asserts (a) every `\bibitem{key}` has a CITATION_REGISTRY entry, (b)
every `\cite{key}` has a corresponding `\bibitem{key}` in the same
`paper_draft.tex`, (c) every `\bibitem{key}` registry entry's `used_in`
field includes the bundle's `paper_draft.tex` path. Owner: Phase 7a
robustness sub-wave (7a.1.x extension). Target: before bundle I2 lift.

This is a Stage 14 candidate because it is structural and would
prevent every finding 1.x in this report from surfacing on subsequent
synthesis-driven bundles.
