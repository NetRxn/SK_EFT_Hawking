---
bundle: D3
paper: D3_emergent_gravity_bh_thermo
reviewer: adversarial-reviewer
model: claude-opus-4-7-1m
review_date: 2026-05-04T23:30:00Z
readiness_gates_version: 1
round: 1
verdict: GREEN
blockers_open: 0
findings_total: 7
findings_blocker: 0
findings_required: 1
findings_recommended: 6
---

# Adversarial Review — D3_emergent_gravity_bh_thermo (round 1)

## Summary

Bundle D3 (Tier-1 deep paper, ~51pp PRD-long-format, 27 sections + 5 substrate
appendices) cleared Stage 10 round 3 GREEN with all 4 round-2 BLOCKERs closed
(commit `6c25ee0`). Stage-13 fresh-context adversarial walk through the eight
finding classes (CitationIntegrity / CrossPaperConsistency / ParameterProvenance /
TheoremNameDrift / ProductionRunBacking / HypothesisDisclosure /
ArchitecturalScope / NarrativeOverclaim) finds **zero new BLOCKERs**.

**Spot-checked 14 high-leverage Lean theorem cite-keys against `lean_deps.json`**
(Sakharov-identity heat-kernel calibration `G_N_from_a2_eq_G_N_sakharov`;
ratio-to-critical-coupling `G_N_sakharov_eq_ratio_critical_coupling`; correctness-push
gate `G_N_emerg_match_locus`; GW170817 disjointness
`natural_range_disjoint_from_ligo_window`; universal-falsifier
`second_sound_graviton_natural_range_universally_falsified`; Kaul-Majumdar
prefactor `kaul_majumdar_log_coefficient`; Sen-disagreement
`sen_4d_disagrees_with_kaul_majumdar`; abelian-MTC falsifier
`abelian_MTC_falsifies_H_HorizonBoundaryCondition`; QEC bridge
`QECHolographyBridge.HPCode.code_distance_scaling_matches_anyonic_fusion_iff_fusion_in_admissible_class`;
diff-invariance gate
`NonlinearDiffInvariance.diff_invariance_a4_iff_dirac_basis_consistent`;
microscopic-match gate
`MicroscopicCoefficientMatch.matchResidual_eq_zero_iff_alpha_unity`;
$\Lambda$-overshoot lower bound
`MicroscopicCoefficientMatch.lambdaEmergMicroscopic_at_planck_natural_far_exceeds_observed`;
torsion-bound passage
`EinsteinCartanExtension.torsionAtCosmologicalBackground_at_planck_natural_below_kostelecky`;
Penrose biconditional
`PenroseSingularity.correctness_push_biconditional_under_applicable`;
no-hair `NoHairTheorem.noHairTheorem_statement`) — **all 14 confirmed PRESENT**
at the claimed module paths.

**Spot-checked 10 high-leverage bibitems against `src/core/citations.py`**
(`Abbott2017GW170817`, `Akama1979`, `BergesSexty2008`, `ChristensenDuff1978`,
`GloriosoLiu2018`, `Hill2025bilocal`, `KaulMajumdar2000`,
`KosteleckyRussellTasson2008`, `Vassilevich2003`, `Wen2003`) — all 10 keys
present in registry; minor metadata drift on three (Akama1979 year-vs-bibkey
1978/1979 acknowledged in registry notes; Wen2003 title field empty;
KaulMajumdar2000 has duplicate registry entry).

**Cross-bundle bridges verified character-for-character:**
- D3 §6 ↔ L1 GW170817 falsification: D3 cites
  `\texttt{GravitationalWaves.natural\_range\_disjoint\_from\_ligo\_window}`,
  `\texttt{vestigial\_natural\_range\_violates\_ligo}`,
  `\texttt{second\_sound\_graviton\_natural\_range\_universally\_falsified}`;
  L1 cites the same names + numerical anchors ($\chi_{\rm vest}\in[0.1,10]$,
  $|\Delta c/c|\le 3\times 10^{-15}$, "14 orders of magnitude",
  $7\times 10^{14}$). Both papers report "fourteen orders / 14 orders"
  consistently. **No drift.**
- D3 §8 ↔ L3 BCH regime partition: D3 carries
  $M_c = N_f \Lambda_{\mathrm{UV}}/(12\pi\,\alpha_{\mathrm{ADW}})$
  identically to L3's $\Mc(\aADW, \Ladw, \Nf) = \Nf \Ladw/(12\pi \aADW)$;
  L3 cites the same `BHThermodynamicsFourLaws.lean` theorems.
  **No drift.**
- D3 §9 ↔ D4 §7: D3 cites
  `QECHolographyBridge.HPCode.code_distance_scaling_matches_anyonic_fusion_iff_fusion_in_admissible_class`;
  D4 §7 cites the same fully-qualified name + companion theorems
  (`one_le_globalDimSq`, `scramblingTimeBound_nonneg`, `codeDistance_nonneg`).
  D4 §7 cross-reference back to "Bundle~D3 \S 7" (the `H_HorizonBoundaryCondition`
  Prop is defined in D3's `BHEntropyMicroscopic.lean`) is consistent with D3's
  §7 BH-entropy section. **No drift.**
- D3 §22.5 ↔ I1 sidebar (algebraic-Lorentzian-geometry): D3 explicitly
  defers primary first-formalization claim to I1 ("Bundle~I1 ships these as
  the primary first-formalization claim; \S\ref{sec:algebraic-gr-backbone}
  is the supplement that consumes them on the substrate side"). Lean
  theorem names (`covRiemann_AntisymLastTwo`, `BundleRiemann.bundleRiemannAux_first_bianchi_torsionfree_apply`,
  Penrose Riccati-focusing core, area-theorem chain, no-hair statement) all
  PRESENT in `lean_deps.json`. **No drift.**

The Stage-10 r3 GREEN is reaffirmed. **Pass criterion met (zero open BLOCKERs).**
Five carry-forward advisories from Stage-10 plus two new Stage-13-elevated
RECOMMENDED findings; one (HypothesisDisclosure registry-key enumeration in
§28) is the analogue of the same gap flagged in D2 finding 6.1 and is
elevated to REQUIRED here because D3 has 11+ tracked-hypothesis Props (vs
D2's 5) and the gap is structurally larger.

## Findings

### 1.1 — 🟡 REQUIRED — §28 Outlook lacks consolidated tracked-hypothesis registry-key disclosure (carry-forward elevated)

- **Gate:** AssumptionDisclosure / HypothesisDisclosure
- **Location:** `papers/D3/paper_draft.tex:1976-1985` (§28 "Open under tracked
  hypothesis" subsection)
- **Observed:** D3 prose references at least 11 tracked-hypothesis Lean Props
  in body text — Vergeles trio (`H_VergelesPositivity`, `H_CriticalLimitCollapse`,
  `H_DeepGapReducesToAdler` in §2 and §5),
  `H_HorizonBoundaryCondition` (with 5 falsifier theorems, §7),
  `H_TopologicalOrderBeyondLG` (§16),
  `H_TetradQuarkScalesNatural` (§15),
  `H_EinsteinCartanExtensionHolds` (§22),
  RHMC-convergence at $L\ge 8$ (§10), `IsADWPenroseApplicable` (§24),
  Stelle higher-curvature aggregate (§21), Hu-Sawicki BHL bilocal back-reaction
  (§11), PMNS structure (§12) — but the §28 "Open under tracked hypothesis"
  subsection enumerates only four narrative groupings without giving the
  canonical Prop identifier-keys.
- **Evidence:** `grep "H_VergelesPositivity\|H_HorizonBoundaryCondition\|H_TopologicalOrderBeyondLG\|H_TetradQuarkScalesNatural\|H_EinsteinCartanExtensionHolds" papers/D3/paper_draft.tex` returns 0 matches in the §28 region
  (lines 1976-1985); the references are scattered across the body
  but not consolidated.
- **Expected:** §28 "Open under tracked hypothesis" enumerates each of
  the 11 Props by canonical identifier (e.g.,
  `\texttt{LinearizedEFE.H\_VergelesPositivity}`,
  `\texttt{BHEntropyMicroscopic.H\_HorizonBoundaryCondition}`,
  `\texttt{CFLChiralLagrangian.H\_TopologicalOrderBeyondLG}`,
  `\texttt{ChiralSSB\_QCD.H\_TetradQuarkScalesNatural}`,
  `\texttt{EinsteinCartanExtension.H\_EinsteinCartanExtensionHolds}`)
  and the corresponding closure path
  (e.g., "Vergeles trio: closes when explicit one-loop calculation of
  $\alpha_{\mathrm{ADW}}$ is delivered"), so a reader can audit the open
  obligations in one place.
- **Fix:** Add a 1-paragraph "Tracked-hypothesis registry" subsection to §28
  (before the "Cross-bundle bridges" subsection) listing each H_ Prop with its
  module-qualified name and 1-line closure path. Mirror D2 finding 6.1's
  `\textbf{H1} — ... (registry key \texttt{<key>}) ...` formatting.
- **Cache:** N/A (structural disclosure gap).
- **Severity rationale:** Elevated from RECOMMENDED → REQUIRED relative to
  D2 finding 6.1 (RECOMMENDED) because D3 has ~2× the tracked-hypothesis
  surface area (11 vs 5) and the disclosure gap is structurally larger.

### 2.1 — 🔵 RECOMMENDED — Hedging gap: bare "first machine-checked" claims in abstract + §28 lack "to our knowledge"

- **Gate:** NarrativeOverclaim
- **Location:** `papers/D3/paper_draft.tex:53` (abstract);
  `papers/D3/paper_draft.tex:1199-1202` (§16 CFL); `papers/D3/paper_draft.tex:1999-2010`
  (§28 first-formalization summary)
- **Observed:** Abstract reads "Five appendices supply the algebraic-Lorentzian-geometry
  ground floor on which the singularity, area, and no-hair theorems then rest as
  \emph{the first machine-checked} Penrose-hypothesis bundle, area theorem, and
  Kerr-family no-hair statement on indefinite-signature metrics."
  No "to our knowledge" hedge. §16 line 1201: "first machine-checked
  formalisation of this identity in any proof assistant" (emergent-$\mathbb{Z}_3$
  matching QCD centre $\mathbb{Z}_3$). §28 lines 1999-2010: "the first
  machine-checked package on indefinite-signature metrics in any proof
  assistant for: ..." (10-item list).
- **Evidence:** `grep "to our knowledge" papers/D3/paper_draft.tex` returns
  0 matches. By contrast `grep "to our knowledge" papers/D2/paper_draft.tex` →
  4 matches (D2 hedges every "first" claim). I1 paper itself at
  `papers/I1/paper_draft.tex:1072-1073` explicitly calls out the prior-occurrence
  failure mode: "narrative overclaims that were factually wrong (a 'first
  machine-checked' claim that proved to have an unrecognized prior art in
  Coq)".
- **Expected:** Bare "first machine-checked" claims hedged with "to our
  knowledge" or backed by an explicit prior-art survey ledger. The
  algebraic-Lorentzian-geometry first-formalization claim is plausibly
  load-bearing for the I1 sidebar (and D3 is its supplement); the CFL
  emergent-$\mathbb{Z}_3$ first-formalization claim has not been searched
  against Coq/Agda/Isabelle/HOL4/Mizar libraries.
- **Fix:** Insert "to our knowledge" before each of the three bare "first
  machine-checked" / "first-formalization" assertions. Optionally, add a
  prior-art-survey footnote to §28 saying "We checked Coq, Agda, Isabelle,
  HOL4, Mizar libraries for prior formalization of [X] and found none."
  Note that D3 §22.5 line 1644 already uses the I1-deferring hedge pattern
  ("Bundle~I1 ships these as the primary first-formalization claim;
  \S\ref{sec:algebraic-gr-backbone} is the supplement"); apply this same
  framing in the abstract.
- **Cache:** N/A.

### 1.2 — 🔵 RECOMMENDED — `KaulMajumdar2000` duplicate registry entry in `src/core/citations.py`

- **Gate:** CitationIntegrity
- **Location:** `src/core/citations.py:3118-3140` (entry 1) and
  `src/core/citations.py:9021-9038` (entry 2)
- **Observed:** Two `'KaulMajumdar2000'` keys in the same Python dict;
  Python silently retains the second. The two entries differ slightly:
  entry 1 says `'page': '5255-5257'`, entry 2 says `'page': '5255'`; entry 1
  has `'doi_verified': True` + `'primary_source_path': 'Lit-Search/...'`,
  entry 2 has `'doi_verified': None` + `'primary_source_path': None`.
- **Evidence:** `grep -n "'KaulMajumdar2000':" src/core/citations.py` returns
  two line numbers (3118 and 9021).
- **Expected:** A single canonical entry per bibkey. The first entry
  (Phase 6a Wave 3 origin, with primary-source path and verified DOI) is
  the canonical one; the second entry (auto-stub pattern from a later
  D3 lift) is the redundant copy.
- **Fix:** Delete the second `KaulMajumdar2000` block at lines 9021-9038;
  ensure the first block at 3118-3140 has all the metadata D3 needs.
  Note: this does not affect bibitem-vs-registry agreement (the bibitem
  in `paper_draft.tex` resolves to exactly one of the two — Python keeps
  the last-defined). It does affect future automated citation-registry
  merges and validate.py's CitationIntegrity check baseline. Bibkey
  rename history (`KaulMajumdar1998 → KaulMajumdar2000`) is correctly
  recorded in entry 2's `notes` field; preserve that history in the
  surviving entry.
- **Cache:** structural-bug; N/A.

### 1.3 — 🔵 RECOMMENDED — `Wen2003` registry entry has empty `title` field; bibitem self-consistent

- **Gate:** CitationIntegrity
- **Location:** `src/core/citations.py:6180-6196`; `papers/D3/paper_draft.tex:2381-2387`
- **Observed:** `CITATION_REGISTRY['Wen2003']` has `'authors': 'X.-G.~Wen,
  Phys.\ Rev.\ D , 065003 (2003)'` (the bibitem-formatted authors string,
  not parsed authors), `'title': ''` (empty), and notes "Auto-generated stub
  from \\bibitem block in `papers/paper3_gauge_erasure/paper_draft.tex`.
  Title not present in source bibitem." However the D3 bibitem at line
  2381-2387 reads "X.-G.~Wen, ``Quantum order from string-net condensations
  and the origin of light and massless fermions,'' Phys. Rev. D \textbf{68},
  065003 (2003), arXiv:hep-th/0302201." — the title IS present in the D3
  bibitem.
- **Evidence:** Bibitem in D3 contains the title; registry stub doesn't.
  Wen 2003 is load-bearing for D3 §3 (gauge-erasure motivation) and §4
  (Wen-ADW deficit factor 6000) — the registry should reflect the D3
  bibitem authoritatively.
- **Expected:** Update `CITATION_REGISTRY['Wen2003']`: `'authors':
  'Wen, X.-G.'`, `'title': 'Quantum order from string-net condensations
  and the origin of light and massless fermions'`,
  `'arxiv': 'hep-th/0302201'`, `'doi': '10.1103/PhysRevD.68.065003'` (canonical),
  `'used_in': ['papers/paper3_gauge_erasure/paper_draft.tex',
  'papers/D3/paper_draft.tex']`.
- **Fix:** One-line registry update; harmonises with the D3 lift.
- **Cache:** N/A.

### 1.4 — 🔵 RECOMMENDED — `Akama1979` bibkey-vs-publication-year drift (carry-forward, accepted)

- **Gate:** CitationIntegrity
- **Location:** `papers/D3/paper_draft.tex:2081-2084`; registry
  `src/core/citations.py:8701-8714`
- **Observed:** Bibitem reads "K.~Akama, ``An attempt at pregeometry: gravity
  with composite metric,'' Prog.~Theor.~Phys.~\textbf{60}, 1900 (\textbf{1978}).";
  cite-key is `Akama1979`. Registry note acknowledges drift: "Bibkey says 1979;
  actual publication Dec 1978 in PTP 60(6)."
- **Evidence:** Bibitem date matches publication record (PTP 60, 1900, 1978).
  Cite-key off-by-one is intentional (matches the convention in the ADW
  literature; the original Akama paper is universally cited as "Akama 1979"
  even though the publication date is December 1978).
- **Expected:** Acceptable drift; registry note documents the convention.
  No change needed unless a project-wide bibkey-versus-publication-year
  validator is implemented.
- **Fix:** None required.
- **Cache:** carry-forward (registry note is the resolution).

### 1.5 — 🔵 RECOMMENDED — `GloriosoLiu2018` bibitem date inconsistency (advisory)

- **Gate:** CitationIntegrity
- **Location:** `papers/D3/paper_draft.tex:2159-2162`; registry
  `src/core/citations.py:730-754`
- **Observed:** Bibitem reads "P.~Glorioso and H.~Liu, ``The second law of
  thermodynamics from symmetry and unitarity,'' arXiv:1612.07705 (2018)."
  But arXiv:1612.07705 is from December 2016 (the v1 submission date).
  Registry has `'year': 2018` and `'journal': 'JHEP'`, but `'volume': None`
  and `'page': None`.
- **Evidence:** Fresh-fetch verification not performed (rate-limit caution);
  arXiv ID prefix 1612.* corresponds to December 2016 submissions per
  arXiv numbering convention. Registry note says "arXiv:1612.07705 verified
  2026-04-26" but doesn't pin a JHEP volume/page. The 2018 date in the
  bibitem is plausibly the published-JHEP-issue year, but JHEP volume/page
  are not given.
- **Expected:** Either (a) verify JHEP publication and update the registry
  + bibitem with `JHEP \textbf{XX}, NNN (2018)` issue/page, or (b) standardise
  the bibitem to "arXiv:1612.07705 (2016)" (the v1 submission year) with a
  `Phys.~Rev.~D` or similar published-version line if available.
- **Fix:** Single human-side DOI fetch on `10.1007/JHEP*` GloriosoLiu pattern
  to pin the JHEP issue, or accept the arXiv preprint citation form and
  align both bibitem and registry.
- **Cache:** advisory-defer; no immediate action needed for D3 submission.

### 7.1 — 🔵 RECOMMENDED — `wen_adw_factor_6000` lemma "in flight" but body cites the factor 6000 quantitatively (carry-forward, accepted)

- **Gate:** TheoremNameDrift / NarrativeGrounding
- **Location:** `papers/D3/paper_draft.tex:425-434` (§4 perturbative-Wen-ADW
  deficit subsection)
- **Observed:** Body asserts "$G_{\mathrm{Wen}}/G_c^{\mathrm{ADW}} \approx 1/6000$"
  as a "closed-form Lean identity — a closed-form Lean-derived ratio rather
  than a numerical estimate", then admits "an explicit
  \texttt{wen\_adw\_factor\_6000}-named lemma is in flight as a single-line
  corollary."
- **Evidence:** `grep "wen_adw_factor_6000" lean/lean_deps.json` returns
  empty; the lemma name does NOT exist as a fully-qualified declaration
  in the project's Lean library.
- **Expected:** Either (a) the lemma ships before submission and the
  in-flight disclaimer is removed, or (b) the disclaimer is preserved
  honestly (current state, which is already honest). The Wen-ADW factor
  6000 is one of the three predictive-boundary closures and is load-bearing
  for the architectural argument; honest disclosure is acceptable but
  the gap should be closed before final submission.
- **Fix:** Add the one-line corollary to `GaugeErasure.lean` or a
  companion module. Algebraically the factor follows from
  $G_{\mathrm{Wen}}/G_c^{\mathrm{ADW}} = (\text{perturbative coefficient})/
  (\text{gap-equation non-perturbative coefficient}) = c_{\rm Wen}/c_{\rm ADW}$
  with $c_{\rm Wen}/c_{\rm ADW} \approx 1/6000$; the closed-form ratio
  should be a `theorem wen_adw_factor_6000 : G_Wen / G_c_ADW = ...` followed
  by a `norm_num` or `unfold + rfl` discharge.
- **Cache:** structural — body self-discloses the gap.

## QI candidates raised

- **QI-D3-1 — `bibitem_versus_registry_authors_normalized`.** Finding 1.3
  (Wen2003 empty title in registry while bibitem has title) and 1.4
  (Akama1979 bibkey-vs-publication-year off-by-one) are caught by ad-hoc
  inspection but not by `validate.py`'s current CitationIntegrity. A
  registry-vs-bibitem normalisation pass that (a) checks `authors` field
  is a parsed `Last, F.` form rather than a raw bibitem string, (b) checks
  `title` field is non-empty for non-textbook entries, and (c) flags
  bibkey-vs-year drift > 1 year would catch this class. Cost: 1-2 hours
  build; benefit: turns these from RECOMMENDED to auto-checked.
- **QI-D3-2 — `tracked_hypothesis_disclosure_in_outlook`.** Finding 1.1
  (§28 lacks consolidated tracked-hypothesis registry-key list) is the
  natural extension of D2's finding 6.1. A new `validate.py --check
  paper_outlook_tracked_hypothesis_disclosure` validator that (a) extracts
  all `H_*` Prop names referenced in body prose, (b) requires they appear
  in §28 (or equivalent "Outlook" section) by canonical identifier, would
  enforce this discipline at compile-gate time. Cost: ~2-3 hours (regex
  + section parser); benefit: Tier-1 papers automatically enforce the
  tracked-hypothesis disclosure pattern.
- **QI-D3-3 — `lean_theorem_in_flight_disclosure_validator`.** Finding 7.1
  (`wen_adw_factor_6000` is "in flight" — body claims it as a Lean identity
  but the lemma is not in `lean_deps.json`). A validator that (a) extracts
  every `\\texttt{module.theorem}`-shaped reference from body prose,
  (b) checks each against `lean_deps.json` membership, (c) requires
  any missing reference to be wrapped in an explicit `% TODO` or `in flight`
  hedge would catch this prospectively. Cost: ~2 hours; benefit: prose
  drift between paper and Lean library auto-detected at submission gate.

## Class-by-class scan summary

- **Class 1 (CitationIntegrity):** 4 findings — 0 BLOCKERs, 0 REQUIRED,
  4 RECOMMENDED (1.2 KaulMajumdar2000 duplicate registry; 1.3 Wen2003
  empty title; 1.4 Akama1979 bibkey-year drift carry-forward; 1.5
  GloriosoLiu2018 date inconsistency). All 61 bibitems in the inline
  thebibliography{99} block resolve to CITATION_REGISTRY. `paper_draft.log`
  scan: 0 'Citation undefined' warnings (verified via Stage-10 r3
  reconciliation). Spot-checked 10 of 61 keys against registry — 100%
  resolution rate, with 3 minor metadata drifts flagged above.
- **Class 2 (CrossPaperConsistency):** 0 findings. D3 §6 and L1 carry
  identical GW170817 numerical anchors ("14 orders / fourteen orders",
  $7\times 10^{14}$, $\chi_{\rm vest}\in[0.1,10]$, $|\Delta c/c|\le 3\times 10^{-15}$).
  D3 §8 and L3 carry identical regime-partition mass formula
  $M_c = N_f\Lambda_{\rm UV}/(12\pi\,\alpha_{\rm ADW})$. D3 §9 and D4 §7
  carry identical QECHolographyBridge.HPCode.* theorem cite-keys. D3 §22.5
  defers first-formalization claims to I1 (primary) explicitly. **No
  CONTRADICTS edges detected.**
- **Class 3 (ParameterProvenance):** 0 findings. D3 numerical literals
  ($\Lambda_{\rm obs} = 2.6\times 10^{-47}\,{\rm GeV}^4$, $M_{\rm Pl} =
  1.2\times 10^{19}\,{\rm GeV}$, $|T_{\rm EC}| = 2.05\times 10^{-77}\,{\rm GeV}$,
  KRT bound $10^{-31}\,{\rm GeV}$, CC heuristic ratio $10^{122}$, Lean lower
  bound ratio $10^{100}$, factor 6000 perturbative-Wen deficit, factor 46
  KRT-passage gap, factor 7×10^14 GW170817-vestigial gap, $f_\pi = 92.07\,$MeV,
  $m_\pi = 134.98\,$MeV, $m_q = 3.45\,$MeV, $\langle\bar q q\rangle =
  -(2.62\times 10^8)^3\,$MeV) all sourced consistently — Sakharov coefficient
  predictions traced through the heat-kernel chain; PDG anchors traced via
  `\cite{PDG2024}`; observational anchors traced via `\cite{Abbott2017GW170817}`,
  `\cite{KosteleckyRussellTasson2008}`, etc. Note: the "$\gtrsim 10^{100}$"
  Lean-rigorous lower bound vs heuristic "$\sim 10^{122}$" is honestly
  disclosed in §1 abstract + §1 intro + §21 — the residual gap is owned
  to "Mathlib-shipped fundamental constants precision".
- **Class 4 (Theorem-name reference drift):** 0 findings. Spot-checked 14
  high-leverage Lean theorem cite-keys — all PRESENT in `lean_deps.json`
  at the claimed module paths. The `wen_adw_factor_6000` lemma is "in
  flight" (body self-discloses); flagged as RECOMMENDED 7.1, not BLOCKER,
  because the disclosure is honest. Stage-10 r3 closure of the 4 prior
  TN-class BLOCKERs (QECHolographyBridge.HPCode prefix, 4× pathB residual
  qualifications, biconditional + perturbation-residual rename, bibliography
  compile gate) is reaffirmed.
- **Class 5 (ProductionRun without backing):** 0 findings. The vestigial-MC
  $L=4,6$ first-order-transition claim (§10) is the only Monte-Carlo
  evidence claim and is correctly hedged ("first-order transition... within
  statistical error" and "RHMC convergence at $L\ge 8$ remains in flight").
  No unbacked production-run claims.
- **Class 6 (HypothesisDisclosure):** 1 REQUIRED (1.1 above). D3 carries
  ~11 tracked-hypothesis Lean Props but §28 Outlook does not enumerate
  them by canonical Prop name. **The largest disclosure gap of any bundle
  reviewed so far** (D2 was 5 Props, D3 is 11+).
- **Class 7 (Architectural-scope sidebar drift):** 0 findings. §22.5
  algebraic-Lorentzian-geometry sidebar is correctly slice-restricted —
  D3 is "the supplement that consumes them on the substrate side" while
  I1 is "the primary first-formalization claim". The cross-reference at
  line 1644 explicitly defers primary attribution to I1. The five appendix
  sections (§22.5 backbone + §23 causal structure + §24 Penrose + §25
  Hawking-Penrose + §26 area + §27 no-hair) describe the algebraic-Lorentzian-geometry
  ground floor as substrate infrastructure for D3's specific gravity
  register, not as a general-relativity textbook reformulation. The
  scope statement is honest.
- **Class 8 (NarrativeOverclaim):** 1 RECOMMENDED (2.1 above). Three "first
  machine-checked / first-formalization" assertions (abstract, §16, §28)
  lack "to our knowledge" hedging or explicit prior-art-survey footnotes.
  D2 paper consistently uses "to our knowledge the first" hedge; D3 does
  not. The §22.5 sidebar correctly defers to I1 (primary) so the algebraic-Lorentzian-geometry
  family of claims is structurally honest, but the abstract sentence
  could be hedged. CFL-emergent-$\mathbb{Z}_3$ first-formalization claim
  has not been searched against Coq/Agda/Isabelle/HOL4/Mizar libraries
  for prior occurrences.

## Verdict

**🟢 GREEN** — zero open BLOCKERs. Stage-10 r3 GREEN reaffirmed. One new
REQUIRED finding (1.1, tracked-hypothesis disclosure gap in §28) elevated
from D2's analogous RECOMMENDED-class finding 6.1, since D3 has 2× the
tracked-hypothesis surface area. Six RECOMMENDED findings (4 carry-forwards
or minor citation-registry hygiene; 1 hedging-discipline class on
"to our knowledge"; 1 in-flight Lean lemma honestly self-disclosed).
**Bundle D3 is submission-ready** pending optional advisory cleanup
(REQUIRED 1.1 should be addressed before PRD-long-format submission;
RECOMMENDEDs are deferrable).

Top 3 most load-bearing concerns for human review (in priority order):

1. **Finding 1.1 — §28 Outlook lacks consolidated tracked-hypothesis
   registry-key disclosure.** D3 has 11+ tracked-hypothesis Lean Props
   scattered across body prose; §28 Outlook does not enumerate them by
   canonical name. Reader cannot audit open obligations in one place.
   This is the same pattern flagged in D2 finding 6.1 and is structurally
   larger here. Single-paragraph fix: add a "Tracked-hypothesis registry"
   subsection to §28 listing each `H_*` Prop by module-qualified name
   plus 1-line closure path.

2. **Finding 2.1 — Bare "first machine-checked" claims in abstract + §16
   + §28 lack "to our knowledge" hedge or prior-art ledger.** D2 hedges
   every such claim with "to our knowledge"; D3 does not. The
   I1 paper itself at line 1072-1073 explicitly cites the prior-occurrence
   failure mode ("a 'first machine-checked' claim that proved to have an
   unrecognized prior art in Coq"). Three sentence-level edits to apply
   the hedge across abstract + §16 + §28.

3. **Finding 7.1 — `wen_adw_factor_6000` lemma claimed but not in
   `lean_deps.json`.** Body self-discloses ("an explicit
   `wen_adw_factor_6000`-named lemma is in flight as a single-line
   corollary"). The factor 6000 is one of the three predictive-boundary
   closures and is load-bearing. Honest disclosure is acceptable for round-1
   GREEN, but the one-line corollary should ship before PRD final submission
   to close the architectural-claim/Lean-library mismatch.

D3 Stage-13 r1 verdict: GREEN with 0 BLOCKERs.
