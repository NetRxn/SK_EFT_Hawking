---
paper: D2
reviewer: adversarial-reviewer
model: claude-opus-4-8
review_date: 2026-06-08T23:15:43Z
readiness_gates_version: 1
re_invocation_of: 2026-06-08-2240-internal-adversarial/D2.md
---

# Adversarial Review — D2 (re-invocation after remediation)

## Summary

Re-run of the 2026-06-08T22:40 review (6 findings: 2 BLOCKER, 3 REQUIRED, 1
RECOMMENDED). **Both prior BLOCKERs (5.1, 5.2) are RESOLVED**, verified
independently against the current draft and Lean artifacts. The two
REQUIRED Lean-staleness items (6.1 the removed `eight_dvd` field; the axiom
half of 9.1) are RESOLVED. The remaining open items are non-blocking
carryovers: per-module count literals (9.1, REQUIRED — draft-acceptable),
the stale editorial comment (9.2, RECOMMENDED), and the `arxiv_verified`
schema gap (1.1, REQUIRED — draft-acceptable). No new BLOCKER or REQUIRED
issues were introduced by the edits. **Net: 0 BLOCKER, 2 REQUIRED
(carryover, draft-OK), 1 RECOMMENDED (carryover).** Stage-13 gate: GREEN
for blocker-class; the two carryover REQUIRED items remain submission-time
obligations but do not block draft advancement.

## Verification of prior findings

### 5.1 — ✅ RESOLVED — "one/lone/sole/true axiom" + "axiom tally remains 1"

- **Prior:** abstract L74, intro L131-132, outlook L886-887/978/998-1000/1058 all asserted a live `gapped_interface_axiom` / "axiom tally remains 1".
- **Current state (RESOLVED):**
  - Grep of D2 for `one axiom|1 axiom|lone/sole/single .*axiom|true axiom|axiom tally` returns exactly **one** hit, L1110 `"The project-local axiom count stays at \axiomcount{}"` — correct macro usage, not an overclaim.
  - `\axiomcount{}` is now used **7×** in D2 (abstract L74, intro L135, §2.4 ladder L904, outlook L968/995/1021/1110).
  - Abstract L74-78: "with \axiomcount{} project-local axioms: the 4+1D gapped-interface statement is carried as a tracked-hypothesis \texttt{Prop} (\texttt{TPFConjecture}, formerly \texttt{axiom gapped\_interface\_axiom}, converted … 2026-05-19)".
  - Intro L135-137: "(\axiomcount{} project-local axioms): one tracked-hypothesis \texttt{Prop} (\texttt{TPFConjecture}, formerly \texttt{axiom gapped\_interface\_axiom}…)".
  - Outlook §"What is axiomatised" L995-1023: "The project declares \axiomcount{} project-local axioms. The 4+1D … conjecture is carried as a tracked-hypothesis \texttt{Prop}, \texttt{TPFConjecture} … consumed by downstream theorems as an explicit \texttt{(H : TPFConjecture)} hypothesis parameter rather than asserted as an axiom." and L1020-1021 "The project's \texttt{lake build} axiom tally is \axiomcount{}".
  - Falsification (iii) L1086-1088: now "falsifies the project's central tracked hypothesis \texttt{TPFConjecture}" (was "sole axiom").
- **Artifact cross-check:** `docs/counts.tex` (regen 2026-06-04) `\newcommand{\axiomcount}{0}`. No live `axiom gapped_interface_axiom` declaration in `lean/SKEFTHawking/` (grep of `^axiom gapped` empty; all `axiom`-keyword grep hits are docstring/comment prose). `SPTClassification.lean:254` `def TPFConjecture : Prop := ∀ (spt : SPTPhaseData), spt.anomaly_free → …` is a genuine ∀∃ Prop (non-placeholder; `TPFConjecture_iff_explicit` confirms statement shape). `constants.py:1391` `gapped_interface_axiom` registry entry documents the 2026-05-19 → `TPFConjecture` Prop conversion.

### 5.2 — ✅ RESOLVED — §2.4 "no current Mathlib substrate" / "assembled pending" framing of 8∣σ / 16∣σ

- **Prior:** L367-378 described `8∣σ` as open, reducible to a classification-existence statement with two pieces having "no current Mathlib substrate" and "assembled pending multivariate Poisson summation".
- **Current state (RESOLVED):** Grep for `no current Mathlib substrate|assembled pending|existence statement|remaining input is solely` returns **empty**. §2.4 (L375-391) now reads: "The classification's two classical inputs are now \emph{both} discharged as kernel-pure theorems, with no remaining Mathlib gap. The Hasse--Minkowski input … is the theorem \texttt{hasIsotropicVector}, established at every rank (… rank 4 via Chevalley--Warning + Hensel + binary Hilbert reciprocity …); … The level-one theta-modularity … is the theorem \texttt{eight\_dvd\_latticeSig\_of\_definite}. Composing the two branches, \texttt{eight\_dvd\_latticeSig} proves $8\mid\mathrm{latticeSig}\,M$ for every even unimodular form unconditionally, and \texttt{sixteen\_dvd\_latticeSig} upgrades this to $16\mid\sigma$ … both are kernel-pure. The only genuinely outstanding interface input is the topological factor of two $2\mid\sigma/8$".
- **Artifact cross-check (lean_verify, kernel-pure all three):**
  - `SKEFTHawking.SmoothSpinManifold4.rokhlin` → `{propext, Classical.choice, Quot.sound}`, no warnings. Body: `sixteen_dvd_latticeSig M.form M.even_unimod M.topo` (substantive, not placeholder).
  - `SKEFTHawking.eight_dvd_latticeSig` → `{propext, Classical.choice, Quot.sound}`.
  - `SKEFTHawking.hasIsotropicVector` → `{propext, Classical.choice, Quot.sound}`.
  - `RokhlinHMRankFour.lean:595/599/604` declare `hasIsotropicVector` / `eight_dvd_latticeSig` / `sixteen_dvd_latticeSig` as theorems; `RokhlinFromHM.lean:56/63` compose the [HM]+[Θ] branches via `eight_dvd_latticeSig_of_definite`. `constants.py:2206-2208` `rokhlin_sigma_mod_16` status `'discharged'`.

### 6.1 — ✅ RESOLVED — §2.4 cited removed structure field `eight_dvd`

- **Prior:** L333-335 told the reader `SmoothSpinManifold4` carries a load-bearing field `eight_dvd : 8 ∣ latticeSig form`.
- **Current state (RESOLVED):** L333-341 now: "The structure \texttt{SmoothSpinManifold4} records … the field \texttt{even\_unimod}, and the topological factor of two $2\mid\sigma/8$ in the field \texttt{topo}. … The algebraic residual $8\mid\mathrm{latticeSig}\,(\text{form})$ is \emph{not} a carried interface field --- it is itself the derived kernel-pure theorem \texttt{SmoothSpinManifold4.eight\_dvd\_sig}". The explicit "not a carried interface field" disclaimer is present at L338-339.
- **Artifact cross-check:** `SpinRokhlinInterface.lean:55-63` — `structure SmoothSpinManifold4` has exactly four fields: `rank`, `form`, `even_unimod`, `topo`. No `eight_dvd` field. `:70` `theorem SmoothSpinManifold4.eight_dvd_sig … := eight_dvd_latticeSig M.rank M.form M.even_unimod` (derived theorem). Prose matches the artifact exactly.

### 9.1 (axiom half) — ✅ RESOLVED / (per-module-count half) — 🟡 STILL-OPEN (REQUIRED, draft-OK)

- **Axiom half (RESOLVED):** the "one axiom"/"1 axiom"/"axiom tally remains 1" inline literals that contradicted `\axiomcount{}=0` are gone (see 5.1). `validate.py --check count_literals` passes (1/1, 17 warnings) and lists **no D2 entry** ("✓ paper8_chirality_master — no count literals found (macros in use)"; D2 not flagged by the syntactic checker).
- **Per-module-count half (STILL-OPEN, unchanged):** ~10 inline per-module theorem-count literals remain: L567 (48), L629 (52), L631 (30), L757 (14), L758 (28), L782 (12), L863 (24), L871 (12), L915 (20), L937 (16). These slip past `count_literals` (which targets canonical totals) and are drift-prone. Severity unchanged: **REQUIRED** — draft-acceptable, submission requires retrofit into `papers/D2/tables.py` or `\input{}`-backed macros per the Gate-9 remediation. Not a regression; carried from prior review.

### 9.2 — 🔵 STILL-OPEN (RECOMMENDED) — stale GoltermanShamir2026 editorial comment

- **Current state (unchanged):** the L1266-1271 cleanup comment still asserts the title was "corrected to 'Generalized no-go theorem for chiral fermions on a lattice'" and PRD coords "PRD 113, 014503 (2026)", while the live bibitem L1146 and `CITATION_REGISTRY['GoltermanShamir2026']` both carry "Symmetric mass generation and the Nielsen-Ninomiya theorem", arXiv:2603.15985, "PoS Lattice 2025 proceedings". Live bibitem ↔ registry agree (not a Gate-1 BLOCKER); the comment is internally contradictory and could mislead a future editor. Severity unchanged: **RECOMMENDED**. Carryover, not a regression.

### 1.1 — 🟡 STILL-OPEN (REQUIRED, draft-OK) — registry lacks machine-readable `arxiv_verified`

- **Current state (unchanged):** All 31 D2 bibkeys resolve to `CITATION_REGISTRY` (no registry-absence BLOCKER). The Gate-1 `arxiv_verified == True` pass-criterion (READINESS_GATES.md:33) remains non-evaluable structurally; arXiv attestation lives only in free-text `notes`. Severity unchanged: **REQUIRED** (draft-OK; blocks submission-time Gate-1 sign-off). Every bibitem↔registry pair compared agrees on title/arXiv/venue, so no `wrong_target` BLOCKER. Carryover, not a regression.

## New-issue pass (classes 1, 2, 3 normal pass; 5, 6, 9 thorough)

- **Class 1 (citations):** No new wrong-target issues. Spot-checked bibitems against registry agreement: Wang2024 (arXiv:2312.14928, PRD 110 125028), GarciaEtxebarria2019 (arXiv:1808.00009), GioiaThorngrenPRL2026 (PRL 136 061601, arXiv:2503.07708), TPF2026 (arXiv:2601.04304). No new finding.
- **Class 2 (cross-paper):** `paper8_chirality_master/paper_draft.tex:459` still reads "(TPF, the project's single axiom)" — the same stale-axiom language D2 just shed. This is **not a D2 finding** (it lives in paper8) but it confirms the prior QI candidate (systemic stale single-axiom disclosure) is still live in the chirality bundle. No D2-internal cross-paper contradiction surfaced. Flagged below as QI.
- **Class 3 (placeholder theorems):** D2's load-bearing cited theorems are substantive: `SmoothSpinManifold4.rokhlin` / `eight_dvd_latticeSig` / `hasIsotropicVector` kernel-pure (verified); `hidden_sector_required` (Z16AnomalyComputation.lean:180-182) is a real existence proof; `TPFConjecture` is a genuine Prop. The one openly-disclosed placeholder, `Z16AnomalyComputation.dai_freed_spin_z4` (Ω₅^{Spin^Z4}≅Z16), is explicitly labeled "trivially-discharged placeholder pending Mathlib cobordism infrastructure" at L612-614 and L1006-1010 — disclosed, not cited-as-verified, so no Gate-5 violation. No new finding.
- **Classes 5/6/9 (thorough):** No new overclaims introduced by the edits. The outlook tracked-Props prose (L1049-1064) now lists exactly the two active registry keys `modular_invariance_framing` and `c_minus_equals_8Nf`, and marks `rokhlin_sigma_mod_16` `discharged` with the correct theorem name `SmoothSpinManifold4.rokhlin` — matching the author's claimed fix and the `constants.py` registry. No regression.

## QI Candidate

**Systemic stale "single axiom" disclosure persists in `paper8_chirality_master`.**
D2 is now clean, but `papers/paper8_chirality_master/paper_draft.tex:459` still
calls TPFConjecture "the project's single axiom" — the same defect class fixed
in D2 (5.1). The fix did not propagate to paper8. Recommend (a) the same
`gapped_interface_axiom → TPFConjecture` / `\axiomcount{}=0` edit on paper8, and
(b) a `validate.py` axiom-count check that greps paper `.tex` for "single/lone/
sole/one (true) axiom" + "axiom tally" and diffs against `\axiomcount{}`
(`count_literals` does not catch axiom-count phrasing). Carries forward the prior
review's QI item, now narrowed to paper8 as the sole remaining locus.

## Verdict

- Prior BLOCKERs: **5.1 RESOLVED, 5.2 RESOLVED.**
- Prior REQUIRED: **6.1 RESOLVED**; **9.1-axiom RESOLVED**, 9.1-per-module-counts STILL-OPEN (draft-OK); **1.1 STILL-OPEN (draft-OK).**
- Prior RECOMMENDED: **9.2 STILL-OPEN.**
- New issues introduced by the edits: **none.**

Final counts: **0 BLOCKER, 2 REQUIRED (both draft-acceptable carryovers: per-module count literals; `arxiv_verified` schema gap), 1 RECOMMENDED (carryover).**

**Stage-13 gate: GREEN** (blocker-class). All submission-blocking findings from the prior round are remediated and independently verified against the current draft and the kernel-pure Lean artifacts. The two remaining REQUIRED items are submission-time obligations (count-literal retrofit + registry `arxiv_verified` field), not draft blockers; they may be tracked into the submission checklist.
