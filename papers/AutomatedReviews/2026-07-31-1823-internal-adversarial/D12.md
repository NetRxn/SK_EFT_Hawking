---
paper: D12
reviewer: adversarial-reviewer
model: claude-opus-5
review_date: 2026-07-31T18:23:27Z
readiness_gates_version: 1
head_commit: ac66444f
round: 9
---

# Adversarial Review — D12

## Summary

2 BLOCKER / 9 REQUIRED / 12 RECOMMENDED across CitationIntegrity,
ParameterProvenance, ComputationCorrectness, LeanProofSubstance,
CrossPaperConsistency, NarrativeGrounding, AssumptionDisclosure,
NumericalFreshness and FixPropagation. All six repairs I was asked to attack
hardest are real and I re-derived each independently — the ENBW constant, the
crossover, the 21.5-point derivation, the three ledger records, and the
blocker-count rise from 9 to 23 (which is arithmetically exact and **not** an
over-correction). Two things are not: the round-8 8.1 guard is bypassable by a
one-key ledger record and by the closing document's own severity glyph, and a
Stage-13 review document that mints zero graph nodes remains invisible to every
guard in the suite — the mechanism that lost rounds 4, 5 and 6. A new
NarrativeGrounding finding: the `prxquantum` → `prx` rationale is false, and I
proved it by compiling both. D12 is **not** submission-ready.

## Environment note — Stages 1–12 are NOT green on the working tree

`validate.py` archived 55/55 at `validation_20260731T180743Z.json`, but the tree
has moved since: `scripts/validate.py`, `scripts/build_graph.py`,
`scripts/bundle_readiness.py` and `src/core/provenance.py` all carry
**uncommitted** edits at review time (round-8 remediations for 2.4, 8.5 and 8.6).
Against the tree as it stands, `bundle_metadata_matches_graph` **fails**:

```
bundle_metadata_matches_graph  passed=False
  summary  21 bundle metadata blob(s) compared against the live graph, 7 with drift
  D12      freshness_stale=false with 23 open blocker(s), which LATE_PHASE6_ABSORPTION_PROTOCOL.md forbids
  D11(18) D7(12) I2(19) I3(16) L1(1) L3(5)  — same leg
```

The *root-cause* fix is right (`write_metadata_counts` now derives
`freshness_stale` rather than reading it), but nobody has re-run
`scripts/bundle_readiness.py`, so the blobs on disk still assert the forbidden
pair. This is a one-command residue, not a design defect, and I proceeded with
the review rather than stopping — but the round cannot close on a red suite.
Filed as 8.6.

## Findings

### 1.1 — 🔵 RECOMMENDED — `IrwinHilton2005` is still trusted unqualified for the loop-gain convention while the same paragraph-pair discloses the source as unread (round-8 1.1 REPRODUCES, third consecutive round)

- **Gate:** CitationIntegrity
- **Location:** `papers/D12/paper_draft.tex:362-363` vs `:433-436`
- **Observed:** Byte-identical to rounds 7 and 8. `:362-363`: "\thm{loopGain\_eq\_irwinHilton} proves it equals the instrument-literature convention~\cite{IrwinHilton2005,Irwin1995}" — no qualifier. `:433-436`: "We attach~\cite{IrwinHilton2005} as the standard review in which this layer is presented, but we have not inspected its text: we hold that source only as a resolved DOI record, so the attribution of this specific closed form to it should be read as provisional."
- **Evidence:** `git diff 837f335e..HEAD -- papers/D12/paper_draft.tex` touches exactly three hunks (`:1-9` class option, `:295-302` ENBW, `:440-444` the 21.5 parenthetical, `:568-573` crossover). Neither line is among them. `CITATION_REGISTRY['IrwinHilton2005'].notes` still asserts the loop-gain convention comes from that text.
- **Expected:** Extend the `:433` provisional-attribution qualifier to the loop-gain convention, or narrow `:363` to `Irwin1995` alone (which *is* read).
- **Fix:** One clause at `:363`, mirrored into the registry `notes`.
- **Cache:** cache-verified (`IrwinHilton2005`, hash `925b58102cb8`, verdict `match`, 2026-07-31T05:27:44Z)

### 1.2 — 🔵 RECOMMENDED — `scripts/citation_cache.stats()` still raises on the cache it summarizes (round-8 1.2 REPRODUCES)

- **Gate:** CitationIntegrity
- **Location:** `scripts/citation_cache.py` (`is_stale` / `stats`)
- **Observed:** `from scripts.citation_cache import stats; stats()` → `TypeError: can't subtract offset-naive and offset-aware datetimes`. Re-run this round; unchanged.
- **Evidence:** `is_stale` catches `KeyError` and `ValueError`; the failure is a `TypeError` at `now - verified`, raised by the six records whose `verified_date` is the bare string `"2026-05-31"`. No D12 bibkey is among them, so D12's own lookups are unaffected — but the summary entry point the CLI and dashboard call is dead for every caller.
- **Expected:** `except (KeyError, ValueError, TypeError)`, or normalise naive timestamps to UTC on read.
- **Fix:** One `except` clause.
- **Cache:** n/a

### 1.3 — 🔵 RECOMMENDED — none of D12's six arXiv-bearing registry entries carries `arxiv_verified`, which `READINESS_GATES.md` names as a Gate-1 pass criterion (round-8 1.3 REPRODUCES)

- **Gate:** CitationIntegrity
- **Location:** `src/core/citations.py`; `docs/READINESS_GATES.md:33`
- **Observed:** Gate 1 "Passes iff … Every registry entry has `arxiv_verified == True` AND `doi_verified == True` (where applicable)" and "Blocks on … A registry entry whose `arxiv_verified` is `False` or stale (hash mismatch vs. last fetch)". Measured this round: the field exists on **8 of 652** entries; **369** entries carry an arXiv ID and **8** of those have `arxiv_verified: True`. All six D12 arXiv entries (`MeiburgLessaSoldati2025`, `coqq2022`, `UnruhCBO2025`, `EchenimMhalla2021`, `RashidHasan2017`, `Degenne2025Kernels`) have no such key.
- **Evidence:** `_eval_citation_integrity` self-documents the divergence ("DOI fetch-and-verify is deferred to Stage 13; this is the registry-coverage check"), so the gate cannot fail on it, and D12's `CitationIntegrity` gate reads `passed — all bibkeys registered`. The consequence: the four fresh fetches I performed this round land in `docs/citation_verifications.jsonl` and nowhere the gate reads.
- **Expected:** Set `arxiv_verified` on the `promote_primary_sources.py` path, or amend `READINESS_GATES.md` Gate 1 to state the deferral the evaluator actually implements. One of the two documents is wrong.
- **Fix:** One writer change or one doc paragraph.
- **Cache:** fresh-fetch (four re-verified independently this round)

*(All 14 bibitems worked. All 14 are in `CITATION_REGISTRY`, all 14 are cited, no bibitem is uncited, and all 14 hit `docs/citation_verifications.jsonl` at the **current** `bibitem_hash` with verdict `match` — I recomputed every hash rather than trusting the key. I did not stop there: I independently fresh-fetched four arXiv abstracts this round and compared character-for-character — `2510.04070` "Markov kernels in Mathlib's probability library", Rémy Degenne, 5 Oct 2025; `2512.05878` "Complex Bounded Operators in Isabelle/HOL", Dominique Unruh then José Manuel Rodríguez Caballero, 5 Dec 2025; `2510.08672` "A Formalization of the Generalized Quantum Stein's Lemma in Lean", Meiburg/Lessa/Soldati, 9 Oct 2025; `2207.11350` "CoqQ: Foundational Verification of Quantum Programs", Zhou/Barthe/Strub/Liu/Ying, 22 Jul 2022. All four `match`; four records appended under reviewer `adversarial-reviewer-round9`. One nit recorded in the `coqq2022` record and not raised as a finding: the bibitem pairs year 2022 (the arXiv year) with DOI `10.1145/3571222` (the POPL'23 publication) — two different events in one entry, but neither is a wrong target.)*

---

### 2.1 — 🟡 REQUIRED — `MATHER_1982_GRADIENT_REDUCTION` now has its `DEPENDS_ON` edge and Gate 3 evaluates non-vacuously, but `human_verified_date` is still `None` and the fix is uncommitted (round-8 2.2 half-closed)

- **Gate:** ParameterProvenance
- **Location:** `src/core/provenance.py:3727-3742` (uncommitted); `src/core/provenance.py:33-56`; graph node `paper:D12`
- **Observed:** The edge half is genuinely done and I verified it end-to-end rather than reading the diff. `PAPER_DEPENDENCIES['D12']` now exists with `'parameters': ['MATHER_1982_GRADIENT_REDUCTION']`, `extract_depends_on_edges` gained an explicit-parameter branch, and the live graph now carries `paper:D12 --DEPENDS_ON--> param:MATHER_1982_GRADIENT_REDUCTION` (`OUT {'CLAIMS': 1, 'DEPENDS_ON': 1, 'HAS_FIGURE': 3, 'CITES_SOURCE': 14}`). Gate 3 correspondingly moved from `passed — no parameter dependencies declared` to `blocked — 1 parameters lack human_verified_date`. That is exactly the behaviour round-7 2.4 asked for.
- **Evidence:** What remains: (a) `'human_verified_date': None` — `READINESS_GATES.md` is explicit that "LLM verification unblocks computation; human verification unblocks paper submission", and this is the numeral D12 §3.2's entire convention argument turns on; (b) the whole fix is **uncommitted** (` M src/core/provenance.py`, ` M scripts/build_graph.py` in `git status`), so it does not exist at `ac66444f` and a fresh clone reproduces round-8 2.2 verbatim.
- **Expected:** Human verification through the provenance dashboard, then commit.
- **Fix:** One dashboard sign-off plus a commit.
- **Cache:** n/a

### 2.2 — 🟡 REQUIRED — the 2.1 remediation flipped D12's ComputationCorrectness gate to `blocked` on a false premise: all four registered formulas ARE tested, and the VERIFIES extractor cannot see it because the tests call them through a module alias

- **Gate:** ComputationCorrectness
- **Location:** `scripts/build_graph.py:3462-3511` (`extract_verifies_edges`); `tests/test_bundle_formulas_d11_d12.py:25`; `src/core/provenance.py:3731` (uncommitted)
- **Observed:** Registering `PAPER_DEPENDENCIES['D12']` made Gate 4 evaluate for the first time. It now says `blocked — 0 formulas bounds-only + 4 formulas untested`. The four are `phonon_psd_gradient_factor`, `phonon_psd_gamma`, `f_link_bath_referred`, `assignment_fidelity`. **Every one of them is tested**, in `tests/test_bundle_formulas_d11_d12.py`.
- **Evidence:** The test module does `from src.core import formulas as F` and calls `F.phonon_psd_gradient_factor(...)`. The PythonTest node's `referenced_names` therefore reads `['F', 'F.phonon_psd_gradient_factor', …]`, and `extract_verifies_edges` looks names up in `formula_name_to_id`, which is keyed on the **bare** name. No match, no `VERIFIES` edge. (`param_name_to_id` handles exactly this case — `param_name_to_id.setdefault(tail, n['id'])` — the formula index does not.) Measured across the whole graph:
  ```
  test modules losing VERIFIES edges to dotted formula refs: 4
     test_bundle_formulas_d11_d12   109 unmatched dotted formula refs
     test_nonlinear_efe              20
     test_quantum_network            19
     test_cosmological_perturbations 12
  distinct formulas that lose coverage this way: 70
  VERIFIES edges into the four D12 formulas: 0, 0, 0, 0
  ```
  So Gate 4 is unreliable project-wide for any test module written in the alias style, and D12 is the first bundle to be *blocked* by that blindness. The blocker is fail-closed, which is the safe direction — but a gate that names a false reason will drive the wrong remediation (writing tests that already exist).
- **Expected:** Mirror the parameter branch: `formula_name_to_id.setdefault(raw.rsplit('.', 1)[-1], …)` when the dotted head resolves to a formulas-module alias. Then re-read Gate 4 for D12 before treating it as a real blocker.
- **Fix:** Three lines in `extract_verifies_edges`.
- **Cache:** n/a

### 2.3 — 🔵 RECOMMENDED — "attains every PSD reduction between 0 and 56 %" states an unattained supremum as attained, in four artifacts (round-8 2.3 REPRODUCES)

- **Gate:** ParameterProvenance
- **Location:** `papers/D12/paper_draft.tex:448-450`; `src/core/formulas.py:11541-11543`; `lean/…/BolometricFloors.lean`; `Lit-Search/Phase-6E/primary-sources/Mather1982.abstract.txt`
- **Observed:** Unchanged. "the largest amplitude reduction the closed form **admits** is … $33\,\%$" and "attains \emph{every} PSD reduction **between $0$ and $56\,\%$**".
- **Evidence:** I recomputed rather than inheriting: $\gamma(r,n)$ decreases strictly in $r$ with $\lim_{r\to\infty}\gamma = n/(2n+1) = 4/9$, so $1-4/9 = 55.556\,\%$ is a supremum, approached and never attained; likewise $1-\sqrt{4/9} = 33.333\,\%$. The project's own test file is still the only artifact that says so ("the maxima, which are SUPREMA — approached as r -> infinity, never attained"). Reporting $55.56$ as $56$ additionally claims the unattainable interval $(55.56\,\%, 56\,\%)$.
- **Expected:** "attains every PSD reduction strictly below $1-n/(2n+1) = 55.6\,\%$"; "the supremum of the amplitude reduction is $33\,\%$".
- **Fix:** One clause at `:449`, mirrored into three artifacts.
- **Cache:** n/a

### 2.4 — 🔵 RECOMMENDED — the $r^{-2}$ in $r^{-(n+1)}$ is still attributed to $G$, which supplies only $r^{-(n-1)}$ (round-8 2.4 REPRODUCES)

- **Gate:** ParameterProvenance
- **Location:** `papers/D12/paper_draft.tex:426-430`; `src/core/formulas.py:11525-11530`; `lean/…/BolometricFloors.lean`
- **Observed:** Unchanged: "the physical re-referencing of $G$ via $G \propto T^{n-1}$ --- the two differ only in bookkeeping and yield the same expression, since $r^{-(n-1)}\cdot r^{-2} = r^{-(n+1)}$."
- **Evidence:** With $F(t,n)=\frac{n}{2n+1}\frac{t^{2n+1}-1}{t^n-1}$ and $t=1/r$, $F(1/r,n)=\frac{n}{2n+1}\frac{r^{2n+1}-1}{r^{n}-1}\,r^{-(n+1)}$ exactly. The second reading needs **two** re-referencing factors and only the primary-source cache names both: "Using G ∝ T^(n-1), G(T_s) = G(T_b) · r^-(n-1) **and T_s² = T_b² · r^-2**". The paper, `formulas.py` and the Lean each name only the $G$ half and then produce $r^{-2}$ from nowhere.
- **Expected:** "the re-referencing of $T^2$ and $G$ from the bath to the bolometer, via $T_s^2 = T_b^2 r^{-2}$ and $G \propto T^{n-1}$".
- **Fix:** One clause at `:428`, mirrored into two artifacts.
- **Cache:** n/a

### 2.5 — 🔵 RECOMMENDED — neither γ function carries a `Lean:` reference and `formula_grounding` is structurally blind to a function with none (round-8 2.5 REPRODUCES, third round)

- **Gate:** ComputationCorrectness
- **Location:** `src/core/formulas.py:11497-11556`; `scripts/validate.py::check_formula_grounding`
- **Observed:** `f_link_bath_referred` and `phonon_psd_gradient_factor` still carry no `Lean:` line; the second offers `Provenance:` instead. Pipeline Invariant #4 and the Stage-2 gate both say "Every function must reference a Lean theorem".
- **Evidence:** `check_formula_grounding` iterates the references that *exist*, so a function with zero references contributes zero entries and is silently clean. Note this compounds 2.2 above: the two mechanisms that could have connected these functions to evidence — a `Lean:` tag and a `VERIFIES` edge — are both absent, and Gate 4 now blocks on the second while the first is unenforced. The paper does **not** overclaim: it says the identity is verified "in `tests/test\_bundle\_formulas\_d11\_d12.py`" and never that it is kernel-checked.
- **Expected:** A `Lean:` line on both (`phononPSDGamma` / `phononPSD_eq_phononPSDGamma_one` for the first; an explicit `Lean: none — literature-quoted closed form, not formalized` for the second), plus a `formula_grounding` sub-check that every public function carries ≥1 reference.
- **Fix:** Two docstring lines plus one sub-check.
- **Cache:** n/a

*(**Round-8 2.1 is genuinely closed and I re-derived it from scratch.** `γ(2,4) = 0.4731481481…`; $1-\gamma = 52.685\,\% \to 52.7$; $1-\sqrt\gamma = 31.214\,\% \to 31.2$; exact difference $21.471$ points → $21.5$; $52.7-31.2 = 21.5$; $53-31 = 22$. The paper now reads "differ by $21.5$ points ($52.7 - 31.2$; the rounded figures quoted above give $22$)" and `BolometricFloors.lean:220` carries the identical construction with both parentheticals labelled. `formulas.py` quotes no difference at all, so it cannot disagree. All three artifacts agree. This sentence's numeral was wrong in rounds 6, 7 and 8; it is right now.)*

---

### 3.1 — 🔵 RECOMMENDED — `correlated_pair_sigma_eq_sqrt_two_mul` REPRODUCES a fourth time: bare real-√ algebra described as quantifying a witness pair

- **Gate:** LeanProofSubstance
- **Location:** `papers/D12/paper_draft.tex:318-320`; `lean/SKEFTHawking/Detection/NEPAlgebra.lean:471-474`
- **Observed:** Both artifacts byte-identical to rounds 6, 7 and 8. Paper: "exhibits a fully-correlated pair whose total power is twice what quadrature predicts, with the discrepancy quantified exactly as $\sqrt{2}$ by \thm{correlated\_pair\_sigma\_eq\_sqrt\_two\_mul}." Theorem: `theorem correlated_pair_sigma_eq_sqrt_two_mul (S : ℝ) : √(4 * S) = √2 * √(S + S)`, closed by two `show … by ring` rewrites and `Real.sqrt_mul`.
- **Evidence:** The statement mentions neither `IsUncorrelatedAt`, nor the witness functionals of `quadrature_uncorrelated_hypothesis_load_bearing`, nor `sigma_eq_responsivity_nep_sqrt_enbw`. It is an identity about reals that survives any edit to the witness — the project's own P3 anti-pattern. The witness itself is substantive; the missing piece is the link between the two, and the paper's verb "quantified" asserts that link exists.
- **Expected:** "the corresponding output-noise ratio is $\sqrt2$, recorded arithmetically as \thm{…}", or a short corollary instantiating the identity at the witness's own `V`/`Vtot`.
- **Fix:** One clause at `:319-320`.
- **Cache:** n/a

### 3.2 — 🔵 RECOMMENDED — `enbw_dcGain_hypothesis_load_bearing` establishes non-droppability through Lean's `x/0 = 0` junk value, and the paper does not say so (round-8 3.2 REPRODUCES)

- **Gate:** LeanProofSubstance
- **Location:** `papers/D12/paper_draft.tex:283-285`; `lean/SKEFTHawking/Detection/FilterFloors.lean:484-497`
- **Observed:** "The hypothesis that DC gain is nonvanishing is non-droppable, witnessed by \thm{enbw\_dcGain\_hypothesis\_load\_bearing}."
- **Evidence:** I read the proof. The witness is $h(y)=y-1$ on $[0,2]$; the proof establishes `(∫ y in 0..2, (y-1)) = 0` and closes via `enbw_of_dcGain_eq_zero`, i.e. `enbw h 2 = 0` and `0 * 2 < 1/2`. The refutation is carried entirely by Lean's division-by-zero convention. As a statement about the *formal* theorem this is correct and I am not calling it unsound — dropping `hDC` does make the Lean statement false. But the physical ENBW at vanishing DC gain **diverges** rather than vanishing, so this is the one "load_bearing" witness in the paper that does not survive translation back to an instrument.
- **Expected:** One parenthetical: "(the witness has exactly zero DC gain, where `enbw` takes its junk value; the hypothesis is non-droppable as a matter of the formal statement)".
- **Fix:** One parenthetical at `:285`.
- **Cache:** n/a

*(No other class-3 findings, and I re-derived the substrate rather than inheriting round 8's. All 181 `\thm{}`/`\texttt{}` identifiers in the draft were resolved against the live graph's Lean node index: **160 resolve to project declarations**; the 21 that do not are Mathlib names (`HasSubgaussianMGF.measure_ge_le`, `bayesRisk_le_bayesRisk_comp`, `cdf`, `gaussianReal`), the three core axioms, literal words (`sorry`, `native_decide`, `maxHeartbeats`, `tsum`, `TODO`, `infotheo`), paths, and version strings. **Zero** intersection with the 576-node `PlaceholderMarker` set. **Zero** occurrences of any of the 13 stale names the anchor list enumerates. Substrate re-verified from `lean_deps.json` axiom closures, not by sampling: **582** declarations across the 13 root-imported modules (Detection 214, Control 200, Electrothermal 168); `axiom_deps_project` empty for **every** one; `axiom_deps_core` is `{Classical.choice, Quot.sound, propext}` for 578 and `{propext}` for 4 — no violations. A regex sweep of all 427 `theorem`/`lemma` bodies in those modules found **zero** discharged by `rfl`/`trivial`/`simp`/`decide`/`norm_num` alone. The four `sorry` / `native_decide` / `maxHeartbeats` / `axiom` token hits are all inside docstrings asserting their own absence. The abstract's PhysLib claim is exactly true and I checked it a different way from round 8: **zero** of the 582 declaration records mention `QuantumInfo` or `Physlib` anywhere, while **47** consume `SKEFTHawking.QuantumNetwork.*` — the project's own Helstrom/POVM substrate, precisely as §2.4 says.)*

---

### 4.1 — 🔵 RECOMMENDED — the duplicate-registry-key finding REPRODUCES, and D10's copy of a D12 source still has the author order reversed against the arXiv record I fetched this round

- **Gate:** CrossPaperConsistency
- **Location:** `src/core/citations.py`; `papers/D10/paper_draft.bbl:187-192`
- **Observed:**
  ```
  "A Formalization of the Generalized Quantum Stein's Lemma in Lean"
     Meiburg2025PhysLib       arxiv=2510.08672  used_in=[D8]
     MeiburgLessaSoldati2025  arxiv=2510.08672  used_in=[D12]
     leanQI2025               arxiv=None        used_in=[D10]
  "Complex Bounded Operators in Isabelle/HOL"
     UnruhCBO2025             arxiv=2512.05878  used_in=[D12]
     isabelleCBO              arxiv=None        used_in=[D10]
  "Markov kernels in Mathlib's probability library"
     Degenne2025MarkovKernels arxiv=2510.04070  used_in=[I3]
     Degenne2025Kernels       arxiv=2510.04070  used_in=[D12]
  ```
  30 normalised titles in the 652-entry registry are carried under ≥2 keys.
- **Evidence:** Gate 2's literal blocking condition ("a bibkey whose `arxiv_id` differs across papers") is still not triggered, because the *keys* differ rather than the IDs. Two residues, both unchanged: (a) the D10 entries carry `arxiv: None` while `papers/D10/paper_draft.bbl` renders the same arXiv IDs D12 asserts, so the registry is strictly less informative than the artifact it governs; (b) `papers/D10/paper_draft.bbl:187-192` still renders `isabelleCBO`'s authors as "J. M. R. Caballero and D. Unruh" — reversed against the arXiv record I fetched **this round** ("Dominique Unruh, José Manuel Rodríguez Caballero") and against D12's own `UnruhCBO2025`, which is correct.
- **Expected:** One registry entry per primary source, aliased; a duplicate-normalised-title/DOI/arXiv detector in `validate.py`; correct D10's author order.
- **Fix:** Alias `leanQI2025 → MeiburgLessaSoldati2025` and `isabelleCBO → UnruhCBO2025`; re-render D10's `.bbl`.
- **Cache:** fresh-fetch (`UnruhCBO2025` re-fetched this round; D12's rendering is the correct one)

*(No cross-paper contradictions. I re-walked the seams the anchor list names rather than inheriting: D9↔D12 — D9 owns the relaxation floor and D12's composites consume it without re-proof, and D9 states no electrothermal device model; D9↔D12 Helstrom/POVM — `quarter_sqrtFidelity_sq_le_povmAvgError` and `IsBinaryPOVM` appear in no draft but D12's and live under `SKEFTHawking/QuantumNetwork/`, i.e. project-side, exactly as both drafts say, and I confirmed the 47 consuming declarations from `lean_deps.json`; D1↔D12 — `johnsonNyquistPSD` is D12-side only and D1's draft contains no occurrence of "Johnson". The paper's "two open prior-art checks" count is consistent across the abstract `:63`, §5 `:631` and the conclusion `:689`, and matches `prior_art_novelty.md §3`.)*

---

### 5.1 — 🟡 REQUIRED — the ENBW constant at `:298` is now correct, but the paragraph's disambiguation still leaves the numerical contradiction it exists to resolve standing (round-8 5.1 half-closed)

- **Gate:** NarrativeGrounding
- **Location:** `papers/D12/paper_draft.tex:293-302`
- **Observed:** The constant is fixed and I verified all four sites: `:48` (abstract), `:267` (§3.1 headline), `:298` (this parenthetical, now `\tfrac12` with `(\thm{enbw\_mul\_window\_ge\_half})` appended) and `:673` (conclusion) all say $1/2$, matching `FilterFloors.lean:197-201` (`1 / 2 ≤ enbw h T * T`). The paper contains no remaining `\tfrac14`; the only `1/4` is the Le Cam prefactor at `:137`, which is correct. **What round-8 5.1 also asked for is still missing**: the paragraph never says why $\mathrm{ENBW} = 1/(4\tau)$ does not contradict $\mathrm{ENBW}\cdot T \ge 1/2$.
- **Evidence:** A reader who sets $T=\tau$ reads $\mathrm{ENBW}\cdot T = 1/4 < 1/2$ and concludes the theorem is false. The paper's whole answer is "the two are different quantities". They are, but not for the reason the sentence gives. `enbw` is defined at `FilterFloors.lean:124-125` as
  ```lean
  noncomputable def enbw (h : ℝ → ℝ) (T : ℝ) : ℝ :=
    (∫ x in (0:ℝ)..T, h x ^ 2) / (2 * (∫ x in (0:ℝ)..T, h x) ^ 2)
  ```
  — a *time-domain functional of the impulse response on the same window `T` that multiplies it*. The instrument convention's $1/(4\tau)$ is a frequency-domain noise bandwidth of an infinite-support single-pole response over the whole line. They are different functionals, not one functional at two arguments. I computed the paper's own `enbw` for the single-pole response $h(x)=e^{-x/\tau}$ on $[0,T]$:
  ```
  T/tau =  0.01   enbw(h,T)*T = 0.50000
  T/tau =  1.00   enbw(h,T)*T = 0.54099
  T/tau =  5.00   enbw(h,T)*T = 1.26696
  T/tau = 20.00   enbw(h,T)*T = 5.00000
  ```
  so the single-pole filter satisfies the theorem at every window and never approaches $1/4$. The honest disambiguation is one sentence about *which functional*, not about *which symbol*.
- **Expected:** "…and the two are different quantities — indeed different functionals: \thm{enbw} is a time-domain ratio over the same finite window that multiplies it, whereas $1/(4\tau)$ is a frequency-domain bandwidth of an infinite-support response. Evaluating \thm{enbw} on a single-pole response over $[0,T]$ gives $\ge 1/2$ at every $T$, as the theorem requires."
- **Fix:** One sentence at `:298-301`.
- **Cache:** n/a

### 5.2 — 🟡 REQUIRED — the `prxquantum` → `prx` switch is defensible but its stated rationale is false, and the false rationale is now in three artifacts including an eight-line comment at the top of the submitted source

- **Gate:** NarrativeGrounding
- **Location:** `papers/D12/paper_draft.tex:1-9`; `papers/D12/bundle_metadata.json` `notes` (final sentence block); `scripts/compile_bundle_pdf.py:76-84` and `:113-116`
- **Observed:** The source header asserts that `revtex4-2` "silently drops `prxquantum` … and typesets in the default APS style, **which means every page count, column measurement and Overfull-hbox reading taken against it was against the wrong configuration**." `bundle_metadata.json` repeats it ("all prior Stage-9 layout measurements were against default APS style … Layout numbers must be re-measured"). `compile_bundle_pdf.py` repeats it twice, as the justification for a hard-failing gate and as that gate's failure message ("every layout number above is against the wrong configuration").
- **Evidence:** The operative clause is false, and I proved it by compiling both variants in a private scratch directory (never inside `papers/`), three passes each, same `.bbl` and `figures/`:
  ```
  variant           pages  Overfull  Underfull  LaTeX Warnings  undefined
  aps,prx           10     0         87         0               0
  aps,prxquantum    10     0         87         1               0

  pdftotext -layout of the two PDFs: byte-identical (md5 7235b883f7ebce2730de2f60216296c8 both)
  raw PDF bytes differing: 56  (the /CreationDate, /ModDate and /ID only)
  ```
  The reason is in the class itself. `aps4-2.rtx:633` reads
  ```tex
  \def\rtx@apsprx{% \class@info{APS journal PRX selected}%
  }%
  ```
  — the `prx` hook is a **no-op**, as are `pra` (the fallback when a journal option is dropped), `prb`, `prc`, `prd`, `pre`, `prab`, `prper`, `prmaterials` and `physrev`. Only `prl`, `prfluids` and `rmp` change layout. So `prxquantum`-dropped-to-`pra` and `prx` are the *same configuration*; the two logs differ only in `Unused global option(s): [prxquantum]` / `No journal specified, using default pra` versus `Selecting journal prx`. **No Stage-9 measurement was ever invalidated, and switching to `prx` did not restore anything.**
  Two consequences: (a) the paper's own source now carries a documented false claim about the project's measurement history, in the file a referee receives; (b) `compile_bundle_pdf.py`'s hard-fail gate is worth keeping — a dropped option *is* a typo signal — but it ships with a rationale that is wrong for the one case it cites as measured evidence, and its failure message will tell a future author to discard valid numbers. Separately, "`prx` is PRX Quantum's parent style" is an editorial-lineage claim, not a typesetting one: `revtex4-2` 4.2f (2022-06-05) predates PRX Quantum's REVTeX support entirely and implements no substyle for it. The trailing "the final PRX Quantum submission must be re-measured against the journal's own class" is correct and should stay.
- **Expected:** Replace the invalidation claim with what is true: `prxquantum` is not a substyle in this `revtex4-2`, so it was dropped and the document fell back to `pra`; `prx` is also a no-op hook, so the two typeset identically (verified: identical text, identical page count, 56-byte PDF delta) and the declaration is a correctness/`-halt-on-warning` hygiene fix, not a layout fix. Same correction in `bundle_metadata.json` and in both `compile_bundle_pdf.py` sites.
- **Fix:** Rewrite three comment blocks; keep the gate.
- **Cache:** n/a

### 5.3 — 🔵 RECOMMENDED — the conclusion's composite-bite sentence still never received the §4 qualification that three rounds of findings were spent adding (round-8 5.3 REPRODUCES)

- **Gate:** NarrativeGrounding
- **Location:** `papers/D12/paper_draft.tex:680-684`; cf. `:567-573`
- **Observed:** "Two of those composites are shown to bite at a \emph{non-degenerate} operating point --- matched budget $2$, ceiling $22/25$ … --- and not merely at the signal-free zero-budget point, which is what the Birnbaum--Feller lower bound buys."
- **Evidence:** Byte-identical across `b6830c7d`, `ad844e42`, `c60e83ad`, `837f335e` and HEAD. It survived round-6 5.1 and round-7 5.2, both of which were about exactly this claim in §4. It is not false — the composite theorems do conclude `assignmentFidelity e0 e1 ≤ 22/25` at a signal-carrying point. What it omits is the four lines §4 now spends saying the value is the single-mechanism one and the bound is not sharper. A referee reading the conclusion takes "the composites bite" to mean the *composite bound* is what bites, which §4 explicitly disclaims. Unpropagated remediation, not a new claim defect.
- **Expected:** One clause: "…, though the numerical value is the single-mechanism one (§4.2)".
- **Fix:** One clause at `:682`.
- **Cache:** n/a

### 5.4 — 🔵 RECOMMENDED — the $f$-divergence generator and "the affinity is its complement" still disagree by a factor 2, in three artifacts (round-8 5.4 REPRODUCES)

- **Gate:** NarrativeGrounding
- **Location:** `papers/D12/paper_draft.tex:243-245`; `CITATION_REGISTRY['LuccioliDegenne2024'].notes`; `Lit-Search/Phase-6E/primary-sources/LuccioliDegenne2024.abstract.txt`
- **Observed:** Unchanged: "The squared Hellinger distance is an $f$-divergence with generator $f(t) = (\sqrt{t}-1)^2$, and the affinity is its complement".
- **Evidence:** With $f(t)=(\sqrt t-1)^2$, $D_f(P\|Q)=\sum_i q_i(\sqrt{p_i/q_i}-1)^2 = 2-2\,\mathrm{BC}$, so the complement relation $H^2 = 1-\mathrm{BC}$ holds only for the $\tfrac12$-normalised squared Hellinger, generator $\tfrac12(\sqrt t-1)^2$. The registry note repeats the unnormalised generator verbatim. The paragraph's actual claim — that the general $f$-divergence DPI plausibly subsumes the affinity DPI — is **unaffected**, since BC is an affine decreasing function of $D_f$ under either normalisation.
- **Expected:** $f(t)=\tfrac12(\sqrt t-1)^2$, or "an affine function of" in place of "the complement of".
- **Fix:** One symbol at `:243`, mirrored into two artifacts.
- **Cache:** n/a

### 5.5 — 🔵 RECOMMENDED — the paragraph that declares the TestingLowerBounds check *blocking* is the only one that withholds its name and citation (round-8 5.5 REPRODUCES)

- **Gate:** NarrativeGrounding
- **Location:** `papers/D12/paper_draft.tex:631-634`
- **Observed:** "First, an out-of-Mathlib Lean project on information theory and hypothesis testing is under active development; a Bayes-binary-risk--divergence lower bound there would be substantially our Le Cam floor, and it is the highest prior-art risk in this work." No `\cite`.
- **Evidence:** §5 names and cites the project two paragraphs earlier (`:602-603`) and the conclusion names it again (`:689-690`). Only the paragraph that declares the obligation withholds the name, so a reader acting on that paragraph alone cannot discharge it. `prior_art_novelty.md §3` names it correctly.
- **Expected:** Insert `\textsc{TestingLowerBounds}~\cite{LuccioliDegenne2024}` at `:631`.
- **Fix:** One insertion.
- **Cache:** n/a

*(Every other class-5 check passes and I re-derived each. **Round-8 5.2 is genuinely closed and I recomputed it independently**: $Q(1)=0.15865525393145707$, $x = 2Q(1)/(1-2Q(1)) = 0.4647947734915442$, and $x/(2(1+x)) = 0.1586552539314571 = Q(1)$ to 15 digits. The paper now says "$x = 2Q(1)/(1-2Q(1)) = 0.46479\ldots$" and "$t/T_1 > 0.4648$"; `CompositeReadoutCeilings.lean:675-678` carries the same closed form, the same 0.4648, and a dated note recording that 0.4651 solved to $Q=0.158726$ rather than $Q(1)$. Both artifacts agree with my computation. **No unhedged first-claim exists**: every "first" in the draft either refutes one or is the phrase "first-class deliverable"; `:620-629` is scoped to "the ecosystems we actually assessed", carves out the Coq and Isabelle libraries, and `:616` explicitly disclaims absence in HOL Light. **The numeric surface re-checks**: $e^5 = 148.41$; $\gamma(2,4) = 0.47315$; $n/(2n+1) = 4/9 = 0.444$; $1-\sqrt{4/9} = 33.3\,\%$; $|1-3| = 2$; $Q(1) \ge 3/25 \Rightarrow 1-Q(1) \le 22/25$.)*

---

### 6.1 — 🔵 RECOMMENDED — `avgError_ge_gaussianQ_sharp` carries an undisclosed ordering binder $\mu_0 \le \mu_1$ (round-8 6.1 REPRODUCES)

- **Gate:** AssumptionDisclosure
- **Location:** `papers/D12/paper_draft.tex:202-206`; `lean/SKEFTHawking/Detection/GaussianThreshold.lean:785-788`
- **Observed:** "The headline is the sharp separation-budget floor \thm{avgError\_ge\_gaussianQ\_sharp}: no threshold placement achieves an average error below $Q\!\left(\Delta\mu/2\sigma\right)$." §2.3 nowhere states the branch-ordering convention.
- **Evidence:** The signature is
  ```lean
  theorem avgError_ge_gaussianQ_sharp {μ₀ μ₁ σ t z₀ : ℝ}
      (hσ : 0 < σ) (hμ : μ₀ ≤ μ₁) (hz : (μ₁ - μ₀) / (2 * σ) ≤ z₀) :
  ```
  `hσ` is a positivity condition a reader supplies; `hμ` is a **sign convention** on the separation, used directly in the proof (`hhnn : 0 ≤ (μ₁ - μ₀) / (2 * σ) := div_nonneg (by linarith) …`). Without it a signed $\Delta\mu$ with $\mu_1 < \mu_0$ makes $Q(\Delta\mu/2\sigma) > 1/2$ and the statement fails. Low severity because the convention is the obvious one and no claim weakens — but §5 enumerates twelve modelling Props by name while this binder of the section's own headline theorem goes unnamed.
- **Expected:** "for a signal branch of the larger mean ($\mu_0 \le \mu_1$, an explicit binder)".
- **Fix:** One parenthetical at `:203`.
- **Cache:** n/a

*(The rest of class 6 re-verified from signatures. The "Twelve propositions" enumeration at `:640-648` is exactly right — I enumerated every `Prop`-valued declaration across the 13 modules and got 12, with no thirteenth, and the 10/1/1 split holds. `poisson_avgError_floor`'s sole hypothesis really is `hδ : IsCountRule δ`, so `:140` is exact. The composite-bite theorems inherit their composites' full binder lists including the attribution hypotheses, which `:543` discloses.)*

---

### 7.1 — 🟡 REQUIRED — D12's entire numeric surface still sits outside every freshness and provenance gate (round-8 7.1 REPRODUCES)

- **Gate:** NumericalFreshness
- **Location:** `papers/D12/paper_draft.tex` throughout; `scripts/validate.py --check count_literals`, `--check numerical_literals`
- **Observed:**
  ```
  count_literals      ✓ D12 — no count literals found
  numerical_literals  ✓ D12 — no inline unit-bearing literals in body
  counts_fresh        ✓ 1/1 checks passed
  ```
  while the draft carries at least `0.4731`, `53 %`, `31 %`, `52.7`, `31.2`, `21.5`, `22`, `33 %`, `56 %`, `22/25`, `3/25`, `0.15866`, `148`, `148.41`, `51.40`, `3491.96`, `76 %`, `95 %`, `50 %`, `80 %`, `0.37`, `1.19`, `0.4648`, `0.46479`, `4/9`, `0.444`.
- **Evidence:** No `REPORTS` edge exists from `paper:D12` to any `CountMetric`, and `papers/D12/tables/` contains no autogen table feeding the prose, so Gate 9 passes on an empty set. The `DEPENDS_ON` edge added this round (2.1) reaches exactly one parameter and no derived value. Every numeral above is correct today — I checked them all — but three consecutive rounds have found a wrong one in this unguarded surface (23 → 21.5-with-a-false-derivation → 0.4651), and the only thing that caught each was a reviewer.
- **Expected:** A `\pipelinevalue{}` chain or a `papers/D12/tables.py` spec for the derived quantities, so a wrong numeral fails a check; or at minimum register the derived values in `PARAMETER_PROVENANCE` as `DERIVED` and emit `DEPENDS_ON` for each, extending the mechanism 2.1 just built for one parameter.
- **Fix:** One tables spec plus a macro retrofit.
- **Cache:** n/a
- **Lane:** `prose`

### 7.2 — 🔵 RECOMMENDED — spelled-out tree counts REPRODUCE outside the macro chain; both correct today, both invisible to `count_literals`

- **Gate:** NumericalFreshness
- **Location:** `papers/D12/paper_draft.tex:101` ("Thirteen root-imported modules"), `:640` ("Twelve propositions carry modelling or admissibility content")
- **Observed:** Both unchanged. Both are hand-written counts of tree contents, not `\input{counts.tex}` macros.
- **Evidence:** Both verified correct at review time — `lean/SKEFTHawking.lean` root-imports exactly 13 `Detection.*`/`Electrothermal.*`/`Control.*` modules, and exactly 12 `Prop`-valued declarations exist across them. `count_literals` reports "no count literals found" because the detector does not match spelled-out numerals, so the class is unguarded across every bundle. (`bundle_metadata.json`'s frozen declaration numeral remains correctly removed; the live count has moved 580 → 582 since round 8, which is why removing it was right.)
- **Expected:** `d12modules` / `d12modelprops` macros in `docs/counts.tex`, or extend `count_literals` to catch spelled-out numerals before "modules"/"propositions"/"theorems".
- **Fix:** Two macros, or one regex.
- **Cache:** n/a

---

### 8.1 — 🔴 BLOCKER — the round-8 blocking-closure guard is bypassable two ways, and I demonstrated both: a one-key ledger record with no status, evidence, commit or date closes a 🔴 BLOCKER, and the closing document sets its own severity

- **Gate:** FixPropagation
- **Location:** `scripts/build_graph.py:1652-1668` (the new guard), `:1521-1543` (`_load_supersession_ledger`), `:1603-1614` (severity inference)
- **Observed:** The guard is
  ```python
  if status == 'fixed' and severity in ('critical', 'major', 'blocker'):
      if finding_id not in supersessions:
          status = 'open'
  ```
  Its only test is **membership**. It never inspects the record.
- **Evidence:** I ran `extract_review_finding_nodes` against a monkeypatched `PROJECT_ROOT` in a temp directory (nothing in the repo was touched) with four 🔴 BLOCKER headings all containing the word FIXED, and a ledger containing three records:
  ```
  finding                                       ledger record                      resulting status
  …:D11:9.1  (bare record)                      {"finding_id": "…"}                'fixed'   ← BYPASS
  …:D11:9.2  (accepted)                         {"finding_id":…, "status":"accepted"}  'accepted'
  …:D11:9.3  (empty status)                     {"finding_id":…, "status":""}      ''
  …:D11:9.4  (no record — control)              —                                  'open'    ← guard works
  ```
  **9.1 is the bypass.** `{"finding_id": "review:…:9.1"}` is a single key; `ledger.get('status', status)` then falls back to the heading-derived `'fixed'`. No evidence, no commit, no note, no date, no `superseded_by`. `ledger_ids_resolve` only checks the ID resolves to a node; `accepted_findings_carry_rationale` only inspects records whose status is literally `"accepted"`, so a statusless record is checked by nothing at all. Closing a blocker still costs one line of self-authored JSON — a lower bar than the "the re-run is evidence" rule Stage 13 states.

  **Second bypass — the non-blocking carve-out is exploitable exactly as you suspected.** `severity` is read from the *closing document's own* heading and body:
  ```python
  severity = 'advisory'
  for glyph, sev in _SEV_GLYPHS.items():          # 🔴→critical 🟡→major 🔵→minor
      if glyph in heading or glyph in body[:600]: severity = sev; break
  if _BLOCKER_RE.search(heading) or _BLOCKER_RE.search(body[:1000]): severity = 'critical'
  ```
  and `_BLOCKER_RE = r'\*\*?BLOCKER\*\*?|severity:\s*critical|severity:\s*BLOCKER'` requires the word to be **asterisk-wrapped**, which this project's report format does not do. So a closing document that simply omits the glyph mints `advisory` and keeps heading-parse closure. Demonstrated in the same harness:
  ```
  ### 5.1 - FIXED - the ENBW constant was wrong; corrected     → sev=advisory status=fixed
  ### 8.1 - resolved - the closure machinery now requires ...  → sev=advisory status=fixed
  ```
  Both closed with zero ledger records. Had the round-6 reconstruction been written this way — or with 🔵 glyphs — all seven of its findings would still self-close today and the guard would be inert. The party seeking closure controls the field that decides whether closure is checked.
- **Expected:** (a) require the ledger record to carry `status == 'fixed'` **and** a non-empty `evidence`/`note` **and** a `commit` before it may close a blocking finding — the same bar `accepted_findings_carry_rationale` already applies to acceptance; (b) do not read severity from the closing document — a finding's severity should come from the finding, so heading-parse closure should be forbidden for *any* status transition to `fixed`, with the ledger as the sole closure channel; (c) a `validate.py` leg asserting that no `ReviewFinding` is `fixed` without a ledger record carrying evidence.
- **Fix:** Two conditions in the guard plus one validator; the guard's own comment already argues for exactly this.
- **Cache:** n/a

### 8.2 — 🔴 BLOCKER — a Stage-13 review document that mints zero graph nodes is invisible to every guard in the suite; nothing requires a round to leave a document, and `stage13_review_doc` is `null` and read by nothing

- **Gate:** FixPropagation
- **Location:** `scripts/build_graph.py:1415-1418` (`_REVIEW_SECTION_RE`); `scripts/validate.py:3336-3355` (`findings_reach_the_graph`, the `_unresolved_files` leg); `papers/D12/bundle_metadata.json:19`; `scripts/bundle_source_manifest.py:141`
- **Observed:** This is the fail-open path that survives. It needs no mutation — only a heading that drifts, or a round that never writes.
- **Evidence:** Three legs, all measured.
  1. **The heading regex is narrow and silent.** `^###\s+(?:Class\s+)?(\d+(?:\.\d+)?)\s*[—\-–]\s*(.+?)$`. Tested:
     ```
     MATCH   '### 1.1 — 🔴 BLOCKER — …'        NOMATCH '#### 1.1 — 🔴 BLOCKER — …'
     MATCH   '### 1.1 - 🔴 BLOCKER — …'        NOMATCH '### 1.1: 🔴 BLOCKER — …'
     MATCH   '### 8.10 — 🔴 BLOCKER — …'       NOMATCH '### Finding 1.1 — 🔴 BLOCKER — …'
                                               NOMATCH '### 1.1.2 — 🔴 BLOCKER — …'
     ```
     I then wrote a complete review document declaring **4 BLOCKERs** in the round-9 output path, with those non-matching headings, and ran the extractor against a monkeypatched `PROJECT_ROOT`:
     ```
     review file present on disk, declares 4 BLOCKERs; ReviewFinding nodes minted: 0
     ```
  2. **The orphan guard structurally cannot see it.** `findings_reach_the_graph`'s second leg builds `_by_file` **from the findings**:
     ```python
     for n in _findings:
         stem = (n['meta'].get('review_file','')).rsplit("/",1)[-1].removesuffix(".md")
         if stem in _roster: _by_file.setdefault(stem, []).append(m.get("inferred_bundle"))
     _unresolved_files = [f for f, vals in _by_file.items() if not any(v == f for v in vals)]
     ```
     A file with zero findings never enters `_by_file`, so it can never be reported unresolved. The leg catches "findings that fail bundle inference"; it is blind by construction to "a file that produced no findings".
  3. **Nothing checks that a round left a document.** `papers/D12/bundle_metadata.json` carries `"last_stage13_review": "2026-07-31"` alongside `"stage13_review_doc": null`. Grepping `scripts/` for `stage13_review_doc` returns exactly one hit — `bundle_source_manifest.py:141`, which *writes* `None` at bundle creation. No reader anywhere.

  This is not hypothetical damage: it is the mechanism by which D12 rounds 4, 5 and 6 completed with no document, and by which round 6's 5 BLOCKERs and 9 REQUIREDs became invisible until round 8 reconstructed 7 of the 14 from commit messages. Downstream, every consumer reads "no findings" as "no problems": Gate 11 evaluates `passed — no review findings`, the heatmap reads GREEN, `bundle_readiness` writes `blockers_open: 0`, `freshness_stale: false` follows from that zero, and `readiness_submission_gate` reports the bundle clear. The whole suite stays green.
- **Expected:** (a) a `validate.py` leg that every `papers/AutomatedReviews/*/<roster-code>.md` mints ≥1 `ReviewFinding` node, failing on zero — the one predicate the current guard cannot express; (b) make `stage13_review_doc` mandatory and non-null whenever `last_stage13_review` is set, and require the named file to exist and mint ≥1 finding for that bundle at or after that date; (c) widen `_REVIEW_SECTION_RE` or add a shape lint on the reviewer's output.
- **Fix:** One check leg plus one metadata invariant. Round-8's QI item 3 proposed the round counter; the zero-node leg is cheaper and catches the same class.
- **Cache:** n/a

### 8.3 — 🟡 REQUIRED — `readiness_submission_gate`'s own `ImportError` handler still returns `passed=True`, ten lines above the early return the round-8 fix corrected in the same function

- **Gate:** FixPropagation
- **Location:** `scripts/validate.py:4305-4312` vs `:4313-4324`
- **Observed:** The round-8 fix rewrote `if not gates:` to `passed=False` in both readiness checks and rewrote both `bundle_readiness` `ImportError` handlers. It did not touch the `build_graph` `ImportError` handler immediately above:
  ```python
  try:
      from build_graph import build_graph_json
  except ImportError as exc:
      return CheckResult(passed=True, details=[
          Detail("import", True, f"build_graph not available ({exc}); skipping", warning=True)])
  ```
- **Evidence:** Mutation-tested in-process by shadowing `sys.modules['build_graph']` with a module lacking `build_graph_json`:
  ```
  MUT-A  build_graph importable but has no build_graph_json
     readiness_submission_gate      passed=True   ← "skipping"
     readiness_verdicts_agree       passed=False
     bundle_metadata_matches_graph  passed=False
     graph_integrity                passed=False
  ```
  I am reporting this at REQUIRED rather than BLOCKER honestly: three siblings fail on the same mutation, so the *suite* still goes red, unlike round-8 8.2 where everything went green. I also re-ran round-8's own MUT-B (delete `readiness_gates.evaluate_all_gates` → zero `ReadinessGate` nodes) and **the fix holds**: `readiness_submission_gate passed=False`, `readiness_verdicts_agree passed=False`, `bundle_metadata_matches_graph passed=False`. What survives is the *class*, not this instance's blast radius — this is the fourth consecutive round in which "not evaluated is spelled passed" has been fixed at the named site and not swept. Round-8's own QI item 2 proposed the AST sweep that would have caught this handler; I ran that sweep and it finds 12 `CheckResult(passed=True)` returns inside exception handlers across the module.
- **Expected:** `passed=False` here; then run the AST sweep as a test so the class closes.
- **Fix:** Three lines plus one test.
- **Cache:** n/a
- **Lane:** `infra`

### 8.4 — 🟡 REQUIRED — `ledger_ids_resolve` still fails open on its own exception, and the 8.1 guard has now made the ledger the *only* thing that can close a blocker (round-8 8.4 REPRODUCES verbatim)

- **Gate:** FixPropagation
- **Location:** `scripts/validate.py:3421-3424`
- **Observed:** Unchanged this round:
  ```python
  except Exception as exc:  # pragma: no cover
      roster_details.append(Detail(
          "ledger_ids_resolve", True,
          f"ledger integrity scan skipped ({type(exc).__name__}: {exc})", warning=True))
  ```
  Forty lines earlier the sibling handler reads "FAIL, not pass: this guard exists to make invisible findings visible, so a build error in the scan must not be indistinguishable from a clean scan (D12 round-6 finding 8.2 — the original `warning=True` failed open)." Both were shipped in the same commit.
- **Evidence:** The path is live: `ledger_ids_resolve` reads `_g`, a local bound inside the *previous* `try` block, so any failure of the graph build raises `UnboundLocalError` here and this handler swallows it. The severity is higher than it was at round 8 for a structural reason: as of the round-8 8.1 guard, the supersession ledger is the sole channel by which a blocking finding can be closed, and `ledger_ids_resolve` is the only check on that ledger's referential integrity. A guard on the closure channel that reports green when it did not run is the wrong shape for that job.
- **Expected:** `passed=False` on this handler, and `_g` computed once outside both blocks so the dependency is explicit.
- **Fix:** Two lines.
- **Cache:** n/a

### 8.5 — 🟡 REQUIRED — 27 blocking-severity `accepted` findings are still invisible to Gate 11; the round-8 8.6 remediation added a rationale floor but not visibility

- **Gate:** FixPropagation
- **Location:** `scripts/readiness_gates.py:690-704`; `scripts/validate.py::check_accepted_findings_carry_rationale`
- **Observed:** The new check is real and it passes: `140 accepted record(s) checked, 0 without a written rationale of at least 40 characters`. That closes the "unenforced discipline" half. The other half is untouched: `_eval_fix_propagation` partitions `accepted` out of `open_findings` before the severity filter runs, so an accepted finding at `critical`/`major` never enters `r.blockers`, never sets `state = 'blocked'` and never drives `needs-recheck`. Measured on the live graph: **27** `ReviewFinding` nodes are `status: accepted` at blocking severity (8 critical, 19 major) — the same 27 round 8 counted.
- **Evidence:** For **D12 specifically the exposure is nil** — all 21 D12 ledger records are `status: fixed` and none is `accepted` — so this neither helps nor hurts this bundle's gate. It is a project-wide surface, and it is now the cheapest closure path in the system: after 8.1, `fixed` at blocking severity requires a ledger record and `accepted` requires a ledger record with ≥40 characters of prose, and only the second removes the finding from the gate's blocking set entirely.
- **Expected:** Surface accepted-at-blocking-severity in `r.notes` (not only in the evidence line), and require an `accepted` record at blocking severity to name a decision-maker as well as a rationale.
- **Fix:** One notes string plus one validator condition.
- **Cache:** n/a
- **Lane:** `infra`

### 8.6 — 🟡 REQUIRED — the `freshness_stale` remediation is correct and complete in code but has not been run or committed, so 7 bundles including D12 still assert the forbidden pair and `validate.py` is red

- **Gate:** FixPropagation
- **Location:** `scripts/bundle_readiness.py:688-702` (uncommitted); `scripts/validate.py:3951-3956` and `:3925-3934` (uncommitted); `papers/*/bundle_metadata.json`
- **Observed:** Both halves of round-8 8.5 are addressed in the working tree and I verified each by execution rather than by reading the diff. `write_metadata_counts` now **derives** the field — `"freshness_stale": not (n_blockers == 0 and stages_green)` — making `LATE_PHASE6_ABSORPTION_PROTOCOL.md:184` the definition rather than a convention. `check_bundle_metadata_matches_graph` gained the matching leg, and its `if not mp.is_file(): continue` is now `drift += 1` with an explicit FAIL, so a deleted metadata blob no longer passes green. Both are right.
- **Evidence:** Neither has been applied. The writer has not been re-run, so the blobs on disk are unchanged and the new checker fails on them:
  ```
  bundle_metadata_matches_graph  passed=False — 7 with drift
    D11 18 blockers   D12 23 blockers   D7 12   I2 19   I3 16   L1 1   L3 5   (all freshness_stale=false)
  ```
  And none of it is committed (` M scripts/bundle_readiness.py`, ` M scripts/validate.py`), so at `ac66444f` round-8 8.5 reproduces unmodified. Stage 13's own precondition — "Ensure Stages 1–12 are all green" — is therefore not met on the tree I reviewed.
- **Expected:** `uv run python scripts/bundle_readiness.py`, then re-run `validate.py`, then commit.
- **Fix:** One command plus a commit.
- **Cache:** n/a
- **Lane:** `infra`

### 8.7 — Class 8 ProductionRunHealth: PASS, no findings

D12 makes no simulation-evidence claim. I grepped the draft for "Monte Carlo", "numerical evidence", "simulation", "simulated" and "we simulate" — **zero** occurrences; every quantitative statement is a kernel-checked rational enclosure or a closed-form evaluation. The graph carries 18 `ProductionRun` nodes and **zero** edges touching `paper:D12`, which is correct for a pure-formalization bundle. (Aside, not a D12 finding and unchanged since round 8: 17 of the 18 `ProductionRun` nodes carry `status: unknown`, which is a blocking status under Gate 8 for any paper that ever links one.)

---

## Verification of the round-8 repairs you asked me to attack

| item | verdict | how I checked it |
|---|---|---|
| **5.1 ENBW constant** | **fixed** | `FilterFloors.lean:197-201` states `1 / 2 ≤ enbw h T * T`. All four paper sites (`:48`, `:267`, `:298`, `:673`) say $1/2$; zero `\tfrac14` remain; the only `1/4` is the Le Cam prefactor at `:137`, which is correct. Residual half of round-8 5.1 is open — see finding 5.1. |
| **5.2 crossover** | **fixed** | Recomputed from scratch: $Q(1)=0.15865525393145707$, $2Q/(1-2Q)=0.4647947734915442$, and $x/(2(1+x))$ returns $Q(1)$ to 15 digits. Paper `:571-573` and `CompositeReadoutCeilings.lean:675-678` both carry the closed form and 0.4648. |
| **2.1 21.5 points** | **fixed, all three artifacts agree** | $52.685 \to 52.7$, $31.214 \to 31.2$, exact difference $21.471 \to 21.5$, $52.7-31.2 = 21.5$, $53-31=22$. Paper `:442-444` and `BolometricFloors.lean:220` state both parentheticals and label them; `formulas.py` quotes no difference, so it cannot disagree. |
| **the 3 ledger records** | **supported by round 7's text** | I read `2026-07-31-1524-.../D12.md` rather than the ledger's own summary. Line 331 records round-6 2.1 as "fixed, verified — I bisected $1-\gamma(r,4)=0.30$ and got $r^*=1.1913538$; at the pinned 1.19135 the reduction is 0.29999643"; line 332 records round-6 2.2 as "fixed, verified — max relative error over the 28-point grid is 3.7e-16"; line 335 records round-6 4.1 as "fixed, and the re-key is correct … I recomputed it from the live graph and got exactly 67". Each ledger `evidence` string is a faithful compression. The decision **not** to file for 1.1, 5.1, 8.1 and 8.2 is also defensible on round 7's text: 5.1 and 8.1 are labelled PARTIALLY FIXED with live residuals, and 1.1/8.2 were "not independently re-verified". |
| **blocker rise 9 → 23 (D12)** | **real, and arithmetically exact** | 9 (metadata at round-8 time) + 4 (reconstruction findings the guard correctly reopened: `1210:1.1`, `1210:5.1`, `1210:8.1`, `1210:8.2`) + 10 (round-8's own report, which had not been written when the metadata was computed: `1730:{2.1, 2.2, 5.1, 5.2, 7.1, 8.1, 8.2, 8.3, 8.4, 8.5}`) = 23. Per-round breakdown from the live graph: `0829:1, 1210:4, 1524:8, 1730:10`. Project-wide the guard reopened only **6** findings (4 D12, 1 D1, 1 paper26). **Not an over-correction.** |
| **blocker rise 14 → 18 (D11)** | **real** | D11's 18 are `1059:5, 1530:9, 1652:4` — all from genuine review documents, **zero** reopened by the guard. The +4 is exactly the round-8 D11 report. |
| **8.1 guard** | **partially effective; two bypasses** | See finding 8.1. Control case (no ledger record) correctly reopens; bare-record and self-authored-severity cases do not. |
| **8.2 fail-open** | **round-8's mutation is closed; a wider path survives** | MUT-B (`evaluate_all_gates` removed) now fails all three checks. Surviving: the zero-node review document (8.2, whole-suite green) and `readiness_submission_gate`'s own `ImportError` handler (8.3, check-level only). |
| **`prxquantum` → `prx`** | **right call, false rationale** | See finding 5.2. Both variants compile to identical text (same md5), 10 pages, 0 Overfull, 87 Underfull; 56 bytes of PDF delta, all timestamps. `aps4-2.rtx:633` shows `\rtx@apsprx` is a no-op, as is the `pra` fallback. `compile_bundle_pdf.py`'s hard-fail on dropped options is a good gate; its rationale is wrong for the case it cites. |

## Compliance note

I ran no git-mutating command — only `git log`, `git status`, `git show`, `git diff`, `git rev-parse`. I ran no `pdflatex` inside `papers/`; both compiles were into a private scratch directory with copied inputs. All mutation testing was in-process (monkeypatched `PROJECT_ROOT` into a `tempfile.mkdtemp` tree, and `sys.modules` shadowing), and every temp tree was removed; **no repository file was mutated or restored**. Files I wrote: this report, and the four mandated verification records appended to `docs/citation_verifications.jsonl`.

## QI Candidate

**Systemic: the closure machinery now has exactly one channel, and that channel is unguarded on both ends.**

Round 8 correctly removed heading-parse closure for blocking findings, which makes the supersession ledger the sole way a BLOCKER can close. But (a) nothing validates a ledger record used for `fixed` — a single-key JSON object suffices (8.1); (b) `ledger_ids_resolve`, the only referential guard on that ledger, fails open on its own exception (8.4); (c) severity, which decides whether the guard applies at all, is still read from the closing party's own heading glyph (8.1); and (d) upstream of all of it, a review round that mints zero nodes leaves nothing for any of this to guard (8.2). Narrowing the closure path without hardening it concentrates risk rather than reducing it. Proposed: one schema validator for `status: fixed` records mirroring the `accepted` rationale floor; `passed=False` on the ledger scan's handler; severity taken from the finding rather than the closing document; and a check that every roster-named review file mints ≥1 node.

**Also, cross-cutting, and each already measured above:**

1. **`VERIFIES` extraction is blind to module-alias calls** (2.2). `from src.core import formulas as F` + `F.foo(...)` yields `referenced_names = ['F.foo']`, which the formula index does not key on — 4 test modules, 160 unmatched references, 70 formulas losing coverage, and Gate 4 now *blocking* a bundle whose tests exist and pass. The parameter index already solves this with one `setdefault`. Any gate that reads test coverage is unreliable until this is fixed.
2. **"Not evaluated" is still spelled "passed", fourth round running** (8.3). An AST sweep of `scripts/validate.py` finds 12 `CheckResult(passed=True)` returns inside exception handlers. Each round fixes the handlers a finding names and no others. The sweep should be a test.
3. **A dropped `\documentclass` option is worth failing on, but the reasoning shipped with the gate is wrong** (5.2). Nine of the twelve APS journal substyles in `revtex4-2` 4.2f are no-op hooks, so dropping one usually changes nothing. Generalising from one un-measured case into three artifacts — the paper's own source, the bundle metadata and a gate's failure message — is the same "narrative inflation around correct mechanics" the D12 anchor list names as this bundle's standing failure mode.
4. **The numeric surface of a pure-formalization bundle is guarded by nothing** (7.1). `count_literals` and `numerical_literals` both report D12 clean while ~26 derived numerals sit in its prose, and three consecutive rounds each found one of them wrong. The `DEPENDS_ON` mechanism built this round for one parameter is the natural place to extend.
