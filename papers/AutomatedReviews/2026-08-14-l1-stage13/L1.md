---
paper: L1
bundle: L1
bundle_target: L1
tier: 2
reviewer: adversarial-reviewer
model: claude-opus-5
review_date: 2026-08-14T18:50:43Z
readiness_gates_version: 1
kind: full-adversarial
prior_reviews:
  - papers/AutomatedReviews/2026-05-11-1550-bundle-stage13/L1.md
  - docs/audits/stage13_attribution_sweep_2026-06-10.md#l1
counts_snapshot: 22682 thm / 22656 substantive / 2040 modules / 0 sorry
lean_axioms_GravitationalWaves: propext, Classical.choice, Quot.sound (0 project axioms)
compile_L1: OK pages=4 tex_errors=0 unresolved_refs_in_pdf=0
---

# Adversarial Review — L1

## Summary

**4 BLOCKER, 7 REQUIRED, 6 RECOMMENDED.** L1 is **not** ready for operator
final review. The closure of the two stale `2.1` majors was **correct** — the
provenance notes at HEAD are honest and the Vergeles re-attribution holds
(§0) — but the sweep that closure was part of never reached the layer beneath
it: the paper's premise, `c_GW = c·√χ_vest` attributed to Volovik, is
unsourced and is **negated by the project's own Phase 5y H1 deep research**,
which computes `s₂ = c` exactly, calls it structural and not tunable, and
records it as GW170817-**compatible** (1.1, 5.1). Independently, L1 asserts
four times that the natural range `[0.1,10]` and the GW170817-compatible
window are "entirely disjoint"; the compatible window is strictly **inside**
`[0.1,10]`, and the paper's own theorem `ligo_satisfied_at_chi_one` and its
own headline figure both say so (3.1, propagated to D3/paper25 as 4.1).
Gates blocked: CitationIntegrity, CrossPaperConsistency, LeanProofSubstance,
NarrativeGrounding.

Stages 1–12 are green enough to review on (`counts_fresh` PASS,
`bundle_consistency` PASS, `citation_primary_sources_present` PASS, L1
compiles clean at 4 pages, `GravitationalWaves` is kernel-pure with zero
project axioms and zero `sorry`). `readiness_submission_gate` FAILs corpus-wide
but records L1 as "all P1 passed" — a state this review overturns, and which
2.3 shows was partly a measurement artifact.

---

## §0 — Verification of the closure you asked me to audit

**Verdict: the closure of `review:2026-05-01-bundle-stage13:L1:2.1` and
`review:2026-05-11-1550-bundle-stage13:L1:2.1` was CORRECT.** Independently
re-derived, not taken on trust:

- `src/core/provenance.py:2760-2812` (`GW.CHI_VEST_NATURAL_LOWER`) and
  `:2814-2858` (`_UPPER`) at HEAD carry `tier: 'PROJECTED'`, a `detail` that
  says in capitals that `[0.1,10]` is "a PROJECT-ADOPTED naturalness window,
  not a published derivation", and an `llm_verified_notes` recording the
  2026-06-10 keyword sweep. The `human_verified_notes` are explicitly labelled
  `PROPOSED HUMAN-VERIFY NOTE (rewritten 2026-06-10 ... supersedes the Phase 7b
  sub-wave 7b.0 pre-fill of 2026-05-01, which misattributed the range to a
  Vergeles 2025 RPA bubble-integral derivation)`. The old remedy — "human-verify
  against a Vergeles 2025 RPA bubble integral" — is not merely stale, it is
  refuted in the artifact itself. Closing those two findings was right.
- The re-attribution is honest in the paper too: `paper_draft.tex:174-187`
  states the window is "a *project-adopted* naturalness window", "a modeling
  choice of this project, not a published derivation", and confines Vergeles to
  ECP lattice unitarity. `CITATION_REGISTRY['Vergeles2025']` agrees
  (title "Unitarity of 4D Lattice Theory of Gravity", `provides:` unitarity only).
- The residual you describe — operator sign-off on the pre-written note — is
  real and is the **only** part of the old 2.1 still live. Re-filed as 2.1 below
  with a Verify that actually fails on it (`parameter_provenance --strict` does
  **not** cover it: that check exempts `PROJECTED` tier, so the obvious command
  passes vacuously).

**But the closure did not, and could not, reach the defect underneath it.**
Findings 1.1 / 3.2 / 5.1 show that the honest disclosure of *where the window
came from* sits on top of a formula (`c_GW = c√χ_vest`) that has no source at
all, and on a Lean grounding chain (`χ_vest ← chi_RPA ←
VestigialSusceptibility.lean`) that exists only in prose — `GravitationalWaves`
has **zero** project-level dependencies. Fixing 2.1 does not touch that.

**Files I touched (rule 11 disclosure).** `docs/citation_verifications.jsonl` —
three append-only `match` records (Volovik2024Vestigial, Volovik2022Counting,
Wetterich2022) per finding-class-1 step 7, superseding 2026-05-01 `wrong_venue`
records that describe pre-fix bibitems. Backup of the pre-append file:
`<scratchpad>/citation_verifications.jsonl.bak`. No other file was modified; no
git state was touched.

---

## Findings

### 1.1 — 🔴 BLOCKER — The second-sound-graviton identification is attributed to `Volovik2024Vestigial`, which does not contain it

- **Severity:** blocker
- **Lane:** `research`
- **Verify:** `cd "$REPO" && grep -qE "2601\.00639|2410\.04392" papers/L1/paper_draft.tex` — asserts that the paper cites the source its own Phase 5y H1 research names as the origin of the second-sound-graviton identification. Exits 1 at HEAD.
- **Needs-operator:** `now` — this is a physics-scope call (retarget the citation, or restate the identification as this project's own construction), not a mechanical repair.
- **Gate:** CitationIntegrity
- **Location:** `papers/L1/paper_draft.tex:75-84` (intro), `:33-37` (abstract); bibitem at `:389-392`
- **Observed:** The paper states "Volovik's 2024 vestigial-fluid review~\cite{Volovik2024Vestigial} proposes this directly", where *this* is "the spin-2 graviton of the broken-symmetry phase is the second-sound mode of the vestigial fluid".
- **Evidence:**
  - Fresh-fetch `arxiv.org/abs/2312.09435` (2026-08-14): title "Fermionic quartet and vestigial gravity", G.E. Volovik. Abstract in full concerns two-step transitions, the 4-fermion condensate, and metric-before-tetrad condensation. It contains no occurrence of "second sound", "graviton", "spin-2", "susceptibility", or any propagation speed.
  - Cached Crossref record `Lit-Search/Phase-6a/primary-sources/Volovik2024Vestigial.json` — same abstract, same absence.
  - The project's own deep research, `Lit-Search/Phase-5y/Phase 5y Hypothesis 1 — Kronecker-Anomaly Second-Sound Graviton as Dark Energy Candidate.md`, §3 heading: "**Massless Graviton as Second Sound (from arXiv:2601.00639)**", and §3(d): "The identification of the graviton as Landau second sound is novel to Volovik's 2024–2026 programme" — sourced throughout to `arXiv:2601.00639` and `arXiv:2410.04392`. **Neither is in L1's bibliography.**
  - The same document characterises the mode as a **scalar** ("the second-sound *scalar* graviton", closing spine statement; §3(d) places it beside Hořava–Lifshitz's spin-0 scalar graviton). L1's abstract says **spin-2**.
- **Expected:** The bibitem cited for a claim contains the claim. Either cite `arXiv:2601.00639` / `arXiv:2410.04392`, or state plainly that the spin-2 vestigial-second-sound identification with a χ-dependent speed is this project's synthesis and not any published proposal.
- **Fix:** Add the two arXiv sources to `CITATION_REGISTRY` + the L1 bibliography and re-anchor `paper_draft.tex:78-84`; or rewrite `:75-84` to attribute the identification to the project. Reconcile "spin-2" against the source's scalar characterisation. Note also that the cached primary source is an **abstract only** — L1 cites no work whose full text the repo holds for this claim.
- **Cache:** fresh-fetch (record appended 2026-08-14, verdict `match` on the *bibitem metadata*; the attribution defect is orthogonal and is recorded in the record's `detail`).

### 1.2 — 🟡 REQUIRED — Round-1 BLOCKER 1.1 (Volovik 2024 page + title) was never propagated to the two Lean files L1 points readers at

- **Severity:** required
- **Lane:** `lean`
- **Verify:** `cd "$REPO" && ! grep -qE "119, ?564" lean/SKEFTHawking/GravitationalWaves.lean lean/SKEFTHawking/VestigialSusceptibility.lean` — asserts the retracted page number is gone from both cited Lean sources. Exits 1 at HEAD.
- **Gate:** CitationIntegrity
- **Location:** `lean/SKEFTHawking/GravitationalWaves.lean:67`, `lean/SKEFTHawking/VestigialSusceptibility.lean:24`
- **Observed:** `GravitationalWaves.lean:67` — `- Volovik, "Vestigial gravity", JETP Lett. 119, 564 (2024).`; `VestigialSusceptibility.lean:24` — `- Volovik, JETP Letters 119, 564 (2024)`. Both wrong page and (for the first) wrong title.
- **Evidence:** Verified metadata is JETP Letters **119, 330–334** (2024), title "Fermionic quartet and vestigial gravity" (arXiv fetch 2026-08-14 + cached Crossref + `CITATION_REGISTRY['Volovik2024Vestigial']`). Round-1 finding `review:2026-05-01-bundle-stage13:L1:1.1` fixed the *paper* bibitem to 330 and is recorded `fixed`; the Lean sources still carry 564. `paper_draft.tex:376-380` directs readers to `GravitationalWaves.lean` as the reproducibility artifact, so the wrong metadata is reader-facing. I could not confirm whether any Volovik JETP Lett. 119, 564 (2024) exists — `arxiv.org/a/volovik_g_1` returned HTTP 404 (verdict `fetch_failed` on that sub-question); no such entry exists in `CITATION_REGISTRY`.
- **Expected:** Both Lean reference blocks carry the registry-verified metadata.
- **Fix:** Replace both lines with `Volovik, "Fermionic quartet and vestigial gravity", JETP Lett. 119, 330 (2024); DOI 10.1134/S002136402460006X`.
- **Cache:** fresh-fetch.

### 1.3 — 🔵 RECOMMENDED — The 2026-05-11 fresh-fetch results were never written to the citation cache

- **Severity:** recommended
- **Lane:** `infra`
- **Verify:** `cd "$REPO" && uv run python -c "import json; r=[json.loads(l) for l in open('docs/citation_verifications.jsonl')]; latest={}; [latest.__setitem__(x['bibkey'],x) for x in r if x.get('paper')=='papers/L1/paper_draft.tex']; bad=[k for k,v in latest.items() if v.get('verdict')!='match']; assert not bad, bad"` — asserts the *most recent* L1-scoped cache record for every bibkey carries `match` (the file is append-only, so the superseded 2026-05-01 `wrong_venue` rows remain and a naive scan over all rows would fail forever). Exited 1 before this review's appends; exits 0 after.
- **Gate:** CitationIntegrity
- **Location:** `docs/citation_verifications.jsonl`
- **Observed:** `papers/AutomatedReviews/2026-05-11-1550-bundle-stage13/L1.md` §1 documents ten fresh Crossref/arXiv fetches, all `match`. None reached the cache. The only L1-scoped records were the two 2026-05-01 `wrong_venue` entries describing pre-fix bibitems, so every subsequent reviewer saw L1's two most-cited bibitems as failing.
- **Evidence:** `lookup_latest('Volovik2024Vestigial', …)` returned `wrong_venue @ 2026-05-01` at HEAD-before-this-review; the review document three rounds later says `match (fresh-fetch)`. Cache/document divergence of 95 days on a 90-day staleness horizon.
- **Expected:** A review that fetches appends; the cache is the shared artifact, the review doc is not.
- **Fix:** Nothing further for L1 (three records appended by this review). The systemic half is the QI candidate below.
- **Cache:** fresh-fetch.

---

### 2.1 — 🟡 REQUIRED — `GW.CHI_VEST_NATURAL_LOWER` / `_UPPER` still lack `human_verified_date` (the correct residual)

- **Severity:** required
- **Lane:** `research`
- **Verify:** `cd "$REPO" && uv run python -c "from src.core.provenance import PARAMETER_PROVENANCE as P; m=[k for k in ('GW.CHI_VEST_NATURAL_LOWER','GW.CHI_VEST_NATURAL_UPPER') if not P[k].get('human_verified_date')]; assert not m, m"` — exits 1 at HEAD. **Do not substitute `validate.py --check parameter_provenance --strict`:** that check exempts `PROJECTED` tier, so it passes on these two entries and would record absence-of-measurement as a pass.
- **Needs-operator:** `now` — this is the sign-off itself.
- **Gate:** ParameterProvenance
- **Location:** `src/core/provenance.py:2785` and `:2835`
- **Observed:** `'human_verified_date': None` on both, with a complete and (as verified in §0) accurate `PROPOSED HUMAN-VERIFY NOTE` awaiting approval.
- **Evidence:** Direct read of HEAD, lines 2760–2858. `llm_verified_date: '2026-04-25'` is set on both; the 2026-06-10 keyword-sweep note is present in `llm_verified_notes`.
- **Expected:** `human_verified_date` populated after the operator reads the note.
- **Fix:** `uv run python scripts/provenance_dashboard.py` → `GW.CHI_VEST_NATURAL_LOWER` / `_UPPER` → approve or edit-and-approve the pre-written note. **Read finding 5.1 first** — if the identification's premise is retargeted, the sentence in the note that anchors the window to "the one-loop bubble normalization O(Λ²/16π²)" may need to change with it.
- **Cache:** N/A.

### 2.2 — 🟡 REQUIRED — A placeholder chosen to produce its own conclusion is presented as "the project's conservative upper bound", and as closing a recovery path

- **Severity:** required
- **Lane:** `prose`
- **Verify:** `cd "$REPO" && ! grep -q "closed in the Lean rather than" papers/L1/paper_draft.tex` — asserts the paper no longer claims recovery-path 3 is *closed* by a theorem whose hypothesis is an unfounded placeholder. Exits 1 at HEAD.
- **Gate:** ParameterProvenance
- **Location:** `papers/L1/paper_draft.tex:339-356`; parameter at `src/core/provenance.py:2927-2941`
- **Observed:** The paper writes "this path is closed in the Lean rather than argued: `vestigial_dispersion_below_ligo_at_inspiral_peak` proves that at the project's conservative upper bound $|\gamma| \leq 10^{-30}$ … the correction … stays inside the GW170817 tolerance, so it does not lift the falsification."
- **Evidence:** `PARAMETER_PROVENANCE['GW.GAMMA_H_VESTIGIAL_DEFAULT']` — `tier: 'PROJECTED'`, `source: 'Phase 6a Wave 2 placeholder …'`, `detail: '… placeholder value chosen so the dispersion correction stays well below the GW170817 cap.'`, `human_verified_date: None`. The word **placeholder** is used by all four upstream artifacts: `constants.py:5749`, `provenance.py:2931-2934`, `src/gravitational_waves/dispersion_relation.py:27`, and the Lean docstring `GravitationalWaves.lean:436`. The Lean theorem takes `|γ| ≤ 1e-30` as a *hypothesis*; it proves nothing about whether the bound holds.
  Note the provenance line is explicit that the value was **chosen to give the conclusion the paper then reports as a result** — that is circular, and "conservative" is unearned.
- **Expected:** Either the bound is derived (or measured), or the prose says the path is *not* closed, only that a placeholder-sized correction would not lift the falsification.
- **Fix:** Restore the disclosure that `10^{-30}` is a placeholder, and downgrade "closed in the Lean" to a conditional ("*conditional on* |γ| ≤ 10⁻³⁰, which is a Phase 6a scaffold value, not a derived or measured bound"). The 2026-05-11 finding 5.2 recommended the opposite edit; that recommendation was wrong and is superseded here (see S.1).
- **Cache:** N/A.

### 2.3 — 🔵 RECOMMENDED — L1 has zero `DEPENDS_ON` edges, so Gate 3 evaluates vacuously for it

- **Severity:** recommended
- **Lane:** `infra`
- **Verify:** `cd "$REPO" && uv run python scripts/build_graph.py --json 2>/dev/null | uv run python -c "import sys,json; g=json.load(sys.stdin); d=[l for l in g['links'] if l['type']=='DEPENDS_ON' and l['source']=='paper:L1']; assert d, 'paper:L1 has no DEPENDS_ON edges to Parameter nodes'"` — exits 1 at HEAD.
- **Gate:** ParameterProvenance
- **Location:** graph node `paper:L1`; `src/core/constants.py:5735-5749` (`GW_PARAMS`)
- **Observed:** `DEPENDS_ON` totals 65 edges across the corpus, all from `paper1/2/3/4/12` and `D12`. `paper:L1` has none — and neither does `paper25_gravitational_waves`. Yet L1 quotes `CHI_VEST_NATURAL_LOWER/UPPER`, `C_GW_DEVIATION_*`, `C_GW_TWO_SIDED_CAP`, `GW170817_PEAK_FREQ_HZ` and `GAMMA_H_VESTIGIAL_DEFAULT`.
- **Evidence:** `readiness_submission_gate` reports `⚠ L1 — all P1 passed; advisory on: NumericalFreshness, FixPropagation, ParameterProvenance, ComputationCorrectness`. "ParameterProvenance passed" for L1 means "the evaluator found nothing to evaluate", which is not the same claim and reads identically on the heatmap.
- **Expected:** The parameters a paper quotes are edges the evaluator can see.
- **Fix:** Add L1 (and paper25) to `PAPER_DEPENDENCIES` with its `GW.*` parameter set; the `paper:L1` node already records `"detail": "Filesystem-discovered; no PAPER_DEPENDENCIES entry"`.
- **Cache:** N/A.

---

### 3.1 — 🔴 BLOCKER — "Entirely disjoint" is false: the GW170817-compatible window lies strictly *inside* the natural range, and the paper's own theorem and figure say so

- **Severity:** blocker
- **Lane:** `prose`
- **Verify:** `cd "$REPO" && uv run python -c "import re; t=re.sub(r'\s+',' ',open('papers/L1/paper_draft.tex').read()); h=re.findall(r'entirely disjoint|are disjoint by|fails GW170817 over the entire natural range',t); assert not h, h"` — asserts the paper no longer claims the two intervals are disjoint, nor universal failure over the whole range. Exits 1 at HEAD (4 hits). **Whitespace must be normalised first:** the abstract's occurrence breaks across lines as `entirely\ndisjoint`, so a line-based `grep` sees only 2 of the 4 and would pass while the abstract still carried the claim.
- **Gate:** LeanProofSubstance
- **Location:** `papers/L1/paper_draft.tex:55-60` (abstract), `:117-118`, `:152-154`, `:215-217`, `:310-312`
- **Observed:** Four separate assertions that `[0.1,10]` and `[(1−τ)²,(1+τ)²]` are "entirely disjoint" / "disjoint by 14 orders of magnitude", plus "the leading-order Volovik identification fails GW170817 over the *entire* natural range χ_vest ∈ [0.1,10]".
- **Evidence:**
  - `τ = 3×10⁻¹⁵`, so `[(1−τ)²,(1+τ)²] ≈ [1−6×10⁻¹⁵, 1+6×10⁻¹⁵] ⊂ [0.1, 10]`. The intervals are **nested**, not disjoint. Two intervals also cannot be "disjoint *by* 14 orders of magnitude"; that phrase is a measure *ratio*, not a set relation.
  - The paper's own cited theorem `ligo_satisfied_at_chi_one` (`GravitationalWaves.lean:144`) proves `LigoSatisfied (c_GW_deviation 1)`, and `H_VestigialModeIsGraviton_at_one` (`:332`) discharges the full bundle — at `χ_vest = 1 ∈ [0.1,10]`. The paper states this itself at `:250-254` ("The one value that survives is the singleton").
  - The headline figure `papers/L1/figures/fig_c_GW_vs_ligo_constraint.png` renders the gold "GW170817-compatible (χ_vest = 1 ± 3×10⁻¹⁵)" star **inside** the shaded "Natural χ_vest range [0.1, 10]" band. The figure contradicts the prose.
  - The theorem the claim is hung on, `natural_range_disjoint_from_ligo_window` (`:284-291`), has statement `¬ ∃ χ, (χ = lower ∨ χ = upper) ∧ LigoSatisfied (c_GW_deviation χ)` — a statement about **two points**, not an interval. Its own docstring (`:272-283`) compounds the error: "c_GW_deviation is strictly monotone, so the deviation cannot return to within the cap without leaving the natural range." It returns to within the cap at `χ = 1`, which is inside the range. The theorem *name* is a misnomer.
- **Expected:** The honest and fully-supported claim is a **measure** claim, which the abstract already makes correctly at `:48-51`: the compatible window has measure `4τ ≈ 1.2×10⁻¹⁴` against a natural-range width of 9.9, i.e. a prior mass of `~1.2×10⁻¹⁵`. That claim needs no disjointness and is not false.
- **Fix:** Replace every "entirely disjoint" with the measure-ratio statement; replace "fails over the entire natural range" with "fails everywhere in the natural range except a window of prior mass ~10⁻¹⁵ about χ_vest = 1". Correct the `:272-283` docstring reasoning. Rename `natural_range_disjoint_from_ligo_window` to what it proves (e.g. `natural_endpoints_violate_ligo_window`) — see also 4.1, which must be fixed in the same pass.
- **Cache:** N/A.
- **Note:** This defect was *introduced* by the round-1 fix to BLOCKER 3.1 (which replaced the retired `second_sound_graviton_not_derived_DOF` with this theorem) and survived rounds 2, 3, the 2026-05-11 sweep and the 2026-06-10 attribution sweep. It is the same class as the majors you just closed: a remedy that outran the artifact.

### 3.2 — 🟡 REQUIRED — The `χ_vest ← RPA ← VestigialSusceptibility.lean` grounding chain exists only in prose; `GravitationalWaves` has zero project dependencies

- **Severity:** required
- **Lane:** `lean`
- **Verify:** `cd "$REPO" && uv run python -c "import json; d=json.load(open('lean/lean_deps.json')); gw=[x for x in d if x.get('module')=='SKEFTHawking.GravitationalWaves']; r={n for x in gw for n in (x.get('name_deps_project') or [])}; assert any('VestigialSusceptibility' in n for n in r), 'GravitationalWaves has NO dependency on VestigialSusceptibility'"` — exits 1 at HEAD.
- **Gate:** LeanProofSubstance
- **Location:** `papers/L1/paper_draft.tex:92-97` and `:168-188`; `lean/SKEFTHawking/GravitationalWaves.lean:10-13`, `:1-3`
- **Observed:** The paper says `χ_vest` is "the metric-channel susceptibility of the vestigial phase, **computed** in the random-phase approximation (RPA) in this project's formalization (`VestigialSusceptibility.lean`)", and that `χ_vest` "is the RPA bubble integral with closed form χ_RPA = χ₀/(1 − γ⋆χ₀) … as formalized in `VestigialSusceptibility.lean`".
- **Evidence:**
  - `GravitationalWaves.lean` imports only `SKEFTHawking.Basic`, `SKEFTHawking.LinearizedEFE`, `Mathlib`. Its 35 declarations have **zero** external project dependencies (`lean_deps.json`, all of `name_deps_project` / `type_deps_project` / `value_deps_project`). `χ_vest` in `c_GW` is a free `ℝ`.
  - The module docstring at `:10-13` nonetheless claims it formalizes the speed "from the vestigial-phase metric-channel susceptibility (`VestigialSusceptibility.chi_RPA`) plus the SK-EFT dissipative correction (`SecondOrderSK.GammaH`)". Neither is a dependency.
  - `VestigialSusceptibility.lean` contains `chi_RPA χ_0 γ_star := χ_0 / (1 - γ_star * χ_0)` (a `def`) and `chi_RPA_closed_form`, whose entire proof is `div_mul_cancel₀ _ h`. Nothing evaluates `chi_RPA`, nothing identifies it with `χ_vest`, and nothing derives `[0.1,10]` from it. **No χ_vest is computed anywhere in the repo.**
  - Physics detail: `χ_RPA = χ₀/(1 − γ⋆χ₀)` is the *resummed* susceptibility; the bubble integral is `χ₀`. The paper's `:169-171` conflates the two.
  - The Lean docstring also cites `Lit-Search/Phase-5y/Phase5y_H1_second_sound_graviton.md`, which does not exist (actual file: `Phase 5y Hypothesis 1 — Kronecker-Anomaly Second-Sound Graviton as Dark Energy Candidate.md`).
- **Expected:** Either a Lean bridge (`c_GW (chi_RPA χ₀ γ⋆) c`, or a lemma pinning `χ_vest` to `chi_RPA`), or prose that says `χ_vest` is a free parameter this Letter constrains — which is in fact what the paper does everywhere else.
- **Fix:** Change "computed in the RPA in this project's formalization" to a statement about what the Lean actually holds; correct the `chi_RPA`-is-the-bubble-integral conflation; repair the two false cross-references in the `GravitationalWaves.lean` module docstring and the dead research path.
- **Cache:** N/A.

### 3.3 — 🔵 RECOMMENDED — `second_sound_graviton_natural_range_universally_falsified` binds `c` and `0 < c` and uses neither; c-independence is definitional, never proved

- **Severity:** recommended
- **Lane:** `lean`
- **Verify:** `cd "$REPO" && grep -qE "c_GW .*=.*c_GW_deviation|c_GW_deviation_eq_rel" lean/SKEFTHawking/GravitationalWaves.lean` — asserts a bridging lemma relating `c_GW χ c` to `c_GW_deviation χ` exists. Exits 1 at HEAD.
- **Gate:** LeanProofSubstance
- **Location:** `lean/SKEFTHawking/GravitationalWaves.lean:302-309`, `:107`; `papers/L1/paper_draft.tex:308-310`
- **Observed:** The theorem is `∀ (c : ℝ) (_ : 0 < c) (χ : ℝ) (_ : χ = lower ∨ χ = upper), ¬ LigoSatisfied (c_GW_deviation χ)`. Its proof opens `intro _ _ χ hcase` — `c` and its positivity are discarded unused. The paper presents it as "the frame-independent form … quantifies over all $c > 0$ explicitly".
- **Evidence:** `c_GW_deviation χ := Real.sqrt χ - 1` (`:107`) mentions no `c`. There is no lemma anywhere in the module relating it to `c_GW χ c := c * Real.sqrt χ` — e.g. no `(c_GW χ c - c)/c = c_GW_deviation χ`. So "Δc/c is independent of c" holds because the definition was written without `c`, not because anything proved that this definition *is* Δc/c. The dummy binder adds no content; it decorates a c-free statement to look like a frame-independence result.
- **Expected:** A one-line bridging lemma (`c ≠ 0 → (c_GW χ c - c) / c = c_GW_deviation χ`) would make the frame-independence claim earned rather than assumed, and would make the unused binder unnecessary.
- **Fix:** Add the bridging lemma; either drop the vacuous `c` binder or derive the statement through `c_GW`.
- **Cache:** N/A.
- **Reasoning for the RECOMMENDED rating:** the algebra is one line and evidently true, so the physics is not wrong. What is wrong is that the paper counts a decoration as a result. Borderline against REQUIRED; a future reviewer may reasonably escalate if the abstract leans harder on "frame-independent".

### 3.4 — 🔵 RECOMMENDED — "These are *decidable* inequalities at the level of Lean's real-number type" is at best vacuous and at worst false

- **Severity:** recommended
- **Lane:** `prose`
- **Verify:** `cd "$REPO" && ! grep -q "decidable" papers/L1/paper_draft.tex` — asserts the paper makes no decidability claim about ℝ. Exits 1 at HEAD.
- **Gate:** LeanProofSubstance
- **Location:** `papers/L1/paper_draft.tex:236-237`
- **Observed:** After describing the two endpoint falsifiers' proofs, the paper asserts they are "*decidable* inequalities at the level of Lean's real-number type".
- **Evidence:** `LinearOrder ℝ` in Mathlib fills its `decidableLE` field classically, so the `Decidable` instance is noncomputable and `decide` closes nothing here. The proofs at `GravitationalWaves.lean:201-236` are `Real.sqrt_le_sqrt` + `Real.sqrt_sq` + `linarith`, not `decide`/`native_decide`. In a paper whose selling point is Lean verification, "decidable" is a technical term with a specific meaning, and the sentence reads as claiming a decision procedure that does not exist. Under the charitable classical reading the sentence is instead trivially true of *every* real proposition, and so says nothing.
- **Expected:** Describe the proofs as closing by `linarith` from explicit `√` monotonicity bounds — which is both true and more informative.
- **Fix:** Delete the sentence, or replace with "both reduce to rational arithmetic once √ is bounded, and close by `linarith`."
- **Cache:** N/A.

---

### 4.1 — 🔴 BLOCKER — D3 states as a proved theorem a proposition that is false, and that `second_sound_graviton_natural_range_universally_falsified` does not say

- **Severity:** blocker
- **Lane:** `prose`
- **Verify:** `cd "$REPO" && uv run python -c "import re; h=[(f,x) for f in ('papers/D3/paper_draft.tex','papers/paper25_gravitational_waves/paper_draft.tex') for x in re.findall(r'entirely disjoint|are disjoint by|universal-quantifier statement over the natural range|fails GW170817 over the entire natural range', re.sub(r'\s+',' ',open(f).read()))]; assert not h, h"` — same whitespace-normalised form as 3.1, for the same line-break reason. Exits 1 at HEAD (1 hit in D3, 3 in paper25).
- **Blocked-by:** review:2026-08-14-l1-stage13:L1:3.1
- **Gate:** CrossPaperConsistency
- **Location:** `papers/D3/paper_draft.tex:650-666`, `:494-496`; `papers/paper25_gravitational_waves/paper_draft.tex:198-202`, `:245-258`
- **Observed:** D3 `:658-666` displays
  `∀ χ_vest ∈ [0.1,10], |c_GW(χ_vest) − c| > 3×10⁻¹⁵ c`
  and states "**and is theorem** `second_sound_graviton_natural_range_universally_falsified`". D3 `:652-653` also says `vestigial_natural_range_violates_ligo` "closes the universal-quantifier statement over the natural range". paper25 `:201-202` and `:257-258` carry L1's "entirely disjoint by 14 orders of magnitude".
- **Evidence:**
  - The displayed proposition is **false** at `χ_vest = 1 ∈ [0.1,10]`, where `|c_GW − c| = 0`. It is directly negated by `SKEFTHawking.GravitationalWaves.ligo_satisfied_at_chi_one`, which L1 cites approvingly at `paper_draft.tex:250-254`.
  - The named theorem's actual statement (`GravitationalWaves.lean:302-309`) quantifies over `χ = chi_vest_natural_lower ∨ χ = chi_vest_natural_upper` — two points, no interval, no universal quantifier over `[0.1,10]`.
  - `vestigial_natural_range_violates_ligo` (`:242-245`) is the *conjunction* of the two endpoint falsifiers (`⟨natural_lower_violates_ligo, natural_upper_violates_ligo⟩`). It contains no quantifier at all.
- **Expected:** A displayed equation attributed to a named Lean theorem is that theorem's statement.
- **Fix:** In D3, replace the displayed equation with the theorem's actual two-point statement (or with the measure-ratio claim of 3.1) and drop "universal-quantifier statement"; propagate the 3.1 wording repair to paper25. Re-run `validate.py --check bundle_consistency` afterwards — note it currently PASSES, because the shared claim is *consistently wrong* across bundles and the check compares bundles to each other, not to the Lean.
- **Cache:** N/A.

### 4.2 — 🔵 RECOMMENDED — "half-decade window" survives in the Lean after the paper corrected it to ±1 decade

- **Severity:** recommended
- **Lane:** `lean`
- **Verify:** `cd "$REPO" && ! grep -q "half-decade" lean/SKEFTHawking/GravitationalWaves.lean` — exits 1 at HEAD (2 matches).
- **Gate:** CrossPaperConsistency
- **Location:** `lean/SKEFTHawking/GravitationalWaves.lean:184-185`, `:463-464`
- **Observed:** `The vestigial-susceptibility "natural range" is χ_vest · Λ² ∈ [0.1, 10] (a half-decade window …)` and `chi_vest_natural_lower, chi_vest_natural_upper: half-decade natural range bounds`.
- **Evidence:** `[0.1, 10]` spans two decades (±1 decade about 1). `papers/L1/change_log.md:27` records that "the inaccurate 'half-decade window' wording was also corrected" — in the paper. `paper_draft.tex:176-178` now says "±1-decade window"; `provenance.py:2764` and `:2818` say "±1 decade". The Lean was not updated in that pass.
- **Expected:** All four artifacts describe the same window the same way.
- **Fix:** Replace both occurrences with "±1-decade window".
- **Cache:** N/A.

---

### 5.1 — 🔴 BLOCKER — The paper's premise, `c_GW = c√χ_vest`, has no source, and the project's own Phase 5y H1 research computes the opposite

- **Severity:** blocker
- **Lane:** `research`
- **Verify:** `cd "$REPO" && grep -qiE "s_?2 = c|s₂ = c|structurally equal to \$c\$" papers/L1/paper_draft.tex` — asserts the paper engages with the result its own Phase 5y H1 research derives for the identification it falsifies (`s₂ = c` exactly, GW170817-compatible). Exits 1 at HEAD. *(A weaker command asserting merely that Eq. (1) carries some `\cite{}` nearby was rejected: it exits 0 at HEAD by picking up the unrelated ADW citation block eight lines above the label.)*
- **Blocked-by:** review:2026-08-14-l1-stage13:L1:1.1
- **Needs-operator:** `now` — whether L1's result survives at all is a physics-scope decision, not a copy-edit.
- **Gate:** NarrativeGrounding
- **Location:** `papers/L1/paper_draft.tex:32-47` (abstract), `:84-97` (Eq. 1 and its framing), `:98-103`
- **Observed:** The abstract opens "Several emergent-gravity programs identify the spin-2 graviton with the second-sound mode of a fermionic vestigial fluid, **predicting** a gravitational-wave propagation speed `c_GW = c√χ_vest`". Eq. (1) itself carries **no citation**. The paper then reports the falsification of that prediction as its headline result, and its title states it as a falsification of "the vestigial-second-sound graviton identification".
- **Evidence:** From `Lit-Search/Phase-5y/Phase 5y Hypothesis 1 — Kronecker-Anomaly Second-Sound Graviton as Dark Energy Candidate.md`, the document L1 invokes by name at `:98-103` and that `GravitationalWaves.lean:18-24` calls **LOAD-BEARING**:
  - §4(b) `[EQ.83]`: "`s₂² c² = … ⇒ **s₂ = c exactly**, independent of H`."
  - §4(c) "**GW170817 check.** … Volovik's `s₂ = c` is **consistent** at leading order … any such correction is far below the bound."
  - §4(d) "**Can `s₂` be tuned?** Not within Volovik's framework: `s₂ = c` is *structural*, not tunable. Deviations would require `w_n ≠ 1` … Neither is offered." And: "**Numerical `c_II/c`:** `c_II/c = 1` exactly in the published framework."
  - §3(a) and the closing spine statement describe a **scalar** ("second-sound *scalar* graviton"), against L1's "spin-2".
  L1 cites this document for exactly one of its findings — the GAP-2 "never derived as a propagating DOF" caveat, which is genuine — and omits §4 entirely, which is the part that negates the premise.
- **Expected:** One of two things, and the paper must choose:
  (a) L1 targets the same identification the H1 document analysed — in which case the target predicts `Δc/c = 0`, is GW170817-compatible, and there is nothing to falsify; or
  (b) L1 targets a different construction (an ADW/vestigial-phase susceptibility grafted onto the second-sound speed) — in which case `c_GW = c√χ_vest` is **this project's own** construction, no published program predicts it, and both the abstract's "Several emergent-gravity programs … predicting" and the title's "falsifies the … identification" are unsupported.
- **Fix:** Resolve (a) vs (b) explicitly in `:84-103`; attribute Eq. (1); reconcile spin-2 against the source's scalar characterisation; and if (b), retitle so the paper does not claim to falsify a proposal no one made. This is the finding that decides whether L1 has a result.
- **Cache:** N/A (project-internal research corpus, read directly).

### 5.2 — 🟡 REQUIRED — The headline figure caption describes a colour the figure does not use, and one annotation is anchored seven decades from its data point

- **Severity:** required
- **Lane:** `pyrust`
- **Verify:** `cd "$REPO" && ! grep -q "red span" papers/L1/paper_draft.tex` — asserts the caption no longer describes the natural-range band as red. Exits 1 at HEAD.
- **Gate:** NarrativeGrounding
- **Location:** `papers/L1/paper_draft.tex:256-271` (caption); `papers/L1/figures/fig_c_GW_vs_ligo_constraint.png`; generator `src/core/visualizations.py:fig_c_GW_vs_ligo_constraint` (~`:8588`)
- **Observed:** The caption reads "the natural `χ_vest ∈ [0.1,10]` range (**red span**)" and "`|Δc/c| ≤ 3×10⁻¹⁵` (**amber band, visually invisible at this scale**)".
- **Evidence:** Direct read of the PNG. The natural-range span is rendered **pale blue** with a dashed blue border and a blue "Natural χ_vest range [0.1, 10]" label — there is no red anywhere in the figure. The GW170817 cap is drawn as an amber dashed line at Δc/c = 0 spanning the full axis and is plainly *visible*, not invisible. Separately, the "χ=10 / Δc/c≈2.162 / Violates LIGO by 7.2e+14×" annotation box sits at x ≈ 10⁸ on a log axis that runs to 10¹⁰, with its arrow pointing further right into empty canvas — roughly seven decades from the χ = 10 datum it labels, and the curve itself is drawn only to χ ≈ 100, leaving eight empty decades.
- **Expected:** The caption describes the rendered figure; annotations point at their data.
- **Fix:** Correct "red span" → the colour actually used, and drop or qualify "visually invisible"; in `visualizations.py`, clamp the x-range to the region carrying data (≈ `[10⁻², 10²]`) and re-anchor the χ = 10 annotation. Regenerate via `scripts/review_figures.py`. Note the figure is the strongest single piece of evidence *against* finding 3.1's prose, so it should not be changed to agree with that prose.
- **Cache:** N/A.

### 5.3 — 🔵 RECOMMENDED — A five-page JETP Letters letter is described as a "review"

- **Severity:** recommended
- **Lane:** `prose`
- **Verify:** `cd "$REPO" && ! grep -q "2024 vestigial-fluid review" papers/L1/paper_draft.tex` — exits 1 at HEAD.
- **Gate:** NarrativeGrounding
- **Location:** `papers/L1/paper_draft.tex:78`
- **Observed:** "Volovik's 2024 vestigial-fluid review~\cite{Volovik2024Vestigial}".
- **Evidence:** `Volovik2024Vestigial` is JETP Letters **119**, 330–334 (2024) — five pages, single author, an original Letter. Calling it a review inflates its standing as a survey of what "several emergent-gravity programs" hold, which is the load L1 puts on it (see 1.1 and 5.1).
- **Expected:** "letter" or simply the citation.
- **Fix:** Drop "review".
- **Cache:** cache-verified (Crossref record, `page: "330-334"`).

---

## Gate 6 AssumptionDisclosure — no findings

**No independent findings.** The hypothesis structure `H_VestigialModeIsGraviton`
is disclosed conjunct-by-conjunct at `paper_draft.tex:273-299` with all four
falsifiers named; the `0 ≤ χ_vest` side condition on
`c_GW_match_iff_chi_close_to_one` is disclosed at `:203`; the `|γ| ≤ 10⁻³⁰`
hypothesis of `vestigial_dispersion_below_ligo_at_inspiral_peak` is disclosed at
`:346-348`. The one genuinely undisclosed load-bearing assumption is Eq. (1)
itself, which the paper presents as an established prediction rather than as an
assumption — filed as 5.1 rather than duplicated here.

---

### 7.1 — 🟡 REQUIRED — Three unresolved `% TODO:` work markers mechanically bar L1 from a recorded Stage-13 green

- **Severity:** required
- **Lane:** `prose`
- **Verify:** `cd "$REPO" && ! grep -q "TODO" papers/L1/paper_draft.tex` — asserts the draft carries no unresolved work marker. Exits 1 at HEAD (3 matches).
- **Gate:** `bundle_todo_free_before_green` (Stage-13 recording gate, `WAVE_EXECUTION_PIPELINE.md` §13 action 0b)
- **Location:** `papers/L1/paper_draft.tex:465-467`
- **Observed:** Three markers survive inside the commented-out `_phase6o_W1b_lean_only` lift stub: `%% TODO: lift content from …`, `%% TODO: ensure all numerical claims trace via formulas.py / counts.tex`, `%% TODO: ensure all citations have primary-source cache entries`.
- **Evidence:** `validate.py --check bundle_todo_free_before_green` reports `⚠ drafting — 9 bundle(s) carry work markers and are correctly NOT green … L1=3`. `:469` records that the section heading was commented out in Session 2 as "an empty post-bibliography stub … no rendered content" — so the markers are vestigial bookkeeping from an abandoned lift, not real outstanding work. They nevertheless block the green.
- **Expected:** No work marker in a draft being advanced to a recorded Stage-13 verdict.
- **Fix:** Delete `:465-467` (or rewrite as non-marker prose, e.g. `%% NOTE: absorption declined 2026-05-06; see append_log.json`), preserving the `:469` bookkeeping note. Then re-run the check.
- **Cache:** N/A.

## Gate 9 NumericalFreshness — no findings

**No findings.** All four of L1's quantitative self-descriptions verify at HEAD:

- "21 theorems" (`:52`, `:377`) — `lean_deps.json` gives 23 `theorem`-kind entries in
  `SKEFTHawking.GravitationalWaves`, of which two are autogenerated `eq_1`
  definitional lemmas; **21** are hand-written. Correct.
- "zero `sorry`" — `counts_fresh` summary reports `sorry=0` corpus-wide.
- "zero new axioms" — all 35 declarations in the module carry
  `axiom_deps_core = {propext, Classical.choice, Quot.sound}` and
  `axiom_deps_project = []`. Correct, and kernel-pure.
- "zero `maxHeartbeats` overrides" — no `set_option` in the file. Correct.
- "49 `pytest` cases" — `pytest tests/test_gravitational_waves.py --collect-only`
  returns exactly `49 tests collected`. Correct.

`counts_fresh` PASS; `count_literals` PASS (it does not scan L1 — bundle drafts are
outside its roster, which is why carryover 7.1 was accepted-with-rationale and
remains reasonable); `paper_provenance` PASS; `tables_fresh` not applicable (L1
has no autogen tables).

---

## Gate 8 ProductionRunHealth — no findings

**No findings.** L1 makes no simulation-evidence claim: `grep -niE "monte
carlo|numerical evidence|simulation"` over `paper_draft.tex` returns nothing, and
no `ProductionRun` node links to `paper:L1` in the graph. The 10⁻³⁰ figure is a
hypothesis, not a run output — its separate problem is filed as 2.2.

---

## Staleness audit of the pre-existing open findings

You asked whether the other open findings have the same defect as the two majors
you closed. **Three of the four do.** Re-measured against HEAD:

| finding | status at HEAD | verdict |
|---|---|---|
| `review:2026-05-11-1550…:L1:5.1` (forensic reference to retired `second_sound_graviton_not_derived_DOF`) | `grep` over `paper_draft.tex` returns **zero** matches for `not_derived_DOF`; the paragraph the Fix targets (`:234-243`) no longer exists | **STALE — remediated, ledger not updated** |
| `review:2026-05-11-1550…:L1:5.2` (Γ_H placeholder) | the word `placeholder` is **absent** from `paper_draft.tex`; the recommended replacement text was applied verbatim | **STALE, and the remedy was wrong** — see 2.2 |
| `review:2026-05-11-1550…:L1:5.3` ("21 theorems and lemmas") | `grep "and lemmas"` returns nothing; `:377` reads "21 theorems" | **STALE — remediated, ledger not updated** |
| `review:2026-08-12-legacy-compile-triage:…:6` (paper25 does not compile) | re-measured 2026-08-14: `compile_bundle_pdf.py paper25_gravitational_waves` → `FAIL pages=5 tex_errors=13`, first error `Unicode character Γ (U+0393)` | **LIVE and correctly rated** |

### S.1 — 🟡 REQUIRED — Three open minors describe artifacts that no longer exist, and one of them recommended the wrong fix

- **Severity:** required
- **Lane:** `infra`
- **Verify:** `cd "$REPO" && uv run python -c "import json; s=json.load(open('docs/review_finding_supersessions.json')); ids={r.get('finding_id') for r in (s if isinstance(s,list) else s.get('records',s.get('supersessions',[])))}; need={'review:2026-05-11-1550-bundle-stage13:L1:5.1','review:2026-05-11-1550-bundle-stage13:L1:5.3'}; assert need <= ids, sorted(need-ids)"` — asserts the two remediated minors carry closure records. Exits 1 at HEAD.
- **Gate:** FixPropagation
- **Location:** `docs/review_finding_supersessions.json`; findings enumerated in the table above
- **Observed:** 5.1 and 5.3 were remediated in the paper but never closed through the ledger, so they still read `open` in the graph and still count against the bundle's advisory total. 5.2's *paper-side* remedy was applied and made the disclosure **less** honest — it removed the word "placeholder" that `constants.py`, `provenance.py`, `dispersion_relation.py` and `GravitationalWaves.lean:436` all still use, and the paper simultaneously gained the stronger "closed in the Lean rather than argued" framing.
- **Evidence:** greps recorded in the table; the 2026-05-11 review's own Fix text for 5.2 ("replace 'at the placeholder Γ_H/c² ~ 10⁻³⁰ value' with 'at the project's current conservative upper bound |γ| ≤ 10⁻³⁰'") matches `paper_draft.tex:346-347` character-for-character.
- **Expected:** A finding whose target no longer exists is closed with evidence, not left open; a finding whose remedy was wrong is superseded rather than silently obeyed.
- **Fix:** `scripts/close_finding.py` records for 5.1 and 5.3 (status `fixed`, with the grep evidence above). For 5.2, close as `accepted` with a note that the remedy is **superseded by 2026-08-14 finding 2.2**, which reverses it.
- **Cache:** N/A.

---

## QI Candidate

**A fix-pass that edits the paper and stops there leaves the cited artifacts
carrying the retracted claim — and the pipeline cannot see it.** Four
independent instances in this one bundle:

1. Round-1 BLOCKER 1.1 corrected the Volovik page in the L1 bibitem; the same
   metadata is still wrong in the two Lean files L1 names as its reproducibility
   artifact (finding 1.2).
2. The 2026-06-05 sweep corrected "half-decade" → "±1-decade" in the paper and
   the provenance registry; `GravitationalWaves.lean` still says "half-decade"
   in two places (finding 4.2).
3. The 2026-05-11 minor 5.2 corrected "placeholder" → "conservative upper bound"
   in the paper; all four upstream artifacts still say "placeholder" — and here
   the paper is the one that is now wrong (finding 2.2).
4. Round-1 BLOCKER 3.1's replacement theorem was adopted into the paper's prose
   with a stronger claim than the theorem carries, and that claim then
   propagated into paper25 and D3, where `bundle_consistency` **passes** because
   the three bundles are consistently wrong with each other (findings 3.1, 4.1).

Instances 1, 2 and 4 share a mechanism: the checks compare paper↔paper and
paper↔counts, but nothing compares **paper prose ↔ the Lean statement or
docstring it names**. `paper_lean_refs_live` verifies a cited theorem *exists*;
it does not verify the paper describes what the theorem *says*. The 2026-05-01
round-2 QI candidate ("body-only fixes for narrative drift") named the
within-paper half of this; the cross-artifact half is larger and is where three
of these four escaped. Suggested surface: a check that, for each
`\texttt{theorem_name}` in a draft, extracts the theorem's binders from
`lean_deps.json` and fails when the surrounding prose asserts a quantifier the
statement does not have (interval vs. finite set is the concrete pattern here,
and it recurred four times in three bundles).

A second, smaller candidate: **a Stage-13 review that fetches must append to
`docs/citation_verifications.jsonl`** (finding 1.3). The 2026-05-11 L1 review
performed ten fresh fetches and recorded them only in prose, so the cache
continued to report L1's two most-cited bibitems as `wrong_venue` for 95 days
across at least three subsequent sweeps.
