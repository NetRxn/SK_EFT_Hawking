---
paper: I3
reviewer: adversarial-reviewer
model: claude-opus-4-7
review_date: 2026-05-11T12:51:00Z
readiness_gates_version: 1
bundle_target: I3
tier: 3
source_basis: sourceless (Phase 6o.zeta Lean-module synthesis)
stage13_redo: true (first fresh-context review per bundle_metadata.json)
---

# Adversarial Review — bundle I3 (Verified Stochastic Calculus for Mathlib4)

## Summary

**Findings: 6 BLOCKER / 7 REQUIRED / 4 RECOMMENDED = 17 total.** Gates affected: Gate 5 LeanProofSubstance (primary — every cited predicate is `Prop := True`; every cited per-module theorem either has body `trivial` on a `True` statement OR is the `inferInstance` of an existing standalone instance), Gate 7 NarrativeGrounding (multiple substantive-content overclaims; abstract calls a typeclass whose four-of-five fields are vacuous a "substantive cross-bridge"; the `ito_reduces_to_calculus_when_zero_qv` cross-bridge claim is structurally a tautology), Gate 9 NumericalFreshness (~1000 line claim is over 2x actual; ~30 predicate claim is over 2x actual; ~5800 theorem count drifts from canonical 5855), Gate 6 AssumptionDisclosure (the `[Fact (σ_sq ≠ 0)]` hypothesis on the headline closure is mentioned only in the Lean code block; not in narrative), Gate 1 CitationIntegrity (Ying-Degenne 2022 Doob martingales identified by anchor list #2 as required Mathlib substrate citation has no bibitem in I3; `mathlib4_2020` is an orphan bibitem). Bundle is **NOT submission-ready**. The substantive-content gap between the abstract's claim ("substantive cross-bridge typeclass that couples the LDP infrastructure to existing project content") and the actual Lean (`Prop := True` predicates discharged by `trivial`) is the dominant failure mode and cannot be remediated by prose edits alone — either the Lean substrate must carry genuine content beyond `True`, or the prose must be rewritten end-to-end to claim only what `Prop := True` predicates can claim (namely: a class registry, not a substantive cross-bridge).

## Findings

### 1.1 — 🔴 BLOCKER — Required Mathlib-substrate citation (Ying–Degenne 2022 Doob martingales) is missing from bibliography

- **Gate:** CitationIntegrity
- **Location:** `papers/I3/paper_draft.tex:1129-1179` (no `\bibitem{YingDegenne2022Martingales}` or similar); `papers/I3/paper_draft.tex:108-124` (intro paragraph attributing martingale infrastructure to Mathlib without attribution)
- **Observed:** The I3 anchor list in `docs/agents/claims-reviewer-bundle-prompts.md:288-296` explicitly enumerates "Mathlib substrate consumed" as: Brownian motion (Degenne–Ledvinka–Marion–Pfaffelhuber 2025), **Doob martingales (Ying-Degenne 2022)**, Markov kernels (Degenne 2025). The Brownian-motion and Markov-kernels papers are cited (`Degenne2025BrownianMotion`, `Degenne2025MarkovKernels`). The Doob-martingales paper is not cited anywhere in `paper_draft.tex` — no bibitem, no inline `\cite`.
- **Evidence:** `grep -in "Ying\|Doob martingales" papers/I3/paper_draft.tex` returns only abstract-text occurrences ("the Doob–Meyer semimartingale decomposition") and unrelated section names. WebSearch + WebFetch confirms: Ying, K. and Degenne, R., "A Formalization of Doob's Martingale Convergence Theorems in mathlib", CPP 2023 / arXiv:2212.05578 (Dec 2022). This is the Mathlib4 substrate that the paper's intro relies on when it says "Mathlib4's measure-theory hierarchy supplies … martingales (with optional sampling and Doob's inequality)".
- **Expected:** A bibitem with key like `YingDegenne2022Martingales` (arXiv:2212.05578) added to the bibliography, and an inline `\cite` in §1 paragraph 1 (line 113, where "martingales (with optional sampling and Doob's inequality)" is asserted) and §1 paragraph "A note on novelty claims" (where Mathlib4 capabilities are enumerated — line 248-255).
- **Fix:** Add `YingDegenne2022Martingales` to `src/core/citations.py` `CITATION_REGISTRY` (`authors: Ying, K. and Degenne, R.`, `title: A Formalization of Doob's Martingale Convergence Theorems in mathlib`, `journal: Proceedings of the 12th ACM SIGPLAN International Conference on Certified Programs and Proofs (CPP 2023)`, `arxiv: 2212.05578`, `doi: 10.1145/3573105.3575675`, `year: 2023`); add bibitem at the appropriate place in the bibliography; add `\cite{YingDegenne2022Martingales}` to line 113 ("martingales (with optional sampling and Doob's inequality)") and line ~250 (where Mathlib4 ships are enumerated).
- **Cache:** fresh-fetch

### 1.2 — 🟡 REQUIRED — Orphan bibitem (`mathlib4_2020` declared but never `\cite`d)

- **Gate:** CitationIntegrity
- **Location:** `papers/I3/paper_draft.tex:1172-1177` (bibitem declared); `grep "mathlib4_2020"` returns only this one occurrence
- **Observed:** `\bibitem{mathlib4_2020}` is in the bibliography block but no inline `\cite{mathlib4_2020}` exists anywhere in the body. Compiling with `\bibliography` style would emit an "unused-reference" warning.
- **Evidence:** Single grep hit on `mathlib4_2020` (the bibitem itself), no `\cite{mathlib4_2020}` in body. Confirmed against `papers/I3/paper_draft.tex` lines 1-1170.
- **Expected:** Either add an inline `\cite{mathlib4_2020}` (the natural anchor would be line 108-110 of §1 where "Mathlib4's measure-theory hierarchy supplies..." opens — Mathlib4 itself should be cited there) OR remove the orphan bibitem.
- **Fix:** Add `\cite{mathlib4_2020}` after "Mathlib4's measure-theory hierarchy" in line 110 of the intro.
- **Cache:** verified-by-grep

### 1.3 — 🔵 RECOMMENDED — `Roehm2026Strategy` registry-vs-implementation drift (registry says "13-bundle"; current architecture is 14-bundle)

- **Gate:** CitationIntegrity (informational)
- **Location:** `src/core/citations.py:7524` says `title: 'SK-EFT Hawking 13-bundle publication architecture (project paper-strategy frame)'`; `docs/PAPER_STRATEGY.md:27` and `:322` enumerate **14 publication targets** (1 flagship + 5 Tier-1 + 3 Tier-2 + 3 Tier-3 + 2 Tier-4); this includes I3 itself.
- **Observed:** The registry entry's `title` field hard-codes "13-bundle" but `PAPER_STRATEGY.md` (the document the registry entry purports to describe) is at 14 bundles. The I3 bundle's own existence (added in Phase 6n Session 4) is what flipped the count from 13 → 14. Since I3 cites `Roehm2026Strategy`, the I3 release co-occurs with the count update.
- **Expected:** Either (a) update registry `title` to "14-bundle" to match `PAPER_STRATEGY.md` and the actual count, or (b) document that the `Roehm2026Strategy` in-prep frame is intentionally pinned at 13-bundle and I3 is an addendum.
- **Fix:** Recommended — update `src/core/citations.py:7524` title to "14-bundle" or "13-to-14-bundle". Not blocking for I3 directly but is the kind of drift Stage-13 surfaces.
- **Cache:** verified-by-grep

### 2.1 — N/A (no quantitative parameters)

I3 reports no experimental parameters that would invoke `PARAMETER_PROVENANCE`. Gate 3 ParameterProvenance has no applicable findings.

### 3.1 — 🔴 BLOCKER — Every cited Lean predicate has `Prop := True` body; every cited per-module theorem either discharges by `trivial` on a vacuous statement OR by `inferInstance` of a separate existing instance — the abstract calls this "substantive cross-bridge typeclass" which is materially wrong

- **Gate:** LeanProofSubstance
- **Location:** Abstract `papers/I3/paper_draft.tex:67-75` ("substantive cross-bridge typeclass `LDPCompatibleSKEFT`"); §5.1 `papers/I3/paper_draft.tex:741-758` ("Why `LDPCompatibleSKEFT` is the substantive deliverable"); Lean substrate `lean/SKEFTHawking/Itô/StochasticIntegral.lean:32-44`, `lean/SKEFTHawking/Itô/QuadraticVariation.lean:17-33`, `lean/SKEFTHawking/Itô/Semimartingale.lean:17-28`, `lean/SKEFTHawking/Itô/ItoIsometry.lean:19-22`, `lean/SKEFTHawking/Itô/ItoLemma.lean:21-30`, `lean/SKEFTHawking/Itô/Novikov.lean:21-38`, `lean/SKEFTHawking/LDP/CramerIID.lean:26-36`, `lean/SKEFTHawking/LDP/Sanov.lean:21-30`, `lean/SKEFTHawking/LDP/Contraction.lean:20-27`, `lean/SKEFTHawking/LDP/CramerLowerBound.lean:24-31`, `lean/SKEFTHawking/LDP/Varadhan.lean:21-28`, `lean/SKEFTHawking/LDP/LDPCompatibleSKEFT.lean:50-66`
- **Observed:** Every one of the 13 predicates `IsStochasticIntegral`, `IsQuadraticVariation`, `IsBrownianQuadraticVariationT`, `IsCovariation`, `IsSemimartingaleDecomposition`, `IsDoobMeyerUnique`, `IsItoIsometry`, `IsItoLemma`, `IsNovikovCondition`, `IsCramerIIDUpperBound`, `IsFiniteAlphabetSanov`, `IsContractionPrinciple`, `IsCramerLowerBoundEsscher`, `IsVaradhanUpperBound` is defined as `Prop := True`. Every cited theorem (other than the `wave_3b_ldp_overall_closure` headline which reuses `linearResponseRateFunctionCentered_isLDPCompatibleSKEFT` whose `ldpRateFn := inferInstance` reuses an instance from `LDPLinearResponse.lean:597-600`) has body `trivial` or `⟨trivial, trivial, …⟩`. The `LDPCompatibleSKEFT` typeclass (`LDPCompatibleSKEFT.lean:50-55`) has five fields; one (`ldpRateFn`) carries content via the existing `IsLDPRateFunction` instance, four (`cramerCompatible`, `sanovCompatible`, `contractionCompatible`, `varadhanCompatible`) are typed by `Prop := True` predicates and discharge with `trivial` — so the typeclass is **mathematically equivalent to `IsLDPRateFunction`** at the Prop level. Calling this a "substantive cross-bridge" (abstract) and "substantive deliverable" (§5.3 heading) misrepresents the formal content.
- **Evidence:** `grep -E "^def Is|^class LDP" lean/SKEFTHawking/Itô/*.lean lean/SKEFTHawking/LDP/*.lean` returns 14 predicate definitions, every one of which has body `True` (verified by direct file Reads). The `linearResponseRateFunctionCentered_isLDPCompatibleSKEFT` instance at `LDPCompatibleSKEFT.lean:59-66` discharges 4 of 5 fields by literal `trivial`. The `IsLDPRateFunction` instance at `LDPLinearResponse.lean:597-600` is the only field carrying substantive content — and that instance is *not* an I3 deliverable (it was authored in Phase 6n Wave 2c.5c+).
- **Expected:** Either (a) replace `Prop := True` predicate bodies with genuine substrate-data statements that carry refutable content (e.g., `IsCramerIIDUpperBound mgf I := ∀ x, I x = sSup {θ * x - Real.log (mgf θ) | θ : ℝ}`) and re-prove the closures non-trivially, OR (b) rewrite the abstract + §5 + §1 paragraph "A note on novelty claims" to say only what the substrate establishes: that a *Prop-level typeclass scaffolding* is registered and that the substantive content lifts later. The current prose makes Gate-5-disallowed substance claims about a `trivial`-bodied artifact.
- **Fix:** This is the load-bearing finding for the bundle. Recommend option (b) — strip the "substantive" language from the abstract (drop the word "substantive" everywhere it modifies "cross-bridge typeclass" or "deliverable"); rewrite §5.3 heading to "Why `LDPCompatibleSKEFT` is the deliverable interface for downstream consumers" (drop "substantive"); add an explicit caveat in §5.1 that 4 of the 5 fields are typed by `True`-bodied predicates and discharge trivially. The Lean substrate (`Prop := True`) is honest as scaffolding; the prose is what overclaims.
- **Cache:** verified-by-direct-file-read

### 3.2 — 🔴 BLOCKER — `ito_reduces_to_calculus_when_zero_qv` is a structural tautology, not a cross-bridge theorem

- **Gate:** LeanProofSubstance
- **Location:** Paper `papers/I3/paper_draft.tex:512-519` ("Cross-bridge. `ito_reduces_to_calculus_when_zero_qv` establishes that when `⟨X⟩ = 0` (i.e., X is finite-variation), Itô's lemma reduces to the ordinary fundamental theorem of calculus."); Lean `lean/SKEFTHawking/Itô/ItoLemma.lean:26-27`
- **Observed:** The paper prose claims the theorem `establishes` a cross-bridge from Itô's lemma to the FTC under zero quadratic variation. The Lean statement is:
  ```
  theorem ito_reduces_to_calculus_when_zero_qv :
      IsItoLemma (fun _ => 0) (fun _ => 0) (fun _ => 0) := trivial
  ```
  The theorem statement says only that the trivial-zero substrate-data witness inhabits `IsItoLemma (fun _ => 0) (fun _ => 0) (fun _ => 0)`. There is no hypothesis on `⟨X⟩ = 0`, no statement about the FTC, no cross-bridge of any kind. The body is `trivial` and is dispatched because `IsItoLemma _ _ _ := True`. The theorem name is descriptive of an intended-future cross-bridge; the formal content is `True := trivial`.
- **Evidence:** Direct read of `lean/SKEFTHawking/Itô/ItoLemma.lean:21-27`: `def IsItoLemma (_f : ℝ → ℝ) (_X _result : ℝ → ℝ) : Prop := True` followed by `theorem ito_reduces_to_calculus_when_zero_qv : IsItoLemma (fun _ => 0) (fun _ => 0) (fun _ => 0) := trivial`.
- **Expected:** Either (a) prove a substantive theorem about Mathlib's `intervalIntegral` reducing to the substrate when QV is zero (substantive content), OR (b) rewrite the paper prose at line 512-519 to say only "`ito_reduces_to_calculus_when_zero_qv` is the substrate-data placeholder for the future zero-QV-implies-FTC cross-bridge; the substantive content lifts when `IsItoLemma` is given a non-trivial body."
- **Fix:** Rewrite §3.5 "Cross-bridge" paragraph (lines 512-519) to honestly describe the theorem as a placeholder.
- **Cache:** verified-by-direct-file-read

### 3.3 — 🔴 BLOCKER — Theorem name `IsCramerLowerBound` in paper §4.4 does not match Lean declaration `IsCramerLowerBoundEsscher`

- **Gate:** LeanProofSubstance
- **Location:** Paper `papers/I3/paper_draft.tex:619-620` ("**Substrate-data predicate.** \texttt{IsCramerLowerBound mgf I} declares the Cramér _lower_ bound"); Lean `lean/SKEFTHawking/LDP/CramerLowerBound.lean:24-25` (`def IsCramerLowerBoundEsscher`)
- **Observed:** Paper §4.4 names the predicate `IsCramerLowerBound`, but the Lean source declares `IsCramerLowerBoundEsscher`. Readers tracing the citation to the Lean source will encounter a name mismatch.
- **Evidence:** `grep "IsCramerLowerBound" papers/I3/paper_draft.tex` (line 619-620 — `IsCramerLowerBound mgf I`); `grep "def Is" lean/SKEFTHawking/LDP/CramerLowerBound.lean` (`def IsCramerLowerBoundEsscher`).
- **Expected:** Either rename the Lean def to `IsCramerLowerBound` (matching the paper) and add `Esscher` as a docstring note, or update the paper to say `IsCramerLowerBoundEsscher`.
- **Fix:** Recommend updating paper to `IsCramerLowerBoundEsscher` — the `Esscher` suffix is informative (the tilting technique).
- **Cache:** verified-by-direct-file-read

### 3.4 — 🟡 REQUIRED — `wave_3b_itoBeta_5_itoLemma_closure` claimed in paper §3.5 as wave-closure but encodes no closure-shape content (`IsItoLemma (fun _ => 0) (fun _ => 0) (fun _ => 0) := trivial`)

- **Gate:** LeanProofSubstance
- **Location:** Paper `papers/I3/paper_draft.tex:521-522`; Lean `lean/SKEFTHawking/Itô/ItoLemma.lean:29-30`
- **Observed:** Paper's §2.2 enumerates the wave-closure pattern as "packaging the predicate, the non-vacuity witness, and any structural separation theorems into a single … result". `wave_3b_itoBeta_5_itoLemma_closure` reduces to `IsItoLemma (fun _ => 0) (fun _ => 0) (fun _ => 0) := trivial` — same statement as the prior `ito_reduces_to_calculus_when_zero_qv`. There is no "packaging" — both theorems are syntactically identical conclusions discharged by `trivial`. The closure isn't a wave-closure; it's a redundant restatement.
- **Evidence:** `grep -E "^theorem " lean/SKEFTHawking/Itô/ItoLemma.lean` returns two theorems with identical body. `lean/SKEFTHawking/Itô/Novikov.lean:30-38` similarly does not have a per-module `wave_3b_itoBeta_6_novikov_closure` — only the overall `wave_3b_ito_overall_closure` exists, which absorbs the would-be Novikov per-module closure.
- **Expected:** Either remove the redundancy (delete `wave_3b_itoBeta_5_itoLemma_closure` since `ito_reduces_to_calculus_when_zero_qv` is the same statement), or add genuine packaging content (the non-vacuity witness + a structural-separation theorem) per the §2.2 pattern.
- **Fix:** Best resolution is to add the missing per-module Novikov closure (`wave_3b_itoBeta_6_novikov_closure`) and restructure §2.2 + §3.5 prose to honestly describe what each closure packages.
- **Cache:** verified-by-direct-file-read

### 4.1 — N/A (no cross-paper construct collisions)

I3 is a sourceless Lean-synthesis bundle; cross-paper consistency at the construct level is checked via shared bibkeys. `Roehm2026Strategy`, `FalascoEsposito2025RMP`, `Degenne2025MarkovKernels`, `Degenne2025BrownianMotion` are all single-bundle (I3) or two-bundle consistent. No CONTRADICTS edges.

### 5.1 — 🔴 BLOCKER — Conclusion's "first explicit articulation of a software-substrate release pattern" first-claim is not grounded; prior practice in mathlib's own PR cycle is the natural prior art

- **Gate:** NarrativeGrounding
- **Location:** `papers/I3/paper_draft.tex:1121-1127` ("the substrate-vs-quantitative-content engineering choice that drives the release scope … is, to our knowledge, the first explicit articulation of a software-substrate release pattern for formally-verified-mathematics infrastructure paired with explicit coordination with the upstream community — a model we hope the program's I2 + I3 release pair makes more legible for future formal-verification software releases")
- **Observed:** This is a first-claim about a software-release methodology (predicate-substrate-first / quantitative-content-later) paired with upstream coordination. The mathlib community has been doing predicate-then-content PR sequencing for many years (e.g., `MeasureTheory` was staged incrementally; the `Mathlib4` port itself was a multi-year coordinated lift; the `condensed mathematics` project explicitly articulates a substrate-first roadmap). The "to our knowledge" hedge is present but the claim is not backed by a literature scan documenting what would constitute prior art.
- **Evidence:** WebSearch on "predicate substrate Mathlib upstream PR coordination" returns multiple prior-art candidates including Doob martingales (Ying-Degenne CPP 2023, staged predicates-first), Brownian motion (Degenne et al. 2025, "Markov kernels first, then Brownian motion"), Statistical Learning Theory in Lean 4 (arXiv:2602.02285, "Empirical Processes from Scratch", same staged-substrate pattern). All three explicitly coordinate with the upstream Mathlib community via Zulip + GitHub.
- **Expected:** Either (a) cite specific prior-art examples (Ying-Degenne 2022 + Degenne et al. 2025 are natural candidates) and refine the claim to "first … in [specific narrow sub-domain]", or (b) remove the first-claim entirely.
- **Fix:** Drop the "first explicit articulation" sentence (lines 1121-1127); replace with a sentence acknowledging the staged-substrate pattern as continuous with prior mathlib practice.
- **Cache:** fresh-fetch

### 5.2 — 🟡 REQUIRED — Section §1 paragraph "A note on novelty claims" survey is not adequately scoped to support the listed first-claims

- **Gate:** NarrativeGrounding
- **Location:** `papers/I3/paper_draft.tex:235-267` (the survey paragraph claims first-explicit-predicate-scaffolding for It\^o stochastic integral, It\^o's lemma, Novikov's condition, Sanov's theorem, contraction principle, Varadhan's lemma "within those three flagship-prover ecosystems and to the best of the survey's findings"; explicitly excludes HOL Light, Mizar, Agda, Metamath, F-star, domain-specific provers)
- **Observed:** The survey says it checked (i) Isabelle/HOL AFP probability-theory topic, (ii) the awesome-coq library list, (iii) Mathlib4. An exhaustive cross-prover search must also check: (a) the Coq stdlib's `coquelicot` + `mathcomp-analysis` formalizations (the awesome-coq list is community-curated and is NOT exhaustive — it misses, e.g., Affeldt's MathComp-Analysis library where measure-theoretic probability is actively developed); (b) Lean 3 mathlib (the predecessor library, retired but historically searched); (c) Isabelle/HOL beyond the AFP (e.g., Hölzl's measure-theoretic probability work in the Isabelle distribution proper, not the AFP). Additionally, the claim "first explicit predicate-level scaffolding" is a structural-engineering claim, not a mathematical-content claim, and the survey did not include a search on that axis (e.g., the Doob martingale convergence formalization Ying-Degenne 2022 includes scaffolding of stopping-times + filtrations + sub/super-martingale predicates that could be described as "predicate-level scaffolding" for the same domain).
- **Evidence:** WebSearch confirms `coquelicot` + `mathcomp-analysis` were not surveyed by name; WebSearch confirms Isabelle/HOL distribution (not AFP) probability content exists separately. The survey paragraph also does not document the date the survey was performed in a way that supports re-running (says "during Phase 7 absorption Session 5 (2026-05-08)" but no Lit-Search task or survey artifact is referenced).
- **Expected:** Either (a) cite a specific survey artifact (`Lit-Search/Tasks/.../i3_first_claim_survey.md` or similar) and extend the search to the missing scopes, or (b) reduce the strength of the first-claim to a narrower domain (e.g., "first in Mathlib4" — which is verifiable from the Mathlib4 docs) and drop the cross-prover scope.
- **Fix:** Drop "the first explicit predicate-level scaffolding" claim's cross-prover reach; restrict to "first in Mathlib4" or "first in the Lean ecosystem".
- **Cache:** verified-via-WebSearch-spot-check

### 5.3 — 🔴 BLOCKER — Abstract's "substantive cross-bridge typeclass" claim is contradicted by the actual Lean instance whose 4-of-5 fields are vacuous

- **Gate:** NarrativeGrounding (cross-references finding 3.1)
- **Location:** Abstract `papers/I3/paper_draft.tex:71-74` ("a typeclass `LDPCompatibleSKEFT` that **couples** the LDP infrastructure to existing project content"); §5 heading `papers/I3/paper_draft.tex:663` ("LDPCompatibleSKEFT — the substantive cross-bridge typeclass"); §5.3 heading `papers/I3/paper_draft.tex:740` ("Why `LDPCompatibleSKEFT` is the substantive deliverable")
- **Observed:** The paper describes `LDPCompatibleSKEFT` as a substantive cross-bridge that "couples the LDP infrastructure". The actual coupling is: 1 field (`ldpRateFn`) which is `inferInstance` of the pre-existing Phase 6n Wave 2c.5c+ `IsLDPRateFunction` instance (substantive content, but authored elsewhere — not a deliverable of I3); 4 fields (`cramerCompatible`, `sanovCompatible`, `contractionCompatible`, `varadhanCompatible`) which are each typed by `Prop := True` predicates and discharge with `trivial`. The 4 trivial fields carry no information beyond their own existence as scaffolding. Calling the resulting typeclass a "substantive coupling" or "substantive cross-bridge" is a Gate-7 overclaim.
- **Evidence:** See finding 3.1. The instance at `LDPCompatibleSKEFT.lean:59-66` literally has `cramerCompatible := trivial; sanovCompatible := trivial; contractionCompatible := trivial; varadhanCompatible := trivial`. The fields are typed by `Prop := True`, so `trivial` is the only inhabiting term — the fields do not constrain the rate function in any way.
- **Expected:** Replace "substantive cross-bridge typeclass" with "predicate-scaffolding typeclass" (or equivalent) in abstract + §5 heading + §5.3 heading + Conclusion. Add an honest caveat in §5.1 documenting that the four LDP-side fields are presently vacuous and will become substantive only when the predicate bodies lift from `True` to genuine substrate statements.
- **Fix:** Mechanical s/substantive cross-bridge/predicate-scaffolding cross-bridge/ in abstract + section headings + body prose; add a 2-3 sentence caveat in §5.1.
- **Cache:** verified-by-cross-finding-3.1

### 5.4 — 🟡 REQUIRED — §1 paragraph "$\sim$5800 theorems" is stale vs canonical counts (5855)

- **Gate:** NarrativeGrounding (interpretive claim with weak quantitative anchoring); related to Gate 9 (count freshness)
- **Location:** `papers/I3/paper_draft.tex:220-223` ("the cadence by which the bulk of the SK--EFT Hawking project's $\sim$5800 theorems were written")
- **Observed:** Canonical count from `docs/counts.tex` line 3 is `\totaltheorems{5855}`. Paper says `~5800`. The `~` is a hedge but the actual canonical literal is the directly retrievable `\totaltheorems` macro and is included in I3's preamble via `\input{../../docs/counts.tex}` (line 34). The paper does not use the macro; it hardcodes a stale literal.
- **Evidence:** `docs/counts.tex:3` (`\newcommand{\totaltheorems}{5855}`); `papers/I3/paper_draft.tex:34` (`\input{../../docs/counts.tex}`); `papers/I3/paper_draft.tex:221` (`$\sim$5800`).
- **Expected:** Replace `$\sim$5800` with `\totaltheorems` (which expands to `5855`) — eliminates the drift permanently. Alternatively, add a `~` macro that rounds.
- **Fix:** `s/$\sim$5800 theorems/$\sim$\totaltheorems\ theorems/` at line 221.
- **Cache:** verified-by-direct-grep

### 5.5 — 🔵 RECOMMENDED — D3/D5/E1 cross-bridge §6 hedge is correct but the cross-bridge claim is currently vacuous

- **Gate:** NarrativeGrounding
- **Location:** `papers/I3/paper_draft.tex:764-826` (§6 explicitly states: "no downstream Lean theorem in the D3, D5, or E1 bundles currently invokes the `LDPCompatibleSKEFT` typeclass or any of the Itô-substrate predicates")
- **Observed:** The section is hedged correctly (no false claim of current consumption), but the §6 thesis "I3 is positioned as software substrate for three of the program's content-bundle targets" is not verifiable as anything more than aspiration. Anchor #4 of the I3 anchor list says "Claims about D3 / D5 / E1 downstream consumers must trace to specific Lean theorems in those modules that explicitly invoke the typeclass." Because there are no such theorems yet (confirmed via `grep -rn "LDPCompatibleSKEFT" lean/SKEFTHawking/` showing only the I3 module itself + the master library import), the anchor is satisfied only vacuously.
- **Evidence:** `grep -rn "LDPCompatibleSKEFT" lean/SKEFTHawking/` returns 12 lines, all in `lean/SKEFTHawking/LDP/LDPCompatibleSKEFT.lean` or `lean/SKEFTHawking/SKEFTHawking.lean` (the master library `import`). No D3/D5/E1 module references the typeclass.
- **Expected:** Add a sentence to the §6 opening stating that the cross-bridge is "designed but not yet consumed" — currently the paragraph implies a future-tense plan but doesn't explicitly state that the I3 substrate is, at release time, freestanding from D3/D5/E1.
- **Fix:** Strengthen the §6 first sentence to make the freestanding-at-release status unambiguous.
- **Cache:** verified-by-direct-grep

### 5.6 — 🟡 REQUIRED — "the program's I2 + I3 release pair" claim implies a deliberate pairing that does not appear to be documented elsewhere

- **Gate:** NarrativeGrounding
- **Location:** `papers/I3/paper_draft.tex:1126-1127` ("a model we hope the program's I2 + I3 release pair makes more legible")
- **Observed:** The conclusion frames I2 and I3 as a deliberate pair, but `PAPER_STRATEGY.md:317` lists I3 as Tier 3 alongside I1 + I2 (three Tier 3 papers), not as paired with I2 specifically. The §2.3 listing of Tier 3 in PAPER_STRATEGY.md does not single out an I2+I3 pairing.
- **Evidence:** `PAPER_STRATEGY.md:186` (I3 paragraph) treats I3 alongside the three Tier-3 papers without specific I2 cross-reference. The `I2 + I3` pairing is asserted only in the conclusion of I3 itself.
- **Expected:** Either (a) document the I2 + I3 pairing in `PAPER_STRATEGY.md` and cross-reference it from I3, or (b) drop the "I2 + I3 release pair" language and replace with "the program's Tier 3 software-paper releases" or similar.
- **Fix:** Replace "I2 + I3 release pair" with "the program's Tier 3 software-paper releases" at line 1126.
- **Cache:** verified-by-direct-grep

### 6.1 — 🟡 REQUIRED — `[Fact (σ_sq ≠ 0)]` hypothesis on the headline `wave_3b_ldp_overall_closure` is not surfaced in paper prose

- **Gate:** AssumptionDisclosure
- **Location:** Paper `papers/I3/paper_draft.tex:728-737` (the §5.2 wave-closure code listing includes `[Fact (σ_sq ≠ 0)]` in the Lean snippet but the surrounding prose does not flag it); Lean `lean/SKEFTHawking/LDP/LDPCompatibleSKEFT.lean:73-75`
- **Observed:** The `wave_3b_ldp_overall_closure` theorem statement is `(β σ_sq : ℝ) [Fact (σ_sq ≠ 0)] : LDPCompatibleSKEFT β (linearResponseRateFunctionCentered β σ_sq) := inferInstance`. The `[Fact (σ_sq ≠ 0)]` is a load-bearing hypothesis (the centered linear-response rate function is defined via division by `σ_sq`, and the substantive `IsLDPRateFunction` instance requires `σ_sq ≠ 0`). The paper's narrative description of the closure (§5.2 paragraph at lines 725-738) does not flag this assumption. Readers who try to instantiate the closure with `σ_sq = 0` will encounter an instance-synthesis failure.
- **Evidence:** Lean source at `lean/SKEFTHawking/LDP/LDPCompatibleSKEFT.lean:73`. Paper §5.2 prose says only "the closure of all six LDP modules' wave-closure summaries is precisely the LDPCompatibleSKEFT-instance-existence on the linear-response Gaussian rate function" — no σ²≠0 caveat.
- **Expected:** Add a sentence in §5.2 documenting the `σ²≠0` (variance positive) assumption.
- **Fix:** Add at line 735: "(The closure requires `σ²≠0` — equivalently, the underlying linear-response Gaussian has positive variance — to ensure the rate function is well-defined.)"
- **Cache:** verified-by-direct-file-read

### 6.2 — 🔵 RECOMMENDED — `IsLDPRateFunction` reuse from Phase 6n Wave 2c.5c+ is not flagged as load-bearing dependency on out-of-bundle content

- **Gate:** AssumptionDisclosure
- **Location:** `papers/I3/paper_draft.tex:712-722` (§5.1); Lean `lean/SKEFTHawking/LDP/LDPCompatibleSKEFT.lean:5` (`import SKEFTHawking.CrooksAnalogHawking.LDPLinearResponse`)
- **Observed:** The only substantive content in the `linearResponseRateFunctionCentered_isLDPCompatibleSKEFT` instance is the `ldpRateFn := inferInstance` field, which is supplied by the existing Phase 6n Wave 2c.5c+ instance at `LDPLinearResponse.lean:597-600`. The paper §5.1 mentions this composition but does not flag that the *only* substantive Lean content in the I3 deliverable is supplied by an out-of-bundle (Phase 6n) artifact.
- **Evidence:** `lean/SKEFTHawking/LDP/LDPCompatibleSKEFT.lean:5` imports `LDPLinearResponse`; the instance at lines 59-66 has `ldpRateFn := inferInstance` (which resolves to the LDPLinearResponse instance) and four `trivial` fields.
- **Expected:** Add a sentence in §5.1 acknowledging that the substantive content (the `IsLDPRateFunction` instance) was authored in Phase 6n Wave 2c.5c+ and is being *consumed* by I3, not produced by I3.
- **Fix:** Add at line ~722: "Note: the substantive `IsLDPRateFunction` instance on `linearResponseRateFunctionCentered` is reused from Phase 6n Wave 2c.5c+; I3's contribution is the typeclass wrapper that composes that instance with the four (currently vacuous) LDP-substrate fields."
- **Cache:** verified-by-direct-file-read

### 7.1 — 🔴 BLOCKER — `~1000 lines of Lean source` claim drifts 2× from actual (446 lines across 12 modules)

- **Gate:** NumericalFreshness (CountFreshness sub-class)
- **Location:** `papers/I3/paper_draft.tex:376` ("**12 modules** ($\sim$1000 lines of Lean source).")
- **Observed:** `wc -l` over the 12 module files returns 446 lines total (24 + 32 + 40 + 35 + 30 + 46 + 29 + 38 + 33 + 77 + 32 + 30 = 446). The paper's `~1000` is over 2x the actual count.
- **Evidence:** `wc -l lean/SKEFTHawking/Itô/*.lean lean/SKEFTHawking/LDP/*.lean` returns `446 total`.
- **Expected:** Replace `~1000 lines` with `~450 lines` or use a `\input{}` macro that retrieves the line count from a fresh canonical source.
- **Fix:** `s/$\sim$1000 lines of Lean source/$\sim$450 lines of Lean source/` at line 376.
- **Cache:** verified-by-direct-wc

### 7.2 — 🔴 BLOCKER — `~30 Prop-typed predicates` claim drifts 2× from actual (14 predicates)

- **Gate:** NumericalFreshness (CountFreshness sub-class)
- **Location:** `papers/I3/paper_draft.tex:377` ("**$\sim$30 Prop-typed predicates** across the 12 modules.")
- **Observed:** Counting all `def Is*` declarations + the one `class LDPCompatibleSKEFT` across the 12 modules yields exactly 14: `IsStochasticIntegral`, `IsQuadraticVariation`, `IsBrownianQuadraticVariationT`, `IsCovariation`, `IsSemimartingaleDecomposition`, `IsDoobMeyerUnique`, `IsItoIsometry`, `IsItoLemma`, `IsNovikovCondition`, `IsCramerIIDUpperBound`, `IsFiniteAlphabetSanov`, `IsContractionPrinciple`, `IsCramerLowerBoundEsscher`, `IsVaradhanUpperBound`, `LDPCompatibleSKEFT`. The paper's `~30` is over 2x the actual count.
- **Evidence:** `grep -E "^def Is|^class LDP" lean/SKEFTHawking/Itô/*.lean lean/SKEFTHawking/LDP/*.lean` returns 14 matches (13 defs + 1 class).
- **Expected:** Replace `~30 Prop-typed predicates` with `14 Prop-typed predicates` (the exact count is small enough to state precisely; use exact count).
- **Fix:** `s/$\sim$30 Prop-typed predicates/14 Prop-typed predicates/` at line 377.
- **Cache:** verified-by-direct-grep

### 7.3 — 🔴 BLOCKER — `~30 non-vacuity witnesses (one per predicate, sometimes more)` is impossible — paper has 14 predicates total

- **Gate:** NumericalFreshness (CountFreshness sub-class)
- **Location:** `papers/I3/paper_draft.tex:378-379` ("**$\sim$30 non-vacuity witnesses** (one per predicate, sometimes more).")
- **Observed:** Since there are 14 predicates total (finding 7.2), "one per predicate, sometimes more" cannot equal `~30`. Direct count of `^theorem ` declarations across the 12 modules returns 21 (= ~14 wave-closures + ~7 separate witnesses). Of these, ~7 are non-vacuity witnesses (`isStochasticIntegral_trivial`, `isBrownianQuadraticVariationT_witness`, `isNovikovCondition_constant`, `cramerIID_subGaussian_witness`, `sanov_methodOfTypes_witness`, `isContractionPrinciple_witness`, `isCramerLowerBoundEsscher_witness`, `isVaradhanUpperBound_witness`). That's 8 non-vacuity witnesses, not ~30.
- **Evidence:** `grep -E "^theorem " lean/SKEFTHawking/Itô/*.lean lean/SKEFTHawking/LDP/*.lean | wc -l` returns 21. Direct enumeration shows ~8 non-vacuity witnesses + ~13 wave/overall closures = 21.
- **Expected:** Replace `~30 non-vacuity witnesses` with `~8 non-vacuity witnesses` (one per substantive predicate, not all 14 — several predicates share witnesses).
- **Fix:** `s/$\sim$30 non-vacuity witnesses (one per predicate, sometimes more)./~8 non-vacuity witnesses (substantive predicates each have at least one; trivially-vacuous predicates are witnessed by the wave-closure)./` at line 378-379.
- **Cache:** verified-by-direct-enumeration

### 7.4 — 🟡 REQUIRED — `12 wave-closure summary theorems (one per module)` is also stale (Novikov has no per-module closure, absorbed into overall)

- **Gate:** NumericalFreshness (CountFreshness sub-class)
- **Location:** `papers/I3/paper_draft.tex:380` ("**12 wave-closure summary theorems** (one per module).")
- **Observed:** Per-module closures exist for `StochasticIntegral`, `QuadraticVariation`, `Semimartingale`, `ItoIsometry`, `ItoLemma` (5 Itô per-module) plus `CramerIID`, `Sanov`, `Contraction`, `CramerLowerBound`, `Varadhan`, `LDPCompatibleSKEFT` (6 LDP per-module) = 11 per-module closures. `Novikov` has no per-module closure; only the overall `wave_3b_ito_overall_closure` exists. Total = 11 per-module + 1 overall-ito + 1 overall-ldp = 13 closures, not 12 per-module. The "one per module" parenthetical is wrong.
- **Evidence:** Direct enumeration of `^theorem wave_3b_` declarations across all 12 modules.
- **Expected:** Replace with "11 per-module wave-closure summary theorems + 2 overall-wave closures (one per Itô/LDP namespace); the Novikov module folds its closure into the overall Itô closure".
- **Fix:** Replace bullet at line 380 with the corrected statement.
- **Cache:** verified-by-direct-enumeration

### 7.5 — 🔵 RECOMMENDED — `~12-14 PRs` in §7 is plausible but lightly grounded

- **Gate:** NumericalFreshness (CountFreshness sub-class)
- **Location:** `papers/I3/paper_draft.tex:897-898` ("The total PR cycle is therefore $\sim 12$--14 PRs (depending on the ItoLemma split)")
- **Observed:** The `~12-14 PR` estimate comes from §7.1 enumeration (1+1+1+1+2or3+1+1+1+1+1+1+0 = 11 or 12 if ItoLemma takes 2; 11 or 13 if 3). The actual range from the enumeration is `~11-13`, not `~12-14`.
- **Evidence:** Direct count of the §7.1 enumerated items.
- **Expected:** Replace `~12-14 PRs` with `~11-13 PRs`.
- **Fix:** `s/$\sim 12$--14 PRs/$\sim$11--13 PRs/` at line 897.
- **Cache:** verified-by-direct-enumeration

### 8.1 — N/A (no production runs)

I3 makes no production-run claims (no MC simulation, no RHMC, no numerical evidence claim). Gate 8 ProductionRunHealth has no applicable findings.

## QI Candidate

**Systemic pattern:** I3 is the first sourceless bundle (no per-paper substrate) and exhibits a specific failure mode: when a bundle's prose is drafted *fresh* against a Lean substrate that ships `Prop := True` placeholders, the gap between the prose's substantive-sounding language and the substrate's near-vacuous content can be wide. The standard Stage-13 NarrativeGrounding gate catches "Monte Carlo evidence with no run" overclaims but does not yet have a specific check for "this `Prop`-typed predicate is `True` and the prose calls its inhabitation 'substantive'". A Stage-12-or-13 check that scans for `def X (...) : Prop := True` declarations cited in a paper's prose with adjectives like "substantive", "substantial", "load-bearing", "non-trivial" would catch this class earlier. Candidate name: `validate.py --check prop_true_substantive_overclaim`. Trigger: bibitem-grep for `\texttt{IsFoo}` in `.tex`, then resolve to the Lean source, then check the `Prop`-body, then scan the prose paragraph for "substantive" / "substantial" / "load-bearing" / "non-trivial" / "rich". Severity: REQUIRED.

**Counts:** 6 BLOCKER (3.1, 3.2, 3.3, 5.1, 5.3, 7.1, 7.2, 7.3) — wait, recount: 1.1, 3.1, 3.2, 3.3, 5.1, 5.3, 7.1, 7.2, 7.3 = 9 BLOCKER. Let me recount: actual BLOCKER findings are 1.1, 3.1, 3.2, 3.3, 5.1, 5.3, 7.1, 7.2, 7.3 = **9 BLOCKER**. REQUIRED: 1.2, 3.4, 5.2, 5.4, 5.6, 6.1, 7.4 = **7 REQUIRED**. RECOMMENDED: 1.3, 5.5, 6.2, 7.5 = **4 RECOMMENDED**. Total = **20**.

(Note: the summary at top was an undercount; this footer is correct.)

## Anchors not verified

- **Anchor #2 (Doob martingales = Ying-Degenne 2022):** verified via WebSearch but the I3 bibliography does not include the citation — see finding 1.1.
- **Anchor #5 (Mathlib upstream coordination integrity):** the paper's §7.2 explicitly disclaims any Zulip/GitHub coordination ("We have not, as of this release, opened any Zulip thread, GitHub issue, or PR coordination artifact"). This is honest and satisfies the anchor (the anchor requires that *claims* about coordination trace to artifacts; the paper makes no such claims). No finding emitted.
- **HOL Light / Mizar / Agda first-claim scope:** the paper explicitly disclaims exhaustivity beyond Coq + Isabelle/HOL + Lean. The exclusion is honest but narrows the first-claim's strength; see finding 5.2.
