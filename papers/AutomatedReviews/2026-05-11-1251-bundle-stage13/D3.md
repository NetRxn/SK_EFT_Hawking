---
bundle: D3
paper: D3_emergent_gravity_bh_thermo
reviewer: adversarial-reviewer
model: claude-opus-4-7-1m
review_date: 2026-05-11T12:51:00Z
readiness_gates_version: 1
round: 1
verdict: RED
blockers_open: 5
findings_total: 16
findings_blocker: 5
findings_required: 6
findings_recommended: 5
---

# Adversarial Review — D3_emergent_gravity_bh_thermo (Stage-13 fresh-context, 2026-05-11)

## Summary

Bundle D3 (Tier-1 deep, ~50pp PRD-long-format, 28 substantive sections + 7
post-bibliography lift-stub headings) is **NOT submission-ready**. Fresh-context
walk of the 8 finding classes surfaces **5 BLOCKERs** (4 CitationIntegrity, 1
NarrativeGrounding tied to a Lean-cite/prose-numerics mismatch), **6 REQUIREDs**
(LeanProofSubstance overclaims on in-flight modules, ParameterProvenance drift
on the GMOR PDG numerics, AssumptionDisclosure, CrossPaperConsistency with
D5/EntropicGravityDarkEnergy, structural incompleteness via 7 TODO stubs in the
body), and **5 RECOMMENDEDs**.

The newly absorbed §17.5 "Sakharov–horizon-Crooks unification" (Phase 7
Session 1 D.3 GATE 2) carries a substantive overclaim at line 1384 about
Verlinde-class entropic gravity being ≥5σ disfavoured across **all eight**
observational checks — this contradicts the Phase 6n+6o Session 5
(2026-05-08) honest downgrade of Barrow HDE from `barrow_log_bayes := 5.5`
(unsourced) to `barrow_aic_delta := 4.7` (Burnham–Anderson moderate-AIC, NOT
Jeffreys decisive). The companion bundle D5 (just reviewed in the same batch
at `D5.md`) cites the same Lean module honestly; D3 §17.5 does not.

The most damaging structural problem is **7 empty `\section{...}` stubs**
inserted after §28 (lines 2225–2313) by `bundle_append.py` from Phase 7
Session 1's 28 D.2/D.4 absorption events — every one of these stubs renders
in the compiled PDF as a section heading with only `%% TODO: lift content`
comments below. Per `feedback_bundle_append_post_bib_section_stubs.md` these
need to be commented out OR the lifts need to land before submission. The
seven affected sections (W1a, W2c on Phase 6n; W1a, W1b, W3b, W1c, W2a on
Phase 6o) are all advertised in the abstract or front-matter narrative as
load-bearing but have zero body content in the draft.

## Findings

### 1.1 — 🔴 BLOCKER — `Wen2003` registry entry is malformed; D3 closure-triad load-bearing

- **Gate:** CitationIntegrity
- **Location:** `papers/D3/paper_draft.tex:130, 413, 2642`; registry at
  `src/core/citations.py` (single key `'Wen2003'`)
- **Observed:** Registry entry reads
  `'authors': 'X.-G.~Wen, Phys.\\ Rev.\\ D , 065003 (2003)'` (authors string
  contains journal data, no separable author list), `'title': ''` (empty),
  `'journal': None`, `'page': None`, `'doi': None`, `'arxiv': None`,
  `'doi_verified': False`. The D3 bibitem at line 2642 expands to
  "X.-G.~Wen, ``Quantum order from string-net condensations and the origin
  of light and massless fermions,'' Phys.\ Rev.\ D \textbf{68}, 065003
  (2003), arXiv:hep-th/0302201." — registry has none of this metadata.
- **Evidence:** `grep -B1 -A12 "'Wen2003':" src/core/citations.py` returns
  the single corrupt entry; cache lookup for `Wen2003` in
  `docs/citation_verifications.jsonl` returns no entries.
- **Expected:** Single canonical `Wen2003` entry with `authors:
  'Wen, X.-G.'`, `title: 'Quantum order from string-net condensations and
  the origin of light and massless fermions'`, `journal: 'Phys. Rev. D'`,
  `volume: 68`, `page: '065003'`, `year: 2003`,
  `doi: '10.1103/PhysRevD.68.065003'`, `arxiv: 'hep-th/0302201'`,
  `doi_verified: True`, with `primary_source_path` populated under
  `Lit-Search/Phase-X/primary-sources/Wen2003.pdf`.
- **Fix:** WebFetch arXiv:hep-th/0302201 to confirm title and authors;
  replace the malformed registry block with a clean canonical entry; add
  primary-source cache file per Pipeline Invariant #11.
- **Cache:** fresh-fetch required (no prior cache entry).
- **Load-bearing rationale:** D3's three-closure-triad architecture
  (§1 "Three closures that force ADW", §4 "Closures forcing ADW") rests on
  the Wen-ADW factor-6000 deficit as Closure (i). The Wen 2003 paper is
  the primary anchor for the perturbative emergent-U(1)-from-string-net
  construction the deficit is computed against. A paper cannot ship a
  forced-closure argument anchored on a citation whose primary metadata
  is empty.

### 1.2 — 🔴 BLOCKER — `KSS2005` registry entry is malformed; D3 §14 cites for KSS viscosity bound

- **Gate:** CitationIntegrity
- **Location:** `papers/D3/paper_draft.tex:1132, 2518`; registry at
  `src/core/citations.py`
- **Observed:** Registry entry reads
  `'authors': 'P.~Kovtun, D.~T.~Son, and A.~O.~Starinets, Phys. Rev. Lett. , 111601 (2005)'`
  (authors field contains journal+page+year), `'title': ''` (empty),
  `'journal': 'Phys. Rev. Lett'` (no trailing period), `'page': None`,
  `'doi': None`, `'arxiv': None`, `'doi_verified': False`. D3 bibitem at
  line 2518 expands to PRL 94, 111601 (2005), arXiv:hep-th/0405231 — none
  of this is in the registry.
- **Evidence:** `grep -B1 -A12 "'KSS2005':" src/core/citations.py` returns
  the corrupt entry; no cache record.
- **Expected:** Clean canonical entry with `authors: 'Kovtun, P., Son, D. T.,
  Starinets, A. O.'`, `title: 'Viscosity in Strongly Interacting Quantum
  Field Theories from Black Hole Physics'`,
  `journal: 'Phys. Rev. Lett.'`, `volume: 94`, `page: '111601'`,
  `year: 2005`, `doi: '10.1103/PhysRevLett.94.111601'`,
  `arxiv: 'hep-th/0405231'`, `doi_verified: True`.
- **Fix:** WebFetch arXiv:hep-th/0405231; rewrite registry block; add
  primary-source cache file.
- **Cache:** fresh-fetch required.

### 1.3 — 🔴 BLOCKER — `Roehm2026Strategy` and `Roehm2026Modular` have duplicate `'inprep'` keys silently flipping False

- **Gate:** CitationIntegrity
- **Location:** `src/core/citations.py` `Roehm2026Strategy` and
  `Roehm2026Modular` blocks
- **Observed:** Both registry blocks contain TWO `'inprep'` keys in the
  same dict literal — the first is `True` (paired with `doi_verified: True`
  on the same line), the second is `False` on the next line; Python's
  last-write-wins semantics retains `False`. Resulting registry-state:
  these self-citations are marked NOT in-preparation, which means
  `validate.py --check citation_primary_sources_present` (Pipeline
  Invariant #11) expects them to have a downloadable primary source —
  they have `primary_source_path: None` (correct), so the invariant
  flags them. The `notes` field for `Roehm2026Strategy` explicitly says
  "Inprep exemption per Pipeline Invariant #11" — but the live flag is
  `False`.
- **Evidence:** `grep -B1 -A12 "'Roehm2026Strategy':" src/core/citations.py`
  shows the two-line `inprep` key collision; same pattern at
  `'Roehm2026Modular':`.
- **Expected:** Single `'inprep': True` line in each block; remove the
  duplicate `'inprep': False` line introduced by an auto-merge.
- **Fix:** Delete the second `'inprep': False` line in each block;
  keep `'inprep': True` paired with `doi_verified: True`.
- **Cache:** structural-bug; not a fetch issue.
- **Load-bearing rationale:** `Roehm2026Modular` is the L2 self-cite for
  D3's $N_f=16$ anchor (§1, §12, §16, §28 closures); shipping the bundle
  with the in-prep flag wrong destabilises the L2/D3 cross-bundle
  audit pipeline (the in-prep self-cite namespace is how the bundle
  graph distinguishes companion papers from external references).

### 1.4 — 🔴 BLOCKER — `WalkerWang2012` has duplicate registry entries

- **Gate:** CitationIntegrity
- **Location:** `src/core/citations.py` — two `'WalkerWang2012'` keys
- **Observed:** Two `WalkerWang2012` blocks in the same dict; entry 1
  has `'doi_verified': True` + `'primary_source_path': None` (used_in
  paper26), entry 2 has `'doi_verified': '2026-04-29'` +
  `'primary_source_path': 'Lit-Search/Phase-6d/primary-sources/WalkerWang2012.pdf'`
  with WebFetch verification note. Python keeps the second entry; the
  first is silently shadowed. The two entries also use different `page`
  formatting (`150` vs `'150'`).
- **Evidence:** `grep -n "'WalkerWang2012':" src/core/citations.py`
  returns two matches.
- **Expected:** Single canonical entry — the second (with verified DOI
  and cached primary source) is the better one; the first is the
  shadowed redundant copy.
- **Fix:** Delete the first `WalkerWang2012` block; preserve the second.
  Same anti-pattern as `KaulMajumdar2000` duplicate from the
  `2026-05-04-bundle-stage13/D3.md` review (still open per same review
  finding 1.2; the duplicate-entry class is reproducible across the
  registry and needs a `validate.py --check
  citation_registry_unique_keys` sweep).
- **Cache:** structural-bug.

### 1.5 — 🔴 BLOCKER — D3 §15.2 GMOR PDG numerics drift from Lean `gmor_pdg_match` discharge values by an order of magnitude on ⟨q̄q⟩

- **Gate:** CitationIntegrity (Lean-cite-vs-prose) +
  ParameterProvenance + LeanProofSubstance (composite)
- **Location:** `papers/D3/paper_draft.tex:1166-1171`
- **Observed:** D3 §15.2 reads "`ChiralSSB_QCD.gmor_pdg_match` discharges
  the numerical match of (eq. gmor) against PDG values: $f_\pi = 92.07$ MeV,
  $m_\pi = 134.98$ MeV, $m_q = 3.45$ MeV (mean of $m_u + m_d$), and
  $\langle\bar{q}q\rangle = -(2.62\times 10^8\,\mathrm{MeV}^3)$, all within
  the GMOR tolerance." The Lean theorem at
  `lean/SKEFTHawking/ChiralSSB_QCD.lean:125-138` actually discharges
  $|gmor\_lhs(0.137, 0.092) - gmor\_rhs\_real(0.0035, -0.0227)| < 10^{-4}$,
  i.e. $f_\pi = 92$ MeV (not 92.07), $m_\pi = 137$ MeV (not 134.98),
  $m_q = 3.5$ MeV (not 3.45), and $\sigma = -0.0227$ GeV³ =
  $-2.27\times 10^7$ MeV³ (NOT $-2.62\times 10^8$ MeV³).
- **Evidence:** Lean docstring: "`m_π = 0.137 GeV`, `f_π = 0.092 GeV`,
  `m_q = 0.0035 GeV` (avg of m_u + m_d / 2), `σ = −0.0227 GeV³` (FLAG
  2021). LHS: `(0.137)² · (0.092)² = 0.018769 · 0.008464 ≈ 1.589e-4 GeV⁴`.
  RHS: `−2 · 0.0035 · (−0.0227) = 1.589e-4 GeV⁴`."
- **Expected:** Either (a) the prose numerics in D3 match the Lean
  discharge values exactly (so a reader who checks the Lean theorem
  finds the same numbers), or (b) the Lean theorem is rewritten with
  the more precise PDG 2024 values D3 cites. Note the ⟨q̄q⟩
  magnitudes differ by a factor of ~11.5 — the canonical literature
  value $\langle\bar{q}q\rangle \approx -(283\,\mathrm{MeV})^3 \approx
  -2.27\times 10^7\,\mathrm{MeV}^3$ matches the Lean value; D3's
  $-2.62\times 10^8$ MeV³ is plausibly a transcription error (off by ×10)
  or it cites a different normalisation convention without disclosure.
- **Fix:** Reconcile prose ↔ Lean. The simplest fix is to either (i)
  rewrite the prose to "`gmor_pdg_match` discharges the relation at
  FLAG-2021 central values ($f_\pi=92$ MeV, $m_\pi=137$ MeV, $m_q=3.5$ MeV,
  $\sigma=-0.0227$ GeV³ = $-2.27\times 10^7$ MeV³); the precise PDG
  2024 values $f_\pi=92.07$ MeV / $m_\pi=134.98$ MeV / $m_q=3.45$ MeV
  lie within the same GMOR tolerance band but are not those discharged
  by the named theorem"; or (ii) strengthen the Lean theorem to use the
  precise PDG values. Treat as an integrity-of-the-cite issue: D3 names
  the Lean theorem and quotes specific numbers; the numbers must come
  from the named theorem (or the citation pattern must be relaxed to
  cite a separate `gmor_pdg_2024_match` theorem with the actual PDG
  numerics).
- **Cache:** structural numerical-attribution; N/A.

### 2.1 — 🟡 REQUIRED — D3 §17.5 contradicts D5 / EntropicGravityDarkEnergy on Verlinde-class 8-check unanimity

- **Gate:** CrossPaperConsistency + NarrativeGrounding
- **Location:** `papers/D3/paper_draft.tex:1383-1390`
- **Observed:** D3 §17.5 reads "Verlinde-class entropic gravity is
  $r_d$-independent disfavoured at $\geq 5\sigma$ across **all eight**
  independent observational checks canvassed (CMB + Bullet Cluster,
  Tsallis HDE, Barrow HDE, Odintsov dark energy, the Padmanabhan–Cosmin
  no-scalar-perturbation-theory criterion, the Hossenfelder–Verlinde
  dS-instability constraint, the Cadoni–Tuveri DEC bound, and the
  DEC–CPL DESI~DR2 1$\sigma$ exclusion); the unanimous-NO-GO closure is
  the program's Phase 6m Track B headline." But per the Phase 6n+6o
  Session 5 honest downgrade (2026-05-08, MEMORY.md entry
  `project_phase6n6o_absorption_session5.md`),
  `EntropicGravityDarkEnergy.lean` ships `barrow_aic_delta := 4.7` with
  `aic_moderate_threshold := 4.0` (Burnham–Anderson) and the explicit
  theorem `barrow_aic_delta_below_jeffreys_decisive : barrow_aic_delta <
  jeffreys_decisive_threshold` — i.e., Barrow HDE is **NOT** at ≥5σ /
  Jeffreys-decisive; it's moderate-AIC only. The Lean aggregator
  `all_three_decisive_bayes_bounds_exceed_jeffreys_decisive` is 3-of-4,
  not 4-of-4; `all_quantitative_bounds_disfavoured` is 4-of-4 but
  **mixed-threshold** (Verlinde+Tsallis+Odintsov at Jeffreys-decisive,
  Barrow at AIC-moderate).
- **Evidence:** `grep "barrow_aic_delta\|all_three_decisive\|all_quantitative_bounds_disfavoured"
  lean/SKEFTHawking/EntropicGravityDarkEnergy.lean` returns
  `barrow_aic_delta := 4.7`, `aic_moderate_threshold := 4.0`,
  `jeffreys_decisive_threshold := 5.0`,
  `theorem barrow_aic_delta_below_jeffreys_decisive : barrow_aic_delta <
  jeffreys_decisive_threshold`. D5 §11
  (`papers/D5/paper_draft.tex:70-77`) reads honestly: "(Barrow HDE)
  reaches Burnham-Anderson moderate-disfavour at the information-criteria
  level" + cites `all_three_decisive_bayes_bounds_exceed_jeffreys_decisive`
  (3 of 4) and `all_quantitative_bounds_disfavoured` (4 of 4 mixed-threshold).
- **Expected:** D3 §17.5 prose to read "Verlinde-class entropic gravity
  is $r_d$-independent disfavoured at decisive-Jeffreys ≥5σ for the
  three Bayes-decisive bounds (CMB+Bullet Cluster, Tsallis HDE, Odintsov
  DE) and at Burnham–Anderson moderate-AIC for the fourth (Barrow HDE);
  combined-quantitative aggregator
  `all_quantitative_bounds_disfavoured` closes at 4-of-4 mixed-threshold,
  decisive-Bayes aggregator `all_three_decisive_bayes_bounds_exceed_jeffreys_decisive`
  at 3-of-4."
- **Fix:** Rewrite D3 §17.5 lines 1383–1390 to match D5 §11's honest
  characterisation; cite both Lean aggregators by name; drop the
  ≥5σ-across-all-eight claim. Also update the count "eight" → "four
  quantitative bounds + four structural / qualitative criteria" if the
  intended scope was the larger set.
- **Cache:** N/A.
- **Cross-bundle propagation:** D5 has already absorbed the honest
  downgrade; D3 has not. The mismatch is precisely the failure class
  Pipeline Invariant #11+#13 are meant to catch.

### 3.1 — 🟡 REQUIRED — Abstract + §1 + §4 + §28 quote a "factor ≈ 6000" Wen-ADW deficit but the named Lean lemma does not exist

- **Gate:** LeanProofSubstance + NarrativeGrounding
- **Location:** `papers/D3/paper_draft.tex:35` (abstract); `137` (§1
  closure-triad); `427-438` (§4 Perturbative Wen-ADW deficit); `2055`
  (§28); `2145` (§28 tracked-hypothesis registry entry
  `wen_adw_factor_6000`)
- **Observed:** Abstract reads "the Wen–ADW gravity-emergence channel
  underpredicts $G_N$ by a factor $\approx 6000$"; §4 reads "$
  G_{\mathrm{Wen}}/G_c^{\mathrm{ADW}} \approx 1/6000$" and "The deficit
  is a closed-form Lean identity — a closed-form Lean-derived ratio
  rather than a numerical estimate (the algebraic ratio enters as a
  coefficient identity downstream of `GaugeErasure.gauge\_erasure`; an
  explicit `wen\_adw\_factor\_6000`-named lemma is in flight as a
  single-line corollary)." But:
  `grep -rn "wen_adw_factor_6000\|Wen_ADW_Factor" lean/` returns **zero
  matches**. No `wen_adw_factor_6000` lemma exists; the "closed-form
  Lean-derived ratio" claim has no Lean discharge.
- **Evidence:** `find lean -name "*.lean" -exec grep -l "6000\|Wen" {}
  \;` returns no Lean module that ships a factor-6000 identity.
  `GaugeErasure.gauge_erasure` exists but does not produce a Wen-ADW
  ratio.
- **Expected:** Either (a) the lemma `wen_adw_factor_6000` ships in Lean
  before D3 submits, OR (b) the prose is downgraded to "numerical
  estimate from the Wen-string-net effective action" with a citation
  to the published Wen 2003 + a project-internal derivation note in
  `Lit-Search/`. The current text claims a Lean machine-check that
  does not exist.
- **Fix:** Land `wen_adw_factor_6000` in `lean/SKEFTHawking/`; remove
  the parenthetical "in flight" hedge once it lands. Until then,
  rewrite the abstract and §4 to remove the "machine-verified" and
  "closed-form Lean identity" language for this specific quantity.
- **Cache:** N/A.
- **Note:** §28's tracked-hypothesis registry line 2145 already lists
  `wen_adw_factor_6000` as "in-flight numerical / non-Lean tracked
  premise"; that disclosure is honest but inconsistent with the
  abstract and §4 prose which treat it as a closed Lean result.

### 3.2 — 🟡 REQUIRED — `HigherCurvatureStelle.lean` and `predictions_below_observational_ceilings` cited as discharging Stelle bounds but the module does not exist

- **Gate:** LeanProofSubstance
- **Location:** `papers/D3/paper_draft.tex:1422-1426` (§18.1);
  `1451-1456` (§18.3 observational ceilings)
- **Observed:** §18 reads "...is in flight as a standalone
  `HigherCurvatureStelle.lean` module to be upstreamed in a subsequent
  iteration." (line 1425). Line 1454: "the explicit
  `predictions\_below\_observational\_ceilings` closure ships in the
  in-flight `HigherCurvatureStelle.lean` module."
- **Evidence:** `grep -rn "HigherCurvatureStelle\|predictions_below_observational_ceilings"
  lean/SKEFTHawking/` returns **zero matches**. The module and
  the named theorem do not exist.
- **Expected:** Either land the module before submission, or
  downgrade the §18.3 prose: "the substrate's higher-curvature
  predictions sit at $\mathcal O(10^{-2})$ in geometric units, well
  beneath both ceilings (closed-form numerical check; the explicit
  Lean closure `HigherCurvatureStelle.predictions_below_observational_ceilings`
  is in flight)."
- **Fix:** Mark the §18 closure-push as "Conjectured / numerical-check
  only" until the Lean module lands; do not cite the missing theorem by
  name as if it discharges the ceiling-passage.
- **Cache:** N/A.

### 4.1 — 🟡 REQUIRED — Seven post-§28 lift-stub `\section{...}` headings render as empty sections in compiled PDF

- **Gate:** NarrativeGrounding (structural completeness) +
  CrossPaperConsistency
- **Location:** `papers/D3/paper_draft.tex:2225-2313`
- **Observed:** Seven `\section{...}` declarations appear after §28
  (the "Synthesis and outlook" close) with bodies containing ONLY
  `%% TODO: lift content from papers/_phase*_W*/paper_draft.tex` +
  `%% TODO: ensure all numerical claims trace via formulas.py /
  counts.tex` + `%% TODO: ensure all citations have primary-source
  cache entries`. The seven affected sections:
  - §`sec:d3-_phase6n_W1a_lean_only` "BEC SK-EFT geometric envelope (NOT
    Gevrey-1)" (line 2226)
  - §`sec:d3-_phase6n_W2c_lean_only` "LDP linear-response framework"
    (line 2239)
  - §`sec:d3-_phase6o_W1a_lean_only` "Boostless / Carrollian soft
    theorems on analog horizons (Phase 6o W1a)" (line 2252)
  - §`sec:d3-_phase6o_W1b_lean_only` "G4 Kerr-Schild double-copy on
    Petrov-D analog gravity" (line 2265)
  - §`sec:d3-_phase6o_W3b_lean_only` "Itô + LDP-α + LDP-β substrate
    (Phase 6o W3b cross-bridge)" (line 2278)
  - §`sec:d3-_phase6o_W1c_writeup` "Dissipative SK-EFT bootstrap-uniqueness
    NO-GO (Phase 6o W1c writeup)" (line 2291)
  - §`sec:d3-_phase6o_W2a_lean_only` "APS η-invariant for analog
    horizons (Phase 6o W2a)" (line 2304)
  All seven render in the PDF as TOC entries with no content. The user
  memory `feedback_bundle_append_post_bib_section_stubs.md` flags this
  as a `bundle_append.py` default-insertion artifact already affecting
  9 of 14 bundles.
- **Evidence:** `grep -n "TODO" papers/D3/paper_draft.tex` returns 21
  matches, all in lines 2231–2313. `grep -n "BUNDLE_APPEND_INSERT_HERE"
  papers/D3/paper_draft.tex` returns line 2315 — confirming the seven
  sections were inserted by `bundle_append.py` and not yet
  body-populated.
- **Expected:** Either (a) the 7 lift sources are body-lifted with
  content before submission, OR (b) the 7 sections are commented out
  (lines wrapped with `%%`) per the memory's "Cleanup: comment lines →
  `%%`" remediation. The current state ships 7 empty sections in
  a body that the abstract advertises as a 22-thread synthesis.
- **Fix:** Comment out the 7 stub sections with `%%` prefix (mechanical
  one-pass); add a TODO note in the bundle's `change_log.md` recording
  that the 7 lifts remain pending and identifying which source paper
  feeds each. Alternative: do the actual lifts; this is multi-session
  work and probably not within the submission window.
- **Cache:** N/A.
- **Severity rationale:** This is REQUIRED rather than BLOCKER because
  the seven empty sections don't introduce false claims — they
  introduce *structural incompleteness*. A reviewer who hits these in
  the PDF will form a clear negative impression, but the prose
  upstream of §28 is internally self-contained.

### 4.2 — 🟡 REQUIRED — §28 tracked-hypothesis registry omits several Props the body references

- **Gate:** AssumptionDisclosure
- **Location:** `papers/D3/paper_draft.tex:2081-2148` (§28
  tracked-hypothesis registry subsection); body references throughout
- **Observed:** §28's tracked-hypothesis registry enumerates 11 Props
  (Vergeles trio, `H_HorizonBoundaryCondition`, `H_TopologicalOrderBeyondLG`,
  `H_TetradQuarkScalesNatural`, `H_RegimePartition`,
  `H_NonlinearEFEHolds`, `H_NonlinearDiffInvariance`,
  `H_EinsteinCartanExtensionHolds`, `H_MicroscopicCoefficientMatch`,
  `H_VestigialModeIsGraviton`, `H_VerlindeKMLiteralSumDerivation`). But
  the body also references:
  - `IsADWPenroseApplicable` (§24 line 1889) — substrate-specific
    Penrose applicability predicate; not in the registry
  - The 5 falsifier theorems on `H_HorizonBoundaryCondition` (§7
    lines 686-691) — these are listed under the parent Prop but the
    closure path isn't stated
  - The 5 falsifier theorems on the BCH four-laws bundle (§8 lines
    790-794: `falsifier_acoustic_decay_form`,
    `falsifier_schwarzschild_heating`, `falsifier_third_law_form`,
    `falsifier_alpha_ADW_dependence`,
    `four_laws_consistent_with_acoustic_regime`) — referenced as
    "tracked at the Lean level"
  - The CFL `H_TopologicalOrderBeyondLG_falsifier_trivial` and
    `_falsifier_too_large` (§16 line 1228)
- **Evidence:** `grep "IsADWPenroseApplicable" papers/D3/paper_draft.tex`
  returns only line 1889 (body reference); not in §28 registry.
- **Expected:** §28 registry includes IsADWPenroseApplicable +
  closure path; the H_HorizonBoundaryCondition entry explicitly
  enumerates the 5 falsifier theorems by name; the BCH four-laws
  bundle is added as a §28 registry entry with its 5 falsifiers.
- **Fix:** Add IsADWPenroseApplicable as a 12th tracked-hypothesis
  entry; expand the existing H_HorizonBoundaryCondition entry to
  enumerate the 5 falsifiers; add a new H_RegimePartition entry
  enumerating the 5 BCH four-laws falsifiers. Mirror the
  module-qualified-name + closure-path format already used.
- **Cache:** N/A.

### 4.3 — 🟡 REQUIRED — Newly absorbed §17.5 cites Stage-2/3 deferral on `Sakharov_iff_horizon_Crooks` but the in-flight Lean target is not registered in the program's `SORRY_GAPS` registry

- **Gate:** AssumptionDisclosure
- **Location:** `papers/D3/paper_draft.tex:1320-1360` (§17.5)
- **Observed:** §17.5 reads "is conjectured biconditional, at the Stage-1
  substrate-level encoded in
  `lean/SKEFTHawking/CrooksAnalogHawking/SakharovHorizonCrooks.lean`
  (the explicit `Sakharov_iff_horizon_Crooks` bridge is itself deferred
  to Stage 2--3 of the same module)" (line 1322-1324). And later:
  "the actual horizon-detailed-balance witnesses for each substrate
  are placeholders (`WorkDistribution.zero`, $\sigma\equiv 0$) pending
  Stage 2--3." (line 1366-1368).
- **Evidence:** `grep -n "Sakharov_iff_horizon_Crooks"
  lean/SKEFTHawking/CrooksAnalogHawking/SakharovHorizonCrooks.lean`
  returns 0 matches. The deferral is honest but the substantive
  content gap (placeholder `WorkDistribution.zero` everywhere) is not
  registered as a tracked-hypothesis in §28.
- **Expected:** Add §28 registry entry: "`HorizonCrooksSubstrate`
  Stage-2/3 substantive witnesses — currently placeholders
  (`WorkDistribution.zero`, $\sigma\equiv 0$); load-bearing closure
  path: explicit substrate-specific work-distribution + entropy-
  production witnesses required to make the partition `${}^3$He-A
  pass / FLS BEC fail` genuinely fluctuation-theorem-content rather
  than four-Boolean-Sakharov restriction."
- **Fix:** Either land the Stage-2/3 witnesses in Lean, OR add the
  tracked-hypothesis disclosure to §28.
- **Cache:** N/A.

### 4.4 — 🟡 REQUIRED — D3 §24 cites `correctness_push_biconditional_under_applicable` but does not disclose `IsADWPenroseApplicable` content

- **Gate:** AssumptionDisclosure
- **Location:** `papers/D3/paper_draft.tex:1885-1893`
- **Observed:** "Under `IsADWPenroseApplicable` (the substrate-specific
  applicability predicate), the Penrose hypothesis is satisfied if and
  only if the spacetime is null-geodesically incomplete to the future
  of the trapped surface." But the prose doesn't enumerate what
  `IsADWPenroseApplicable` actually requires — and per the
  preemptive-strengthening discipline in `CLAUDE.md` ("Cross-module
  bridge integrity (P6)"), if the docstring or prose references a
  substrate-specific predicate, the *content* of that predicate must
  be in the paper.
- **Evidence:** `grep "IsADWPenroseApplicable" papers/D3/paper_draft.tex`
  returns just one line (line 1889); the substrate-specific content
  is not unfolded.
- **Expected:** A 2-3 sentence unfolding of what
  `IsADWPenroseApplicable` requires (e.g., ADW substrate scale +
  trapped-surface presence + global-hyperbolicity witness + NEC
  closure on the ADW stress-energy).
- **Fix:** Add an `IsADWPenroseApplicable` field listing in §24.3
  ("Correctness-push biconditional under applicability").
- **Cache:** N/A.

### 5.1 — 🔵 RECOMMENDED — D3 abstract Λ_emerg/Λ_obs bound "$\gtrsim 10^{100}$ ... heuristically $\sim 10^{122}$" — the rigorous lower bound vs heuristic gap is unexplained

- **Gate:** NarrativeGrounding
- **Location:** `papers/D3/paper_draft.tex:48-50` (abstract); `1604-1609`
  (§21.2 microscopic predictions)
- **Observed:** Abstract claim
  "$\Lambda_{\mathrm{emerg}}/\Lambda_{\mathrm{obs}} \gtrsim 10^{100}$
  (the formal Lean lower bound; heuristically $\sim 10^{122}$)". §21.2
  explains: "the residual two-orders gap to the heuristic estimate
  $\gtrsim 10^{100}$ from the substrate-natural formula reflects the
  precision of Mathlib's shipped fundamental constants used at the
  formal-proof match locus."
- **Evidence:** Lean theorem
  `MicroscopicCoefficientMatch.lambdaEmergMicroscopic_at_planck_natural_far_exceeds_observed`
  exists and discharges the $\gtrsim 10^{100}$ lower bound.
- **Expected:** Either the abstract drops "heuristically $\sim 10^{122}$"
  (which is widely-cited Weinberg textbook number not a substrate
  prediction) OR it states clearly that the $10^{122}$ is the
  classical Weinberg estimate, not the substrate's tighter bound. The
  current "rigorous $10^{100}$ vs heuristic $10^{122}$" framing is
  confusing — a reader may interpret it as a 22-order-of-magnitude
  prediction uncertainty.
- **Fix:** Abstract line 50: replace "(the formal Lean lower bound;
  heuristically $\sim 10^{122}$)" with "(formal Lean lower bound;
  the classical Weinberg estimate of $\sim 10^{122}$ is recovered when
  Mathlib's shipped precision is sufficient to evaluate to that order)".
- **Cache:** N/A.

### 5.2 — 🔵 RECOMMENDED — `Adler1980` registry entry has `doi_verified: None` and no primary source path; foundational paper

- **Gate:** CitationIntegrity
- **Location:** `src/core/citations.py:9161-9176`; D3 bibitem at
  `papers/D3/paper_draft.tex:2336-2340`
- **Observed:** `Adler1980` (Phys. Rev. Lett. 44, 1567, 1980 — the
  Adler order-R vacuum-action paper, half of the Sakharov–Adler
  induced-gravity foundation) has `doi: '10.1103/PhysRevLett.44.1567'`
  but `doi_verified: None` and `primary_source_path: None`. The DOI
  resolves; the bibitem is fetchable; verification has not been done.
- **Evidence:** Registry block shows `doi_verified: None`,
  `primary_source_path: None`; no entry in
  `docs/citation_verifications.jsonl`. Note: this is NOT a Pipeline
  Invariant #11 textbook exemption because the entry HAS a DOI.
- **Expected:** WebFetch the DOI to confirm; flip `doi_verified: True`;
  add primary-source PDF cache.
- **Fix:** Verify via WebFetch and update the registry.
- **Cache:** fresh-fetch.

### 5.3 — 🔵 RECOMMENDED — `Sakharov1968` foundational paper has no primary source; pre-DOI; would qualify for textbook exemption

- **Gate:** CitationIntegrity
- **Location:** `src/core/citations.py` `'Sakharov1968':` block; D3
  bibitem at `papers/D3/paper_draft.tex:2592-2596`
- **Observed:** `Sakharov1968` (Sov. Phys. Dokl. 12, 1040, 1968) has
  `doi: None`, `arxiv: None`, `primary_source_path: None`, no
  verification record. The Pipeline Invariant #11 textbook/pre-DOI
  exemption permits this (no DOI + no arxiv + no primary source =
  canonical pre-DOI), but the registry entry lacks the explanatory
  `notes` field that would document this exemption.
- **Evidence:** Registry block; D3 abstract + §1 + §21 cite this
  paper as the Sakharov-induced-gravity anchor.
- **Expected:** Add `notes` field: "Pre-DOI (1968 Soviet Physics
  Doklady); canonical-reference textbook exemption per Pipeline
  Invariant #11. Verified via secondary academic citations." Same
  pattern as `Trautman1973` mentioned in the invariant text.
- **Fix:** Append `notes` field documenting the exemption.
- **Cache:** N/A.

### 5.4 — 🔵 RECOMMENDED — `Verlinde2017dSEmergent` has `doi_verified: None` despite being cited in newly-absorbed D3 §17.5

- **Gate:** CitationIntegrity
- **Location:** `src/core/citations.py` `'Verlinde2017dSEmergent':` block
- **Observed:** Registry has `doi: '10.21468/SciPostPhys.2.3.016'`,
  `arxiv: '1611.02269'`, `primary_source_path` populated, but
  `doi_verified: None`. The bibitem is newly load-bearing for D3
  §17.5's Verlinde-vs-Jacobson distinction (Phase 7 Session 1
  absorption).
- **Evidence:** Registry block; no cache record under bibkey.
- **Expected:** WebFetch DOI or arXiv to confirm; flip
  `doi_verified: True`.
- **Fix:** Verify and update.
- **Cache:** fresh-fetch.

### 5.5 — 🔵 RECOMMENDED — D3 §6 "approximately $7\times 10^{14}$" vs L1 "$7.21\times 10^{14}$" — precision drift across cross-bundle bridge

- **Gate:** CrossPaperConsistency
- **Location:** `papers/D3/paper_draft.tex:465, 605`;
  `papers/L1/paper_draft.tex:177, 251`
- **Observed:** D3 §6.1 line 465 reads "falsifies the entire natural
  range by approximately $7\times 10^{14}$" and abstract line 37 reads
  "fourteen orders of magnitude". L1 line 177 quotes the precise value
  $|\dccap|/\tau \approx 7.21\times 10^{14}$ at $\chi_{\rm vest}=10$
  endpoint. The numerical content matches at order-of-magnitude but
  the precision differs.
- **Evidence:** Both papers cite the same Lean theorem
  `GravitationalWaves.natural_range_disjoint_from_ligo_window`; no
  numerical contradiction in the underlying source.
- **Expected:** D3 §6 to either (a) match L1's precision ("approximately
  $7.21\times 10^{14}$ at $\chi=10$ endpoint, $2.28\times 10^{14}$ at
  $\chi=0.1$ endpoint"), or (b) explicitly stay at the order-of-magnitude
  rounded form. Current state has neither matched precision nor a
  clear rounding-to-1sf statement.
- **Fix:** Either match L1's "$7.21\times 10^{14}$" exactly, or rewrite
  as "by approximately $10^{14}$ at both endpoints of the natural
  range" (more honest at the rounded precision).
- **Cache:** N/A.

## QI Candidate

**Pattern:** Two corrupt registry entries (`Wen2003`, `KSS2005`) — each
with an empty title, no DOI, no arxiv, no primary source, and with the
authors field carrying garbled journal-plus-page content. These
registry blocks look like they were generated by an earlier
auto-extraction pass that took the LaTeX bibitem text and parsed it
into the authors field as a single string. The same pattern shows up
elsewhere in the registry whenever a bibitem hasn't been manually
canonicalised. The two cases here (Wen2003 closure-triad-anchor;
KSS2005 viscosity-bound-anchor) are both load-bearing for D3, but the
pattern is general.

**QI proposal:** add `validate.py --check citation_registry_well_formed`
which fails any registry entry whose `authors` field contains a digit,
a journal abbreviation (`Phys.`, `Rev.`, `Nucl.`, `Commun.`), or
parentheses (suggesting year-data leaked into authors); whose `title`
is the empty string; or whose `doi_verified: False` for an entry whose
bibitem cites a DOI in the `.tex`. Same registry block-by-block
schema-validation that the in-prep self-cite duplicate-`inprep` key
issue from finding 1.3 would catch.

**QI proposal #2:** add `validate.py --check
citation_registry_unique_keys` which fails any registry-state where a
bibkey appears in more than one dict block (the `WalkerWang2012`,
`KaulMajumdar2000`, `Sen2013` / `Sen2013Schwarzschild` cases). Same
class as Session-2's `feedback_promote_primary_sources_duplicate_keys.md`
memory; can re-use that helper.

**QI proposal #3:** add `validate.py --check
lean_module_cited_but_absent` which scans `.tex` `\texttt{...}` blocks
referencing a Lean module name (e.g. `HigherCurvatureStelle`,
`wen_adw_factor_6000`) and confirms each module/theorem exists in the
current `lean_deps.json`. This would catch findings 3.1 and 3.2
automatically.
