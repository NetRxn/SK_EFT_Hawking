---
paper: L2
bundle: L2
tier: 2-PRL-splash
reviewer: adversarial-reviewer
model: claude-opus-4-7[1m]
review_date: 2026-05-11T14:32:26Z
readiness_gates_version: 1
prior_close_verdict: GREEN (2026-05-01); 2 R2 BLOCKERs CLOSED at R3 (2026-05-06)
---

# Adversarial Review — Bundle L2 (Three SM fermion generations from modular invariance)

## Summary

Fresh-context Stage-13 review of L2 surfaces **4 BLOCKERs** and **5 REQUIREDs**. The headline finding is a Gate 5 LeanProofSubstance catastrophe: the Lean module `ChangeOfRings.lean` that the paper cites as having "established" the Hom-tensor adjunction reducing Ext_A to Ext_{A(1)} contains **zero substantive theorems** — every theorem in the module is `rfl` on a tautology (`rank = rank := rfl`, `ext_dim = ext_dim := h_ext`, `True := trivial`, or pure arithmetic via `norm_num`). The companion "tracked hypotheses" H1–H4 in `ExtBordismBridge.lean` are defined as `Prop := True`, so the bridge theorem `generation_constraint_chain` takes vacuous markers as input. The `A1Ext.lean` ext-dimension theorems are statements of the form `(Fintype.card (Fin 8)) / 8 = 1`, with the substantive Ext-group identification asserted by docstring and theorem-name only. Cross-bundle: the 5812-theorem count is stale (canonical 5855); D2 cites L2 under bibkey `Roehm2026Wave10` while L2 self-cites under `Roehm2026D2`, creating a paired-splash key asymmetry. Wang2024, Schellekens1993, MollerScheithauer2024, GarciaEtxebarria2019, Kitaev2009, FidkowskiKitaev2010 all WebFetch-verified content-match. L2 is **NOT submission-ready**.

## Findings

### 1.1 — 🔴 BLOCKER — Wang2024 registry entry has empty title; bibitem missing title field

- **Gate:** CitationIntegrity
- **Location:** `src/core/citations.py:6389–6405`; L2 bibitem at `papers/L2/paper_draft.tex:313–315`
- **Observed:** The L2 bibitem reads `J.~Wang, Phys.\ Rev.\ D \textbf{110}, 125028 (2024) [arXiv:2312.14928]` — no title. The `CITATION_REGISTRY['Wang2024']` entry has `'title': ''` (empty string) and is annotated `'notes': '... Title not present in source bibitem.'`. The paper is real (verified via arXiv:2312.14928 WebFetch): actual title is **"Family Puzzle, Framing Topology, c_-=24 and 3(E8)_1 Conformal Field Theories: 48/16 = 45/15 = 24/8 = 3"** by Juven Wang. The PRD volume 110 / page 125028 / year 2024 are plausible but PRD page-level abstract was not fetchable (403). The omitted title is load-bearing because the **abstract title is itself the formal claim** ("48/16 = 45/15 = 24/8 = 3") that L2 derives — readers should be able to see it.
- **Evidence:** WebFetch `https://arxiv.org/abs/2312.14928` returned title "Family Puzzle, Framing Topology, $c_-=24$ and 3(E8)$_1$ Conformal Field Theories: 48/16 = 45/15 = 24/8 =3" by Juven Wang.
- **Expected:** Bibitem string should include the actual title. Registry `Wang2024` `'title'` field should be populated.
- **Fix:** Update `papers/L2/paper_draft.tex:313` bibitem to `J.~Wang, ``Family Puzzle, Framing Topology, $c_-=24$ and $3(E_8)_1$ Conformal Field Theories'', Phys.\ Rev.\ D \textbf{110}, 125028 (2024) [arXiv:2312.14928]`. Backfill registry. Same applies to D2 (also cites Wang2024 sans title).
- **Cache:** fresh-fetch (no Wang2024 entry in `citation_verifications.jsonl`).

### 1.2 — 🟡 REQUIRED — Multiple registry entries have empty `title` fields (auto-stub artifact)

- **Gate:** CitationIntegrity
- **Location:** `src/core/citations.py` — affected entries: `AlvarezGaumeWitten1984` (line 4849, title=''), `FidkowskiKitaev2010` (line 5378, title=''), `GarciaEtxebarria2019` (line 5446, title=''), `GrossJackiw1972` (line 5533, title=''), `Rokhlin1952` (line 6048, title='').
- **Observed:** Five registry entries used by L2 carry `'title': ''` with the note `'Auto-generated stub from \bibitem block ... Title not present in source bibitem.'` The L2 bibitems themselves also omit titles (revtex4-2 PRL style permits this, but Gate 1 evaluator checks registry → bibitem agreement; both being empty means there is nothing to compare). For Garcia-Etxebarria-Montero the actual title is "Dai-Freed anomalies in particle physics" (JHEP 08 (2019) 003), verified via WebFetch of arXiv:1808.00009. Each fetched primary source had a real title that should be in the registry for downstream consumers (flagship paper bundle F bibliography, machine-readable provenance).
- **Evidence:** WebFetch arXiv:1808.00009 → "Dai-Freed anomalies in particle physics" by García-Etxebarria + Montero; WebFetch arXiv:0904.2197 → "The effects of interactions on the topological classification of free fermion systems" by Fidkowski + Kitaev; WebFetch arXiv:0901.2686 → "Periodic table for topological insulators and superconductors" by Kitaev.
- **Expected:** Each registry entry's `'title'` field populated with the primary-source title.
- **Fix:** Backfill titles in `CITATION_REGISTRY` for the five entries above. Optional: add titles to bibitem strings (PRL style guide allows author/journal/year-only form, so this is registry-side only).
- **Cache:** fresh-fetch for arXiv:1808.00009, arXiv:0904.2197, arXiv:0901.2686.

### 1.3 — 🟡 REQUIRED — `Kitaev2009` registry `used_in` list missing L2

- **Gate:** CitationIntegrity
- **Location:** `src/core/citations.py:1579–1595`
- **Observed:** Registry `Kitaev2009` `'used_in': ['papers/paper18_doublon_gate/paper_draft.tex']`. L2 cites Kitaev2009 at `papers/L2/paper_draft.tex:340–343`. Provenance integrity broken — the `used_in` list is the canonical mapping the dashboard + `paper_provenance` check rely on. Same `used_in` gap may affect other L2 bibkeys (multiple registry entries pre-date L2 lift and have not been updated).
- **Evidence:** `grep -n "papers/L2" src/core/citations.py | grep Kitaev2009` returns no match.
- **Expected:** `'used_in'` list should include `'papers/L2/paper_draft.tex'`.
- **Fix:** Append `papers/L2/paper_draft.tex` to `Kitaev2009` `used_in` list. Programmatic sweep recommended: walk all bibkeys cited from `papers/L2/paper_draft.tex` and confirm each registry entry's `used_in` includes the L2 path.
- **Cache:** n/a (registry hygiene, not a fetch).

### 1.4 — 🟡 REQUIRED — Three bibkeys for the same L2 splash (registry sprawl)

- **Gate:** CitationIntegrity / CrossPaperConsistency
- **Location:** `src/core/citations.py:7392` (`Roehm2026D2`), `:7483` (`Roehm2026Wave10`), `:7649` (`Roehm2026Modular`)
- **Observed:** Three registry entries point at semantically overlapping in-prep targets:
  - `Roehm2026D2` — title "Anomaly constraints on Standard Model particle content (Bundle D2)", used_in `papers/F/paper_draft.tex` (but L2 actually cites it at line 293, 383 — registry not updated).
  - `Roehm2026Wave10` — title "Three generations from modular invariance: Spin^{Z_4} five-bordism (Bundle L2 PRL splash)", used_in `[F, D2, D3, L2]` — but **L2 does not actually cite Roehm2026Wave10**; L2 cites Roehm2026D2.
  - `Roehm2026Modular` — title "Three Standard Model fermion generations from modular invariance (Bundle L2 splash)", used_in `[D3]` — explicit alias for Roehm2026Wave10 per its notes.
  
  Plus `Roehm2026D2` has a duplicate `'inprep'` field (True at line 7397, False at line 7398) and a duplicate `'primary_source_path'` field, per `feedback_promote_primary_sources_duplicate_keys.md` (Phase 7 Session 2). The same multi-key sprawl exists for the L2 splash.
- **Evidence:** `grep -n "Roehm2026Wave10\|Roehm2026D2\|Roehm2026Modular" papers/L2/paper_draft.tex papers/D2/paper_draft.tex` shows L2 uses only `Roehm2026D2`; D2 uses only `Roehm2026Wave10`; cross-bundle they each cite the *other* bundle but under different aliases. Reader who follows the D2-cited `Roehm2026Wave10` will not find L2 in any concrete sense; reader who follows the L2-cited `Roehm2026D2` will not find D2 either because D2 self-publishes under a different shim.
- **Expected:** One canonical bibkey per in-prep paper. L2 splash = one key; D2 deep paper = one key. Cross-cites use those keys.
- **Fix:** Decide canonical naming (`Roehm2026L2` and `Roehm2026D2` is the consistent pattern). Merge `Roehm2026Wave10` and `Roehm2026Modular` into `Roehm2026L2`. Repair duplicate `inprep` / `primary_source_path` lines per Session 2 feedback file. Update D2 and D3 bibitems to use the canonical key.
- **Cache:** n/a.

### 2.1 — (no findings) — Parameter provenance

- **Gate:** ParameterProvenance
- **Observed:** L2 ships no novel experimental parameters. All numerical content (16, 24, 8, 3, 15/2, ext-dim sequence 1,2,2,2,3,4) is either Mathlib arithmetic or chain-complex algebraic invariants — no `EXPERIMENTS` dict dependency.

### 3.1 — 🔴 BLOCKER — `ChangeOfRings.lean` cited as "establishing" Hom-tensor adjunction is entirely tautologies

- **Gate:** LeanProofSubstance (Gate 5)
- **Location:** `papers/L2/paper_draft.tex:200–204` (paper text) + `lean/SKEFTHawking/ChangeOfRings.lean` (entire file)
- **Observed:** L2 §IV (line 200–204) claims: "The Hom-tensor adjunction $\mathrm{Hom}_A(A \otimes_{A(1)} M, N) \cong \mathrm{Hom}_{A(1)}(M, N)$ is **established in ChangeOfRings.lean as pure homological algebra**, reducing the full Adams spectral sequence input from Ext_A to the finite Ext_{A(1)}." The abstract (line 36–38) elevates this further: "the change-of-rings adjunction ..., **the first such computation in any proof assistant**."

  Reading `lean/SKEFTHawking/ChangeOfRings.lean`, the file contains five theorems and **every single one is a tautology**:
  - Line 67: `theorem hom_tensor_adjunction_dim (rank : ℕ) : rank = rank := rfl` — proves `n = n` for any natural number `n`.
  - Line 93–96: `theorem change_of_rings_ext_dim (n ext_dim : ℕ) (h_ext : ext_dim = ext_dim) : ext_dim = ext_dim := h_ext` — takes `n = n` as hypothesis, returns `n = n` as conclusion.
  - Line 110: `theorem h2_discharged : True := trivial` — proves `True`.
  - Line 134–138: `theorem remaining_hypotheses : (4 : ℕ) - 1 = 3 := by norm_num` — proves `3 = 3`.
  - Line 142–152: `theorem generation_constraint_status : (7 : ℕ) + 3 = 10 := by norm_num` — proves `10 = 10`.

  **There is no `Module`, `LinearMap`, `TensorProduct`, `Hom`, `Subalgebra`, or `Hopf` object anywhere in the file.** The Hom-tensor adjunction is a docstring narration (line 12–15, 57–63, 88–91), not a Lean theorem statement. The paper's claim that the adjunction is "established" is false at the formal-content level.
- **Evidence:** Full module body read at `lean/SKEFTHawking/ChangeOfRings.lean:1–154`. `grep -nE "theorem|def " lean/SKEFTHawking/ChangeOfRings.lean` confirms only the listed five theorems plus zero substantive definitions.
- **Expected:** Either (a) replace prose with hedge: "documented in ChangeOfRings.lean as a tracked algebraic hypothesis (formally `H2_change_of_rings : Prop := True`), with the elementary proof recapitulated in the docstring; full Lean discharge deferred pending Mathlib's tensor-over-non-commutative-subring infrastructure"; or (b) actually formalize the adjunction (likely a multi-week effort given Mathlib's current state per the in-file Phase 5r roadmap note line 79–80).
- **Fix:** Option (a) is the honest reframing consistent with the existing `H2_change_of_rings : True` shim in `ExtBordismBridge.lean:82–83`. Strike the abstract's first-claim ("the first such computation in any proof assistant") for the adjunction step; the Ext-computation first-claim survives if the resolution-level computation is honestly described (see finding 3.2). Section IV should be rewritten to acknowledge that ChangeOfRings.lean records the algebraic hypothesis, not a formal proof.

### 3.2 — 🔴 BLOCKER — `A1Ext.ext_dim_n` theorems are `Fin n / 8` arithmetic, not Ext computations

- **Gate:** LeanProofSubstance (Gate 5)
- **Location:** `papers/L2/paper_draft.tex:193–199, 36–38` (abstract); `lean/SKEFTHawking/A1Ext.lean:88–103`
- **Observed:** The L2 abstract claims "the algebraic core of the Rokhlin bound is verified through homological degree~5 by an **explicit minimal free resolution** $\dim \mathrm{Ext}^n_{A(1)}(\mathbb{F}_2,\mathbb{F}_2) = 1,2,2,2,3,4$". §IV elaborates that `A1Ext.lean` "**certifies minimality and reads off** $\dim_{\mathbb{F}_2} \mathrm{Ext}^n_{A(1)}(\mathbb{F}_2, \mathbb{F}_2) = 1, 2, 2, 2, 3, 4$".

  The Lean theorems backing this claim are:
  ```
  theorem ext_dim_0 : (Fintype.card (Fin 8)) / 8 = 1 := by native_decide
  theorem ext_dim_1 : (Fintype.card (Fin 16)) / 8 = 2 := by native_decide
  theorem ext_dim_2 : (Fintype.card (Fin 16)) / 8 = 2 := by native_decide
  theorem ext_dim_3 : (Fintype.card (Fin 16)) / 8 = 2 := by native_decide
  theorem ext_dim_4 : (Fintype.card (Fin 24)) / 8 = 3 := by native_decide
  theorem ext_dim_5 : (Fintype.card (Fin 32)) / 8 = 4 := by native_decide
  ```
  
  **Each theorem's statement is a `ℕ`-arithmetic identity** (8/8=1, 16/8=2, 24/8=3, 32/8=4). It contains no reference to `Module`, `Ext`, `A(1)`, the resolution `P_n`, or the differentials `d_n`. The mapping from "card(Fin 8)/8 = 1" to "dim Ext^0_{A(1)}(F_2, F_2) = 1" is asserted by the theorem **name and docstring** alone.

  The substantive content does exist elsewhere (`A1Resolution.d1_d2_zero` ... `d4_d5_zero` via `native_decide` on actual F_2 matrices; `d_n_minimal` row-zero checks; RREF witnesses for d_4 and d_5 ranks). But the cited "reads off" step that connects "resolution exists" to "Ext dimensions are these" is missing — it would require a theorem of the form `Matrix.rank (d_n) + Matrix.rank (d_{n+1}) = dim (P_n)` (an actual exactness witness using `Matrix.rank`), plus a step linking exactness to Ext via the minimality-implies-zero-coboundary lemma.

  Companion finding: `A1Resolution.exactness_rank_nullity : 1+7=8 ∧ 7+9=16 ∧ 9+7=16 ∧ 7+9=16 ∧ 9+15=24 := by omega` is similarly self-discharging arithmetic — the docstring claims "All ranks machine-checked: d₁–d₃ via kernel enumeration, d₄–d₅ via RREF witnesses" but the theorem statement does not invoke `Matrix.rank` or relate the integers to anything computed.

- **Evidence:** Full read of `A1Ext.lean:1–154` and `A1Resolution.lean:1–254`. The substantive ranking lemmas — `d1_kernel_card`, `d4_rank_9`, `d5_rank_15` — exist (lines 125, 183, 226 of A1Resolution.lean) but are NOT chained into `ext_dim_n` as a Lean dependency; the connection is via narrative only.
- **Expected:** Either (a) restate the ext_dim_n theorems to use Matrix.rank explicitly and chain through `d_n_minimal` + chain-complex property + `exactness_rank_nullity` (rewritten as a real Matrix.rank statement); or (b) soften the paper claim from "reads off dim Ext^n_{A(1)}(F_2, F_2)" to "**defines** ext_dim_n as the resolution ranks, with the Ext-group identification documented by the resolution-minimality theorems and the standard textbook lemma (minimal resolution ⇒ Ext^n ≅ Hom(P_n, F_2))."
- **Fix:** Option (a) is moderate effort (Mathlib has `Matrix.rank` and `Matrix.ker` infrastructure). Option (b) is a paper-only rewrite. Either way, the abstract's framing "the first such computation in any proof assistant" applies only to the substantive resolution + minimality work in A1Resolution.lean / A1Ring.lean, not to the ext_dim_n theorems as written.

### 3.3 — 🔴 BLOCKER — `ExtBordismBridge` hypotheses H1–H4 are `Prop := True`; bridge theorem takes vacuous markers

- **Gate:** LeanProofSubstance (Gate 5) / AssumptionDisclosure (Gate 6)
- **Location:** `papers/L2/paper_draft.tex:207–216` (paper §IV "tracked hypotheses" prose); `lean/SKEFTHawking/ExtBordismBridge.lean:82, 96, 115`
- **Observed:** L2 §IV (line 207–216) describes three "textbook topology results" — ko cohomology, ASS convergence, ABP splitting — as entering "as tracked hypotheses, not as project-originated axioms". The reader is led to believe these are genuine Lean Props on which the conclusion depends.

  In the Lean source:
  ```
  def H1_ko_cohomology : Prop := True
  def H2_change_of_rings : Prop := True
  def H3_ass_collapses : Prop := True
  def H4_abp_splitting : Prop := True
  ```
  
  The bridge `theorem generation_constraint_chain (h1 : H1_ko_cohomology) (h2 : H2_change_of_rings) (h3 : H3_ass_collapses) (h4 : H4_abp_splitting) ...` takes four `True` witnesses, which are immediately constructible via `trivial`. **The theorem has no real conditional structure** — it is provable unconditionally by substituting `trivial` four times.

  This is the textbook P5 anti-pattern from `CLAUDE.md` ("falsifiers whose body restates hypotheses are structural-tautology repackaging"). The hypotheses are not "tracked" in any operative sense; they are narrative markers Lean treats as vacuous.

- **Evidence:** Full read of `ExtBordismBridge.lean:75–170`.
- **Expected:** Either (a) define each H_i as a real mathematical Prop (e.g., `H1_ko_cohomology : Prop := H_star_ko_pt_eq_module_quotient` with `H_star_ko_pt_eq_module_quotient` an axiomatized Mathlib-style statement of "H*(ko; F_2) ≅ A//A(1) through degree 5"); or (b) rewrite paper prose to acknowledge that the "tracked hypotheses" are documented in docstrings as out-of-scope content, not as Lean-typed assumptions. Honest framing: "Three textbook topology results enter as **commented documentation**, not as project-originated axioms — formalizing them as load-bearing Lean Props requires Mathlib infrastructure (Adams spectral sequences, Thom spectra) that does not yet exist; their absence does not affect the algebraic Ext computation, but means the chain to Ω^Spin_4 ≅ ℤ is **commentary, not formal proof**."
- **Fix:** Option (b) preserves the L2 narrative scope (the algebraic content is the publishable claim; the bordism-completion is honest commentary). Option (a) requires writing actual axioms or stub Mathlib-style declarations.

### 3.4 — 🟡 REQUIRED — `RokhlinBridge.constraints_without_nu_R` / `constraints_with_nu_R` are pure arithmetic identities, not physics

- **Gate:** LeanProofSubstance (Gate 5)
- **Location:** `papers/L2/paper_draft.tex:258–267` (paper §V combined-constraint table); `lean/SKEFTHawking/RokhlinBridge.lean:262–267`
- **Observed:** L2 §V combined-constraint table claims "Without $\nu_R$, both constraints must hold simultaneously, forcing $N_f = \mathrm{lcm}(16,3) = 48$ (\texttt{RokhlinBridge.constraints\_without\_nu\_R})". The Lean theorem reads:
  ```
  theorem constraints_with_nu_R : (3 : ℕ) ∣ 3 := dvd_refl 3
  theorem constraints_without_nu_R : Nat.lcm 16 3 = 48 := by decide
  ```
  Neither theorem has any reference to N_f, ν_R, the Z_16 anomaly, or the modular constraint. They prove "3 divides 3" and "lcm(16, 3) = 48" — true but **arithmetic facts**, not physics theorems. The connection to the paper's combined-constraint table is by theorem name alone.
- **Evidence:** Read of `lean/SKEFTHawking/RokhlinBridge.lean:262–267`.
- **Expected:** Rewrite to take both anomaly hypotheses as inputs and produce N_f ∈ 48ℤ as conclusion: e.g., `theorem constraints_without_nu_R (N_f : ℕ) (h_modular : 3 ∣ N_f) (h_z16 : 16 ∣ N_f) : 48 ∣ N_f := by exact (Nat.Coprime.mul_dvd_of_dvd_of_dvd (by decide) h_z16 h_modular)`. Or soften paper claim to "where the joint constraint follows from `Nat.lcm 16 3 = 48` (RokhlinBridge.constraints_without_nu_R) combined with the trivial fact that lcm(a,b) divides any common multiple."
- **Fix:** Theorem rewrite is one-line in Lean; paper-side hedge is one-sentence.

### 3.5 — 🟡 REQUIRED — `sixteen_convergence_full` bundle conjunct is redundant (CLAUDE.md P2)

- **Gate:** LeanProofSubstance (Gate 5)
- **Location:** `lean/SKEFTHawking/RokhlinBridge.lean:112–122`; L2 paper §V references it at line 234–238
- **Observed:** The theorem statement bundles 4 conjuncts:
  ```
  (∑ f : SMFermion, components f) = 16 ∧
  (16 : ZMod 16) = 0 ∧
  (∀ M : SpinManifold4, (16 : ℤ) ∣ M.signature) ∧
  (∑ f : SMFermion, components f) = (16 : ℕ)
  ```
  The first and fourth conjuncts are identical (both assert SM Weyl count = 16). This violates the CLAUDE.md preemptive-strengthening checklist item #1 (P2 bundle redundancy): "If I drop any conjunct, does the theorem still mean the same thing?" — yes, drop the fourth, the bundle is unchanged.
- **Evidence:** Read of `RokhlinBridge.lean:112–122`; the comment at line 120 ("All are the same 16") betrays the redundancy.
- **Expected:** Drop the duplicate fourth conjunct, or restate it in a different mathematical form (e.g., "= card (Fin 16)" rather than "= 16 (ℕ)") if a typeclass-distinct copy is genuinely load-bearing downstream.
- **Fix:** Single-line theorem edit; rebuild.

### 4.1 — 🔴 BLOCKER — Cross-bundle bibkey asymmetry L2 ↔ D2 (paired-splash key drift)

- **Gate:** CrossPaperConsistency (Gate 2)
- **Location:** `papers/L2/paper_draft.tex:293, 383–386` cite `Roehm2026D2`; `papers/D2/paper_draft.tex:113, 176, 222, 343, 907, 1034–1036` cite `Roehm2026Wave10`
- **Observed:** L2's "deep companion paper" cite uses `\cite{Roehm2026D2}` and registers the matching `\bibitem{Roehm2026D2}`. D2's "PRL splash companion" cite uses `\cite{Roehm2026Wave10}` and registers `\bibitem{Roehm2026Wave10}`. Both bibkeys point at the L2-or-D2 in-prep targets, but the cross-bundle reference structure is **asymmetric in name**: D2 says "the L2 splash" but writes `Roehm2026Wave10` (a Phase 5o/6n-era wave-numbered name); L2 says "the D2 deep companion" but writes `Roehm2026D2` (a bundle-named key). A reader reconciling bibliographies between the two papers cannot follow either citation back consistently. This was the explicit class of fix the Phase 7 Session 2 close memory says was resolved ("L2 r2 Roehm2026Wave10 → D2 cite (the L2 self-cite was renamed)") — but the renaming was done in only one direction. D2 still uses the legacy bibkey.

  Wider impact: `papers/F/paper_draft.tex` flagship bundle relies on stable cross-bundle keys to construct its review-paper bibliography (per `claims-reviewer-bundle-prompts.md` Tier 0 anchor list). The triple-key sprawl (Roehm2026Wave10, Roehm2026D2, Roehm2026Modular all aliasing the L2 splash; the D2 deep paper alias of Roehm2026D2 is the same key as L2's deep-companion cite) is going to surface again at flagship-bundle compile.
- **Evidence:** `grep -n "Roehm2026" papers/L2/paper_draft.tex papers/D2/paper_draft.tex` shows the asymmetry above.
- **Expected:** Choose one canonical convention. The natural one is `Roehm2026L2` for the L2 splash and `Roehm2026D2` for the D2 deep paper — but currently `Roehm2026D2` is being used as the L2-side label for the D2 deep paper. Either (a) rename D2's `Roehm2026Wave10` → `Roehm2026L2` and have L2 cite `Roehm2026D2` for D2 (the existing semantics), or (b) flip the L2 cite to `Roehm2026D2` actually pointing at D2, and ensure D2 cites L2 via `Roehm2026L2`. Option (a) is the smaller change.
- **Fix:** Replace `Roehm2026Wave10` → `Roehm2026L2` in D2 bibitem + every `\cite{Roehm2026Wave10}` in D2 prose; add canonical `Roehm2026L2` registry entry; mark `Roehm2026Wave10` and `Roehm2026Modular` as `@[deprecated]` aliases of `Roehm2026L2`.

### 4.2 — 🟡 REQUIRED — Cross-bundle 5812-theorem count stale (L2 + D2 both)

- **Gate:** CrossPaperConsistency (Gate 2) / NumericalFreshness (Gate 9)
- **Location:** `papers/L2/paper_draft.tex:42, 300` (abstract + conclusion); `papers/D2/paper_draft.tex:64, 122, 779`
- **Observed:** Both L2 and D2 cite `5812-theorem` count. The current canonical `docs/counts.tex` macros (regenerated 2026-05-06) show `\totaltheorems{5855}` and `\substantivetheorems{5830}`. The 5812 value was correct at Phase 7 Session 2 fix time (2026-05-06) and at L2's bundle close (2026-05-01), but Phase 6n+6o math substrate work (overnight Sessions 23–29 2026-05-06 per project memory) pushed counts to 5855. The cross-bundle agreement of "5812" is internally consistent across L2+D2 but **both stale vs canonical**.

  Both papers also report `322`-module count which is still current.
- **Evidence:** `cat docs/counts.tex | grep -E "totaltheorems|substantive|leanmodules"` → 5855 / 5830 / 322. `grep -n "5812" papers/L2/paper_draft.tex papers/D2/paper_draft.tex` confirms five occurrences.
- **Expected:** Both papers should either (a) `\input{../../docs/counts.tex}` and use `\totaltheorems{}` / `\substantivetheorems{}` macros instead of inline literals (per Gate 9 remediation pattern), or (b) refresh inline 5812 → 5855 (and 25/0/1 axiom values).
- **Fix:** Macro retrofit is the durable fix per `READINESS_GATES.md` Gate 9 § Remediation. The inline literal pattern guarantees this drifts again at the next math-substrate wave.

### 4.3 — 🔵 RECOMMENDED — Author affiliation differs L2 ↔ D2

- **Gate:** CrossPaperConsistency
- **Location:** `papers/L2/paper_draft.tex:16–18` ("J.~Roehm, Independent Researcher"); `papers/D2/paper_draft.tex:23–25` ("John Roehm, Independent Researcher, jgroehm@gmail.com")
- **Observed:** L2 uses "J.~Roehm" (initial-only); D2 uses "John Roehm" (full name). L2 omits the `\email{}` line that D2 includes. Minor inconsistency; published companions are expected to share author block conventions.
- **Expected:** Same author block in both. Per `currentDate` / user email, "John G. Roehm, Independent Researcher, jgroehm@gmail.com" is the canonical form (matches `Roehm2026D2` registry `'authors': 'Roehm, J. G.'`).
- **Fix:** One-line edit per paper.

### 5.1 — 🟡 REQUIRED — First-claim "first machine-checked Ext computation over a Steenrod sub-algebra in any proof assistant" insufficiently ledger-backed; abstract/body hedge inconsistency

- **Gate:** NarrativeGrounding (Gate 7) / FirstClaimVerification (Gate 10)
- **Location:** `papers/L2/paper_draft.tex:36–43` (abstract, unhedged "the first such computation"), `:65` ("the first machine-checked formalization"), `:204–206` (§IV, hedged "We are not aware of any prior...")
- **Observed:** The abstract makes a hard first-claim with no hedge: "the first such computation in any proof assistant". §IV softens to "We are not aware of any prior machine-checked Ext computation over a Steenrod sub-algebra in any proof assistant." The asymmetric hedging is the recurring abstract-vs-body anti-pattern that the L2 READINESS_GATES.md file itself flagged at bundle close ("QI-candidate observation: programmatic abstract-vs-body first-claim hedge validator would catch L1+L2 recurring pattern").

  Ledger-side: per `READINESS_GATES.md` Gate 10, the `FirstClaimLedger` artifact does not yet exist; the gate is "advisory only" but "surfaces the obligation". `docs/roadmaps/Phase5v_Roadmap.md` explicitly lists "First Ext over A(1) **unvetted**" as the obligation owner. Fresh WebSearch over (Mathlib4, Coq Mathematical Components, Isabelle AFP, Agda stdlib + cubical, Lean 3 mathlib) found:
  - Joël Riou's "Formalization of derived categories in Lean/Mathlib" (Annals of Formalized Math 2025) — abstract Ext functor framework but no Steenrod-algebra computation (PDF extract confirms).
  - Cubical Agda formalization of Brunerie's Steenrod squares + Adem relations (arXiv:2504.08664) — formalizes Sq^i + Adem but no Ext over A or A(1) (abstract content).
  - GitHub `pouiyter/MinimalResolution` (Nassau-style C/C++ minimal-resolution computer) — non-proof-assistant code; not a counterexample.
  - No prior Lean/Coq/Isabelle/HOL Light/Mizar formalization surfaced.
  
  The first-claim is **plausibly correct as a "first in a foundational proof assistant"** but the project's own roadmap acknowledges it is unvetted, and the documented hedge ("we are not aware") is appropriate, but the abstract should match.
- **Evidence:** WebSearch results above; `docs/roadmaps/Phase5v_Roadmap.md:101`; `docs/stakeholder/Phase5a_Implications.md:90` ("first Steenrod algebra formalization"); `docs/stakeholder/companion_guide.md:32`.
- **Expected:** (a) Match abstract hedge to §IV form ("to our knowledge, the first ..." or "we are not aware of any prior ..."); (b) commit a ledger entry to `docs/first_claim_ledger.jsonl` (or whichever artifact form the project adopts) documenting the search performed and date.
- **Fix:** Abstract softening is a one-line edit; ledger entry is a small JSON record.

### 5.2 — 🔵 RECOMMENDED — Wang2024 attribution accurate but informal-name drift

- **Gate:** NarrativeGrounding (Gate 7) / attribution-claim sub-class
- **Location:** Throughout L2, "Wang" cited as the originator of the modular generation constraint (e.g., line 53, 65, 234, 282–293)
- **Observed:** L2 attributes "Wang~\cite{Wang2024}" the proposal "that a *global* modular constraint — $24 \mid c_-$ on the chiral central charge of the partition function under the modular $T$-transformation $\tau \to \tau+1$ — combined with the SM's fermion content $c_- = 8 N_f$, forces $N_f \equiv 0 \pmod 3$." The actual Wang2024 paper title is "**Family Puzzle, Framing Topology, $c_-=24$ and 3(E8)$_1$ Conformal Field Theories: 48/16 = 45/15 = 24/8 = 3**" — the WebFetch abstract confirms Wang derives "the family number constraint $N_f \in (\frac{48}{16} = \frac{24}{8} = 3) \mathbb{Z}$" via "framing anomaly-free $c_- = 0 \mod 24$ modular invariance". Attribution **content** matches; the project name "modular generation constraint" is L2's own shorthand. Minor: arXiv 2312.14928 is the preprint number (December 2023 dated); the L2 bibitem labels it 2024 per PRD publication; first-author year `Wang2024` is correct on publication grounds.
- **Expected:** No fix required — attribution is content-correct, just informal-name-distinct from Wang's own title. Adding a short footnote acknowledging Wang's preferred "Family Puzzle" framing would be honest discipline but is not load-bearing.

### 6.1 — 🟡 REQUIRED — Mathlib commit `8850ed93` cited; provenance/version

- **Gate:** AssumptionDisclosure (Gate 6)
- **Location:** `papers/L2/paper_draft.tex:298–302, 307–308`
- **Observed:** Conclusion + Acknowledgments state "Lean~4 v4.29.0 against Mathlib commit \texttt{8850ed93}". `lean/lakefile.toml` should be the source of truth. Acknowledged in CLAUDE.md "Lean Mathlib: Pinned in `lean/lakefile.toml` (currently `8850ed93`)". This is currently consistent with the project state but is an inline literal that drifts on the next Mathlib bump.
- **Expected:** Macro-retrofit pattern: define `\mathlibcommit{8850ed93}` in `docs/counts.tex` (or a sibling `docs/lean_versions.tex`), use the macro in the paper. Otherwise paper drifts silently with every Mathlib upgrade.
- **Fix:** Same Gate 9 macro pattern as 4.2.

### 7.1 — (no findings beyond 4.2) — Count literal drift

- **Gate:** NumericalFreshness
- **Observed:** Covered under 4.2. No additional count-literal issues found in L2.

### 8.1 — (no findings) — Production run health

- **Gate:** ProductionRunHealth
- **Observed:** L2 ships zero claims of numerical simulation or Monte Carlo evidence; ProductionRun queries are inapplicable.

## QI Candidate

**QI 1 — adversarial-reviewer-spot-checks-extracted-Lean-bodies for tautology-via-rename anti-pattern.** Findings 3.1, 3.2, 3.3, 3.4 are all manifestations of the same anti-pattern: a Lean theorem whose **statement** is a trivial arithmetic / tautological identity (`rank = rank`, `8/8 = 1`, `True`, `Nat.lcm 16 3 = 48`) but whose **name and docstring** describe a substantive mathematical content (Hom-tensor adjunction, Ext dimensions, hypothesis discharge, combined-constraint physics). The `PlaceholderMarker` extractor catches `rfl` / `trivial` on top-level Prop statements but does not catch the "wrapped-in-an-arithmetic-equality" form. A new validator should flag any cited theorem whose **statement type** is in {`(_ : ℕ) = _`, `(_ : ℤ) ∣ _`, `True`, `_ = _` where both sides are decidable arithmetic literals} but whose **name** contains physics nouns (`adjunction`, `Ext`, `Hom`, `cohomology`, `bordism`, `anomaly`, `constraint_physics`, etc.). This would have caught 3.1 + 3.2 + 3.4 automatically.

**QI 2 — `used_in` registry sweep on bundle lift.** Finding 1.3 is one instance of a likely broader pattern: older registry entries (Kitaev2009, FidkowskiKitaev2010, GarciaEtxebarria2019, etc.) pre-date the bundle restructuring (Phase 6i Wave 7) and their `used_in` lists track only the original per-paper draft paths, not the lifted bundle paths. A `scripts/registry_used_in_sweep.py` that walks `papers/*/paper_draft.tex` and reconciles each `\bibitem{key}` against `CITATION_REGISTRY[key]['used_in']` would be a one-time cleanup + ongoing pre-Stage-13 validation hook. Per Phase 7 Session 5 user-policy ("do that kind of thing from now on" on primary-source verification), this is a natural companion automation.

**QI 3 — Abstract-vs-body first-claim hedge linter.** L2's own bundle-close READINESS_GATES.md surfaced this same QI. Finding 5.1 confirms the recurrence (L1 had the same pattern per the L2 close memo). A `validate.py --check firstclaim_hedge_consistency` walking each `papers/*/paper_draft.tex` and comparing abstract vs body for asymmetric hedge markers ("the first", "to our knowledge the first", "we are not aware of any prior") on each first-claim sentence would close this systematic class.

---

## Output summary

- 🔴 BLOCKERs: 4 (1.1 Wang2024 title; 3.1 ChangeOfRings tautologies; 3.2 ext_dim_n arithmetic; 3.3 ExtBordismBridge vacuous-hypothesis; 4.1 cross-bundle bibkey drift)
- 🟡 REQUIREDs: 5 (1.2 registry stub titles; 1.3 Kitaev2009 used_in; 1.4 in-prep key sprawl; 3.4 RokhlinBridge arithmetic; 3.5 sixteen_convergence_full bundle redundancy; 4.2 5812 stale; 5.1 first-claim hedge + ledger; 6.1 Mathlib commit macro)
- 🔵 RECOMMENDEDs: 2 (4.3 author-block consistency; 5.2 Wang attribution informal-name)

**L2 verdict: BLOCKED for submission.** The four BLOCKERs strike at the heart of the paper's load-bearing claim: the "first machine-checked Ext computation over a Steenrod sub-algebra" is real at the resolution + minimality + chain-complex level (A1Ring.lean, A1Resolution.lean), but the **change-of-rings step that converts Ext_{A(1)} to Ext_A** is a placeholder, the **ext-dim read-off** is name-only, and the **bordism completion to Ω^Spin_4 ≅ ℤ** depends on four `Prop := True` markers. A submitted PRL with these gaps would not survive substantive referee inspection. Suggested response sequence: (1) close 1.1 + 1.2 + 1.3 + 1.4 (registry hygiene, one short PR); (2) decide on 3.1 / 3.2 / 3.3 either as honest paper-prose softening (smaller change) or actual Lean formalization (multi-week effort, higher publication ceiling); (3) close 4.1 with the canonical bibkey rename; (4) retrofit 4.2 + 6.1 with the docs/counts.tex macros; (5) ledger entry for 5.1.
