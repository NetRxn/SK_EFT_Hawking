\documentclass[aps,prl,twocolumn,superscriptaddress,nofootinbib]{revtex4-2}
\usepackage{amsmath,amssymb,amsfonts}
\usepackage{graphicx}
\usepackage{hyperref}
\usepackage{siunitx}

\input{../../docs/counts.tex}

%% ═══════════════════════════════════════════════════════════════════
%% Bundle: E1 (tier 4, target PRL | PRR)
%% Source manifest: papers/E1/source_manifest.md
%% Charter: docs/PAPER_STRATEGY.md §2.5 "Paper E1"
%%
%% STAGE-10 FULL REDRAFT — 2026-08-15. The prior draft is superseded in
%% full. Findings raised by this redraft:
%%   papers/AutomatedReviews/2026-08-15-e1-stage10-redraft/E1.md
%%
%% SPINE (derive before spend):
%%   §I    the opportunity, the obstacle, and what this Letter delivers
%%   §II   measured device parameters -> kappa, D, T_H            [derives]
%%   §III  the perturbative-dissipation criterion, evaluated       [derives]
%%   §IV   the stimulated-gain spectrum and its threshold          [spends]
%%   §V    noise floor and the falsifiable window                  [spends]
%%   §VI   what we need measured
%%   §VII  conclusion
%% ═══════════════════════════════════════════════════════════════════

\input{bundle_counts}

\begin{document}

\title{Cavity Lifetime Gates Analog Hawking Detection in
Exciton-Polariton Microcavities}

\author{J.~Roehm}
\affiliation{Independent Researcher}

\date{\today}

\begin{abstract}
Exciton-polariton fluids now support acoustic horizons of tailored
curvature with the surface gravity measured directly. At the
Paris-LKB platform's published smooth-horizon value
$\kappa = \input{tables/lkb_kappa_smooth.tex}\unskip\,\mathrm{s}^{-1}$~\cite{Falque2025} the
standard analog-Hawking relation gives
$T_H = \input{tables/lkb_T_H_smooth_mK.tex}\unskip\,\si{\milli\kelvin}$, eight orders above the
atomic-BEC analogs, and a coherent probe crossing the horizon is
amplified by $G(\omega) = \Gamma(\omega)/(e^{2\pi\omega/\kappa_{\rm eff}}-1)$,
which reaches one half at
$\omega = \input{tables/lkb_gain_threshold.tex}\unskip\,\kappa_{\rm eff}$ for a
saturated greybody factor. The obstacle is not the horizon but the
cavity: the as-built \input{tables/lkb_tau_cav_ps_standard.tex}\unskip\,\si{\pico\second}
device has $\Gamma_{\rm pol}/\kappa = \input{tables/lkb_ratio_standard.tex}\unskip$,
so the weak-dissipation expansion that licenses these numbers fails
by more than an order of magnitude and holds only for
$\tau_{\rm cav} \gtrsim \input{tables/lkb_tau_required_ps.tex}\unskip\,\si{\pico\second}$.
We give the predicted spectrum, its falsifiable window
$\omega \lesssim 0.3\,\kappa$, and the two measurements that would
close it.
\end{abstract}

\maketitle

%% ══════════════════════════════════════════════════════════════════
\section{Introduction}

Analog Hawking radiation has been observed in atomic Bose-Einstein
condensates at $T_H \approx \SI{0.35}{\nano\kelvin}$
\cite{Steinhauer2016,deNova2019}, so far below every systematic in
the apparatus that the signal must be recovered from
density-density correlations accumulated over thousands of
realizations. Exciton-polariton fluids remove that constraint
rather than fighting it: their sound speed exceeds a dilute atomic
gas's by some eight orders of magnitude, and their horizons are
written optically, so the velocity gradient setting the surface
gravity is both large and adjustable, and predicted temperatures
lie in the \SI{0.1}{\kelvin} to \SI{1}{\kelvin} range
\cite{Gerace2012,Nguyen2015}.

Falque \emph{et al.}~\cite{Falque2025} have now built the horizon.
Their microcavity polariton fluid crosses from sub- to supersonic
flow along an optically imprinted velocity profile, the horizon
steepness is tuned all-optically between two measured values, and
negative-energy Bogoliubov modes are resolved in the supersonic
region, which is the kinematic signature that a horizon has formed
and that pair emission across it is possible. What the platform does
not yet have is a prediction sharp enough to falsify, and a
statement of the device conditions under which that prediction is
entitled to be believed.

This Letter supplies both. We evaluate the Schwinger-Keldysh
effective-field-theory (SK-EFT) analog-Hawking spectrum at the
published Paris-LKB parameters, and ask in the same expansion
whether the cavity is quiet enough for that spectrum to be the
leading behavior. The answer is that the horizon is already
good enough and the cavity is not. Cavity loss, not surface gravity,
is the gating parameter, and the gate closes at a lifetime we can
name.

%% ══════════════════════════════════════════════════════════════════
\section{Device parameters and the predicted Hawking temperature}

%% Data source: papers/E1/tables.py -> render_paper_tables.py, which reads
%%   src/core/constants.py :: POLARITON_PLATFORMS['Paris_standard'] and
%%   src/core/constants.py :: FALQUE_STEEP_HORIZON_KAPPA,
%% and derives D = xi*kappa/c_s and delta_disp = -pi D^2/6 rather than
%% quoting either. Every number in this section is generated.
%% Primary-source verification (2026-08-15, page-level read of
%% Lit-Search/Phase-5w/primary-sources/Falque2025.pdf):
%%   p.5 §IV.A  "the healing length ... is typically 3.4 um upstream and
%%               4.0 um downstream"; "the speed of sound c_s(x) ~ 0.40 um/ps"
%%   p.5 §IV.A  "horizon steepness kappa = 0.07 ps^-1 (red) and 0.08 (purple)"
%%   p.5 §IV.B  "We measure M_u = 0.7 and M_d = 4, with kappa = 0.11 ps^-1"
%%   p.4 §III   "effective polariton mass at the working point, m* = 7.0e-35 kg"
%%   p.9 App.B1 "We measure a polariton decay rate hbar*gamma ~ 80 ueV"

We take the fluid parameters from Falque \emph{et al.}~\cite{Falque2025}
directly rather than re-deriving them. Their \S IV.1 reports a sound
speed $c_s \approx \input{tables/lkb_c_s_um_per_ps.tex}\unskip\,\si{\micro\meter/\pico\second}$
and a healing length $\xi \approx \input{tables/lkb_xi_um.tex}\unskip\,\si{\micro\meter}$
upstream (\SI{4.0}{\micro\meter} downstream), measured on a fluid whose
effective polariton mass is $m^* = \SI{7.0e-35}{\kilo\gram}$. Two
horizon steepnesses are reported: a smooth configuration at
$\kappa = \input{tables/lkb_kappa_smooth.tex}\unskip\,\mathrm{s}^{-1}$ and a steep one at
$\kappa = \input{tables/lkb_kappa_steep.tex}\unskip\,\mathrm{s}^{-1}$.

That sound speed is not a reservoir-free Bogoliubov estimate
rescaled after the fact: the dark excitonic reservoir is already
inside it, carried as $g_r n_r \approx 1.84\,gn$ through the
dispersion from which $c_s$ is extracted. Reservoir-free estimates
for this class of device reach
$\sim \SI{1.0}{\micro\meter/\pico\second}$, so the reservoir costs a
factor of about two and a half, but that reduction is the primary
source's result rather than
ours~\cite{Estrecho2021,Stepanov2019,Claude2023}.

\begin{center}
\input{tables/lkb_device_params.tex}
\end{center}

The dimensionless ratio $D = \xi\kappa/c_s$ compares the horizon's
gradient scale to the healing length and controls how far the
hydrodynamic description can be trusted. It is
$\input{tables/lkb_D_smooth.tex}\unskip$ at the smooth horizon and
$\input{tables/lkb_D_steep.tex}\unskip$ at the steep one. We take the leading
dispersive correction to be
\begin{equation}
\delta_{\rm disp} = -c_1 D^2 ,
\qquad c_1 \equiv \frac{\pi}{6},
\label{eq:delta_disp}
\end{equation}
giving $\input{tables/lkb_delta_disp_smooth.tex}\unskip$ and
$\input{tables/lkb_delta_disp_steep.tex}\unskip$ respectively.

The $D^2$ scaling is the established one: Coutant and Weinfurtner obtain
the leading correction to the effective temperature as
$O(\xi^2\kappa^2/c_s^2)$ in the adiabatic regime. The \emph{coefficient}
is not. No derivation of a universal $c_1$ exists, and none is possible in
the form Eq.~\eqref{eq:delta_disp} suggests: for a locally linear
velocity profile the near-horizon calculation returns
$\beta_\omega = e^{-\pi\omega/\kappa}\alpha_\omega$ exactly, so the
temperature is $\kappa/2\pi$ with no $D^2$ term at all, and a correction
appears only through the profile's non-linear part~\cite{Coutant2014}.
Corley and Jacobson, correspondingly, report numerically fitted powers
$(T_H/k_0)^p$ whose exponent is itself profile-dependent, $p \simeq 3$ and
$2$ for smooth profiles and $p \simeq 1$ for kinked
ones~\cite{CorleyJacobson1996}. What that literature establishes is the
robustness of $T_H$, not a coefficient. The value $c_1 = \pi/6$ is
therefore fixed here as a declared normalization of an $O(1)$
profile-dependent quantity --- it is the single value carried by this
project's computational and formal substrate, so that every $\delta_{\rm
disp}$ quoted below and in the companion papers is the same statement
about the same definition. A reader who prefers a different $O(1)$
normalization should rescale every dispersive number in this paper by
$c_1/(\pi/6)$; the quadratic $\kappa$-scaling and the
dispersive-versus-dissipative ordering are unaffected.

The Hawking temperature follows from the surface gravity alone,
\begin{equation}
k_B T_H = \frac{\hbar \kappa}{2\pi} ,
\label{eq:T_H}
\end{equation}
which at the smooth horizon gives
$T_H = \input{tables/lkb_T_H_smooth_mK.tex}\unskip\,\si{\milli\kelvin}$.

Two qualifications attach to that number, and both come from the
primary source rather than from the theory.

The first is a normalization. Falque \emph{et al.} quote a Hawking
temperature of \SI{3}{\kelvin} for their smoothest horizon, and
their Eq.~(9) writes the emitted flux as
$1/(e^{\omega/2\pi\kappa}-1)$. That exponent corresponds to
$k_B T = h\kappa$, which at the same
$\kappa = \input{tables/lkb_kappa_smooth.tex}\unskip\,\mathrm{s}^{-1}$ evaluates to
$\input{tables/lkb_T_H_falque_norm_K.tex}\unskip\,\si{\kelvin}$ and reproduces the
flux values they report. It exceeds Eq.~\eqref{eq:T_H} by
$(2\pi)^2 \approx \input{tables/lkb_norm_ratio.tex}\unskip$. We use
Eq.~\eqref{eq:T_H}, the convention of the analog-gravity literature
the same paper cites~\cite{Visser1998,Unruh1981,Barcelo2011}, and we
flag the factor here so that a reader holding \SI{3}{\kelvin} in
mind does not read
$\input{tables/lkb_T_H_smooth_mK.tex}\unskip\,\si{\milli\kelvin}$ as a
disagreement about $\kappa$. The two papers agree on every measured
quantity; they normalize $T_H$ differently.

The second is that Eq.~\eqref{eq:T_H} is only an observable where
the spectrum is thermal, and at the steep horizon it is not. Falque
\emph{et al.} state that the steep-horizon spectrum is dominated by
nonlinear frequency dispersion and is therefore not thermal, which
$D = \input{tables/lkb_D_steep.tex}\unskip$ anticipates: the dispersive correction
Eq.~\eqref{eq:delta_disp} is then near one half and the expansion
that produced it has stopped converging. We therefore quote a
Hawking temperature at the smooth horizon only, and treat the steep
configuration as what the source shows it to be, a demonstration
that $\kappa$ is tunable, not a hotter black hole.

%% ══════════════════════════════════════════════════════════════════
\section{The cavity lifetime the measurement requires}

%% Lean anchors, statements read 2026-08-15 (not docstrings):
%%   PolaritonTier1.lean:84   attenuation_ge_one
%%       1 <= attenuationFactor p,  attenuationFactor p = exp(Gamma*L/v_g)
%%   PolaritonTier1.lean:115  attenuation_mono_Gamma
%%       p1.L = p2.L -> p1.v_g = p2.v_g -> p1.Gamma <= p2.Gamma ->
%%       attenuationFactor p1 <= attenuationFactor p2
%%   PolaritonTier1.lean:98   attenuation_eq_one_at_zero_decay
%%       p.Gamma_pol = 0 -> attenuationFactor p = 1
%%   PolaritonTier1.lean:196  polariton_tier1_fails_tmds
%%       (1/10 : R) <= validityRatio pennTmdPolaritonParams
%%   All four kernel-only; axiom closure {propext, Classical.choice,
%%   Quot.sound}; no project-local axioms, no tracked Props.

Polaritons leak. A quantum written into the fluid at the horizon
decays at the cavity rate $\Gamma_{\rm pol} = 1/\tau_{\rm cav}$
while it propagates to where it is read out, so the occupation the
apparatus records is smaller than the occupation the horizon
emitted by
\begin{equation}
A = \exp\!\left(\frac{\Gamma_{\rm pol} L}{v_g}\right),
\label{eq:attenuation}
\end{equation}
with $L$ the propagation distance and $v_g$ the group velocity.
Recovering the intrinsic spectrum means dividing by $A$. Three
elementary properties of Eq.~\eqref{eq:attenuation} are checked in
Lean~4 for every parameter choice rather than a fitted one:
$A \geq 1$ always, so the recovery never deflates an inferred
occupation (\texttt{attenuation\_ge\_one}); $A$ is monotone in
$\Gamma_{\rm pol}$ at fixed geometry
(\texttt{attenuation\_mono\_Gamma}); and $A = 1$ exactly at
$\Gamma_{\rm pol} = 0$, the closed-system limit
(\texttt{attenuation\_eq\_one\_at\_zero\_decay}).

The question is not whether $A$ can be divided out, but whether
$\Gamma_{\rm pol}$ is small enough that what is being corrected is
still the emission Eq.~\eqref{eq:T_H} describes. Dissipation
enters the SK-EFT as an expansion in $\Gamma_{\rm pol}/\kappa$, and
we take
\begin{equation}
\Gamma_{\rm pol} < \input{tables/lkb_weak_dissipation_ratio.tex}\unskip\,\kappa
\label{eq:weak_dissipation}
\end{equation}
as the condition under which the leading term is the answer. The
cut in Eq.~\eqref{eq:weak_dissipation} is a declared convention, not
a derived boundary; what is not conventional is the size of the gap
between it and the device.

Falque \emph{et al.} measure a polariton decay rate
$\hbar\gamma \approx \SI{80}{\micro\electronvolt}$, or
$\tau_{\rm cav} \approx \input{tables/lkb_tau_cav_ps_standard.tex}\unskip\,\si{\pico\second}$.
Against $\kappa = \input{tables/lkb_kappa_smooth.tex}\unskip\,\mathrm{s}^{-1}$ this gives
\begin{equation}
\frac{\Gamma_{\rm pol}}{\kappa} = \input{tables/lkb_ratio_standard.tex}\unskip ,
\label{eq:ratio_asbuilt}
\end{equation}
about eighteen times the cut. The as-built cavity is not weakly
dissipative: over one healing length the recovery factor $A$ is
already
$\input{tables/lkb_atten_per_xi_standard.tex}\unskip$, because the exponent in
Eq.~\eqref{eq:attenuation} per healing length is exactly the product
$(\Gamma_{\rm pol}/\kappa)\,D$ of this Letter's two dimensionless
parameters.

This is a cavity specification, and it is the one axis of the
platform that is a fabrication choice rather than a physical limit.
Holding the horizon fixed and varying only the mirror stack, a
\SI{100}{\pico\second} cavity gives
$\Gamma_{\rm pol}/\kappa = \input{tables/lkb_ratio_long.tex}\unskip$ and a
\SI{300}{\pico\second} cavity gives
$\input{tables/lkb_ratio_ultralong.tex}\unskip$. Eq.~\eqref{eq:weak_dissipation}
is met from
\begin{equation}
\boxed{\;\tau_{\rm cav} \gtrsim
  \input{tables/lkb_tau_required_ps.tex}\unskip\,\si{\pico\second}\;}
\label{eq:tau_required}
\end{equation}
onward, where the per-healing-length recovery factor has fallen to
$\input{tables/lkb_atten_per_xi_required.tex}\unskip$. Whether a GaAs
microcavity at that lifetime is available to this group is a
fabrication question rather than a physics one; the horizon itself
does not need to change.
%% TODO: the claim that GaAs microcavities with tau_cav >= 143 ps are
%% demonstrated is NOT made here, because it is not verified. The
%% repo's PARAMETER_PROVENANCE cites Ballarini 2017 for long-lifetime
%% cavities, but only in support of the 100 ps projection, which is
%% BELOW this threshold, and that source is not held in full text.
%% Acquire it (and the higher-Q GaAs literature) before any future
%% draft asserts availability. Filed nowhere else deliberately: this
%% is a source-acquisition task, not a defect in the present text.

Figure~\ref{fig:regime} places the device on that axis. The same
criterion applied outside the GaAs family closes the other end of
the range: the UPenn MoSe$_2$ nanocavity of Wang \emph{et
al.}~\cite{WangKimBZHe2026PennTMDPolariton}, whose lower-polariton
linewidth $\gamma_{\rm LP} = \SI{1.8}{\milli\electronvolt}$ implies
$\Gamma_{\rm LP} \approx \SI{2.7e12}{\per\second}$, sits at
$\Gamma_{\rm LP}/\kappa = \input{tables/penn_ratio_vs_steep.tex}\unskip$ even when
paired with the most generous surface gravity the polariton family
has demonstrated, so no SK-EFT statement in this Letter extends to
ultrafast TMD nanocavities. That the ratio there is at least
$\input{tables/lkb_weak_dissipation_ratio.tex}\unskip$ is itself kernel-checked
(\texttt{polariton\_tier1\_fails\_tmds}); the value
$\input{tables/penn_ratio_vs_steep.tex}\unskip$ is arithmetic on the same
encoded parameters.

\begin{figure}[t]
\centering
\includegraphics[width=\columnwidth]{figures/fig_polariton_regime_map.png}
\caption[The weak-dissipation criterion against cavity lifetime]{Normalized
polariton decay $\Gamma_{\rm pol}/\kappa$ against cavity lifetime
$\tau_{\rm cav}$, at the smooth-horizon
$\kappa = \input{tables/lkb_kappa_smooth.tex}\unskip\,\mathrm{s}^{-1}$ of
Falque \emph{et al.}~\cite{Falque2025}. The as-built
\input{tables/lkb_tau_cav_ps_standard.tex}\unskip\,\si{\pico\second} device
(``standard'') sits at $\input{tables/lkb_ratio_standard.tex}\unskip$, above unity,
where the weak-dissipation expansion of
Eq.~\eqref{eq:weak_dissipation} does not apply. The dotted line is
$\Gamma_{\rm pol}/\kappa = 1/(\tau_{\rm cav}\kappa)$, the trajectory
a longer cavity moves the device along at fixed horizon; it crosses
the criterion at Eq.~\eqref{eq:tau_required}. The
\SI{100}{\pico\second} (``long'') and \SI{300}{\pico\second}
(``ultralong'') points are projected cavity generations, not
measured devices. Shaded bands are the qualitative regimes the
expansion passes through, coarsest at top.}
\label{fig:regime}
\end{figure}

Two consequences separate cleanly, and the rest of this Letter
depends on the separation. What does \emph{not} depend on the
cavity is anything fixed by $\kappa$ alone: the horizon exists, its
steepness is measured, and Eqs.~\eqref{eq:T_H} and~\eqref{eq:gain}
set the temperature and the shape of the gain spectrum, including
the frequency at which the gain crosses any given threshold. What
\emph{does} depend on the cavity is the claim that those leading-order
expressions are the whole answer, and with it the quoted gain
magnitudes, the recovery of an intrinsic occupation through
Eq.~\eqref{eq:attenuation}, and the error bars on the window of
\S V. The numbers in \S IV and \S V are therefore stated for a
device satisfying Eq.~\eqref{eq:tau_required}; at the as-built
cavity they are the correct target of the measurement, not a
prediction of what it will return.

%% ══════════════════════════════════════════════════════════════════
\section{The stimulated-Hawking gain spectrum}

%% Numbers consumed from src/core/formulas.py :: stimulated_hawking_gain
%% and :: stimulated_hawking_snr via papers/E1/tables.py. The argument is
%% omega/kappa_eff, which is what the exponent contains.

Waiting for spontaneous emission is what makes the atomic-BEC
measurements slow, and driving the horizon removes that cost. A
coherent probe injected at frequency $\omega$ scatters anomalously
at the horizon, and the amplitude returned in the partner channel is
set by the same Bogoliubov mixing that produces the spontaneous
pairs, so a classical mean-field measurement reads out a
quantum-gravitational analog coefficient~\cite{Grisins2016}. The
gain is
\begin{equation}
G(\omega) = \frac{\Gamma(\omega)}{e^{2\pi\omega/\kappa_{\rm eff}} - 1},
\label{eq:gain}
\end{equation}
with $\Gamma(\omega)$ the greybody transmission factor and
$\kappa_{\rm eff} = \kappa\,(1 + \delta_{\rm disp})$ the surface
gravity carrying the dispersive correction of
Eq.~\eqref{eq:delta_disp}. At the smooth horizon
$\kappa_{\rm eff}/\kappa = \input{tables/lkb_kappa_eff_ratio_smooth.tex}\unskip$.

Every number below assumes the greybody-saturation limit
$\Gamma(\omega) \equiv 1$, and the statements that follow are
universal in $\kappa$ only within that limit. The assumption is
defensible in the low-frequency window
$\omega \lesssim 0.3\,\kappa$ of interest here, where greybody
corrections enter at $\mathcal{O}(\omega^2/\kappa^2)$ in the
hydrodynamic regime and leave the plateau
flat~\cite{Finazzi2012,Jacquet2023QNM}; it is not defensible outside
it, and a measured $\Gamma(\omega)$ profile is one of the things the
experiment would deliver.

Under it the gain falls monotonically from a low-frequency
divergence: $G = \input{tables/lkb_gain_at_0p1.tex}\unskip$ at
$\omega = 0.1\,\kappa_{\rm eff}$,
$\input{tables/lkb_gain_at_0p3.tex}\unskip$ at $0.3\,\kappa_{\rm eff}$, and
$\input{tables/lkb_gain_at_1.tex}\unskip$ at $\kappa_{\rm eff}$. Setting
$G = 1/2$ inverts Eq.~\eqref{eq:gain} exactly, with no device
parameter surviving:
\begin{equation}
\frac{\omega_{1/2}}{\kappa_{\rm eff}} = \frac{\ln 3}{2\pi}
  = \input{tables/lkb_gain_threshold.tex}\unskip .
\label{eq:threshold}
\end{equation}
Equation~\eqref{eq:threshold} is the sharpest prediction in this
Letter, because it is a pure number. Expressed in the surface
gravity the experiment measures rather than in $\kappa_{\rm eff}$,
it sits at $\omega_{1/2} = \input{tables/lkb_gain_threshold_absolute.tex}\unskip\,\kappa$
at the smooth horizon, and the difference between
$\input{tables/lkb_gain_threshold.tex}\unskip$ and
$\input{tables/lkb_gain_threshold_absolute.tex}\unskip$ is exactly the dispersive
correction. A measurement that locates the half-gain frequency
therefore measures $\delta_{\rm disp}$, and through it $D$, without
needing an absolute calibration of the probe.

\begin{figure}[t]
\centering
\includegraphics[width=\columnwidth]{figures/fig_stimulated_hawking_spectrum.png}
\caption[Stimulated-Hawking gain against normalized frequency]{Stimulated-Hawking
gain $G(\omega)$ against normalized frequency, at the smooth-horizon
$\kappa = \input{tables/lkb_kappa_smooth.tex}\unskip\,\mathrm{s}^{-1}$ and
$D = \input{tables/lkb_D_smooth.tex}\unskip$ of Falque \emph{et al.}~\cite{Falque2025},
in the greybody-saturation limit $\Gamma \equiv 1$. The abscissa is
$\omega/\kappa$ with the dispersive correction \emph{not} folded in,
so the plotted crossing of the dashed $G = 1/2$ line sits at
$\ln(3)/2\pi = \input{tables/lkb_gain_threshold.tex}\unskip$; carrying
$\kappa_{\rm eff}$ moves it to
$\input{tables/lkb_gain_threshold_absolute.tex}\unskip$ in these units.
At $G = 1/2$ a single shot with 100 probe photons per mode reaches
$5\sigma$. The shading marks the upper bound
$\omega \lesssim 0.30\,\kappa$ of Eq.~\eqref{eq:window}; it is drawn
down to $\omega = 0$ because this Letter does not fix the window's
lower edge.}
\label{fig:gain}
\end{figure}

For a shot-noise-limited readout,
$\mathrm{SNR} = \sqrt{N_{\rm shots} N_{\rm probe}}\;G(\omega)$, so
$G = 1/2$ with $N_{\rm probe} = 100$ probe photons per mode is a
single-shot $5\sigma$ detection. At
$\omega = 0.1\,\kappa_{\rm eff}$ the same 100 photons give
$\mathrm{SNR} = \input{tables/lkb_snr_at_0p1.tex}\unskip$. What matters is
the scaling rather than any single figure: a spontaneous correlation
signal at the $5\times 10^{-5}$ level needs of order $10^9$
repetitions to resolve~\cite{Grisins2016,Jacquet2022}, and a
stimulated measurement trades that repetition count for probe
power.

%% ══════════════════════════════════════════════════════════════════
\section{Noise floor and the falsifiable window}

The polariton fluid is not cold. Its mean-field energy scale
corresponds to
\begin{equation}
T_{\rm noise} = \frac{m^* c_s^2}{k_B} = \input{tables/lkb_T_noise_K.tex}\unskip\,\si{\kelvin},
\end{equation}
an order of magnitude above $T_H$: in a spontaneous measurement the
Hawking occupation would have to be dug out of a background ten
times larger.

The stimulated measurement does not face that ratio, and the reason
is spectral and phase structure rather than magnitude. The
driven-dissipative background is broadband and phase-incoherent
with the probe, whereas Eq.~\eqref{eq:gain} describes a response at
the probe frequency, phase-locked to it. A narrowband
phase-referenced readout therefore rejects the background by the
ratio of its bandwidth to the detection bandwidth, and this is not
a modification of the Paris-LKB apparatus but a description of it:
the existing coherent probe spectroscopy already modulates the
probe at \SI{5}{\mega\hertz} and detects in a \SI{300}{\hertz}
bandwidth around that modulation~\cite{Falque2025}. What survives
in that band is the probe's own shot noise, which is what the SNR
above assumes; the background enters only through condensate
amplitude fluctuations, spectrally flat across the
window~\cite{Amelio2020} and so unable to mimic the Planckian shape
of Eq.~\eqref{eq:gain}.

That shape is what makes the prediction falsifiable. We give the
window
\begin{equation}
\boxed{\;\omega/\kappa \lesssim 0.30\;}
\label{eq:window}
\end{equation}
at the smooth horizon. The upper edge is derived: above it $D$ is
no longer small against the frequencies being probed, the
$\mathcal{O}(\omega^2/\kappa^2)$ greybody corrections that
Eq.~\eqref{eq:gain} neglects stop being negligible, and
$G$ has fallen to $\input{tables/lkb_gain_at_0p3.tex}\unskip$, near the point where
the returned signal is no longer a large fraction of the probe. The
lower edge is set by the apparatus, not by the theory: $G$ grows
without bound as $\omega \to 0$, and what stops the window there is
the frequency resolution of the probe scan and the finite spatial
extent of the fluid. We do not fix it here, because fixing it
requires the resolution figures for the tailored-profile
configuration rather than for the one already
published.

Inside the window the prediction is over-determined, which is what
gives it teeth. The three measured quantities $(c_s, \xi, \kappa)$
enter $G(\omega)$ only through the single combination
$\kappa_{\rm eff}$, so a measured gain profile is a one-parameter
fit to a three-parameter set already fixed independently.
Disagreement in the \emph{shape} of $G(\omega)$, as opposed to its
overall scale, cannot be absorbed into the greybody factor, and
falsifies the framework.

%% ══════════════════════════════════════════════════════════════════
\section{What would settle it}

The horizon is built and its steepness is measured, so the analog
Hawking temperature at the Paris-LKB platform is not an open
theoretical question: Eq.~\eqref{eq:T_H} fixes it at
$\input{tables/lkb_T_H_smooth_mK.tex}\unskip\,\si{\milli\kelvin}$ from a
quantity already in print. What is open is whether the leading-order
spectrum is what the device emits, and that is decided by the
mirror stack rather than by the optics that write the horizon. At
$\Gamma_{\rm pol}/\kappa = \input{tables/lkb_ratio_standard.tex}\unskip$ the
as-built cavity is outside the expansion; at
$\tau_{\rm cav} \gtrsim \input{tables/lkb_tau_required_ps.tex}\unskip\,\si{\pico\second}$
it is inside, and the same horizon then emits a spectrum whose
half-gain frequency, Eq.~\eqref{eq:threshold}, is a pure number that
no device parameter can move.

Two measurements would convert this from a prediction into a test,
and both are within the existing apparatus. The first is the
in-situ polariton decay rate $\Gamma_{\rm pol}$ and cavity lifetime
$\tau_{\rm cav}$ at the tailored-profile configuration rather than
at the homogeneous fluid, since Eq.~\eqref{eq:weak_dissipation} is
evaluated there and the published
$\hbar\gamma \approx \SI{80}{\micro\electronvolt}$ is not. The
second is the probe-scan frequency resolution and the fluid's usable
extent at that configuration, which is what would let the lower edge
of Eq.~\eqref{eq:window} be fixed by the experiment instead of left
open. Given a longer-lifetime cavity generation and its $\kappa$,
the band edges follow from the same expressions used here, and we
would recompute and circulate them before rather than after a
measurement.

\begin{acknowledgments}
Formal-verification layer built on Lean~4 (v\leantoolchain) with
Mathlib at commit \texttt{\mathlibrev}. The polariton bounds cited in
\S III are \texttt{attenuation\_ge\_one},
\texttt{attenuation\_mono\_Gamma},
\texttt{attenuation\_eq\_one\_at\_zero\_decay} and
\texttt{polariton\_tier1\_fails\_tmds} in \texttt{PolaritonTier1.lean},
closing over \bundleTheorems{} theorems with \axiomcount{}
project-local axioms and \sorrycount{} \texttt{sorry}.
\end{acknowledgments}

\section*{Methods and tools disclosure}
The formal-verification layer used LLM-based agents (Anthropic
Claude-family models) for Lean~4 proof drafting, and the Aristotle
automated prover (Harmonic) for a subset of the underlying library;
every proof is certified by the Lean~4 kernel via \texttt{lake
build} on the public repository, independently of any AI tool.
Manuscript prose was AI-assisted and human-reviewed. Every
device-derived number here is generated from
\texttt{src/core/constants.py} and \texttt{src/core/formulas.py},
not typed into the text.

\begin{thebibliography}{99}

\bibitem{Steinhauer2016}
J.~Steinhauer, Nat.\ Phys.\ \textbf{12}, 959 (2016).

\bibitem{deNova2019}
J.~R.~M.~de~Nova, K.~Golubkov, V.~I.~Kolobov, and J.~Steinhauer,
Nature \textbf{569}, 688 (2019).

\bibitem{Unruh1981}
W.~G.~Unruh, Phys.\ Rev.\ Lett.\ \textbf{46}, 1351 (1981).

\bibitem{Visser1998}
M.~Visser, Class.\ Quantum Grav.\ \textbf{15}, 1767 (1998)
[arXiv:gr-qc/9712010].

\bibitem{Barcelo2011}
C.~Barcel\'o, S.~Liberati, and M.~Visser,
Living Rev.\ Relativ.\ \textbf{8}, 12 (2005)
[arXiv:gr-qc/0505065].

\bibitem{Gerace2012}
D.~Gerace and I.~Carusotto,
Phys.\ Rev.\ B \textbf{86}, 144505 (2012).

\bibitem{Nguyen2015}
H.~S.~Nguyen \emph{et al.},
Phys.\ Rev.\ Lett.\ \textbf{114}, 036402 (2015).

\bibitem{Falque2025}
K.~Falque, A.~Delhom, Q.~Glorieux, E.~Giacobino, A.~Bramati,
and M.~J.~Jacquet,
Phys.\ Rev.\ Lett.\ \textbf{135}, 023401 (2025)
[arXiv:2311.01392].

\bibitem{Estrecho2021}
E.~Estrecho \emph{et al.},
Phys.\ Rev.\ Lett.\ \textbf{126}, 075301 (2021).

\bibitem{Stepanov2019}
P.~Stepanov \emph{et al.},
Nat.\ Commun.\ \textbf{10}, 3869 (2019).

\bibitem{Claude2023}
F.~Claude \emph{et al.},
Phys.\ Rev.\ B \textbf{107}, 174507 (2023).

\bibitem{Grisins2016}
P.~Grisins \emph{et al.},
Phys.\ Rev.\ B \textbf{94}, 144518 (2016).

\bibitem{Jacquet2022}
M.~Jacquet \emph{et al.},
Eur.\ Phys.\ J.\ D \textbf{76}, 152 (2022).

\bibitem{Finazzi2012}
S.~Finazzi and R.~Parentani,
Phys.\ Rev.\ D \textbf{85}, 124027 (2012).

\bibitem{CorleyJacobson1996}
S.~Corley and T.~Jacobson,
Phys.\ Rev.\ D \textbf{54}, 1568 (1996).

\bibitem{Coutant2014}
A.~Coutant and R.~Parentani,
Phys.\ Rev.\ D \textbf{90}, 121501(R) (2014).

\bibitem{Jacquet2023QNM}
M.~Jacquet \emph{et al.},
Phys.\ Rev.\ Lett.\ \textbf{130}, 111501 (2023).

\bibitem{Amelio2020}
I.~Amelio and I.~Carusotto,
Phys.\ Rev.\ B \textbf{101}, 064505 (2020).

\bibitem{WangKimBZHe2026PennTMDPolariton}
Z.~Wang, B.~Kim, B.~Zhen, and L.~He,
Phys.\ Rev.\ Lett.\ \textbf{136}, 146901 (2026)
[arXiv:2411.16635].

\end{thebibliography}

\end{document}
