# GENERATED by scripts/slotctl.py config render.
# source-digest = {{SOURCE_DIGEST}}
# This file is a template. The rendered config.toml is gitignored because its
# absolute paths and optional client-auth mode are checkout-local. Trusted-local
# mode needs no credential; bearer mode references LEAN_SLOT_CODEX_TOKEN.

[agents]
# Installed Codex CLI 0.144.x uses the legacy key; current releases retain it
# as an alias for max_concurrent_threads_per_session.
max_threads = 3

[agents.lean_wt1_worker]
description = "Lean proof worker restricted to leased public slot wt1."
config_file = "{{AGENT_CONFIG_DIR}}/lean-wt1-worker.toml"

[agents.lean_wt2_worker]
description = "Lean proof worker restricted to leased public slot wt2."
config_file = "{{AGENT_CONFIG_DIR}}/lean-wt2-worker.toml"

[agents.lean_wt3_worker]
description = "Lean proof worker restricted to leased public slot wt3."
config_file = "{{AGENT_CONFIG_DIR}}/lean-wt3-worker.toml"

[mcp_servers.skeft_wt1]
url = "http://127.0.0.1:8761/mcp?client=codex"
{{MCP_CLIENT_AUTH}}
required = false
tool_timeout_sec = 120
disabled_tools = ["lean_build"]

[mcp_servers.skeft_wt2]
url = "http://127.0.0.1:8762/mcp?client=codex"
{{MCP_CLIENT_AUTH}}
required = false
tool_timeout_sec = 120
disabled_tools = ["lean_build"]

[mcp_servers.skeft_wt3]
url = "http://127.0.0.1:8763/mcp?client=codex"
{{MCP_CLIENT_AUTH}}
required = false
tool_timeout_sec = 120
disabled_tools = ["lean_build"]
